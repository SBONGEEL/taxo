# تسليم — TAXO

**آخرُ تحديث: ٢٠٢٦-٠٩-١١.** هذا الملفُّ يُقرأ أولاً في كلِّ جلسةٍ جديدة. **ولا
سرَّ فيه ولا مسارَ إلى سرّ.**

المرجعُ الكامل في `SPEC.md` (المواصفة والقرارات) و`CLAUDE.md` (كيف يُشتغَل على
هذا المستودع، والدروسُ المستخلَصة). هذا ملخَّصُ حالٍ لا بديلٌ عنهما.

---

## ٠-ق. أين نقف — ٢٠٢٦-٠٩-١١ (**بندا التسليم غيرُ الـPlay أُغلقا، وحارسُ الحزم كان يقيس جهازي**)

    الشجرة   `9d4557c` = `origin/master`  ·  نظيفةٌ قبل الإيداع وبعده
    الحرّاس  الساكنون كلُّهم خضر  ·  `check:docs` أخضر
    الإنتاج  الحزمُ على صفحة التنزيل **مقيسةٌ بايتاً** ومتّسقةٌ مع بيانها

### ١) البند الرابع — **بابُ الرفع بُني** (`scripts/deploy-here.sh`)

**ثلاثُ قواعدَ كانت تُتذكَّر صارت شرطاً يُطبَّق**: WSL تُرفض بعلّتها ولينكسُ
الحقيقيُّ يمرّ · `core.fileMode` بالبيئة لا بتعديل إعداد · الشجرةُ تُقاس بعد
الضبط لا قبله · والإطلاقُ عمليةٌ مستقلّةٌ **بملفِّ مشغِّل لا سطرٍ مقتبَس**.

**ومقيسٌ في موضعه**: `git status --porcelain` من Git Bash على شجرة WSL —
**بلا ضبطٍ ٥٣ سطراً، ومعه سطران**. ومسارا الرفض قِيسا (خروجٌ `1` في
الاثنين). **ولم يُقَس رفعٌ كاملٌ خلاله بعد** — مكتوبٌ في رأس الملفّ.

### ٢) البند الخامس — **قِيس، فكشف حارساً يقيس جهازي لا الإنتاج**

    صفحةُ التنزيل    v0.2.4 · إيداع 346efdbb · 2026-09-10 · رمز 514
    بيانُ هذا الجهاز  v0.2.0 · إيداع 41656f2e · 2026-08-28 · رمز 392
    وحُمِّلت الحزمتان من الحافّة: 7,641,837 و7,864,877 بايتاً
    وبصمتاهما dff07a68ee… و061e7e4656… = **ما يصرّح به بيانُ الصفحة**

**فالإنتاجُ سليمٌ والحارسُ كان يصيح.** والعلّةُ أن مرجعَه بيانُ هذا الجهاز،
و`landing/downloads` مُهمَلٌ في git **فتبلى نسخةُ المطوّر وحدَها**. فُصل
المرجعان، وجُرِّب الإصلاحُ بحافّةٍ مزيَّفةٍ فسقط الحارسُ كما يجب. التفصيلُ
في `GUARDS.md`.

### ٣) وثمانيةُ نطاقاتٍ قِيست حيّةً — **جوابُها لا سجلُّها**

    api.tajora.ly          404   ← لا مسارَ جذر؛ و/health = 200 ok
    app · driver · admin   200
    dev-api · dev-app · dev-driver · dev-admin    530 · error code: 1033

**والأربعةُ التجريبيةُ نفقُ هذا الجهاز وهو مطفأ** — فالجوابُ متوقَّع، **وهو
بعينه ما لا يمسكه `check:hosts`** (يقيس السجلَّ لا الجواب، ويقول ذلك).
**والحارسُ يبقى لجولةٍ مستقلّة** كما كُتب حين رُصد، **ولم يُبنَ اليوم بقصد**.

### ⚠ ما ينتظر المالك — **غيرَ Play**

1. **جولةُ هاتفٍ لتطبيق الكبتن عند API 36** — رُفع الهدفُ ولم يُقَس على جهاز
   قطّ. وأندرويد 16 يغيّر سلوكَ الخدمات الأمامية والموقع الخلفيّ ونوافذ
   التنبيه، **والكبتنُ يستعمل الثلاثة**.
2. **ثلاثُ حاوياتٍ موقوفة** — `taxo-worker` (يمسّ كنسَ الاشتراكات لا التسجيل)
   و`taxo-minio` و`taxo-postgres` (على شبكةِ مشروعٍ قديم لا تبلغها الخلفية).
   **قرارُ تشغيلٍ لا إصلاحُ عطب.**
3. **دقّةُ حدِّ النطاق** — الصندوقُ يبتلع ما جاور الحدَّ من سوريةَ وفلسطينَ
   والسعودية. **والمضلَّعُ ميزةٌ لها سطحُ لوحةٍ وقرارُ مالك**، لا إصلاحُ عطب.

### وأولُ خمسةِ أعمالٍ للجلسة القادمة

1. **فيديو YouTube واحدٌ (≤٣٠ ثانية)** يُغلق بياني الموقع الخلفيّ والخدمات
   الأمامية، ومعه «لا» للمعرِّف الإعلانيّ — **ثم ينشر المسارُ المغلق**.
2. **حارسُ «نفقٌ حلَّ اسمُه ولم يتّصل»** — عمودٌ ثانٍ في `check:hosts` يقرأ
   الجوابَ لا السجلّ، **وسقوطُ الجميع يُقرأ «لا شبكة» لا «النطاقاتُ زالت»**.
3. **رفعٌ كاملٌ خلال `deploy-here.sh`** — البابُ بُني ومساراتُ رفضِه وحدَها
   قِيست.
4. **جولةُ هاتفٍ للكبتن عند 36** قبل أن يصل مختبِرين.
5. **`admin-panel` عند SDK 35** — بندٌ مكتوبٌ بعلّته (لا يُنشر على المتجر)،
   يُراجَع حين يُنشر لا قبله.

---

## ٠-ص. أين نقف — ٢٠٢٦-٠٩-١٠ بعد الرفع (**حدُّ النطاق منشورٌ ومقيسٌ على الإنتاج**)

    الإنتاج  be381f16 = إيداعُ CI  ·  الترحيلة 0075 = رأسُ الشجرة  ·  ١١ حاويةً تعمل
    البوّابات الستُّ خضراءُ كلُّها  ·  CI 34431249193  ·  صفرُ خطأٍ في السجلّ ثلاثَ دقائق
    النسخة   `/d/taxo-backups/20260910T060236Z`  ·  20260910T060236Z (UTC)  ·  16M
    المتجر   اسمُ الإصدار صار **`496 (1.0)`** على حزمةٍ رمزُها 496

### حدُّ النطاق مقيسٌ على الإنتاج — **من داخل الحاوية، ولم يُقَس عبر HTTP**

شُغِّل `service_area` **بالبايتات المنشورة** في حاوية الإنتاج:

    BOX_JO = MarketBox(29.18, 33.38, 34.95, 39.3)
    amman → irbid              PASSED
    aqaba → irbid  (~٣٥٠ كم)   PASSED        ← المشروعُ لا يُمنع
    amman → tripoli            REJECTED 422 outside_service_area
    tripoli → amman            REJECTED 422 outside_service_area
    amman → irbid + محطّةٌ في طرابلس  REJECTED 422
    النصّ: «هذه الوجهة خارج نطاق خدمتنا — اختر مكاناً داخل الأردن.»
    والبابان يناديانه: routers/rides.py:91 · services/rides.py:396

**وحدُّ هذا القياس مكتوبٌ لا مسكوتٌ عنه**: هو يقيس **الشيفرةَ المنشورة**، **ولا
يقيس مسارَ HTTP حيّاً** — لأن `POST /rides/estimate` خلف `RiderUser`، **ورمزُ
راكبٍ على الإنتاج شيءٌ لا يملكه إلا المالك**: كلمةُ مرورٍ لا تُكتب، أو قراءةٌ على
قاعدة الإنتاج تُعرض قبل تنفيذها. **فيبقى «تُرفض على الشاشة» غيرَ مقيس** حتى يأذن.

### وثلاثةُ أعطابٍ في **مسار الرفع نفسِه** وقعت مقيسةً — ولا واحدَ منها في المشروع

1. **`ssh.exe` الخاصُّ بويندوز يموت في منتصف تدفّقٍ ثنائيٍّ حين يُنادى من WSL.**
   وقعت ثلاثَ مرّاتٍ بثلاثة وجوه: تعلُّقٌ سبعَ دقائقَ بلا مهلةٍ تقطعه (`setsid`
   بلا طرفيّة)؛ و`bundle.tar` **بزيادةِ 1.84 ميغا** يُفتح فيسقط عند رأسٍ تالفٍ
   («Skipping to next header»)؛ وتوقّفٌ عند **1,912,832 بايت** **ولا عمليةَ ssh
   قائمة** أصلاً. **وعُزل المتغيّر**: وُجِّهت النسخةُ إلى قرص WSL نفسِه (ext4)
   فسقطت كذلك — **فليست 9p على `/mnt/d`، بل الجسر**.
   **والعلاجُ: يُشغَّل `deploy.sh` من Git Bash** حيث `ssh.exe` أصليٌّ بلا جسر —
   تمَّ النقلُ نفسُه في دقيقتين.
2. **Git Bash يقرأ شجرةَ WSL «٥١ تغييراً» وهي نظيفة** — **بتُّ التنفيذ وحدَه**:
   `git -c core.fileMode=false status` يعطي **صفراً**. **والبوّابةُ الأولى كانت
   ستسقط بقراءةٍ كاذبة.** ويُمرَّر بالبيئة لا بتعديلِ إعداد:
   `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=core.fileMode GIT_CONFIG_VALUE_0=false`.
3. **حارسُ المهامّ يقتل الرفعَ عند ضغط الذاكرة** — وقعت في **منتصف البوّابة
   الخامسة** (متاحٌ ٥٩٧ ميغا من ١١,٩٩٢). **وتُقرأ الحالُ نصفَ مطبَّقةٍ لا رجوعاً**:
   الخادمُ سحب `be381f16`، **والحاوياتُ لم تُعَد** (Up 5 hours) فالخدمةُ على
   البايتات القديمة. **ونجا بناءُ الواجهات لأنه `setsid nohup` على الخادم.**
   **والعلاجُ: يُطلق الرفعُ عمليةَ ويندوزَ مستقلّةً** خارج متتبِّع المهامّ
   (`Start-Process bash.exe <مشغّل>`) — **ووسائطُ `Start-Process` تُعاد اقتباسُها
   فتكسر السطر**، فالمشغّلُ ملفٌّ لا سطرٌ مقتبَس.

**والإعادةُ بعد كلِّ سقطةٍ كانت تُقاس أوّلاً** كما يأمر السكربت: `git rev-parse
HEAD` على الخادم قبل أيِّ فعل. **ولم يُمسّ الإنتاجُ في أيٍّ من الثلاث.**

### ⚠ ما ينتظر المالك — **أربعة**

1. **بنودُ IARC** — اتفاقيةٌ لا تُوقَّع بالنيابة.
2. **الإقراران** — أمانُ البيانات وتصنيفُ المحتوى.
3. **رقمُ حساب المراجعة** — `+962790000038` بعد تبديل كلمته، **والكلمةُ يكتبها
   بيده** في حقل «تفاصيل تسجيل الدخول».
4. ~~قياسُ حدِّ النطاق حيّاً~~ — **أُغلق ٢٠٢٦-٠٩-١٠**: قاسه المالكُ بنفسه من
   الهاتف على الإنتاج، ودبّوسُ طرابلس أظهر **«هذه الوجهة خارج نطاق خدمتنا —
   اختر مكاناً داخل الأردن.»** وزرُّ الطلب معطَّل وحقلُ السعر فارغ. التفصيلُ
   في `STATE.md`.

### وبندٌ ثالثٌ كُتب اليوم في `STATE.md` — **ثمّ طُبِّق وسقط نصفُه**

**٢١١ ملفاً في نسخة العمل بنهاياتٍ CRLF والفهرسُ LF** — إرثُ حقبة ويندوز.
**طُبِّعت كلُّها** (٣٨٬١٧٩ بايتَ CR أُزيلت)، والباقي `w/crlf` **ثلاثةٌ فقط**
وهي `.ps1` التي `eol=crlf` صحيحٌ لها.

**وصفرُ إيداعٍ نتج**: الفهرسُ كان LF طوال الوقت، وبصمةُ ملفِّ بناءِ الخلفية
`f743ed82b164` **قبل وبعد سواء** — **فالعطبُ في السحب لا في المستودع**،
و`git add --renormalize` لا يجد ما يودعه. **ولم يُصنع إيداعٌ فارغٌ ليطابق
خطّة.** والمجموعةُ بعده: **1433 passed · EXIT=0**.

**ودعوى «الخطرُ قائمٌ في ملفِّ بناءِ الخلفية» سقطت بالقياس**: `docker build
--check` **نظيفٌ على النسختين** — LF (2,473 بايت) وCRLF (2,518 · 45 CR).
**وصفرُ ملفِّ `.sh` كان بين الـ٢١١** أصلاً، وهو الصنفُ الذي تحذّر منه القاعدة.
**فبقي خطرٌ واحدٌ حقيقيّ: قارئُ البايتات** (`check:docs` أمسكه من قبل).

### وأولُ خمسةِ أعمالٍ للجلسة القادمة

1. **مسارُ Google Play** — بطاقةُ الراكب، والاختبارُ المغلق، وما ينتظر يدَ
   المالك. (حدُّ النطاق أُغلق: مبنيٌّ ومنشورٌ ومقيسٌ حيّاً.)
2. **حين يصل رقمُ المراجعة**: ثلاثةُ حقولِ «تفاصيل تسجيل الدخول» → الجمهورُ
   المستهدف → الخطوةُ الخامسة، **واعرض الإقرارَ ولا تضغطه**.
3. **اللوحيّان**: يُقاس أيمنع Play الإرسالَ بدونهما فعلاً — **وهو ما لم يُقس**.
4. **بابٌ واحدٌ لتشغيل الرفع على هذا الجهاز** — العطبُ ١ و٢ و٣ أعلاه قواعدُ
   تُتذكَّر اليوم، **والقاعدةُ التي تُتذكَّر تُنسى**. مشغّلٌ في `scripts/`
   يضبط الثلاثة ويرفض ما عداها.
5. **`check:served` و`check-apk`** على الإنتاج — **الأعمدةُ الأربعةُ للثلاثة لم
   تُقس بعد الرفع**، والبابُ يقول ذلك عن نفسه.

## ٠-س. أين نقف — ٢٠٢٦-٠٩-١٠ (**حدُّ النطاق مبنيٌّ مودَعٌ غيرُ منشور، وبطاقةُ المتجر بسبع لقطات**)

    الشجرة   294f665  ·  نظيفةٌ (٠)  ·  المجموعة 1433 passed · EXIT=0  ·  الحرّاسُ كلُّهم خضر
    الإنتاج  f5cb8bfe — **ولا يحمل حدَّ النطاق**: البوّاباتُ الستُّ لم تُشغَّل على `294f665`
    المتجر   لقطاتُ الهاتف 7/8 محفوظةً · اللوحيّان فارغان بقرار · **لم يُرسل شيءٌ للمراجعة**

### ١) حدُّ نطاق الخدمة — **مبنيٌّ ومودَعٌ ولم يُنشر**

`backend/app/core/service_area.py` — صندوقٌ **يحوي** البلدَ لا يطابق حدودَه،
ثابتٌ في الشيفرة بقرار المالك (٢٠٢٦-٠٩-١٠)، **ولا رقمَ مسافةٍ فيه إطلاقاً**.
وبابٌ واحدٌ يناديه `request_ride` و`estimate_ride` معاً، والطرفانِ والمحطّاتُ
تُفحص كلُّها.

**والعطبُ الذي وُجد له قِيس على الإنتاج بإذن المالك، قراءةً محضة**: **ثلاثٌ من
ستِّ رحلاتٍ أردنيةٍ هناك أحدُ طرفيها خارج البلد** — فليس عطبَ تطويرٍ وحدَه.

**⚠ والإنتاجُ اليومَ عند `f5cb8bfe` ولا يحمل هذا الحدَّ.** الإيداعُ `294f665`
**لم تُشغَّل عليه البوّاباتُ الستّ**، ولا CI بعده. **فأوّلُ عملٍ في الجلسة
القادمة هو النشر** — وهو يمسّ الخلفية.

### ٢) بطاقةُ المتجر — سبعٌ محفوظة، والأربعُ الكبتنيّةُ لا مكانَ لها

**البطاقةُ لتطبيق الراكب `ly.tajora.rider` وحدَه** — فلقطاتُ الكبتن الأربعُ
(`captain-home` · `captain-earnings` · `captain-subscription` ·
`captain-documents`) **لا تُرفع هنا**، وهي على الموقع لا في المتجر.

المرفوعُ سبعٌ: `rider-home` · `rider-request-map` · `rider-captain-accepted` ·
`rider-tracking` · `rider-rate` · `rider-wallet` · `rider-women` — والعدّاد
`7 / 8`، **حُفظت ولم تُرسل للمراجعة** («ليس الآن» على سؤال الانتقال).

**وحقلا اللوحيِّ ٧″ و١٠″ فارغان بقرار المالك** (٢٠٢٦-٠٩-١٠): لا محاكيَ
يُصنع ولا صورةَ تُولَّد — **يُرفع بلا لوحيٍّ ويُقرأ ما يقوله المتجر**.

### ٣) حكمُ المتجر على المقاس — **بنصّه، ولا يُقدَّر ثانيةً**

> «يجب أن تكون لقطات الشاشة بتنسيق PNG أو JPEG، وبحجم لا يزيد عن 8 ميغابايت،
> **وبنسبة عرض إلى ارتفاع تبلغ 16:9 أو 9:16**، بحيث تتراوح أبعاد كل جانب بين
> 320 بكسل و3,840 بكسل.»

**فلا «حتى 2:1»** كما ظُنّ. و`1080×2308` **رُفض بالسكوت**: لا مادّةَ أُضيفت
ولا رسالةَ ظهرت — **والسكوتُ هنا رفضٌ لا قبول**، وأمسكه مراقبُ DOM لا العين.

**والعلاجُ هامشٌ لا قصٌّ** (قرارُ المالك): قُصَّ شريطُ حالة النظام (٩٢ بكسل
مقيسةً من الجهاز: `DisplayCutout insets=Rect(0, 92 - 0, 0)`)، ثمّ **وُسِّع
العرضُ** من `1080` إلى `1296` — **ولم يُنقص الارتفاع**، فقصُّ ٣٨٨ بكسلاً كان
يأكل شريطَ التنقّل أو رأسَ الشاشة. الهامشُ `108` بكسلاً كلَّ جانبٍ **بلونٍ
مأخوذٍ من بكسل الصورة نفسِها لا مخترع**. وأجاب المتجر: `9∶16` · `1296x2304px`.

الملفّاتُ الجاهزة: مجلَّد `Downloads/taxo-store-shots/` على ويندوز (مقصوصةٌ
`1080×2308`)، والمهمَّشةُ في مجلَّد الجلسة `scratchpad/store/`.

### ⚠ ما ينتظر المالك — **أربعة**

1. **بنودُ IARC** — اتفاقيةٌ لا تُوقَّع بالنيابة.
2. **الإقراران** — أمانُ البيانات وتصنيفُ المحتوى.
3. **رقمُ حساب المراجعة** — `+962790000038` بعد تبديل كلمته.
4. **إذنُ النشر** لإيداع `294f665` (حدُّ النطاق يمسّ الخلفية).

### وأولُ خمسةِ أعمالٍ للجلسة القادمة

1. **البوّاباتُ الستُّ على `294f665`** — ولا رمزَ لقراءة CI في البيئة:
   يُؤخذ من `gh auth token` على ويندوز (مُصادَقٌ بصلاحية `repo`)، ويُمرَّر
   `GITHUB_TAXO_TOKEN` للأمر. **ولا يُطبع ولا يُكتب في ملفّ.**
2. **اسمُ الإصدار «493» على حزمةٍ رمزُها 496** — **ولم يُقس بعدُ** أين
   يُصحَّح: أتعديلُ اسمِ إصدارٍ قائمٍ في مسار الاختبار الداخليّ أم إنشاءُ
   إصدارٍ جديد. يُقاس من الكونسول أوّلاً.
3. **حين يصل الرقم**: ثلاثةُ حقولِ «تفاصيل تسجيل الدخول» → الجمهورُ
   المستهدف → الخطوةُ الخامسة، **واعرض الإقرارَ ولا تضغطه**.
4. **اللوحيّان**: يُقاس أيمنع Play الإرسالَ بدونهما فعلاً — وهو ما لم يُقس.
5. **المجموعةُ تُشغَّل بـ`setsid nohup` منفصلةً**، وتُراقَب بنداءاتٍ قصيرةٍ
   في المقدّمة — فحارسُ المهامّ يقتل ما يتتبّعه عند ضغط الذاكرة (وقع مرّتين).

### وما مُسَّ على قاعدة **التطوير** وحدَها — يُقرأ قبل أن يُقاس شيءٌ عليها

- **الرحلاتُ الملوَّثةُ صُحّحت بالمعنى لا بالنصّ، وبلا حذفِ صفّ**: ١٩ انطلاقاً
  اسمُه «الجمهورية» صار «دوار الداخلية، عمّان» نقطةً وعنواناً؛ و٤٣ بلا عنوان
  صحّت نقطتُها **وبقي عنوانُها فارغاً كما كان**؛ و٢٢ وجهةً خارجةً عادت
  عمّانية. **رحلاتُ `JO` خارج صندوق الأردن: صفر. وصفوفُ `LY` لم تُمسّ.**
  والعددُ قبل وبعد: `153 = 153`.
- **وسابقاً**: عمولةُ الاشتراكات المبذورة `0.00 → 2.00`، ومحفظةُ الراكبة
  شُحنت `25.000 د.أ` بمسار `topups.create_confirmed` نفسِه.

### وبندٌ مفتوحٌ بحدِّه المكتوب

**صندوقُ النطاق لا يمسك نقطةً خلف الحدود مباشرة** — ما جاور الحدَّ من سوريةَ
أو فلسطينَ أو السعوديةِ داخلُ الصندوق. **والدقّةُ مضلَّعٌ يُخزَّن ويُحرَّر،
وهي ميزةٌ لها سطحُ لوحةٍ وقرارُ مالك، لا إصلاحُ عطب.**

## ٠-ن. أين نقف — ٢٠٢٦-٠٩-٠٩ ليلاً (**اللقطاتُ الإحدى عشرةَ حقيقيةٌ، وأربعةُ أعطابٍ مقيسةٌ مُصلَحة**)

    اللقطات  ١١ موضعاً · ١١ ملفّاً · صفرُ «لم تُلتقط» · `check:shots` ✓
    الأجهزة  Note 20 (RF8N80XEHBD) · حزمتا `.test` للراكب والكبتن على dev
    التطوير  dev-api · dev-app · dev-driver = 200 · الأعمدةُ الثلاثةُ متطابقةٌ للتطبيقين
    Play     كما كانت — أمانُ البيانات ١·٢·٣·٤ ✓ · بياناتُ المحتوى ٩ ← ٤

**وما تغيّر اليوم**: كلُّ لقطةٍ في `site/index.html` صارت صورةً من تطبيقٍ
حيٍّ على هاتفٍ حقيقيّ (1080×2400)، **أردنيةً بحتة** — عمّان و`د.أ` و`+962`،
وصفرُ ذكرٍ لليبيا في سوق `JO`. وأربعةُ أعطابٍ قِيست وأُصلحت:

1. **«وجهات أخيرة» تقفز فوق حدِّ البلد** الذي يلتزمه بابُ البحث
   (`customer-app/src/lib/places.tsx`) — الشكلُ ٢٣.
2. **«صفر عمولة ما دام اشتراكك سارياً»** مشروطةٌ بمجموع الشهر لا بالنسبة،
   فتقع في كلِّ أوّلِ شهر (`driver-app/src/screens/Wallet.tsx`) — الشكلُ ٢٤.
3. **«كل المستندات مقبولة» وصفرُ مستنداتٍ مرفوعة**
   (`driver-app/src/screens/Account.tsx` + `lib/documents.ts` بيتاً واحداً
   للقائمة) — الشكلُ ٢٥.
4. **`taxo-driver-app` كانت تُقلع بملفِّ compose واحد**، فتخدم خادمَ Vite
   بعنوان `127.0.0.1:8001` — و`127.0.0.1` من الهاتف هو الهاتف: `ERR
   network_unreachable`. أُعيدت **بالملفَّين معاً** فصارت تخدم `dist`.

**وحارسان صُحِّحا ببلاغَيهما الكاذبين**: `check-commission-text` كان يقرأ
**سطورَ تعليقٍ متعدّدةَ الأسطر** نصّاً معروضاً؛ و`check-apk-target` كان يقيس
**كلَّ حزمةٍ بقناة تطبيق الراكب**، فأعلن حزمةَ الكبتن الصحيحةَ كاذبة.

### ⚠ ما ينتظر المالك — **وأوّلُ ثلاثةٍ كما كانت**

1. **رقمُ حساب المراجعة** — `+962790000038` بعد تبديل كلمته.
2. **بنودُ IARC** — اتفاقيةٌ لا تُوقَّع بالنيابة.
3. **الإقراران** — أمانُ البيانات وتصنيفُ المحتوى.
4. **جديدٌ اليوم — كلمةُ مرور الكبتن في التطبيق التجريبيّ على الهاتف**:
   شاشتا الكبتن فُتحتا برمز **التسليم بين التطبيقين** (`/auth/handoff`)،
   وهو مسارٌ مبنيٌّ لكنّه **يُصدَر من الخلفية لا من يد مستخدم**. من أراد
   جولةً يدويةً على الهاتف يكتب الكلمةَ بنفسه — **ولم أكتبها ولم أطبعها**.

### وأولُ خمسةِ أعمالٍ للجلسة القادمة

1. **البوّاباتُ الستّ** لنشر الموقع باللقطات الجديدة — **الخادمُ يبني الموقعَ
   بنفسه** ومخرَجُ `landing/` مُهمَلٌ في git، فالمنشورُ هو المصدر.
2. **بطاقةُ المتجر**: اللقطاتُ الإحدى عشرةَ صالحةٌ لها، **وصحّح اسمَ
   الإصدار** («493» على حزمةٍ رمزُها 496).
3. **حين يصل الرقم**: ثلاثةُ حقولِ «تفاصيل تسجيل الدخول» → الجمهورُ
   المستهدف → الخطوةُ الخامسة، **واعرض الإقرارَ ولا تضغطه**.
4. **بندُ نطاق الخدمة** في `STATE.md` — الخلفيةُ تقبل رحلةً بلدُها `JO`
   ووجهتُها طرابلس. **يمسّ التسعير، فلا يُبنى بلا إذن.**
5. **أعد إذنَ الموقع** لحزمة `ly.tajora.rider.test` إن أُريدت جولةٌ يدويةٌ
   بخريطةٍ تتبع الجهاز — نُزع عمداً ليقع مركزُ عمّان في اللقطات
   (`pm grant ly.tajora.rider.test android.permission.ACCESS_FINE_LOCATION`).

### وما مُسَّ على قاعدة **التطوير** وحدَها — يُقرأ قبل أن يُقاس شيءٌ عليها

- **عناوينُ الرحلات الليبيةُ في سوق `JO` أُعيدت عمّانيةً** (٣٩ وجهةً و١٤
  انطلاقاً و١٢ وجهةَ «جنزور») — **بلا حذفِ صفٍّ واحد**، والصفُّ الوحيدُ
  الذي بلدُه `LY` بقي ليبيّاً صادقاً.
- **`commission_percent_at_purchase` للاشتراكات المبذورة** كان `0.00`
  فصار `2.00` ليوافق `commission_settings` الحيّة — **ولذلك كانت الشاشةُ
  تقول «عمولة TAXO 0%» وهي صادقةٌ عن بذرةٍ بالية، لا عطبٌ في الواجهة**.
- **محفظةُ الراكبة شُحنت 25.000 د.أ** بمسار `topups.create_confirmed` نفسِه
  (نقطةُ كاشٍ معتمدة) لا بكتابةٍ في الدفتر. **والتحويلُ رفضته الخلفيةُ لأنه
  غيرُ مفعَّلٍ للأردن — ولم يُشعَل لأجل لقطة.**

### وثلاثةُ بنودٍ مفتوحةٍ لا تمنع شيئاً

- **حارسُ «نفقٌ حلَّ اسمُه ولم يتّصل»** لم يُبنَ — مضيفٌ يجيب **530** بجسم
  `error code: 1033` **نفقٌ غيرُ متّصل**، و`check:hosts` يقيس السجلَّ لا
  الجواب **ويقول ذلك**. عمودٌ ثانٍ في جولةٍ مستقلّة.
- **`taxo-worker` موقوف** — لا يمسّ التسجيل، ويمسّ كنسَ الاشتراكات.
- **`taxo-minio` و`taxo-postgres` موقوفتان** — قِيس أنهما على شبكة `taxo-net`
  من مشروع `taxo` القديم، **والخلفيةُ على `taxo-app_default` فلا تبلغهما**.

---

## ٠-ك. أين نقف — ٢٠٢٦-٠٩-٠٦ (**v2 منشورةٌ حيّة، والعرضُ موقوفٌ بقياسه**)

    الإنتاج   2c534b5c  ·  الترحيلة 0074  ·  الوثائقُ الأربعُ v2 **منشورة**
    التدقيق   4 قيودِ activate بختمٍ واحد 16:20:19 — **معاملةٌ واحدةٌ مقيسة**
    v1        **مسحوبةٌ لا محذوفة** · published_at محفوظٌ 2026-09-05 12:34:54

**والبابُ الحيُّ يخدم v2 للتطبيقين والموقع** — صفرُ «مسوّدة» في الأربع،
و`/privacy` و`/terms` يجيبان ٢٠٠.

### ⚠ ما ينتظر المالك — **ثلاثة، وأوّلُها قرارُ تسعير**

1. **العرضُ الأول موقوفٌ بنصِّ القياس**: **لا خطّةَ شهريةٌ فعّالةٌ في الأردن
   على الإنتاج.** الشهريّةُ (30 JOD) **مطفأة**، والفعّالةُ الوحيدةُ
   **أسبوعيةٌ 17 JOD**. **وعرضُ «شهرك الأول علينا» لا يُعلَّق على خطّةٍ
   مطفأةٍ ولا على أسبوعيّة.**

   **وعلى الإنتاج عرضان مطفآن أصلاً** — «شهرٌ مجانيٌّ للكبتن الجديد»
   (على الأسبوعية 9 JOD المطفأة) و«الشهر الأول مجاناً» (على الشهرية
   المطفأة). **كلاهما `commission_percent = NULL`** — أي **لا يمنح صفرَ
   عمولة**، ولكلٍّ **سقفُ تنازلٍ 300.000** لم يُذكر في قرار المالك.

   **فالقرارُ له**: أيُّ خطّةٍ تُفعَّل وبأيِّ سعر · وأيُّ العرضين يُستعمل أم
   يُنشأ ثالثٌ · وما مصيرُ سقف الـ300.

2. ~~بيانُ الجهة فارغ~~ — **بندٌ بالٍ، صُحّح 2026-09-07**: قِيس على الإنتاج
   فوُجد **مملوءاً بالقيَم نفسِها التي أمر بها المالك حرفاً**
   (`taxo-joly` · «عمّان، الأردن» · `privacy@tajora.ly`) — **ولم يُكتب حرف**.
   **والاسمُ موحَّدٌ على `taxo-joly` في كلِّ موضعٍ في الشجرة**: صفرُ
   `Taxo-joly` مقيساً.
3. **تدويرُ `GITHUB_TAXO_TOKEN`** — في نصِّ المحادثة، **وبلا انتهاء**.

### وبنودٌ قُرئت فوُجدت مغلقةً — **لا تُعاد**

| البند | الحكم |
|---|---|
| `SkinMapPreview` | **مغلقٌ 2026-09-04** — أثرُ `StrictMode` في التطوير، وصفرُ خطأٍ في بناء الإنتاج مع ٥ علامات |
| `wallet_owner_undecided` | **مغلق** — كلُّ نداءٍ يعلن جانبَه (`side="driver"` · `"rider"`)، ومعه `test_admin_wallet_side.py` |
| `landing/index.html` | **لم يكن بنداً** — غيرُ متتبَّع؛ والدعوى وُلدت من `$( )` مُضِغ في صدفةٍ متداخلة |
| أعطابُ `deploy.sh` الثلاثة | **أُصلحت ومُقيست** (`44befd0`) |

### ⚠ وما لا أستطيعه بنفسي — **قاعدةٌ لا عجز**

**جولةُ المتصفّح على اللوحة تحتاج تسجيلَ دخول**، **وإدخالُ كلمة سرٍّ في حقلٍ
ممنوعٌ عليّ** مهما كان الحساب. **فرسمُ أزرار «افتح الملفّ» ومرشِّحُ حال
الشحنات يبقى «لم يُقَس بصرياً»** حتى يفتح المالكُ الجلسةَ ثمّ أقيس.

### وما بقي عندي مفتوحاً

1. **الهاتف** — `adb devices` فارغة، ينتظر الكبل وضغطةَ «السماح».
2. **قياسُ أهليّة العرض الثلاثيّة** (جديدٌ يراه · مشترِكٌ سابقٌ لا · ملغٍ لا
   يستحقّه ثانية) — **يُقاس على التطوير**، وعلى الإنتاج بعد حسم الخطّة.
3. **جولةُ المتصفّح** أعلاه — بعد دخول المالك.

---

## ٠-ي. أين نقف — ٢٠٢٦-٠٩-٠٦ (**رُفع `2c534b5c`، والوعودُ الثمانيةُ زالت**)

    الإنتاج   2c534b5c  ·  الترحيلة 0074  ·  البوّاباتُ الستُّ خضراء
    النسخة    /mnt/d/taxo-backups/20260906T121809Z  (16M، محقَّقةٌ بالفتح)
    CI        https://github.com/SBONGEEL/taxo/actions/runs/34028793036
    المجموعة  1401 خضراء · صفرُ أحمر

**والمقارنةُ بخطِّ أساسٍ أُثبت قبل الرفع**: وعودٌ مطلقةٌ **8 ⇒ 0** ·
`driver_keeps_per_100` **غائب ⇒ 98** بلا أُسّ · `hero_subtitle` صار نصَّه
الجديد · «والسوق يبدأ من» و«تُجمَّد لحظةَ» حاضران.

### ⚠ ما ينتظر المالك — **واحد**

**v2 للوثائق ليست على الإنتاج**، والأربعُ ما زالت `v1` وفيها «مسوّدة».
**والعلّةُ بنيويةٌ لا نسيان**: الرفعةُ تحمل **الشيفرة** لا **الصفوف**،
ووثائقُ السياسات صفوفٌ في القاعدة (§34). و`seed_policies.py` **idempotent
بالثلاثية** فلا يكتب حيث يوجد صفّ — **بقصدِ ألّا يدفن مراجعةَ إنسان**.

**والنصُّ المصحَّحُ مرفوعٌ في الشيفرة** (صفرُ «لسنا ناقلاً» · Mapbox وWhatsApp
وFirebase بأسمائهم · أربعةُ مطالعَ «تصف هذه الوثيقةُ») — **ينقصه أن يُكتب
صفّاً على الإنتاج**، وبابُه اللوحة: `POST /admin/policies` ثمّ `/{id}/publish`.
**والنشرُ فعلُ إنسانٍ بنصِّ §39٫١٠.** **وكتابةٌ على قاعدة الإنتاج تُعرض قبل
تنفيذها** — فإن أَذِن بسكربتٍ فُعل، وإلا فمن اللوحة بيده.

### وصفُّ العرض — **لم يُنشأ على الإنتاج بعد**

العمودُ وصله الآن (`0074`). و«شهرك الأول علينا» موجودٌ **على التطوير مطفأً**
بنسبةِ عمولةٍ `0.00`. **والإنشاءُ على الإنتاج يُنشأ مطفأً، والإطلاقُ بيد
المالك** (§55٫٥).

### وثلاثةٌ في `deploy.sh` — **جولةٌ لا رفعَ فيها**

افتراضاتُ Git Bash بعد الانتقال إلى WSL: `_pick_ssh` (`/c/…` ⇒ **ارتدادٌ
صامت**) · `TAXO_BACKUP_DEST` (`/d/…`) · **وأمرُ إطلاق البناء لا يعود** مع
عميل ويندوز فيعلّق (**قِيس ٤٢ دقيقة، والبناءُ ناجحٌ منذ العاشرة**).
**مُرَّ عليها بتصريح السكربت المعلَن** (`TAXO_SSH` و`TAXO_BACKUP_DEST`)
**ولم يُعدَّل السكربت** — تعديلُ بابِ الرفع أثناء رفعٍ فعلٌ ثانٍ على السطح
نفسِه. **و`landing/index.html` لم يكن بنداً أصلاً** — قِيس ٢٠٢٦-٠٩-٠٦ فتبيّن
أنه **غيرُ متتبَّع**، والمتتبَّعُ في `landing/` ملفّا إعدادِ نجينكس وحدَهما.
**وأصلُ الدعوى `$( )` مُضِغ في صدفةٍ متداخلة.**

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **v2 على الإنتاج** — بإذنه، من اللوحة أو بسكربتٍ يُعرض قبل تنفيذه.
2. **صفُّ العرض على الإنتاج مطفأً**، ثمّ يُعرض عليه.
3. **إصلاحُ الثلاثة في `deploy.sh`** ومعها `landing/index.html` — بلا رفع.
4. **الهاتف**: `RF8N80XEHBD` حين يقول إنه ضغط «السماح».
5. **تدويرُ `GITHUB_TAXO_TOKEN`** — صار في نصِّ المحادثة، **وبلا انتهاء**.

---

## ٠-ط. أين نقف — ٢٠٢٦-٠٩-٠٦ (**إيداعان جاهزان، وبابان مقفلان بيد المالك**)

    الإنتاج    27d665d  ·  الترحيلة 0072  ·  الشجرةُ المحليّةُ عند cb4080f + عملُ اليوم
    الريموت    f44085e  ← **لم يُدفع شيء**
    الحرّاس    كلُّهم خضر (ومعهم حارسا الموقع بعد إدخالهما)

### ⚠ ما يوقف الرفعَ — **اثنان، كلاهما بيد المالك**

1. **`GITHUB_TAXO_TOKEN` غيرُ موجودٍ في البيئة.** الدفعُ يسقط بـ`Invalid
   username or token`. **بُدِّل بعد تسرّبٍ** (البند في «ما ينتظر طرفاً
   ثالثاً» أدناه)، **ولم يُخزَّن في أيِّ ملفّ** — فهو سرٌّ يُعطى في الجلسة.
   **وبلا الدفع لا CI ولا رفع.**
2. **بوّابةُ المال وقفت بنصّها**: «مالٌ **يمسّ** — `subscription_offers`».
   **وعُرضت كما توجب المطلقة**: `0073` عمودٌ `nullable` بلا `server_default`
   وقيدُ حدٍّ [0,100]، **فكلُّ صفٍّ قائمٍ يصير `NULL` = «لا يمسّ النسبة» =
   السلوكُ اليومَ بحرفه**. **ورحلةُ الذهاب والإياب مقيسةٌ نظيفة**
   (`0073→0072→0073`). **تنتظر `TAXO_MONEY_OK=1` بكلمته.**

### ⚠ وما هو حيٌّ على الإنتاج الآن — **وعدٌ يكذب**

    commission_percent = 2.00        ← العمولةُ مشتعلة
    hero_subtitle      = «… — بلا عمولة على الكبتن المشترِك.»
    الصفحة             «صفر عمولة»×5 · «بلا عمولة»×1 · «عمولة 0%»×1

**سبعةُ وعودٍ مطلقةٍ بصفر عمولةٍ أمام من ينزّل التطبيقَ اليوم، والعمولةُ ٢٪.**
**والرفعةُ الموقوفةُ هي التي تنزعها** — فتأخيرُها ليس حياداً.
**والوثائقُ على الإنتاج v1 وفيها «مسوّدة»**، وv2 على التطوير وحدَه.

### وعملُ اليوم — **عطبان في بناء أمس، كشفهما الباب الحيّ**

| العطب | العلاج | المقياس |
|---|---|---|
| `driver_keeps_per_100` = `"1E+2"` | `format(…,"f")` لا `str(…normalize())` | **زُرع فسقط الاختبار، ثمّ خضّره العلاج** |
| `hero_subtitle` في القاعدة يَعِد مطلقاً | **ترحيلةُ بيانات `0074`** | الباب الحيُّ صار يقول النصَّ الجديد |

**و`DEFAULTS` «تُكتب مرّةً عند أوّل قراءة»** — فتصحيحُ المصدر لا يبلغ صفّاً
قائماً، **ولا حارسَ ساكنٌ يرى الصفّ**. و`0074` تكتب فوق النصِّ القديم
**بحرفه وحدَه** فيبقى أيُّ تحريرٍ من اللوحة.

**وحارسا الموقع كانا خارج `guards.sh`** — والسطرُ الختاميُّ يقول «كلُّ
الحرّاس خضر» فوق كنسٍ لا يشملهما. أُدخلا.

### وصفُّ العرض — **مطفأٌ على التطوير، والإطلاقُ بيد المالك**

«شهرك الأول علينا» · `money_percent 100` ⇒ مجّاناً · **`commission_percent
= 0.00`** (لا `NULL`) · `new_driver` · مرّةً واحدة · بلا نافذة ·
**`is_active=False`**. **والإنتاجُ `offer: null` ولا عمودَ فيه بعد.**
**واللوحةُ فيها زرُّ «شغّل» وحقلُ النسبة** — فيدُه تكفي هناك.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **الرمزُ من المالك** ⇒ دفعُ الإيداعين ⇒ CI.
2. **كلمتُه على بوّابة المال** ⇒ `TAXO_MONEY_OK=1` ⇒ الخمسُ بوّاباتٍ بترتيبها.
3. **إنشاءُ صفِّ العرض على الإنتاج مطفأً** بعد الرفع (العمودُ يصله حينها).
4. **التحقّقُ على الإنتاج**: البطاقتان · العدّادُ والمواضعُ الستّ · v2 · وأن
   الوعودَ السبعةَ زالت.
5. **الهاتف**: `RF8N80XEHBD` — حين يقول المالك إنه ضغط «السماح».

---

## ٠-ح. أين نقف — ٢٠٢٦-٠٩-٠٥ (**الوثائقُ منشورةٌ وبوّابةُ القبول تعمل**)

    الإنتاج    27d665d  ·  الترحيلة 0072  ·  ١١ حاويةً تعمل
    النسخة     /mnt/d/taxo-backups/20260905T160729Z  (16M، محقَّقةٌ بالفتح)
    CI         https://github.com/SBONGEEL/taxo/actions/runs/33975882623
    المجموعة   ١٬٣٩٥ خضراء · صفرُ أحمر

**أربعُ وثائقَ منشورةٌ على الإنتاج** (الأردن × راكب/كبتن × خصوصية/شروط، v1)،
**وبوّابةُ القبول مقيسةٌ حيّةً**: لا تسجيل بلا موافقةٍ صريحة، والصفُّ يُكتب
بالمستخدم والتاريخ ورقم النسخة.

### ⚠ ما ينتظر المالك — **ثلاثة**

1. **كلمةُ «مسوّدة» في مطلع الوثائق الأربع** — كلُّها تبدأ «هذه مسوّدةٌ تصف…»
   **وهي منشورةٌ الآن، فالكلمةُ صارت خبراً كاذباً عن نفسها**. **ولم تُمَسّ**:
   نصٌّ قانونيٌّ أقرّه المالكُ لا يُحرَّر بلا إذنه، **وكلُّ حفظٍ نسخةٌ ثانية**
   (§34). **يُقرَّر: تُحذف الكلمةُ في v2، أو تبقى.**
2. **حزمةُ المشرف: إدراجٌ عامٌّ أم رابطٌ غيرُ مدرَج** — الوجهان بكلفتيهما في
   تقرير الجلسة. **ولم يُرفع شيء.**
3. **وإذنُ الهاتف**: `RF8N80XEHBD` موصولٌ **بحال `unauthorized`** — ينتظر
   ضغطةَ «السماح بتصحيح USB» على شاشته. **ولم يُثبَّت شيء.**

### وقياسٌ يغيّر معنى «حزمةٌ محدَّثة»

**الحزمُ أغلفةُ شبكة** (`server.url` في `capacitor.config.ts`) — الشاشاتُ
تُحمَّل من النطاق لا من داخل الحزمة. **ومقيسٌ أن الغلافَ الأصليَّ وجدولَ
القنوات لم يتغيّرا حرفاً منذ v0.2.3**، بينما تغيّر ٣٦١ سطراً من شيفرة الويب.

**فبوّابةُ القبول وصلت كلَّ هاتفٍ مثبَّتٍ لحظةَ الرفعة**، **وحزمةٌ جديدةٌ
تشحن `versionCode 476` بصفر محتوىً جديد**. **ولذلك لم يُصنع وسمٌ** — والوسمُ
أمرٌ واحدٌ يومَ يريده المالك.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **قرارُ «مسوّدة»** — وإن حُذفت فهي v2 بنسخةٍ جديدةٍ ونشرٍ ثانٍ.
2. **جوابُ حزمة المشرف**، ثمّ التثبيتُ حين يُؤذن الهاتف.
3. **قياسُ اللوحة بعد الدخول على الإنتاج** — يحتاج جلسةً، والتحقّقُ بخطوتين
   ليس للوكيل.
4. **`test_route` بهامشٍ جديد** — يُراقَب في الدفعات القادمة؛ **وإن سقط ثانيةً
   فالعلّةُ في تصميمه لا في هامشه**.
5. **خانتان بلا لقطة** (`captain-garage` · `rider-rides`) — خانةٌ لهما أو
   تُترك الأداةُ لا تلتقطهما.

---

## ٠-ج. أين نقف — ٢٠٢٦-٠٩-٠٥ (**الموقعُ منشورٌ على `taxo.tajora.ly`**)

    الإنتاج    a5ee520  ·  الترحيلة 0072  ·  ١١ حاويةً تعمل
    النسخة     /mnt/d/taxo-backups/20260905T104015Z  (16M، محقَّقةٌ بالفتح)
    CI         https://github.com/SBONGEEL/taxo/actions/runs/33960416691

**ورفعتان في اليوم**: `861405d` ثمّ `a5ee520` — **والثانيةُ لأن الأولى خضّرت
بوّاباتِها الستَّ والموقعُ مكسور**.

### ما ينتظر المالك — **ثلاثةٌ لا يبدأ فيها أحدٌ قبله**

1. **مراجعةُ المسوّدات الأربع** (خصوصيةُ وشروطُ الراكب والكبتن، الأردنُ وحدَه).
   **والمفتاحان `policies_public` و`is_published` مطفآن** ومقيسٌ أنهما كذلك:
   `/api/policy` يعود `null`، والصفحتان تقولان «لم تُنشر بعد» **بـJS وبلا JS**.
   **ولا يُشعَل شيءٌ إلا بأمره.**
2. **وثلاثةُ أسئلةٍ في المسوّدات** — في تقرير الجلسة: أتُنسخ فقرةُ المزوّدين
   إلى خصوصية الكبتن · أتبقى فقرةُ «لا نحتفظ برقم بطاقتك» · وصفوفُ السياسات
   على الإنتاج **لم تُقَس** لأن قراءةَ قاعدة الإنتاج تحتاج إذنَه.
3. **وحالُ التوزيع `apk`** — **يبدّلها بنفسه من اللوحة**، والرابطان جاهزان.

### وما لم يُقَس — **يُقال ولا يُقرأ سكوتُه ضماناً**

- **تخطيطُ اللوحة بعد الدخول على `admin.tajora.ly`**: قِيست شاشةُ الدخول
  خضراءَ في المقاسات الأربعة، **والترويسةُ المُصلَحةُ خلف جلسة** — والتحقّقُ
  بخطوتين ليس لي. **وأُثبت أن الإصلاح شُحن**: `safe-top` و`size-44`
  و`min-width:44rem` و`group-focus-within` و`viewport-fit=cover` كلُّها في
  الحزمة المخدومة، **والحزمةُ = ما بناه الخادم** (`index-bL4gNAKw.js`).
- **ولا هاتفَ حقيقيّ** — متصفّحُ مكتبٍ بمقاسِ هاتفٍ و`hasTouch`.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **انتظار قرار المالك في السياسات** — ولا يُشعَل مفتاحٌ قبله.
2. **قياسُ اللوحة بعد الدخول على الإنتاج** حين يتاح دخولٌ (أو بجلسةٍ يفتحها هو).
3. **`check:mobile` في CI؟ لا** — يحتاج بيئةً حيّةً وChrome؛ **ويبقى شرطاً
   يدوياً موثَّقاً في `COMMANDS.md`**، أو يُبنى له مسارٌ يقلع الحزمةَ في CI.
4. **خانتان بلا لقطة**: `captain-garage` و`rider-rides` مُلتقَطتان ولا موضعَ
   لهما في التصميم — **إمّا خانةٌ لهما أو تُترك الأداةُ لا تلتقطهما**.
5. **مراجعةُ `max-parallel: 4`** بعد دفعةٍ كبيرةٍ ثانية — **الرقمُ محسوبٌ على
   قياس اليوم، وقد بلي مثلُه مرّةً**.

---

## ٠-ث. أين نقف — ٢٠٢٦-٠٩-٠٥ (**موقعُ `taxo.tajora.ly` بُني ولم يُنشر**)

**والمواصفةُ `SPEC.md` §52، والخبرُ في `STATE.md`.**

    الخلفية    site_settings · GET /public/site · GET /public/policy · PATCH /admin/site
    اللوحة     شاشةُ «الموقع» — كلُّ نصٍّ ورابطٍ ومفتاح
    الصفحة     site/ → landing/  ·  / · /privacy · /terms · 404
    الترحيلة   0072 (مطبَّقةٌ على التطوير وحدَه)

**ولم يُنشر شيء** — الإنتاجُ ما زال على `2c3daca` بالصفحة القديمة.

### المعاينة

    http://localhost:5177/          (من هذا الجهاز وحدَه)
    التشغيل: cd ~/prj/TAXO/site && npx vite preview --host 0.0.0.0 --port 5177

**ولا مضيفَ نفقٍ لها** — إضافتُه تحتاج سجلَّ DNS وتعديلَ إعداد النفق، **ولم
يُطلَب**. فمن أراد فتحَها من هاتفه يقولها.

### ما ينتظرك أنت — **خمسة**

1. **TOTP** · **مراجعةُ الوثائق الثمانِ** · **بيانُ الجهة** — كما هي.
2. **شارةُ Google Play الرسمية** — ملفُّ Google كما هو بمساحته الآمنة.
   **ولا تُرسم بديلاً**، وحالُ `play` تعرض شارةً معطَّلةً حتى يصل.
3. **روابطُ التواصل الاجتماعيّ** (فيسبوك · إنستغرام · تيك توك · إكس ·
   واتساب) — **والفارغُ لا يُرسم أيقونةً**، فالتذييلُ اليومَ بلا أيقونة.
4. **رابطا Google Play للتطبيقين** — يومَ يُفتح المتجر.
5. **وإذنُ النشر** — عبر `scripts/deploy.sh` والبوّابات، ولا شيءَ قبله.

### ⚠ وما لم يُنجَز — ولا يُقرأ سلامة

1. **اللقطاتُ الخمسَ عشرةَ لم تُلتقط** — إضافةُ المتصفّح تمنع التقاطَ الصور
   على هذا الأصل (`Chrome blocked the extension from accessing this page`)،
   جُرّب مرّتين. **والخاناتُ مستطيلاتٌ محايدةٌ باسم الشاشة** كما اشترط المالك.
2. **ولقطاتُ حزمة التصميم مرفوضةٌ كلُّها** — ٩٢٤×٥٤٠ أفقيّ، وساعةُ 9:41،
   **وأرقامٌ عربية-هنديةٌ يمنعها `check:digits`**.
3. **الأداءُ في Lighthouse غيرُ مستقرّ**: ستُّ تشغيلاتٍ أعطت `81·93·97·64·79·94`
   — **والجهازُ يشغّل ١٢ حاويةً والمجموعةَ وChrome معاً**. يُعاد على جهازٍ هادئ.
4. **و`prefers-reduced-motion` قِيس وجودُه في المخرَج لا أثرُه** — لم يُحاكَ
   الاستعلام.
5. **ولم يُقَس شيءٌ على الإنتاج.**

### وبندان معماريّان يستحقّان قرارَك

1. **مخرَجُ البناء مودَعٌ في `landing/`** (٨٦٠ ك.ب) لأن نجينكس يخدم الشجرةَ
   مباشرةً. **والأنظفُ أن يبنيَه الخادمُ كما يبني الثلاثة** — سطرٌ في
   `deploy.sh`، **ولا يُقاس إلا برفعة**.
2. **وعطبُ تخطيط اللوحة على الهاتف ما زال مفتوحاً** — قِيس ولم يُصلَح:
   ترويسةُ اللوحة تتجاوز `73px` يساراً على 390، **وزرّان `34×34` خارج الشاشة
   تماماً**. تفصيلُه في ردِّ الجولة، ولم يدخل هذا الإيداع.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **اللقطاتُ الخمسَ عشرة** — بإذنٍ للإضافة على الأصل، أو Playwright على `D:`.
2. **وعطبُ تخطيط اللوحة على الهاتف** — السببُ مسمّىً والقياسُ جاهز.
3. **وشارةُ Google Play** حين تصل، ثمّ قياسُ الحالتين بالضغط.
4. **وLighthouse على جهازٍ هادئ.**
5. **وقرارُك في بناء الموقع على الخادم.**

---

## ٠-ت. أين نقف — ٢٠٢٦-٠٩-٠٥ (**رُفع على الإنتاج**: ٤٧ إيداعاً، وشجرةُ WSL هي الرافعة)

**أوّلُ رفعٍ منذ ٢٠٢٦-٠٨-٣٠، وأوّلُ رفعٍ من الشجرة اللينكسيّة.**

    الخادم      c6caf79  →  d4f8e10  →  2c3daca   (٤٨ التزاماً، رفعتان)
    القاعدة     0063     →  0071           (٨ ترحيلات)
    الحزم       v0.2.2   →  v0.2.3         (versionCode 418 → 464)
    البوّابات   الستُّ خضراءُ كلُّها، والسجلُّ صفرُ خطأٍ في ثلاث دقائق

### والأنماطُ الأربعةُ صارت موجودةً على `admin.tajora.ly`

**مقيسةً من الحزمة التي يخدمها الخادمُ الآن** — تُجلب الصفحةُ، ومنها الحزمةُ
الرئيسة، ومنها **كلُّ قطعةٍ كسولةٍ تذكرها** (٢٦ قطعة):

    /m/payments · cliq_claim_declared · pushNotificationActionPerformed  →  main.js
    «إذن الإشعارات مرفوض»                                                 →  chunk-MobilePayments-BEW2qzAb.js
    index-fy3fA2pB.js (227,650 ب) → index-gtGmXo_h.js (244,612 ب)
    والصفوفُ بلا فرقٍ حرفاً: diff صامت، وبصمتا الصفوف واحدة e72b87d7…

**والأعمدةُ الأربعةُ خضراءُ للثلاثة**، والثلاثةُ فُتحت في متصفّحٍ وقُرئ DOMها:
اللوحةُ ٣٢ عقدةً والنموذجُ مرسوم · الراكبُ «اطلب تاكسي في ثوانٍ» · الكبتنُ
حقلان وثلاثةُ أزرار. **وصفرُ خطأٍ في الثلاث**، والرسائلُ الوحيدةُ من إضافةِ
متصفّح.

### ⚠ عطبان في بابِ الرفع نفسِه — أُصلحا، **ويُقرآن قبل أيِّ رفعٍ قادم**

1. **نسخةُ الوثائق كانت خضراءَ وهي عمياء**: `driver_documents` **٣٧ صفّاً** و
   `documents.tar.gz` **١٢٤ بايت** — الوثائقُ في مجلَّدٍ مسمّى لا مسارِ مضيف،
   والفحصُ كان يطابق **اسمَ المدخل**. **وثائقُ ٣٧ كبتناً بلا نسخةٍ سبعةَ عشرَ
   يوماً.** أُخذت النسخةُ (`16,100,316` بايت · **٧٦٨ ملفّاً**)، وأُصلح البابُ،
   **ومقيسٌ في أربعة اتجاهات**.
2. **فجوةُ الستِّ ثوانٍ بين اتصالات `ssh` كانت على حافّة حدِّ `ufw` بالضبط** —
   فسقطت الرفعةُ مرّتين بـ`Connection refused`، **وسقطت صامتةً** لأن
   `PULL="$(_ssh …)"` تحت `set -e` يقتل `case` الذي يحمل تشخيصَه. صارت الفجوةُ
   **١٢ ثانية** و`TAXO_SSH_GAP` مصرَّح. **ولم يُمسّ `ufw` بحرف.**

### والرفعةُ الثانية أثبتت البوّابةَ المُصلَحة

**`d4f8e10 → 2c3daca`**، والشاهدُ سطر: `✓ الوثائق … (16,100,316 بايت)` وكان
`124`، وحجمُ النسخة `16M` وكان `100K`. **ومسارُ نشرٍ جديدٌ لا يُصدَّق حتى يمرّ
مرّتين** — وقد مرّ. وأُعيد قياسُ الأنماط الأربعة على `index-JzWUZ2qv.js`
فبقيت الأربعة، **وأشكالُ التمييز الخمسةُ في حزمة الإنتاج**.

### ما ينتظرك أنت — **ثلاثة، وهي هي**

1. **TOTP** — لم يُمسّ.
2. **مراجعةُ الوثائق الثمانِ** (البند ١٠) — كما هي. **ولم تُنشر سياسةٌ ولم
   تُعرض على مستخدم.**
3. **بيانُ الجهة** — فارغٌ كما هو.

**وسقط من قائمة انتظارك**: مفتاحُ تكامل WSL — **عاد وحدَه بإقلاع Docker
Desktop**، والمجسُّ `34` لا صفراً، والنفقُ `4` وصلات.

### ⚠ وما لم يُقَس — ولا يُقرأ سلامة

1. **لم تُفتح لوحةُ الإنتاج بحساب** — بابُ الدخول محدودُ المحاولات، وطرقُه
   ينفق من ميزانيتك أنت. **فالمقيسُ شاشةُ الدخول ورسمُها وحزمتُها.**
2. **ولا حزمةَ أندرويد على هاتف** — بُنيت في CI ونُشرت وبصمتاها مقيستان
   **بالتنزيل**، **ولم تُثبَّت ولم تُفتح**.
3. **والإخفاقُ الجزئيُّ في الإشعار الجماعيّ لم يُقَس** — لم يقع إخفاق.
4. **وخليّةُ «الطرفان» في النزاعات ما زالت غيرَ مقيسة** — صفرُ دفعةِ كليك
   معلَّقةٍ في قاعدة التطوير، والنزاعُ لا يُفتح إلا على كليك.
5. **و`VerificationCampaign` فيه عطبُ التمييز نفسُه ولم يُحوَّل** — العددُ
   ملفوفٌ بـ`<b>`، وتحويلُه ينقل العريضَ إلى العبارة كلِّها: **تغييرُ شكلٍ لم
   يُقَس**، فقُيِّد ولم يُخمَّن.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **`VerificationCampaign` — تمييزُ العدد**، وقرارٌ في موضع `<b>`: أعلى الرقم
   وحدَه أم العبارة؟ **حملةٌ تشمل ألفاً تقول اليومَ «1000 حساباً».**
2. **خليّةُ «الطرفان» في النزاعات** — تُصنع دفعةُ كليك معلَّقةٌ في التطوير
   ويُنازعها الكبتنُ من بابه، **ثم تُقاس على الشاشة** لا بالاستنتاج.
3. **جولةُ هاتفٍ على `v0.2.3`** — تُثبَّت الحزمتان ويُفتحان، **فالعمودُ الذي
   لا يقيسه حارس**.
4. **والإخفاقُ الجزئيُّ للإشعار الجماعيّ** — يُفتعل بحسابِ موظّفٍ من بابٍ يراه.
5. **ومراجعةُ ما بقي من حرّاسٍ خارج `guards.sh`** — الجردُ الأخيرُ أعطى ثلاثةً
   لا يستحقّ واحدٌ منها الدخول، **والسؤالُ يُعاد بعد بابين جديدين**.

---

## ٠-ش. ~~عائقٌ واحدٌ يوقف كلَّ قياسٍ حيّ~~ — **زال ٢٠٢٦-٠٩-٠٥، ويبقى نصُّه لعلّته**

> **⚠ سقط هذا العائقُ ولم يُحذف نصُّه.** التكاملُ عاد **بإقلاع Docker
> Desktop وحدَه** — لم يكن مطفأً في إعداداته بل **البرنامجُ نفسُه لم يكن
> يعمل**، و`docker-desktop` في `wsl -l -v` كانت `Stopped`. **والمقيسُ بعده**:
> `/var/run/docker.sock` موجود · المجسُّ `34` لا صفراً · ١٢ حاويةً تعمل ·
> والنفقُ `4` وصلاتٍ مسجَّلة بعد `docker start taxo-tunnel`.
>
> **ويبقى النصُّ لأن تشخيصَه يُقرأ قبل أن يُعاد**: كلُّ ما جُرّب في الجولة
> السابقة (إعادةُ التوزيعة · إعادةُ Desktop ثلاثاً · `docker desktop restart` ·
> تشغيلُ الوكيل بيد · كتابةُ `settings-store.json`) **كان يعالج إعداداً
> والعلّةُ أن البرنامجَ لا يعمل**. **فأوّلُ سؤالٍ قبل أيِّ تشخيصٍ لتكامل
> WSL: أيعمل Docker Desktop أصلاً؟** — و`Get-Process` يجيبه في ثانية.
>
> **وحالُ اليوم في `٠-ت` فوق، لا هنا.**


**تكاملُ Docker Desktop مع `Ubuntu-24.04` نصفُ مطبَّق**، والمقيسُ:

    /mnt/wsl/docker-desktop/cli-tools     ٢٠ ملفّاً ✅  (كان صفراً)
    /var/run/docker.sock                  غائبٌ ✗
    docker run -v /home/loly3/prj/TAXO:/p ls /p | wc -l   →  0
    ls ~/prj/TAXO | wc -l                                 →  34

**وأثرُه أن المركَّباتِ العابرةَ للتوزيعات تصل فارغة**: `taxo-backend` و
`taxo-admin-panel` و`taxo-driver-app` و`taxo-customer-app` و`taxo-worker`
و`taxo-beat` **تقلع بـ`/app` فارغ وتدور في حلقة**، **والنفقُ يسقط** بـ
`open /etc/cloudflared/config.docker.yml: no such file or directory`
**والملفُّ موجود**.

**وما جُرّب ولم يُصلحه**: إعادةُ تشغيل التوزيعة · إعادةُ تشغيل Desktop ثلاثاً ·
`docker desktop restart` · تشغيلُ `docker-desktop-user-distro proxy` بيدٍ
(**أنشأ المقبسَ ولم يجسر**). **ولا مفتاحَ للتكامل في `docker desktop` CLI**
(`enable` لا يعرف إلا `model-runner`)، **وكتابتُه في `settings-store.json`
يرفضها مصنِّفُ الأذونات**.

> **فأوّلُ ما يُفعل**: Docker Desktop → Settings → Resources → **WSL
> Integration** → `Ubuntu-24.04` → **Apply & restart**. **وبرهانُ نجاحه سطران
> لا واحد**: `/var/run/docker.sock` موجود، **والمجسُّ يعطي ٣٤ لا صفراً**.

**وحتى يُفعل**: اللوحةُ تُخدَم من المصدر خارج Docker —
`VITE_API_BASE_URL=http://127.0.0.1:8001 npx vite --host 0.0.0.0 --port 5175`
من `~/prj/TAXO/admin-panel` — **لكنّ الخلفيةَ ساقطةٌ فلا جدولَ يُحمَّل**،
وبايثونُ التوزيعة بلا تبعيّات.

---

## ٠-ر. أين نقف — ٢٠٢٦-٠٩-٠٤ (بندُ التسهيل تمّ، والدفعةُ اكتملت)

**الترتيبُ المقرَّر كلُّه بُني**: ١ · ٢ · ٤ · ٥ · ٦ · ٨ · ٩ · ١٠ · ١١ ·
**التسهيل** — **ولم يبقَ منه شيء**، والفرعُ ٤ صار **شقّين تامّين** بعد وصل
«افتح الملفّ» (§٤٧٫١٧).

### وما أُنجز في جولة ٢٠٢٦-٠٩-٠٤

| ما | المقيس |
|---|---|
| **تحذيرُ `autoMemoryReclaim`** | **اختفى** — إقلاعٌ باردٌ: `stderr = 0 bytes` و`/proc/uptime = 23.90` ثانية |
| **`taxo-suite` أُزيلت** | **`D:\prj\TAXO` اختفى من خريطة `working_dir`** — والعشرُ كلُّها على `/home/loly3/prj/TAXO` |
| **جولةُ المتصفّح** | Chrome لا Edge — **وثلاثةُ عطوبٍ لم يمسكها حارس** (§٤٧٫١٦) |
| **«افتح الملفّ»** | بيتٌ واحدٌ في **ستّة مواضعَ من خمسة جداول** (§٤٧٫١٧) |
| **مرشِّحُ حال الشحنات** | ومعه **عطبٌ كامنٌ**: `confirmed` لم تكن في خريطة الأسماء (§48) |

### ما ينتظرك أنت — **ثلاثة**

1. **مفتاحُ تكامل WSL** (٠-ش فوق) — **وبه يعود كلُّ شيء**.
2. **مراجعةُ الوثائق الثمانِ** (البند ١٠) — كما هي.
3. **بيانُ الجهة** — فارغٌ كما هو.

### وبندان مستقلّان لم يُبنَيا

1. **حراسةُ `/admin/users` وأخواتِها الثلاث بصلاحياتٍ مسمّاة** (§٤٧٫١٠).
2. **عمودُ اختيارٍ في `Table.tsx`** — يفتح الأفعالَ الجماعيةَ لـ٢٢ جدولاً
   (§٤٧٫١١)، **وبه تُوصَل بقيّةُ «افتح الملفّ»**.

### ⚠ وما لم يُقَس — ولا يُقرأ سلامة

1. **رسمُ أزرار «الملفّ» ومرشِّح الشحنات لم يُقَس بصرياً** — الخلفيةُ ساقطة.
   **والمقيسُ الحرّاسُ والمُترجِمُ وحدَهما.**
2. **ولا حزمةَ أندرويد ولا جولةَ هاتف** منذ النقل إلى WSL.
3. **ولم يُقَس شيءٌ على الإنتاج.**
4. **والمجموعةُ لم تُعَد منذ `1365 passed`** — **وصفرُ ملفٍّ في `backend/`
   تغيّر بعدها**، فالخضرةُ تصف الخلفيةَ القائمة.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **المفتاح، ثمّ إقلاعُ كلِّ شيء والتحقّقُ بالمجسّ (٣٤ لا صفراً).**
2. **وجولةُ متصفّحٍ تقيس ما لم يُقَس**: أزرارُ «الملفّ» ومرشِّحُ الشحنات.
3. **وعطبُ `wallet_owner_undecided`** — إعلانُ جانب المحفظة في نداء اللوحة.
4. **والبندان المستقلّان** بترتيبك.
5. **والبندُ الجانبيّ** (خرائطُ اللوحة في وضع التطوير).

---

## ٠-ر. أين نقف — ٢٠٢٦-٠٩-٠٣ (بندُ التسهيل تمّ، ودفعةُ اللوحة اكتملت)

**البندُ الأخيرُ من الترتيب المُقرّ بُني**: ١ · ٢ · ٤ · ٥ · ٦ · ٨ · ٩ · ١٠ ·
١١ · **التسهيل** — **ولم يبقَ من الدفعة شيء**.

### ما بُني في هذه الجولة

| الفرع | المقيس |
|---|---|
| **٤ البحثُ العامّ** | `GET /admin/search` — أربعةُ أصنافٍ وأربعُ وجهات، **وصفرُ بابٍ ثانٍ للفتح** |
| **٧ الصلاحيات** | اختبارٌ على ما يقع — **والأربعُ بلا `require_permission` اليوم** |
| **٣ الجماعيّ** | «اعتمِد ما عاينتَه» وحدَه، **والاثنان الباقيان بعلّتيهما** |
| **١ الاقتراحات** | `level_required` صار اختياراً — **`7` كان يصنع مركبةً لا يبلغها أحد** |
| **٢ الحقول** | المدّةُ ١٦ · التاريخُ ٦ · المالُ ٤ |
| **٥ الجمل** | ستٌّ بسند، **وسبعٌ تُسمّى ساكتة** |
| **٦ القياس** | −١ · −٢ · **صفر** · −١ · −١ |

**والمواصفةُ في `SPEC.md` §47** بخمسةَ عشرَ فرعاً، **والخبرُ في `STATE.md`**.

### وقراران نُفِّذا بأمرك

1. **`restart: "no"` تبقى** — والسطرُ في `COMMANDS.md`: `docker start
   taxo-tunnel` بعد كلِّ إقلاع، **`docker start` لا `compose up`** بعلّته.
2. **`DateField` حُذف** — ٦٣ سطراً، نسخةٌ أفقرُ من `Field type="date"`.

### ما ينتظرك أنت — **أربعة**

1. **مراجعةُ الوثائق الثمانِ** (البند ١٠) — كما هي.
2. **بيانُ الجهة** — فارغٌ كما هو.
3. **`wsl --shutdown` بيدك** — لقياس تحذير `autoMemoryReclaim` وحدَه
   (`STATE.md`: لا يُقاس من داخل جلسةٍ تعمل). **وأقصرُ نافذة: قبل أن تبدأ
   جولةً لا في وسطها.**
4. **وثلاثةُ بنودٍ وُلدت من التسهيل** — تُقرأ بنوداً لا نواقص:
   - **لسانٌ ثانٍ للمطالبات المؤكَّدة** أو مرشِّحُ حالٍ في اللسان.
   - **حراسةُ `/admin/users` وأخواتِها الثلاث بصلاحياتٍ مسمّاة**.
   - **عمودُ اختيارٍ في `Table.tsx`** يفتح الأفعالَ الجماعية لـ٢٢ جدولاً.

### ⚠ وما لم يُقَس في هذه الدفعة — **ولا يُقرأ سلامة**

1. **لم تُفتح شاشةٌ في متصفّح**: لا Playwright ولا Edge في التوزيعة. **فحقلُ
   البحث في الرأس ودرجُه لم يُرَ مرسوماً**، **وقياسُ الضغطات عدٌّ من الشيفرة**.
2. **ودرجُ `Picker` في الرأس لم يُقَس بصرياً** — `absolute` داخل رأسٍ `sticky`،
   **ودرسُ شريط التنقّل في هذا المشروع يقول إن حاويةَ القصّ لا تُرى إلا
   بالقياس**.
3. **ولا حزمةَ أندرويد ولا جولةَ هاتف** منذ النقل إلى WSL.
4. **ولم يُقَس شيءٌ على الإنتاج.**

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **جولةُ متصفّحٍ على اللوحة من `D:`** — الرأسُ والدرجُ والاعتمادُ الجماعيّ،
   **فهي الوحيدةُ التي تملك Edge**.
2. **وعطبُ `wallet_owner_undecided`** — إعلانُ جانب المحفظة في نداء اللوحة.
3. **والبنودُ الثلاثةُ الموروثةُ من التسهيل** بترتيبك.
4. **ومراجعةُ ما بقي من حرّاسٍ خارج `guards.sh`.**
5. **والبندُ الجانبيّ** (خرائطُ اللوحة في وضع التطوير).

---

## ٠-ق. **أين يُعمل اليوم** — قاعدةٌ قائمةٌ لا خبرُ يوم (قرارُ المالك ٢٠٢٦-٠٩-٠٣)

**شجرةُ العمل `~/prj/TAXO` في `Ubuntu-24.04`. كلُّ بناءٍ وكلُّ إيداعٍ فيها،
ولا يُعمل على `D:\prj\TAXO` إلا بطلبٍ صريحٍ من المالك.**

**و`D:` ليست نسخةً احتياطية**: **بناءُ أندرويد وجولةُ الهاتف وجولةُ Playwright
ما زالت عليها** — **فهي نصفُ مسارِ عملٍ قائم**، لا أرشيف. **ولا تُهجَر حتى
تخضرَّ الجديدةُ جولتين أو ثلاثاً** — والمقياسُ اليومَ واحدٌ بعد التحويل.

**وما يبقى على ويندوز بقصدٍ لا بإهمال، مقيساً:**

| ما | القياس |
|---|---|
| **بناءُ أندرويد** | لا SDK ولا JDK في التوزيعة، و`local.properties` ×٣ تشير إلى SDK ويندوز |
| **الهاتفُ بالـUSB** | `/dev/bus/usb` غيرُ موجود، و`usbipd-win` غيرُ مركّب؛ و`adb` على ويندوز |
| **جولةُ Playwright** | لا `msedge` ولا Chrome ولا Chromium في التوزيعة |
| **`scripts/pull-backup.ps1`** | PowerShell — ويندوزيٌّ بقصد |

**والنفقُ خرج من هذه القائمة** — يعمل من لينكس منذ `7bf961a`.

### ⚠ وأثرٌ تركتُه على `D:` — يُقال ولا يُطوى

**قياسُ «قبل» لبناء اللوحة شغّل بناءً على `D:`**، فكتب فيها **ثلاثةَ ملفّاتٍ
مُهمَلة**: `admin-panel/dist/` و`tsconfig.{app,node}.tsbuildinfo`. **وصفرُ
فرقٍ في أيِّ ملفٍّ متعقَّب**، والرأسُ والحالُ كما هما (`5eadb43` · `?? video-assets/`).

**لكنّ `admin-panel/dist` هناك صار يحمل هدفَ `http://localhost:8001`** بدل ما
كان — **فمن يعود إلى `D:` لجولةِ نفقٍ يعيد بناءه أوّلاً**.

### ما ينتظرك أنت — **ثلاثة**

1. **مراجعةُ الوثائق الثمانِ** (البند ١٠) — كما هي.
2. **بيانُ الجهة** — فارغٌ كما هو.
3. **`wsl --shutdown` بيدك** — بعد أن يُنقل سطرُ `autoMemoryReclaim` إلى
   `[experimental]`. **ومتى؟** حين لا تكون مجموعةٌ تعمل ولا جولةُ هاتفٍ قائمة:
   يقتل الأمرُ آلةَ WSL كلَّها ومعها `docker-desktop`، **فتسقط الحاوياتُ العشر
   والنفق** ثمّ تعود بإقلاع Docker Desktop. **والمجلَّداتُ المسمّاة تبقى
   وقاعدةُ التطوير لا تُمسّ** (مقيسٌ ٢٠٢٦-٠٩-٠٣: عادت الحاوياتُ كلُّها و٥٨
   صورةً وكلُّ مجلَّدات `taxo-app` بلا نقص). **وأقصرُ نافذة: قبل أن تبدأ جولةً،
   لا في وسطها.**

> **والملفُّ لم يُمسّ بعد**: متنُ رسالتك يأذن بالنقل وشرطُك يقول «إلا بعد أن
> أقول الآن» — **فأُخذ بالأضيق**. والتعديلُ سطران: تُنقل
> `autoMemoryReclaim=gradual` من `[wsl2]` إلى قسمٍ جديدٍ `[experimental]`،
> **ويبقى `memory=6GB` و`swap=6GB` مكانَهما** فهما صحيحان في `[wsl2]`
> (ولا تحذيرَ عليهما — وهو الدليلُ أن القسمَ يُقرأ والمفتاحَ وحدَه مرفوض).

### جردُ الحرّاس — الجوابُ على «هل بقي ثالث؟»

**ثلاثةٌ خارجَ البابِ الواحد، ولا واحدَ منها يستحقّ الدخول — ولونُ كلٍّ مقيسٌ
قبل القرار** (التفصيلُ في `GUARDS.md`):

| الحارس | لونُه في بيئة خطّاف | القرار |
|---|---|---|
| `check:target` ×٣ | **أحمرُ `rc=1`** | **لا يدخل** — يوقف كلَّ إيداع |
| `check:dist` ×٣ | **أحمرُ `rc=1`** | **لا يدخل** — بلا بناءٍ يسقط، ومع قديمٍ يكذب |
| `check:served` ×٣ | أخضرُ `rc=0` | **لا يدخل** — ٤٥ ث في كلِّ إيداعٍ والحاوياتُ مطفأة |

**وصفرُ أداةٍ يتيمة**، **وصفرُ `check:` مُصرَّحٍ لا يشغّله أحد.**

### وتصحيحٌ في اليوم نفسِه

**دعوى «`tsbuildinfo` سترَ خطأَ `AppVersion`» خاطئة** — أسقطها `stat`:
الذاكرةُ `01:52:40` والمصدرُ `01:55:23`، و`dist` بعمر أربعةِ أيام. **فالسببُ
أن البناءَ لم يعمل، وهو عينُ سبب `check:sheet`** — **درسٌ واحدٌ لا اثنان.**

---

## ٠-ص. أين نقف — ٢٠٢٦-٠٩-٠٣ (الاعتمادُ تمّ: الحاوياتُ والنفقُ على الشجرة الجديدة)

**كلُّ شيءٍ صار من `~/prj/TAXO`**: الحاوياتُ العشر والنفقُ معها، مقيساً من
`docker inspect` لا من ملفّ. **و`D:\prj\TAXO` باقيةٌ لم تُمسّ ولم يُحذف منها
شيء** — والرجوعُ إليها أمرُ `up` واحد.

### الأعمدةُ الثلاثة — خضراءُ لأوّل مرّةٍ في المشروع

    admin-panel   المبنيّ = 127.0.0.1:5175 = dev-admin.tajora.ly   assets/index-C770noDi.js
    customer-app  المبنيّ = 127.0.0.1:5176 = dev-app.tajora.ly     assets/index-CVhzOZBB.js
    driver-app    المبنيّ = 127.0.0.1:5174 = dev-driver.tajora.ly  assets/index-DukPcDGW.js

**كان عمودُ النفق يقول «لم يُقَس (HTTP 530)» في كلِّ جولةٍ سابقة** — **وقياسٌ
ناقصٌ لا يشهد لشيء**.

### وحارسان أحمران عاشا على `master` إيداعاً كاملاً — كشفهما النقل

**كلاهما من `ebe87c7` (البند ٨)، وكلاهما أُصلح، وتفصيلُهما في `GUARDS.md`:**

1. **`check:sheet` أحمرُ ولم يُشغَّل** — هو في `npm run build` لتطبيق الراكب
   وحدَه **وليس في `scripts/guards.sh`**، والمشروعُ لا يدفع فلا CI. أُصلحت
   البطاقةُ في الثلاثة معاً، **وأُدخل الحارسُ في البابِ الواحد**.
2. **`AppVersion` مستعملٌ بلا استيرادٍ في تطبيقين** — وأوّلُ بناءٍ في لينكس
   كشفه (`TS2304`). **وقيل أوّلاً إن الذاكرةَ المخبَّأة سترته، والقياسُ أسقط
   ذلك**: المصدرُ أحدثُ من الذاكرة بثلاث دقائق، و`dist` بعمر أربعةِ أيام —
   **فالسببُ أن البناءَ لم يعمل، وهو عينُ سبب الأوّل.**

### وثلاثةٌ كسرها المسارُ الجديد وأُصلحت — كلُّها في `docker-compose.tunnel.yml`

| ما | العلّة | العلاج |
|---|---|---|
| `${USERPROFILE}` | متغيّرُ ويندوز، غيابُه في لينكس يجعل المسارَ `/.cloudflared`. **والجردُ بحث عن `C:\` لا عن متغيّرات** | `${TAXO_CLOUDFLARED_DIR:-${USERPROFILE}/.cloudflared}` — مقيسٌ في الاتجاهين |
| صلاحيةُ الاعتماد | الصورةُ تعمل بـ65532، والاعتمادُ `600` بملك `1000`. **وعلى drvfs كلُّ شيءٍ `777` فلا يظهر** | `user: "${TAXO_TUNNEL_USER:-65532:65532}"` — **ولا تُرخى صلاحيةُ سرٍّ لأجل حاوية** |
| `restart: "no"` | **هي التي أبقت النفقَ ساقطاً** بعد `wsl --shutdown`، وإخوتُه `unless-stopped` فعادوا | لم يُغيَّر — **مصرَّحٌ بقصد**، والسببُ صار مكتوباً |

### ⚠ وثلاثةُ أسطرٍ في هذا الملفّ كذبت، فتُقرأ هنا لا فوق

1. **«تُعاد بصمةُ `taxo-contabo` إلى `authorized_keys`» — ساقطٌ نهائياً.**
   قِيس بدخولٍ فعليٍّ (`SSH_OK` · `vmi3521522` · `taxo`)، **والبصمتان هناك**.
   **ولا ينتظر إصبعَ المالك.**
2. **و`deploy-command.sh` لا ينتظر إصبعَه أيضاً**: جذرُه أن الفهرسَ لم يحمل
   بتَّ التنفيذ قطّ، **وأُصلح في `8c36570`** — فأوّلُ سحبٍ يحمله يجعله `755`
   من نفسه.
3. **ودعوى «الكلفةُ المهيمنة `TRUNCATE` على قرص ويندوز» باطلةٌ مقيساً** —
   القاعدةُ في مجلَّدٍ مسمّى داخل آلة Docker ولم تعبر جسرَ 9p يوماً.
   **والمكسبُ المقيس: المجموعةُ ١٠٫٨٪، والحرّاسُ ٧٠٪ (٤٠ ث → ١٢ ث)، وبناءُ
   اللوحة ٢٢٪ (٥٠ ث → ٣٩ ث).** **فلا يُبنى على الدعوى القديمة قرارٌ ثانٍ.**

### وما لم يعد يعمل من داخل لينكس — **بعد أن خرج النفقُ من القائمة**

**بناءُ أندرويد** (لا SDK ولا JDK) · **الهاتفُ بالـUSB** (لا `/dev/bus/usb`
ولا `usbipd-win`) · **جولةُ Playwright** (لا متصفّحَ في التوزيعة) ·
**`scripts/pull-backup.ps1`** (PowerShell بقصد). **والنفقُ صار يعمل من لينكس**
فخرج من هذه القائمة.

### وما ينتظر المالك — **اثنان لا أربعة**

1. **مراجعةُ الوثائق الثمانِ** (البند ١٠) — كما هي.
2. **بيانُ الجهة** — فارغٌ كما هو.

**ولا شيءَ ثالثٌ ينتظر إصبعَه**: علاجا الخادم سقطا كلاهما (فوق).

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **قرارُ المالك في هجر `D:`** — كلُّ ما كان يمنعه سقط.
2. **بندُ التسهيل** (§39٫١٢) — ومنه وصلُ `Picker.tsx` و`RecordModal`.
3. **وعطبُ `wallet_owner_undecided`** — إعلانُ جانب المحفظة في نداء اللوحة.
4. **ومراجعةُ ما بقي من حرّاسٍ خارج `guards.sh`** — `check:sheet` كان واحداً،
   **والسؤالُ: كم غيرُه؟**
5. **والبندُ الجانبيّ** (خرائطُ اللوحة في وضع التطوير).

---

## ٠-ف. أين نقف — ٢٠٢٦-٠٩-٠٣ (النقلُ إلى WSL — تُقرأ قبل ما تحتها)

**شجرةُ العمل صارت داخل لينكس: `~/prj/TAXO` في `Ubuntu-24.04`.**
و**`D:\prj\TAXO` باقيةٌ كما هي، لم تُمسّ ولم يُحذف منها شيء** — بقرار المالك،
حتى يقول هو. **والرأسُ في الجديدة `8c36570`، متقدّمةٌ على `D:` بإيداعٍ واحد.**
والشجرةُ نظيفةٌ إلا `?? video-assets/` غيرَ المتعقَّب، كما على `D:` تماماً.
**ولا شيءَ دُفع إلى ريموت.**

### الحكم — رقمٌ واحد

| | زمنُ `pytest` نفسِه | الاختبارات |
|---|---:|---|
| **قبل — `D:\prj\TAXO`** | ٢٣١٥٫٣١ ث = ٣٨:٣٥ | ١٣٥٦ ✅ |
| **بعد — `~/prj/TAXO`** | ٢٠٦٥٫٠٣ ث = ٣٤:٢٥ | ١٣٥٦ ✅ |
| **الفرق** | **−٢٥٠٫٢٨ ث (−١٠٫٨٪)** | صفرُ اختلاف |

**والمكسبُ أقلُّ ممّا وعدت النظرية، ويُقال كما هو.**

> **وسطرٌ في هذا الملفّ كذب، فيُصحَّح**: كُتب أن الكلفةَ المهيمنة «`TRUNCATE`
> لكلِّ اختبارٍ محجوباً **على قرص ويندوز**». **والشقُّ الثاني باطلٌ مقيساً**:
> قاعدةُ التطوير في **مجلَّدٍ مسمّى داخل آلة Docker** (`taxo-app_taxo_pgdata`)
> لا على `D:` — **فلم تعبر `TRUNCATE` جسرَ 9p يوماً**. الذي كان يعبره ربطُ
> `./backend` وحدَه، **وثمنُه أربعُ دقائق لا أربعون**.

**والمكاسبُ الحقيقيةُ في موضعٍ آخر، وكلُّها مقيسة:**

- `npm ci` للثلاثة: **١١ + ٧ + ٦ = ٢٤ ثانية**.
- الحرّاسُ الساكنون كلُّهم: **١٢ ثانية**، `rc=0`.
- بناءُ اللوحة (`DEV_BUILD=1 VITE_API_BASE_URL=…`): **٣٩ ثانية**، `rc=0`،
  و`dist` ٣٫٥ م.ب.
- **وبتُّ التنفيذ صار في الفهرس** — انظر أدناه، فهو أثمنُ من الدقائق الأربع.

### ⚠ وعلاجُ الخادم (أ) قد تمّ — **والتسليمُ كان يقول خلافَه**

**قِيس بدخولٍ فعليٍّ لا بافتراض** (`SSH_OK` · `vmi3521522` · `taxo` · `rc=0`):
`~taxo/.ssh/authorized_keys` **فيه البصمتان معاً** —
`SHA256:p9Nwx/Y58ImBoh21yvm+chqkngSIC9cbGaVRpoL7MNk` (`taxo-contabo`)
و`SHA256:0yjKQGhmjvA1Jw1PikGcwR4+k4LLyzxfvXBTgzKUIcQ` (`taxo-deploy`، مقيَّد).

**فبندُ «تُعاد بصمةُ taxo-contabo» ساقط.** **ومتى عادت؟ لا يُعرف** — وليست
«لم تسقط قطّ»، هي **«لم تُقَس منذ كُتب»**. وهو الشكلُ المسجَّل بعينه: خبرٌ
يُقرأ حالاً قائمةً وقد بلي.

**والعلاجُ (ب) قائمٌ كما هو**: `~/taxo/scripts/deploy-command.sh` =
`-rw-rw-r--` → **غيرُ قابلٍ للتنفيذ** (قُرئ ولم يُكتب).

> **وجذرُه أعمقُ من `chmod`**: **الفهرسُ لم يحمل بتَّ التنفيذ قطّ**، فكلُّ
> سحبٍ يعيده `644` مهما `chmod` أحد. **وأُصلح في `8c36570`** — فأوّلُ سحبٍ
> يحمل ذلك الإيداع يجعله `755` من نفسه، **ولا يحتاج يداً**.

### ما ينتظرك أنت — أربعة

1. **مراجعةُ الوثائق الثمانِ** (البند ١٠) — كما هي، لم تُمسّ.
2. **بيانُ الجهة** (الاسمُ القانونيّ والعنوانُ وبريدُ الخصوصية) — فارغٌ كما هو.
3. **`chmod +x ~/taxo/scripts/deploy-command.sh`** — **أو لا تفعل شيئاً**:
   أوّلُ سحبٍ يحمل `8c36570` يكفيه.
4. **وهجرُ `D:` أو إبقاؤها** — القرارُ لك، وما ينقصها مكتوبٌ تحت.

### ما لم يعد يعمل من داخل لينكس — **مقيسٌ لا مظنون**

| ما | القياس | أين يبقى |
|---|---|---|
| **بناءُ أندرويد** | لا SDK ولا JDK في التوزيعة، و`local.properties` ×٣ تشير إلى SDK ويندوز | **على ويندوز** |
| **الهاتفُ بالـUSB** | `/dev/bus/usb` غيرُ موجود، و`usbipd-win` غيرُ مركّب. و`adb` على ويندوز (خادمُه `tcp:5037`، **ولا جهازَ متّصلٌ الآن**) | **على ويندوز** |
| **جولةُ Playwright البصرية** | لا `msedge` ولا Chrome ولا Chromium في التوزيعة — و`COMMANDS.md` يشترط Edge **المركّب** | **على ويندوز** |
| **نفقُ Cloudflare** | `cloudflared` غيرُ مركّبٍ في التوزيعة، و`cloudflared/config.yml` **المتعقَّب** يحمل `credentials-file: C:\…` | يحتاج مساراً لينكسيّاً |
| **`scripts/pull-backup.ps1`** | PowerShell — **ويندوزيٌّ بقصد لا بالإهمال** | **على ويندوز** |
| `deploy.sh::_pick_ssh` | فرعُ `ssh.exe` صار ميّتاً، **ويسقط إلى `ssh` النظام سليماً** (مقيس) | لا شيء |
| `whatsapp-gateway/node_modules` | لم يُنسخ ولم يُركَّب — **الخدمةُ تُبنى في صورةٍ ولا تُربط من المضيف** | لا شيء |

### وثلاثةٌ قِيست في الطريق — **تُكتب ولا تُصلَح**

1. **`taxo-tunnel` خرج بـ`255`** بعد إعادة آلة WSL، وكان يعمل قبلها. **لم
   يُشخَّص**، وليس من عمل النقل.
2. **`C:\Users\loly3\.wslconfig:22`** فيه مفتاحٌ مجهول `wsl2.autoMemoryReclaim`
   — تحذيرٌ عند كلِّ إقلاع، **ليس من عمل هذه الجولة**.
3. **الثلاثةُ `google-services.json`** في التطبيقات الثلاثة **بالبصمة نفسِها**
   (`a0883fbcb9921e2c`) — ملفٌّ واحدٌ لثلاثة أغلفة. **لم يُسأل أهو مقصود.**

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **بندُ التسهيل** (§39٫١٢ بفروعه الثمانية) — **من داخل `~/prj/TAXO`**، ومنه
   وصلُ `Picker.tsx`، وأوّلُ ما يُستبدل به المنتقي المبنيُّ بيده في
   `RecordModal`.
2. **وعطبُ `wallet_owner_undecided`** — إعلانُ جانب المحفظة في نداء اللوحة.
3. **وتحويلُ حاويات التطبيقات الثلاث لتخدم الشجرةَ الجديدة**: `db` و`redis`
   أُقلعتا من الجديدة، **والثلاثةُ ما زالت تربط `D:`** — **ولذلك قال
   `check:served` «لم يُقَس»** لعمودَي المحلّيِّ والنفق، وهو الحارسُ صادقٌ لا
   عطب.
4. **ومسارُ `credentials-file` في `cloudflared/config.yml`** إن أُريد النفقُ من
   داخل لينكس — **وهو متعقَّبٌ في git**، فتغييرُه إيداع.
5. **والبندُ الجانبيّ** (خرائطُ اللوحة في وضع التطوير) — في جولةٍ لا شيءَ فيها
   غيرُه.

---

## ٠-ع. أين نقف — ٢٠٢٦-٠٩-٠٣ (أربعةُ إيداعات، والباقي بندُ التسهيل — تُقرأ قبل ما تحتها)

**الشجرةُ نظيفة، وآخرُ إيداع `4aa4d28`.** ولا شيءَ غيرَ مودَع سوى
`?? video-assets/` **غيرِ المتعقَّب** — وليس من عمل هذه الدفعة.
**ولا شيءَ دُفع إلى ريموت.**

### ⚠ والجولةُ القادمة نقلُ الشجرة إلى داخل WSL — **قرارُ المالك 2026-09-03**

**لا يُبدأ النقلُ من هنا**: أمرُه يُعطى كاملاً في جلسةٍ نظيفة. **ولم يُنسخ
شيءٌ ولم يُمسّ قرص**.

**ولمَ يُذكر في رأس التسليم**: البنودُ الباقيةُ وعطبُ المحفظة **مؤجَّلةٌ إليه
بقرارٍ صريح** — فمن يفتح جلسةً ويبدأ بندَ التسهيل قبل النقل يخالف أمراً، لا
يُسرِع عملاً.

### أين نقف من الترتيب

**الترتيبُ المقرَّر**: ١ · ٢ · ٤ · ٥ · **٦ · ٨ · ٩ · ١٠ · ١١ · التسهيل** —
والبند ٣ ساقطٌ لأنه مبنيّ، والبند ٧ مؤجَّلٌ حتى تُبنى الدردشة.

| البند | الحال |
|---|---|
| **١٢ · ١ · §34 · ٢ · §39 · ٤ · ٥** | ✅ دفعةُ ٢٠٢٦-٠٩-٠٢ (سبعةُ إيداعات) |
| **٦ — مسارُ الرحلة النشطة** | ✅ `8960375` (§42) |
| **٨ — لوحةُ الإصدارات والتحديثُ الإلزاميّ** | ✅ `ebe87c7` (§43، ترحيلة `0071`) |
| **٩ — الإعلان** | ✅ **ساقطٌ لأنه مبنيّ** — قِيس بالأبواب (§44) |
| **١٠ — السياساتُ والشروط** | ✅ `3a4bdb9` (§45) — **والنصوصُ الثمانِ مسوّدةٌ تنتظر مراجعتك** |
| **١١ — التحكّم والتواصل الفرديّ** | ✅ `4aa4d28` (§46) |
| **بندُ التسهيل** | ⏭ **الباقي وحدَه — ولا يُبدأ قبل النقل** |

**ورقمُ الترحيلة على قاعدة التطوير `0071`**، ودورةُ `downgrade 0071→0070 →
upgrade` خضراء. **ولا ترحيلةَ في البنود ٩ · ١٠ · ١١.**

**والمجموعةُ الكاملة خضراءُ عند كلِّ إيداع**: 1320 · 1336 · 1349 · **1356**،
ومداها 39–47 دقيقة.

### ما ينتظرك أنت — **ثلاثةٌ، وأوّلُها نصّ**

1. **مراجعةُ الوثائق الثمانِ** (البند ١٠): خصوصيةُ الراكب وشروطُه، وخصوصيةُ
   الكبتن وشروطُه — **لكلِّ سوق**. **كلُّها مسوّدةٌ غيرُ منشورة**، وتُقرأ من
   «النظام ← السياسات والشروط». **ومن أين جاء كلُّ بندٍ مكتوبٌ في §45٫٦**
   جدولاً لكلِّ وثيقة. **ولا تُنشر قبل أن يقرأها إنسان** — وهي مسوّدةُ وكيلٍ
   لا نصُّ محامٍ.
2. **بيانُ الجهة** (الاسمُ القانونيّ والعنوانُ وبريدُ الخصوصية) — **فارغٌ
   الآن**، وتذكره الوثيقتان في بندهما الأول.
3. ~~**العلاجان على الخادم**~~ — **زال نصفُه، وكان الحالَ ساعةَ كُتب**
   (يُقرأ مع ٠-ف فوقه): **بصمةُ `taxo-contabo` في `authorized_keys` مقيسةً
   ٢٠٢٦-٠٩-٠٣**، فالبندُ ساقط. **والباقي `chmod +x
   ~/taxo/scripts/deploy-command.sh`** — وجذرُه أُصلح في `8c36570` فيكفيه أوّلُ
   سحبٍ يحمله.

### عطبٌ جانبيٌّ مسجَّلٌ — **مؤجَّلٌ إلى ما بعد النقل بقرارك**

**درجُ الراكب ينادي `GET /admin/wallets/{user_id}` بلا إعلان أيِّ محفظة**،
**فحسابٌ يحمل الدورين يردّ 409 ولا تُحمَّل بطاقةُ المحفظة أبداً**. قِيس على
«عمر الراكب» في قاعدة التطوير (يحمل الدورين):

    409 /admin/wallets/fa743c36… ::
    {"code":"wallet_owner_undecided",
     "message":"لم تُعلَن محفظةُ هذه العملية، والحسابُ يحمل الدورين"}

**والخلفيةُ مُحقّة** — «كودٌ لكلِّ موضع» من نموذج الأدوار (§21/§22)،
**والناقصُ إعلانُ الجانب في نداء اللوحة**. **ولا يُصلَح الآن** (§46٫٦).

### والبندُ الجانبيُّ الأقدم — **وقد اتّسع بالقياس**

**أخطاءُ `map-rtl`/`applyProjectionUpdate` ليست خاصّةً بـ`SkinMapPreview`**:
قِيس `/vehicle-skins` (لم يُمسّ) فأعطى **الأخطاءَ الخمسةَ نفسَها** ومنها
**٤٠٣ من `api.mapbox.com`**، **وكلُّ خريطةٍ في اللوحة تُخرجها في وضع
التطوير**. **والمرجَّح `StrictMode`** يفكّك الخريطةَ قبل أن يكتمل الملحق —
**ويُقاس بإطفائه أو ببناء إنتاج، ولم يُقَس**.

### وما لم يُقَس في هذه الدفعة — **ولا يُقرأ سلامة**

1. **لم تُبنَ حزمةُ أندرويد ولم تُفتح على هاتف**: بوّابةُ التحديث (البند ٨)
   قِيست بمنصّةٍ مزيَّفة في متصفّح، **و`App.getInfo()` الحقيقيُّ لم يُقَس**.
2. **ولا عقدَ FCM على التطوير**: رسالةُ البند ١١ قِيست **صفَّ صندوقِ وارد**،
   لا وصولاً إلى جهاز.
3. **وشارةُ إيقاف حملة تأكيد الأرقام رُسمت ولم تُرَ لحسابٍ موقوف** — لا حساب
   كذلك على التطوير، والحملةُ لم تُطلق قطّ.
4. **ولم يُقَس شيءٌ على الإنتاج**: جداولُ السياسات هناك فارغةٌ كما كانت،
   **ولا يُشغَّل بذرُ المسوّدات عليها إلا بإذنٍ ومعه نسخة**.

### وحالُ قاعدة التطوير بعد القياس

- **ثمانِ مسوّداتِ سياساتٍ ولا منشورةَ فيها** — نُشرت واحدةٌ أثناء القياس ثمّ
  **سُحبت**.
- **لا صفَّ إصداراتٍ البتّة** (`app_releases` فارغ): صفٌّ باقٍ كان يفرض تحديثاً
  على حزمةِ قناةٍ تجريبيةٍ في أوّل جولةِ هاتف.
- **لافتتان للقياس مطفأتان** («قياسُ البند ٩ …») — لا تُحذفان لأن ما عُرض لا
  يُحذف، **وهو الحارسُ يعمل**.
- **ورحلةٌ جاريةٌ لزيد السائق** بقيت من قياس البند ٦.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **النقلُ إلى داخل WSL** — أمرُه من المالك، **ولا يُبدأ قبله**.
2. **ثمّ بندُ التسهيل** (§39٫١٢ بفروعه الثمانية) — **ومنه وصلُ `Picker.tsx`**،
   وأوّلُ ما يُستبدل به المنتقي المبنيُّ بيده في `RecordModal`.
3. **وعطبُ `wallet_owner_undecided`** — إعلانُ جانب المحفظة في نداء اللوحة.
4. **والعلاجان على الخادم** حين يفعلهما المالك — ويُتحقَّق بدخولٍ فعليّ.
5. **والبندُ الجانبيّ** (خرائطُ اللوحة في وضع التطوير) — في جولةٍ لا شيءَ فيها
   غيرُه.

---

## ٠-س. أين نقف — ٢٠٢٦-٠٩-٠٢ (دفعةُ اللوحة، سبعةُ إيداعات — تُقرأ قبل ما تحتها)

> ### ✅ زال — **كان الحالَ ساعةَ كُتب، ولم يعد** (يُقرأ مع ٠-ع فوقه)
>
> **الرأسُ يومَها `e439cdf` والتالي البند ٦.** ومنذ ذلك اليومِ أُودعت أربعةٌ:
> `8960375` (٦) و`ebe87c7` (٨) و`3a4bdb9` (٩ و١٠) و`4aa4d28` (١١) —
> **والباقي بندُ التسهيل وحدَه**. وسؤالُ الوتيرةِ المسجَّلُ تحته **أُجيب**:
> «امضِ كما أنت — بند فمجموعةٌ كاملةٌ فإيداع».

**الشجرةُ نظيفة، وآخرُ إيداع `e439cdf`.** ولا شيءَ غيرَ مودَع سوى
`?? video-assets/` **غيرِ المتعقَّب** — **ولم يدخل أيَّ إيداعٍ من السبعة**
(مقيسٌ: صفرُ ذكرٍ له في `git log --stat`)، وليس من عمل هذه الدفعة.

**ولا شيءَ دُفع إلى ريموت.**

### أين نقف من الترتيب

**الترتيبُ المقرَّر**: ١ · ٢ · **٤ · ٥ · ٦ · ٨ · ٩ · ١٠ · ١١ · التسهيل** —
**والبند ٣ ساقطٌ لأنه مبنيّ**، **والبند ٧ (غرفُ الدردشة) مؤجَّلٌ بقرارٍ حتى
تُبنى الدردشةُ نفسُها**.

| البند | الحال |
|---|---|
| **١٢ — بحثُ الخادم** | ✅ مودَع (`cf2eadb` أكمل وصلَه في اللوحة) |
| **١ — الملفُّ الشخصيُّ الكامل** | ✅ `f8bbc7a` |
| **§34 — شكلُ الوثائق (`0068`)** | ✅ `dd67f53` |
| **٢ — إلغاءُ الاشتراك (`0069`)** | ✅ `0910b03` |
| **§39 — نصوصُ البنود** | ✅ `cd09898` |
| **٤ — التعديلُ والحذف** | ✅ `1467d6d` (§40) |
| **٥ — مصفوفةُ الصلاحيات (`0070`)** | ✅ `e439cdf` (§41) |
| **٦ — مسارُ الرحلة النشطة في ملفِّ الكبتن** | ⏭ **التالي — ولم يبدأ** |
| **٨ · ٩ · ١٠ · ١١ · التسهيل** | لم تبدأ |

**ورقمُ الترحيلة على قاعدة التطوير `0070`** — والثلاثةُ (`0068` · `0069` ·
`0070`) **شُغِّلت ومُقيست**، ودورةُ `downgrade base → upgrade head` خضراء.

### ⚠ ونصوصُ البنود كلُّها صارت في `SPEC.md` §39 — **فلا تُطلب من المالك ثانيةً**

**وهذا هو الدرسُ الذي دفعنا ثمنَه**: ضاعت نصوصُ ستّةِ بنودٍ لأنها قيلت في
محادثةٍ ولم تُكتب في ملفّ. **وهي اليومَ مكتوبةٌ بفروعها**: ٤ (ومعه **قائمةُ
ما لا يُحذف**) · ٥ (**الإحدى عشرة** وثلاثتُها المحروسة) · ٦ · ٨ · ٩ · ١٠ ·
١١ · **وبندُ التسهيل بثمانيةِ فروعه**.

**فمن يفتح جلسةً جديدةً يقرأ §39 ويمضي** — ولا يسأل المالكَ عمّا أجاب عنه.

### البندُ الجانبيُّ المفتوح — **يُقرأ ولا يُصلَح في هذه الدفعة**

**شاشةُ «مركبات المتجر»** (`/vehicle-skins`) تُخرج خطأين في سجلّ المتصفح،
مصدرُهما `SkinMapPreview`:

    [map-rtl] تعذّر تحميل ملحق العربية: AbortError: Actor removed
    TypeError: Cannot read properties of undefined (reading 'applyProjectionUpdate')

**وليس من عمل هذه الدفعة**: لم يُمسّ أيُّ ملفِّ خريطة، **والشاشاتُ الأخرى
المقيسةُ أعطت صفرَ خطأ**. **وتفصيلُه في `STATE.md`** ومعه أوّلُ سؤالٍ يُسأل
قبل أن يُتَّهم الكود: `AbortError: Actor removed` يقع حين **يُفكَّك المكوّنُ
قبل أن يكتمل تحميلُ ملحق RTL** — فالسؤالُ «أيقع مع معاينةٍ تبقى مفتوحة؟».

### ما ينتظر إصبعَ المالك — **العلاجان على الخادم**

**ولا يُنفَّذان من جهاز التطوير، مقيساً**: مفتاحُ النشر **يبتلع أيَّ أمرٍ
يُرسَل** (أمرٌ مفروضٌ في `authorized_keys`)، **وهو نفسُه مكسور**.

- **أ) تُعاد بصمةُ `taxo-contabo` العامّة إلى `~taxo/.ssh/authorized_keys`** —
  `SHA256:p9Nwx/Y58ImBoh21yvm+chqkngSIC9cbGaVRpoL7MNk`، والعامُّ في
  `~/.ssh/taxo-contabo.pub` على جهاز المالك.
- **ب) `chmod +x ~/taxo/scripts/deploy-command.sh`** — رمزُ `126` («وُجد ولا
  يُنفَّذ»)، **وهو يُعطّل سحبَ الإصدار في البوّابة الرابعة**.

**والضابطُ الذي حسم**: مفتاحُ النشر يصادق على المضيف والمنفذ والمستخدم
أنفسِهم، **فالشبكةُ والجدارُ والاستدعاء سليمة** — **والعطبُ في المفتاح لا في
طريقة الاستدعاء**، و`SSH_ASKPASS` لا يغيّر شيئاً (العبارةُ تُطلب **بعد** أن
يقبل الخادمُ العامَّ، والخادمُ رفض قبل ذلك).

**وسؤالان لا يُجابان من هنا**: **كيف سقطت البصمة؟ لا يُعرف.** **وأفيه مفاتيحُ
أخرى؟ لا يُعرف** — وليست «لا»، هي «الملفُّ غيرُ مقروء».

### وسطرٌ عن وتيرة العمل — **قرارٌ للمالك لا للوكيل**

**كلُّ بندٍ يكلّف مجموعةً كاملة**: شُغِّلت اليومَ **تسع مرّات**، ومداها
**٣٥–٤٩ دقيقة** لكلِّ مرّة (والكلفةُ المهيمنة `TRUNCATE` لكلِّ اختبارٍ محجوباً
على قرص ويندوز، **لا المعالج**).

**والشرطُ يبقى كما هو**: «لا يُودَع فوق مجموعةٍ لم تنتهِ». **والسؤالُ المعروض
على المالك**: أيُؤذن بجمع البنود التي **لا تمسّ الخلفية** في مجموعةٍ واحدةٍ
قبل إيداعها، بدل مجموعةٍ لكلِّ بند؟ **ولم يُجب بعد، والافتراضُ المضيُّ كما
هو** — بندٌ، فمجموعةٌ كاملة، فإيداع.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **البند ٦** — مسارُ الرحلة النشطة في ملفِّ الكبتن، **على بثِّ الموقع
   القائم** (`GET /admin/live/map` و`ride_route_points`) **لا على بنيةٍ جديدة**.
2. **ثمّ ٨ · ٩ · ١٠ · ١١ · التسهيل** بترتيبها — **ونصوصُها في §39**.
3. **وبندُ التسهيل يُبنى مع كلِّ شاشةٍ تُلمس لا دفعةً بعدها** — ومنه
   **وصلُ `Picker.tsx`**، **وأوّلُ ما يُستبدل به المنتقي المبنيُّ بيده في
   `RecordModal`** (شاشة الاشتراكات).
4. **العلاجان على الخادم** حين يفعلهما المالك — **ويُتحقَّق بدخولٍ فعليٍّ لا
   بافتراض**، وتُطبع الصلاحياتُ والبصمات.
5. **والبندُ الجانبيّ** (`SkinMapPreview`) — في جولةٍ لا شيءَ فيها غيرُه.

---

## ٠-ن. أين نقف — ٢٠٢٦-٠٩-٠٢ (دفعةُ اللوحة، تُقرأ قبل ما تحتها)

> ### ✅ زال — **كان الحالَ ساعةَ كُتب، ولم يعد** (يُقرأ مع ٠-س فوقه)
>
> **كُتب حين كانت الشجرةُ تحمل عملَ جولةٍ لم يُودَع** والرأسُ `45d0c4d`.
> **وقد أُودع كلُّه**: المجموعةُ الكاملةُ خضرّت ثمّ أُودعت الثلاثةُ، وتلتها
> أربعةٌ — والرأسُ اليومَ `e439cdf` **والشجرةُ نظيفة**.
>
> **ويُصحَّح في موضعه ولا يُترك**: «خبرٌ في هذا الملفّ يُقرأ حالاً قائمة —
> فيحمل حالَه وتاريخَه أو يُحذف»، **وتحذيرٌ باقٍ بعد زوال سببه يوقف من يقرؤه
> عن عملٍ لا مانعَ منه**.

### ما بُني وقِيس

**١) البند ١ — الملفُّ الشخصيُّ الكامل** (المواصفةُ في `SPEC.md` §37): أقسامٌ
**على `DriverDrawer` و`RiderDrawer` القائمين لا بجانبهما**، تُفتح بطلبٍ، وكلُّ
قسمٍ يقرأ **من بابِ مفهومه** بالتزام §5-ج — **ولا بابَ جامعٌ في الخلفية**،
لئلا يصير مصدراً ثانياً لِما له مصدر.

**وقِيس من DOM في متصفّحٍ حقيقيّ** (Edge على `127.0.0.1:5175`، لا لقطة):

    ✓ درجُ الكبتن: الحساب · المركبة · الاشتراك · المحفظة والدفتر ·
                   ما عليه (السلف والمستحقّات) · آخرُ رحلاته كبتناً · رسومُ الإلغاء
                   — ومعها الوثائقُ وتوثيقُ الجنس وسقفُ السلفة كما كانت
    ✓ درجُ الراكب: الحساب · آخرُ رحلاته راكباً · رسومُ الإلغاء
                   — ومعها المحفظةُ بزرِّ تجميدها كما كانت
    ✓ ولا مركبةَ ولا «ما عليه» في درج الراكب      (مقيسٌ بالنفي)
    ✓ ولا قسمَ يقول «تعذّرت قراءة هذا القسم»      ✓ صفرُ خطأٍ في سجل المتصفح

**وبابان جديدان بزرَّيهما** — و`check:doors` **١٦٨** مساراً (كانت ١٦٦):
`GET /admin/drivers/{id}/vehicles` (**ولم يكن للوحة بابٌ يقرأ مركبةَ كبتنٍ
قطّ**) و`GET /admin/users/{id}` (وصفُّ الكبتن بلا `is_blocked`، **فكان درجُ
كبتنٍ محظورِ الحساب لا يقول ذلك**).

**وثلاثةُ مرشِّحاتٍ بالمعرِّف على أبوابٍ قائمة**: `driver_id` على السلف
والمستحقّات، و`user_id` على رسوم الإلغاء (الطرفان معاً).

**٢) وصلُ حقل البحث — ١٢ سطحاً، فصارت الأربعَ عشرةَ قائمةً كلُّها موصولة**
(كانت اثنتين). **وليست إحدى عشرة كما قيل في التسليم السابق**: الدفعاتُ
والنزاعاتُ **شاشتان على بابٍ واحد**، والماليةُ **حقلان لا واحد**.

**وقِيس السلوكُ لا الشكل** على سجلّ الرحلات — الوحيدُ الذي **استُبدل** نموذجُه:

    صفوفُ السجل قبل البحث: 51 · بعد نصٍّ لا يطابق أحداً: 1
    ✓ النداءُ حمل `q`   ✓ الجدولُ تقلّص   ✓ «لا نتائج لبحثك» لا «لا بيانات»

**وحقلان لم يُوصَلا بعللهما**: مطالباتُ كليك ومطالباتُ السداد — **بابهما لا
يقبل `q`**، وحقلٌ يزيّن ولا يرشِّح أسوأُ من غيابه.

**٣) §34 والترحيلة `0068`** — الثلاثةُ سُدَّت **في الشكل قبل أن يُبنى عليه**:
`doc_type` · `app` (**بلا مشرف**) · `min_accepted_version`، والفهرسُ الجزئيُّ
والقيدُ الفريدُ صارا على (سوق + نوع + تطبيق). **وشُغِّلت على قاعدة التطوير
ومُقيست**:

    doc_type | policy_doc_type | not null        app | policy_app | not null
    min_accepted_version | integer | not null
    privacy_policy_one_published UNIQUE (country_code, doc_type, app) WHERE is_published
    privacy_policy_version       UNIQUE (country_code, doc_type, app, version)
    رقمُ الترحيلة على قاعدة التطوير: 0068

**والترحيلةُ تعدّ الصفوفَ ساعةَ التشغيل وتقف بصياحٍ مسمّى** (شرطُ المالك)،
**وحارسُها مقيسٌ بالنفي**: اختبارٌ ينزل إلى `0067`، **يزرع صفّاً**، ويشترط
الوقوفَ **بنصِّه**.

**وأرقامُ §34**: `privacy_policies` · `user_policy_consents` · `org_profile` =
**٠ · ٠ · ٠** على التطوير، **وصفرُ كاتبٍ في الشجرة كلِّها**.

**٤) وثلاثةُ عطبٍ من إيداع أمس (`45d0c4d`) قِيست وأُصلحت**: بحثٌ يقارن
`users.id` بعمودٍ يشير إلى `drivers.id` في **الدفعات وطلبات الصرف ورسوم
الإلغاء** (**يُسقط نصفَ البحث صامتاً**) · و`AdminAdvanceOut` بلا اسمِ صاحبها
فكانت اللوحة تعرض ثماني خاناتٍ من UUID · و`METHOD_LABEL` مكتوباً **ثلاث
مرّاتٍ وافترق فعلاً**، فصار بيتُه `admin-panel/src/lib/labels.ts`.

**والحرّاسُ الساكنون خضر** (`scripts/guards.sh` شُغِّل)، **وبناءُ اللوحة أخضر**
و`check:served` طابق الأعمدةَ الثلاثة.

### ⚠ وما لم يُقَس — ولا يُقرأ سلامة

**المجموعةُ الكاملةُ لم تنتهِ على الشيفرة الحالية.**

- **جرت مرّةً كاملة**: `1 failed, 1294 passed in 2110.61s (0:35:10)`.
- **والسقطةُ كانت لي**، وأمسكها حارسُ البند ٨:
  `test_no_shortcut_fixtures::test_no_test_builds_a_money_row_by_hand` —
  `test_admin_profile.py:108  DriverSubscription(…)`.
- **وأُصلحت بالطريق الذي يسمّيه الحارسُ نفسُه في رأسه** («ما يُسمح به بعلّته:
  بناءُ صفٍّ مباشرةً حين يكون موضوعُ الاختبار **العرضَ** لا المسار»): أُضيف
  المفتاحُ `test_admin_profile.py:108` إلى `_ACCEPTED` بعلّته المكتوبة،
  **ولم يُوسَّع الحارسُ ولم يُطفأ**. والسابقةُ حرفيّةٌ في القائمة نفسِها:
  `test_advances.py:114`.
- **ثمّ شُغِّل الحارسُ ومعه ما مُسّ**: `test_no_shortcut_fixtures` +
  `test_admin_profile` + `test_migrations` + `test_admin_search` →
  **٤٣ خضراء في ٥:٢٣**.
- **والإعادةُ الكاملةُ بدأت ثمّ أوقفها المالك.**

> **فـ«١٢٩٤ خضراء والسقطةُ أُصلحت» استنتاجٌ لا قياس** — ولا يُودَع عليه.

**وسطران يخصّان القياسَ نفسَه، لا هذه الدفعة**: **زمنُ المجموعة الفعليُّ ٣٥
دقيقة** لا الساعاتُ التي تبدو على الساعة — الفارقُ إقلاعُ الحاوية وتهيئةُ
القاعدة؛ **وكلفتُها المهيمنةُ `TRUNCATE` لكلِّ اختبار** محجوبةً على
`DataFileImmediateSync`: قِيس أن حاويةَ المجموعة عند **0.00% معالج** بينما
القاعدةُ تنفّذ **٣٫٦ معاملةٍ في الثانية** — **فهي مقيَّدةٌ بقرص ويندوز لا
بالمعالج**.

**⚠ وفخُّ المقاطعة وقع مقيساً**: قتلُ عميل compose **لا يقتل الحاوية**، فبقيت
`taxo-suite-run-7885` ماسكةً `taxo_test`. **أُزيلت** والاتصالاتُ صارت صفراً —
ومن وجد تشغيلاً يسقط بألفِ خطأٍ متكرِّرٍ عند التهيئة **فليقرأ هذا السطرَ قبل
أن يتّهم الكود**.

### الفعلُ الوحيدُ الباقي قبل الإيداع

**تشغيلُ المجموعة كاملةً حتى تخضرّ** — ولا شيءَ غيرُه. فإن خضرّت، تُودَع
**ثلاثةُ إيداعاتٍ مسمّاة**؛ **ولمَ ثلاثةٌ لا اثنان**: `0068` **شكلُ قاعدةٍ لا
يخصّ دفعةَ اللوحة**، وخلطُه بها يجعل الرجوعَ عن أحدهما رجوعاً عن الآخر.

1. **وصلُ حقل البحث في القوائم الباقية — وبيتٌ واحدٌ لأسماء الحالات.**
2. **البند ١ — الملفُّ الشخصيُّ الكامل، ونصفُ بحثٍ كان لا يبحث.**
3. **§34 — شكلُ الوثائق يُسدُّ قبل أن يُبنى عليه، والترحيلةُ تقيس ولا تفترض.**

### وما في الشجرة غيرُ مودَع، بالضبط

**خمسةٌ جديدة:**

| جديد | ما هو |
|---|---|
| `backend/alembic/versions/0068_policy_shape.py` | ترحيلةُ §34 وحارسُها |
| `backend/tests/test_admin_profile.py` | اختباراتُ البند ١ (٥) |
| `admin-panel/src/components/Profile.tsx` | أدواتُ الأقسام: طيٌّ · قراءةٌ عند الفتح · قوائمُ مصغَّرة |
| `admin-panel/src/components/profile/` | `Sections.tsx` المشتركة و`DriverSections.tsx` |
| `admin-panel/src/lib/labels.ts` | بيتُ أسماء الحالات ونغماتها |

**والمعدَّل**: `SPEC.md` (§34 و§37) · `STATE.md` · ثمانيةُ ملفاتِ خلفية
(`services/admin_search` · `services/payments` · `services/withdrawals` ·
`routers/admin_users` · `routers/admin_cancellations` · `schemas/driver` ·
`models/enums` · `models/privacy`) · ثلاثةُ ملفاتِ اختبار
(`test_admin_search` · `test_migrations` · `test_no_shortcut_fixtures`) ·
و**١٦ ملفَّ لوحة**.

> **و`video-assets/` غيرُ متعقَّبٍ وليس من هذه الجولة** — ٢٠٧ ملفاً تحت
> `wan-frames`، **لم يمسسها هذا العمل**، وتُترك كما هي.

### ما ينتظر إصبعَ المالك

**١) علاجان على الخادم — ولا يُنفَّذان من جهاز التطوير، مقيساً.**

| المفتاح | ما قِيس |
|---|---|
| `taxo-contabo` (بعبارة) | يُعرَض عامُّه ثمّ `Permission denied` — **ولا سطرَ «Server accepts key»** |
| `taxo-deploy` (بلا عبارة) | `Server accepts key` → `Authenticated` ✓ ثمّ `deploy-command.sh: Permission denied` |

**والضابطُ حسم**: مفتاحُ النشر يصادق على المضيف والمنفذ والمستخدم أنفسِهم —
**فالشبكةُ والجدارُ والاستدعاء سليمة**. **وعرضُ العامِّ لا يحتاج العبارةَ
أصلاً** (تُطلب **بعد** أن يقبل الخادمُ العامَّ ليُوقَّع التحدي)، **والخادمُ
رفض قبل ذلك** — فـ`SSH_ASKPASS` لا يغيّر شيئاً، **والعطبُ في المفتاح لا في
الاستدعاء**.

- **أ) تُعاد بصمةُ `taxo-contabo` العامّة إلى `~taxo/.ssh/authorized_keys`** —
  البصمةُ `SHA256:p9Nwx/Y58ImBoh21yvm+chqkngSIC9cbGaVRpoL7MNk`، والعامُّ في
  `~/.ssh/taxo-contabo.pub` على جهاز المالك.
- **ب) `chmod +x ~/taxo/scripts/deploy-command.sh`** — رمزُ `126` («وُجد ولا
  يُنفَّذ»). **ومفتاحُ النشر لا يستطيع اليوم شيئاً على الإطلاق**: أمرٌ من
  عندي (`id`) وأمرُه المفروض (`pull-release v9.9.9`) يعطيان **المخرَجَ نفسَه
  حرفاً** — فالأمرُ المفروضُ يبتلع ما يُرسَل قبل أن يُقرأ. **وهذا يُعطّل سحبَ
  الإصدار في البوّابة الرابعة.**

**ولمَ لم يُنفَّذا من هنا**: العلاجان دورةٌ مغلقة — الأولُ يحتاج صدفةً،
والصدفةُ تحتاج المفتاحَ الذي يُصلحه الأول. **والطريقُ الباقي استغلالُ مسار
`pull-release` ليكتب على القرص، وذاك تجاوزُ حدٍّ أمنيٍّ بناه المالكُ قصداً** —
تمنعه المطلقةُ الثالثة.

**وسؤالان لا يُجابان من هنا**: **كيف سقطت البصمة؟ لا يُعرف** — الملفُّ غيرُ
مقروءٍ ولا أثرَ في المستودع، **ولا يُرجَّح**. **وأفيه مفاتيحُ أخرى؟ لا يُعرف
كذلك** — وليست «لا»، هي «الملفُّ غيرُ مقروء». ويجيبهما على الخادم:
`ssh-keygen -lf ~/.ssh/authorized_keys` و`ls -ld ~/.ssh`.

**٢) قرارُ البند ٢ — إلغاءُ اشتراكٍ من اللوحة.**

**والسؤالُ محورانِ لا واحد، وكلاهما مال:**

- **المدّةُ المتبقية**: أتُهدر، أم تُردّ إلى المحفظة بالتناسب، أم تبقى ساريةً
  إلى نهايتها والإلغاءُ يمنع التجديد وحدَه؟
- **والعمولةُ المجمَّدة** (`drivers.commission_percent_from_subscription`،
  §25.11): أتعود إلى نسبة السوق فوراً، أم تبقى مجمَّدةً إلى نهاية المدّة
  المدفوعة؟

**ولم تُعرض الخياراتُ بكلفتها بعد** — لأن البند لم يُبلَغ، وشرطُ المالك أن
تُعرض حين يُبلَغ. **ولم يُختَر شيء، ولم يُبنَ على ترجيح.**

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **المجموعةُ الكاملة حتى تخضرّ**، ثمّ **الإيداعاتُ الثلاثة** بترتيبها أعلاه.
2. **البند ٢** — تُعرض خياراتُه بكلفتها على المالك، **ولا يُختار**.
3. **ثمّ ٤ ثمّ ٥ ثمّ ٦** من دفعة اللوحة بترتيبها المقرَّر
   (١ · ٢ · ٤ · ٥ · ٦ · ٨ · ٩ · ١٠ · ١١ · ١٢).
4. **و`Picker.tsx` و`Inputs.tsx` يُوصَلان مع البند ٢ و٤** كما ينصّ `STATE.md`
   — **ولا يُبنى منتقٍ ثانٍ ولا حقلُ مالٍ ثانٍ**. **وفي شاشة الاشتراكات منتقٍ
   مبنيٌّ بيده** (نموذجُ بحثٍ بزرِّ «ابحث» في `RecordModal`) **هو أوّلُ ما
   يُستبدل به**.
5. **العلاجان على الخادم** حين يفعلهما المالك — **ويُتحقَّق بدخولٍ فعليٍّ لا
   بافتراض**، وتُطبع الصلاحياتُ والبصمات.

---

## ٠-م. أين نقف — ٢٠٢٦-٠٩-٠١ (جولةُ كليك، تُقرأ قبل ما تحتها)

**البنودُ الستّةُ بُنيت إلا غلافَ المشرف** — وما تحته كلُّه قائم: القناةُ
الخامسة، وحارسُها المقيس، وصفحةُ المدفوعات، والإشعار.

### والمرجعُ لم يُمسّ — **والقراءةُ قرّرت**

المطابِقُ اليومَ **`provider_orders.cart_id`** وهو **نفسُه المرجعُ المعروض**،
عبر `GET /admin/cliq-claims`. **وهو كافٍ** — فريدٌ بقيدٍ في القاعدة ويحلّ إلى
مستخدمٍ واحد. **والعطبُ كان في العرض**: الحمولةُ تحمل `user_id` **والجدولُ لا
يعرضه**، فصار يحمل **الاسمَ والرقم**.

**والأرقام**: سقفُ **٢٥** حرفاً (EMVCo 62/05، وهو طولُ العمود نفسِه)،
و`cart_id` **١٩** — **فالمتّسع ٦**. والاسمُ الأولُ **خارجٌ بالطول قبل
الخصوصية**، **ولا «رقمَ حساب» في النظام أصلاً**.

### وأجوبةُ أسئلة تطبيق المشرف — الأربعة

**١) المسارات في القائمة الأولى** — ولا تُنقل اللوحةُ كلُّها:

| # | المسار | لمَ |
|---|---|---|
| 1 | **`/m/payments`** ✅ **مبنيّ** | ما يوقظه إشعارٌ ويحتاج جواباً في دقائق |
| 2 | `/m/claims` | مطالبةُ الدَّين والشحن اليدويّ — الجنسُ نفسُه |
| 3 | `/m/withdrawals` | صرفُ الكبتن ينتظر إذناً، ومالٌ ينتظر لا يُؤجَّل لمكتب |
| 4 | `/m/drivers/pending` | اعتمادُ كبتنٍ جديد — قرارٌ بنعم/لا وصورةٌ تُفتح |
| 5 | `/m/disputes` | نزاعٌ مفتوحٌ يعطّل طرفين |

**وما لا يُنقل**: الخريطةُ الحيّة · التسعيرُ والإعدادات · التقارير ·
العروضُ والحملات — **قراراتٌ تُتّخذ على مهلٍ أمام شاشةٍ عريضة، ونقلُها إلى
هاتفٍ يدعو إلى تعديل تسعيرةٍ في طابور**.

**٢) وكيف تُفصل — مسارٌ في اللوحة بتخطيطٍ ثانٍ، لا مشروعٌ رابع.** والاختيارُ
بُني عليه فعلاً: **كلُّ شاشةٍ في اللوحة ترسم `<Shell>` بنفسها، فالفصلُ أن لا
تُرسم** — بلا بنيةٍ جديدةٍ ولا شرطٍ في الغلاف. **وهو ما يقلّ افتراقاً بعد
شهر**: مصدرٌ واحدٌ وبناءٌ واحدٌ ونشرٌ واحد، **والجلسةُ والصلاحياتُ وعقدُ
الأخطاء وسلّمُ التصميم مشتركةٌ لا منسوخة** — وهي بعينها ما ينسخه المشروعُ
الرابعُ ثم يفترق.

**٣) وما يلزمك بيدك — تسجيلُ Firebase بحرفه.** أضِف **تطبيقَي أندرويد**
بالنكهتين كما فعلتَ من قبل:

    ly.tajora.admin          ← النكهةُ العامّة (public)
    ly.tajora.admin.test     ← النكهةُ التجريبية (trial)

وحمّل `google-services.json` لكلٍّ منهما. **ولا شيءَ غيرَهما.**

**٤) والإشعار — تسجيلُ الجهاز من الغلاف يكفي، مقيساً.** `PUT /devices`
حارسُه `CurrentUser` **بلا شرطِ دور** — فحسابُ المشرف يسجّل جهازَه كما يسجّل
الكبتن، بلا بابٍ جديدٍ ولا نظامِ أذوناتٍ ثانٍ. **والإرسالُ مبنيٌّ اليومَ**
ويكتب صفَّ صندوق الوارد **ولو لا جهازَ مسجَّلاً** — فما ينقص هو الغلافُ وحدَه.

### ما ينتظر إصبعَ المالك

1. **تسجيلا Firebase** أعلاه — بالحرف.
2. **افتح `/m/payments` على هاتفك** بعد الدخول — تُقرأ وتُضغط بإصبع.
3. **جولةُ اللوحة والحملة** — لم تُفتح في متصفّح بعد (TOTP بيدك).
4. **عقدُ Resend** — «ليس الآن».

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **غلافُ المشرف**: `capacitor.config.ts` يقرأ `admin-panel` من الجدول ·
   نكهةُ gradle · الأيقونة · وتسجيلُ الجهاز بعد الدخول.
2. **المطالبةُ اليدويّةُ للراكب** إلى صفحة المدفوعات — جدولٌ ثانٍ
   (`wallet_topup_requests`) وقائمةٌ واحدة.
3. **بقيّةُ مسارات `/m/*`** بترتيبها أعلاه.
4. **جولةُ الهاتف** لِما بُني: شاشةُ المطالبة وصفحةُ المدفوعات.
5. **الأبوابُ الـ٤٦ غيرُ المطروقة** — تُبدأ بعائلة النسخ الاحتياطية.

---

## ٠-ل. أين نقف — ٢٠٢٦-٠٨-٣١ (الجلسة الرابعة، تُقرأ قبل ما تحتها)

**الحملةُ بُنيت ولم تُطلق. والبريدُ واللوحةُ تمّا قبلها. وFastSpring رُفضت
وكُتبت. والملفُّ حُذف ولم يدخل إيداعاً.**

### قراءةُ الإنتاج التي غيّرت التصميم

**رقمُه غير مؤكَّد على الإنتاج: صفر** — 3 ركّاب و4 كباتن كلُّهم مؤكَّدون،
**والوحيدان حسابا مشرف** لا رقمَ لهما ويدخلان باسمِ مستخدم.

> **ولولا القراءةُ لَبُني عطب**: حملةٌ لا تستثني الموظّفين **توقف اللوحةَ عن
> نفسها** — من يفكّ الإيقافَ يحتاج لوحةً، واللوحةُ موقوفة. **فصار النطاقُ
> ثلاثةَ شروطٍ لا شرطاً.**

### الحملةُ — ما بُني

`0066` بجدولَيها وعمودِ الإيقاف · **حالٌ مستقلّةٌ عن `is_blocked`** ·
النصوصُ الثلاثةُ بلفظ المالك **بمدّةٍ محقونةٍ من الحقل** · سببُ الإيقاف
**منشورٌ من بيتٍ واحد** · **التجمّدُ والاستئنافُ آليّان والمهلُ تُمدَّد بما
تجمّدت** · **الفكُّ بتأكيد الرقم وحدَه** من الباب نفسِه · **الاشتراكُ يُمدَّد
والمحفظةُ لا تُمسّ** · أربعةُ أبوابٍ **بلا بابِ فكٍّ يدويّ** · قسمٌ في اللوحة
**يعرض `scope_size` على الزرّ ويستأذن مرّتين** · **١٦ اختباراً**.

**ولم تُطلق** — الزرُّ بيد المالك، والنطاقُ اليومَ صفر.

### وكُتبت في `SPEC.md` ثلاثةُ أقسامٍ جديدة

**§31 البريد · §32 الحملة · §33 FastSpring** — وذلك قبل الإيداع كما تفرض
القاعدة: «ما بُني ولا تصفه المواصفةُ يصير فرقاً لا يُميَّز عن عطب».

### ما ينتظر إصبعَ المالك

1. **زرُّ إطلاق الحملة** — في «الإعدادات» بعد اختيار السوق. ولا تُطلق بغيره.
2. **افتح اللوحةَ مرّةً** (TOTP بيده): جولةُ المتجر · وقسمُ الحملة.
3. **عقدُ Resend** — «ليس الآن: أفتح الحساب وأحقّق النطاق ثم أقول».
4. **`CLAUDE.md` على 57,844 — ١٥٦ من ٥٨,٠٠٠.** وحين تبلغها: **يُنفَّذ النقلُ
   بلا سؤال** — قسمُ `Commands` إلى **ملفٍّ خامسٍ بشرطه**، بالصرامة نفسِها:
   نقلٌ لا تحرير · الرقمان مطبوعان · دفعةٌ واحدةٌ مسمّاة · وبصمةُ الأسطر
   مرتَّبةً كما في المرّة الأولى.
5. **سقفُ الدَّين** — يبقى `NULL` كما قرّر، ولا رقمَ اليوم.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **جولةُ اللوحة والحملة في متصفّح** بعد أن يفتحها المالك — DOMاً لا لقطة.
2. **الأبوابُ الـ٤٦ غيرُ المطروقة** — تُبدأ بعائلة النسخ الاحتياطية الثمانية.
3. **شاشةُ التسجيل بالبريد** في التطبيقين — حين يأذن بالعقد.
4. **`otp.py`: `phone` → `subject`** — دَينٌ مكتوبٌ بتاريخه، في جولةٍ لا شيءَ
   فيها غيرُه.
5. **مراقبةُ `CLAUDE.md`** — وتنفيذُ النقل عند بلوغ ٥٨,٠٠٠ بلا سؤال.

---

## ٠-ك. أين نقف — ٢٠٢٦-٠٨-٣١ (الجلسة الثالثة، تُقرأ قبل ما تحتها)

**جولةُ اللوحة تمّت، والبريدُ بُني خلفيّةً وتطبيقين. والحملةُ لم تُبدأ —
بسؤالٍ واحدٍ ينقصها، مكتوبٌ أدناه.**

### FastSpring — **قُرئت ورُفضت** (قرارُ المالك)

**لا تُبنى: لا الاشتراكُ ولا غيرُه.** والقراءةُ كاملةً بالبنود والأرقام في
`STATE.md`، ومعها **الأربعةُ التي تُقاس بها أيُّ بوّابةٍ قادمة**: أتقبل
الدينارَ عرضاً **وصرفاً** · أتسمح بالقيمة المخزَّنة · أنسبتُها منشورةٌ برقم ·
متى يصل المال. **ويبقى Telr كما هو، وكليك اليدويُّ هو الطريقُ العامل.**

### `fastspring.txt` — **حُذف، ولم يدخل أيَّ إيداع**

قِيس على المراجع كلِّها قبل الحذف: **لا سجلَّ للملفّ في أيِّ إيداع، ولا أثرَ
للسلسلة في أيِّ blob**. فهو حذفٌ لا خبر. **وأُضيف نمطُه إلى `.gitignore`**
(`/fastspring*` ومعه `/*credential*` و`/*.secret` وأخواتُها) **ومُحقٌّ بملفٍّ
وهميّ**.

### أبوابٌ لا يطرقها اختبار — **عُدَّت ولم تُصلَح**

**٤٦ من ٢٨٨** (و**٣٧ من ١٦٠** أبوابِ لوحة). وأثقلُها عائلاتٌ كاملة:
**النسخُ الاحتياطيةُ بأبوابها الثمانية** — وهي البوّابةُ الثالثةُ في الرفع.
والتفصيلُ في `STATE.md`، **وحدُّ العدد مكتوبٌ معه**: الطرقُ ليس القياس.

### البريدُ — ما بُني وما لم يُبنَ

**بُني**: `0065` (العمودان و`phone_pending` وعضوُ التعداد والفهرسُ الجزئيّ) ·
عقدٌ مجرَّدٌ ومُرسِلٌ وهميّ · مفتاحٌ **لا يُشعَل بلا عقدٍ فعّال** · بابا
الرمز والتسجيل · **الحسابُ المحدود** (لا رحلةَ ولا محفظة، والتحويلُ من طرفيه) ·
ورفعُ الحدِّ من بابِ إثباتِ الرقم نفسِه · و`PhonePendingNotice` في التطبيقين ·
و**١٥ اختباراً**.

**ولم يُبنَ بعلّته**: **لا مُرسِلَ حقيقيّ** (لا يُلمس عقدُ Resend) —
و`build_provider` **يرفض بنصّه** أيَّ عقدٍ ليس وهميّاً · **ولا شاشةَ تسجيلٍ
بالبريد في التطبيقين**: البابان مبنيّان **ولا يبلغهما إصبع**، وهي أوّلُ ما
يُبنى حين تُشعَل القناة.

### ⚠ الحملةُ — **لم تُبدأ، وسببُ ذلك سؤالٌ لا وقت**

**ستٌّ من قراراتها مفهومةٌ ومكتوبة**: سببٌ منشورٌ على الحجب · تجميدٌ وفكٌّ بلا
مشرف · تمديدُ الاشتراك بمقدار أيام التجميد · المحفظةُ لا تُمسّ · تجمّدٌ آليٌّ
حين تسقط قناةُ السوق واستئنافٌ بما بقي · ١٤ يوماً وثلاثُ تذكيراتٍ ودفعةُ ٢٠٠.

**والناقصُ واحد: ما الذي تطلبه الحملةُ من الكبتن؟** — أي **ما الذي إن لم
يفعله خلال الأربعة عشر يوماً يُحجب**. لم أجده في `SPEC.md` ولا في التسليم،
**وهو قلبُ الميزة لا تفصيلٌ فيها**: عليه يقوم من تشمله الحملة، ونصُّ
التذكيرات الثلاث، ونصُّ سبب الحجب المنشور، وشرطُ فكِّ الحجب.

**ولم يُبنَ على ترجيح** — كما لم يُبنَ في FastSpring.

### أولُ خمسةِ أعمالٍ للجلسة القادمة

1. **جوابُ سؤال الحملة** أعلاه، ثم بناؤها كاملةً بأرقامها.
2. **جولةُ اللوحة في متصفّح** — TOTP بيد المالك.
3. **شاشةُ التسجيل بالبريد** في التطبيقين — حين تُشعَل القناة.
4. **قرارُ `CLAUDE.md`**: على **57,844** حرفاً، **١٥٦ من علامة ٥٨,٠٠٠**.
5. **الأبوابُ الـ٤٦ غيرُ المطروقة** — تُبدأ بعائلة النسخ الاحتياطية.

---

## ٠-ي. أين نقف — ٢٠٢٦-٠٨-٣١ (الجلسة الثانية، تُقرأ قبل ما تحتها)

**جولةُ اللوحة تمّت** — الثلاثةَ عشر بنداً، ومعها بندٌ زائد. **والبريدُ
والحملةُ لم يُبدآ.**

**والترحيلةُ `0064` شُغِّلت ومُقيست** على قاعدة التطوير، و`test_migrations`
خضراء بدورة `downgrade base → upgrade head`.

### ما بُني

* **الخلفية**: `require_icon` و`stamp_if_shown` موصولان بالأبواب الأربعة ·
  بابا حذفٍ **للمسوّدة وحدَها** · `GET /admin/settings/service-icons` ·
  `GET /admin/settings/service-destinations` (**زائدٌ على القائمة**، بعلّة
  الأيقونة نفسِها) · رفعُ صورة اللافتة ونزعُها · بابا خدمتها (التطبيقُ بشرط
  السوق، واللوحةُ بلا شرط).
* **التطبيقان**: بطاقةُ اللافتة تعرض الصورةَ — `api.blob` وعنوانٌ من
  `blob:`، **ولا حقلَ `has_image`**: الطلبُ نفسُه هو الجواب.
* **اللوحة**: محرِّرا الصفّين (٧ حقولٍ و٩) · منتقي أيقوناتٍ يقرأ القائمةَ من
  بابها · منتقي مقصدٍ يعرف الأدوار · معاينةٌ **بالبطاقة نفسِها** في إطار
  الجهاز · زرّا حذفٍ **معطَّلان بعلّتهما** على ما عُرض · رفعُ صورةٍ ونزعُها.
* **حارسان**: `check:fields` و`check:storefront-card` — موصولان بـ
  `npm run build` في اللوحة، فيمرّان في CI.
* **٢١ اختباراً** في `backend/tests/test_storefront.py` — **وأوّلُ اختبارٍ
  لهذين الجدولين على الإطلاق**.

### ثلاثةُ عطبٍ قِيست وأُصلحت

1. `cliq-qr` كان يكتب `stored.path` **وهو غيرُ موجودٍ على `StoredFile`** —
   ٥٠٠ على كلِّ رفع، وملفٌّ يتيم. **ولا اختبارَ كان يطرقه.**
2. وتحته `MissingGreenlet` على `updated_at` — **كان الأولُ يستره.**
3. و`stamp_if_shown` كانت تترك لافتةً تُشعَل قبل نافذتها بلا ختم — **فتُعرض
   على الناس وهي تُقرأ مسوّدةً تُحذف.**

### ما ينتظر إصبعَ المالك — ستّة

1. **فتحةُ لوحةٍ واحدة**: حسابُ مشرف التطوير يطلب TOTP **والرمزُ بيده**،
   فلم تُفتح الشاشةُ في متصفّح. يُقاس: منتقي الأيقونات · المعاينة · زرّا
   الحذف · رفعُ صورةٍ ونزعُها.
2. **`fastspring.txt` في جذر المستودع** — بيانُ اعتمادٍ **غيرُ متعقَّبٍ وغيرُ
   مستثنى**، فـ`git add -A` يودعه. **لم يُحذف ولم يُودَع** (تنظيفُه يخفي أنه
   كان هناك). ومكانُه `provider_credentials` مشفَّراً من صفحة العقود.
3. **`CLAUDE.md` على 57,844 حرفاً** — **١٥٦ حرفاً من علامة البدء ٥٨,٠٠٠**.
   ولم يُبدأ نقلٌ: **القرارُ للمالك** كما نصّ.
4. **قرارُ FastSpring** — القراءةُ كاملةً في `STATE.md`: **الاشتراكُ مقبول،
   وشحنُ المحفظة تمنعه ببندين، وسدادُ الدَّين لا يُبنى**. وثلاثةٌ لا يعرفها
   إلا هو: **نسبتُهم** (غيرُ منشورة) · **أالأردنُ بلدُ صرفٍ مدعوم؟** (لم أجد
   قائمة) · **والحسابُ نفسُه**.
5. **إذنُ أوّلِ بريدٍ حقيقيّ وعقدُ Resend** — ما زال كما كان.
6. **رقمُ سقف الدَّين** — ما زال `NULL` على الإنتاج.

### أولُ خمسةِ أعمالٍ للجلسة القادمة — بترتيبها

1. **جولةُ اللوحة في متصفّح** بعد أن يفتحها المالك — DOMاً لا لقطة.
2. **البريدُ** — لم يُبدأ: العمودان و`phone_pending` · مفتاحُ
   `email_otp_enabled` مطفأً **لا يُشعَل بلا عقدٍ فعّال** · `email_otp` في
   التعدادات والقنوات · عقدٌ مجرَّدٌ ومُرسِلٌ وهميّ · قالبٌ بمدّةٍ محقونةٍ من
   `CODE_TTL_SECONDS` · **والحسابُ المحدود** · وسطرُ «أكّد رقمك».
3. **الحملةُ** — لم تُبدأ: سببٌ منشورٌ على الحجب · تجميدٌ وفكٌّ بلا مشرف ·
   تمديدُ الاشتراك بمقدار أيام التجميد · والمحفظةُ لا تُمسّ · وتجمّدٌ آليٌّ
   حين تسقط قناةُ السوق. **ولا تُطلق على أحد.**
4. **اشتراكُ FastSpring** إن أذن — وبعد أن يأتي بالنسبة وبلد الصرف.
5. **قرارُ `CLAUDE.md`** — أيُنقل منه شيء أم يُرفع السقف.

---

## ٠-ط. أين نقف — ٢٠٢٦-٠٨-٣١ (يُقرأ قبل ما تحته)

**الإنتاجُ على `c6caf79a` والقاعدةُ على `0063`** — رُفع اليومَ بالبوّابات
الستِّ كلِّها خضراء، ونسختُه المحقَّقةُ في `/d/taxo-backups/20260830T205046Z`
**واستُعيدت وقِيست** (تعيد `0057` و369 سِمةً كما كانت). والحزمُ على صفحة
التحميل **`v0.2.2`** — الكبتنُ تغيّر (١٩٣١ سطراً وثلاثةُ أذونات)، والراكبُ
صفرُ فرق.

**والشجرةُ المحلّيّةُ متقدّمةٌ على الإنتاج بإيداعين لم يُدفعا:**

| الإيداع | ما فيه |
|---|---|
| `16ac52a` | **منفذُ الفحص** — يسأل واتساب ولا يرسل، والفرقُ صار رمزاً |
| `5abc8bc` | **نصفُ جولة اللوحة** — الترحيلةُ والنموذجان وقائمةُ الأيقونات |

**ولم يُدفَعا** بأمر المالك (٢٠٢٦-٠٨-٣١): «أودِع ولا تدفع».

### ما بُني من جولة اللوحة — وما بقي

| # | البند | الحال |
|---|---|---|
| 1 | `0064` — `first_shown_at` على الجدولين، `image_path` على اللافتة | ✅ **مبنيّةٌ ولم تُشغَّل على قاعدةٍ قطّ** |
| 2 | العمودان في النموذجين | ✅ |
| 3 | `SERVICE_ICONS` (٤٠) و`require_icon` | ✅ **معرَّفٌ ولا يناديه أحدٌ بعد** |
| 4 | `stamp_if_shown` · `require_draft` | ✅ **معرَّفان ولا يناديهما أحد** |
| 5 | ربطُ الثلاثة بالراوتر (إنشاءٌ وتعديل) | ❌ **هنا انقطع الاتصال** |
| 6 | بابا الحذف (بلاطة · لافتة) — للمسوّدة وحدَها | ❌ |
| 7 | `GET /admin/settings/service-icons` | ❌ |
| 8 | رفعُ صورة اللافتة على `storage.save` + بابُ خدمتها + عرضُها | ❌ |
| 9 | تحريرُ الصفّين في اللوحة (٧ حقولٍ و٩) | ❌ |
| 10 | `audience` في الإنشاء — **وبه بلاطاتُ الكبتن** | ❌ |
| 11 | منتقي الأيقونات في اللوحة | ❌ |
| 12 | المعاينةُ بالبطاقة نفسِها + حارسُ بصمة | ❌ |
| 13 | **حارس «حقلٌ بلا زرّ»** بحدِّه المكتوب | ❌ |

**والخطوةُ التالية بعينها**: في `backend/app/routers/admin_settings.py` — نداءُ
`storefront.require_icon(payload.icon)` في `create_service_tile` قبل
`require_destination`، و`require_icon` + `stamp_if_shown(tile)` في
`update_service_tile` بعد `_apply_updates`. **ثمّ مثلُهما للّافتة.**

### ثمّ البريد (لم يُبدأ)

**قناةً بديلةً حين تسقط واتساب** — أُقرَّت قراراتُه كلُّها ولم يُكتب سطر:
العمودان `email` و`email_verified_at` · **و`phone_pending`** (الرقمُ يُحجز
ولا يُملَك) · ومفتاح `email_otp_enabled` لكلِّ سوقٍ **مطفأً**، **ولا يُشعَل
بلا عقدِ مُرسِلٍ فعّال** · و`email_otp` في التعدادات والقنوات · **وعقدٌ
مجرَّدٌ ومُرسِلٌ وهميّ** · والقالبُ **بمدّةٍ محقونةٍ من `CODE_TTL_SECONDS`
(٣٠٠ ثانية = خمس دقائق) لا مكتوبة** · **والحسابُ المحدود** (لا رحلةَ ولا
محفظة — **وهو غيرُ مبنيٍّ اليوم بحرف**: `require_verified` له مُنادٍ واحدٌ
هو اعتمادُ الكبتن) · وسطرُ «أكّد رقمك» في كلِّ فتحة.

**ولا يُرسل بريدٌ حقيقيٌّ ولا يُلمس عقدُ Resend** حتى إذن المالك.

### ثمّ الحملة (لم تُبدأ)

حقلُ سببٍ **منشورٌ** على الحجب (اليومَ السببُ في قيد التدقيق وحدَه فلا يقرؤه
صاحبُ الحساب) · والتجميدُ والفكُّ **بلا مشرف** · **وتمديدُ الاشتراك بمقدار
أيام التجميد** (لا تعويضَ مبنيٌّ اليوم) · **والمحفظةُ تبقى ولا تُمسّ** ·
**وتجمّدُ الحملة آلياً حين تسقط قناةُ سوقها وتستأنف بما بقي**. والمهلةُ
**١٤ يوماً**، والتذكيراتُ **ثلاث**، والدفعةُ **٢٠٠**. **ولا تُطلق حملةٌ على
أحد — البناءُ وحدَه، والإطلاقُ بيد المالك.**

### وما ينتظر إصبعَ المالك

1. **وقتُ محاولة واتساب** — قال «١٢:٥٦ اليوم» وهي **لم تأتِ بعد** وقتَ
   القراءة (كانت الساعةُ ٠٢:٣٠). **وسجلُّ ١٢:٥٦ أمس مُحي بإعادة إنشاء
   الحاويات في جولة النشر.** والمقيسُ الوحيد: **من ١٤:٥٧ أمس إلى الآن لم يبلغ
   الخلفيةَ نداءُ تحقّقٍ واحد** — ويوافق أن `fetch` لم يصل. **ويحسمها أن
   يعيد المحاولةَ ويقول متى.**
2. **إذنُ إرسال أوّلِ بريدٍ حقيقيّ**، و**عقدُ Resend** (يحتاج حسابَه وتحقّقَ
   نطاق).
3. **رقمُ سقف الدَّين** — ما زال `NULL` على الإنتاج كما قرّر.
4. **Docker Desktop مطفأٌ الآن** — فـ`test_migrations` لم يُشغَّل على `0064`،
   **و`0064` لم تُشغَّل على أيِّ قاعدةٍ قطّ**. **تُشغَّل أوّلاً في الجلسة
   القادمة قبل البناء عليها.**

---

## ٠-ح. أين نقف — ٢٠٢٦-٠٨-٢٦ (يُقرأ أوّلاً)

**الخادمُ صار الأسماءَ العارية.** `api` · `app` · `driver` · `admin` تشير إلى
نفق الخادم (`e9ad4a78…`)، و`dev-*` إلى جهاز المالك (`e0ef1dd8…`)، و`stg-*`
باقيةٌ عاملةً كطريقِ رجوع. **والتطبيقاتُ لم تعد تتوقّف بإطفاء الحاسوب** — وهو
الهدفُ الذي بدأ منه اليوم. والرجوعُ ثوانٍ: تُعاد الأربعةُ إلى `e0ef1dd8…`.

**والخادمُ على `b6f6868`** — مدفوعٌ ومخضَّرٌ في CI. **وحالُه مقيسةٌ لا موصوفة**:
`production` · الترحيلة `0057` · الصفوف `3/1/0/1` · الواجهاتُ الثلاثُ من بناء
اليوم ومخبوزُها `api.tajora.ly` · وCORS يقبل الأصلَ العاريَ حيّاً.

### ما ينتظر إصبعَ المالك

1. **تسجيلُ حساب من هاتفٍ عبر واتساب** — `whatsapp_otp_enabled/JO` **أُشعل
   اليوم** وصار `verification=whatsapp_otp`، والبوّابةُ `linked` بالرقم
   `447366186776` وطابورُها صفر. **ولم يُقَس المسارُ كاملاً بعد**: رقمٌ ←
   رمزٌ يصل ← حسابٌ يُنشأ. **وهو أوّلُ يومٍ يمكن أن يُقاس فيه سقوطُ واتساب
   إن سقط** — أيصيح أم يستره البديل.
2. **تسجيلُ حزمتين في Firebase**: `ly.tajora.rider.test` و
   `ly.tajora.driver.test` ثمّ إعادةُ تنزيل `google-services.json`.
   **والنكهةُ التجريبيةُ لا تُبنى قبلها** (`No matching client found`).
3. **ضبطُ سرَّي التوقيع**: `gh secret set ANDROID_KEYSTORE_PASSWORD` و
   `ANDROID_KEY_PASSWORD`. **والقائمُ منها اثنان** (`ANDROID_KEYSTORE_B64` ·
   `ANDROID_KEY_ALIAS`)، **والكلمةُ لم تُطلب ولن تُكتب في مكانٍ يبقى**.
4. **وبصمةُ المفتاح**: `keytool -list -v -keystore D:\taxo-keys\taxo-release.jks
   -alias taxo` — تُلصق ليُطابَق بها توقيعُ أوّل حزمة.

### ما ينتظر إذنَه

- **نزعُ Firebase من كلِّ مسار حساب** — القرارُ مُتَّخذٌ والبندُ مكتوبٌ في
  `STATE.md` بجدولِ سبعةِ مواضع، **ولم يُنفَّذ منه حرف**. **ولا يُبدأ قبل أن
  يثبت واتساب** — نزعُه أوّلاً يترك JO بلا قناةٍ إطلاقاً.
- **وسمُ إصدارٍ `v*`** — يبني حزماً موقَّعةً ويسحبها الخادمُ إلى صفحة التنزيل
  **تلقائياً**. **وحزمةٌ تُنشر مرّةً لا تُسحب**، فلا وسمَ بلا إذن.
- **فتحُ ليبيا** — `country_visible=false` و`whatsapp_otp_enabled=false`،
  والسوقُ محجوبٌ كلّياً.
- **إيقاظُ متجر المركبات** — حاجزُ الرخصة **رُفع** (`LICENSE`، إقرارُ المالك
  2026-08-26)، **والأربعُ مراحلَ باقيةٌ موقوفةً بأمره**: الاستخراج · الانتقاء ·
  الصفوف · المتجر.

### أولُ ثلاثةِ أعمالٍ للجلسة القادمة

1. **قياسُ تسجيلٍ كاملٍ بواتساب من هاتفٍ** — ومعه قراءةُ السقوط من السجلّ إن
   سقط. **وهو ما يفتح البابَ لِما بعده.**
2. **ثمّ نزعُ Firebase من مسار الحساب** — بالبند المكتوب، وFCM يبقى.
3. **ثمّ إتمامُ النكهة التجريبية** بعد تسجيل الحزمتين: `assembleTrialDebug`
   وقراءةُ معرّفه من داخل الحزمة، وجدولُ المعرّفات الأربعة.

---

## ٠-ز. ⚠ **حالُ الخادم سؤالٌ مفتوحٌ لم يُجَب — ويُقرأ قبل أيِّ شيء**

**جلسةُ ٢٠٢٦-٠٨-٢٤ (الثانية) لم تمسّ الخادمَ ولم تقِسه البتّة.** كلُّ عملها
كان في الشجرة وCI. **فما في §٠ عن الإنتاج مؤرَّخٌ ٢٠٢٦-٠٨-٢٣، ويُقرأ خبراً
عن ذلك اليوم لا حالاً قائمة.**

**والمقيسُ اليوم من الشجرة وحدَها:**

| | |
|---|---|
| ما نُشر آخرَ مرة | `de612356` · وسم `v0.1.4` |
| **وتقدّمت الشجرةُ منذُه** | **٥٠ إيداعاً** (قِيس ٢٠٢٦-٠٨-٢٤) |
| وإيداعاتُ اليوم | **لا تمسّ مسارَ نشرٍ** — لا `deploy` ولا `landing` ولا `compose` ولا `cloudflared` |

**وما لم يُقَس فلا يُقال**: إيداعُ الخادم · رأسُ قاعدته · ما تخدمه اللوحةُ
هناك · وأربعةُ المفاتيح المشتعلة. **ولا يُقرأ سكوتُها سلامة.**

**والوصولُ إلى الخادم لم يكن ممكناً في هذه الجلسة**: مفتاحُ SSH بعبارةِ
مرور، **والدخولُ غيرُ التفاعليِّ مرفوض** (`Permission denied (publickey)`)،
ولم تُنفَق محاولاتُ حساب اللوحة.

> **فأوّلُ ما يُسأل عنه في الجلسة القادمة**: **أين الخادمُ من هذه الشجرة؟**
> وما دام غيرَ مقيسٍ **فلا رفعَ ولا حكمَ عليه** — والبوّاباتُ الخمسُ هي
> الطريق، لا القياسُ من الذاكرة.

---

## ٠-د. **أوّلُ خُضرةٍ مُثبَتةٍ في وظيفة «المجموعةُ الكاملة»** — التشغيل ٤٦

**٢٠٢٦-٠٨-٢٤ · التشغيل ٤٦ · `32720618028` · الإيداع `1437955`.**

| | |
|---|---|
| الحكم | **`success`** — والأربعُ وظائفَ خضراء (والخامسةُ `skipped` بشرطٍ مُعلَنٍ على الأوسمة) |
| المجموعة | **١١٧٦ خضراء · صفرُ فشل · `EXIT=0`** في ١٤د٥٦ث |
| التوابع | `db` و`redis` **سليمتان بعد ٥ث** |
| المدّة | 11:11:54Z ← 11:28:00Z |

**وهي الأولى المُثبَتة، ويُقال ذلك صراحةً**: ما قبلها من خُضرةٍ في هذه
الوظيفة **مشطوبٌ من الشهادة** (قرارُ المالك) — لأن البوّابةَ كانت تخرج بصفرٍ
حين تسقط، **فلم تكن قادرةً على قول «لا»**.

**وما يجعلها مُثبَتةً لا مُعلَنة**: **البابُ أثبت أنه يستطيع أن يسقط** —
أربعُ سقطاتٍ صادقةٍ متتاليةٍ قبلها، **كلٌّ بسببٍ مختلفٍ مسمّى**:

| التشغيل | ما سقط | السبب |
|---|---|---|
| ٤٢ | خطوةُ التوابع | اسمُ ملفِّ البيئة لم يُصرَّح لها |
| ٤٣ | المجموعة | `InvalidPasswordError` — بيانُ اعتمادٍ مكتوبٌ في الكود |
| ٤٤ | المجموعة | نفسُه (إيداعٌ سابقٌ للإصلاح) |
| ٤٥ | اختبارٌ واحد | `filesystem_percent=82` — قرصُ العامل |
| **٤٦** | **لا شيء** | **١١٧٦ خضراء** |

**والعددُ يطابق المحلّيَّ بحرفه**: ١١٧٦ في CI · ١١٧٦ محلياً — **فلا بابَ
ثالثَ في الجمع**، والفروقُ كلُّها كانت بيئةً.

> **وتساوي الأعداد ليس تساويَ الأسباب** (`CLAUDE.md`): ١١٧٦ **خطأً** في ٤١
> و١١٧٦ **خطأً** في ٤٣ بعلّتين مختلفتين، ثم ١١٧٦ **خضراء** في ٤٦. **الرقمُ
> نفسُه ثلاثَ مرّاتٍ بثلاثة معانٍ.**

**وخمسُ طبقاتِ سترٍ نُزلت للوصول إليها** — **ولم تكن أيٌّ منها مرئيةً قبل
التي فوقها**:

| # | الطبقة | لم تكن تُرى لأن… |
|---|---|---|
| ١ | `exit "${code:-1}"` **يخرج بصفر** | — (هي الأعلى) |
| ٢ | **المجموعةُ تعمل بلا قاعدة** | الحكمُ مبتلَعٌ فلا أحدَ يقرأ السقوط |
| ٣ | **بيانُ اعتمادٍ في الكود** (`taxo:taxo`) | الاتصالُ لا يقع أصلاً |
| ٤ | **قرصُ المضيف ٨٢٪** | المجموعةُ لا تبلغ اختبارَ النسخ |
| ٥ | **`.suite.out` لا يُمسح** | ظهرت بعد إصلاح ما فوقها |

> **وثلاثٌ من الخمس شكلٌ واحد**: **قيمةٌ من العالم الحقيقيِّ يقرؤها اختبارٌ
> لا يملكها** — **ساعةُ الجدار** (رسمُ الوقفة ٢٫٠٠١) · **كلمةُ المرور التي
> وافقت صدفةً** (`taxo:taxo`) · **القرصُ عند ٨٢٪**. **توافق جهازاً وتخالف
> آخر، ولا شيءَ في الشجرة يتغيّر بينهما.** والقاعدةُ كاملةً في `CLAUDE.md`.
>
> **والخامسةُ أخطرُ بموضعها لا بآليتها**: خضرةٌ كاذبةٌ **على جهاز المطوّر**،
> وهو موضعُ قرار الدفع — فما بعدها مبنيٌّ عليها ولا يعود إليها أحد.

### والحالُ الآن — التشغيل ٥٠ (`20f64ba`، ٢٠٢٦-٠٨-٢٤)

**١١٧٩ خضراء · `EXIT=0` · ١٧د٥٢ث** — والعددُ ارتفع عن ١١٧٦ **بثلاثةٍ
معلومة**: حارسُ `filesystem_percent` الذي بُني بأمر المالك.

> **⚠ وبندٌ غيرُ مشخَّصٍ يُقال ولا يُطوى**: **التشغيلُ ٤٨ قُطع عند ٣٠ دقيقة**
> (مقابل ١٥ في ٤٦ و٤٧ و١٨ في ٥٠)، **ولم يُعرف سببُه** — لأن ٤٩ سقط في ٥٨
> ثانيةً لعلّةٍ أخرى فلم يقس البطء. **والتقييسُ جاهزٌ لتكراره**: المخرَجُ غيرُ
> مخزَّن (`python -u`) وخطوةُ التشخيص تعمل على القطع (`cancelled()`).
> **والهامشُ ضيّق**: ١٨ من ٣٠. فإن تكرّر فالقرارُ بين رفع المهلة والتشخيص.

**وما لا تشهد له هذه الخُضرة**: **عمرُ الحزمة المخدومة** — `check:served`
يعلن «لم يُقس» في CI ويخرج بصفر. **الشكلُ العاشرُ يُقاس محلياً وعند البوّابة
الخامسة وحدَهما** (`CLAUDE.md`).

---

## ٠-هـ. ثلاثةٌ تنتظر المالك — **مسجَّلةٌ بحالها لا بتوصيتها**

### أ) أربعةُ مفاتيحَ مشتعلةٌ على الإنتاج وتُشحن مطفأة

**مقيسٌ من `check-markets.sh` على `stg-api.tajora.ly`** (٢٠٢٦-٠٨-٢٤)، ولا
سببَ مكتوباً لأيٍّ منها في `DELIBERATE`:

| المفتاح | موضعُ الفحص | أثرُ الإطفاء |
|---|---|---|
| `wallet_transfer_enabled` | `wallet.py:118` | التحويلُ يُرفض — **والأرصدةُ لا تُمسّ** |
| `multi_stop_enabled` | `rides.py:356` — **عند الإنشاء** | طلباتٌ جديدةٌ بمحطات — **والجاريةُ تُكمَل** |
| `scheduled_rides_enabled` | `bookings.py:169` — عند الإنشاء | حجزٌ جديد — **والقائمُ يُنفَّذ** |
| `vehicle_skins_enabled` | `vehicle_skins.py:43` | المتجرُ والشراء — **وما مُلِك يبقى** |

**فلا واحدٌ منها يقطع شيئاً جارياً**؛ الأثرُ في الأربعة **منعُ فعلٍ جديد**.

**و«متى ومن» غيرُ مقروء**: الدخولُ إلى الخادم يحتاج مفتاحاً بعبارة مرور،
**والدخولُ غيرُ التفاعليِّ مرفوض** — ولم تُنفَق محاولاتُ حساب اللوحة.
**والمالكُ يقرؤها بنفسه** (قرارُه 2026-08-24).

**والتدقيقُ يسجّل كلَّ تبديلٍ يمرّ بـ`set_flag`** — مقيسٌ من الكود، ومناديه
اثنان (راوترُ الإعدادات، ومزامنةُ عقود المزوّدين). **وكتابةُ SQL مباشرةً
تتجاوزه**، و`HANDOFF` يسجّل أن جلسةً سابقةً أشعلت مفتاحين «من الخادم».

### ب) `jwt_secret` — **الإلزامُ شيءٌ والتبديلُ شيءٌ آخر**

**والخلطُ بينهما هو ما يجعل القرارَ يبدو أثقلَ ممّا هو:**

| | ما يقع |
|---|---|
| **إدراجُه في `REQUIRED_SECRETS`** | يفشل الإقلاعُ **إن كان غائباً** فحسب. **ولا أثرَ على جلسةٍ واحدة** إن كان مضبوطاً |
| **تبديلُ قيمته** | **كلُّ جلسةٍ تسقط** — راكباً وكبتناً ومشرفاً |

**والثاني مقيسٌ من الكود**: `_create_token` واحدٌ **للوصول وللتجديد معاً**،
وكلاهما موقَّعٌ بـ`settings.jwt_secret`. **فلا مخرجَ بالتجديد** — رمزُ
التجديد نفسُه يسقط.

**وقيمتُه على الإنتاج لم تُقرأ** — لا سبيلَ بلا الخادم. **والتوثيقُ يقول**
(`SPEC` §25.1، ٢٠٢٦-٠٨-٢٠) إن `JWT_SECRET` **وُلِّد على الخادم نفسِه** في
`/home/taxo/secrets/taxo.env`. **وهذا توثيقٌ لا قياس**، ويُقرأ كذلك.

### ج) بطءٌ غيرُ مشخَّص — **والمهلةُ لا تُرفع**

**التشغيل ٤٨ قُطع عند ٣٠ دقيقة**، مقابل ١٤:٥٦ (٤٦) و١٥:٢٤ (٤٧) و١٧:٥٢ (٥٠).
**والسببُ مجهول**: ٤٩ سقط في ٥٨ ثانيةً لعلّةٍ أخرى فلم يقس البطء.

**والهامشُ ٢٠ من ٣٠** (التشغيل ٥١: ١٩د٤٨ث). **ولا تُرفع المهلة** (قرارُ
المالك): رفعُها يُخفي ما لا يُعرف. **والتقييسُ مسلَّح**: مخرَجٌ غيرُ مخزَّن
(`python -u`)، وخطوةُ تشخيصٍ تعمل على القطع (`failure() || cancelled()`)،
**وبصمةُ تشغيلٍ** ترفض مخرَجاً ليس لهذا التشغيل.

> **وفرضيةٌ جُرّبت وسقطت — تُكتب كي لا تُعاد** (٢٠٢٦-٠٨-٢٤): ظُنّ أن
> `python -u` هو سببُ البطء لأنه أُدخل بين ٤٨ و٤٩. **وقِيس فسقط**: ملفّان،
> تشغيلان لكلِّ حال — **مخزَّن ١١٨ث و١٢١ث · وغيرُ مخزَّن ١٠٠ث و١٣٠ث**.
> **فالتباينُ داخل الحال الواحدة يفوق الفرقَ بين الحالين.**
>
> **وما تعلّمناه منه أهمُّ من الفرضية**: هذا الحملُ **تباينُه ±١٥٪ على الجهاز
> نفسِه**، وأزمنةُ CI (١٤:٥٦ ← ١٩:٤٨) مدىً ٣٣٪ — **متّسقٌ مع تباينٍ لا مع
> اتجاه**. **وقراءةُ أربع نقاطٍ «تسلُّقاً» إفراطٌ**، وقد وقع مني وصُحّح.
> **فأرقامٌ مختلفةٌ لا تعني أسباباً مختلفة**، كما أن تساويَها لا يعني تساويَ
> أسبابها.
>
> **ويبقى ٤٨ غيرَ مفسَّرٍ**: ٣٠+ خارجُ هذا المدى. **ولا يُقرَّر فيه شيءٌ حتى
> يتكرّر ويُقرأ تشخيصُه.**

---

## ٠-ج. جلسةُ ٢٠٢٦-٠٨-٢٤ (الثانية) — خمسةُ إيداعات، **ولم يُدفع شيء**

| الإيداع | ما فيه |
|---|---|
| `5dbf91e` | صنفُ لونٍ بلا رمز — المُبلَّغُ عنه وأربعةٌ معه |
| `c9b8577` | **`SPEC.md` §28.9-ج** الاتجاهُ المقلوب شرطاً على المنتقاة |
| `5406dc1` | الثلاثةُ التي لا تسقط بحذف الحارس — تُسمّى بما تقيسه |
| `86c2dd1` | **حارسا المال يُطفآن من اللوحة** — ومدُّ `check:flags` |
| `e57c6f5` | تحديثُ التسليم |
| `8020211` | **رسمُ الوقفة: حدٌّ لا مساواة** — بشرطين، وفي المواضع الثلاثة |
| `880f151` | **٤٣ موضعاً**: اسمُ متغيّرِ CSS مكتوباً صنفاً — ترجمةٌ لا قرار |
| `db84917` | **دروسُ اليوم في `CLAUDE.md`** — وأثقلُها المفتاحُ الذي لا يُطفأ |
| `<هذا>` | تحديثُ التسليم (الثاني) |

### ~~الأحمرُ الوحيد~~ — **أُصلح بشرطين** (2026-08-24)

`tests/test_ride_pauses.py::test_the_rate_is_frozen_against_a_later_settings_change`
— `pause_charge` **2.001 بدل 2.000**.

**وهو تشخيصٌ لا فرضية**: `_age_pause` يكتب `started_at = now − 10د` ثم يُحسب
الرسمُ في نداءٍ لاحقٍ بـ`now` الخاصِّ به، **والانتظارُ يُحتسب بالثانية**. وعند
٠٫٢٠٠/دقيقة فإن جزءاً من الألف **يساوي ٠٫٣ ثانيةٍ بالضبط** من تأخّرٍ بين
النداءين. **ومقيسٌ**: يمرّ منفرداً مرتين، ويسقط تحت حمل المجموعة.

**وما يحرسه ليس الرقمَ بل التجميد**: لو انكسر التجميدُ لكان الناتج
`10 × 5.000 = 50.000` لا `2.001`. **فالتأكيدُ على المساواة التامّة أضيقُ من
معناه.** والعلاجُ المقترَح **شرطان لا واحد** (كي لا يُستبدل المحروسُ بأضعفَ):
أن يكون بالمعدَّل المجمَّد ضمن هامشٍ صغير، **وألّا يكون بالمعدَّل الجديد**.
**ونُفِّذ بأمره** (`8020211`) — `_assert_billed`، **وفي المواضع الثلاثة**
لا في المُبلَّغِ عنه وحدَه: ثلاثةُ اختباراتٍ بالحساسية نفسِها (٠٫٢٠٠/دقيقة).
**ومقيسٌ في الاتجاهين**: بكسر التجميد عمداً يسقط المستهدَفُ ومعه سبعة،
وبالاستعادة ١٤ خضراء.

### وثلاثةٌ قِيست على الطريق وتُقرأ قبل أيِّ قياسٍ قادم

1. **الخلفيةُ كانت تنشر ٢٢ مفتاحاً والتعدادُ ٢٤** — `--reload` مُشتعلٌ
   **وinotify لا يعبر تجسيرَ ويندوز**، فالعمليةُ الحيّةُ تحمل وحدةً قُرئت
   2026-08-23. **وعمليةٌ جديدةٌ داخل الحاوية نفسِها ترى ٢٤** — فلا يُسأل
   `docker exec python -c`، **يُسأل الخادم**. حُلَّ بإعادة تشغيل الخلفية.
2. **`driver-app/dist` كان أقدمَ من الشجرة** فلا يحمل المفتاحَ ولا سطرَ
   الكبتن — **فأولُ قراءةٍ قاست منتجاً آخر**. الشكلُ العاشر.
3. **`tsc --noEmit` ليس `tsc -b`** — والعلّةُ بنيويّة: `tsconfig.json`
   في الثلاثة `{"files": [], "references": […]}` **فالأمرُ المجرَّدُ يفحص
   صفرَ ملفّ**. **وقِيست أبوابُ المشروع كلُّها فوُجدت سليمة**: `tsc -b` في
   `build` و`tsc -b --noEmit` في `lint`، **ولا حارسَ بالأمر الخاطئ** —
   فالكاذبُ كان أمراً كُتب باليد. **والثلاثةُ مسجَّلةٌ في `CLAUDE.md`.**

---

## ٠-أ. تسليمُ جلسة ٢٠٢٦-٠٨-٢٤ — **اقرأ هذا القسمَ كلَّه قبل أن تلمس شيئاً**

> **الجلسةُ انتهت لسببِ حساب، لا لأن العملَ تمّ.** وما لم يُقَس هنا مكتوبٌ
> أنه لم يُقَس. **وكلُّ رقمٍ يحمل تاريخَه أو يُحال إلى حارسٍ يقيسه** — ومن
> نقل رقماً من هذا الملفّ إلى تقريرٍ بلا تشغيل حارسه فقد أخطأ (القاعدةُ في
> `CLAUDE.md`، ووقعت مقيسةً في هذه الجلسة نفسِها).

### ما أُنجز وأُودع (خمسةُ إيداعات، ولم يُدفع شيء)

| الإيداع | ما فيه |
|---|---|
| `c9e6e55` | **`check:money-math`** — الشقُّ الأول وحدَه: «أثمّة حسابُ مالٍ في الواجهة؟» لا «أصحيحٌ هو؟». ثلاثُ فجواتٍ بعللها |
| `dd8b3bd` | قياسُ المادّة: الاتجاه · المكرَّر · الهويّات — **وتصحيحُ خبرٍ متأخّر** |
| `aff101f` | توحيدُ الاتجاه · عرضُ ٣٧ زوجاً · تصميمُ مفتاحَي الإيقاف |
| `6343f77` | **مفتاحا الإيقاف — الخلفية** بثمانية اختبارات |
| `d1a14f2` | **زرّاهما وشريطاهما** وسطرُ الكبتن |
| `<هذا>` | التوثيقُ وتقليمُ الجدول |

### ⚠ ما لم يُقَس — ولا يُقرأ سلامةً

1. ~~المجموعةُ الكاملةُ لا نتيجةَ لها~~ — **شُغِّلت كاملةً 2026-08-24**:
   **١١٧٥ خضراء وواحدٌ أحمر** في ٤٢د ٥٣ث. **والزمنُ لا يُقارَن بالـ٣٣ دقيقة**
   ولا يُسجَّل رقماً: شُغِّلت وS21 موصولٌ بالكابل (بإذنِ المالك المسبَق)،
   و`adb` مُطفأٌ فلا شيءَ يستطلع الجهاز — **لكنّ الشرطَ لم يتحقّق فلا تُقارن**.
   والأحمرُ **أُصلح بأمر المالك** (`8020211`). **وقِيست المجموعةُ كاملةً في
   CI على شجرةٍ نظيفةٍ بعدها**: **١١٧٦ خضراء** (التشغيل ٤٦، §٠-د) — وهي
   **أوّلُ خُضرةٍ مُثبَتة**. والقياسُ المحلّيُّ على شجرةٍ هادئةٍ يُعاد **مرّةً
   واحدةً حين يُفصَل الهاتف**، **لا اليوم** (قرارُ المالك 2026-08-24).
2. **الحاويةُ والقاعدةُ محرَّرتان بعد الإيقاف** — مقيسٌ ٢٠٢٦-٠٨-٢٤:
   `pg_stat_activity` على `taxo_test` = **صفر**، والقاعدةُ **غيرُ موجودةٍ
   أصلاً**، ولا حاويةَ `suite` حيّة، ولا `backend/.suite.container`.
3. ~~`npm run build` لم يُشغَّل كاملاً في أيِّ تطبيق~~ — **الثلاثةُ بُنيت
   وأخضرَ 2026-08-24** بهدفٍ مُعلَن، **و`check:served` أخضرُ للثلاثة**.
   **وتطبيقُ الراكب نُشر بأمر المالك** وقُرئت بصمتُه **من داخل WebView على
   S21**: `index-B0xm-B0l.js` / `c6861b9137` — **مطابقةً للأعمدة الأربعة**
   (`dist` · الحاوية · النفق · الجهاز).

   **وما تغيّر فيه**: ٥ ملفّات · ٣٣ صنفاً — `screens/Referrals.tsx` (٢٧
   ترجمة) · `ride/PauseNotice.tsx` (خلفيةُ صندوق الوقفة في الحالين) ·
   `ride/StopProgress.tsx` و`home/StopsEditor.tsx` (`text-inv`) ·
   **`screens/Notifications.tsx`** — وهو الوحيدُ **بمعنىً لا لون**: غيرُ
   المقروء صار له حدٌّ يُميّزه.

### ~~عطبُ اللون~~ — **أُصلح ومعه أربعةٌ أخرى** (2026-08-24)

`Backups.tsx:76` أُصلح **بالقائم لا بالإضافة**: ستةُ صناديقَ في اللوحة تكتب
`border-warn bg-surface-2`، وإضافةُ رمزين تمسّ `tailwind.config.js` في ثلاثة
تطبيقات وهو نسخةٌ حرفيةٌ من `DESIGN.md` §6.

**والكنسُ أخرج أربعةً**: `Referrals.tsx:107` (اللوحة) ·
`Notifications.tsx:175` (الراكب — **غيرُ المقروء لا يُميَّز**) ·
`PauseNotice.tsx:47` (الراكب — **بلا خلفيةٍ في الحالين**) ·
`ActiveRide.tsx:446` (الكبتن).

> **ودرسٌ يبقى**: أوّلُ مِسبارِ كنسٍ **مرّ أخضرَ على العطب** — كتبتْه صدفةُ
> `heredoc` في Git Bash فطوت `\\b` إلى محرف backspace، فلم يطابق شيئاً
> وأعلن شجرةً نظيفة. **وهو `check:readers` يقرأ صفرَ ملفٍّ ويمرّ.** ولولا
> قياسُ الاتجاه الثاني لَنُقلت «نظيفة» تقريراً. **ولا يُكتب مِسبارٌ فيه
> هروبُ regex بـ`heredoc` في هذه البيئة.**

**وعائلةُ أسماء المتغيّرات نُفِّذت بأمره** (`880f151`): **٤٣ موضعاً في ٧
ملفّات**، **والخريطةُ مشتقّةٌ من `tailwind.config.js` نفسِه** لا مكتوبةً بيد.

**والدليلُ من المتصفّح لا من العدد** — حُقن الصنفان في الصفحة نفسِها:

| القديم | ما كان يُنتجه | الصحيح |
|---|---|---|
| `text-mut` | **الحبرُ الكامل** (موروث) — فلا تراتُبَ بصريّ | ‎rgb(139,148,158) |
| `text-tx` | حبرٌ موروث — **صحيحٌ مصادفةً**، وهو سببُ بقائها | نفسُه |
| `bg-sur` · `bg-sur2` · `bg-sa` | **شفّاف** — بطاقاتٌ بلا خلفية | لونُ السطح |
| `border-brd` | **‎rgb(229,231,235)** رماديُّ Tailwind الافتراضيّ، **خارج اللوحة** | ‎rgb(42,49,58) |

**والشاشتان فُتحتا**: `PhotoReports` (اللوحة، داكنة) و«ادعُ صديقك»
(الراكب، فاتحة) — ترتسمان بتراتُبٍ صحيحٍ وبطاقاتٍ لها خلفيةٌ وحدّ.

### الاتجاهُ المقلوب — **قِيس وسقط، ولا يُقترح ثانيةً**

المطلوب: أيُّ طرفٍ هو أنفُ المركبة، لئلّا يشير سهمُ الاتجاه إلى الذيل.
**جُرِّبت خاصّيةُ التناقص العرضيّ** (متوسّطُ عرض الخُمس الأول مقابل الأخير)
**وسقطت مقيسةً**، والتنفيذُ سليم (تنقلب إشارتُها بالقلب ١٨٠° في ست عيّنات):

- **المقاديرُ ٠٫٠٣–٠٫١٢ أي في حدود الضجيج**، والوسيطُ ‎−٠٫٠١ على ٤١٨.
- **ولوحةُ F تنقسم ٢١/٤٨ وهي قرعة** — إشارةٌ حقيقيّةٌ لا تُنتج ذلك.
- **وتناقض قراءةَ العين** على ست لوحات.

**فالمركباتُ متماثلةُ العرض تقريباً، ولا خاصّيةَ هندسيّةً تفرّق الأنف من
الذيل.** وقراءةُ العين على تسعٍ (واحدةٍ من كلِّ لوحة) قالت «كلُّها أنفُها
إلى أعلى» — **وهي قراءةٌ على تسعٍ لا قياسٌ على ٤١٨، ولا تُنقل حكماً**.
**وقُضي فيه 2026-08-24 — `SPEC.md` §28.9-ج**: **شرطٌ على المنتقاة وحدَها،
بجوار فحص الظلال، والاثنان بعد الانتقاء لا قبله.** وأرقامُ التناقص العرضيّ
مكتوبةٌ هناك **كي لا يُقترح ثانية**.

### الـ٤١٨ — ما بقي فيها

- **`B-40` و`D-40` لم يُحذفا بعد.** قِيس أنهما **ليسا مركبتين** (قصاصتان
  معطوبتان)، **وأقرّ المالكُ حذفَهما ولم يُنفَّذ**.
- **والعددُ بعد الحذف مقيسٌ سلفاً**: ٤١٦؛ **وبإسقاط نسخةٍ من كلِّ زوجٍ
  مكرَّرٍ حيّ (٣٥ زوجاً): ٣٨١**.
- **و٣٦ زوجاً مُقَرَّةٌ من المالك**، منها زوجُ المعطوبَين. **والسابعُ
  والثلاثون مرفوض** (`G-11≡F-09` عند ٣٠٫١ وأعلى ما سواه ١٤٫١).
- **والمخزونُ كلُّه خارج المستودع**، في مجلَّد الجلسة المؤقّت — **و`car/`
  في `.gitignore` بعلّتها**. فمن يستأنف يعيد الاستخراجَ من `car/*.svg`:
  كلُّ مركبةٍ **صورتان مضمَّنتان** (قناعُ شفافيةٍ + طبقةُ لون) لا واحدة.

### جدولُ ما لا يُوقَف (`SPEC §28.16`) — حالُه الآن

**صفّا المفتاحين حُذفا فعلاً، والجدولُ سليمٌ مقروء** (٢٠٢٦-٠٨-٢٤): **سبعةُ
صفوفٍ مرقَّمةٌ ١–٧ بلا تكرار**. وكان الحذفُ قد **كسره مرّةً** — إعادةُ
ترقيمٍ بالاستبدال أنتجت صفوفاً متفرّقةً وأرقاماً مكرَّرة — **فأُعيدت كتابةُ
الجدول كلِّه ثم قُرئ**. وفوقه سطرٌ يقول لِمَ حُذف الصفّان ولا يُعادان.

### الموقوفُ بقرار المالك — لا يُبدأ منه شيء

**الاستخراج · الانتقاء · الصفوف · المتجر · جولةُ الهاتف · فحصُ الظلال ·
والحدُّ اللونيّ.**

**والحدُّ اللونيُّ مُقَرٌّ تصميماً وموقوفٌ تنفيذاً**: حلقتان (خارجيّةٌ
بـ`--inv` لتفصله عن أيِّ أرضيّة، وداخليّةٌ بلون الندرة)، وثلاثةُ ألوانٍ
محجوزةٍ لا تصطدم بمعاني الخريطة (بنفسجيّ · ذهبيّ · فيروزيّ). **وشرطُه
المكتوبُ: لا يُبنى قبل قياس الإطارات على جهازٍ حقيقيّ، ولا استثناءَ لأن
الكلفةَ «صفرٌ نظرياً».**

**وفحصُ الظلال مشروطٌ بالانتقاء** (`SPEC §28.9-أ`): يقع **بعده على
المنتقاة وحدَها**، لا على ٤١٨ قبل أن يُعرف أيُّها يدخل.

### وأربعةُ بنودٍ جديدةٍ عند المالك لم تصل بعد

**موجودةٌ ولم تُوصف** — وهي ما سيبدأ به على الأرجح. **لا يُخمَّن مضمونُها.**

---

## ٠. أين نقف الآن — تُقرأ قبل كلِّ شيء (٢٠٢٦-٠٨-٢٣)

**رُفع على الإنتاج، والبوّاباتُ الستُّ خضراء.** الخادمُ عند إيداعٍ معلوم،
والقاعدةُ عند `0056`، **وحالُ الإنتاج مشتقّةٌ من git لأول مرة** — كانت قبلها
«إيداعاً قديماً زائداً ٨٣ ملفاً دُفعت بـtar».

**والحزمتان منشورتان بوسم `v0.1.4`** من الإيداع نفسِه الذي خضّره CI، **ومقيسةٌ
بصمتُهما من الحافة**: `8255abeca4` للراكب و`13ca209217` للكبتن —
**والمثبَّتُ على S21 مطابقٌ للاثنين** (`versionCode = 288`).

**⚠️ ولم يُدفع شيءٌ إلى `origin` في هذه الجلسة الأخيرة**، والشجرةُ نظيفة.

### حالُ جولة الجهاز

**ما قِيس**: التصريحُ على S21 نافذٌ («الوثوق دائماً») · البصمتان مطابقتان
للبيان الحيّ · الجهازُ أندرويد ١٥ والبطارية ١٠٠٪ · **وأُلغي إذنُ الإشعارات
عمداً** كي يُقاس حوارُه كما يقع لأول مرة.

**وأين توقّفنا بالضبط**: الخطوةُ ١ (إشعالُ المفتاحين من اللوحة) **أُلغيت** —
أشعلتُهما من الخادم بإذنك، لأن `stg-admin` يخدم حزمةً بلا زرَّيهما.
**وأُعطيتَ الخطوةَ ٢ ولم يصل «تمّ»**: افتح تطبيقَ الكبتن، سجّل الدخول
بـ`+962791300099`، **واقبل حوارَ إذن الإشعارات**. **فالجولةُ عند الخطوة ٢،
ولم يُقَس منها شيءٌ بعد.**

**والإذنُ أُلغي عمداً قبلها** (`pm revoke POST_NOTIFICATIONS`) كي يُقاس
الحوارُ كما يقع لأول مرة — **فإن لم يظهر فذاك نتيجةٌ لا عطبٌ في الجهاز**.

**⚠️ و`Note 20` على بناءِ ١٩ آب** (`versionCode = 1`) — **يُترك مغلقاً طوال
الجولة**: جلستان على حسابٍ واحدٍ تفسدان قياسَ الإشعارات.

### ⚠️ ثلاثةٌ تنتظر المالك قبل أن تكتمل الجولة

1. **مسارُ صور الكتالوج** — النادرةُ والأسطوريةُ **بلا رسم**، والمولّدُ غطّى
   العاديةَ والمميزة. **ولكلِّ مركبةٍ الآن شكلان لا واحد** (§28، قرارُ
   2026-08-23): **مجسّمٌ للمتجر والكراج، وعلويّةٌ للخريطة** — ولنفس السيارة،
   والفرقُ زاويةُ النظر. **وما لا يحمل شكليه لا يُعرض في المتجر.**
   فالمطلوبُ رفعتان لكلِّ مركبةٍ نادرة، **من شاشة اللوحة بإصبعك** —
   **وهي في قائمة جولتك بندٌ قائمٌ بذاته**: ترفع صورةَ مركبةٍ وترى المعاينةَ
   الحيّةَ على قطعة الخريطة. **والمسارُ الذي ستعطيني إياه هو مصدرُ الصور**،
   ومنه تُبنى الرسمتان — **ولا يبدأ الرفعُ قبل أن يصل**.

   > **وفي جذر المستودع مجلَّدُ `car/` فيه خمسُ صورٍ `webp` (٨٢٨ ك.ب) ليست من
   > عملي ولا مودَعة.** تُركت كما هي — **ولم تُفحص**: قرارُ §28.8 يشترط ألّا
   > تحمل شعاراً ولا طرازاً مُعرَّفاً، **وذلك فحصُ إنسانٍ ينظر لا حارسٌ يقيس**.
   > فإن كانت مرشَّحةً للكتالوج فهي تنتظر نظرَك، **ولا تدخل المستودعَ قبله**.
2. **المفتاحان اللذان لم تُشعلهما** — أُشعلا على الإنتاج بيدي:
   `vehicle_skins_enabled` و`subscription_offers_enabled` للأردن. **ولم
   تُشعلهما أنت لأنك لم تجدهما**: `stg-admin` يخدم حزمةَ ٢١ آب وفيها **عشرون
   مفتاحاً لا اثنان وعشرون**.
3. **`dist` على الخادم** — قرارُ معمارٍ لم يُحسم: `admin-panel/dist` **متجاهَلٌ
   في git**، فالسحبُ يجلب المصدرَ ولا حزمةَ معه، **ولا `node` على الخادم**.
   إمّا بناؤه هناك، **أو جعلُه أثرَ إصدارٍ يُسحب** كما تُسحب الحزم — وهو
   الأنظفُ ويتبع البوّابةَ الرابعة. **وبدونه تبقى لوحةُ الإنتاج قديمة.**

### ⚠ أيُّ لوحةٍ إلى أيِّ خادم — قِيس 2026-08-23، ويُقرأ قبل أيِّ شكوى «اللوحة فارغة»

**لوحتان لا واحدة، وكلٌّ تقرأ قاعدةً أخرى** — مقروءاً من الحزمة المخدومة لا
من `dist`:

| المضيف | الحزمة | `API_URL` فيها | الخلفية | القاعدة |
|---|---|---|---|---|
| `admin.tajora.ly` | `index-O0BwrwN2.js` | `api.tajora.ly` | `development` | **جهازُ المالك** — فيها رحلاتُ التجربة |
| `stg-admin.tajora.ly` | `index-002e1LnC.js` | `stg-api.tajora.ly` | `production` | **الخادم** |

**و`stg-admin` موجودٌ ومربوطٌ ويردّ ٢٠٠** — والسطرُ الذي كان يقول إنه «لم
يُنشأ وأن الوصول بنفق SSH وحدَه» **كان متأخّراً عن الواقع وحُذف**.

**والهاتفان يكتبان في قاعدة التطوير**: حزمتاهما تحملان `api.tajora.ly`
(مقروءاً من داخل WebView). **فمن فتح `stg-admin` وبحث عن رحلةٍ ساقها من
هاتفه لن يجدها أبداً** — لا لعطبٍ بل لأنهما خادمان.

**وقياسُ صفوف الإنتاج** (`rides=0 · drivers=0 · users=2`) **من ٢٠٢٦-٠٨-٢٢
ولم يُعَد اليوم** — إعادتُه تحتاج دخولاً بحساب مشرفٍ إنتاجيّ، **وذلك ينفق من
محاولات المالك الخمس**، فتُرك بأمره.

### ما ينتظر طرفاً ثالثاً — وبندٌ بعد الإطلاق

- **عقدُ SMS قناةً ثانية** — **بندُ إطلاقٍ لا بندٌ مؤجَّل** (`SPEC.md` §24،
  بنصِّك: «لا فتحَ للأردن على قناةٍ واحدةٍ بلا بديل»). ينتظر **مزوّداً
  خارجياً**، والتأجيلُ اليومَ مقبولٌ بإقرارك لأن الإنتاج فيه حسابان.
- **عقودُ Telr وCliQ والدفع** — صيغةُ سلكها **أفضلُ قراءةٍ لا عقدٌ محقَّق**،
  وتنتظر بياناتِ اعتمادٍ حقيقيةً لم تصل.
- **⚠️ و`GITHUB_TAXO_TOKEN` يُبدَّل** — تسرّب في نصِّ جلسةٍ سابقة. **والسجلُّ
  محادثةٌ لا ملفّ**، فلا يُنظَّف: العلاجُ التدويرُ لا الحذف.

**وبندٌ بعد الإطلاق (قرارُ المالك 2026-08-23)**: **واحدٌ وعشرون زرّاً بلا
مفتاح** — نصفُ شاشات اللوحة **لا يمكن إطفاؤها إن ساءت**. وهو الوجهُ المقابلُ
لـ«مفتاحٌ مبذورٌ بلا زرّ»، **ويُقاسان معاً في الاتجاهين**.

### جولةُ السبعة — **رحلةٌ واحدةٌ تقيس أربعاً** (كُتب 2026-08-28)

**الحساباتُ على قاعدة التطوير، وكلمتُها `TaxoTest123`.** والمفاتيحُ السبعةُ
**مشتعلةٌ كلُّها للأردن** — قِيس، فلا شيءَ يُشعَل قبل البدء.

**والترتيبُ يوفّر ستَّ جولات**: أربعٌ من السبع تركب **رحلةً واحدة**.

#### أ) رحلةٌ واحدةٌ تقيس أربعاً — **هاتفان**

| الجهاز | الحساب |
|---|---|
| هاتف ١ — الراكب | `+962790000022` «ليلى» — **معلِنةٌ أنثى**، فترى منتقيَ «كبتنة» |
| هاتف ٢ — الكبتن | `+962790000012` «هناء» — **مختومةٌ أنثى**، فتصلها الطلباتُ النسائية |

1. **هناء**: افتح تطبيق الكبتن ← اشترِ اشتراكاً من محفظتها ← «ابدأ الاستقبال».
   *(بلا اشتراكٍ ساري لا يصلها طلب — وهذا شرطُ الأربعة كلِّها.)*
2. **ليلى**: اطلب رحلةً و**أضف وجهةً ثانية** قبل الطلب، واختر «كبتنة».
   → **`women_service`** يُقاس هنا: **أيصل الطلبُ هناء وحدَها؟**
   → **`multi_stop`** يُقاس بوجود الوجهة الثانية في الطلب.
3. **هناء**: اقبل ← ابدأ ← **اقرأ سطرَ التوجيه أعلى الخريطة**.
   → **`next_instruction`**: أيتبدّل السطرُ مع الحركة، أم يبقى ثابتاً؟
4. **هناء**: عند الوجهة الأولى اضغط «وصلت المحطة» ← انتظر دقيقةً ← «تابع».
   → **`multi_stop`** يكتمل: **أتُحتسب دقائقُ الانتظار في الأجرة؟**
5. أنهِ الرحلة واقبض.
6. **ليلى**: في شاشة التقييم **اترك إكرامية**.
   → **`tips`**: أتنقص من محفظة ليلى وتزيد محفظةَ هناء **بنفس المبلغ**؟

> **وهذه الأربعُ لا تُقاس بحسابٍ واحد**: الطلبُ يحتاج راكباً يطلب وكبتناً
> يقبل، **وجهازان لأن الدفعَ والموقعَ يخصّان جهازين**.

#### ب) مشاركةُ الرحلة — **هاتفٌ ومتصفّح**

`ride_sharing`: تُطلب رحلةٌ من **ليلى** على الهاتف، ويدخل **عمر**
(`+962790000021`) من متصفّحٍ على `dev-app.tajora.ly` ويطلب رحلةً مشابهةً
بمسارٍ متقاطع. **والراكبُ الثاني لا يحتاج هاتفاً** — لا موقعَ حقيقيَّ يلزمه
لقياس أن المشاركةَ تُعرض وتُقبل.

#### ج) السحب — **هاتفٌ ولوحةٌ على الحاسوب، بلا رحلة**

`withdrawal_payout`: **هناء** ← المحفظة ← «طلب سحب». **والحدُّ الأدنى ٥٫٠٠٠
د.أ ومحجوزٌ ٥٫٠٠٠** — أي يلزمها **عشرةٌ في المحفظة** ليمرّ طلبُ خمسة.
ثمّ من اللوحة: المالية ← الطلب ← «اعتمد» ← «حُوِّل» بمرجعٍ نصّيّ.
→ **يُقاس**: أينقص رصيدُها **عند «حُوِّل» لا عند «اعتمد»**؟

#### د) الإحالة — **إصبعُك ورقمٌ حقيقيّ، ولا يُقاس بحسابٍ تجريبيّ**

`referred_reward`: **إحالةُ راكبٍ تكافئ بعد رحلةٍ واحدةٍ لا ثلاث** (مقيسٌ من
`referral_settings`: `rider` — مكافأة **٢٫٠٠٠** و`required_rides = 1`؛
و`driver` ٥٫٠٠٠ بثلاث رحلات). **فاختر برنامجَ الراكب** — يوفّر رحلتين.

1. **ليلى**: حسابي ← الإحالات ← انسخ رمزَها.
2. **سجّل حساباً جديداً برقمك الحقيقيّ** وأدخل الرمز عند التسجيل.
   **وهنا يلزم إصبعُك**: التسجيلُ يحتاج رمزَ واتساب يصل هاتفاً حقيقياً —
   **ولا يُقاس بحسابٍ مبذور** لأن المبذورَ لا يمرّ بالتسجيل أصلاً.
3. أكمل **رحلةً واحدةً** بالحساب الجديد.
4. → **يُقاس**: أتصل ليلى **٢٫٠٠٠** في محفظتها، ومتى؟

#### ما يلزمه هاتفان وما يكفيه جهازك

| الميزة | ما يلزم |
|---|---|
| `multi_stop` · `women_service` · `next_instruction` · `tips` | **هاتفان** — في رحلةٍ واحدة |
| `ride_sharing` | هاتفٌ + متصفّحٌ على جهازك |
| `withdrawal_payout` | هاتفٌ + اللوحةُ على جهازك — **بلا رحلة** |
| `referred_reward` | **رقمٌ حقيقيٌّ يستقبل واتساب** — وهو الوحيدُ الذي لا يُقاس بحسابات التطوير |

> **⚠ وشيءٌ يُصلَح قبل الجولة**: `+962790000011` «زيد السائق» **مختومٌ أنثى**
> في قاعدة التطوير (مقيسٌ 2026-08-28) — أثرُ سيناريو سابق. فإن استُعمل في
> قياس الخدمة النسائية **أعطى نتيجةً كاذبةً في الاتجاهين**. استعمل «هناء»
> للكبتنة، و«زيد» لا يصلح ضابطاً ذكَراً حتى يُصحَّح ختمُه من اللوحة.

### جولةُ الهاتف القادمة — **خمسةٌ تُجمع في جلسةٍ واحدة** (قرارُ المالك 2026-08-23)

**ولا تُبعثر**: كلُّها بإصبعٍ على جهاز، وتفريقُها يُنفق خمسَ جلساتٍ على ما
تكفيه واحدة.

| # | ما يُقاس | لماذا لا يكفي غيرُ الإصبع |
|---|---|---|
| ١ | **صفحتا الراكب والكبتن في اللوحة** — والوصولُ إلى الرحلة من كلٍّ منهما | لم تُفتحا قطّ؛ ولا يُعرف أثمّة رابطٌ من الشخص إلى رحلته |
| ٢ | **بكسلُ العلامة** على الخريطة الحيّة وهي تتحرّك | قِيست الإحداثياتُ في الردّ **ولا الرسمُ** |
| ٣ | **تطبيقُ الكبتن بالإصبع** — لا بالـAPI | جلساتُ الرمز تشابكت بين الهاتفين في جولة ٢٣ آب |
| ٤ | **`at_stop` على الخريطة الحيّة** | مفردتان اليوم (`available` · `on_ride`)، **والواقفُ عند محطةٍ يُقرأ «في رحلة»** — وتوسيعُها يمسّ نموذجَ حال الكبتن فلا يُبنى بلا إذن |
| ٥ | **زرُّ التحويل بعد إصلاحه** | أُصلح وقِيس بـCDP؛ **والضغطةُ الحقيقيةُ لم تقع** |

### أولُ خمسةِ أعمالٍ للجلسة القادمة — بترتيبها

1. **أكمل جولةَ الجهاز** من الخطوة ٢ (القائمةُ في §٢-ج) — **ولا تُقاس قبل
   رفع صور الكتالوج**، وإلا كان المتجرُ فارغاً.
2. **القياسُ الحيُّ للاثنين والعشرين مفتاحاً** — بابٌ حقيقيٌّ لكلٍّ **وضابطٌ
   في كلِّ مرة**، فيصير جدولُ الأثر مقيساً. **والمسحُ النصّيُّ لا يُبنى عليه**:
   جُرّب فنفاه الحيُّ في حالتين من ثلاث.
3. **شاشةُ الإعدادات**: قسمٌ واحدٌ بأقسامٍ فرعية، **وجملةُ أثرِ كلِّ مفتاح من
   الجدول لا نصّاً يشيخ**. وتحت `check:doors`.
4. **تسجيلُ تبديل المفاتيح في التدقيق** — من ومتى وأيُّ مفتاحٍ ومن أيِّ حالٍ
   إلى أيّ.
5. **شاشةُ مشتريات المركبات** — مستقلّةٌ لا بطاقةً في الكتالوج (قرارُ المالك:
   «مالٌ يخرج من محافظ الكباتن له شاشتُه»): سجلُّ المشتريات · كم بيع من كلٍّ
   وكم بقي · **المستهلَك من ميزانية الشهر المجاني** · ومركبةُ كلِّ كبتنٍ
   النشطةُ في صفحته. وتحت `check:doors`.

### ما أُنجز في هذه الجلسة — رؤوسُ أقلام

**الدمجُ والرفع**: الفروعُ الأربعةُ مدموجة · ١١٦٦ اختباراً خضراء ·
**البوّابتان الرابعةُ والخامسةُ بُنيتا** (لم تكونا مبنيّتين، والرأسُ يعدّ
خمساً ويطبّق ثلاثةً ونصفاً) · **والنسخةُ صارت أربعةً** ومقيسةً بمحتواها.

**وأعطابٌ أُصلحت**: `.env` كان يُنسَخ **وصلةً بلا سرّ** والبوّابةُ تُخضِّره ·
`_trim_and_fit` يُصغّر ولا يُكبّر · بابُ الرسمة لم يخدم ملفاً قطّ (مفاتيحُ
مخترعة) · **ستةُ أنواعِ مالٍ بلا اسم** في كشف الكبتن وسابعٌ في حال الحساب ·
**مفتاحُ السلف يوقف المنحَ ويُبقي الأخذ صامتاً** · وثلاثةُ مساراتٍ تقفز فوق
`check:contract`.

**وحرّاسٌ جُدد**: `check:client-doors` · مدُّ `check:contract` إلى بناءِ
العناوين خارج الطبقة · **ومدُّ `check:enums` إلى النقص لا المخترَع وحدَه**.

### ⚠️ ما تغيّر تحت الأقدام — يُقرأ قبل أيِّ قياس

- **قاعدةُ التطوير كُتبت** (لا الإنتاج): `vehicle_skins_enabled/JO = true` ·
  صفوفُ مركباتٍ وأسعارِها ومِلكيّاتٍ · واشتراكٌ يدويٌّ لـ`+962791300013`.
  **و`FEATURE_DEFAULTS` هو الجوابُ عن «ما يُشحن» لا
  `SELECT * FROM feature_flags`.**
- **`+962790000011` صار `pending`** في قاعدة التطوير.
- **وعلى الإنتاج**: `vehicle_skins_enabled` و`subscription_offers_enabled`
  أُشعلا للأردن · **عرضُ الشهر المجاني قائمٌ بسقف ٣٠٠٫٠٠٠ د.أ** · وكبتنُ
  الجولة `+962791300099` معتمَدٌ ومشحونٌ ٥٠٫٠٠٠ د.أ بسببٍ مكتوبٍ في التدقيق.
  **ولا حسابَ ثانٍ ولا شحنَ ثانٍ بلا إذنٍ جديد.**

---

## ٠-ب. قراراتُ هذه الجلسة — كلُّها كُتبت في ملفّاتها

**والموضعُ هو المرجع؛ هذا فهرسٌ يدلّ عليه لا نسخةٌ ثانية.**

| القرار | موضعُه |
|---|---|
| **الأسماءُ من عندنا لا أسماءُ الشركات** · ولا شعارَ ولا اسمَ صانعٍ في أيِّ حقلٍ أو ملفّ · والرسمُ **يوحي ولا يقلّد** · ويسري على **ما يُرفع** كما يسري على المولَّد | `SPEC.md` §28.8 |
| **التصنيفُ بمنطقٍ مكتوب**: الندرةُ حقلٌ **يُقرأ ولا يُستنتج**، ومعها `visible_before_accept` و`map_rotates` و`max_supply` و`level_required` | `SPEC.md` §28.8 |
| **التعديلُ من اللوحة بشروطه الثلاثة**: السعرُ المجمَّدُ لا يتحرّك · وما مُلِك لا يُنتزع · وكلُّ تعديلٍ في التدقيق | `SPEC.md` §28.8 |
| **«ما دُفع ثمنه يبقى، وما يُباع يتوقف»** — ووجهُها الثاني: **الدَّينُ حقٌّ نشأ قبل الإطفاء فلا يُسقطه، والمفتاحُ يمنع الجديدَ ولا يمحو القائم** | `SPEC.md` §4 |
| **لكلِّ مركبةٍ شكلان لا واحد** — مجسّمٌ وعلويّة، **مصدرٌ واحدٌ لثلاثة مواضع** · وما لا يحملهما لا يُعرض | `SPEC.md` §28 |
| **«أثمّة قيمةٌ مخترعة؟» ليست «أينقص عضو؟»** — والنقصُ لا يراه `tsc` لأن اتحاداً أصغرَ صحيحٌ في نفسه · ستةُ أنواعِ مالٍ بلا اسمٍ وسابعٌ في حال الحساب | `CLAUDE.md` (الفهرس) |
| **شكلان لشيءٍ واحد** — لا يمسكهما اختبارُ البابين لأنهما ليسا بابين | `CLAUDE.md` (الفهرس) |
| **مفتاحٌ مبذورٌ بلا زرّ** — ميزةٌ لا يعرف بها إلا من كتبها · **ووجهُها المقابل**: زرٌّ بلا مفتاح | `CLAUDE.md` (الفهرس) + `HANDOFF.md` |
| **زرٌّ يكتب مسارَه بيده يقفز فوق `check:contract`** — والحارسُ يحرس البابَ ولا يرى من قفز السور | `CLAUDE.md` (الفهرس) |
| **ثلاثُ تسمياتٍ لشيءٍ واحد** — عائلةُ `detail`: اسمٌ واحدٌ لمعنى واحد، والنصوصُ من سجلٍّ مركزيّ | `CLAUDE.md` (الفهرس) |
| **لا يُزاح إلا ما يتصادم** — ما لا مسارَ له في الإيداع الهدف **ملكُ الخادم** لا نسخةٌ قديمة | `CLAUDE.md` (البوّابة الرابعة) |
| **شرطُ البناء JDK 21** — يُكتب **حيث يُقرأ** لا في وثيقةٍ جانبية | `tools/apk-manifest.mjs` (رأسُه) |
| **قرارا المتجر**: المركبةُ تُرسم قبل القبول (النادرةُ محجوبةٌ أصلاً والبديلُ يملكه الجميع) · و**«الرصيدُ بعده» حقلٌ على `SkinOut`** — طرحٌ واحدٌ في الخلفية وبابٌ واحدٌ لقارئين | `SPEC.md` §28.7 |
| **الوقفةُ تُبطَّن**: لا وقوفَ لتفصيلٍ داخل قرارٍ مأذونٍ به، ولا لشكلِ شاشةٍ أو تسميةٍ أو ترتيب، ولا لإعلان بدءٍ أو انتهاءِ بند | `CLAUDE.md` (قاعدةُ الوقوف) |

**وواحدٌ بقي في رأسي ولم يكن مكتوباً — يُكتب هنا:** **صمتُ الحارس عن
المرفوع** — `check:artwork` يقيس **ما وُلِّد**، ولا أداةَ تقيس أن رسمةً
مرفوعةً لا تحمل شعاراً أو طرازاً مُعرَّفاً. **فهو شرطٌ بشريٌّ لا حارس**،
وقد كُتب كذلك صراحةً في §28.8 كي لا يُقرأ سكوتُ الحارس ضماناً.

---

## ١. حالُ الأشياء — سطرٌ لكلٍّ

| | الحال |
|---|---|
| **الخادم** | Contabo · Ubuntu 24.04 · **في فرنسا** (Lauterbourg) · ٤ أنوية · ٧ جيجا · قرصٌ ١٠٠ جيجا |
| **الدخول** | مفتاحُ `ed25519` بعبارةِ مرور · كلمةُ المرور ودخولُ `root` **مطفآن ومُثبَتان** · جدارٌ ناريٌّ يفتح SSH وحدَه |
| **الحاويات** | `db` · `redis` · `backend` · `whatsapp-gateway` · **`worker` و`beat` — كلُّها تعمل** (رُفعت 2026-08-20، والجدولةُ مقيسةٌ من سجلّ beat لا من الملف) |
| **القاعدة** | مُرقّاةٌ إلى **`0056`** (قِيس 2026-08-23 عند الرفع) · مبذورة · فيها **كبتنُ الجولة وعرضُ الشهر المجاني** ولا شيءَ سواهما |
| **النفق** | `taxo-prod` **يعمل** (قِيس 2026-08-23): `cloudflared` حاويةٌ قائمة، والصحّةُ تُقرأ عبره |
| **الواجهات على الإنتاج** | **صفحةُ التنزيل تعمل وتخدم الحزمتين** · واللوحةُ تخدم حزمةَ **٢١ آب** (`dist` متجاهَلٌ في git ولا `node` على الخادم — قرارٌ معلَّق، §٠) |
| **نطاقاتُ الإنتاج** | `api` · `app` · `driver` · `admin` **ما زالت على نفق جهاز المالك ولم تُمسّ** |
| **اللوحة** | تُدخَل **عبر نفق SSH وحدَه** — غيرُ معلَنةٍ على الإنترنت، وهذا ما يعوّض غيابَ العامل الثاني |
| **المحلّي** | يعمل كما كان · قاعدةُ التطوير فيها بياناتُ تجارب · لا يُخلط بالإنتاج |

**والاختبارات: ١١٦٦ تمرّ، صفرُ فشل** (٩٧ ملفاً، قِيس 2026-08-23)، والبناءاتُ
الثلاثةُ بحرّاسها خضراء.

---

## ٢. ما بُني ولم يُقَس — مقسومٌ بما يلزمه

**القسمةُ ليست بالأهمية بل بالأداة**: ما يُقاس من متصفحٍ قِيس اليوم، وما يمسّ **نظامَ
التشغيل** وحدَه يبقى — الكاميرا، والنيّات، ورؤيةُ الحزم، والإقلاعُ البارد، والإطارات.
وهذا هو **الشكلُ الحادي عشر**: عائقٌ لا يُرى إلا على جهاز.

### أ) قِيس اليوم من متصفح — لم يبقَ منه شيء

| البند | ما قِيس | القراءةُ الحرفية |
|---|---|---|
| **شريطُ «التعليمة التالية»** | على رحلةٍ حقيقيةٍ من طلبها إلى انطلاقها | على الخط: «39 م · الاتجاه نحو اليمين» — وبعد انحرافِ ٧٠٠ م: **غائبٌ صامتاً**. نصفا القرار مقيسان |
| **ضغطُ صور الوثائق** | صورةُ ٤٠٠٠×٣٠٠٠ عبر **مدخلِ الشاشة نفسِه** في حزمةِ الإنتاج | «رُفع المستند — وحسابك عاد «قيد المراجعة» … · **صُغِّرت قبل الرفع: 2.0 م.ب ← 0.3 م.ب**». وقاعدةُ «الاستبدالُ يعيد المعتمَدَ للمراجعة» ظهرت معها |
| **سطرُ عمولة TAXO** | محفظةُ الكبتن الحيّة | «عمولة TAXO هذا الشهر: 0.229 د.أ»، والحركةُ «عمولة TAXO · 10.00٪ · −0.229» |
| **شاشةُ تسجيل الكبتن** | فُتحت لأول مرة | الشروطُ المنشورةُ ترتسم حيّةً: «8 محارف على الأقل ✓ / 128 محرفاً على الأكثر» |
| **شاشةُ استعادة كلمة المرور** | فُتحت لأول مرة | «هذا إثبات ملكية الرقم لا دخولاً» — الفرقُ الذي بُني ولم يُقرأ قط |
| **ورقةُ الترحيب** | ارتسمت حيّةً بعرضٍ حقيقي | «اشتراك شهري بـ24.000 د.أ / عرض الافتتاح — 30.000 د.أ» |
| **حزمُ `stg`** | مبنيّةٌ ومنشورةٌ وتُخدَم | المضيفاتُ الأربعة تجيب، والخلفيةُ `db+redis: ok` |

**ما لم يُقَس منها على الإنتاج وحدَه**: ورقةُ الترحيب هناك تقرأ **بيانات الإنتاج**، وقاعدتُه
غيرُ قاعدةِ التطوير: **حسابان، ولا كبتنَ واحد** — فلا حسابَ تُفتح به الورقةُ هناك.
والبياناتُ التي تقرؤها موجودة (٦ خطط، **عرضٌ واحد**، ولا مهامّ — فسطرُ المهامّ لا
يرتسم هناك بحقّ). وفتحُها على الإنتاج يحتاج **كبتناً مسجَّلاً**، وهو محجوبٌ بـ§أ-٤.

### ب) جولةُ الهاتف — القائمةُ كاملةً في مكانٍ واحد

**ترتيبُها ترتيبُ الجلوس**: ما يُقضى بالحزمة المثبَّتة أولاً، ثم ما يحتاج طريقاً.

| # | ما يُفعل | لماذا لا يكفي المتصفح | ما يُقرأ |
|---|---|---|---|
| ١ | **تثبيتُ الحزمتين من `taxo.tajora.ly`** على الهاتفين | **مثبِّتُ الحزم** نظامٌ لا صفحة: «مصادرُ غيرُ معروفة» ونصُّ الإذن حوارٌ يرسمه أندرويد | أن التنبيهَ قبل التحميل ظهر، وأن التثبيت تمّ، والحزمةُ `ly.tajora.rider` / `…driver` |
| ٢ | **زرُّ التبديل ④** (راكب↔كبتن) بالإصبع في الاتجاهين | `intent://` سلوكُ كروم لا WebView · **رؤيةُ الحزم** في أندرويد ١١+ تُخفي التطبيقَ الآخر · الإقلاعُ البارد يسلّم الرابطَ بـ`getLaunchUrl()` لا `appUrlOpen` | أن التطبيقَ الآخر فتح **بجلسةٍ قائمة** لا على شاشة الدخول، وأن «غيرُ مثبَّت» جوابٌ من النظام لا تخمين |
| ٣ | **رفعُ وثيقةٍ من الكاميرا** لا من المعرض | الحجمُ قِيس (2.0 م.ب ← 0.3 م.ب) — الباقي **EXIF ودورانُ الصورة**، وهما من العدسة | أن الصورةَ المرفوعة **غيرُ مقلوبة**، والنصُّ «صُغِّرت قبل الرفع» يرتسم |
| ٤ | **كاميرا الملاحة** — **ولا تُبنى قبل قياسها** | إطاراتٌ وحرارةٌ وبطارية: لا يقيسها إلا زجاجٌ في شمس | **ثلاثُ عتبات** (قرارُك 2026-08-20): دون **٣٠** إطاراً/ث مرفوضة · بطاريةٌ فوق **١٥٪** لنصف ساعةٍ مرفوضة · بلوغُ `THROTTLING` يُسقط القياس. على **الهاتف الأضعف**، برحلةِ ثلاثين دقيقة |
| ٥ | **رمزُ واتساب على الإنتاج** — بعد كلمتك في §أ-٤ | إثباتُ الوصول يدٌ تقرأ شاشةَ واتساب | أن الرمزَ وصل، ومن **أيِّ رقمٍ** ظهر للمستلِم |
| ٦ | **جولةُ الإشعارات والخدمة — تُقضى دفعةً واحدة** | كلُّها نظامُ تشغيل: إذنٌ يرسمه أندرويد، وإشعارٌ يرسمه النظام، وخنقُ مؤقتاتٍ وبطاريةٌ لا تُقاس إلا على زجاج | تفصيلُها في §٢-ج أدناه |

### ج-٠) **ترتيبُ القياسين — سؤالُ المالك 2026-08-21، وجوابُه**

**«نصفُ الساعة تسبق الطلبَ أم تليه؟»** — **الاثنان قياسان مختلفان، وخلطُهما
يُفسد كليهما**:

| القياس | ما يشترطه |
|---|---|
| **«يصل والشاشةُ مقفلة»** | خدمةٌ **سليمةٌ لم تُخنق بعد** — أي بعد دقائقَ من القفل لا بعد نصف ساعة |
| **«يبقى بعد نصف ساعة»** | القفلُ الطويلُ نفسُه |

**فلو سبقت النصفُ ساعةِ الطلبَ**، وكانت الخدمةُ قد اختنقت في الدقيقة الثالثة،
لَجاء الطلبُ فلم يصل — **ولا نعرف: أالإشعارُ لم يصل أم لم يُصنع عرضٌ أصلاً؟**
وهي عينُ قاعدة «الشاشةُ الفارغةُ ليست دليلاً حتى يُعرف سببُ فراغها».

**فالترتيبُ النهائيُّ: طلبٌ مبكّرٌ، ثم النصفُ ساعة، ثم طلبٌ ثانٍ** — فيقيس
الأولُ الوصولَ والثاني البقاء، **ولا يُخلطان**. **ويسبق كلَّ طلبٍ قراءةُ
مفتاح الحضور في Redis** (أفعلها أنا)، فإن لم يُصنع عرضٌ عرفنا ذلك **قبل** أن
نفسّر صمتَ الإشعار.

**وتصحيحٌ ثانٍ في القائمة**: حزمةُ الكبتن على صفحة التنزيل **لا تحمل الخدمةَ
ولا إضافةَ الإشعارات** — فتثبيتُها من الصفحة يمحو ما نقيسه. **فتُقاس الصفحةُ
بحزمة الراكب**، وحزمةُ الكبتن تُثبَّت بالكابل. **ونشرُها على الصفحة رفعٌ
بخمس بوّابات — قرارُك، لا خطوةٌ في جولة.**

### ج) **جولةُ الجهاز كاملةً — اثنتا عشرةَ خطوةً في جلسةٍ واحدة**

**وهاتفٌ واحدٌ يكفي للاثنتي عشرة** (مقيسٌ من محتوى الخطوات لا مُقدَّر):
الحزمتان تُثبَّتان على جهازٍ واحد، **وزرُّ التبديل يحتاجهما على الجهاز نفسِه
أصلاً**، **وطلبُ الرحلة أسوقه أنا من الـAPI** — فالمقيسُ في الخطوتين ٦ و٨
**إشعارُ الكبتن** لا شاشةُ الراكب، والطلبُ زنادٌ لا قياس.

**والذي يتغيّر بتبديل الجهاز شيئان لا أكثر**: **البطاريةُ والخنق** (٥ و٧)
خاصيّةُ جهازٍ لا خاصيّةُ تطبيق — وإدارةُ الطاقة عند سامسونج تختلف بالطراز،
**والأشدُّ خنقاً أصدقُ للقياس**. **والثاني أن ما قِيس حتى الآن كان على S21**
(غيابُ `Notification`، وسقوطُ الحضور في أقلَّ من ٣٠ ثانية، ثم ≈١٢٠ بالخدمة) —
**فإتمامُها على S21 يصل بالأرقام إلى نسبٍ واحد**، وعلى Note 20 تُفتح نقطةٌ
ثانيةٌ وتبقى أرقامُ S21 بلا تأكيدٍ للبناء الجديد.

**وما لا يتغيّر**: الخطواتُ وترتيبُها وما يُقرأ في كلٍّ منها.

**ما أفعله أنا لحظةَ يُوصَل الهاتف** (دقيقتان، ولا يحتاج يدَك): تثبيتُ حزمة
الكبتن الجديدة، وقراءةُ بصمة الحزمة، وتأكيدُ أن الأعمدة الثلاثة متطابقة.
**والحزمُ الثلاثُ مبنيّةٌ ومنشورةٌ الآن**، والحزمةُ الأصلية جاهزةٌ في
`driver-app/android/app/build/outputs/apk/debug/`.

| # | ما تفعله | ما يُقرأ — **وما يعنيه** |
|---|---|---|
| ١ | **صِلِ الهاتفَ بالكابل**، وتصحيحُ USB مفتوح | — |
| ٢ | **ثبّت حزمةَ الراكب من `taxo.tajora.ly`** | التنبيهُ قبل التحميل ظهر · التثبيتُ تمّ · الحزمةُ `ly.tajora.rider`. **وحزمةُ الكبتن بالكابل**: المنشورةُ لا تحمل الخدمة |
| ٣ | **افتح تطبيق الكبتن وسجّل الدخول** | **يظهر حوارُ إذن الإشعارات — اقبله**. وهو أولُ مرةٍ يُطلب فيها في تاريخ المشروع |
| ٤ | **اضغط «ابدأ الاستقبال»** | إشعارٌ دائم: **«متصل — تستقبل الطلبات»**. **واضغطه**: يفتح التطبيق |
| ٥ | **أقفِل الشاشة، وانتظر ثلاثَ دقائقَ لا أكثر** | — (أقرأ أنا مفتاحَ الحضور: **حاضرٌ = العرضُ سيُصنع**) |
| ٦ | **الشاشةُ مقفلة: اطلب رحلةً من هاتف الراكب** — **قياسُ الوصول** | **أيصل إشعارُ الطلب؟** ونصُّه بحروفه · **وهل تفتح الضغطةُ بطاقةَ العرض**. **ولا تقبلها** — دعها تنقضي |
| ٧ | **أعد قفلَ الشاشة نصفَ ساعةٍ كاملة** — **قياسُ البقاء** | **نسبةُ البطارية قبل وبعد** · وأن الإشعارَ بقي «متصل». **وانقلابُه إلى «انقطع الاتصال» بلاغٌ لا عطبٌ في القياس** |
| ٨ | **والشاشةُ ما زالت مقفلة: اطلب رحلةً ثانية** | أيصل بعد نصف ساعة؟ **وهنا يفترق «يصل» عن «يبقى»** |
| ٩ | **أغلق تطبيق الكبتن تماماً** (إزاحةٌ من المهامّ) وأعد الطلب | **المتوقَّعُ ألّا يصل، والإشعارُ يختفي** — الخدمةُ توقف نفسَها عمداً. **قياسٌ يثبّت الحدَّ لا ينفيه** |
| ١٠ | **أطفئ إشعارات TAXO من إعدادات الهاتف**، ثم افتح الرئيسية | سطرٌ فوق زرِّ الاستقبال: «إشعارات هذا الجهاز مغلقة — لا تنبيهَ بصوتٍ ولا على الشاشة المقفلة…» |
| ١١ | **زرُّ التبديل ④** (راكب↔كبتن) بالإصبع في الاتجاهين | أن التطبيقَ الآخر فتح **بجلسةٍ قائمة** لا على شاشة الدخول |
| ١٢ | **ارفع وثيقةً من الكاميرا** لا من المعرض | أن الصورةَ **غيرُ مقلوبة**، وأن «صُغِّرت قبل الرفع» يرتسم |

**والرابعةُ من القائمة القديمة — كاميرا الملاحة — ليست هنا**: بندُ إطلاقٍ
بعتباتٍ ثلاث، **ولا تُبنى قبل قياسها**. ورمزُ واتساب على الإنتاج محجوبٌ
بقرارك في §أ-٤ لا بأداة.

---

## ٣. ما ينتظر المالكَ بالضبط

### أ) إصبعُه على الهاتف — **تُجمع في جلسةٍ واحدة**

1. ~~مسحُ رمز ربط واتساب~~ — **وقع بالفعل**: الجلسةُ `linked` منذ
   `2026-08-20T10:30:22Z`، `needs_human:false`، `last_error:null`. **ويبقى
   سؤالٌ واحد**: الرقمُ المرتبط `447366186776` (**بريطانيّ**) لا
   `218930385734` الليبيُّ المسجَّل في §24.8 — أيُّهما المرسِل المقصود؟
   **ولا يُفتح واتساب في مكانٍ ثالث** — فتحُه في مكانٍ آخر هو ما أسقط الجلسةَ
   بـ`conflict` في ٢٠٢٦-٠٨-٢٠.
2. **رفعُ وثيقةٍ من تطبيق الكبتن** بعد إعادة البناء — ليُقاس الحجمُ قبل الضغط
   وبعده على ملفٍ حقيقيّ.
3. **ضغطُ زرِّ التبديل ④** — سطرٌ ولقطة.

### أ-٣) **تصحيح: لم يكن هناك إغلاق — كنتُ أطرق بابَ غريب**

**ما أبلغتُ به كان خطأً، وسببُه أهمُّ منه.** طرقتُ `173.212.220.101` وليس عنوانَ
خادمنا. ذلك العنوان يخدم موقعاً اسمه **penguinlabs** على `nginx/1.20.1` و`OpenSSH_7.4`
— جهازُ طرفٍ ثالث. وخادمُنا **`169.58.207.123`**، والدخولُ إليه يعمل بلا تعديل:
`taxo@vmi3521522`، والتحصينُ **نافذٌ كما كُتب** (`permitrootlogin no` ·
`passwordauthentication no` · `pubkeyauthentication yes`).

**والدرسُ في «البرهان» لا في العنوان.** قلتُ «الخادمُ هو هو — مفتاحُ المضيف لم
يتغيّر»، وكان ذلك **دوراناً**: مِسباري نفسُه كتب ذلك المفتاح في `known_hosts` قبل
دقائقَ بـ`StrictHostKeyChecking=no`، ثم قارنتُه بنفسه فوجدتُه مطابقاً. **مطابقةٌ
لقيمةٍ كتبتَها أنت ليست إثباتاً**، وهي الشكلُ الذي حذّرت منه قاعدةُ المِسبار الثانية:
حارسٌ — أو مِسبارٌ — **يخترع** حقيقةً أغلى من واحدٍ يفوته.

**فتُضاف قاعدةُ مِسبارٍ سابعة**: `StrictHostKeyChecking=no` **يكتب** ما ستقارن به
لاحقاً، فلا يُستعمل في مِسبارٍ يقيس هويةَ مضيف — ويُستعمل `yes`، فيصير الاختلافُ
رفضاً لا سكوتاً.

**و`173.212.220.101` يُترك في `known_hosts` ولا يُعاد إليه**، كما تُرك
`102.213.180.186` بقرارك: حذفُه يمحو الدليلَ لا الخطر.

---

### أ-٤) **الأردنُ على الإنتاج لا يستطيع التسجيل — صفٌّ واحدٌ ناقص**

**مقيسٌ من البابِ الذي يقرّر، لا من جدول.** `GET /config` على الإنتاج ينشر للأردن:

```
JO | التحقق: firebase | المشتعل: cliq, card, wallet, otp_verification, country_visible
```

و`firebase` يردّ `auth/billing-not-enabled` (رفضتَ Blaze)، و`otp_verification_enabled`
**مشتعل** — فالتحققُ **مطلوبٌ ومستحيل**. وهي عينُ نتيجة تدقيق 2026-08-19، أُصلحت على
جهاز التطوير **ولم تُنقل**. وهذا هو بالضبط ما سجّلناه في §24.5: **مفاتيحُ كلِّ سوقٍ
تُراجَع لكلِّ بيئة**، لا مرةً واحدةً في مكانٍ واحد.

**وما يلزم موجودٌ كلُّه**: عقدُ واتساب **عالميٌّ ونشط** على الإنتاج، والبوابةُ
**مرتبطة**. الناقصُ صفُّ `whatsapp_otp_enabled/JO` وحدَه.

**ولم أشعله، وهذا سببُه**: رقمُ الإرسال على الإنتاج **`447366186776`** — بريطانيٌّ،
غيرُ الليبيِّ الموثَّق في §24.8. فإشعالُه يبدأ إرسالاً حقيقياً إلى أرقامٍ أردنية من
رقمٍ أجنبيٍّ عارٍ، وهو ما وصفتَه أنت بأنه «مشكَّلٌ كالاحتيال». **ولا شيءَ يُعطَّل
اليوم**: الإنتاج فيه حسابان ولا كبتنَ واحد.

---

### أ-٢) ~~ثلاثةٌ قِيست على الإنتاج~~ — **أُغلقت كلُّها 2026-08-21**

| كان | صار |
|---|---|
| الخادمُ متأخّرٌ بستَّ عشرةَ التزاماً | **مطابقٌ للمودَع** — ٢٤٤ ملفَّ `backend/app` بفرقٍ صفر |
| الخلفيةُ بلا `CORS_ORIGINS` | حاضرةٌ بثلاثة نطاقات، ومنها اللوحة |
| نفقُ الإنتاج ساقط | يعمل — **خمسةُ مضيفات** بعد إضافة `stg-admin` (وقِيس 2026-08-23 أنه يردّ ٢٠٠) |

**ويُترك السطرُ ليُقرأ الدرس**: هذه الثلاثةُ كانت مترابطةً، وإصلاحُ الثاني بلا
الثالث لا يُقاس. **ورتّبها: نشرٌ ← بيئةٌ ← نفق.**

### د) القراراتُ الأربعةُ بعد الدمج — بخياراتها وأثرِ كلٍّ (٢٠٢٦-٠٨-٢٢)

**١ — مركبةُ الكبتن على خريطة الراكب قبل القبول.** الخلفيةُ تنشرها
(`NearbyDriverOut.skin`)، **والمرآةُ في تطبيق الراكب تُسقطها عمداً وبتعليقٍ
مكتوب**. والنادرةُ لا تصل أصلاً (`publishable_skin_for`)، فالمعروضُ هو
**البديلُ الذي يملكه الجميع**.

| الخيار | أثرُه |
|---|---|
| **أ) تبقى مسقَطة** | خريطةُ ما قبل القبول لا تتغيّر أبداً — وحقلٌ يُنشر ولا يُقرأ يبقى **باباً بلا زرّ** |
| **ب) تُرسم** | كلُّ السيارات تصير البديلَ نفسَه — **لا فرقَ يُقاس** لأن النادرةَ محجوبة، فالمكسبُ شكلٌ أجمل بلا وشاية |
| **ج) تُرسم النادرةُ أيضاً** | **يُنقض §10**: أيقونةٌ «يقتنيها ثلاثةٌ فقط» معرِّفٌ ثابتٌ يعيش أبداً — وقد رفضتَه في §28.3/١ |

**التوصية: (ب)**. الحمايةُ في `publishable_skin_for` لا في صمت المرآة، وهي
مقيسةٌ باختبارٍ يقارن حمولةَ صاحبِ النادرة بحمولة صاحبِ العادية **شكلاً
وقيمة**. و(أ) تُبقي حقلاً ينتقل على السلك ولا يقرؤه أحد.

**٢ — «الرصيدُ بعده» قبل الشراء.** `StoreOut` ينشر `balance`، و`SkinOut` ينشر
السعر، **و`BuySkinOut` ينشر `balance_after` بعد الشراء وحدَه**.

| الخيار | أثرُه |
|---|---|
| **أ) حقلٌ على كلِّ `SkinOut`** | الطرحُ في الخلفية (§14)، ويظهر في البطاقة وورقةِ التأكيد معاً — **بابٌ واحدٌ لقارئين** |
| **ب) بابُ معاينةٍ للشراء** | نداءٌ ثانٍ قبل كلِّ تأكيد — تأخيرٌ في لحظةٍ يُضغط فيها زرُّ مال |
| **ج) يبقى غائباً** | الكبتنُ يؤكّد دفعاً لا يعرف ما يتركه له — **وأول شكوى تُمنع بسطرٍ يُقرأ قبل الدفع** (علّتُك في §28.3/٦) |

**التوصية: (أ)** — طرحٌ واحدٌ في الخلفية، ولا نداءَ ثانٍ في مسار مال.

**٣ — رسمُ `rare`/`legendary`.** المولّدُ غطّى العاديةَ والمميزة. **ولا يُبنى
لهما شيءٌ اليوم**: بطاقةٌ برسمةٍ مكسورةٍ أسوأُ من فئةٍ لا تظهر.

| الخيار | أثرُه |
|---|---|
| **أ) ترفع رندراً واقعياً** (PNG/WebP بخلفيةٍ شفافة) | البابُ جاهزٌ ومعقَّم، و`map_rotates:false` للواقعيّ مكتوبٌ في الصفّ |
| **ب) يولَّد متجهيٌّ كالباقي** | «أسطورية» ترسم كالعادية — **ندرةٌ لا يفهمها من دفع** |
| **ج) تُخفى الفئتان حتى ترفع** | المتجرُ يعرض ما له رسم، ولا وعدَ لا يُنجَز |

**التوصية: (ج) الآن و(أ) متى رفعتَ** — والقاعدةُ قاعدتُك في §28.3/٤.

**٤ — القناةُ الثانيةُ للتحقق. مقيسٌ من الكود الذي يقرّر**: البديلُ
**`None` في السوقين**. فايربيس *متاحٌ* لكنه ليس قناةَ رمزٍ (يُرسَل من جهاز
المستخدم)، فلا يصلح بديلاً — والرسائلُ القصيرةُ عقدُها **غيرُ نشط**.

| الخيار | أثرُه |
|---|---|
| **أ) قناةٌ واحدةٌ بلا بديل** | يومَ تسقط الجلسةُ أو يُحظر الرقمُ **يقف التسجيلُ في السوقين معاً**. الكشفُ يعمل (إشعارٌ لكلِّ مشرفٍ في ٥٣ث) **والإصلاحُ إنسانٌ يمسح رمزاً** |
| **ب) عقدُ رسائلَ قصيرةٍ حقيقيّ** | بديلٌ فعليّ — يحتاج تعاقداً ومالاً، وحقلٌ في صفحة العقود بلا نشر |
| **ج) Firebase بخطة Blaze** | قناةٌ ثالثةٌ قويّة — رفضتَها لكلفتها، وهي **ليست قناةَ رمزٍ** فلا تُغني في مسار الرمز |

**التوصية: (ب) قبل الإطلاق**، و**(أ) مقبولةٌ اليوم** لأن الإنتاجَ فيه حسابان
ولا كبتنَ واحد — **فالخطرُ مؤجَّلٌ لا منتفٍ**. ومعها **دليلُ إصلاحٍ مكتوبٌ في
سطرين** لمن يوقظه الإشعار.

**وخامسٌ ظهر بالقياس اليومَ وليس قراراً مؤجَّلاً بل حالاً**: حزمتا APK على
صفحة التنزيل **ليستا ما بُني** — بصمتاهما متّسقتان مع أنفسهما ومع القرص،
**ونسبتُهما تقول «بُنيت من شجرةٍ متّسخة»** وأثرا الإصدار على GitHub
يخالفانهما. **فما في يد من ينزّل اليوم غيرُ ما نقيسه** — وهو الشكلُ العاشرُ
في وجهه الخامس. **ونشرُهما رفعٌ بخمس بوّابات**، فلم يقع.

### هـ) رفعةُ `0052 → 0056` — ما قِيس قبلها (٢٠٢٦-٠٨-٢٢)

**أذِن بها المالكُ بشروطٍ ثلاثة، وهذا ما قِيس استيفاءً لها.**

**١) ماذا تفعل كلُّ ترحيلة، وأتمسّ صفوفاً قائمةً أم بنيةً فارغة:**

| الترحيلة | ما تفعل | أتمسّ صفوفاً قائمة؟ |
|---|---|---|
| `0053` بلاغُ صورة الراكب | جدولٌ جديد `user_photo_reports` · **عمودان على `users`** (`photo_path`, `photo_hidden_at`) كلاهما `NULL`-able | **صفّان** يكتسبان عمودين فارغين — ولا قيمةَ تُكتب |
| `0054` إعداداتُ الخريطة | جدولٌ جديد `map_settings` | لا |
| `0055` كتالوجُ المركبات | ثلاثةُ جداولَ جديدة · **عمودٌ على `drivers`** (`active_skin_id`) `NULL`-able | **صفرُ صفوف** (`drivers = 0`) |
| `0056` نوعُ قيدِ الشراء | **قيمةُ تعدادٍ** `skin_purchase` + إعادةُ بناء قيد الإشارة | **لا صفَّ البتّة** — تعريفُ نوعٍ لا بيانات |

**٢) عدُّ الصفوف في جداول المال — بالعدّ لا بالتقدير** (قِيس على الإنتاج،
قراءةٌ محضة):

```
alembic=0052 · users=2 · drivers=0 · rides=0
wallet_transactions=0 · payments=0 · driver_subscriptions=0
withdrawal_requests=0 · tips=0 · promo_codes=0 · subscription_offers=1
```

**فصفرُ صفوفِ مالٍ تُمسّ.** والصفُّ الوحيدُ في `subscription_offers` **تعريفُ
عرضٍ لا مالٌ تحرّك**، ولا ترحيلةَ من الأربع تكتب فيه حرفاً.

**٣) وماذا يقع لو أُرجعت — مقيسٌ بتشغيلها لا بقراءتها.** دورةٌ كاملةٌ على
قاعدةٍ خادشة (`taxo_downgrade_probe`، حُذفت بعدها):
`0052 → 0056 → 0052 → 0056` بلا خطأ. وما بقي بعد النزول:

| ما قِيس | النتيجة |
|---|---|
| الجداولُ الخمسةُ الجديدة | **لا شيء** — حُذفت كلُّها |
| الأعمدةُ الثلاثةُ المضافة | **لا شيء** |
| رأسُ القاعدة | `0052` |
| **قيمةُ التعداد `skin_purchase`** | **باقية** |

**والباقيةُ الوحيدةُ مقصودةٌ ولا ضررَ فيها**: بوستجرس **لا يحذف قيمةَ تعداد**
(قاعدةٌ مكتوبةٌ في `CLAUDE.md`)، **وقيدُ الإشارة يُعاد بناؤه بدونها** — فأيُّ
صفٍّ يحمل النوعَ يُرفض. أي أن الحارسَ يعود ويبقى **اسمٌ لا يستعمله أحد**.

### ب) سرٌّ أو حسابٌ لا يملكه غيرُه

| ما ينقص | الأثر |
|---|---|
| **اعتماداتُ Telr** (`store_id` · `auth_key`) | لا دفعَ بالبطاقة |
| ~~SMS~~ · ~~payout~~ · ~~cliq_acquirer~~ | **مؤجَّلةٌ بقراره** — لا تُنتظر |

### ج) قراراتٌ معلَّقة

- **إسقاطُ `users.role` مؤجَّلٌ بشرطه** (§22.5، قِيس 2026-08-20): لا دورةَ نشرٍ
  مضت، وستُّ قراءاتٍ ما زالت تضمّ العمود. **والثالثُ وحدَه يبرّر التأجيل**:
  §25.6/٣ تُنشئ **أولَ مشرفٍ على الإنتاج** بإدخال صفٍّ مباشرةً — فبعد
  الإسقاط يصير **صفّاً يخوّل ولا يظهر في قائمة**. وصار الإجراءُ يكتب صفَّ
  الدور معه.
- ~~**مكافأةُ حاملِ رسم الإلغاء**~~ — **ليست معلَّقة: أُسقطت بقرارك 2026-08-16**
  (`design/CANCELLATION-FEE.md` §6-أ)، ومكانَها شكرٌ صريحٌ **مبنيٌّ ويعمل** في
  `publish_cancellation_carried`. كانت مُدرجةً هنا سهواً.
- **هل تتحمّل الشركةُ فرقَ الراكب الباقي قبل الانطلاق** في الرحلة المشتركة
  (SPEC §5.12). **والمبنيُّ اليوم: يُرفع السعر** (تصفيرُ النسبة المجمَّدة).
- **معدَّلُ الوقفة**: رقمٌ واحدٌ للحالتين أم اثنان (§5.10-ب). **والمبنيُّ رقمٌ
  واحد**، وهو قراءةُ البنّاء لا نصُّك — فالتأكيدُ أو النقضُ لك.
- **قيدُ `Android key`**: مؤجَّلٌ **مع توقيع النشر** لا سهواً — قيدُه بصمةُ
  توقيعِ حزمةٍ لا توجد بعد (الحزمُ موقَّعةٌ بتوقيع تطوير)، فيُضبط في الجلسة
  التي يُولَّد فيها التوقيع. و`Browser key` **مقيَّدٌ ومقيسٌ في الاتجاهين**
  (SPEC §٢٧٫٧-أ). **وأثرُه**: `127.0.0.1` لم يعد مُدرَجاً، فمسارُ
  Firebase-ويب من جهاز التطوير مرفوضٌ — **وإدراجُ `localhost` قرارُك**.
- **سقفُ OTP لكل IP**: بقي في `routers/auth.py` بعلّةٍ مكتوبة (سؤالٌ آخر لا بيتٌ
  ثانٍ) — **والمفتوحُ فيه شيئان قِيسا**: أنه ثابتٌ في الكود لا يحرّره المشرف،
  وأنه **يُحسب قبل الإرسال** فيستهلك عطبُ قناتنا حصّةَ من خلف الشبكة — وهي
  بعينها العلّةُ التي أُصلح لأجلها سقفُ الرقم.

---

## ٤. ما بقي بعد قائمة §٠

**القائمةُ الحيّةُ هي §٠** — هذا القسمُ لِما لا يدخل فيها.

1. **البند ٨**: إسقاطُ عمود `users.role` — آخرُ شيء، بعد أن يستقرَّ ما قبله
   (وشرطُ تأجيله ما زال قائماً، §٣-ج).
2. **`at_stop` مفردةً في حال الكبتن على الخريطة الحيّة** — **لا يُبنى بلا
   إذنٍ مستقلّ** (قرارُ المالك 2026-08-23): `_state_of` يعرف قيمتين
   (`available` · `on_ride`)، **فالكبتنُ الواقفُ عند محطةٍ يُقرأ «في رحلة»**.
   وتوسيعُها يمسّ **نموذجَ حال الكبتن** وهو منطقٌ قائم.
3. **مسارُ الرحلة على الخريطة الحيّة** — **تحسينٌ لا عطب** (قرارُ المالك
   2026-08-23). نقاطُ `ride_route_points` موجودةٌ ويحملها بابُ التفاصيل
   (`route` + `route_truncated`)، **ولا ترسمها الخريطةُ الحيّة**.
4. **النصفُ الثاني من قرار (أ) في رفع الرسمة** — النداءُ صار صحيحاً
   (`…/artwork/{slot}`) ويرفع إلى `store`، **ولم تُبنَ حالةُ «خانةٌ فارغة»
   صريحةً**: من رفع للمتجر تبقى علويّتُه فارغةً بلا سطرٍ يقولها. **وهو
   نصفُ قرارٍ مُعلَنٌ لا عطبٌ مكتشَف.**
5. **الوضعُ الأفقيُّ في تطبيق الكبتن — يُقاس قبل أن يُقرَّر** (بند المالك
   ٢٠٢٦-٠٨-٢٣). قُفل تطبيقُ **الراكب** على الرأسيّ لعلّةٍ مقيسة (إطارُ
   ٨٠٠×٣٦٠ يترك منطقةَ تمريرٍ ارتفاعُها صفر). **ولم يُمسّ غلافُ الكبتن
   إطلاقاً** — وعلّةُ استثنائه أن هاتفَه **يُثبَّت أفقياً في السيارة**
   وكاميرا الملاحة قادمة، فالأفقيُّ عنده قد يكون الوضعَ الأصلَ لا الشاذّ.
   **وما يُقاس قبل القرار**: شاشةُ الرحلة النشطة وبطاقةُ العرض والوقفةُ في
   ٨٠٠×٣٦٠ على جهازٍ حقيقيّ، ثم عتباتُ كاميرا الملاحة الثلاث. **ولا يُقفل
   ولا يُترك مفتوحاً بلا قياس.**
3. **مسافةُ التقدير إلى وجهةٍ بعيدة — يُتحقَّق منها ولم تُعالَج** (بند المالك
   ٢٠٢٦-٠٨-٢٣). في لقطةِ «قبلَ الإصلاح» ظهرت وجهةُ «طرابلس، شعبية طرابلس،
   ليبيا» من عمّان بـ**٢٨٢٧٫٤ كم** و**١٬١١٤٫٦٦١ د.أ** و**٤١س ٢٥د**.
   **والقراءةُ الحاليةُ أنها اختيارُ المِسبار لا عطبٌ** — «وجهاتٌ أخيرة» في
   حساب الاختبار تحمل نقطةً ليبيةً من جلسةٍ سابقة، فحُسب تقديرٌ صحيحُ الحساب
   لمسارٍ لا معنى له. **ولم يُقَس شيءٌ من ذلك**: لم يُفحص هل يقبل الطلبُ
   وجهةً خارج السوق أصلاً، ولا ما حدُّ المسافة إن وُجد، ولا ما تفعله التسعيرةُ
   عبر الحدود. **فيبقى بنداً يُتحقَّق منه**، ولا يُقرأ سكوتُه سلامة.

**وما لا يُبدأ**: ثغرةُ الاعتماد (اشتراطُ فتح كلِّ وثيقةٍ قبل زرِّ الاعتماد)
مؤجَّلةٌ بأمره حتى تستقرَّ الوثائق.

> **وحُذف من هنا قسمٌ كامل** (٢٠٢٦-٠٨-٢٢): «التحقّقُ من multi-stop — العطبُ في
> عرضِ المال»، وكان يقول إن رسمَ الانتظار والمحطات لا سطرَ له في أيِّ شاشة وإن
> البناءَ موقوفٌ على كلمة المالك. **وقد بُني وقِيس في ٢٠٢٦-٠٨-٢١** — قِيس اليومَ
> في الكود: `AdminRideDetail` يحمل الخمسة، و`RideEstimateOut` يحمل شروطَ الوقوف
> **قبل الطلب**، ومحلُّها التقديرُ لا `GET /config` (فحتى «بابُهما واحدٌ» كان
> خطأً). **وحُذف ولم يُترك سجلاً بقرار المالك**: هذا **وصفٌ متأخّرٌ عن الواقع**
> لا تشخيصٌ خاطئ يُتعلَّم منه — ومن يقرؤه يبني على ما ليس كذلك. وهو من عائلة
> **الجدول الأحمر**: كانت `SPEC.md` §٥٫١٠-ب/و تحمل قسمين متتاليين، أولُهما «غيرُ
> مبنيّ» وثانيهما «بُني وقِيس»، وهذا الملفُّ يشير إلى الأول.

---

## ٥. الحرّاسُ الثمانية — تُشغَّل مع كلِّ بناء

| الحارس | يمنع |
|---|---|
| `check:scale` | صنفٌ مفقودٌ من سلّم البكسل — يُصرَّف بلا أثرٍ ولا خطأ |
| `check:enums` | اتحادُ سلاسلَ يخالف تعدادَ الخلفية، أو يخلط قيمةً مخترعةً بحقيقية |
| `check:digits` | خانةٌ عربية-هندية في نصٍّ معروض، أو مُنسِّقٌ بلا لغةٍ مثبَّتة |
| `check:slot` | `asChild` بأكثرَ من ابنٍ واحد — يُفرِّغ الشاشةَ وقتَ التشغيل |
| `check:flags` | مفتاحُ ميزةٍ بلا زرٍّ في شاشة الإعدادات |
| `check:config` | حقلٌ تنشره الخلفيةُ ولا مرآةَ له، أو مرآةٌ بلا مُرسِل |
| `check:doors` | مسارٌ إداريٌّ بلا زرٍّ — **أو استثناءٌ بُني له زرٌّ ولم يُنظَّف** |
| `check:contract` | نداءٌ من تطبيقٍ لا يقابله **فعلٌ ومسارٌ** في الخلفية |

ومعها في التطبيقين: `check:target` (هدفٌ غيرُ مُصرَّحٍ به) و`check:dist` (حزمةٌ
تخالف هدفَها أو فيها عنوانٌ محلّيّ).

**وقاعدةُ كلِّ حارسٍ جديد**: **يُقاس في الاتجاهين قبل أن يُصدَّق** — يمسك عطباً
مصنوعاً، **ويصمت على شجرةٍ سليمة**. وبلاغُه الأولُ لا يُبنى عليه أمرٌ قبل التحقق
منه استقلالاً: حارسٌ **يخترع** عطباً يُفقد الثقةَ بما يجده حقاً، وقد وقع ذلك
فعلاً في ٢٠٢٦-٠٨-٢٠.

---

## ٦. خطوةُ صفرٍ الثلاثية — جزءٌ من التسليم لا خطوةٌ تُتذكَّر

**قبل أيِّ قياسٍ على جهازٍ أو متصفح، وبعد كلِّ بناء**، يُملأ هذا الجدولُ في
التقرير:

| التطبيق | المبنيُّ في `dist` | ما يخدمه الحاويةُ | ما يمرّ بالنفق | **ما تخدمه صفحةُ التنزيل** | **المثبَّتُ على الهاتف** | **ما تخدمه الحافة** |
|---|---|---|---|---|---|---|
| `admin-panel` | بصمةُ الحزمة | **تطابق** | **تطابق** | — | — | — |
| `customer-app` | … | … | … | بصمةُ APK | بصمةُ المثبَّت | `cf-cache-status` |
| `driver-app` | … | … | … | بصمةُ APK | بصمةُ المثبَّت | `cf-cache-status` |
| `landing` | — | … | … | بصمةُ APK | — | **`cf-cache-status` + `Age`** |

**والعمودُ الخامسُ صار مقيساً لا مكتوباً** (قرارُ المالك 2026-08-21): **ما
تخدمه صفحةُ التنزيل = ما بناه CI = وسمُه**. و`tools/check-apk.mjs` يقرأ بيانَ
الصفحة نفسِها — إيداعَه ووسمَه — **ويقارنه بأثر الإصدار على GitHub**، فما
تخدمه الصفحةُ لم يعد يُصدَّق لأنه متّسقٌ مع نفسِه. **وما لا يُقاس يُعلَن**:
بلا وسمٍ أو رمزٍ يقول «لم يُقس» ولا يُقرأ سكوتُه ضماناً.

**والعمودان الأخيران لنفس المنتج، وهذا وجهٌ جديدٌ للشكل العاشر** (قرارُ المالك
2026-08-21): **ما يُقاس بالكابل وما يُنزَّل من الصفحة شيئان**، وأحدُهما
**يتأخّر صامتاً**. وقع في هذه الجلسة: ثبَّتُّ حزمةَ الكبتن بالكابل وفيها
خدمةُ الاستقبال وإضافةُ الإشعارات، **وصفحةُ التنزيل ما زالت تخدم بناءَ ١٩ آب
بلا واحدةٍ منهما** — فلو قِيست الجولةُ على المنشورة لَقيست منتجاً آخر، ولو
ثبَّتها إنسانٌ من الصفحة **لَمحا ما نقيسه**.

**والقاعدةُ**: كلُّ حزمةٍ تُقاس بالكابل **تُنشر أو يُقال صراحةً أنها لم
تُنشر** — ولا تُترك الصفحةُ متأخّرةً بينما يُقاس غيرُها.

**خمسةُ أعمدةٍ منذ 2026-08-21** (قرارُ المالك). **والقاعدةُ التي تجمعها: قِس ما
تقرؤه الحافةُ لا ما يخرج من الأصل** — يُقاس عند آخر نقطةٍ قبل القارئ لا عند
أوّلِ نقطةٍ نملكها: الحاويةُ المحليةُ قد تخدم حزمةً
جديدةً بينما يخدم النفقُ قديمة، والهاتفُ قد يحمل أقدمَ من الاثنتين، **وصفحةُ
التحميل قد تعطي إنساناً ملفّاً أقدمَ من الثلاثة ويبقى في هاتفه شهوراً**،
**والحافةُ قد تخدم نسخةَ أمس وكلُّ ما نملكه صحيح** — وقع مقيساً: `cf-cache-status:
HIT` و`Age: 1086` تحت `max-age=14400` بعد نشرةٍ سليمة.

**والعمودُ الرابع يُملأ بأمرٍ واحد**:

```
node tools/apk-manifest.mjs                    # يولّد البيان من مخرج البناء
node tools/check-apk.mjs https://taxo.tajora.ly  # يقارن الأربعة
```

**ولا يُقارَن برقم النسخة**: قِيس أن الحزمتين تقولان `versionName=1.0`
و`versionCode=1`، ونسختان بفارق خمسةِ أيامٍ وستةِ ميجابايت تحملان الرقمَ نفسَه.
**فالمقارنةُ بالبصمة وحدَها.**

**والقاعدةُ التي تجمع الخمسة: قِس ما تقرؤه الحافةُ لا ما يخرج من الأصل.**
كلُّ عمودٍ هو هذا المبدأ في طبقة — الشجرةُ ليست `dist`، و`dist` ليس ما تخدمه
الحاوية، وما تخدمه الحاويةُ ليس ما يمرّ بالنفق، وما يمرّ بالنفق ليس ما تخزّنه
الحافة، **وما تخزّنه الحافةُ ليس ما في يد إنسان**. فيُقاس عند **آخر نقطةٍ قبل
القارئ**، لا عند أوّلِ نقطةٍ نملكها.

**والعلّة**: «شجرةٌ خضراء» لا تقول شيئاً عن القطعة التي يشغّلها إنسان. كلّف هذا
المشروعَ **ثلاثةَ تحقيقاتٍ منفصلة** — ورقةُ تحويلٍ ترسل المال بضغطةٍ واحدة،
وزرُّ عرضِ وثيقةٍ «غيرُ موجود»، ورفعٌ «يرسل POST». **ولم يكن أيٌّ منها عطباً في
الكود.**

---

## ٧. قواعدُ يُشتغَل بها هنا

- **المواصفةُ هي المرجع.** أيُّ انحرافٍ يُكتب في `SPEC.md` **في الجلسة نفسِها**،
  وإلا صار بعد أسبوعٍ فرقاً لا يُميَّز عن عطب.
- **يُقاس ولا يُفترض.** «فشلٌ تحت الحمل» فرضيةٌ لا تشخيص، و«الإعدادُ مكتوب» ليس
  «الإعدادُ سارٍ» — يُقرأ من الأداة التي تستهلكه (`sshd -T` مثلاً) لا من الملف.
- **قفلٌ لا يُثبَت بحذفه لا يُصدَّق.** كلُّ اختبارِ تزامنٍ يُتحقَّق منه بحذف القفل
  ورؤيته يسقط.
- **لا نجاحَ يُعلَن قبل التحقق ممّا كُتب**: كلُّ مسارٍ يكتب ملفاً يُقرّ بالنجاح
  **بعد أن يرى الملفَّ بحجمه** لا بعد انتهاء النقل.
- **مساعدُ اختبارٍ يختصر مساراً حقيقياً يصير مصدرَ حقيقةٍ ثانياً** — وكلُّ حقلٍ
  يضيفه الأصلُ ولا يضيفه المساعد يجعل الاختباراتِ تقيس عالماً لا وجودَ له.
- **`102.213.180.186` في `known_hosts` يُترك ولا يُعاد إليه** (قرارُ المالك
  2026-08-20): أقدمُ من المشروع — لا أثرَ له في الشجرة ولا في `git log --all -S`
  ولا في أيِّ ملفّ، أُضيف بدخولٍ ناجحٍ قديم (ثلاثةُ أنواعِ مفاتيح بلا سطرِ
  `ssh-keyscan`، أي `UpdateHostKeys`)، **ومفتاحُ مضيفه تغيّر منذُها**. وحذفُه
  يمحو الدليلَ لا الخطر. **ولا يُتصل به، ولا يُتجاوَز تحذيرُ تغيّر مفتاحٍ على أيِّ
  عنوان.**
- **الأسرارُ في `.env` على الخادم وحدَه** — لا في `compose`، ولا في صورة، ولا في
  شجرةٍ، ولا في تقرير. **ولا احتياطَ صامتٌ لسرّ**: بيئةٌ ناقصةٌ تُوقف الإقلاعَ
  وتسمّي الناقص.
- **متى يُتوقَّف ويُسأل**: القاعدةُ كاملةً في `CLAUDE.md` («When to stop and ask»).
  خلاصتُها: يُتوقَّف لقرارِ نموذجٍ أو معمار، أو تعارضٍ مع المواصفة، أو ما يمسّ مالَ
  غيرِ المالك أو أمانَه ولم يُحسم، أو خيارين يختلف أثرُهما على المستخدم، أو سرٍّ
  لا يملكه غيرُه، أو ما يحتاج إصبعَه. **ولا يُتوقَّف** لعرض جدولٍ، ولا لإذنٍ بإصلاح
  عطبٍ مكتشَف، ولا لتأكيد شرطٍ مسجَّل، ولا لسؤالٍ يحسمه القياس. **والأسئلةُ تُجمع
  في تقريرٍ واحد.**
