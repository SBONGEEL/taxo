# تسليم — TAXO

**آخرُ تحديث: ٢٠٢٦-٠٨-٢٠.** هذا الملفُّ يُقرأ أولاً في كلِّ جلسةٍ جديدة. **ولا
سرَّ فيه ولا مسارَ إلى سرّ.**

المرجعُ الكامل في `SPEC.md` (المواصفة والقرارات) و`CLAUDE.md` (كيف يُشتغَل على
هذا المستودع، والدروسُ المستخلَصة). هذا ملخَّصُ حالٍ لا بديلٌ عنهما.

---

## ١. حالُ الأشياء — سطرٌ لكلٍّ

| | الحال |
|---|---|
| **الخادم** | Contabo · Ubuntu 24.04 · **في فرنسا** (Lauterbourg) · ٤ أنوية · ٧ جيجا · قرصٌ ١٠٠ جيجا |
| **الدخول** | مفتاحُ `ed25519` بعبارةِ مرور · كلمةُ المرور ودخولُ `root` **مطفآن ومُثبَتان** · جدارٌ ناريٌّ يفتح SSH وحدَه |
| **الحاويات** | `db` · `redis` · `backend` · `whatsapp-gateway` · **`worker` و`beat` — كلُّها تعمل** (رُفعت 2026-08-20، والجدولةُ مقيسةٌ من سجلّ beat لا من الملف) |
| **القاعدة** | مُرقّاةٌ إلى `0051` · مبذورة · **فارغةٌ من بيانات التشغيل** (لا رحلاتٍ ولا مستخدمين إلا حسابَي المشرف) |
| **النفق** | `taxo-prod` مستقلٌّ عن نفق التطوير · عليه `stg-api` و`stg-app` و`stg-driver` |
| **نطاقاتُ الإنتاج** | `api` · `app` · `driver` · `admin` **ما زالت على نفق جهاز المالك ولم تُمسّ** |
| **اللوحة** | تُدخَل **عبر نفق SSH وحدَه** — غيرُ معلَنةٍ على الإنترنت، وهذا ما يعوّض غيابَ العامل الثاني |
| **المحلّي** | يعمل كما كان · قاعدةُ التطوير فيها بياناتُ تجارب · لا يُخلط بالإنتاج |

**والاختبارات: ١٠٧٢ تمرّ، صفرُ فشل** (٩٤ ملفاً)، والبناءاتُ الثلاثةُ بحرّاسها
خضراء.

---

## ٢. ما بُني ولم يُقَس بعد

**لا يُقال «يعمل» عن شيءٍ في هذا الجدول.**

| البند | ما ينقصه |
|---|---|
| **ضغطُ صور الوثائق** | بُني في تطبيق الكبتن (١٦٠٠ بكسل · جودةُ ٠٫٨)، **ولم يُقس على ملفٍ من كاميرا حقيقية** |
| **زرُّ التبديل ④** (راكب↔كبتن) | المسارُ واحدٌ والمكتبةُ نسخةٌ واحدة، لكنه **لم يُضغط بحدِّ الأداة قط** |
| **قناةُ البطاقة (Telr)** | لا اعتمادات — **غيرُ مُثبَتةٍ على جهاز** (SPEC §18.1-ب) |
| **كاميرا الملاحة** (البند ٦ من عمل الخريطة) | **لا تُبنى قبل قياس الإطارات على جهازٍ حقيقيّ** — شرطُ المالك |
| **حزمُ `stg`** | لم تُبنَ بهدف النطاقات التجريبية بعد |

---

## ٣. ما ينتظر المالكَ بالضبط

### أ) إصبعُه على الهاتف — **تُجمع في جلسةٍ واحدة**

1. ~~مسحُ رمز ربط واتساب~~ — **وقع بالفعل**: الجلسةُ `linked` منذ
   `2026-08-20T10:30:22Z`، `needs_human:false`، `last_error:null`. **ويبقى
   سؤالٌ واحد**: الرقمُ المرتبط `447366186776` (**بريطانيّ**) لا
   `218930385734` الليبيُّ المسجَّل في §24.8 — أيُّهما المرسِل المقصود؟
   **ولا يُفتح واتساب في مكانٍ ثالث** — فتحُه في مكانٍ آخر هو ما أسقط الجلسةَ
   بـ`conflict` في ٢٠٢٦-٠٨-٢٠.
2. **رفعُ وثيقةٍ من تطبيق الكبتن** بعد إعادة البناء — ليُقاس الحجمُ قبل الضغط
   وبعده على ملفٍ حقيقيّ.
3. **ضغطُ زرِّ التبديل ④** — سطرٌ ولقطة.

### ب) سرٌّ أو حسابٌ لا يملكه غيرُه

| ما ينقص | الأثر |
|---|---|
| **اعتماداتُ Telr** (`store_id` · `auth_key`) | لا دفعَ بالبطاقة |
| ~~SMS~~ · ~~payout~~ · ~~cliq_acquirer~~ | **مؤجَّلةٌ بقراره** — لا تُنتظر |

### ج) قراراتٌ معلَّقة

- ~~**مكافأةُ حاملِ رسم الإلغاء**~~ — **ليست معلَّقة: أُسقطت بقرارك 2026-08-16**
  (`design/CANCELLATION-FEE.md` §6-أ)، ومكانَها شكرٌ صريحٌ **مبنيٌّ ويعمل** في
  `publish_cancellation_carried`. كانت مُدرجةً هنا سهواً.
- **هل تتحمّل الشركةُ فرقَ الراكب الباقي قبل الانطلاق** في الرحلة المشتركة
  (SPEC §5.12). **والمبنيُّ اليوم: يُرفع السعر** (تصفيرُ النسبة المجمَّدة).
- **معدَّلُ الوقفة**: رقمٌ واحدٌ للحالتين أم اثنان (§5.10-ب). **والمبنيُّ رقمٌ
  واحد**، وهو قراءةُ البنّاء لا نصُّك — فالتأكيدُ أو النقضُ لك.
- **سقفُ OTP لكل IP**: بقي في `routers/auth.py` بعلّةٍ مكتوبة (سؤالٌ آخر لا بيتٌ
  ثانٍ) — **والمفتوحُ فيه شيئان قِيسا**: أنه ثابتٌ في الكود لا يحرّره المشرف،
  وأنه **يُحسب قبل الإرسال** فيستهلك عطبُ قناتنا حصّةَ من خلف الشبكة — وهي
  بعينها العلّةُ التي أُصلح لأجلها سقفُ الرقم.

---

## ٤. أين نقف — أولُ ما تفعله الجلسةُ القادمة

**بالترتيب، ولا يُبدأ بغيره:**

1. **إعادةُ بناء التطبيقات الثلاثة ونشرُها** — الضغطُ بُني بعد آخر بناء، فما
   يخدمه الحاويةُ لا يحمله. ثم **خطوةُ صفرٍ الثلاثية** (§٦ أدناه).
2. **شاشةُ ترحيب الكبتن وبندُ عمولة TAXO** — مواصفتُهما في أمر المالك:
   ورقةٌ عائمةٌ عند أول فتحٍ **بعد الاعتماد** لا عند التسجيل، ونصوصُها من السجل
   المركزي، وتحت `check:doors`؛ وبندُ «عمولة TAXO» في كلِّ رحلةٍ داخل المحفظة
   بقيمته **المجمَّدة**، ومجموعٌ شهريٌّ في الرأس. **والوعدُ المكتوب**: «صفر
   عمولة **ما دام اشتراكك سارياً**» — لا مطلقةً.
3. **البند ٧ من عمل الخريطة**: «التعليمةُ التالية» (تخزينُ `steps`) — **وقرارُه صار مسجَّلاً**
   (SPEC §26، والتفصيل في `design/FUTURE-FEATURES.md` §17 تحت 2026-08-20): **تختفي عند
   الانحراف ولا تُجمَّد**، ولا إعادةَ توجيهٍ جديدة، **وعتبةُ الانحراف تُقاس لا تُقدَّر**.
4. **البند ٨**: إسقاطُ عمود `users.role` — آخرُ شيء، بعد أن يستقرَّ ما قبله.
5. **التحقّقُ من multi-stop** — تقريرٌ قبل أيِّ بناء.

**وما لا يُبدأ**: ثغرةُ الاعتماد (اشتراطُ فتح كلِّ وثيقةٍ قبل زرِّ الاعتماد)
مؤجَّلةٌ بأمره حتى تستقرَّ الوثائق.

---

## ٥. الحرّاسُ الثمانية — تُشغَّل مع كلِّ بناء

| الحارس | يمنع |
|---|---|
| `check:scale` | صنفٌ مفقودٌ من سلّم البكسل — يُصرَّف بلا أثرٍ ولا خطأ |
| `check:enums` | اتحادُ سلاسلَ يخالف تعدادَ الخلفية، أو يخلط قيمةً مخترعةً بحقيقية |
| `check:digits` | خانةٌ عربية-هندية في نصٍّ معروض، أو مُنسِّقٌ بلا لغةٍ مثبَّتة |
| `check:slot` | `asChild` بأكثرَ من ابنٍ واحد — يُفرِّغ الشاشةَ وقتَ التشغيل |
| `check:flags` | مفتاحُ ميزةٍ بلا زرٍّ في شاشة الإعدادات |
| `check:config` | حقلٌ تنشره الخلفيةُ ولا مرآةَ له، أو مرآةٌ بلا مُرسِل |
| `check:doors` | مسارٌ إداريٌّ بلا زرٍّ — **أو استثناءٌ بُني له زرٌّ ولم يُنظَّف** |
| `check:contract` | نداءٌ من تطبيقٍ لا يقابله **فعلٌ ومسارٌ** في الخلفية |

ومعها في التطبيقين: `check:target` (هدفٌ غيرُ مُصرَّحٍ به) و`check:dist` (حزمةٌ
تخالف هدفَها أو فيها عنوانٌ محلّيّ).

**وقاعدةُ كلِّ حارسٍ جديد**: **يُقاس في الاتجاهين قبل أن يُصدَّق** — يمسك عطباً
مصنوعاً، **ويصمت على شجرةٍ سليمة**. وبلاغُه الأولُ لا يُبنى عليه أمرٌ قبل التحقق
منه استقلالاً: حارسٌ **يخترع** عطباً يُفقد الثقةَ بما يجده حقاً، وقد وقع ذلك
فعلاً في ٢٠٢٦-٠٨-٢٠.

---

## ٦. خطوةُ صفرٍ الثلاثية — جزءٌ من التسليم لا خطوةٌ تُتذكَّر

**قبل أيِّ قياسٍ على جهازٍ أو متصفح، وبعد كلِّ بناء**، يُملأ هذا الجدولُ في
التقرير:

| التطبيق | المبنيُّ في `dist` | ما يخدمه الحاويةُ محلياً | ما يصل عبر النفق |
|---|---|---|---|
| `admin-panel` | بصمةُ الحزمة | **يجب أن تطابق** | **يجب أن تطابق** |
| `customer-app` | … | … | … |
| `driver-app` | … | … | … |

**ثلاثةُ أعمدةٍ لا عمودان**: الحاويةُ المحليةُ قد تخدم حزمةً جديدةً بينما يخدم
النفقُ قديمة، والهاتفُ قد يحمل أقدمَ من الاثنتين.

**والعلّة**: «شجرةٌ خضراء» لا تقول شيئاً عن القطعة التي يشغّلها إنسان. كلّف هذا
المشروعَ **ثلاثةَ تحقيقاتٍ منفصلة** — ورقةُ تحويلٍ ترسل المال بضغطةٍ واحدة،
وزرُّ عرضِ وثيقةٍ «غيرُ موجود»، ورفعٌ «يرسل POST». **ولم يكن أيٌّ منها عطباً في
الكود.**

---

## ٧. قواعدُ يُشتغَل بها هنا

- **المواصفةُ هي المرجع.** أيُّ انحرافٍ يُكتب في `SPEC.md` **في الجلسة نفسِها**،
  وإلا صار بعد أسبوعٍ فرقاً لا يُميَّز عن عطب.
- **يُقاس ولا يُفترض.** «فشلٌ تحت الحمل» فرضيةٌ لا تشخيص، و«الإعدادُ مكتوب» ليس
  «الإعدادُ سارٍ» — يُقرأ من الأداة التي تستهلكه (`sshd -T` مثلاً) لا من الملف.
- **قفلٌ لا يُثبَت بحذفه لا يُصدَّق.** كلُّ اختبارِ تزامنٍ يُتحقَّق منه بحذف القفل
  ورؤيته يسقط.
- **لا نجاحَ يُعلَن قبل التحقق ممّا كُتب**: كلُّ مسارٍ يكتب ملفاً يُقرّ بالنجاح
  **بعد أن يرى الملفَّ بحجمه** لا بعد انتهاء النقل.
- **مساعدُ اختبارٍ يختصر مساراً حقيقياً يصير مصدرَ حقيقةٍ ثانياً** — وكلُّ حقلٍ
  يضيفه الأصلُ ولا يضيفه المساعد يجعل الاختباراتِ تقيس عالماً لا وجودَ له.
- **الأسرارُ في `.env` على الخادم وحدَه** — لا في `compose`، ولا في صورة، ولا في
  شجرةٍ، ولا في تقرير. **ولا احتياطَ صامتٌ لسرّ**: بيئةٌ ناقصةٌ تُوقف الإقلاعَ
  وتسمّي الناقص.
- **متى يُتوقَّف ويُسأل**: القاعدةُ كاملةً في `CLAUDE.md` («When to stop and ask»).
  خلاصتُها: يُتوقَّف لقرارِ نموذجٍ أو معمار، أو تعارضٍ مع المواصفة، أو ما يمسّ مالَ
  غيرِ المالك أو أمانَه ولم يُحسم، أو خيارين يختلف أثرُهما على المستخدم، أو سرٍّ
  لا يملكه غيرُه، أو ما يحتاج إصبعَه. **ولا يُتوقَّف** لعرض جدولٍ، ولا لإذنٍ بإصلاح
  عطبٍ مكتشَف، ولا لتأكيد شرطٍ مسجَّل، ولا لسؤالٍ يحسمه القياس. **والأسئلةُ تُجمع
  في تقريرٍ واحد.**
