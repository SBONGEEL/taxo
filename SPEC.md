# SPEC.md — TAXO | تطبيق توصيل تاكسي

> هذا الملف هو المرجع الوحيد للمشروع. أي غموض → اسأل قبل التنفيذ. لا تضف ميزات غير مذكورة هنا.

---

## 1. نظرة عامة

**اسم التطبيق: TAXO** — يُستخدم في كل الواجهات، عناوين PWA manifest، ورسائل النظام.

منصة توصيل تاكسي مكونة من ثلاثة تطبيقات + خلفية واحدة:

| التطبيق | المستخدم | النوع |
|---------|----------|-------|
| Customer App | الراكب | PWA (موبايل أولاً) — ومغلَّفٌ APK باسم `ly.tajora.rider` |
| Driver App | الكبتن | PWA (موبايل أولاً) — ومغلَّفٌ APK باسم `ly.tajora.driver` |
| Admin Panel | الإدارة | Web (ديسكتوب أولاً) |

**الأسواق:**
- 🇱🇾 ليبيا — مرحلة التجريب (كاش فقط، عملة LYD)
- 🇯🇴 الأردن — الإطلاق الفعلي (كل طرق الدفع، عملة JOD)

كل الإعدادات الحساسة (طرق الدفع، الميزات، الأسعار) تُدار **per-country** من لوحة الإدارة عبر نظام Feature Flags — بدون تعديل كود.

**نموذج الربح:** اشتراك مسبق الدفع للكبتن (يومي/أسبوعي/شهري) + صفر عمولة على الرحلات مبدئياً، مع مفتاح لتفعيل عمولة نسبية من الإدارة لاحقاً.

---

## 2. التقنيات (ملزمة — لا تستبدلها)

### الخلفية
- **Python 3.12+ (يُفضَّل أحدث إصدار مستقر، 3.14 مقبول) + FastAPI** — REST API + WebSockets. قبل التثبيت تحقق أن كل الاعتماديات (SQLAlchemy, Celery, asyncpg) تدعم الإصدار المختار، وإن ظهر تعارض انزل لـ 3.12
- **PostgreSQL 16 + PostGIS** — البيانات + الاستعلامات الجغرافية
- **Redis** — مواقع السائقين اللحظية (GEO commands) + قنوات pub/sub + كاش
- **SQLAlchemy 2 (async) + Alembic** — ORM + migrations
- **Celery + Redis broker** — مهام خلفية (إشعارات، انتهاء اشتراكات، تقارير)
- **JWT** (access + refresh) — المصادقة. **الدخول اليومي بكلمة المرور دائماً ولكل المستخدمين.**
  و**OTP تحقّقٌ لا دخول**: إثباتُ ملكية الرقم يقع مرتين في عمر الحساب — عند التسجيل وعند استعادة
  كلمة المرور — عبر **Firebase Phone Auth** (التطبيق يتحقق عندهم ويرسل رمز الهوية، والخلفية تتحقق
  منه) أو عبر مزود SMS تقليدي حين لا عقد Firebase. ولا يدخل النظامَ أحدٌ بتوكن Firebase: ذاك إثباتُ
  ملكيةِ رقمٍ لا جلسةَ عمل، والجلساتُ تُصدرها الخلفية وتُبطلها (القسم 15/أ)
- إدارة الأسرار: أسرار البنية التحتية فقط (DB, Redis, JWT, مفتاح التشفير) في `.env` — **أما كل مفاتيح المزودين الخارجيين فمن صفحة العقود حصراً** (القسم 15)

### الواجهات
- **React 18 + Vite + TypeScript**
- **Tailwind CSS** — مع دعم RTL كامل من اليوم الأول (`dir="rtl"`, خصائص `start/end`)
- **shadcn/ui** — المكونات (خصوصاً Drawer للـ bottom sheets)
- **Framer Motion** — الحركات والانتقالات
- **Lucide Icons**
- خط عربي: **IBM Plex Sans Arabic**
- وضع ليلي كامل (`dark:`) — إلزامي لتطبيق الكبتن
- PWA: manifest + service worker — **والتغليفُ بـCapacitor صار مبنياً** (المرحلة 13،
  2026-08-14): حزمتان مستقلتان `ly.tajora.rider` و`ly.tajora.driver`، بأيقوناتِ كلِّ تطبيق
  وشاشةِ بدءٍ أصلية، وأذونِ الموقع والإشعارات — **والموقعُ في الخلفية للكبتن وحدَه**.
  والقطعُ الأولُ **غلافٌ حول الخادم الحي** (`server.url`) لا أصولاً محزَّمة: يُحمَّل من
  النطاق نفسِه الذي تعرفه قيودُ Mapbox ونطاقاتُ Firebase، فلا يُعاد ضبطُ تكاملٍ تحقّقنا
  منه. وثمنُه مكتوب: **لا يعمل والخادمُ مغلق**. وتحزيمُ الأصول بندٌ تالٍ يحتاج إضافةَ
  `localhost` إلى قيود توكن Mapbox أولاً

### الخرائط — Mapbox
- **Mapbox GL JS** — العرض (ستايل `streets-v12` نهاري + `dark-v11` ليلي)
- **Directions API** — المسار + المسافة + المدة (تُستدعى من الخلفية فقط)
- **Geocoding API** — العناوين (مع اعتماد أساسي على تحديد الموقع بالدبوس)
- التوكنات تُدار من صفحة العقود (لا `.env`): التوكن السري (sk) للخلفية حصراً، والتوكن العام (pk، مقيد بالنطاق) تسلّمه الخلفية للواجهات عبر endpoint إعدادات عام `GET /config`

### الدفع
- **Telr** — بطاقات + Apple Pay + Google Pay (Hosted Payment Page + Webhooks، وضع Sandbox أثناء التطوير)
- **CliQ** — يدوي: عرض alias + تأكيد بشري (لا يوجد API)
- **كاش** — تأكيد الكبتن
- **المحفظة الداخلية** — خصم فوري

---

## 3. هيكل المشروع

```
taxo/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/          # config, security, deps
│   │   ├── models/        # SQLAlchemy models
│   │   ├── schemas/       # Pydantic
│   │   ├── routers/       # auth, riders, drivers, rides,
│   │   │                  # payments, wallets, subscriptions, admin
│   │   ├── services/      # dispatch, pricing, wallet, telr
│   │   ├── ws/            # WebSocket: tracking, ride events
│   │   └── tasks/         # Celery
│   ├── alembic/
│   └── tests/
├── customer-app/          # React PWA
├── driver-app/            # React PWA
├── admin-panel/           # React
└── docker-compose.yml     # postgres+postgis, redis, backend
```

---

## 4. قاعدة البيانات (الجداول الأساسية)

> كل الجداول تحتوي `id (uuid)`, `created_at`, `updated_at`. الأعمدة المالية `NUMERIC(12,3)` — ممنوع float.

### users
- `phone` (unique — هو مُعرّف الدخول), `name`, `role` (rider/driver/admin/support)
  - **وبادئةُ كل دولة وطولُ رقمها الوطني يُنشران في `GET /config`** (`dial_code`,
    `national_number_length`) من `core/phone.py` نفسه، ومعهما `default_country_code` —
    الدولة التي تفترضها شاشاتُ ما قبل الدخول. فالتطبيقان يرسمان البادئة ثابتةً أمام الحقل
    ويحذفان الصفر البادئ، بلا كتابة «962» في كودهما: قيمةٌ مكتوبةٌ في تطبيقين تفترق عن ذلك
    الجدول يوماً وتفترق عن نفسها في التطبيقين قبله
- `country_code` (LY/JO), `is_blocked`, `password_hash` — **الدخول بهاتف + كلمة مرور دائماً**
  (المرحلة 8-ب). العمود nullable لحساباتٍ أُنشئت قبلها بـ OTP وحده، ولا يُفتح حسابٌ بلا كلمة مرور
  بواحدةٍ يخترعها أحد — مخرجُه استعادةُ كلمة المرور
- `phone_verified_at` (nullable) — **لحظةُ إثبات ملكية الرقم** (المرحلة 8-ب). فارغةٌ تعني رقماً غير
  محقق، ولا تقع إلا حين يُطفئ المشرف `otp_verification_enabled` للطوارئ. الحساب حينها **موسومٌ في
  اللوحة وقابلٌ للفلترة** ويُطالَب بالتحقق عند أول فرصة (`POST /auth/me/verify-phone`)، **ولا يُعتمد
  كبتنٌ قبل ملئها مهما كان المفتاح**: رقمُ الكبتن هو ما يستلم عليه حوالات كليك (القسم 6/9)
- `marketing_push_enabled` (افتراضي true) — إشعارات الحملات وحدها (القسم 10)
- `wallet_frozen` — تجميد المحفظة وحدها دون حظر الحساب (القسم 7/13.3): تُوقَف حركة محفظةٍ مشبوهة بينما يبقى صاحبها راكباً يدفع نقداً
- **`gender`** (male/female، nullable) و**`gender_verified_at`** (nullable) — خدمة التوصيل النسائي
  (المرحلة 10-ج). قيمتان لا ثلاث و**الغيابُ `NULL` لا عضوٌ ثالث**: «غير معروف» ليس جنساً، وجعلُه
  قيمةً يجعل الاستعلام يقارنه بجنسٍ فيطابق. ومصدرُ الحقل يختلف باختلاف من يعلنه، وهو فرقٌ في
  الأمان لا في التنفيذ:
  - **الراكبة تعلنه عن نفسها** عند التسجيل أو من ملفها (`PATCH /auth/me`) — إعلانُها يقيّد رحلتَها
    هي، فلا يُختم ولا يُشترط ختمُه.
  - **وجنسُ الكبتن يضبطه المشرف** من هويته المرفوعة (`PUT /admin/drivers/{id}/gender`، admin
    حصراً، بقيدِ تدقيق) ويُختم بـ`gender_verified_at`. **ومسارُ تسجيل الكبتن يرفض الحقل** ولا
    يُهمله صامتاً. وسببُ التشديد أن إعلانه يقيّد أمانَ غيره: بلا ختمٍ يصير بلوغُ صفة «سائقة
    للنساء» كتابةَ كلمةٍ في حقل.
  - **والمطابقة تقرأ المختوم وحده** في جانب الكبتن. وضبطُ الجنس **لا يعيد دورة اعتماد**: الهوية
    مرفوعةٌ ومراجَعةٌ أصلاً، وإرجاعُ كبتنٍ معتمدٍ إلى الطابور لأجل حقلٍ واحد يجعل تفريغ متراكم
    المعتمدين مستحيلاً عملياً — وهو المتراكم الذي يبقى `women_service_enabled` مطفأً حتى يُفرَّغ
    (`GET /admin/drivers?gender_verified=false` هو ما يجعله مرئياً)
  - **والسِمةُ النسائية عرضٌ يكفيه الإقرار — ولا تُخلط بالمطابقة** (قرارُ المالك 2026-08-13):
    السِمةُ الوردية تُفعَّل **تلقائياً** لمن أعلنت جنسها أنثى، راكبةً كانت أو كبتنة، من أول فتحةٍ
    بلا انتظار ختمٍ ولا مفتاحٍ قُطريّ — لأنها **لا تَعِد بشيء**: لا تفتح باباً ولا تُدخل مطابقةً
    ولا تجعل حساباً «سائقةً للنساء». أما **دخولُ المطابقة واستقبالُ الطلبات المجنَّسة** فيبقى على
    `gender_verified_at` كما هو. وخلطُ الاثنين هو الخطأ الذي تحرسه هذه الجملة: لونٌ معلَّقٌ على
    ختمٍ إداريٍّ يجعل التصميمَ ينتظر طابوراً، وختمٌ يُستنتج من لونٍ يجعل الأمانَ يُشترى بإقرار.
  - **ويُلغى الوضعُ النسائيُّ عن الحساب حين تخالف الوثائقُ ما أُقرّ**: يُثبّت المشرفُ الجنسَ
    المخالفَ **بسببٍ مكتوبٍ إلزاميّ** (`reason` على `PUT /admin/drivers/{id}/gender`، ومن دونه
    ٤٢٢)، فيصير `gender` غيرَ `female` — وبذلك تختفي السِمةُ **ومفتاحُها معاً**، إذ كلاهما مبنيٌّ
    على الإقرار وحده. **ولا عمودَ «مُلغى» يُضاف**: الجنسُ المختوم هو الحقيقة، وعمودٌ ثانٍ يقول
    الشيءَ نفسَه يفترق عنه أولَ مرةٍ يُكتب أحدُهما بلا الآخر.
  - **ويصلها إشعارٌ صريحٌ عند الإلغاء** (`women_mode_revoked`): اختفاءُ لونٍ وميزةٍ بلا تفسير
    تذكرةُ دعمٍ في نفس الدقيقة. **والنصُّ يقول السببَ العامَّ ولا ينقل ما كتبه المشرف**: ذاك سببٌ
    للتدقيق يُقرأ عند مراجعةٍ أو نزاعٍ بعد شهر، ونقلُه إلى إشعارٍ يجعل حكمَ موظفٍ خطاباً للحساب.

- **`ride_gender_preference`** (male/female/any، افتراضي `any`) — تفضيلُ الراكبة الافتراضي، يُنسخ
  إلى الرحلة عند الطلب ولا يُقرأ بعده
- **`gender_mismatch_reports`** (افتراضي 0) — عددُ بلاغات «الطرف ليس بالجنس المعلَن» على الحساب.
  **عدّادٌ لا راية**: الوسمُ سؤالٌ عن العدد مقابل حدٍّ في الخدمة (ثلاثة)، فتغييرُ الحدّ يعيد تقييم
  الجميع ولا يترك حساباتٍ موسومةً بحدٍّ قديم. وثلاثةٌ لا واحد لأن بلاغاً واحداً قد يكون سوءَ فهم،
  ووسمٌ من أول بلاغ سلاحٌ بيد من يريد الإضرار

### drivers (يمتد من users)
- `user_id`, `status` (pending/approved/rejected/suspended)
- `cliq_alias` (لاستلام التحويلات), `rating_avg`
- `is_online`, `current_ride_id`
- **`gender_preference`** (male/female/any، افتراضي `any`) — تفضيلُ جنس الركاب. **دائمٌ لا لكل
  رحلة**: الراكبة تختار لرحلةٍ بعينها والكبتن يقرر لعمله كلِّه، فمكانُه هنا لا على الرحلة

### driver_documents (مستندات الكبتن — المرحلة 9-ب)

الجدول قائمٌ منذ المرحلة 1 ولم يكن له مسارٌ يكتبه: كان الاعتماد يمر والمستنداتُ فارغة. المرحلة 9-ب
بنت الرفع والمراجعة وأضافت عمودَي وصف الملف.

- `driver_id`, `doc_type` — ثلاثُ وثائق (driving_license/national_id/vehicle_registration)
  **وستُّ صورٍ للمركبة أنواعاً مسمّاة** (البند ١١، قرارُ المالك 2026-08-14):
  `vehicle_front` · `vehicle_back` · `vehicle_side_right` · `vehicle_side_left` ·
  `vehicle_interior` · `vehicle_plate`. **مسمّاةٌ لا مرقّمة**: «الصورة الجانبية مرفوضة» جملةٌ
  مفهومة، و«الصورة الثالثة مرفوضة» ليست كذلك — ويبقى القيدُ الفريد `(driver_id, doc_type)`
  على حاله فلا جدولَ صورٍ ثانٍ ولا ترقيم. و`vehicle_photo` **مهجورٌ ولا يُعرض**: التعدادُ لا
  تُنزع منه قيمة، وصفوفٌ قديمةٌ تحملها
- `file_path` — **مسارٌ نسبي** إلى جذر التخزين، و`content_type` و`size_bytes` مستنتجان من **بايتات
  الملف** لا من ترويسة العميل
- `review_status` (pending/approved/rejected), `review_note`, `reviewed_by`, `reviewed_at`
- **فريد `(driver_id, doc_type)`**: النوع الواحد صفٌّ واحد، ورفعُه ثانيةً **استبدال** يعيد الحالة
  `pending` ويمحو أثر المراجعة السابقة — مراجعةٌ لملفٍ لم يعد موجوداً حكمٌ على غائب. وثلاثُ رخصٍ
  متراكمة تجعل سؤال «هل رخصتُه مقبولة؟» بلا جواب واحد
- **قرارُ سياسة: استبدالُ مستندٍ مطلوبٍ يُسقط اعتماد الكبتن نفسه** إلى `pending` تلقائياً حتى
  تُراجَع الوثيقة الجديدة. الاعتماد شهادةٌ على مستنداتٍ بعينها رآها مشرف؛ فمن استبدلها صار
  معتمَداً على ما لم يره أحد — وذاك بابُ تبديلِ رخصةٍ سارية برخصةٍ منتهية **بعد** الاعتماد،
  والاعتمادُ يفتح باب استقبال الطلبات وقبض المال. القيدُ يدخل سجل التدقيق بـ
  `{"status": "pending", "reason": "document_replaced"}` **بلا فاعلٍ مشرف** — فعلُ الكبتن نفسه هو
  ما أسقطه، وسؤال «لماذا سقط اعتمادُه؟» يُسأل بعد شهر. والمستندُ الاختياري (`vehicle_photo`) لا
  يمسّ الاعتماد لأنه لا يُشترط له
- **ويُرفض الاستبدال وحده أثناء رحلةٍ جارية** (409): إسقاطُ الاعتماد وسطها يُخرج الكبتن من
  `presence_context` فيتجمّد موقعُه على خريطة راكبه ثم يُطلق تنبيهَ «انقطع اتصال الكبتن» (القسم 5)
  على من لم ينقطع — نفس الاعتبار الذي يجعل كنسَ الاشتراكات يترك صاحب الرحلة الجارية أونلاين.
  والانتظارُ إلى نهاية الرحلة نافذةٌ واحدة قصيرة. والمستندُ الاختياري يمر: لا يمسّ الاعتماد فلا
  يمسّ الرحلة
- **وجوابُ الرفع يقول ذلك صراحةً** (`approval_reverted` و`driver_status`): بغيره يكتشف الكبتن أنه
  خرج من التوزيع حين لا تصله طلبات، لا حين فعلَ ما أخرجه
- **المطلوب للاعتماد ستّة**: الرخصة والهوية ورخصة المركبة والتأمين، **ومن صور المركبة ثلاث**:
  الأمام والخلف واللوحة. والجانبان والداخل **اختياريان** — «الثلاثةُ تكفي للتحقق، والباقي يزيد
  الثقة ولا يمنع كبتناً من العمل» (قرارُ المالك). والاختياريُّ يُراجَع كغيره: يُقبل أو يُرفض،
  ولا يحبس اعتماداً — فرقُهما في `REQUIRED_DOCUMENT_TYPES` وحدَه.
  **والقائمةُ تُنشر للتطبيق** (`GET /drivers/me/documents.required`) لا يحتفظ بنسخةٍ منها:
  الناقصُ وحدَه لا يفرّق بين اختياريٍّ ومرفوع، فمن رفع اختيارياً يظهر كمن رفع مطلوباً

#### الصورةُ الشخصية للسائق (`profile_photo`) — مسجَّلةٌ قبل البناء (2026-08-14)

> **قرارُ المالك (2026-08-14)**: تُبنى ضمن مستندات الكبتن بآلية 9-ب نفسِها، **ولا اعتمادَ
> بدونها** — إلا على من ثبَّتت الإدارةُ جنسَها أنثى. سُجِّلت هنا **قبل** البناء بطلبه، وسببُ
> الاستثناء مكتوبٌ معه كي لا يُقرأ سهواً بعد شهر فيُصلَّح بوصفه ثغرة.

- **نوعُ مستندٍ خامس** `profile_photo`، بآلية 9-ب حرفياً: النوعُ من بايتات الملف لا من ترويسة
  العميل، وسقفُ الحجم بقراءةٍ مقطّعة لا بـ`Content-Length`، والاسمُ على القرص `uuid4`،
  ومراجعةٌ إدارية (`review_status`) — فصورةُ شخصٍ آخر أو صورةٌ مسيئة تُرفض قبل أن يراها راكب.
- **وشرطٌ للاعتماد**: `drivers.approve` يشترطها كما يشترط الباقي. **والمطلوبُ لم يعد جدولاً
  ثابتاً بل دالةً** (`models/driver.py::required_document_types`) تقرأ ختمَ الجنس — إذ لا يجوز
  لجدولٍ واحدٍ أن يمنع اعتمادَ من أُعفي من الشرط أصلاً.
- **ولا أثرَ رجعياً** (قرارُ المالك 2026-08-16): الشرطُ يسري على **من يُعتمد بعده**، ومن اعتُمد
  قبله يصير **متراكماً يُلاحَق واحداً واحداً** من شاشة السائقين (`missing_required` يعرضه). وذلك
  يقع من نفسه لأن `approve` لا تُنادى إلا عند اعتمادٍ جديد — وإسقاطُ اعتمادِ من يعمل اليوم بشرطٍ
  أُضيف اليوم يوقف سوقاً بأكمله لأجل صورة.
- **واستبدالُها يُسقط الاعتماد كأيِّ مستندٍ مطلوب** (القاعدةُ أعلاه) — ما يراه الراكبُ ليس أقلَّ
  من الرخصة في هذا الباب. **لكنّ رفعَها أوّلَ مرةٍ ليس استبدالاً**: من اعتُمد قبل الشرط لا صفَّ
  له من هذا النوع، فرفعُه لا يناقض شيئاً رآه مشرف — ولولا هذا القيدُ لأسقط **أوّلُ امتثالٍ** من
  المتراكمين اعتمادَهم، أي لكان الردُّ على «ارفع صورتك» إيقافَهم عن العمل.
- **والاستثناءُ: اختياريةٌ لمن جنسُها أنثى**، ونشرُ وجهها لكل من يطلب رحلةً يفتح البابَ الذي
  وُجدت الخدمةُ النسائية (المرحلة 10-ج) لإغلاقه.
- **ويُقاس الاستثناءُ على `gender_verified_at IS NOT NULL` لا على الإقرار وحده.** الإقرارُ يقيّد
  رحلةَ صاحبته وحدها فيكفيه أن يُكتب؛ **والاستثناءُ يُسقط شرطاً على غيرها** — فلو قُرئ
  `users.gender` وحدَه لأُسقط شرطُ التعرّف **بكلمةٍ يكتبها أيُّ أحدٍ عن نفسه**. وهي نفسُ
  القراءةِ التي يعتمدها التوفيقُ في 10-ج ومكافأةُ الإحالة في 12-ح، وللسبب نفسِه: ما يمسّ
  غيرَك يحتاج ختماً.
- **وحيث لا صورة، يرى الراكبُ أوّلَ حرفٍ من الاسم بنمط التصميم** — لا مربعاً فارغاً ولا أيقونةً
  عامة: كلاهما يُقرأ **عطباً في التطبيق** لا خياراً لصاحبته، فيُبلَّغ عنه ويُصلَّح بإظهار ما
  أُخفي عمداً.
- **والمشرفُ يرى الهويةَ المرفوعة على أي حال**: الاستثناءُ يخصّ **ما يراه الراكب** لا ما تتحقق
  منه الإدارة. صورةُ الهوية مستندٌ مراجَعٌ منذ 9-ب، ولا تمسّها هذه القاعدة بحرف.
- **ومنفذُ القراءة العام أضيقُ من منفذ المستندات**: `document_response` اليومَ يفحص الملكيةَ
  ويردّ `private, no-store` — ولا يصلح لصورةٍ يراها الراكب. فالمطلوب مسارٌ ثانٍ يردّها **لطرف
  رحلةٍ قائمة** وحده، لا مسارٌ يعرض وجوهَ الكباتن لمن يعدّ المعرّفات.
- التخزين **على القرص لا في خدمةٍ سحابية**: مفاتيح أي مزوّد خارجي مكانها `provider_credentials`
  وصفحةُ العقود، فإدخال S3 اليوم يعني مفتاحاً في `.env` وهو ما يمنعه القسم 14. والمجلد حجمٌ مسمّى
  في compose، خارج Git
- **ولا يُخدَم بخادم ملفاتٍ ساكن**: القراءة تمر بمسارٍ يتحقق من الملكية أولاً، واسمُ الملف على
  القرص `uuid4` بامتدادٍ من محتواه — لا اسمَ العميل ولا امتدادَه (اجتياز المسار)

### vehicles
- `driver_id`, `make`, `model`, `year`, `color`, `plate_number`, `category` (economy/comfort)

### rides
- `rider_id`, `driver_id` (nullable قبل القبول)
- `pickup_point` (geography POINT), `pickup_address`
- `dropoff_point`, `dropoff_address`
- `status`: `requested → searching → accepted → arrived → in_progress → completed | cancelled_by_rider | cancelled_by_driver | no_driver_found`
  - **لا انتقال مباشر `requested → accepted`.** مخرجا `requested` هما `searching` (يدخلها التوزيع) أو `cancelled_by_rider` لا غير، فالقبول لا يكون إلا من `searching` وبعد عرضٍ قائم (القسم 5.4)
  - و**`at_stop`** (المرحلة 12-ب) بين `in_progress` ونفسها: `in_progress → at_stop → in_progress`
    لكل محطةٍ وسيطة، ومخرجٌ ثالثٌ منها `at_stop → completed` حين يُنهي الكبتن الرحلة عند المحطة
    بعد تجاوز سقف الانتظار (القسم 5.10). **ولا إلغاء من `at_stop`** كما لا إلغاء من `in_progress`:
    الراكب في السيارة، والانتظارُ جزءٌ من الرحلة لا فاصلٌ يعيد فتح ما أُغلق
- **`stops_count`** (المرحلة 12-ب، افتراضي 0) — عددُ المحطات الوسيطة. عمودٌ على الرحلة لا عدٌّ
  للجدول: يُقرأ في كل بطاقةِ عرضٍ وفي كل صفٍّ في اللوحة، وعدُّ جدولٍ لكل صفٍّ هو بالضبط ما
  تتجنبه بقيةُ القوائم
- **`current_leg`** (المرحلة 12-ب، افتراضي 0) — أيُّ ساقٍ يسير فيها الكبتن الآن، ومنها تُنسب
  نقاطُ المسار. **كاتبُها واحدٌ لا غير** (`rides.resume_stop` تحت قفل صف الرحلة)، فلا تفترق عن
  عدد المحطات المستأنَفة
- `distance_km`, `duration_min` (من Mapbox Directions)
- `actual_distance_km` (nullable) — المسافة المقطوعة فعلاً محسوبةً من `ride_route_points` عند الإنهاء
  (القسم 5.7). **فارغة لا صفر** حين لا تكفي النقاط: صفرٌ يعني «سارت صفر كيلومتر» فيهبط السعر إلى
  الحد الأدنى ظلماً بسبب تطبيقٍ صمت
- `estimated_fare`, `final_fare`, `currency`
- **`commission_percent_at_ride`** — تُجمّد لحظة إنشاء الرحلة (صفر مبدئياً). ممنوع إعادة حسابها بأثر رجعي
- **وأربعةُ حقولِ كوبونٍ مجمَّدةٌ عليها** (المرحلة 12-ز): `promo_code_id` و`promo_type_at_ride`
  و`promo_value_at_ride` و`promo_cap_at_ride` — **القاعدةُ لا المبلغ**: المبلغُ يعتمد على
  `final_fare` الذي لا يُعرف قبل الإنهاء، فيسكن في صفِّ دفعة `promo` وحده (القسم 6.6). والتجميدُ
  كتجميد العمولة: تعديلُ الرمز في اللوحة يحكم ما يأتي بعده لا رحلةً رأى صاحبُها خصمَها
- **وأربعةُ حقولِ انتظارٍ مجمَّدةٌ معها** (المرحلة 12-ب): `stop_fee_at_ride` و
  `stop_free_minutes_at_ride` و`stop_price_per_min_at_ride` و`stop_max_wait_minutes_at_ride`.
  تُنسخ من `pricing_rules` لحظة الإنشاء ولا تُقرأ من الإعدادات بعدها أبداً — **نفس منطق
  العمولة حرفاً بحرف**: مشرفٌ يرفع سعر الدقيقة ورحلةٌ واقفةٌ عند محطةٍ الآن لا يجوز أن يتغيّر
  عدّادُها تحت عين راكبها. وأربعةٌ لا واحد لأن كلَّ واحدٍ منها يدخل الحساب وحدَه
- **`gender_preference`** (male/female/any، افتراضي `any`) — ما طُلب في **هذه** الرحلة. يُنسخ من
  ملف الراكبة عند الطلب و**يُجمَّد** كالعمولة: تغييرُ التفضيل في الملف يحكم الطلب القادم لا طلباً
  يبحث له عن كبتنٍ الآن
- `cancelled_reason` (نصٌّ حر) و**`cancel_reason_code`** (`gender_mismatch` / `other`، nullable)
  — سببٌ **مصنَّف** بجانب النص، وهو `String(32)` محروسٌ في طبقة Pydantic لا ENUM في القاعدة (نفس
  استثناء `feature_flags.feature_key`). و`gender_mismatch` ليست تصنيفاً للتقارير: هي التي **تُسقط
  رسوم الإلغاء** وتُدخل بلاغاً على حساب الطرف الآخر، فلا تُترك لنصٍّ حر
- timestamps لكل انتقال حالة

### ride_stops (المحطات الوسيطة — تعدد الوجهات، المرحلة 12-ب)

الراكب يضيف **حتى ثلاث وجهات** في الرحلة الواحدة. والوجهةُ الأخيرة تبقى
`rides.dropoff_point` كما كانت، وهذا الجدول يحمل **الوسيطة وحدها** (صفراً إلى اثنتين):

- **ولماذا لا تُنقل الوجهاتُ الثلاث كلُّها إلى الجدول؟** لأن `dropoff_point` يقرؤه اليوم التسعيرُ
  والتوزيعُ وبطاقةُ العرض وسجلُّ اللوحة وإطاراتُ المقبس. ونقلُه يعني إمّا تعديلَ كلِّ ذلك، وإمّا
  إبقاءَ العمود **مرآةً** لآخر صفٍّ في الجدول — ومرآةٌ تُكتب في مكانين تفترق يوماً. فالوجهةُ
  الأخيرة عمودٌ واحدٌ كما كانت، والوسيطةُ صفوفٌ: لا قيمةَ لها موضعان.
- **والترتيبُ والحذفُ يقعان قبل الطلب لا بعده**: الراكب يرتّب ويحذف في شاشة الطلب، فتصل الخلفيةَ
  قائمةٌ مرتَّبة. فلا مسارَ إعادةِ ترتيبٍ على رحلةٍ قائمة — تغييرُ الوجهة بعد أن سار الكبتن
  يغيّر السعرَ المتفق عليه، وهو طلبٌ جديد لا تعديل.

الأعمدة:

- `ride_id`, `sequence` (1..2) — **فريد `(ride_id, sequence)`**
- `point` (geography POINT), `address` (nullable)
- `arrived_at`, `resumed_at` (كلاهما nullable) — **نافذةُ الانتظار، وهي مصدرُها الوحيد**:
  زمنُ الانتظار **يُحسب من ختم الخلفية لا من مؤقتٍ في الواجهة** (القسم 14)، ويُقفل عند
  الاستئناف. **ولا عمودَ `waited_minutes`**: عمودان لزمنٍ واحد عمودان يفترقان — نفس سبب غياب
  `recorded_at` عن `ride_route_points`
- `notified_at` (nullable) — لحظةُ تنبيه الطرفين ببلوغ سقف الانتظار، **أثرٌ يمنع تكرار التنبيه**
  في كل دورة كنس (نفس دور مفتاح Redis في تنبيه الاشتراك، وعمودٌ هنا لأن الصفَّ قائمٌ أصلاً)
- `ON DELETE RESTRICT` كبقية ما يتعلق بالرحلة: المحطةُ جزءٌ من فاتورةٍ، لا تُمحى بمحو غيرها

### ride_route_points (نقاط المسار الفعلي — القسم 5.7)
- `ride_id`, `point` (geography POINT), `heading` (nullable)
- **`leg`** (المرحلة 12-ب، افتراضي 0) — أيُّ ساقٍ من الرحلة: صفرٌ من الانطلاق إلى المحطة الأولى،
  ثم واحدٌ منها إلى التالية، وهكذا حتى الوجهة الأخيرة. **تُنسخ من `rides.current_leg`** لحظة
  الالتقاط، فلا تُحسب من الصفوف ولا يعرفها الملتقِط من عنده. وبها تُقرأ المسافةُ الفعلية
  **لكل ساقٍ على حدة** في تفاصيل الرحلة وفي دليل النزاع
- **لا عمود `recorded_at`:** حمولة البثّ `{lat, lng, heading}` بلا زمن، فزمن التسجيل هو `created_at`
  نفسه — وعمودان لزمنٍ واحد عمودان يفترقان. الترتيب الزمني على فهرس `(ride_id, created_at)`
- الاستثناء الوحيد لقاعدة «الموقع اللحظي في Redis وحده» (القسم 10): للنقاط غرضان لا يؤديهما مفتاحٌ
  عمره 60 ثانية — المسافة الفعلية ودليلُ النزاع
- الصف يُكتب ولا يُعدَّل، لكن **بلا مُشغّل قاعدة** بخلاف `wallet_transactions`: هذه أدلةٌ لا قيود
  مالية، وكلفةُ مُشغّلٍ على جدولٍ يستقبل نقطةً كل 20 ثانية لكل رحلة جارية أعلى من فائدته

### payments
- `ride_id`, `method` (cash/cliq/card/wallet/**promo**)، `provider` (telr/null)
  - **و`promo` قناةٌ لا يدفعها الراكب** (المرحلة 12-ز): تُنشئها المنصةُ وتؤكّدها بنفسها لحظةَ الإنهاء
    بقيمة خصم الكوبون. وهي **ليست في `WALLET_FUNDED_METHODS` ولا في `DIRECTLY_COLLECTED_METHODS`**
    — كالبطاقة تماماً: الكبتنُ يُقيَّد له والراكبُ لا يُخصم منه. ولو دخلت الأولى لخُصم من محفظة
    الراكب مبلغُ خصمٍ لم يدفعه، فيصير الكوبونُ فاتورةً عليه. وبها يصير «الشركة تتحمّل الخصم» جملةً
    تنفيذية: الشركةُ **تدفع صفَّ دفعةٍ** فتُجمع أرباحُ الكبتن وعمولتُه من الصفَّين كأن لا كوبون
- `provider_payment_id`, `amount`, `currency`
- `status`: `pending → confirmed | failed | disputed | refunded`
- `confirmed_by` (driver/system/admin), `confirmed_at`
- `idempotency_key` (nullable، فريد) — يفرضه القسم 14 على **الدفع** كما على التحويل: webhook يُعاد إرساله أو ضغطةٌ مكررة لا يجوز أن تُحصّل مرتين
- `transaction_id` (nullable) — القيد الناتج في دفتر المحفظة حين تُدفع الرحلة من الرصيد؛ الوصلة بين
  الدفعة وأثرها المالي، كما في `wallet_topup_requests` و`withdrawal_requests`
- **حقول قناة كليك** (المرحلة 9، ترحيلة `0013`): `cliq_alias` (المعروض لحظتَها — **مُجمَّد**، فلا
  يغيّر الكبتنُ وجهةَ حوالةٍ وقعت)، `cliq_reference` (المرجع الداخلي الذي يولّده TAXO، **فريد**)،
  `cliq_transfer_reference` + `cliq_reference_at` (ما أدخله الراكب ومتى — **يُكتبان مرةً واحدة**).
  بهذه الأربعة يكتمل ما يطلبه القسم 6.5: المبلغ وalias والمرجعان ومن أكّد ومتى. وقيدان في القاعدة:
  حقولُ كليك لا تُملأ على دفعةٍ بقناةٍ أخرى، و`(cliq_transfer_reference IS NULL) = (cliq_reference_at IS NULL)`
- **حقول فصل النزاع:** `dispute_reason`, `disputed_at`, `resolution` (paid/unpaid), `resolution_note`,
  `resolved_by`, `resolved_at`
  - `resolution` تصف **الواقعة** لا الحالة الناتجة: `paid` = وصل المالُ الكبتنَ فعلاً فتصير الدفعة
    `confirmed` وتُحسب عمولتها، و`unpaid` = لم يصل فتصير `failed` ويعود مبلغها ديناً على الرحلة يفتح
    له الراكب قناةً أخرى. اشتقاق الحالة من الحكم في مكان واحد يمنع صفّاً محكوماً بحالةٍ تناقض حكمه
  - قيد في القاعدة: `(resolution IS NULL) = (resolved_at IS NULL)` — حكمٌ بلا زمنٍ أو زمنٌ بلا حكم
    نصفُ فصلٍ، والنصف لا يُقرأ
- دعم الدفع المختلط: صفان مرتبطان بنفس الرحلة (wallet + cash). لذلك **لا قيد فريد على `ride_id`**،
  وحارسُ «لا تُدفع الرحلة مرتين» هو مجموع الدفعات في `pending`/`confirmed`/`disputed` مقابل
  `final_fare` تحت قفل صف الرحلة
- **قناة البطاقة لا تُقيَّد على محفظة الراكب.** ثلاث فئات لا فئتان: ما لا يمر بالمنصة (كاش وكليك)،
  وما يمر بها من المحفظة، وما يمر بها من **غير** المحفظة — البطاقة. مالُ البطاقة ينتقل من بطاقة
  الراكب إلى المزود مباشرة، فقيدُ `ride_payment` على رصيده يخصم منه مرتين. والكبتن يُقيَّد له
  `ride_earning` في الفئتين الأخيرتين معاً (القسم 6.3). الفئات مكتوبة قوائمَ في
  `models/payment.py` (`DIRECTLY_COLLECTED_METHODS` و`WALLET_FUNDED_METHODS`) لا شروطاً مبثوثة

### provider_orders (طلبات الدفع لدى مزود البطاقات — القسم 6.4، المرحلة 6-ب)

جدولٌ **أضافته المرحلة 6-ب** ولم يكن في هذا القسم: بين لحظة إنشاء صفحة الدفع ولحظة جواب المزود
فراغٌ زمنيٌّ يعيش فيه العميل على شبكةٍ لا نملكها — يغادر الراكب الصفحة، أو يصل الـ webhook مرتين،
أو يصل قبل أن يعود المتصفح. لا صفَّ في المخطط السابق يعرف «طلبٌ فُتح ولم يُحسم بعد»، ولا مكانَ
لمُعرِّفٍ نرسله للمزود ويعود به إلينا. بديلاه كانا أسوأ: تحميلُ `payments.provider_payment_id`
معنيين، أو صفُّ `wallet_topup_requests` لشحن بطاقةٍ لا ينتظر إنساناً — وهو نقضٌ لتعريف ذلك الجدول.

- `provider` (telr), `purpose` (ride_payment / wallet_topup / **subscription** — أضافته المرحلة 7)،
  `status` (created/paid/failed/cancelled)
- `cart_id` (**فريد**) — ما نرسله للمزود ويعود به؛ به وحده يجد الإشعار صاحبَه ولا يجد صاحبين.
  قصيرٌ عمداً (25 حرفاً) لسقف `ivp_cart` لدى Telr
- `provider_order_ref`, `redirect_url`, `failure_reason`, `refund_ref`
- `user_id` (الدافع), `country_code` (**دولة العقد** — العقود per-country، فبغير تثبيتها يُستعلم
  طلبُ الأمس بعقد اليوم إن انتقل صاحبه دولة), `amount`, `currency`
- `ride_id` + `payment_id` لغرض `ride_payment`، و`transaction_id` لغرض `wallet_topup`، و`plan_id`
  لغرض `subscription` (المرحلة 7) — **الخطةُ لا الاشتراك**: صفُّ الاشتراك لا يُنشأ إلا بعد أن يقول
  المزود «دُفع»، فالطلب يحمل ما يكفي لإنشائه لا وصلةً إلى صفٍّ قد لا يوجد أبداً
- `save_card` — حفظُ البطاقة قرار صاحبها لا افتراضنا
- ثلاثة قيود في القاعدة: `(purpose='ride_payment') = (ride_id IS NOT NULL)` والمثلُه على
  `payment_id`، و`(purpose='subscription') = (plan_id IS NOT NULL)`
- **حالةُ الطلب هي حارس عدم التكرار الأول:** `created` وحدها تُسوّى، فإشعارٌ مُعاد لا حدث

### saved_cards (البطاقات المحفوظة — Tokenization، القسم 6.4)
- `user_id`, `provider` (telr), `provider_token`
- `brand`, `last4`, `expiry_month`, `expiry_year`, `is_default`
- **لا رقم بطاقة ولا CVV ولا أي بيانٍ يمكن أن يُدفع به.** المخزَّن رمزٌ يصدره المزود ولا يعمل إلا لدى
  نفس المتجر، وما يكفي لعرض البطاقة لصاحبها. هذا هو كل غرض الـ tokenization: أن يبقى رقم البطاقة عند
  المزود لا عندنا
- فريد `(user_id, provider, provider_token)`؛ و`ON DELETE CASCADE` بخلاف بقية الجداول المالية —
  الرمز ليس سجلاً محاسبياً يُحتفظ به بل بيانُ راحةٍ لا معنى له بعد ذهاب صاحبه
- الجدول من **المرحلة 6-أ** ويملؤه الكود في **6-ب**: توليد `payment_provider` وقيود الجدول مع بقية
  مخطط الدفع في ترحيلة واحدة أرخص من تعديل ENUM على جدولٍ حيّ لاحقاً
- **`provider_token` لا يخرج من الخلفية أبداً** (6-ب): هو ما يُدفع به، فعرضُه يبطل غرض
  الـ tokenization كله. مخطط الخروج يحمل العلامة وآخر أربعة والانتهاء و`is_default` لا غير
- أولى بطاقات المستخدم هي الافتراضية بلا اختيار، وحذفُ الافتراضية يرقّي غيرها: محفظةُ بطاقاتٍ بلا
  افتراضية تجعل «الدفع بضغطة» بلا ضغطة تُعرض

### device_tokens (رموز أجهزة الإشعارات — القسم 10، المرحلة 8)

جدولٌ **أضافته المرحلة 8** ولم يكن في هذا القسم: إشعار Push لا يُرسل إلى مستخدم بل إلى **جهاز**،
ورمزُ الجهاز يصدره FCM على الجهاز نفسه فلا سبيل لمعرفته إلا أن يرسله التطبيق ويُحفظ. وعمودٌ على
`users` كان يفترض جهازاً واحداً لحسابٍ قد يكون على هاتفٍ ولوحٍ ومتصفح معاً.

- `user_id`, `device_id` (مُعرّف ثابت يولّده التطبيق), `token` (رمز FCM), `platform` (ios/android/web)
- `last_seen_at`, `is_active`
- **`token` فريد على الجدول كله**: رمز FCM يعرّف تثبيتَ تطبيقٍ واحد، فإن سجّل عليه حسابٌ آخر انتقل
  الصفُّ إليه — وإلا وصل إشعارُ الحساب السابق إلى من يحمل الهاتف الآن
- **`(user_id, device_id)` فريد**: الرمز يدور من تلقاء FCM، فالجهاز الواحد صفٌّ يُحدَّث لا صفوفٌ
  تتراكم لرموزٍ ماتت
- `ON DELETE CASCADE` بخلاف الجداول المالية — بيانُ توصيلٍ لا سجلٌّ محاسبي (كـ `saved_cards`)
- **الرمز الميت يُعطَّل ولا يُحذف**: ردُّ FCM بـ unregistered/invalid يُنزل `is_active`، فيبقى الصفُّ
  يتعرّف على الجهاز عند إعادة التسجيل ولا يُعاد الإرسال إليه في كل حملة
- **`token` لا يخرج من الخلفية**: به يُرسل إشعارٌ باسم المنصة إلى جهاز صاحبه

### notification_campaigns (حملات إدارية/تسويقية — القسم 13، المرحلة 8)
- `title`, `body`, `audience` (all_riders/all_drivers/by_country/segment), `country_code` (مُضيِّق لأي
  جمهور، ومطلوبٌ لجمهور «مستخدمو دولة»)
- `status` (draft/scheduled/sent/cancelled), `scheduled_at`, `sent_at`, `sent_count`, `created_by`
- **لا حالة «قيد الإرسال»**: الحملة تُرسل في دورةِ مهمةٍ واحدة على دفعات وصفُّها مقفول طوالها،
  فمهمتان متزامنتان لا ترسلان حملةً مرتين
- `segment` محجوزة لشرائح المرحلة 12 وتُرفض اليوم برسالة صريحة
- قيدان في القاعدة: `(audience='by_country') ⇒ country_code IS NOT NULL`، و
  `(status='scheduled') ⇒ scheduled_at IS NOT NULL`

### notification_deliveries (سجل التسليم)
- `campaign_id`, `user_id`, `status` (sent/failed/skipped), `sent_at`
- **فريد `(campaign_id, user_id)`** — وهو حارس عدم التكرار: مهمةٌ أُعيد تشغيلها بعد سقوطٍ في منتصف
  دفعة لا ترسل لمن وصله
- `skipped` ليست فشلاً: أطفأ التسويق أو لا جهاز مسجّل له. الفرق بينها وبين `failed` هو الفرق بين
  «لم نرسل عمداً» و«أرسلنا فلم يصل» — وبها يصير احترامُ الإطفاء **مُثبَتاً** لا مزعوماً

### notification_settings (ساعات الهدوء per-country — المرحلة 8)
- `country_code`, `quiet_hours_start`, `quiet_hours_end`, `timezone`
- **المِنطقة الزمنية عمودٌ صريح**: «لا إرسال بعد العاشرة» جملةٌ بلا معنى ما لم يُقل عاشرةُ مَن
- النافذة الافتراضية 22:00 → 08:00 وتعبر منتصف الليل، وتساوي الطرفين يعني «لا هدوء» لا «هدوءٌ دائم»
- تخص **التسويقي وحده**: كبتنٌ يعمل ليلاً يجب أن تصله بطاقة الطلب

> وفي `users` عمود `marketing_push_enabled` (افتراضي true) — إطفاءُ الإعلانات بيد صاحبها، ولا أثر له
> على المعاملاتي إطلاقاً.

### user_notifications (صندوق وارد المستخدم — المرحلة 9-ب)

قبل هذا الجدول كان الإشعار حدثاً بلا سجل: تُبثّ على القناتين ثم يُنسى. فمن أُغلق تطبيقُه ساعةً لا
يعرف أن اشتراكه انتهى ولا أن نزاعه فُصل ولا أن وثيقته رُفضت — وأيقونةُ الجرس في التطبيقين بلا مصدر.

- `user_id`, `kind`, `title`, `body`, `data` (jsonb), `read_at`
- **يُكتب من بابَي الإرسال وحدهما** (`services/notifications.py` و`services/campaigns.py`)، فقناةٌ
  ثالثة تُضاف يوماً تكتب فيه لأن البابين هما ما يُضاف إليهما لا لأن أحداً تذكّر
- **يُكتب ولو لم يُرسل Push**: لا عقد FCM، أو الجهاز مفتوحٌ فلا يُرسل إليه — كلاهما لا يعني أن
  الحدث لم يقع. الصفُّ أثرُ الحدث لا أثرُ المزوّد
- **`kind` هي `data["type"]` نفسها** التي تحملها حمولة Push، فلا يفترق ما يفتحه الضغط على الإشعار
  عمّا يفتحه الضغط على صفّه في الصندوق
- **بطاقة الطلب وحدها لا تُحفظ** (`EPHEMERAL_KINDS`): عمرُها عشرون ثانية، وبعدها إمّا قُبلت — فحدثُ
  `driver_assigned` هو الأثر الصحيح — أو انتقلت لغيره. وصفٌّ باقٍ لطلبٍ مضى يفتح عند الضغط لا شيء
- **والتسويقي يُكتب لمن أُرسل إليه فعلاً وحده** (`sent` لا `skipped`): من أطفأ إشعارات العروض
  أطفأها، وإدخالُها صندوقَه من بابٍ آخر التفافٌ على إطفاءٍ صريح
- `ON DELETE CASCADE` — سجلُّ عرضٍ لا سجلٌّ مالي، يذهب مع صاحبه كـ `device_tokens`
- **لا تقليم بعد**: الجدول ينمو، والقراءةُ مسقوفة والفهرس على `(user_id, created_at)`. مهمةُ الكنس
  الدورية في المرحلة 12 مع بقية مهام الصيانة

### wallet_transactions (Ledger — سجل قيود غير قابل للتعديل)
- `owner_type` (driver/rider), `owner_id` — **`owner_id` يشير إلى `users.id` في الحالتين** (مفتاح أجنبي حقيقي بـ RESTRICT)، و`owner_type` هو ما يميّز محفظة الراكب عن محفظة الكبتن. فرصيدُ كبتنٍ يُستعلم بـ `driver.user_id` لا `driver.id`
- `type`: `topup | ride_payment | ride_earning | commission | transfer_in | transfer_out | withdrawal | refund | subscription_payment | adjustment`
  - **ومعها زوجُ البقشيش** (المرحلة 12-و): `tip_payment` مديناً على الراكب و`tip` دائناً للكبتن،
    بلا `commission` يقابلهما — يقبضه كاملاً (القسم 6.5)
  - **و`referral_bonus`** (المرحلة 12-ح): دائنٌ **بلا مدينٍ مقابل**، تتحمّله الشركةُ كخصم
    الكوبون — ووعاءُ الشركة ليس محفظةً في هذا النظام (القسم 9.1)
- `amount` (+/-), `balance_after`, `ride_id` (nullable), `reference`, `created_by`
- `idempotency_key` (nullable، فريد لكل مالك) — منع تكرار عملية أُعيد إرسالها (القسم 14)
- **قاعدة صارمة:** الرصيد يُحسب من القيود، لا عمود رصيد يُحدَّث. كل عملية = قيد جديد. التحويلات والخصومات داخل DB transaction ذرّية
- **«غير قابل للتعديل» مفروضٌ في القاعدة** بمُشغّل يرفض UPDATE وDELETE على الجدول. تصحيح الخطأ قيدُ `adjustment` مضاد، لا تعديلُ صفٍّ ولا محوُه
- إشارة `amount` يمليها النوع (قيد فحصٍ في القاعدة)، و`balance_after >= 0` دائماً: المحفظة مسبقة الدفع لا تُسحب على المكشوف

### wallet_topup_requests (طلب شحن ينتظر تأكيداً بشرياً)
- `owner_id`, `method` (cliq/cash/card), `amount`, `reference` (مرجع الحوالة), `note`
- `status` (pending/confirmed/rejected), `processed_by`, `processed_at`, `transaction_id` (القيد الناتج)
- كليك بلا API فيؤكدها إنسان (القسم 7)؛ الكاش يُنشئه الموظف مؤكداً؛ شحن Telr فوريٌّ آلي فلا يمر بطلب
- **لا رصيد يتغيّر قبل `confirmed`** — الطلب المعلّق أثرٌ نصّي لا مالي

### withdrawal_requests (للكباتن فقط)
- `driver_id`, `amount`, `method` (cliq/bank), `status` (pending/approved/paid/rejected)
- `reference`, `note`, `processed_by`, `processed_at`, `transaction_id`
- قيد `withdrawal` يُكتب عند `paid` وحدها. الطلبات في `pending`/`approved` تحجز مبلغها من الرصيد المتاح، فلا يُطلب نفس المال مرتين

### wallet_settings (حدود المحفظة per-country — تُدار من اللوحة)
- `country_code`, `transfer_daily_limit`, `transfer_monthly_limit`, `min_withdrawal_amount`,
  **`withdrawal_reserve_amount`** (البند ١٣)
- صفرٌ في حدّي التحويل يعني **«لم يُضبط بعد»** فيُرفض التحويل — لا يخترع الكود حداً سخياً لقيمةٍ مالية غائبة

#### الرصيدُ المحتجَز (البند ١٣ — قرارُ المالك 2026-08-14)

**مبلغٌ يبقى في محفظة الكبتن لا يُسحب** (خمسةُ دنانير مبدئياً)، ويُصرف عند **طلب إلغاء تفعيل
الحساب**. وثلاثُ قواعدَ تحكمه:

- **شرطٌ على السحب لا قيدٌ في الدفتر.** لا تُكتب حركةٌ تحجزه: `available_balance` تطرحه كما تطرح
  ما حجزته طلباتٌ قائمة (`_reserved_amount`) — والقاعدةُ نفسُها التي تمنع عمودَ رصيدٍ تمنع قيداً
  يمثّل حجزاً. وقيدٌ يُكتب لحجزه يجعل **الرصيدَ يكذب على صاحبه**: يقرأ رقماً ناقصاً بلا أن يُدفع له.
- **وصفرٌ يعني «لا احتجاز»** لا «احتجزْ صفراً» — كبقية أصفار هذا الجدول.
- **ولا يُطبَّق على ما دخل قبل ضبطه**: هو شرطٌ على السحب لحظةَ الطلب، فتعديلُه في اللوحة يحكم ما
  يأتي؛ ولا صفَّ يُعاد حسابُه ولا مالَ يُستردّ.

#### طلبُ إلغاء تفعيل الحساب (البند ١٣)

بابٌ لم يكن في المشروع، وبه وحده يخرج المحتجَز. `deactivation_requests`: `driver_id`,
`status` (pending/approved/rejected/cancelled), `reason`, `resolved_by`, `resolved_at`.

- **يُرفض الطلبُ إن كان له رحلةٌ جارية، أو دَينٌ غيرُ مسدَّد، أو نزاعٌ مفتوح** — وكلُّها تُقرأ
  حيّةً لحظةَ الطلب لا تُخزَّن على الصف.
- **والصرفُ عبر مسار السحب نفسِه** لا بكتابةٍ مستقلة في الدفتر: الموافقةُ تفتح السحبَ على الرصيد
  كلِّه (المحتجَزُ يسقط شرطُه)، والمالُ يخرج بـ`withdrawal` كأيِّ سحب. **بابٌ واحدٌ للمال.**
- **ومصيرُ المحتجَز مع دَينِ سلفة** (البند ١٥، محسومٌ هنا بقرار المالك): **يُقتطع للدَّين أولاً
  والباقي يُصرف** — فالحسابُ لا يُغلق ودَينُه قائم، والمحتجَزُ وُجد لهذه اللحظة بعينها.
- **وحسابٌ أُلغي تفعيله لا يُعتمد ثانيةً بضغطة**: يعود من باب `drivers.approve` وحدَه بشروطه.

### subscription_plans
- `name`, `duration_type` (daily/weekly/monthly), `price`, `currency`, `country_code`, `is_active`

### driver_subscriptions
- `driver_id`, `plan_id`, `starts_at`, `expires_at`
- `amount_paid`, `payment_method` (cash/cliq/wallet/card), `status` (active/expired)
- التجديد = سجل جديد (تاريخ كامل محفوظ)
- أضافت **المرحلة 7** ثلاثة أعمدة (ترحيلة `0009`):
  - `transaction_id` (nullable) — قيد `subscription_payment` الناتج، **للمحفظة وحدها**: القسم 9
    يكتب الرصيد «أرباح − عمولات − الاشتراكات المدفوعة **منها** − سحوبات»، وكاشٌ وكليكٌ وبطاقةٌ لا
    يمر مالها بالمحفظة فقيدُ خصمٍ عليها يخصم من الكبتن مرتين
  - `reference` (nullable) — حوالة كليك أو إيصال الكاش أو مرجع الطلب لدى المزود
  - `idempotency_key` (nullable، **فريد**) — ضغطتان على «تجديد» لا تشتريان اشتراكين (القسم 14)
- **حالتان لا ثالثة**: لا `pending` لأن الصف لا يُنشأ إلا وقد وصل ماله (القنوات اليدوية تُسجَّل بعد
  القبض، والفوريتان بعد جواب الدفتر أو المزود)، ولا `cancelled` لأن مدفوعاً مقدماً لا يُلغى

### commission_settings
- `country_code`, `commission_enabled` (default **false**), `commission_percent`, `applies_to`
- `applies_to`: `all_rides` (كل الرحلات) أو `cashless_rides` (البطاقة والمحفظة فقط — ما تمر أمواله عبر المنصة)
- **هذا الجدول هو المصدر الوحيد لتفعيل العمولة.** لا مفتاح `commission_enabled` في `feature_flags`: التفعيل والنسبة والنطاق تسكن معاً في صف واحد، فلا تنشأ حالتان قابلتان للاختلاف في أمر مالي

### feature_flags
- `country_code`, `feature_key`, `enabled`
- المفاتيح المبدئية: `cliq_enabled`, `card_enabled`, `wallet_enabled`, `wallet_transfer_enabled`،
  و`otp_verification_enabled` (المرحلة 8-ب)، و`women_service_enabled` (المرحلة 10-ج)،
  و`multi_stop_enabled` (المرحلة 12-ب)، و`whatsapp_otp_enabled` (المرحلة 12-هـ)،
  و`tips_enabled` (المرحلة 12-و)، و`promo_codes_enabled` (المرحلة 12-ز)
- غياب الصف = الميزة **معطّلة** — لا يُفترض التفعيل أبداً
- **الاستثناء الوحيد: المفاتيح الحارسة** (`otp_verification_enabled`). القاعدة أعلاه وُضعت كي لا
  تُفتح ميزةٌ بالسكوت؛ وهذا ليس ميزةً تُفتح بل حارساً يُطفأ، فغيابُ صفّه يعني **مفعّلاً**. القائمة
  في `settings_service.DEFAULT_ENABLED_FLAGS` ولا يُضاف إليها مفتاح ميزة
- `otp_verification_enabled`: **لـ admin حصراً**، و**إطفاؤه يشترط سبباً مكتوباً** يدخل سجل التدقيق
  (من أطفأه ومتى ولماذا). أثرُه محصورٌ في **التسجيل**: يُنشأ الحساب بـ `phone_verified_at = NULL`.
  **ولا يمسّ استعادة كلمة المرور إطلاقاً** — عفوُها يجعل إطفاءَ المفتاح طريقاً للاستيلاء على أي حساب
  بمجرد معرفة رقمه. وهو إجراء طوارئ تعرضه اللوحة بتحذير صريح
- `whatsapp_otp_enabled`: **ميزةٌ لا حارس** (المرحلة 12-هـ)، وغيابُ صفّها معطَّلة. وهي المفتاحُ
  الوحيد الذي **يُقرأ مع عقدٍ عامّ**: عقدُ واتساب لا دولةَ له (رقمُ أعمالٍ واحد يخدم السوقين)
  فالمفتاحُ هو ما يقول «نفعل في هذا السوق» — ويُشعَل بعد اعتماد قالب المصادقة **بلغة ذلك السوق**،
  فقالبٌ غير معتمد يجعل كلَّ تسجيلٍ يرتدّ من ميتا. ولذلك لا مزامنةَ تلقائية بينه وبين العقد كما في
  عقود البطاقة وكليك: المزامنةُ تُشعل مفتاحَ دولةِ العقد، وهذا عقدٌ بلا دولة
- `women_service_enabled`: **ميزةٌ لا حارس**، فغيابُ صفّها يعني معطّلة كالبقية. وتبقى مطفأةً
  حتى تكتمل **مراجعةُ أجناس الكباتن المعتمدين المتراكمين**: تشغيلُها قبلها يعني خدمةً نسائيةً
  بلا سائقاتٍ يمكن ترشيحُهنّ، فتُقرأ ميزةً معطوبة لا ميزةً جديدة. **وما دامت مطفأةً لا يظهر
  مفتاحُ التفضيل للراكبة إطلاقاً** — لا خياراً معطّلاً ولا رسالةَ اعتذار: خيارٌ معطّلٌ يعرض
  عليها ما لا تناله، ورسالةٌ تعتذر تعِد بما لم يُوعَد به. والتطبيق يقرأ المفتاح من `GET /config`
  كبقية المفاتيح، فتشغيلُها لا يحتاج نشراً
- **وإطفاؤها بعد تشغيلها لا يحبس أحداً**: تفضيلٌ نسائيٌّ بقي على حساب راكبةٍ
  من سوقٍ كانت الخدمةُ فيه مشتعلة كان يجعل **كلَّ طلباتها ترتدّ** — الخلفيةُ
  ترفض بحق (`women_service_unavailable`: طلبٌ يُسنَد لأيّ كبتنٍ بعد أن طُلب
  فيه غيرُه أسوأ من رفض)، **والتطبيقُ يخفي المفتاح فلا تجد ما تغيّره**. فصار
  التطبيق **لا يرسل تفضيلاً لخدمةٍ لا تُعرض عليه** (`lib/women.ts`)،
  و**المخزَّن لا يُمحى** — هو اختيارُها ويعود بعودة الخدمة، والذي يتوقف
  إرسالُه لا حفظُه. ومعه رسالةٌ صريحة وزرُّ «اقبل أي كبتن» إن ارتدّ طلبٌ
  بالسبب نفسه من مسارٍ آخر: **رفضٌ بلا مخرجٍ ليس رفضاً**
- `multi_stop_enabled`: **ميزةٌ لا حارس**، فغيابُ صفّها يعني معطّلة — **ولا تُضاف إلى
  `DEFAULT_ENABLED_FLAGS`** مهما بدت صغيرة: تلك قائمةُ الحرّاس وحدهم (القسم 4/feature_flags).
  ويُكتب صفُّها **صراحةً مطفأةً في `scripts/seed.py`** للدولتين، فلا يُقرأ غيابُها اجتهاداً.
  وثلاث قواعد لإطفائها:
  - **يختفي زرُّ «إضافة محطة» من شاشة الطلب تماماً** — لا خياراً معطّلاً ولا رسالةَ اعتذار،
    حرفياً كما يختفي مفتاحُ التفضيل النسائي (القسم 4/`women_service_enabled`)
  - **والرحلاتُ الجارية متعددةُ المحطات تكمل طبيعياً**: الإطفاء يمنع **الطلبات الجديدة** وحدها.
    مفتاحٌ يقطع رحلةً في منتصفها يترك راكباً بين محطتين، والمحطاتُ مجمَّدةٌ على الرحلة أصلاً
  - والفحصُ عند **إنشاء الطلب** لا عند العرض: واجهةٌ تخفي الزر لا تمنع طلباً مصنوعاً بيد
- إعداد ليبيا الافتراضي: الكل **false** (كاش فقط) / الأردن: `cliq_enabled` و`card_enabled` و`wallet_enabled` مفعّلة، و`wallet_transfer_enabled` معطّل

### provider_credentials (بيانات عقود المزودين — تُدار من صفحة العقود)
- **القاعدة: أي مزود خارجي يحتاج مفتاح/عقد API يُدار من هنا حصراً — بما فيهم Mapbox وTelr** — لا مفاتيح مزودين في `.env` أو الكود
- `provider_key` (mapbox / telr / sms / cliq_acquirer / fcm / payout), `country_code` (nullable للعام)
- `credentials` (JSONB **مشفر at rest** — حقول مختلفة لكل مزود، مع تمييز الحقول العامة publishable عن السرية secret)
- `is_active`, `last_tested_at`
- تفعيل المزود = تفعيل ميزته تلقائياً؛ كل تعديل يدخل audit log
- الحقول السرية للخلفية فقط (لا تصل للواجهات)؛ الحقول العامة بطبيعتها (مثل Mapbox pk) تُسلَّم للواجهات عبر `GET /config`
- **ملاحظة تشغيلية:** Mapbox وTelr Sandbox أساسيان من اليوم الأول — أول خطوة بعد تشغيل النظام هي إدخال توكناتهما من صفحة العقود (أو seed script للتطوير المحلي)

### payment_settings (سياساتُ الدفع per-country)
- `cliq_confirmation_hours` — مهلةُ تأكيد حوالة كليك (القسم 6.2)، **تُجمَّد على الدفعة** عند إرسال
  المرجع ولا تُقرأ حيّةً بعده
- **وثلاثةُ حقول بقشيشٍ (المرحلة 12-و)**: `tip_preset_small` و`tip_preset_medium` وسقفٌ `tip_max`،
  وكلُّها **صفرٌ افتراضاً** — والصفرُ «لم يُضبط» فيُخفي الميزة لا «بقشيشاً مقداره صفر» (القسم 6.5)

### pricing_rules
- `country_code`, `vehicle_category`, `base_fare`, `price_per_km`, `price_per_min`, `minimum_fare`, `cancellation_fee`
- **وأربعةُ حقولِ محطاتٍ تديرها الإدارة per-country** (المرحلة 12-ب)، وكلُّها **صفرٌ افتراضاً**
  فلا تُفتح كلفةٌ على راكبٍ بالسكوت (نفس قاعدة العمولة):
  - `stop_fee` — رسمٌ **مقطوع** يُضاف عن كل محطةٍ وسيطة، يدخل التقدير المعروض قبل الطلب
  - `stop_free_minutes` — دقائقُ انتظارٍ مجانية لكل محطة
  - `stop_price_per_min` — سعرُ الدقيقة بعد المجانية
  - `stop_max_wait_minutes` — **سقفُ الانتظار للمحطة الواحدة**؛ ببلوغه يُخطَر الطرفان ويُفتح
    للكبتن خيارُ إنهاء الرحلة عند تلك المحطة (القسم 5.10). **وصفرٌ يعني «لا سقف»** لا «سقفٌ
    مقداره صفر» — كما يُقرأ صفرُ حدِّ التحويل «لم يُضبط» (القسم 7)

### ratings
- `ride_id`, `rater_type` (rider/driver), `rater_id`, `stars` (1-5), `comment`
- فريد `(ride_id, rater_type)` — كل طرف يقيّم مرة واحدة، **مفروضاً في القاعدة** فسباقُ ضغطتين لا
  ينتج تقييمين
- `rater_id` إلى جانب `rater_type`: الأول يقول أيّ حساب والثاني يقول أيّ الطرفين، فيبقى السجل مقروءاً
  بعد أي تغيّر في إسناد الرحلة
- **`drivers.rating_avg` مشتق لا مُراكَم:** يُعاد حسابه من الجدول كله بعد كل تقييمٍ يكتبه راكب. المتوسط
  المتدحرج يعتمد على عدد سابقٍ مخزَّن، وأيُّ قيدٍ ضاع أو أُعيد يجعله ينحرف انحرافاً لا يُكتشف — نفس سبب
  حساب رصيد المحفظة من دفترها
- التقييم اختياري ويقع بعد `completed` وحدها، ومن الطرفين وحدهما: الدور لا يكفي، فكبتنٌ آخر دورُه
  `driver` ليس طرفاً في هذه الرحلة، والإدارة تقرأ الرحلات ولا تقيّمها

### promo_codes (الكوبونات — المرحلة 12-ز)
- `code` — **فريدٌ لكل دولة** ويُطبَّع بالحروف الكبيرة عند الكتابة والقراءة: رمزٌ يُقرأ من ملصقٍ أو
  رسالةٍ يكتبه الناسُ بحالاتٍ مختلفة، ومطابقةٌ حسّاسةٌ للحالة تجعل الرمزَ الصحيح يُرفض
- `country_code` — سوقٌ واحد لكل رمز: العملةُ تختلف، والحملةُ تُقاس بسوقها
- `discount_type` (`percent` | `fixed`)، `discount_value`، و`max_discount` سقفاً للنسبة (بغيره
  يبتلع كوبونُ ٥٠٪ رحلةً طويلة)
- `budget_total` — سقفُ الحملة (شرطُ المالك). **ولا `budget_spent`**: المصروفُ مجموعُ دفعات `promo`
  على رحلات هذا الرمز، وعمودٌ يراكم رقماً موجوداً أصلاً يفترق عنه يوماً (قاعدةُ «لا عمود رصيد»)
- `per_user_limit` (افتراضُه 1)، `total_usage_limit` (nullable = بلا حدٍّ غير الميزانية)
- `valid_from`, `valid_until`, `is_active`
- **ولا عمودَ `bearer`**: القيمةُ واحدةٌ اليوم (الشركة) واللوحةُ تقولها نصّاً — وعمودٌ بقيمةٍ واحدة
  ممكنة هو نفسُ ما حُذف من جدول البقشيش. يُضاف يومَ يظهر متحمّلٌ ثانٍ بافتراضِ `company`
- **ولا جدولَ «استعمالات»**: الرحلةُ تحمل الرمز والدفعةُ تحمل المبلغ (القسم 6.6)

### tips (البقشيش — المرحلة 12-و)
- `ride_id` (FK rides، **unique**) — بقشيشٌ واحدٌ لكل رحلة، والحارسُ في القاعدة كما في `ratings`
- `rider_id`, `driver_id` (FK users) — **الطرفان معاً وإن كانا مشتقّين من الرحلة**: صفُّ الدفتر
  يقابل هذا الصفَّ بمالكٍ لكلِّ قيد، وقراءةُ «كم بقشيشاً قبض هذا الكبتن» لا يجوز أن تكون ضمّاً على
  الرحلات — وهي قراءةُ شاشة أرباحه في كل فتحة
- `amount` (`NUMERIC(12,3)`)، `currency` — والعملةُ من دولة الرحلة لا من العميل (القسم 4)
- `method` — `wallet` وحدها في هذه المرحلة (القسم 6.5)، والقيمةُ مكتوبةٌ كي لا يصير إضافةُ البطاقة
  لاحقاً تخميناً في صفوفٍ قديمة
- `status` — `confirmed` أو `failed`، **ولا `pending`**: قناةُ المحفظة تستقر في نفس المعاملة
- `created_at`
- **ولا صفَّ في `payments`**: البقشيشُ ليس أجرةً، وصفُّه هناك يُفسد مجموعَ «لا تُدفع الرحلة مرتين»
  (القسم 6.5)

### ride_bookings (الرحلات المجدولة — المرحلة 12-ط) — ✅ مُنجزة
- `rider_id` (FK users)، `country_code`
- `pickup_point` / `dropoff_point` — `geography(POINT,4326)` كالرحلة، و`*_address` نصّاً
- `vehicle_category` و`payment_method_hint` و`gender_preference` — نفسُ حقول الطلب الفوري
- `scheduled_at` (timezone-aware) — موعدُ الانطلاق. **والمقارنةُ بالساعة لا بيومِ الدولة**:
  الموعدُ لحظةٌ بعينها لا نافذةٌ يومية
- `status`: `pending → dispatched`، ومعهما `missed` و`cancelled`. **أربعٌ ولا خامسة.**
  - **ولا `fulfilled` ولا `no_driver`** — وهذا تصحيحٌ لأول صياغةٍ لهذه المواصفة، ظهر عند البناء:
    ما جرى بعد التسليم **تقوله الرحلةُ نفسُها**، وقيمةٌ هنا تكرّرها بيتٌ ثانٍ يفترق عن الأول أولَ
    مرةٍ تُلغى فيها رحلةٌ وُجد لها كبتن. وهي قاعدةُ «لا عمودَ مشتقّاً» نفسُها التي بُني عليها رصيدُ
    المحفظة وحالُ الاشتراك واستحقاقُ الإحالة
  - و`ride_id` (nullable، FK rides، **فريد**) يُكتب عند `dispatched` — **الوصلةُ بين الحجز
    ورحلته**، وبها تُقرأ حالُ الرحلة عند العرض. وقيدُ CHECK يمنع `dispatched` بلا رحلة: حجزٌ
    سُلّم إلى لا شيء لا يستطيع أحدٌ تفسيرَه بعد شهر
- `estimated_fare_at_booking` — **تقديرٌ للعرض لا التزام** (القسم 5.11)، ولذلك اسمُه يقول ذلك:
  عمودٌ باسم `fare` يُقرأ التزاماً بعد شهر
- `cancelled_at`, `cancelled_by`, `notified_at` — و`notified_at` أثرُ الإبلاغ عن فشلٍ أو تخطٍّ،
  يمنع تكرارَه في كل دورة (نفسُ `ride_stops.notified_at` لا مفتاحُ Redis: الصفُّ قائمٌ أصلاً)
- **ولا عمودَ تكرار ولا جدولَ وقوعات** في هذا القطع (القسم 5.11)
- **ولا `commission_percent_at_ride` ولا حقولَ كوبونٍ مجمَّدة**: كلُّها تُجمَّد على الرحلة لحظةَ
  ولادتها، وتجميدُها هنا نسخةٌ ثانيةٌ تفترق عن الأصل

### driver_referrals (إحالةُ السائقات — المرحلة 12-ح)
- `referrer_driver_id` (FK drivers) — من أحال
- `referred_driver_id` (FK drivers، **unique**) — من أُحيل. والفرادةُ في القاعدة: حسابٌ واحدٌ يُحال
  مرةً واحدةً في عمره، فسباقُ تسجيلين برمزين مختلفين لا ينتج مكافأتين
- `code_used` — الرمزُ كما كُتب. **وهو ليس تكراراً لـ`referrer_driver_id`**: الرمزُ قد يُبدَّل
  لاحقاً (رمزٌ سُرّب مثلاً)، والسجلُ يجيب «بأي رمزٍ جاءت» بعد التبديل
- `created_at` — لحظةُ التسجيل بالرمز، ولا شيءَ بعده حتى تُدفع المكافأة
- `rewarded_at`, `reward_amount`, `reward_currency`, `transaction_id` (FK wallet_transactions) —
  **تُكتب كلُّها معاً لحظةَ الدفع أو لا تُكتب**، والمبلغُ **مجمَّدٌ عليها** كـ
  `commission_percent_at_ride`: مالٌ خرج بقيمةٍ كانت في الإعدادات، وتعديلُ الإعداد لا يعيد كتابة
  ما دُفع
- **ولا عمودَ `qualified_at` ولا `status`**: الاستحقاقُ **مقارنةٌ حيّةٌ في الخدمة** لا حالةٌ مكتوبة
  — عددُ رحلاتٍ مكتملةٍ ضدَّ حدٍّ في الإعدادات — فتغييرُ الحدِّ يعيد تقييمَ الجميع بدل أن يترك
  صفوفاً وسمها حدٌّ قديم. وهي نفس قاعدةِ «flagged» في تقارير عدم تطابق الجنس (المرحلة 10-ج).
  والمدفوعُ وحده يُجمَّد، لأن المالَ لا يُعاد تقييمه
- `drivers.referral_code` — عمودٌ **فريدٌ عالمياً** (لا per-country: الرمزُ يُقال في مكالمة، وواحدٌ
  في الأردن يطابق واحداً في ليبيا هو رمزٌ يذهب لصاحب الحساب الخطأ)، يُولَّد عند إنشاء الكبتن
  وللقائمين ترحيلةً — **ولا يُولَّد عند أول فتحةٍ للشاشة**: توليدٌ عند القراءة يحتاج قفلاً على
  صفٍّ لا يُكتب فيه شيءٌ آخر، وبغيره تُنتج ضغطتان رمزين

### referral_settings (حافزُ الإحالة per-country — المرحلة 12-ح)
- `reward_amount` (`NUMERIC(12,3)`) — **صفرٌ افتراضاً**، والصفرُ «لم يُحدَّد بعد» فلا يُكتب قيدٌ
  ولا تُعرض الشاشةُ مبلغاً (نفس قراءةِ صفرِ حدِّ التحويل في القسم 7). **وهذا قرارُ المالك
  (2026-08-12)**: تُبنى الآليةُ والتتبّعُ ويُترك المبلغُ له
- `required_rides` — كم رحلةً مكتملةً على المُحالة قبل الاستحقاق، **افتراضُه ٣** (قرارُ المالك:
  أقلُّ يفتح باب حساباتٍ وهمية، وأكثرُ يُبعد الحافزَ عن سببه)
- **ولا حقلَ «مهلةٍ للإحالة»**: مكافأةٌ تنتهي صلاحيتُها تحتاج ساعةً تُقرأ في مكانين (لحظةَ
  التسجيل ولحظةَ الدفع)، ولا أحدَ طلبها

### user_totp (العامل الثاني لدخول اللوحة — TOTP، المرحلة 12-د)
- `user_id` (FK users، **unique**) — صفٌّ واحدٌ للمستخدم، وغيابُه يعني «لا عامل ثانٍ»
- `secret_encrypted` (JSONB) — مظروفُ Fernet `{"v":1,"ciphertext":…}` بنفس `core/crypto.py`
  الذي يحمي `provider_credentials`. سرُّ TOTP مفتاحٌ دائم: من قرأه من نسخةٍ احتياطية يولّد
  رموزَ صاحبه إلى الأبد، فهو مثلُ مفتاح مزوّدٍ لا مثلُ كلمة مرور — **يُشفَّر ولا يُهشَّم**،
  لأن التحقق يحتاج قراءتَه
- **ولماذا جدولٌ مستقلٌّ لا عمودان على `users`**: `users` يُقرأ في **كل** طلبٍ لكل مستخدم
  (`core/deps.get_current_user`)، فوضعُ السرِّ على الصف يحمّل سرَّ عاملٍ ثانٍ في كل نداءٍ في
  المنصة كلها — ولمن لا يملك عاملاً أصلاً. والجدولُ المستقل لا يُقرأ إلا في مسارَي الدخول
  والتسجيل
- `confirmed_at` (nullable) — **تسجيلٌ غير مؤكَّد ليس عاملاً**: السرُّ يُنشأ قبل أن يُثبت صاحبُه
  أنه وصل تطبيقَه، فاعتبارُه عاملاً يقفل صاحبَه خارج اللوحة برمزٍ لا يملك مصدره
- `recovery_codes_verified_at` (nullable) — لحظةُ إثبات أن الاسترداد يعمل فعلاً. شرطُ إشعال
  مفتاح الإلزام (القسم 14.1)
- `last_step` (BIGINT، nullable) — آخرُ خطوةِ زمنٍ قُبلت. **منعُ إعادة الاستخدام**: الرمزُ صالحٌ
  ثلاثين ثانية، فمن قرأه من فوق كتف صاحبه يستعمله في نفس النافذة — والقبولُ مرةً واحدةً لكل خطوة
- ولا عدّادَ محاولاتٍ هنا: المحاولاتُ في Redis عبر `core/rate_limit` كبقية سقوف القسم 14 —
  عدّادٌ في القاعدة يعني كتابةً على كل محاولةٍ فاشلة

### user_recovery_codes (رموز استرداد العامل الثاني — المرحلة 12-د)
- `user_id` (FK users)، `code_hash`، `used_at` (nullable)
- **عشرةُ رموزٍ تُولَّد مرةً وتُعرض مرةً ولا تُقرأ بعدها أبداً**، وتُخزَّن **مُهشَّمة** لا مشفَّرةً
  كالسرّ: التحقق منها مقارنةٌ لا استرجاع، وجدولٌ يحفظها مقروءةً هو نسخةٌ ثانيةٌ من العامل الثاني
  بلا عاملٍ يحرسها
- **والتهشيم HMAC-SHA256 بمفتاح الخدمة لا bcrypt** — خلافاً لكلمة المرور، ولسببين يعملان معاً:
  الرموزُ عاليةُ العشوائية (من مولّدٍ تشفيري) فدالةٌ بطيئةٌ لا تشتري مقاومةً لتخمينٍ غيرِ ممكنٍ
  أصلاً؛ والبصمةُ الحتمية تتيح **استعلاماً مفهرساً عن الصفِّ الواحد**، وهو ما يجعل القفل على صفٍّ
  واحد بدل قفلِ عشرةِ صفوفٍ وتجربةِ bcrypt على كلٍّ منها تحته. وهو نفسُ `services/otp.py::_digest`
- **وأبجديةُ الرمز بلا `I O U L 1 0`**: يُنسخ بيدٍ ويُقرأ من ورقة، والحرفُ الذي يُقرأ رقماً يجعل
  رمزاً صحيحاً يبدو خاطئاً في اللحظة التي لا يملك صاحبُه فيها بديلاً
- **والاستهلاك صفٌّ واحدٌ تحت قفل صفّه** (`with_for_update` قبل قراءة `used_at`): بلا القفل
  يمرّ نداءان متزامنان بنفس الرمز فيُستعمل رمزٌ «لمرةٍ واحدة» مرتين — وهي القاعدةُ نفسُها التي
  تحرس كل انتقالِ حالةٍ في هذا المشروع
- واستهلاكُ رمزٍ يكتب قيدَ تدقيقٍ **وصفَّ إشعارٍ لصاحب الحساب** (`user_notifications`): لا بريدَ
  في المنصة، والحدثُ من أوضح ما يفعله من استولى على حساب

### security_settings (سياسةُ دخول اللوحة — **صفٌّ واحدٌ عالمي**، المرحلة 12-د)
- `admin_totp_required` (boolean، افتراضي **false**)، `admin_idle_timeout_minutes` (int، افتراضي 30)
- **عالميٌّ لا per-country** خلافاً لكل جداول الإعدادات، ولا شيءَ في القسم 14 per-country: أمنُ
  لوحةٍ مكتبية ليس سياسةَ سوق. **والأخطرُ أن per-country يفتح ثقباً**: حسابُ المشرف في دولةٍ
  واحدة واللوحةُ تخدم السوقين، فإلزامٌ يُقرأ من دولة الحساب يُتجاوَز بحسابٍ دولتُه الأخرى
- صفٌّ واحدٌ يحرسه عمودٌ ثابتٌ عليه `unique` + `CHECK`، والغيابُ يُقرأ بالافتراضات أعلاه
- **والافتراضُ `false` عكسَ `DEFAULT_ENABLED_FLAGS`، وليس نقضاً لها**: قاعدةُ «غيابُ صفِّ حارسٍ
  يعني مفعّلاً» وُضعت حيث السكوتُ **يُطفئ** حماية. وهنا السكوتُ لا يطفئ شيئاً بل **يُشعل قفلاً على
  بابٍ لا مفتاح له**: صفٌّ مفقودٌ يُقرأ «إلزام» يقفل كلَّ مشرفٍ خارج اللوحة، ولا أحدَ يستطيع
  إطفاءه من داخلها. والإلزامُ فعلُ مالكٍ صريحٌ لا حالةٌ تقع بالسكوت

### admin_audit_logs (سجل التدقيق الذي يفرضه القسم 14)
- `actor_id` (يبقى بعد حذف الحساب), `action` (create/update/activate/…), `entity_type`, `entity_id`, `details`
- `details` يحمل **أسماء الحقول المتغيّرة فقط، لا قيمها** — سجلُّ تدقيقٍ يخزّن الأسرار يصير نسخةً ثانيةً منها
- يُكتب في **نفس معاملة** التغيير: لا تغييرَ بلا قيد، ولا قيدَ لتغييرٍ فشل

---

## 5. تدفق الرحلة (Ride Lifecycle)

1. **الطلب:** الراكب يحدد نقطتي الانطلاق والوصول (دبوس على الخريطة + Geocoding اختياري)
2. **التسعير:** الخلفية تستدعي Mapbox Directions → `estimated_fare = base_fare + (km × price_per_km) + (min × price_per_min)` مع حد أدنى. **السعر يُحسب في الخلفية فقط — أبداً في الواجهة**
3. **التوزيع (Dispatch):**
   - Redis GEO: أقرب الكباتن ضمن نصف قطر 3كم (يتوسع حتى 7كم — **وحتى 10كم للطلب المجنَّس**،
     المرحلة 10-ج: دائرةُ المرشَّحين فيه أضيق ابتداءً، فسبعةٌ تعني «لم نجد كبتناً» لطلبٍ كانت
     تسعةٌ كافيةً له. والانتظارُ الأطول خيارُ من طلبت الخدمة، أما الرفضُ فليس خياراً لأحد)
   - الشروط: `is_online=true` + `approved` + `اشتراك ساري (expires_at > now)` + بدون رحلة حالية
   - **ومطابقةُ الجنس في اتجاهين** (المرحلة 10-ج)، ومكانُها داخل شرط الأهلية نفسه لا فوقه —
     فالدالةُ التي تجيب «من يصلح لهذا الطلب» واحدة، وأيُّ تصفيةٍ فوقها تُنسى في أحد المسارين
     (التوزيع وخريطة الراكب يستدعيانها معاً). والاتجاهان **مستقلان**:
     - *ما تطلبه الراكبة*: تفضيلٌ غير `any` يقصر النتيجة على كبتنٍ جنسُه **مختوم** بذلك الجنس
     - *ما يقبله الكبتن*: كبتنٌ تفضيلُه غير `any` لا يُعرض عليه إلا راكبٌ من ذلك الجنس، ومن لم
       يعلن جنسه لا يطابق أحداً منهم
     فراكبٌ اختار «لا يهمّني» **لا يُعرض طلبُه** على كبتنةٍ قصرت عملها على النساء — وسكوتُ
     الاتجاه الأول لا يُلغي الثاني
   - **والمفتاح `women_service_enabled` مطفأً يعني لا مطابقةَ إطلاقاً** لا «مطابقةً بتفضيلٍ
     فارغ»: تفضيلٌ باقٍ على حساب كبتنةٍ في سوقٍ لا خدمةَ نسائيةً فيه كان سيحجب عنها الطلبات
     صامتاً بلا أن يفهم أحدٌ لماذا جفّت
   - العرض للأقرب أولاً، مهلة قبول **20 ثانية**، ثم التالي. بعد 5 محاولات أو دقيقتين → `no_driver_found`
4. **القبول:** يقبل الرحلةَ **الكبتنُ المعروضة عليه في تلك اللحظة وحده وضمن مهلته**. لا يقبلها كبتن لم تُعرض عليه ولو رآها، ولا من انقضت مهلته أو انتقل العرض لغيره — وإلا التفَّ على الدور الذي بُني عليه التوزيع كله. القبول ممكن من `searching` فقط (القسم 4)، فرحلةٌ لم يبدأ توزيعها ليست قابلة للقبول أصلاً
   → بعد القبول يشاهد الراكب بيانات الكبتن (اسم، صورة، مركبة، لوحة، تقييم) وموقعه لحظياً
5. **التتبع:** WebSocket — الكبتن يبث موقعه كل 3 ثوانٍ → Redis → يُبث للراكب
6. **الرحلة:** `arrived` (إشعار للراكب) → `in_progress` → `completed`
7. **مسار الرحلة:** أثناء `in_progress` تُخزَّن نقاط المسار الفعلي **في القاعدة** — نقطة كل 15-30 ثانية مأخوذة من بثّ الكبتن. غرضان لا يؤديهما البثّ اللحظي لأنه متطايرٌ بعمر محدود في Redis:
   - **المسافة الفعلية** عند الإنهاء، وعليها يُعاد حساب `final_fare` عند الانحراف الكبير عن المقدّر
   - **دليلٌ عند النزاعات** في لوحة الإدارة (القسم 13.4): أين سار فعلاً ومتى

   نُفِّذ في **المرحلة 6-أ**. قرارات التنفيذ التي لم يحسمها النص أعلاه:
   - **العيّنة كل 20 ثانية** (وسط نافذة 15-30). البثّ كل 3 ثوانٍ، فتخزينه كله عشرون صفاً لكل دقيقة
     رحلة بلا زيادةٍ تُذكر في دقة المسافة. النافذة نفسها هي عمر مفتاح Redis، فلا عدّاد ثانٍ يفترق عنها
     ولا يكتب عاملا uvicorn نقطتين للحظة واحدة
   - **«الانحراف الكبير» = أكثر من 20%** بين المقدّرة والفعلية، **في الاتجاهين**: أطولُ يُنصف الكبتن
     وأقصرُ يُنصف الراكب. الرقم يتجاوز خطأ تقدير Mapbox المعتاد وتحويلةً قصيرة، ولا يبلغه إلا مسارٌ
     اختلف فعلاً
   - **إعادة الحساب على المسافة وحدها**، والمدة تبقى المقدّرة: هذا البند يبني على المسافة، وإقحامُ
     زمنٍ فعليٍّ لم يطلبه يحمّل الراكبَ ازدحامَ الطريق مرتين
   - **أقل من نقطتين = لا مسافة فعلية** فيبقى المقدَّر هو الحكم — لا تخمين لمسافةٍ صمت عنها التطبيق
8. **الدفع:** `final_fare` (يُعاد حسابه على المسافة الفعلية من نقاط المسار إن انحرف كثيراً، وإلا = المقدّر) → شاشة الدفع
9. **التقييم:** متبادل، اختياري

### 5.10 تعدد الوجهات — المحطات الوسيطة (المرحلة 12-ب)

الراكب يضيف **حتى ثلاث وجهات**: وسيطتان على الأكثر ثم الوجهة الأخيرة. ويرتّبها ويحذفها **قبل
التأكيد** لا بعده — تغييرُ الوجهة بعد أن سار الكبتن يغيّر السعرَ المتفق عليه، وذاك طلبٌ جديد.

**آلةُ الحالات لكل محطة، داخل `services/rides.py` وحدها ومحروسةً بـ`ALLOWED_TRANSITIONS`:**

```
in_progress ──(وصل المحطة)──> at_stop ──(استئناف)──> in_progress
                                 └────(إنهاء عند المحطة بعد السقف)────> completed
```

- **`arrive_stop`** يختم `ride_stops.arrived_at` ويضع الرحلة في `at_stop`.
- **`resume_stop`** يختم `resumed_at`، ويعيدها `in_progress`، و**يزيد `rides.current_leg` واحداً**
  — وهو الكاتبُ الوحيد لذلك العمود، تحت قفل صف الرحلة.
- والانتقالان يمران بنفس الباب الذي تمر به كل انتقالات الرحلة: **لا راوتر يكتب حالة**.

**والزمنُ يُختم في الخلفية لا في الواجهة** (القسم 14): العدّادُ الذي يراه الراكب يمشي عنده من
`arrived_at`، **وأما المبلغُ فيأتي محسوباً من الخلفية** في حمولة الرحلة ويُحدَّث معها. عرضُ
الوقت حسابُ وقت، وعرضُ المال حسابُ مال — والثاني في الخلفية حصراً. **ولا مفاجأةَ في شاشة
الدفع**: ما يراه أثناء الانتظار هو ما يُحصَّل.

**والحساب:**

```
التقدير  = الأساس + (كم × سعر) + (دقيقة × سعر) + (عدد المحطات الوسيطة × stop_fee)
النهائي  = التقدير (أو المُعاد حسابه على المسافة الفعلية، 5.7)
           + Σ لكل محطة: max(0، دقائق الانتظار − stop_free_minutes) × stop_price_per_min
```

- **رسمُ الانتظار خارج التقدير عمداً**: لا يُعرف قبل أن يقع. والشاشةُ تقول سعرَه قبل الطلب،
  فيكون معلوماً ولو لم يكن مقدَّراً.
- **والأربعةُ مجمَّدةٌ على الرحلة** (`*_at_ride`) كالعمولة: تعديلُ الإعداد يحكم ما بعده.

**وسقفُ الانتظار** (`stop_max_wait_minutes`، وصفرٌ يعني لا سقف): مهمةٌ دورية تمر كل دقيقة،
فما تجاوز سقفَه **يُخطر الطرفين** — الراكبَ أنه تجاوز، والكبتنَ أن له مخرجاً — ويُختم
`notified_at` فلا يتكرر التنبيه في كل دورة. **ولا إنهاءَ آلي**: الإنهاءُ فعلُ الكبتن وحده،
لأن راكباً تأخّر دقيقتين فوق السقف ليس راكباً تُترك أمتعتُه على الرصيف. وما بعد السقف **يُحتسب
كاملاً**: السقفُ حدُّ صبرٍ لا حدُّ فاتورة.

**ونقاطُ المسار تُنسب لكل ساقٍ على حدة** (`ride_route_points.leg`)، فتُقرأ المسافةُ الفعلية
مفصَّلةً في دليل النزاع: أين سار بين المحطتين، لا أين سار في الرحلة كلها.

---

**الإلغاء:** مجاني قبل القبول. بعد القبول تُطبق `cancellation_fee` (قابلة للتصفير من الإدارة). انقطاع اتصال الكبتن > 60 ثانية أثناء `in_progress` → تنبيه للطرفين + عدم إنهاء الرحلة آلياً.

**وتحصيلُ الرسم مبنيٌّ منذ 2026-08-15** (`design/CANCELLATION-FEE.md`، وقراراتُ
المالك في قسمها ١١): جدولُ `ride_cancellation_charges` بطرفَيه المسمَّيَين،
و`cancellation_settings` per-country، ونوعان في الدفتر (`cancellation_fee`
مدينٌ على الراكب، `cancellation_compensation` دائنٌ للكبتن) — **لا قيدٌ صافٍ
واحد**، فالمالُ يمرّ بين مستخدمَين والمنصّةُ تنقله ولا تملكه.

وأربعُ قواعدَ تُقرأ من الكود مباشرةً: **الإعفاءُ بالقرب** من آخر موقعٍ مبثوث،
**وبلا موقعٍ يُعفى الراكب** (الفرع أ — الشكُّ لمن سيُخصم منه)، **ولا يُرفض
الإلغاء لفراغ الرصيد أبداً** بل يُسجَّل ديناً، **وتراكمُ الديون يمنع الطلبَ**
عند حدٍّ تحدّده الإدارة (`cancellation_debt_blocked`، صفرٌ يعني «لا إيقاف»).

**وتمّت المواصفةُ كاملةً في اليوم نفسِه**: السدادُ مع رحلةٍ لاحقة يقع في
`payments.settle` **بعد الأجرة والعمولة** (الفرع و: حقُّ المنصّة على رحلةٍ وقعت
أولاً)، و**القاعدةُ تنقسم بحسب من يقبض**: الكاشُ وكليك يجعلان الكبتنَ الحاليَّ
**حاملاً** — يُخصم من محفظته ويُقيَّد للمتضرر، وله مهلةٌ per-country ثم منعٌ من
التوزيع (`drivers.cancellation_carry_blocked`، بشكلِ `advance_blocked` نفسِه)؛
والمحفظةُ تُخصم مباشرةً؛ **والبطاقةُ لا تحمل الدَّين** لأن `ride_earning` و
`commission` يُحسبان من `payment.amount`، فرفعُه بقيمة الدَّين يحسب عمولةً على
مالٍ ليس أجرة — ومخرجُها الشحن. **والتحصيلُ يقع لحظةَ كل شحنٍ يكتمل** (§7) في
مسارات الشحن الثلاثة، **ويرفع المنعَ في مساره** لا بدورةٍ تالية.

**والبطاقةُ على العرض** (`rides.carried_cancellation_fee`) مجمَّدةٌ لحظةَ الطلب:
الرحلةُ تحمل **ما عُرض على الكبتن قبل أن يقبل** كـ`commission_percent_at_ride`
و`scheduled_for` — ومن قَبِل وهو يعرف لا يشتكي، ومن لم يُرد رفض بلا عقوبة.

**ومآلُ ما لا يُسدَّد** (§10) مهمّةٌ دوريةٌ كل عشر دقائق: «يبقى معلّقاً» لا فعلَ
لها، و«تشطبه الإدارة» **تتركه لإنسانٍ باسمه** (وإلا صار الخياران واحداً)،
و«تتحمّله الشركة» تكتب **دائناً بلا مدينٍ مقابل** كـ`referral_bonus` مع قيدِ
تدقيقٍ بلا فاعلٍ يسمّي الإعداد الذي قرّر. وفي اللوحة: حقولُ السياسة في
«الإعدادات»، وجدولُ الرسوم بالإعفاء والشطب في «المالية».

**وبقي بندٌ واحدٌ بقرارٍ ينتظر المالك: مكافأةُ الحامل** (رفعُ تقييمه وكوبونُ
اشتراك) — الأولُ يحتاج بيتاً ثانياً لقيمةٍ تُجمع من جدول التقييمات، والثاني
مسارَ مالٍ جديداً لا وجودَ له في `driver_subscriptions`. التفصيلُ في §6-أ من
ملف المواصفة.

**واستثناءٌ واحد يُسقط الرسم بعد القبول: `cancel_reason_code = gender_mismatch`** (المرحلة 10-ج).
امرأةٌ طلبت سائقةً فوجدت غيرها لا تُغرَّم لأنها رفضت الركوب — وإلا صار الأرخصَ لها أن تركب، وهو
ثمنٌ لا يجوز وضعُه على أحد. ويسري على الطرفين: الكبتن لا يدفع رسوماً أصلاً، لكن **البلاغ يُسجَّل
على حساب الطرف الآخر** (`gender_mismatch_reports`) وعند بلوغ الحدّ يُوسم للمراجعة.
و**السبب مرفوضٌ حيث لا محلَّ له**: لا تُقبل حجّةُ الجنس من راكبٍ لم يطلب جنساً ولا من كبتنٍ لم
يقصر عمله على جنس — بغير هذا الشرط يصير السببُ باباً للإفلات من الرسوم ووسماً لبريء.

### 5.10-ب وقفةٌ أثناء الرحلة، وانتظارٌ عند الوصول — ✅ **مبنيّةٌ** (2026-08-16)

> **طلبُ المالك (2026-08-15)**، وفروعُه الستةُ محسومةٌ في آخر القسم ومبنيّةٌ كلُّها.
>
> **جدولُها `ride_pauses`** (ترحيلة `0043`)، وخدمتُها `services/pauses.py`، ومهمّةُ سقفها
> `tasks/pauses.py` كلَّ دقيقة. **وحارسُ مالها فهرسٌ جزئيّ** — `uq_ride_pauses_open`: ضغطتان
> متزامنتان كانتا تفتحان وقفتين فيُحتسب الوقتُ **مرتين على راكبٍ وقف مرة**. ويفشل اختبارُه
> بحذف الفهرس، كما يفشل اختبارُ الفرع (هـ) بحذف شرط النطاق.
>
> **وثلاثةُ حقولٍ في التسعيرة** per-country: قيمةُ الدقيقة، ومهلةُ الوصول المجانية، والسقف.
> **وقيمةُ الدقيقة واحدةٌ للحالتين** بقرارٍ مكتوبٍ في `models/pricing.py`: دقيقةُ الكبتن
> الواقف تساوي دقيقتَه الواقفة، ورقمان لمفهومٍ واحدٍ يفترقان أوّلَ تعديلٍ لأحدهما.

**وهي ليست المحطاتِ الوسيطة وإن تشابه الحساب.** الفرقُ يقرّر الشكلَ كلَّه:

| | المحطاتُ الوسيطة (5.10) | **الوقفةُ غير المخطَّطة** |
|---|---|---|
| من يقرّرها | **الراكبُ قبل الطلب** | **الكبتنُ أثناء الرحلة** بضغطة |
| متى | قبل التقدير | بعد `in_progress` |
| أثرُها في التقدير | **داخلةٌ فيه** (المسافةُ والرسم) | **لا شيء** — تقعُ بعد أن قُدِّرت الأجرة |
| صفُّها | `ride_stops` بترتيبها | لا صفَّ مسبقاً: تُنشأ لحظةَ الضغط |

**وحالتان تُحسبان بالقاعدة نفسِها ومصدرُهما مختلف:**

1. **وقفةٌ في منتصف الرحلة** — «الراكبُ ينزل عند محلٍّ ويعود». يضغط الكبتنُ «نقطة توقف»
   فيبدأ عدّادٌ **لصالحه**، وقيمةُ الدقيقة من الإدارة per-country.
2. **انتظارٌ عند الوصول** — بعد «وصلتُ إلى الراكب» يبدأ عدّادٌ **بعد مهلةٍ مجانية** تحدّدها
   الإدارة، وقيمةُ الدقيقة كذلك.

**والقواعدُ التي تسري عليهما بلا نقاش، لأنها مبنيّةٌ ومختبَرة:**

- **المبلغُ يُحسب في الخلفية والعدّادُ يمشي في التطبيقين** (§14): الوقتُ حسابُ وقت، والمالُ
  حسابُ مال. **وفي التطبيقين معاً** — فالكبتنُ اليوم يرى «٠ دقيقة» طوال وقوفه بينما يرى
  الراكبُ عقارباً تمشي، وهو عطبٌ في دفعة الإصلاحات.
- **المعاملاتُ تُجمَّد على الرحلة** لحظةَ وقوعها (قيمةُ الدقيقة والمهلةُ المجانية)، كـ
  `commission_percent_at_ride` — فتعديلُ اللوحة يحكم ما يأتي لا وقفةً يقف فيها كبتنٌ الآن.
- **ولا عمودَ «دقائق منتظَرة»**: تُقاس من `started_at`/`resumed_at` كما تُقاس المحطات —
  عمودان لمدةٍ واحدةٍ يفترقان.
- **ولا رسمَ وقفةٍ ثابتٌ هنا** (بخلاف `stop_fee` للمحطات المخطَّطة): الوقفةُ لم تُطلب في
  التقدير، فالمحاسبةُ على **الوقت وحدَه**.

**وما يراه كلٌّ منهما**: الكبتنُ زرَّ «نقطة توقف» ثم «استئناف»، وعدّاداً يمشي ومبلغاً
يتحدّث. والراكبُ **يُخبَر أن العدّاد يعمل ولماذا** — سطرٌ صريحٌ لا رقمٌ يظهر في الفاتورة
آخرَ الرحلة، لأن مبلغاً لم يُعلَن حين نشأ يُقرأ خطأً في الحساب.

#### فروعٌ ترتفع إلى المالك قبل البناء

| # | الفرع | ✅ قرارُ المالك (2026-08-15) |
|---|---|---|
| أ | سقفُ الوقفة | **سقفٌ per-country، وسلوكُه كسقف المحطات**: يُنبَّه الطرفان ولا تُنهى الرحلة — الإنهاءُ فعلُ الكبتن |
| ب | من يوقف العدّاد | **الكبتنُ بضغطة** — عدّادٌ توقفه الحركةُ يخطئ في الزحام، وعدّادٌ لا يُصدَّق لا يُحتجّ به |
| ج | حدُّ عدد الوقفات | **لا حدَّ**، والسقفُ الزمنيُّ يكفي — وحدٌّ عدديٌّ يعاقب رحلةً طويلةً بطبيعتها |
| د | بدءُ عدّاد الوصول | **بالضغطة**، والنطاقُ دليلٌ للنزاع لا محرِّكٌ للمال |
| هـ | إن كان الكبتنُ هو المتأخّر | **خارجَ النطاق لا يبدأ عدّاد.** وتعليلُه مثبَّتٌ بنصِّ المالك: **وإلا صار «وصلت» الكاذبُ باباً للكسب** |
| و | في الأجرة أم سطرٌ مستقل | **داخلةٌ في `final_fare`، وسطرٌ مستقلٌّ في التفصيل** — المجموعُ واحدٌ والسببُ ظاهر |

### 5.11 الرحلات المجدولة (المرحلة 12-ط) — ✅ **مُنجزة** (أقرّها المالك 2026-08-13)

> `FUTURE-FEATURES` بند 29، والقسم 15-ب يسمّيها أولَ ميزات Phase-2. خلف
> `scheduled_rides_enabled` per-country، مطفأً افتراضاً ومبذوراً صراحةً.

**القرارُ الذي تقوم عليه المواصفةُ كلُّها: الحجزُ ليس رحلة.** صفٌّ في
`ride_bookings` يقول «أريد رحلةً غداً السابعة»، وصفُّ `rides` **يُولد لحظةَ
التنفيذ** لا لحظةَ الحجز. والبديل — رحلةٌ بحالة `scheduled` تنتظر — يكسر ثلاثةَ
أشياء قائمة:

1. **`uq_rides_active_rider`**: «رحلةٌ نشطةٌ واحدةٌ لكل راكب» فهرسٌ في القاعدة.
   ورحلةٌ محجوزةٌ لغدٍ تمنع صاحبَها من طلب رحلةٍ الآن — وهذا وحده كافٍ لرفض الشكل.
2. **`commission_percent_at_ride` مجمَّدةٌ عند الإنشاء** (قاعدةٌ ثابتة): صفُّ
   رحلةٍ يُنشأ قبل أسبوع يجمّد عمولةً قد تتغيّر مرتين قبل أن يتحرك أحد.
3. **كلُّ قائمةِ حالاتٍ نشطة**، وإطارُ المقبس، واستعلامُ الأهلية — سيتعلّم كلٌّ
   منها حالةً لا تعني «جارية». ودرسُ `at_stop` قريب: قيمةٌ واحدةٌ جديدة نُسيت في
   قائمتين فكذبت الشاشتان (القسم 5.10).

**والسعرُ يُحسب عند التنفيذ لا عند الحجز.** يُعرض تقديرٌ لحظةَ الحجز **بوصفه
تقديراً**، ولا يُجمَّد: التسعيرةُ تتغيّر من اللوحة، والمسارُ الفعليُّ يُعاد
حسابُه أصلاً على نقاط المسار (القسم 5.7). وسعرٌ مُلتزَمٌ به قبل أسبوع يعني
احترامَ تعريفةٍ لم تعد قائمة — أو التنصّلَ منها، وكلاهما أسوأ من قولِ «يُحسب عند
التنفيذ» في الشاشة. **ولا كوبونَ يُجمَّد على حجز**: الرمزُ يُقرأ ويُطبَّق حين
تُنشأ الرحلة، بقواعد القسم 6.6 نفسِها.

**والتنفيذُ مهمةٌ دوريةٌ كلَّ دقيقة**، تطلق التوزيعَ **قبل الموعد بهامش**
(`LEAD_MINUTES`، عشرُ دقائق) لأن التوزيعَ نفسَه يستغرق حتى دقيقتين (القسم 5.3).
وثلاثةُ فروقٍ عن الرحلة الفورية، كلُّها لأن صاحبَ الحجز **ليس أمام الشاشة**:

- **`no_driver_found` على حجزٍ يُبلَّغ فوراً** بإشعارٍ وصفٍّ في الصندوق. الرحلةُ
  الفوريةُ يراها صاحبُها تفشل؛ ومن حجز موعدَ مطارٍ سادسةَ الصباح ونام يستحق أن
  يعرف قبل أن يستيقظ متأخراً. والصمتُ هنا أسوأُ من الفشل نفسه.
- **حجزٌ حلَّ موعدُه وصاحبُه في رحلةٍ جارية** لا يُنشئ رحلةً ثانية (الفهرسُ
  يمنعها أصلاً): يصير `missed` **بإشعار** — لا يُحذف بصمتٍ ولا يُترك معلّقاً.
- **والإلغاءُ قبل بدء التوزيع مجانيٌّ بلا استثناء**، وليس هذا كرماً بل نتيجة:
  رسمُ الإلغاء تعويضٌ لكبتنٍ تحرّك (القسم 5)، ولا كبتنَ بعد. وبعد أن يقبل كبتنٌ
  تصير رحلةً عادية ويسري عليها رسمُها كما هو — **فلا قاعدةَ مالٍ جديدةٌ هنا**.

**وشرطُ الجنس يُفحص مرتين لا مرة**: طلبُ «كبتنة» يُرفض عند **الحجز** حيث الخدمةُ
مطفأة (لا يُوعَد بما لا يُقدَّم)، وإن أُطفئت بين الحجز والتنفيذ **تُنشأ الرحلةُ
بلا تفضيلٍ ويُبلَّغ صاحبُها بذلك**. والتفضيلُ ساقطاً بصمتٍ يجعلها تركب مع من لم
تقبله؛ ورفضاً عند التنفيذ يتركها بلا رحلةٍ في موعدٍ رتّبت حياتَها عليه — والإبلاغُ
هو المخرجُ الوحيد الذي لا يكذب ولا يهجر.

**وثلاثةُ أشياء تُترك عن قصدٍ في هذا القطع الأول**، وكلُّها في `FUTURE-FEATURES`
بند 29 بانتظار حاجةٍ حقيقية:

- **لا تكرار** (`يومي عمل` · `أسبوعي`): قاعدةُ تكرارٍ تعني جدولَ وقوعاتٍ
  واستثناءاتِها (عطلةٌ، يومٌ يُلغى وحده) وشاشةَ إدارةٍ لها. و«ذهابٌ وإياب»
  **حجزان** لا قاعدةَ تكرارٍ واحدة. فالقطعُ الأول حجزٌ لمرةٍ واحدة.
- **ولا محطاتٍ وسيطة** على الحجز: المحطاتُ ميزةٌ خلف مفتاحها (القسم 5.10)،
  وضمُّها هنا يضاعف حالاتِ الاختبار قبل أن يطلبها أحد.
- **ولا تعيينَ سائقٍ مسبقاً**: التوزيعُ يعرض على واحدٍ في كل مرة (القسم 5.3)،
  وكبتنٌ يُحجز له موعدٌ قبل ساعاتٍ إما يعمل حينها أو لا — وحجزُه يعني أن نمنعه من
  العمل أو نكسر الحجز. وهو بند 27 في `FUTURE-FEATURES` لا هذا القطع.

### 5.12 مشاركةُ الرحلة بين ركاب (المرحلة 12-ي) — **قراراتُ المالك مثبَّتةٌ أدناه، والبناءُ لم يبدأ**

> القسم 15-ب يسمّيها «مشاركة الرحلة بين ركاب»، وهي **أكبرُ بنود Phase-2** — أكبرُ
> من الرحلات المجدولة، ولأسبابٍ ليست في الشاشة بل في ثلاثة أشياء قائمة: فهرسٌ في
> القاعدة، وسؤالُ مالٍ لا جوابَ له في المواصفة، وسلامةٌ لا يحلّها كود.

**أولاً: الشكلُ المعماريّ — وهو مفرقُ الطريق كلِّه.**

الرحلةُ اليوم صفٌّ لراكبٍ واحد: `rides.rider_id` عمودٌ واحد، وكلُّ فحص ملكيةٍ
(`get_ride_for_user`) وكلُّ دفعةٍ (`payments.ride_id`) وكلُّ تقييمٍ
(`ratings.(ride_id, rater_type)`) وكلُّ نزاعٍ وكلُّ قناةِ مقبسٍ مبنيةٌ عليه.
وللمشاركة شكلان لا ثالث:

- **(أ) رحلةٌ واحدةٌ بركّاب**: جدولُ `ride_passengers`، و`rider_id` يصير «الأول»
  أو يُحذف. وهذا يعني إعادةَ كتابةِ الملكية والدفع والتقييم والنزاع والمقابس —
  ولكلٍّ منها قواعدُ مالٍ مستقرّةٌ تُختبر اليوم. **وهو الشكلُ الذي يُنصح
  بتجنّبه**: الكلفةُ ليست في الجدول بل في أن «من يملك هذه الرحلة» يصير سؤالاً
  بجوابين، وهو السؤالُ الذي يحرس كلَّ منفذٍ في النظام (القسم 14).
- **(ب) رحلتان بمجموعةٍ واحدة** — **وهذا ما تقترحه المواصفة**: يبقى كلُّ صفِّ
  رحلةٍ لراكبه بملكيته ودفعته وتقييمه ونزاعه كما هي، ويُضاف عمودُ
  `share_group_id` يجمع الرحلتين على كبتنٍ واحد. فلا قاعدةَ مالٍ تُعاد كتابتُها،
  والتغييرُ ينحصر في التوزيع والتسعير والخريطة.

**وثمنُ (ب) الحقيقيُّ فهرسٌ واحد**: `uq_rides_active_driver` فهرسٌ **فريدٌ جزئي**
على `driver_id` لكل حالةٍ نشطة — أي أن القاعدةَ نفسَها تمنع رحلتين على كبتنٍ
واحد، وهو حارسٌ مقصودٌ يحمي من سباقٍ لا من خطأٍ برمجي. والمشاركةُ تكسره بالتعريف.
واستبدالُه بفحصٍ في الخدمة **تراجعٌ عن قاعدةِ المشروع** («القاعدةُ تفرض رحلةً
نشطةً واحدة حتى تحت السباق»)، فالبديلُ المقترح: فهرسٌ فريدٌ على
`(driver_id, COALESCE(share_group_id, id))` — يسمح لرحلتين **في مجموعةٍ واحدة**
ويمنع كلَّ ما عداهما، فيبقى الحارسُ في القاعدة لا في الذاكرة. **وهذا بندٌ يُقاس
باختبار تزامنٍ قبل أن يُقاس بشاشة.**

**ثانياً: أسئلةُ المال — ولا تُبنى الميزةُ قبل جوابها.**

1. **كم يوفّر الراكب؟** نسبةٌ ثابتةٌ per-country (٢٥٪ مثلاً) أم حسابٌ بالمسافة
   المشتركة؟ والثابتُ يُفهَم ويُعلَن، والمحسوبُ أعدلُ ولا يُشرح في شاشة.
2. **وماذا يقبض الكبتن؟** المشاركةُ لا تنجح إلا إذا كان مجموعُ ما يقبضه **أكثر**
   من رحلةٍ منفردة — وإلا رفضها وهو محقّ. فهل المجموعُ = الأجرةُ المنفردة + نسبةٌ
   له، والفرقُ خصمٌ للراكبين تتحمّله الشركة كالكوبون؟ أم يقبض مجموعَ ما دفعه
   الراكبان بلا زيادة؟
3. **ومن يحمل خطرَ ألّا يأتي ثانٍ؟** الراكبُ طلب مشاركةً بسعرٍ أقل، ولم يوجد له
   شريك. فإما **تُنفَّذ بالسعر المخصوم وتتحمّل الشركةُ الفرق** (وعدٌ يُحترم،
   وكلفةٌ تُقاس)، أو **يُرفع السعرُ إلى المنفرد ويُبلَّغ قبل الانطلاق** (لا كلفةَ،
   ووعدٌ يُخلف). **والثالثُ مرفوضٌ**: أن يُقال له في نهاية الرحلة إن السعرَ صار
   غيرَ ما وافق عليه.
4. **ورسمُ الإلغاء لأحد الراكبين**: إلغاءُ الأول بعد ركوب الثاني لا يُلغي رحلةَ
   الثاني، وحسابُ ما يُخصم من الأول يحتاج قاعدةً مكتوبة. **← أُجيب في القرار
   الخامس أدناه (2026-08-13).**

**ثالثاً: سلامةٌ لا يحلّها كود، وهي أخطرُ ما في البند.**

`ride_gender_preference` تفضيلٌ في **جنس الكبتن** لا في جنس من يجلس بجانبها. وأول
ما تفتحه المشاركةُ هو **راكبٌ ثانٍ لم تختره ولم تُسأل عنه** — وامرأةٌ طلبت كبتنةً
لأمانها ثم وجدت رجلاً غريباً في المقعد الآخر تكون الخدمةُ قد نقضت غرضَها بيدها.
فالمواصفةُ تقترح: **رحلةٌ بتفضيلٍ نسائيٍّ لا تُشارَك إلا مع راكبةٍ أعلنت جنسها**،
وحيث `women_service_enabled` مطفأٌ **لا مشاركةَ لأي طلبٍ بتفضيل**. وهذا قرارُ
سياسةٍ لا تفصيلُ تنفيذ — ويحتاج إقرارَ المالك صريحاً.

**والمطابقةُ نفسُها عملٌ جغرافيٌّ جديد**: ممرٌّ حول مسار الرحلة الأولى (نقطةُ
انطلاق الثاني ووجهتُه داخل مسافةٍ من المسار)، وسقفُ التفافٍ بالدقائق (لا يُطيل
رحلةَ الأول أكثر من كذا)، ونافذةُ انتظارٍ للشريك. وثلاثتُها أرقامُ إعداداتٍ
per-country لا ثوابتُ كود، وكلُّها تُقاس بمسارِ Mapbox — أي نداءٌ لكل مرشَّح.

**رابعاً: قراراتُ المالك** (2026-08-13) — ثمانيةٌ مثبَّتة، وشطرٌ واحدٌ من سؤال
كلفةٍ مؤجَّلٌ إلى أن تُقاس أرقامُه.

1. **الشكل (ب): رحلتان في مجموعةٍ واحدة** بعمود `share_group_id`. فلا تُعاد كتابةُ
   ملكيةٍ ولا دفعةٍ ولا تقييمٍ ولا نزاع، ويبقى «من يملك هذه الرحلة» سؤالاً بجوابٍ
   واحد.
2. **الحارسُ يبقى في القاعدة لا في الخدمة، واختبارُ تزامنٍ عليه قبل أي شاشة** —
   فهذا المكانُ وحدَه هو ما تستطيع فيه هذه الميزةُ أن تصنع مالاً من لا شيء.

   **وصيغةُ الفهرس صُحِّحت بالقياس (2026-08-13).** اقترح هذا القرارُ أولاً
   `(driver_id, COALESCE(share_group_id, id))`، وكُتب اختبارُ التزامن قبل أيِّ
   كودٍ آخر كما يأمر القرارُ نفسُه — فأظهر أن الصيغة **مقلوبةٌ تماماً**:

   - رحلتان في **مجموعةٍ واحدة**: مفتاحُهما واحدٌ (المجموعةُ نفسُها)، فتُرفض
     الثانية — أي أن الفهرس يمنع **الميزةَ التي وُجد ليسمح بها**.
   - رحلتان **منفردتان** على كبتنٍ واحد: مفتاحُ كلٍّ منهما مُعرِّفُها هي، فيختلفان
     وتمرّان — أي أن الفهرس **يُلغي الحارسَ القديم الذي جاء ليوسّعه**.
   - ومجموعتان مختلفتان على كبتنٍ واحد: تمرّان كذلك.

   ولم يكن شيءٌ من ذلك ليسقط اختباراً قائماً: الحارسُ القديم لم يكن له اختبارُ
   تزامنٍ خاصٌّ به، فكان اختفاؤه صامتاً — وهو بعينه سببُ تقديم الاختبار.

   **والصيغةُ التي تحرس فعلاً تقوم على المقعد لا على المجموعة**: عمودٌ
   `rides.share_seat` قيمتُه ١ للرحلة الأولى **ولكلِّ رحلةٍ منفردة**، و٢ لشريكها.
   ثم:

   - `uq_rides_active_driver` = فريدٌ على `(driver_id, share_seat)` بالشرط الجزئي
     نفسِه. فللكبتن مقعدٌ واحدٌ من كلِّ رقم — أي رحلتان نشطتان على الأكثر؛
     **ورحلتان منفردتان تتنازعان المقعد ١ فتسقط الثانية، فينجو الحارسُ القديم
     بحرفه** بدل أن يُستبدل.
   - `uq_rides_active_share_group` = فريدٌ على `(share_group_id, share_seat)` حيث
     `share_group_id IS NOT NULL`. وهو يحدّ **المجموعةَ** براكبين مهما اختلف
     كبتناها — وهي الحالُ الوحيدةُ التي لا يراها حارسُ الكبتن، ولذلك أُفرد لها
     اختبارٌ يملكها وحدَه (`test_a_group_holds_two_rides_whatever_the_drivers`)
     بعد أن تبيّن أن حذفَ الفهرس لا يُسقط أيَّ اختبارٍ آخر.
   - وقيدُ `ride_share_seat_valid`: المقعدُ ١ أو ٢، **والمقعدُ ٢ لا يوجد بلا
     مجموعة** — فلا يُحجز المقعدُ الثاني لرحلةٍ منفردة.

   **وما يبقى للخدمة تحت القفل**: أن يكون مقعدُ الشريك في **مجموعة الكبتن نفسِها**
   لا في مجموعةٍ أخرى. وهذا لا يعبّر عنه فهرسٌ فريد (يحتاج مقارنةً بين صفّين)،
   وأثرُه ليس مالاً من لا شيء بل تجميعٌ خاطئ — فيُذكر هنا صراحةً بوصفه ما تملكه
   الخدمةُ لا القاعدة، كي لا يُظنَّ أن الفهرس يحرسه.

   **وقياسُ تكوين المجموعة (2026-08-13) صحّح ظنّاً ثانياً في هذا القرار نفسِه.**
   `tests/test_ride_sharing_join_concurrency.py` يطلق ملتحقَين على الرحلة الأولى
   معاً: مقعدٌ واحدٌ يُملأ والثاني يُرفض — **لكنّ حذفَ `with_for_update` عن الرحلة
   الأولى يُبقي الاختبارَ أخضر**، لأن الملتقِطَ هو فهرسُ `(driver_id, share_seat)`
   لا القفل. فالمقعدُ ملكُ الفهرس، وذلك الاختبارُ يحرس **سلوكاً** (رمزُ الرفض الذي
   يقرؤه الراكب) — كما في اختبار «مستخدمٍ واحدٍ للكوبون» في 12-ز.

   **والذي يملكه قفلُ الرحلة الأولى وحدَه هو حالتُها بين الفحص والكتابة**: كبتنٌ
   ينطلق بينما ملتحقٌ يقرأ حالةً سابقةً للانطلاق. بلا القفل يجتاز الفحصَ ثم يكتب،
   فيصير راكبٌ ملحَقاً بسيارةٍ غادرت مكانَه — **صفٌّ سليمٌ تماماً يقول ما لم يقع**،
   بلا استثناءٍ ولا سطرٍ في السجل. قِيس بالحذف: `ok` بدل `share_group_unavailable`.
3. **خصمٌ بنسبةٍ ثابتةٍ per-country**، بقيدٍ صريحٍ على معايرتها: **ما يقبضه الكبتن
   من رحلتين أعلى بوضوحٍ مما يقبضه من رحلةٍ منفردة** — وإلا رفض المشاركةَ وهو محقّ.
   **والشركةُ تتحمّل الفرقَ في القطع الأول**، بآلية 12-ز نفسِها (صفُّ دفعةٍ تتحمّلها
   الشركة)، فلا ينقص دخلُ الكبتن بخصمٍ لم يقرّره. وبهذا يُجاب السؤالُ الثالث أعلاه:
   **الوعدُ يُحترم** — تُنفَّذ بالسعر المخصوم ولو لم يوجد شريك.
4. **السلامة — وقد شدَّد المالكُ ما اقترحته المواصفة**: الاقتراحُ كان «تُشارَك مع
   راكبةٍ أعلنت جنسها»، والقرارُ أضيقُ منه: **طلبٌ بتفضيلٍ نسائيٍّ غيرُ قابلٍ
   للمساقاة افتراضياً — حتى مع راكبةٍ أخرى — ولا يصير قابلاً إلا بخيارٍ صريحٍ
   تختاره الراكبةُ نفسُها.** وحجّتُه القاعدةُ التي تُعمَّم على كل بندٍ أمنيٍّ بعده:
   **القبولُ الصامتُ لا يكفي في مسألة أمان** — «كانت ستوافق لو سُئلت» ليس موافقة،
   والافتراضُ في السلامة يكون على الأضيق لا على الأنفع.

5. **رسمُ الإلغاء حين يلغي أحدُ الراكبين** — جوابُ السؤال الرابع أعلاه، وهو
   **شطران لا حكمٌ واحد**، لأن في الحادثة طرفين أحدُهما فعل والآخرُ وقع عليه
   الفعل:

   - **مَن ألغى: الرسمُ العاديُّ عليه وحدَه.** لا رسمَ مشاركةٍ جديد، ولا مضاعفةَ
     رسمٍ لأنه أخلف اثنين. صفُّ رحلتِه يُلغى بقواعد الإلغاء القائمة
     (`rides.cancel_ride` والرسمُ المجمَّد على الرحلة) كأنه رحلةٌ منفردة — فلا
     قاعدةَ مالٍ جديدة تُكتب، **وهذا بعينه ما اشتُري بالشكل (ب)**: كلُّ صفِّ
     رحلةٍ يُحاسَب بمفرده، والمجموعةُ توزيعٌ لا محفظةٌ مشتركة.
   - **ومن بقي: تتحوّل رحلتُه إلى منفردةٍ بسعرها الكامل، ويُبلَّغ صراحةً.** الخصمُ
     كان ثمنَ مشاركةٍ لم تقع، فبقاؤه بعد زوال سببها خصمٌ بلا سبب. و**الإشعارُ
     الصريحُ هو ما يفصل هذا عن الخيار المرفوض** في السؤال الثالث: المرفوضُ أن
     يُقال له **في نهاية الرحلة** إن السعرَ صار غيرَ ما وافق عليه، والمقبولُ أن
     يُقال له **حين يقع** — وهو الخيار (ب) من ذلك السؤال مطبَّقاً على حالة
     الإلغاء لا على حالة «لم يوجد شريك».

   **ولا يناقض هذا القرارَ الثالث**، وإن بدا كذلك: القرارُ الثالث يحكم **غيابَ
   الشريك من أصله** (الوعدُ يُحترم والشركةُ تتحمّل)، وهذا يحكم **شريكاً وُجد ثم
   انسحب**. والفرقُ ليس شكلياً — في الأول لم يقع للراكب حدثٌ يُبلَّغ به، وفي
   الثاني وقع حدثٌ له وقتٌ معلوم، وهو ما يجعل الإبلاغَ ممكناً أصلاً.

**وسؤالُ الكلفة — لا سؤالُ التصميم: هل تتحمّل الشركةُ فرقَ من بقي؟** قرارُ المالك
(2026-08-13): **مفتوحٌ حتى يرى الأرقام.** فالقطعُ الأولُ يرفع السعرَ إلى المنفرد كما
أعلاه، وإن أظهرت الأرقامُ أن الفرقَ صغيرٌ وأن رفعَه يُخسر راكباً أكثرَ مما يوفّر،
**فآليةُ تحمُّله مبنيةٌ سلفاً**: صفُّ دفعةٍ بقناة `promo` تتحمّلها الشركة (12-ز) —
أي أن الجواب حين يأتي **إعدادٌ لا بناء**، وهذا وحدَه ما يبيح تأجيلَه إلى ما بعد
القياس. (وقد ضاق هذا السؤالُ بالقرار السادس أدناه إلى ما **قبل** الانطلاق وحدَه.)

6. **ومن بقي وقد بدأت رحلتُه فعلاً (`in_progress`): الشركةُ تتحمّل الفرق، ولا
   يُرفع عليه سعر.** لأن رفعَه في منتصف الطريق **هو الخيارُ المرفوضُ بحرفه** —
   وما يجعله مرفوضاً ليس مقدارَ المال بل **انعدامَ البديل**: من عُرض عليه السعرُ
   وهو في السيارة لا يملك قبولاً ولا رفضاً، فالإخبارُ حينئذٍ إعلامٌ بأمرٍ واقعٍ
   لا إشعارٌ يُبنى عليه قرار. وبهذا يتّضح أن شرطَ القرار الخامس ليس «أن يُقال
   له» بل **«أن يُقال له وهو قادرٌ على التصرّف»**.
7. **ولا إعفاء من رسم الإلغاء لمن رفض السعرَ الجديد.** وحجّتان: **إعفاءٌ ثانٍ في
   نظامٍ له إعفاءٌ وحيد يفتح باباً** — و`gender_mismatch` إعفاءٌ يقوم على شرطٍ
   يمكن التحقّقُ منه من الصفّ نفسِه، أما «رفضتُ سعراً» فحالٌ يبلغها كلُّ من ألغى
   لأي سبب؛ **ومن رُفض عليه السعرُ قد عُرض عليه قبل أن يمضي**، فإلغاؤه بعده
   إلغاءٌ عاديٌّ بقرارٍ اتخذه وهو يعلم.
   - **والقراران السادس والسابع يُكمل أحدُهما الآخرَ ولا يتصادمان**: السادس يمنع
     رفعَ السعر حيث لا بديل، فلا يبقى للسابع إلا الحالُ التي **فيها** بديل — أي
     قبل الانطلاق، حيث الإشعارُ يسبق المضيَّ ويكون الرفضُ مجّانياً بطبيعته
     (إلغاءٌ قبل القبول بلا رسمٍ أصلاً، وبعده رسمُ الإلغاء المعتاد كأيّ رحلة).
8. **ولا بحثَ عن شريكٍ ثالثٍ في القطع الأول.** «راكبان لا أكثر»، ونافذةُ انتظارٍ
   ثانيةٌ تُطيل رحلةَ من بقي لأجل خصمٍ لم يعد يُطبَّق عليه.

**فلم يبقَ مفتوحاً إلا سؤالُ الكلفة أعلاه، وقد ضاق بالقرار السادس**: الشركةُ
تتحمّل الفرقَ **بعد الانطلاق** بقرارٍ مثبَّت، والمفتوحُ هو **ما قبل الانطلاق**
وحدَه — أي هل يُرفع السعرُ على من بقي ولم يتحرّك بعد، أم تتحمّله الشركةُ هناك
أيضاً. وكلا الجوابين يمرّ بآلية 12-ز نفسِها، فالفرقُ بينهما إعدادٌ لا بناء.

**وأصغرُ قطعٍ قابلٍ للتسليم** (إن أُقرّت الميزة): **راكبان لا أكثر**، وخصمٌ
**ثابتٌ per-country**، والشركةُ تتحمّل الفرقَ حين لا يوجد شريك (كالكوبون تماماً،
فآليةُ «دفعةٌ بقناةٍ تتحمّلها الشركة» مبنيةٌ أصلاً في 12-ز)، وبلا محطاتٍ وسيطة،
وبلا حجزٍ مسبق. وما زاد على ذلك بندٌ مستقلٌّ لاحقاً.

**خامساً: المطابقةُ كما بُنيت (2026-08-13)** — ستُّ قراراتٍ تنفيذيةٍ مشتقّةٍ من
الثمانية أعلاه، مكتوبةٌ هنا لأن أربعاً منها لا يقرؤها أحدٌ من الكود:

1. **مكانُها مهمةُ التوزيع لا الراوتر.** `POST /rides` يجيب فوراً بـ`requested`،
   والمطابقةُ نداءُ Mapbox — فوقوفُ ردِّ الطلب عليه هو ما رفضته المرحلةُ الرابعة
   حين أخرجت العرضَ الأولَ من الراوتر. فـ`dispatch._run` يجرّبها **بعد**
   `searching` و**قبل** أوّل عرض، وإن لم يجد شريكاً مضت الحلقةُ كأن الميزةَ غيرُ
   موجودة. وأيُّ تعثّرٍ فيها يعيد الرحلةَ إلى التوزيع العادي: المشاركةُ **زيادةٌ
   محتملة** بنصِّ القرار الثالث، فلا يجوز أن يتركَ بحثٌ عن شريكٍ راكباً بلا سيارة.
2. **الممرُّ تصفيةٌ في القاعدة، والحكمُ سقفُ الالتفاف من Mapbox.** الممرُّ محسوبٌ
   حول **الوتر** بين نقطتي الرحلة الأولى (`ST_MakeLine` + `ST_DWithin` على
   `geography`)، وهو أضيقُ من ممرِّ المسار الحقيقيِّ في الأطراف وأوسعُ في الوسط —
   ومقبولٌ لأنه ليس القرار: ما يقرّر هو الالتفافُ المقيس. وفائدتُه أنه يمنع نداءً
   خارجياً لكلِّ رحلةٍ قائمةٍ في البلد.
3. **الترتيبُ المقيسُ هو الأسوأُ للأول عمداً**: `الأول ← الثاني ← وجهةُ الثاني ←
   وجهةُ الأول`، **نداءٌ واحدٌ لكلِّ مرشَّح**. فأيُّ ترتيبٍ يسوقه الكبتنُ فعلاً
   أقصرُ أو مساوٍ — أي أن السقفَ يصحّ مهما ساق، بدل أن يصحّ في ترتيبٍ ويُخلَف في
   غيره. **والمرشَّحون ثلاثةٌ على الأكثر، والأقدمُ أولاً**: من انتظر أطولَ يُخدَم
   أولاً، وقياسُ الجميع لاختيار الأقلِّ التفافاً نداءان زائدان في مسارٍ ينتظره راكب.
4. **وشرطُ السلامة متناظرٌ ويقوم على الإعلان**: يكفي أن يكون أحدُ الطلبين مجنَّساً
   ليُشترط في **كليهما** أن يكون صاحبُه قد أعلن أنوثته. فمن اشترطت كبتنةً لا
   تُعطى راكباً، ومن لم تشترط لا تُقحَم في مقعدٍ اشترطته غيرُها. **وإعلانٌ لا ختمُ
   مشرف**: `gender_verified_at` للكبتنة وحدَها لأن إعلانَها يقيّد أمانَ غيرها،
   ورفعُ الشرط هنا إلى ختمٍ يُغلق البابَ على كلِّ راكبةٍ في السوق — ولا أحدَ يختم
   الركّاب.
5. **والكبتنُ يُخطَر ولا يُستأذَن، وهذا يقوم على شرطٍ يجب أن يتحقّق في تطبيقه**:
   الرحلةُ تُعرض عليه **معلَّمةً بالمشاركة** فيقبلها بها — فالمقعدُ الثاني احتمالٌ
   وافق عليه لا أمرٌ وقع عليه. وهي حجّةُ شارةِ «طلب نسائي» فوق الأجرة نفسُها
   (10-ج): ما يمنع كبتناً من القبول ثم الإلغاء هو ما يقرؤه **قبل** أن يقبل.
   **فبطاقةُ العرض التي لا ترسم الشارةَ تُبطل هذا القرار** — والحقولُ الثلاثة
   (`share_discount_percent`، `share_group_id`، `share_seat`) في `RideOut` أصلاً،
   فتصل إليه في إطار العرض بلا استعلامٍ زائد. **وهذا القرارُ معروضٌ على المالك**
   بوصفه الوحيدَ من الستة الذي يمسّ ما يوافق عليه إنسان.
6. **ولا بابَ ثانيَ لإسناد كبتن**: `join_group` يقرأ جدولَ `ALLOWED_TRANSITIONS`
   من `services/rides.py` نفسِه ولا ينسخه، فلا يبلغ `accepted` إلا من `searching`
   كأيِّ رحلة. وهو ليس قبولاً بلا عرض: **الكبتنُ اختير بعرض الرحلة الأولى**،
   والملتحقُ يدخل على قبولٍ وقع.

---

## 6. الدفع — التدفقات التفصيلية

### كاش
نهاية الرحلة → الراكب يسلّم → الكبتن يضغط "استلمت المبلغ" → `confirmed`

### كليك (الأردن فقط) — صفحة دفع كليك داخل التطبيق

يستبدل هذا التدفقُ التدفقَ اليدوي السابق (عرض alias + زر نسخ + تأكيد):

1. شاشة الدفع تعرض **المحفظة خياراً أول وكليك ثانياً**
2. عند اختيار كليك: TAXO يولّد **QR ديناميكياً بصيغة CliQ** يحمل **alias الكبتن** والمبلغ
   و**مرجعاً داخلياً يولّده TAXO**، ومعه **deep link** يفتح تطبيق البنك
3. الراكب يحوّل من بنكه، ثم يعود ويُدخل **رقم مرجع الحوالة** (ورفع لقطة شاشة اختياري)
4. **إشعار فوري للكبتن** → يؤكد «وصلني» أو يرفض
5. كل الخطوات تُسجَّل بطوابع زمنية في `payments`: المبلغ، وalias المعروض، والمرجع الداخلي، ومرجع
   الحوالة، ومن أكّد ومتى — **سجلٌّ غير قابل للتعديل** كبقية الدفتر
6. عند الرفض أو انقضاء مهلة التأكيد → `disputed` بحقول الفصل القائمة (القسم 4/13.4)
   - **المهلة مبنيةٌ (المرحلة 10):** رقمُها `payment_settings.cliq_confirmation_hours` لكل دولة
     (افتراضُه ٢٤ ساعة، يديره المشرف من `PATCH /admin/settings/payments/{country}`)، **ويُجمَّد
     على الدفعة** في `payments.cliq_confirmation_expires_at` لحظة إدخال المرجع — فتعديلُ الإعداد
     يحكم ما يأتي بعده ولا يحرّك مهلةً رآها الكبتن. ومهمةُ
     `app/tasks/payments.py::sweep_cliq_confirmations` تمر كل خمس دقائق فتنقل ما انقضى إلى
     `disputed` تحت قفل الصف، **وتُخطر الطرفين**: الكبتنَ أن دفعته صارت نزاعاً، والراكبَ أن
     تحويله لم يُؤكَّد — وصمتُ النظام هنا يصنع تذكرتَي دعمٍ لا واحدة
   - **ولا يُؤكِّد الكبتن دفعةً صارت نزاعاً**: `disputed → confirmed` بابُ فصلِ الإدارة (القسم
     13.4)، فتأكيدٌ من التطبيق يُقيّد المال ويترك النزاع بلا `resolution` ولا مَن فصل. من انقضت
     مهلته ثم وصله المال يفصل له المشرف `paid`

**المال لا يمر بحساب الشركة إطلاقاً في هذه القناة**، فلا تأكيد آلي ممكن لدفع الرحلة بكليك: ما يشهد
عليه حساب التاجر هو ما يدخله، والحوالة هنا تذهب من الراكب إلى **alias الكبتن** مباشرةً. الحمايةُ من
النزاع تأتي من **السجل والمرجع** لا من تحقّقٍ آلي. وتفعيلُ عقد الـ acquirer (القسم 15/أ) **لا يغيّر
هذا**: نطاقه شحن المحفظة وحده (القسم 7)، ولا يُخرج `cliq` من `DIRECTLY_COLLECTED_METHODS`.

**التنفيذ:** الخلفية والسجل في المرحلة 6-أ (قائمان)، وشاشتا القناة — توليد الـ QR والـ deep link
وإدخال المرجع عند الراكب، وبطاقة التأكيد عند الكبتن — في **المرحلة 9** (تطبيق الراكب) و**المرحلة 10**
(تطبيق الكبتن).

**نُفِّذت شاشة الراكب في المرحلة 9.** القرارات التي لم يحسمها النص أعلاه:

- **الرمز يُبنى في الخلفية والواجهة ترسمه.** الحمولة تحمل مبلغاً ومرجعاً يُحاسَب عليهما، وبناؤها
  في الواجهة يجعلها طرفاً في تحديد ما يُدفع (القسم 14). فـ `services/cliq_qr.py` هو **الملف
  الوحيد الذي يعرف صيغة الرمز**، والواجهة تأخذ `qr_payload` نصّاً وترسمه مربّعاً.
- **الحقول الأربعة أضافتها الترحيلة `0013` إلى `payments`**: `cliq_alias` و`cliq_reference`
  (فريد) و`cliq_transfer_reference` و`cliq_reference_at`. البند 5 أعلاه يطلب تسجيل الخطوات كلها،
  والمبلغُ ومن أكّد ومتى قائمةٌ منذ 6-أ — والثلاثة الباقية كانت مفقودة.
- **alias مُجمَّد على الدفعة لا مقروءٌ من `drivers` عند العرض:** الكبتن قد يغيّر aliasه غداً،
  فيقرأ فاصلُ النزاع بعد أسبوع وجهةً غير التي حُوِّل عليها فعلاً — وهي بالضبط المعلومة التي وُجد
  السجل لأجلها.
- **مرجع الحوالة يُكتب مرةً واحدة ولا يُعدَّل** (`409` على الثانية)، وفاءً لـ«سجلٌّ غير قابل
  للتعديل» في البند 5: مرجعٌ يتبدّل بعد أن رآه الكبتن سجلٌّ لا يُحتج به. ومن أخطأ في الرقم يقول
  الكبتنُ «لم يصلني» وتفصل الإدارة (13.4). والكتابة تحت **قفل صف الدفعة**، وإلا كتبت ضغطتان
  متزامنتان مرجعين وأرسلتا إشعارين لحوالةٍ واحدة.
- **صيغة الرمز معيارية وثلاثةُ تفاصيلها تحتاج مطابقة**، كما في `telr.py` و`cliq/acquirer.py`:
  الهيكل EMVCo Merchant-Presented Mode بـ CRC-16 في الحقل 63 (معيارٌ لا اجتهاد فيه)، وما يخص
  كليكاً بعينه — معرّف النظام `CLIQ_GUID`، ورمز فئة التاجر، ومخطط الـ deep link — مجموعٌ في
  ثوابت مسمّاة أعلى الملف. وخطؤها **لا يحرّك مالاً**: لا شيء في ذلك الملف يكتب في الدفتر، ورمزٌ
  لا يقرؤه تطبيق البنك يعني تحويلاً يدوياً على الـ alias المعروض بجانبه ثم إدخالَ المرجع كما هو.
- **لا زرّ «تم الدفع» عند الراكب**: المال يذهب إلى alias الكبتن ولا يمر بالمنصة، فلا شاهد عندنا —
  والتأكيد قولُ الكبتن وحده. و**مهلةُ التأكيد** (البند 6) مع بطاقة الكبتن في **المرحلة 10**.
- **رفعُ لقطة الشاشة الاختياري مؤجَّل**: لا بنيةَ رفعِ ملفاتٍ في المشروع بعد (مستندات الكبتن في
  المرحلة 10 تحتاجها كذلك)، وبناءُ مسارِ تخزينٍ لحقلٍ وصفَه النص **اختيارياً** بناءٌ سابقٌ لأوانه.
  المرجعُ النصّي هو الدليل الملزِم في السجل، واللقطة تدعمه ولا تقوم مقامه.

### بطاقة عبر Telr (الأردن)
1. الخلفية تنشئ طلب دفع لدى Telr → توجيه الراكب لصفحة الدفع (بطاقة/Apple Pay/Google Pay)
2. Telr Webhook → الخلفية تتحقق وتحدّث `confirmed`
3. قيد `ride_earning` بنصيب الكبتن في محفظته (المبلغ − العمولة إن كانت مفعلة)
4. Tokenization: حفظ البطاقة للدفع بضغطة لاحقاً
5. **مرجع التوثيق:** يُسلّم رابط توثيق Telr الرسمي في جلسة التنفيذ. البناء على Sandbox

**نُفِّذ في المرحلة 6-ب.** القرارات التي لم يحسمها النص أعلاه:

- **الحقيقةُ جوابُ المزود للخلفية، لا حمولةُ الإشعار.** التوقيع شرطٌ لا سند: يُتحقق منه، ثم لا
  يُقرأ من الحمولة إلا **أيُّ طلبٍ ننظر فيه**؛ والحالة والمبلغ يأتيان من نداء `check` من الخلفية
  إلى المزود، وعليه وحده يتحرك الدفتر. فتوقيعٌ صحيحٌ لحمولةٍ عُدّل مبلغها لا يقيّد ديناراً زائداً،
  ومبلغٌ آتٍ من الشبكة لا يصل الدفتر بحال. `WebhookNotice` بلا مبلغ عمداً كي لا يكون ذلك ممكناً.
- **الإشعار واستعلامُ العميل شريكان لا أصلٌ واحتياط.** الواجهة تعود من صفحة الدفع فتسأل
  `GET /payments/card/orders/{cart_id}`، وأيُّهما وصل أولاً سوّى؛ الآخر يجد الطلب محسوماً فلا
  يقيّد شيئاً ولا يرتدّ بخطأ. مسارٌ واحدٌ فقط يحرّك المال (`card_payments.apply_state`).
- **لا تخمين على غير محسوم، في الاتجاهين.** رمزُ حالةٍ لا نعرفه أو `pending` يبقيان الطلب معلّقاً:
  عدُّه مدفوعاً يقيّد مالاً لم يصل، وعدُّه ساقطاً يفتح الرحلة لقناةٍ ثانية والراكب على صفحة الأولى.
  وسقوطُ العملية يُسقط دفعتها فيعود مبلغها ديناً على الرحلة — نفس منطق `resolution=unpaid` في كليك.
- **مبلغُ المزود يخالف مبلغَنا ⇒ نزاع لا قيد.** الطلب `failed` والدفعة `disputed` (لا `failed`):
  مالُ الراكب قد خرج فعلاً، فلا يُقيَّد بلا يقين ولا تُفتح الرحلة للدفع مرتين — تفصل الإدارة (13.4).
- **الاسترداد يعود من حيث جاء المال:** دفعةُ البطاقة تُردّ **عند المزود** إلى نفس البطاقة، فلا قيد
  `refund` للراكب — رصيدُه لم يُخصم أصلاً فردُّه إليه رصيداً يمنحه مرتين. وقيدُ `adjustment` المضاد
  على الكبتن يبقى في الحالتين. ومرجعُ الردّ يُحفظ في `provider_orders.refund_ref`، وحالةُ الطلب
  تبقى `paid`: الطلب دُفع فعلاً، والردُّ حركةٌ تالية له لا نقضٌ لتاريخه.
- **مزوّدٌ وهمي يُدار من صفحة العقود** (حقل `use_mock` في عقد Telr) — وهو ما تصفه فقرة (أ) من
  القسم 15. عليه بُنيت المرحلة واختُبرت كلها، لأن بيانات Sandbox لم تكن متوفرة. **ممنوع في
  الإنتاج**: مزودٌ يقول «دُفع» بلا مال بابٌ لا يُترك مفتوحاً حيث مالٌ حقيقي.
- **أسلاك Telr في ملف واحد** (`services/card_gateway/telr.py`) وكلُّ ما عداه يتكلم واجهةً موحدة
  (`base.CardGateway`). ولمّا لم يُسلَّم رابط التوثيق الرسمي، ثلاثة تفاصيل فيه **مبنيّة على أفضل
  قراءةٍ للواجهة العامة وتحتاج مطابقةً قبل أول تشغيل حقيقي**: ترتيب حقول توقيع الإشعار
  (`TRAN_CHECK_FIELDS`)، وحقول الخصم على بطاقة محفوظة (`SAVED_CARD_*`)، وموضع رمز البطاقة في جواب
  `check`. وخطؤها لا يحرّك مالاً خطأً: توقيعٌ لا يطابق يرفض الإشعار بـ 400 ظاهرٍ في السجل، ويبقى
  مسار الاستعلام يسوّي؛ وخصمٌ فاشل يرتدّ 502 قبل أي قيد.
- **لا انتهاء آلي للطلب المعلّق في هذه المرحلة.** المزود يُنهي طلباته غير المدفوعة بنفسه، وأول
  استعلامٍ بعدها يُسقط الدفعة. مهمة Celery دورية تستعلم عن الطلبات المعلّقة تأتي في **المرحلة 7**.

### المحفظة
- رصيد كافٍ → خصم فوري + `ride_earning` للكبتن
- رصيد جزئي → **دفع مختلط**: خصم المتاح + الباقي كاش (صفا payments)
- إلغاء بعد دفع محفظة → `refund` فوري آلي

### قيود التنفيذ على قيود الدفتر (المرحلة 6-أ)

- **`ride_earning` بكامل المبلغ ثم `commission` خصماً منفصلاً**، لا قيدَ واحداً صافياً. الصافي واحد في
  الحالتين، لكن القسم 9 يكتب رصيد الكبتن «أرباح − عمولات» فيقرأ حرفياً بهذا الشكل، وتبقى العمولة سطراً
  ظاهراً في الكشف. بقيدٍ واحد صافٍ لا تظهر عمولةُ الرحلات النقدية إلا وحدَها (لأن لا `ride_earning` لها
  تُطرح منه) فيصير الكشفان مختلفين لنفس النسبة.
- **النسبة مجمّدة والنطاق يُقرأ لحظة الدفع.** `commission_percent_at_ride` لا تُقرأ من الإعدادات أبداً
  (القسم 4)، أما `applies_to` فلا يمكن تجميده أصلاً: يتعلق بقناة الدفع وهي لا تُعرف قبل شاشة الدفع.
- **عمولة الرحلة النقدية تُخصم من محفظة الكبتن** حين يكون النطاق `all_rides`: القسم 9 ينفي أثر الكاش
  على الرصيد بوصفه **أرباحاً**، لا بوصفه إعفاءً من عمولةٍ لا قناة تحصيل أخرى لها. ومحفظةٌ فارغة تعني
  **رفضَ تأكيد التحصيل** برسالة صريحة لا خصماً على المكشوف (القسم 4). قيدٌ تشغيلي على من يرفع النطاق
  إلى `all_rides`: على الكباتن رصيدٌ يغطي عمولاتهم.
- **«إلغاء بعد دفع محفظة → refund آلي» لا مسار له في 6-أ**، وليس نقصاً في التنفيذ: الدفع يقع بعد
  `completed`، ولا إلغاء بعد `in_progress` (القسم 5)، فلا تتقاطع الحالتان. الاسترداد موجود **إجراءً
  إدارياً** على دفعةٍ مرّ مالها بالمنصة، بقيدين متقابلين (`refund` للراكب و`adjustment` مضاد على الكبتن
  بصافي ما قُيّد له) لا بمحو قيد. يصير الاسترداد الآلي مسارَ عملٍ حقيقياً في 6-ب مع حجز البطاقة المسبق.
- **الكاش وكليك لا يُستردّان من المنصة**: مالهما لم يدخلها فلا تملك ردّه (القسم 9).

### 6.5 البقشيش (المرحلة 12-و)

> **قرارُ المالك (2026-08-12)**: **يموّله الراكب لا الشركة** — فلا سياسةَ مالٍ تنتظر — و**الكبتن
> يقبضه كاملاً بلا عمولة**.

**البقشيشُ ليس أجرة، وكلُّ ما يلي يتبع هذه الجملة.** لا يدخل `final_fare`، ولا وعاءَ العمولة، ولا
مجموعَ «ما لا يُدفع مرتين».

**ولا يكون صفَّ `payments` أبداً.** «لا تُدفع الرحلةُ مرتين» في القسم 6 هو مجموعُ
`OWING_PAYMENT_STATUSES` على الرحلة مقابل `final_fare`؛ فصفُّ بقشيشٍ هناك يجعل رحلةً مدفوعةً بالكامل
تبدو **مدفوعةً أكثر من أجرتها**، أو — أسوأ — يجعل بقشيشاً غيرَ مؤكَّدٍ يُقرأ ديناً على الراكب. فله
جدولُه: `tips`.

#### القناة: المحفظة وحدها في هذه المرحلة

**وهذا تضييقٌ لجواب المالك ومعه سببُه** (أجاز «المحفظة أو البطاقة»): بقشيشُ نصفِ دينار عبر بوابة
بطاقات **يكلّف في رسوم المزوّد أكثرَ مما يجمع** — فتدفع الشركةُ لتوصيل هديةٍ من الراكب إلى الكبتن،
ويلزمه فوق ذلك غرضٌ جديد في `provider_orders` ومسارُ webhook وحكايةُ استردادٍ لمبلغٍ أصغر من رسمه.
فالمحفظةُ أولاً، والبطاقةُ إضافةٌ لاحقةٌ إن قرر المالك أن الرسم مقبول — **ولا تُبنى مناصفةً**.

- **وحيث لا محفظة لا بقشيش**: `wallet_enabled` مطفأً في الدولة، أو رصيدٌ لا يكفي، يعني ألّا تظهر
  الأزرارُ أصلاً — لا زرَّ معطَّلاً يقول «رصيدك لا يكفي» بعد أن رفعَ توقّعَ الكبتن.
- **وبقشيشُ اليد لا يُسجَّل بحكم التعريف**: الكاشُ لا تراه المنصة، وهو نفسُ منطق
  `DIRECTLY_COLLECTED_METHODS` — ما لا تشهده المنصةُ لا تُقيّده.
- **وكليك مستثناةٌ عمداً**: تأكيدُها فعلٌ بشريٌّ من الكبتن بمهلةٍ مجمَّدةٍ وكنسٍ ونزاعٍ (القسم 6.2).
  إجراءُ كلِّ ذلك على نصف دينارٍ يفتح طابورَ دعمٍ لا يُغطي كلفتَه، ويجعل هديةً سبباً لنزاع.

#### القيدان في الدفتر

نوعان جديدان يقابلان زوجَ الأجرة حرفياً: **`tip_payment`** خصماً على الراكب و**`tip`** إضافةً
للكبتن، ويُضافان إلى `CREDIT_TYPES`/`DEBIT_TYPES` — ومعهما يُعاد بناءُ قيد CHECK الذي يشتق إشارةَ
المبلغ من نوعه (المرحلة 5).

**ولا قيدَ عمولةٍ يقابلهما، ولا يدخل البقشيشُ نطاقَ `commission_settings.applies_to` بحال.** وهو
لذلك **الدخلُ الوحيد للكبتن الذي لا عمولةَ عليه**، فيظهر **سطراً مستقلاً** في شاشة أرباحه: مبلغٌ
يُضاف إلى «ما دخل المحفظة» بلا أن يزيد «ما خرج عمولةً» يجعل الرقمين لا يتّسقان لمن يجمعهما بيده،
والسطرُ هو ما يفسّر الفرق (`services/earnings.py`، القسم 12/7).

#### المبالغ والحدود

- **ثلاثةُ مبالغ مقترحةٍ per-country** على `payment_settings`: `tip_preset_small` و
  `tip_preset_medium` وسقفٌ `tip_max`. لأن نصفَ دينارٍ أردنيٍّ ليس نصفَ دينارٍ ليبيّ، ورقمٌ مكتوبٌ في
  التطبيق يخالف السوقَ الآخر يومَ إطلاقه.
- **والصفرُ يعني «لم يُضبط» فيُخفي الميزة** — لا «بقشيشاً مقداره صفر»؛ نفسُ قاعدة
  `wallet_settings.transfer_*_limit`.
- **ولا مبلغَ حرٌّ يكتبه الراكب** في هذه المرحلة: زرّان ومبلغٌ يُكتب بالإصبع على شاشةِ هاتفٍ في سيارة
  هو الطريقُ إلى بقشيشٍ من ثلاثة أصفارٍ زائدة، ومخرجُه دعمٌ يشرح لماذا خُصم. والسقفُ حارسٌ ثانٍ في
  القاعدة على كل حال.

#### من يجوز له، ومتى

- **الراكبُ صاحبُ الرحلة وحده**، وعلى رحلةٍ `completed` وحدها، **ومرةً واحدة**: `UNIQUE (ride_id)` —
  والحارسُ في القاعدة لا في فحصٍ سابقٍ يمكن أن يُسبَق، كما في `ratings`.
- **ونافذةٌ اثنتان وسبعون ساعة** ثابتةٌ في الخدمة لا إعداداً: البقشيشُ قريبٌ من الرحلة أو لا يكون —
  وخصمٌ من محفظةٍ بعد ثلاثة أسابيع يفاجئ صاحبَها ولا يذكّره بشيء.
- **وشرطُ «أربع نجوم أو أكثر» تضييقٌ في الواجهة لا قيدٌ في الخلفية** (نفسُ تمييز الخدمة النسائية):
  الخلفيةُ تقبل البقشيشَ على رحلةٍ مكتملةٍ يملكها صاحبُها أيّاً كان تقييمُه. فمن قيّم ثلاثاً وأراد أن
  يشكر الكبتن على حمل حقيبةٍ لا يُردّ بقاعدةٍ وُضعت لكي **لا نسأل في لحظةٍ سيئة**؛ وربطُ المال
  بتقييمٍ يمكن تغييره يجعل المالَ تابعاً لرأيٍ متغيّر.

#### الحالة والأقفال

- **ولا عمودَ `status` ولا عمودَ `method` أصلاً** — وهذا تصحيحٌ ظهر عند البناء لا في المواصفة
  الأولى: قناةُ المحفظة تستقر في **نفس المعاملة**، فلو فشل الخصمُ انسحبت المعاملةُ كلُّها ولم يبق
  صفٌّ — أي أن قيمة `failed` **لا تُكتب أبداً**، وحالةٌ لا تُكتب هي بعينها `awaiting_confirmation`
  التي كسرت تأكيدَ الكاش في المرحلة 10. فوجودُ الصفِّ **هو** أن المال تحرّك، كما في
  `driver_subscriptions` («لا صفَّ لاشتراكٍ لم يصل ماله»). ويومَ تُضاف البطاقةُ يُضاف عمودُ
  `method` بافتراضٍ `wallet` في ترحيلته فتُقرأ الصفوفُ القديمة صحيحةً — وذاك أصدقُ من عمودٍ اليومَ
  بقيمةٍ واحدةٍ ممكنة.
- **ترتيبُ الأقفال كما هو**: صفُّ الرحلة أولاً (فيصير فحصُ `completed` والملكية ذرّياً)، ثم قفلا
  المحفظتين بترتيب UUID المرتَّب (`wallet._lock_wallets`) — راكبٌ وكبتنٌ في عمليةٍ واحدة.
- **ومفتاحُ التكرار مشتقٌّ من صفّ البقشيش** (`tip:{id}`) كما في `topup:{id}`، **ولا يقوم مقام القفل**:
  ضغطتان متزامنتان تُفشل إحداهما بقيد `UNIQUE (ride_id)` بجوابٍ مفهوم، والقفلُ يحرس أن يُقرأ رصيدٌ
  واحدٌ مرتين. واختبارُ تزامنٍ يفشل بحذف القفل شرطٌ لإنهاء المرحلة (القسم 14).
- **ولا نزاعَ على بقشيش ولا مسارَ استرداد**: تصحيحُه قيدُ `adjustment` مضادٌّ من اللوحة كأيِّ تصحيحٍ في
  دفترٍ لا يُعدَّل (المرحلة 5).

#### الإشعار والمفتاح

- **الكبتن يُخبَر** بحدثٍ في `services/notifications.py`، وحمولتُه **قيمٌ خام** (`amount`,
  `currency`, `ride_id`) لا جملةٌ مصوغة ولا رقمٌ منسَّق (القسم 10) — والصفُّ في صندوقه أثرُ الحدث.
- **ومفتاحٌ per-country `tips_enabled`**، ميزةٌ لا حارس، **مطفأٌ افتراضاً ومكتوبٌ صريحاً في البذر**.
  ويحكم **ما يُرسله التطبيق كما يحكم ما يرسمه**: مطفأً لا تظهر الأزرار ولا يُقبل النداء — وهي القاعدةُ
  التي تعلّمناها من `ride_gender_preference` (تطبيقٌ يخفي زرّاً ويبقى يرسل قيمةً يرتدّ بها الطلب).


### 6.6 الكوبونات والخصومات (المرحلة 12-ز)

> **قرارُ المالك (2026-08-12)**: تُبنى، **بشرطين**: أن تسمّي اللوحةُ **من يتحمّل الخصم** (الشركةُ
> دائماً في هذه المرحلة)، وأن يكون **لكل كوبونٍ سقفُ ميزانية**.

#### الخصمُ صفُّ دفعةٍ بقناة `promo` — وهذا هو القرار الذي يتفرّع عنه كلُّ ما بعده

الطريقُ البديهي أن يُنقص الخصمُ `final_fare`. **وهو خطأٌ يكلّف الكبتن**: `ride_earning` و
`commission` كلاهما يُحسب من `payment.amount` (`services/payments.py`)، فخصمٌ ينقص ما يدفعه الراكب
ينقص ما يقبضه الكبتن — والمالكُ قرر أن **الشركة** تتحمّله لا السائق.

فالخصمُ **دفعةٌ ثانية على الرحلة** بقناةٍ اسمُها `promo`، تُنشئها المنصةُ وتؤكّدها بنفسها لحظةَ
الإنهاء. و«الدفعُ المختلط رحلةٌ بصفَّين» آليةٌ قائمةٌ منذ القسم 6، فتتولّى الحسابَ كلَّه بلا قاعدةٍ
مالية جديدة:

- `outstanding = final_fare − Σ(الدفعات القائمة)` — فيصير ما على الراكب **تلقائياً** الأجرةَ ناقصَ
  الخصم، بلا لمس `outstanding_amount` ولا `_payable_ride`.
- `ride_earning` يُجمع من الصفَّين فيساوي **الأجرة كاملةً**: الكبتن يقبض ما تستحقه رحلتُه، والخصمُ
  ليس شأنه.
- والعمولةُ تُحسب من الصفَّين أيضاً، فتبقى **كما لو لم يكن كوبون**: صافي الكبتن لا يتغيّر بوجود
  الخصم ولا بغيابه. وهذا هو المعنى العمليُّ لـ«الشركة تتحمّله».
- و`promo` **ليست في أيٍّ من التصنيفين** — كالبطاقة حرفياً (`models/payment.py`): فليست في
  `DIRECTLY_COLLECTED_METHODS` كي يُكتب للكبتن `ride_earning`، وليست في `WALLET_FUNDED_METHODS` كي
  **لا يُخصم من محفظة الراكب شيء**. ولو وُضعت في الثاني لدفع الراكبُ الخصمَ من رصيده — أي لانقلب
  الكوبونُ إلى ضريبةٍ عليه، وهو نقيضُ الغرض كلِّه. ومالُ الخصم لم يخرج من محفظةِ أحد.
- **وتُحسب `promo` قناةً تمرّ بالمنصة** في نطاق `cashless_rides`: مالُها من الشركة إلى الكبتن،
  فإخراجُها من وعاء العمولة يجعل الكوبونَ يخفّض عمولةَ الرحلة أيضاً — أي أن الشركة تدفع الخصمَ مرتين.

**وأثرُه الجانبي أنه يفتح «الرحلة الأولى مجاناً» بلا بناءٍ إضافي**: كوبونٌ يغطي الأجرةَ كلَّها يعني
`outstanding = 0`، و`create_payment` يرفض حينها بـ`ride_already_paid` — فلا شاشةَ دفعٍ ولا صفَّ
للراكب، والإيصالُ يقول «مدفوعة بكوبون». ولو كان الخصمُ عموداً على الرحلة لاحتاجت الرحلةُ المجانية
باباً ثانياً يكتب أرباحَ الكبتن عند الإنهاء — وذاك ما يمنعه القسم 6: **المال يتحرك عند `confirmed`
وحدها**.

#### ما يُجمَّد على الرحلة: القاعدةُ لا المبلغ

أربعةُ حقولٍ (كأربعةِ حقول الانتظار في 12-ب): `promo_code_id` و`promo_type_at_ride` و
`promo_value_at_ride` و`promo_cap_at_ride`. **ولا حقلَ للمبلغ**: المبلغُ يعتمد على `final_fare` الذي
لا يُعرف قبل الإنهاء (يُعاد حسابُه على المسافة الفعلية، القسم 5.7)، فيُحسب مرةً واحدةً عند الإنهاء
**ويسكن في صفِّ دفعة `promo`** — بيتٌ واحدٌ لا بيتان، وقيمةٌ لها بيتان تفترقان (درسُ
`waited_minutes` في 12-ب).

**والتجميدُ لأن تعديلَ الكوبون في اللوحة لا يجوز أن يمسّ رحلةً جارية**: راكبٌ رأى «خصم مُطبَّق ١٠٪»
قبل أن يطلب لا يُحاسب على ٥٪ لأن أحداً عدّل الرمزَ بينهما — نفسُ منطق `commission_percent_at_ride`
و`cliq_confirmation_hours`.

#### الحساب وحدوده

- **نوعان**: `percent` (بسقفٍ أعلى `promo_cap_at_ride` — بغيره يبتلع كوبونُ ٥٠٪ رحلةً طويلة) و
  `fixed` (مبلغٌ مقطوع).
- **الترتيب**: تُحسب الأجرةُ كاملةً بقواعدها (ومنها `minimum_fare`) **ثم** يُطبَّق الخصم. فالكوبونُ
  قد ينزل بالمدفوع تحت الحدّ الأدنى — وهذا هو الغرض منه، لا خللٌ فيه.
- **ولا يُخصم من رسم الإلغاء**: الرسمُ عقوبةٌ لا أجرة، وخصمُها يجعل الإلغاء أرخصَ لمن يملك كوبوناً.
- **ولا يزيد الخصمُ على الأجرة**: `discount = min(value_or_percent, final_fare)`.

#### الميزانية: سقفٌ يحدّ التطبيقَ الجديد لا رحلةً تحمل كوبوناً

- **ولا عمودَ `budget_spent`**: المصروفُ **مجموعُ دفعات `promo` على رحلات هذا الرمز** — نفسُ قاعدة
  «لا عمود رصيد؛ الرصيد مجموعُ الدفتر» (القسم 4). وعمودٌ يُراكم رقماً موجوداً أصلاً يفترق عنه يوماً.
- **والسقفُ يُفحص عند تطبيق الرمز** (لحظةَ الطلب)، لا عند الإنهاء: المبلغُ لا يُعرف قبل الإنهاء،
  ورفضٌ عند الإنهاء يعني محاسبةَ راكبٍ بسعرٍ غير الذي أظهرته الشاشة. فالسقفُ **يمنع تطبيقاً جديداً
  حين نفدت الميزانية**، ورحلةٌ تحمل الكوبونَ سلفاً تُنهى بخصمها كاملاً.
- **ويُقاس بالالتزام لا بالمصروف** — وهذا تصحيحٌ كشفه اختبارُ التزامن ولم تره الصياغةُ الأولى:
  المصروفُ مجموعُ دفعات `promo` **المؤكَّدة**، وهي لا تُنشأ إلا عند الإنهاء. فسقفٌ يُقاس به وحده
  تمرّ من تحته مئةُ رحلةٍ في ثانيةٍ قبل أن تُنشأ أولُ دفعة، وتتجاوز الحملةُ ميزانيتَها مئةَ ضعف.
  فالالتزامُ = دفعاتٌ مؤكَّدة + **خصمُ كل رحلةٍ جاريةٍ تحمل الرمز مقيساً على تقديرها** + خصمُ الرحلة
  التي تُطلب الآن. والتقديرُ لا الأجرةُ النهائية لأنها مجهولةٌ بعد، وهو تقريبٌ **في الاتجاه الصحيح**:
  يحجز أكثرَ قليلاً لا أقل.
- **فالتجاوزُ ممكنٌ ومحدود** (الميزانيةُ الباقية + أقصى خصمٍ × الرحلات الجارية)، **ويُعرض في اللوحة
  كما هو** لا مقصوصاً: رقمٌ يُخفي تجاوزاً يجعل المشرفَ يظن السقفَ صارماً. وهو نفسُ سلوك
  `multi_stop_enabled`: الإطفاءُ يمنع الطلباتَ الجديدة ولا يقطع رحلةً جارية.
- **والقفل**: صفُّ `promo_codes` يُقفل (`with_for_update`) قبل جمعِ المصروف وقبل تثبيت الرمز على
  الرحلة. وموضعُه في ترتيب الأقفال: **بعد صفِّ الرحلة وقبل صفِّ الدفعة** — فلا ترتيبَ ثانٍ يُخترع.

#### من يجوز له، وكم مرة

- **حدٌّ لكل مستخدم** (`per_user_limit`، افتراضُه واحد) **وحدٌّ إجمالي** (`total_usage_limit`،
  و`null` يعني بلا حدٍّ غير الميزانية).
- **والعدُّ يشمل الرحلاتَ الجارية لا المكتملةَ وحدها**: من عدَّ المكتملةَ فقط يفتح البابَ لمن يطلب
  ثلاث رحلاتٍ في اللحظة نفسها بنفس الرمز. والعدُّ تحت قفل صفِّ الرمز، وإلا مرّ طلبان متزامنان
  بعدٍّ واحد.
- **ولا صفَّ «استعمالات» مستقلاً**: الرحلةُ تحمل الرمزَ والدفعةُ تحمل المبلغ، فالعدُّ والمصروفُ
  استعلامان على ما هو مكتوبٌ أصلاً — وجدولٌ ثالث يعني ثلاثةَ مواضعَ لحقيقةٍ واحدة.

#### من يتحمّل الخصم

**تقولها اللوحةُ نصّاً ولا يخزّنها عمود**، وهذا قراءةٌ لشرط المالك («تحدد اللوحةُ من يتحمّل
الخصم — الشركةُ دائماً في هذه المرحلة»): القيمةُ واحدةٌ لا تحتمل غيرها اليوم، وعمودٌ بقيمةٍ واحدة
ممكنة هو نفسُ ما حُذف من جدول البقشيش (`status` الذي لا يُكتب أبداً). ويومَ يظهر متحمّلٌ ثانٍ —
سائقٌ يمنح خصماً أو شريكٌ يرعى حملة — يُضاف العمودُ في ترحيلته بافتراضِ `company` فتُقرأ الصفوفُ
القديمة صحيحةً. **وتقريرُ الكلفة لا ينتظر ذلك**: مصروفُ كل رمزٍ مجموعُ دفعات `promo` عليه.

#### المفتاح والشاشات

- **مفتاحٌ per-country `promo_codes_enabled`** — ميزةٌ لا حارس، مطفأٌ افتراضاً ومكتوبٌ صريحاً في
  البذر، **ويحكم ما يُرسله التطبيق كما يحكم ما يرسمه**.
- **الراكب**: زرُّ «كوبون» في ورقة التأكيد، وحقلُ رمزٍ وزرُّ «تطبيق»، ثم «خصم مُطبَّق» بلون `--ok`
  وأسعارُ الفئات مخصومة. ومسارُ تحقّقٍ لا يستهلك شيئاً (`POST /promo/validate`) يُرجع الخصمَ
  المتوقَّع على التقدير — فالشاشةُ لا تحسب خصماً بنفسها (القسم 14).
- **وصفُّ «خصم الكوبون» في الإيصال** هو دفعةُ `promo` نفسُها، بتسميتها العربية — لا سطرٌ مصنوعٌ في
  الواجهة.
- **اللوحة**: قسمُ «العروض والحملات» يكتمل بجدول رموز — رمزٌ ونوعُ خصمٍ وقيمةٌ وسقفٌ وميزانيةٌ
  ومصروفٌ وحدودُ استعمالٍ ومدةٌ ودولة — ومعه **جملةُ من يتحمّل** و**مصروفٌ قد يتجاوز السقف** ظاهراً.
  وكلُّ كتابةٍ تدخل سجل التدقيق.


---

## 7. محفظة الراكب

- **شحن فقط — لا سحب نهائياً** (لا يُبنى أي مسار سحب للراكب)
- قنوات الشحن:
  - Telr (فوري آلي)
  - كليك: تحويل على alias الشركة + إدخال المرجع → تأكيد الإدارة من اللوحة
  - كاش: موظف/نقطة معتمدة تشحن من اللوحة
- **شحن Telr لا يمر بجدول طلبات الشحن** (المرحلة 6-ب): `wallet_topup_requests` بيتُ ما ينتظر
  تأكيداً بشرياً، والبطاقة يشهد عليها المزود. فالمسار `POST /wallet/me/topups/card` يفتح صفَّ
  `provider_orders` ويعيد رابط صفحة الدفع، وأثرُه الوحيد قيدُ `topup` عند تأكيد المزود. ولذلك
  مسارٌ خاص لا `POST /wallet/me/topups`: ذاك يعيد صفَّ طلبٍ معلّق وهذا يعيد رابطاً، وردّان
  مختلفان لعمليتين مختلفتين لا يُدمجان في مسارٍ بحقولٍ فارغة في كل حالة
- **تجميدُ المحفظة يمنع فتح الشحن لا إكمالَه:** طلبٌ بدأ قبل التجميد ووصل ماله فعلاً يُقيَّد،
  والمنعُ يبقى على الإنفاق. مالٌ دخل لا يُحرق لأن حالةَ الحساب تغيّرت بين خطوتين
- **تحويل P2P بين محافظ الركاب** — خلف `wallet_transfer_enabled`:
  - إدخال رقم هاتف المستلم → عرض الاسم للتأكيد → تنفيذ ذرّي (قيدا `transfer_out`/`transfer_in`)
  - حدود يومية وشهرية تحددها الإدارة
  - راكب→راكب فقط في هذه المرحلة
  - سجل كامل في اللوحة + إمكانية تجميد محفظة

---

## 8. اشتراكات الكباتن

- **لا اشتراك ساري = لا رحلات.** الفحص في الخلفية عند التوزيع (وليس في الواجهة فقط)
- الدفع **مقدماً** بأي قناة: كاش (تأكيد إدارة)، كليك (تأكيد إدارة)، رصيد المحفظة (فوري)، بطاقة Telr (فوري)
- تطبيق الكبتن: عدّاد الأيام المتبقية + إشعار قبل الانتهاء بـ 24 ساعة + تجديد بضغطة
- Celery task دورية: تعليم الاشتراكات المنتهية + إخراج الكبتن من التوزيع
- العمولة: صفر افتراضياً. لو فُعّلت من اللوحة تُطبق على الرحلات **الجديدة فقط** (عبر `commission_percent_at_ride`)

نُفِّذ في **المرحلة 7**. قرارات التنفيذ التي لم يحسمها النص أعلاه:

- **مدد الخطط: 1 / 7 / 30 يوماً.** الشهر ثلاثون يوماً لا شهرٌ تقويمي: التقويمي يعطي مشتري الأول من
  فبراير ثمانيةً وعشرين يوماً بنفس الثمن، وليس في النظام مِنطقةٌ زمنية لكل دولة يُبنى عليها حدُّ يومٍ
  أصلاً. الأرقام ثوابت كود لا إعدادات — «الخطة الشهرية» في القسم 4 هي مدتُها
- **التجديد المبكر يُكدَّس لا يُحرَق:** الاشتراك الجديد يبدأ من `expires_at` السابق لا من الآن، فمن
  جدّد قبل يومٍ من الانتهاء لا يخسر يومه. ولذلك «متى ينتهي اشتراكي» = **أقصى** `expires_at` بين
  صفوفه لا أحدثُها، وعدّادُ الأيام وتنبيهُ الأربع والعشرين ساعة يُبنيان عليه: تنبيهُ من اشترى شهرين
  بأن اشتراكه ينتهي غداً يدفعه لدفعٍ لا يحتاجه
- **السريان يُقرأ بالساعة لا بعمود الحالة**: شرط التوزيع `status='active' AND starts_at <= now() AND
  expires_at > now()`. المهمة الدورية تمرّ كل خمس دقائق، وبين دورتين يبقى صفٌّ انقضى وقتُه محمولاً
  على `active` — ولو قرأ التوزيعُ العمودَ وحده لأخذ كبتنٌ رحلةً بلا اشتراك. غرضُ التعليم أن يقول
  الجدولُ ما تقوله الساعة فتُقرأ التقارير بلا حساب، لا أن يحرس الأهلية
- **القناتان اليدويتان تُسجَّلان من اللوحة بعد قبض المال، ولا جدولَ طلباتٍ لهما**: لا حالة «بانتظار
  الدفع» في `driver_subscriptions` كما لا يتغيّر رصيدٌ قبل تأكيد شحنته (القسم 7). والكبتن الذي يريد
  كليكاً بلا انتظارِ موظفٍ يشحن محفظته بكليك (القسم 7) ثم يشتري منها فوراً — فلا مسار مفقود
- **لا يُباع الاشتراك لحسابٍ غير معتمد**: عدُّ أيامه يبدأ فوراً، فبيعُه لمن ينتظر الموافقة يحرق أيامه
  في الانتظار. وهو ترتيب القسم 16.13 نفسه: تسجيل ← موافقة ← اشتراك
- **الكنس لا يُخرج كبتناً في رحلة**: إسقاط حضوره وسط `in_progress` يقطع خريطة راكبه ويوقظ تنبيه
  «انقطع الكبتن» (القسم 5) على من لم ينقطع. ولا رحلة جديدة تصله على أي حال — الأهلية تُقرأ بالساعة
- **تنبيه الأربع والعشرين ساعة أثرُه مفتاحُ Redis لا عمودٌ في الجدول**: حدثٌ عابر لا سجلٌّ يُحاسَب
  عليه، وبغير أثرٍ يتكرر التنبيه في كل دورة كنس حتى ينتهي الاشتراك. وقناته `ws:user:{id}` حتى تصل
  إشعاراتُ Push في المرحلة 8 (القسم 15/أ)

---

### 8.1 عروضُ الاشتراكات — وما يحرس حدَّ استعمالها (البند ٥٤)

**حارسُ «مرةٍ واحدةٍ لكل عرض» قفلان معاً: قفلُ صفّ الكبتن**
(`subscriptions._locked_driver`) **وقفلُ صفّ العرض** (`offers.resolve(for_update=True)`).
**وأيُّهما كفى**: بحذف أحدهما يبقى الحدُّ قائماً، **ويسقط بحذفهما معاً** — قِيس على المسارين
(المحفظة واليدويّ) فخرج `['3.000', '3.000']`: عرضٌ حدُّه واحدٌ مُنح مرتين.

**والقفلُ الاستشاريُّ للمحفظة لا يحرس هذا الحدّ**، وهذه واقعةٌ مقيسةٌ لا استنتاج: بحذف القفلين
وإبقائه وحدَه سقط الحدُّ كذلك. **والسببُ الترتيبُ لا القفل** — `offers.resolve` تقع **قبل**
`wallet.record` في `purchase_with_wallet`، فيقرأ المساران عدَّ الاستعمالات قبل أن يطلب أيٌّ منهما
قفلَ المحفظة. **وقفلٌ يأتي بعد القراءة لا يُسلسِلها.**

> ⚠️ **والشرطُ الذي يُبطل هذا التحليل مكتوبٌ معه**: إن تغيّر الترتيبُ يوماً فصار طلبُ قفلِ المحفظة
> **قبل** قراءة العدّ، **يُعاد القياسُ ولا يُنقل هذا السطر كما هو** — فالخلاصةُ هنا خاصّةٌ بترتيبٍ
> بعينه، لا بطبيعة الأقفال.

**والقياسُ نفسُه يُعاد بالاختبارات الثلاثة** في `tests/test_subscription_offers.py`:
`test_two_simultaneous_buyers_cannot_exceed_the_budget` (الميزانية، يسقط بحذف قفل العرض وحدَه) ·
`test_two_simultaneous_manual_records_consume_one_use_not_two` (الحدُّ على المسار اليدويّ) ·
`test_the_cap_holds_on_the_wallet_path_too` (الحدُّ على مسار المحفظة). **وتداخلُها مُرتَّبٌ عمداً**
لا متروكٌ لـ`asyncio.gather` على مسار HTTP: أوّلُ نسختين مرّتا والقفلُ محذوف، لأن الأولَ ثبّت قبل
أن يبدأ الثاني.

---

## 9. محفظة الكبتن والسحب

- الرصيد = أرباح البطاقات/المحفظة − العمولات (إن وجدت) − الاشتراكات المدفوعة منها − السحوبات
- طلب سحب: حد أدنى (إعداد إدارة) → `pending` → المحاسب يحوّل (كليك على `cliq_alias` أو بنكي) → يسجل المرجع → `paid` → قيد `withdrawal`
  - **ومن التطبيق كليك وحدها** (المرحلة 10): لا حقلَ لبيانات حسابٍ بنكي على `drivers` ولا على
    `withdrawal_requests`، فطلبٌ بنكيٌّ من الكبتن طلبٌ بلا وجهةٍ يعرفها أحد. والقناة البنكية تبقى
    للإدارة حيث تُعرف الوجهة خارج الجدول، وأعمدتُها في `FUTURE-FEATURES.md` بند 41
- الرحلات النقدية وكليك **لا تؤثر على رصيد المحفظة** (الكبتن قبضها مباشرة)، لكنها **تُسجل في العمليات والكشوفات**: تظهر في شاشة "أرباحي" وكشف حساب الكبتن (من جدولي rides/payments) بوسم "مُحصَّل مباشرة"، وتدخل في تقارير الإدارة المالية — فيكون الكشف شاملاً لكل الدخل بغض النظر عن طريقة التحصيل

### 9.1 حوافزُ الإحالة (12-ح، ثم تعميمُها 2026-08-16)

> **تعميمُ 2026-08-16** — أربعةُ قراراتٍ للمالك، وتفصيلُها في
> `design/REFERRALS-GENERALIZATION.md` §12-أ بنصِّه:
>
> 1. **العلاوةُ النسائية علاوةٌ لا برنامجٌ ثالث** — تُضاف إلى `reward_amount` حين تكون المُحالةُ
>    **موثَّقةَ الجنس**، والمجموعُ **قيدٌ واحد**. ولو كانت برنامجاً بمبلغه لَدُفع صفرٌ لمن أحال
>    سائقةً ومئةٌ لمن أحال سائقاً حين لا يُحدَّد مبلغُها — أي ينقلب الحافزُ على غرضه بصمت.
>    **وشرطُ المالك**: لا تُحفظ سالبةً، ولا تُعرض في اللوحة **منفصلةً** عن الأساس بل بجانبه
>    بجملةٍ تقول إن المُحصَّل = الأساس + العلاوة — فمن يرى رقمين منفصلين قد يظنّ الثاني بديلاً.
> 2. **البرنامجُ يُختار من دور المُحال لا من دور المُحيل**: الغرضُ اكتسابُ حسابات، ومن جاء
>    بكبتنٍ جاء بكبتن أياً كان دورُه هو. **وشرطُه**: شاشةُ الإحالة عند الطرفين تشرح أن الرمز
>    يعمل مع الاثنين وأن المكافأة تختلف بحسب من يسجّل به.
> 3. **سقفٌ شهريٌّ يمنع الدفعَ لا الإسناد**، ويُقاس **بشهر إنشاء الإحالة** لا بشهر الدفع (وإلا
>    صار جدولَ صرفٍ لا سقفاً). **وشرطُه بحرفه**: الشاشةُ تقول السقفَ وما بقي منه **قبل أن يدعو**؛
>    ومن تجاوز تُسجَّل إحالتُه وتُنسب له ويُقال له صراحةً إنها فوق السقف ولن تُدفع —
>    **لا صمتَ ولا رقمٌ يختفي**.
> 4. **«اشتراكٌ» تعني «اشترى مرةً على الأقل»** لا «نشطٌ لحظةَ الدفع». وسببُه بنصِّه: **حقٌّ
>    اكتُسب لا يُمحى بمرور الزمن**، وقراءةُ «نشطٌ لحظة الدفع» تجعل الاستحقاقَ يرقص مع تقويم
>    الكبتن — تُدفع إن صادفت الدورةُ يومَ سريانه وتضيع إن صادفت يومَ انقطاعه. **والعملُ هو
>    الحكمُ لا التوقيت.**
>
> **والبنيةُ نقلُ طرفَي الإحالة من `drivers` إلى `users`** (`referrals`، ترحيلة `0038`)، لا
> عمودُ نوعٍ على الجدول القائم: صفُّ الراكب لا يوجد في `drivers` أصلاً، فعمودُ نوعٍ عليه يجعله
> جدولاً بعمودٍ يكذب. **ولا `referral_type` على صفِّ الإحالة**: النوعُ مشتقٌّ من `users.role`
> وهو ثابتٌ مدى عمر الحساب؛ وحيث يُكتب فعلاً هو **الإعدادات**، لأن الصفَّ هناك هو **البرنامج**.
> و`referral_settings` صار فريدُه `(country_code, referral_type)`.
>
> **والقفلُ الذي يحرس السقف هو قفلُ محفظة المُحيل، لا قفلُ صفِّ الإحالة**: هذا يحمي الصفَّ من
> دفعتين، ولا يحمي مُحيلاً من صفَّين مختلفَين يُدفعان معاً فيقرآن العدَّ نفسَه.
> `test_two_referrals_paid_at_once_cannot_pass_one_referrers_cap` يفشل بحذفه — **بعد** أن
> أُعيد بتشابكٍ صريح، إذ مرّ في صيغته الأولى بحذف القفل لأن `gather` وحدَه لم يُلقِ الدفعتين
> على العدّ نفسِه.

#### الأصلُ: حافزُ إحالة السائقات (المرحلة 12-ح)

> **قرارُ المالك (2026-08-12)**: تُبنى الآليةُ والتتبّع، ومبلغُ الحافز حقلٌ في الإعدادات قيمتُه
> صفرٌ حتى يحدّده، وشرطُ الاستحقاق ٣ رحلاتٍ مكتملة. (`FUTURE-FEATURES` بند 46.)

**السببُ الذي يحدّد الشكل**: الخدمةُ النسائية (المرحلة 10-ج) محدودةٌ **بعدد السائقات لا بعدد
الطالبات**، ونصفُ قطرٍ أوسعُ علاجُ عرَضٍ لا علاجُ سبب. فالحافزُ أداةُ عرضٍ لا أداةُ تسويق.

**والرمزُ يُستهلك في التسجيل وحده.** `POST /auth/register` يقبل `referral_code` اختيارياً، ولا
يوجد أي مسارٍ يُسند إحالةً بعد ذلك. إسنادٌ رجعيٌّ («سجّلتُ الأسبوع الماضي، أضف رمز صديقتي») هو
بابُ التلاعب الوحيد الذي لا يمكن إغلاقه بعد فتحه: من يملك حسابين يُحيل نفسه متى شاء.

**وشروطُ الاستحقاق أربعة، وكلُّها تُقرأ حيّةً حتى لحظة الدفع:**
1. المفتاحُ `driver_referrals_enabled` مشتعلٌ في دولة **المُحيلة** (مالُ المكافأة يدخل محفظتَها)
2. و`reward_amount` أكبرُ من صفرٍ في تلك الدولة
3. و**المُحالةُ `approved`**: مستنداتُها راجعها إنسانٌ، وهو الحارسُ الوحيد الذي يقف في وجه
   الحسابات الوهمية فعلاً — عددُ الرحلات وحده يُشترى بثلاث رحلاتٍ من الحساب نفسه
4. **وأكملت `required_rides` رحلة** (`rides.status = completed` وهي كبتنتُها)

**وشرطٌ خامسٌ هو صلبُ البند**: **المُحالةُ سائقةٌ بجنسٍ مثبَّت** — `gender = female` **و**
`gender_verified_at IS NOT NULL`، وهي نفسُ القراءةِ التي يعتمدها التوفيقُ في المرحلة 10-ج: بلا
الوسم «سائقة» كلمةٌ يكتبها أحدٌ عن نفسه، ومكافأةٌ تُدفع عليها مكافأةٌ على كلمة.

**والمُحيلةُ أيُّ كبتنٍ كان — وهذا انحرافٌ عن نصِّ البند** («مكافأةٌ للسائقة التي تُحيل سائقةً») **له
سببُه**: الهدفُ زيادةُ عدد السائقات، وكبتنٌ يُسجّل أختَه يحقّقه تماماً كما تحقّقه سائقة. وقصرُ
المُحيل على النساء يقصّ نصفَ منافذ الوصول بلا أن يحمي شيئاً — إذ الحارسُ على الجنس مفروضٌ على
**المُحالة** حيث يُهمّ. **وأقرّه المالك (2026-08-13)** بنصِّه: «الحارس على المُحالة حيث يهمّ».

**ولا يُقاس الحافزُ بمفتاح `women_service_enabled`.** ذاك مطفأٌ في الدولتين حتى يُصفّى متراكمُ
إثباتِ الجنس، والحافزُ هو ما يبني العرضَ الذي تنتظره الخدمةُ لتُشعَل — فربطُه بها يجعل الخدمةَ
تنتظر عرضاً لا سبيلَ لبنائه. فله مفتاحُه: `driver_referrals_enabled`، per-country، مطفأٌ
افتراضاً ومكتوبٌ صراحةً في الـseed.

**والدفعُ مهمةٌ دوريةٌ لا فعلٌ على مسار الرحلة.** `tasks/referrals.py` كلَّ عشر دقائق: تقرأ
الإحالاتَ غيرَ المكافأة، وتقيس الشروطَ، وتكتب `referral_bonus` في دفتر المحفظة بمفتاح تكرارٍ
`referral:{id}` تحت قفلِ صفِّ الإحالة. وليست على `complete_ride` لسببين: عدُّ رحلاتٍ في مسار
الإنهاء عملٌ لا يقرؤه أحد في تلك اللحظة، **وأخطرُ منه** أن فشلَ كتابةِ مكافأةٍ لا يجوز أن يُفشل
إنهاءَ رحلة — ولا يجوز في المقابل أن يُبتلع صامتاً كما يُبتلع فشلُ التقاط نقطة، فالمالُ ليس أثراً.

- `referral_bonus` **نوعُ قيدٍ جديدٌ دائنٌ** في `wallet_transactions` (القسم 4)، بلا قيدٍ مدينٍ
  مقابل: الشركةُ تتحمّله كما تتحمّل خصمَ الكوبون، ووعاءُ الشركة ليس محفظةً في هذا النظام
- **ولا عمولةَ عليه ولا يدخل `final_fare`**: ليس أجرةً ولا أرباحَ رحلة
- وفي شاشة أرباح الكبتن يظهر **رقماً منفصلاً**، لا مضموماً إلى أرباح الرحلات: كبتنٌ يقرأ أرباحَه
  ليعرف ما جنته سيارتُه، ومكافأةُ إحالةٍ داخلَ الرقم تجعله يظنّ يومَه أفضلَ مما كان

**والشاشة**: في تطبيق الكبتن قسمٌ في الحساب يعرض الرمزَ (لاتينياً بلا تحويل أرقام — يُقرأ ويُنطق
حرفاً حرفاً كاللوحة والمرجع) وعددَ من سجّل به، ولكلٍّ **أين وصلت**: «بانتظار اعتماد حسابها»،
«أكملت رحلةً من ٣»، «مكافأةٌ مدفوعة». **وحيث المبلغُ صفرٌ لا يُذكر مبلغٌ ولا يُوعَد به** — يُعرض
الرمزُ والعدُّ وحدهما، فوعدٌ بمالٍ لم يقرّره أحدٌ بعد أسوأُ من صمت. وفي اللوحة جدولُ الإحالات
بحالتها لكل صفٍّ ومجموعُ ما دُفع.

### 9.2 سلفُ الكباتن (البند ١٥)

> **قراراتُ المالك التسعة (2026-08-14)** في `design/DRIVER-ADVANCES.md` §٩، وأُجيبت كلُّها قبل
> أوّل سطرٍ بطلبه. وهذه هي **أولُ مرةٍ تُقرض فيها المنصّةُ أحداً**.

**والدَّينُ ليس رصيداً سالباً.** كلُّ ما بُني قبله مكتوبٌ على أن المحفظة لا تَدين — `balance_after
>= 0` قيدٌ في القاعدة نفسِها، ولا عمودَ رصيدٍ أصلاً. فرفعُ ذلك القيد كان سيُسقط حارساً يحمي **كلَّ**
مسارٍ ماليٍّ ليخدم ميزةً واحدة: **ثمنٌ لا يُدفع**. والشكلُ المعتمد: `driver_advances` جدولٌ مستقل،
وقيدان في الدفتر — `advance` دائنٌ عند الصرف و`advance_repayment` مدينٌ عند كل اقتطاع.
**والمتبقّي مطروحٌ من الدفتر لا عمودٌ على الصف**، كرصيد المحفظة تماماً.

**الأهليةُ شروطٌ بأسمائها لا نقاطُ «مصداقية»** (القرار ١): معتمد · اشتراكٌ أسبوعيٌّ أو شهريٌّ
**مضى** · رحلاتٌ مكتملةٌ فوق حدٍّ · تقييمٌ فوق حدٍّ · لا نزاعَ مفتوح · لا سلفةَ قائمة. وكلُّ شرطٍ
يعود إلى الشاشة برمزه وقيمته والمطلوب، فمن مُنع يقرأ «رحلاتٌ مكتملة ٣١ من ٥٠» ويعرف ماذا يفعل.
**و«مضى» لا «قائمٌ الآن»** (القرار ٣): الشرطُ يقيس تاريخاً لا حالة، ومن اشترى شهرياً قبل ساعةٍ لم
يُثبت شيئاً. **واليوميُّ لا يُؤهِّل**، وإلا صارت السلفةُ تُشترى باشتراكٍ يوميٍّ واحد.

**والسقفُ طبقتان والأدنى يحكم**: المحسوبُ يبدأ من **سعر الخطة اليومية** — لا رقمَ جديدٌ يُخترع
فيتقادم يومَ تتغيّر الأسعار — وينمو بنسبةٍ عن **كل سلفةٍ سُدِّدت** وحدها، محدوداً بسقفٍ أقصى؛
وتخصيصُ الإدارة لكبتنٍ بعينه (`drivers.advance_cap_override`) **يخفض ولا يرفع**: `NULL` لا تخصيص،
**وصفرٌ منعٌ من السلف** — حالتان لا يحملهما رقمٌ واحد. **والصرفُ تلقائيٌّ داخل السقف، وما فوقه
بموافقة مشرفٍ يبقى اسمُه على الصف** (القرار ٢).

**والاقتطاعُ خصمٌ من أرباحٍ داخلة**: موضعُه `payments.settle` بعد العمولة، **وداخل شرط
`DIRECTLY_COLLECTED_METHODS`** — الكاشُ وكليكٌ لا يمرّان بالمحفظة، فلا شيءَ فيهما يُقتطع. ويقف عند
**ثلاثةِ حدود**: النسبة (٢٠٪)، والمتبقّي، **وما يجب أن يبقى للكبتن** — فاقتطاعٌ يستنزف أرباحَه
يوقفه عن العمل فيمتنع السدادُ نفسُه (القرار ٤). **ولا يُفشل تسويةً أبداً**: محفظةٌ لا تكفي تعني
اقتطاعاً يؤجَّل، لا دفعةً تُرفض. وسدادٌ يدويٌّ كاملٌ متاحٌ في أي وقت.

**والمهلةُ مجمَّدةٌ على السلفة لحظةَ صرفها** (`due_at`) ولا تُقرأ من الإعدادات بعدها — قاعدةُ
`commission_percent_at_ride` نفسُها.

**وبانقضائها إيقافٌ عن العمل حتى السداد**، وثلاثةُ حدودٍ تحرسه من أن يصير أذى:

1. **يمنع الطلبَ التالي ولا يقطع رحلةً جارية** — قاعدةُ كنس الاشتراكات: القطعُ يترك راكباً في
   الطريق ويُطلق تنبيهَ «انقطع اتصال الكبتن» على من لم ينقطع.
2. **ولا يمنع السداد**: شحنُ المحفظة والتسديدُ يبقيان مفتوحَين، وإلا صار الإيقافُ يمنع الخروجَ منه.
3. **ويُقال له السببُ والمقدارُ وطريقُ الخروج**، لا «حسابك موقوف» وحدها.

**ويقرؤه التوزيعُ من عمودٍ محضَّر** (`drivers.advance_blocked`) لا استعلامٍ في مسارٍ يمرّ به كلُّ
طلبٍ وكلُّ خريطةِ راكب. **وتكتبه دورةٌ كلَّ عشر دقائق، ويطفئه مسارُ السداد نفسُه** — من سدَّد وبقي
ممنوعاً عشر دقائق يقرأ السدادَ بلا أثر. **ولا يُباع له اشتراكٌ يوميٌّ وعليه سلفةٌ تجاوزت مهلتها**
(القرار ٥)، وموضعُ الشرط `subscriptions._create`: البابُ الوحيد الذي يكتب صفَّ اشتراك.

**ومن اختفى بدَين** (القرار ٧): **الحمايةُ في الحجم لا في التحصيل** — أسوأُ خسارةٍ ممكنةٍ لكبتنٍ
جديدٍ هي اشتراكٌ يوميٌّ واحد، وهذا وحده ما يجعل البندَ آمناً، لا مهلةٌ ولا إيقاف. وبعد **تسعين
يوماً** من انقضاء المهلة يصير الشطبُ **مطروحاً**، ويقع **بقرارٍ إداريٍّ بسببٍ مكتوبٍ في التدقيق**
— لا شطبَ آليّ. **ولا قيدَ يُكتب له في الدفتر**: لم يتحرك مالٌ في محفظة أحد، وقيدُ «تسويةٍ» وهميٌّ
يجعل الدفترَ يقول إنه سدَّد. **والشطبُ يرفع الإيقاف**: عقوبةٌ على دَينٍ لم يعد قائماً في دفاترنا.

**ودَينٌ قائمٌ يمنع إغلاقَ الحساب** (القسم 7). **وبه يعمل قرارُ المالك ٦ بلا سطرٍ واحد**: الرصيدُ
المحتجَز شرطٌ على **السحب** والاقتطاعُ ليس سحباً، فالدَّينُ يُستوفى من الرصيد كلِّه ثم يُصرف
الباقي — وهو نصُّ «يُقتطع للدَّين أولاً».

**وفي شاشة أرباح الكبتن سطرٌ خامس**: ما اقتُطع سداداً للسلفة. رقمٌ ينقص بلا سببٍ مكتوبٍ يُقرأ عطباً
— نفسُ سببِ إفراد سطر البقشيش. **وصرفُ السلفة نفسُه ليس في ذلك الكشف**: قرضٌ يُعرض في كشف أرباحٍ
يُقرأ ربحاً.

**والسوقُ واحدٌ أولاً: الأردن** (القرار ٨) — والمفتاحُ `driver_advances_enabled` per-country هو ما
يجعلها تجربةً لا التزاماً. **ويشحن مطفأً في الدولتين**: البذرةُ تصف ما يشحن، والإشعالُ قرارٌ يقع
في اللوحة — وسلفةٌ تُصرف في التطوير بلا قرارٍ تشغيليٍّ هي **مالٌ يخرج**، لا ميزةٌ تُجرَّب. **ولا
خطةَ يوميةً مفعّلة = لا سلف** ولو أُشعل المفتاح: أساسُ السقف غيرُ موجود.

---

---

### 9.3 المهامُّ والمستوياتُ والشارات (البند ٥٣، 2026-08-16)

> التفصيلُ والقراراتُ الخمسة في `design/MISSIONS-LEVELS.md`. وهذا ما يلزمُ من يقرأ المواصفة
> وحدَها: الجداولُ، والقيدُ الذي لا يُتجاوز، ومن يكتبُ ماذا.

**ثلاثةُ أشياء لا شيءٌ واحد**، وفصلُها هو التصميم:

| | من يكتبُه | متى يتبدّل |
|---|---|---|
| **المهمّة** (`missions`) | الإدارة، per-country، شهرياً | بإنشائها وتعديلها |
| **المستوى** (`drivers.level`) | **مهمّةٌ دوريةٌ وحدَها** (`tasks/levels.py`، كلّ ساعة) | بإعادة التقييم |
| **الشارة** (`badges` / `driver_badges`) | **المشرف بيده** بسببٍ مكتوب | بمنحها أو سحبها |

**وخلطُ الأخيرَين يُفسد الاثنين**: شارةٌ آليةٌ تصير مستوىً ثانياً بأسماءٍ مختلفة، ومستوىً يُمنح
بيدٍ يصير **محاباةً في ترتيب التوزيع** — أي في المال. **فلا زرَّ «ارفع مستوى هذا الكبتن» في
اللوحة ولا في أي مكان، ولا تدخل الشاراتُ ترتيبَ التوزيع بحال.**

**والقيدُ الذي لا يُتجاوز**: الأقربُ يبقى الأوّل، والمستوى يفصل بين المتقاربين ولا يعلو المسافة
أبداً. وصيغتُه (قرارُ المالك ١ و٢): **المسافةُ المؤثّرة = المسافة − خصمُ المستوى بالأمتار،
بحدٍّ أقصى ١٠٠م** (`level_settings.discount_meters` per-country)، **ولا عتبةَ حادّة** — وصيغةُ
الشرائح مرفوضةٌ لأنّ حدَّ الشريحة يجعل فرقَ مترين يقلب القاعدة. **وأقصى إزاحةٍ = الخصمُ
نفسُه**، فيُقرأ «كم مترٍ يساوي هذا المستوى» جواباً مباشراً. والحدُّ محروسٌ في ثلاث: نموذجُ
الطلب، والخدمة، و`CHECK` في القاعدة.

**والتقدّمُ يُقاس حيّاً ولا يُراكَم في عمود** (قاعدةُ الإحالة والكوبون)، **والمستوى وحدَه
محضَّر** لأنّ `dispatch` لا يحتمل استعلاماً زائداً — وسابقتُه `drivers.rating_avg`. والفرقُ بينه
وبين `qualified_at` المرفوض **مَن يكتب ومتى** لا التخزين: كاتبٌ واحد، ويُعاد بناؤه كاملاً،
و`level_computed_at` تجيب «متى حُسب؟». **وتعديلُ مهمّةٍ يستدعي إعادةَ تقييمٍ فوريةً** لتلك
الدولة، وإلاّ عاش المستوى على تعريفٍ زال.

**ومعياران لا ثلاثة**: `completed_rides` و`min_rating`. و`active_hours` يحتاج **التقاطاً جديداً**
(`driver_activity_days`) لأنّ الحضور مفتاحٌ في Redis مهلتُه ٣٠ ثانية، و`drivers.is_online` يقول «الزرُّ
مرفوع» لا «كان هنا ثلاث ساعات» — **وما لا يُقاس لا يُشحن**.

**والشهرُ بتقويم الدولة** (قاعدةُ «يومِ الدولة»)، **و`driver_levels_enabled` مطفأٌ والأثرُ صفر**
— فتشحن الميزةُ خامدةً مرتين، ومطفأً **لا ترجيحَ ولا شاشةَ ولا مستوىً يُحسب أصلاً**.

---

## 10. التتبع اللحظي (WebSockets)

- قناة الكبتن: يبث `{lat, lng, heading}` كل 3 ثوانٍ → تحديث Redis GEO
- قناة الراكب:
  - **قبل الطلب (الشاشة الرئيسية):** يستقبل مواقع الكباتن المتاحين القريبين (ضمن ~3كم من Redis GEO) لعرضهم كأيقونات سيارات تتحرك على الخريطة — تحديث كل 5 ثوانٍ، **بيانات مجهّلة تماماً**: إحداثيات + اتجاه + فئة المركبة فقط، بدون هوية الكبتن أو رقم اللوحة، والمتاحون فقط (online + اشتراك ساري + بدون رحلة). حركة الأيقونات تُنعَّم بالـ interpolation في الواجهة
  - **أثناء الرحلة:** يستقبل موقع كبتنه المُسنَد فقط (كل 3 ثوانٍ، ببياناته الكاملة)
- أحداث الرحلة تُبث للطرفين: `driver_assigned`, `driver_arrived`, `ride_started`, `ride_completed`, `ride_cancelled`
- Reconnect تلقائي مع استرجاع آخر حالة عبر REST

**إشعارات Push بجانب البثّ (المرحلة 8).** الـ WebSocket يصل التطبيقَ وهو مفتوح، وPush يصله وهو مغلق —
فكلُّ ما يُبث في هذه القائمة يُرسل Push معه: أحداث الرحلة الخمسة، وعرضُ الطلب على الكبتن، وتنبيها
الاشتراك (قبل 24 ساعة وعند الانتهاء). وثلاث قواعد تحكمه:

- **لا Push لجهازٍ سوكته نشط.** المقبس يحمل `?device_id=` ويترك أثراً في Redis ما دام مفتوحاً، فيُستثنى
  ذلك الجهاز وحده: الحدثُ وصله بالفعل، وإشعارٌ ثانٍ على شاشةٍ مفتوحة يقول للمستخدم إن التطبيق لا يعرف
  أنه يستعمله. وهذا نفسه ما يمنع أن يصل الفاعلَ إشعارٌ بفعله. ومقبسٌ بلا `device_id` لا يمنع Push:
  حرمانُ كل أجهزة الحساب لأن أحدها مفتوح يعني هاتفاً في الجيب لا يرنّ لأن لوحاً على الطاولة مفتوح
- **طلبات الرحلة بأولوية عالية** (`android.priority=high` و`apns-priority=10` و`Urgency: high`): مهلة
  القبول عشرون ثانية، وDoze mode ووضعُ توفير البطارية يؤجّلان الإشعار العادي دقائق — فتصل البطاقة بعد
  أن انتقل الطلب لكبتنٍ آخر. وما عداها بأولوية عادية
- **هذه الفئة غير قابلة للإيقاف من المستخدم**: جزءٌ من الخدمة لا إعلان. ولا تحبسها ساعات هدوء
  (القسم 13). أما الحملات التسويقية فأولويةٌ عادية، ومفتاحُ إطفاءٍ بيد صاحبها، ونافذةُ هدوء

**الفشل يُبتلع ويُسجَّل**: إشعارٌ لم يصل خسارةٌ أهون من رحلةٍ لم تبدأ لأن خدمةً بعيدة تأخرت.

**حمولةُ الإشعار: `data` قيمٌ خام، و`title`/`body` لدرج النظام وحده.** كلُّ إشعارٍ معاملاتي يضع في
`data` ما تحتاجه الواجهة لتصوغ جملتها: `type` و`ride_id` و`amount` و`currency` وما يخصّ الحدث
(`payment_id`، `transfer_reference`، `expires_at`، `doc_type`، `review_note`…) — **لا جملةً مصوغة
ولا أرقاماً منسّقة**. والتنسيقُ قرارُ عرضٍ لا قرارُ خدمة: نصٌّ تبنيه الخلفية بـ`f"{amount}
{currency}"` يخرج «4.100 JOD» بخاناتٍ لاتينية ورمزٍ إنجليزي في تطبيقٍ كلُّ أرقامه عربيةٌ-هندية.
**ولا تُعرَّب الخانات في الخلفية**: ذلك يضع قرارَ لغةٍ في طبقةٍ لا تعرف من يقرأ، ويمرّ بالمال حيث
لا يجوز أن يُمسّ إلا حسابياً.

**وساعاتُ الهدوء تُنشر في `GET /config`** لكل دولة ومعها مِنطقتُها الزمنية (`quiet_hours_start`،
`quiet_hours_end`، `quiet_hours_timezone`): عامّةٌ بطبيعتها — تخبر المستخدم متى **لا** تصله
الحملات — وبغير نشرها تكتب الواجهةُ رقماً من عندها فيخالف الجدول يوم يغيّره المشرف. والغائبةُ
`null` لا صفر: «لم تُضبط» ليست «منتصف الليل».

وتبقى `title`/`body` مكتوبتين في الخلفية لسببٍ واحد: **درجُ نظام التشغيل يرسمهما والتطبيق مغلق**،
فلا واجهةَ تصوغ شيئاً حينها. أما ما ترسمه الواجهة بنفسها — صندوقُ الوارد والأوراق السفلية — فيصوغ
نصَّه من `data`، ويقع على `title`/`body` المخزَّنين احتياطاً لنوعٍ لا يعرف صياغته (نوعٌ جديد في
الخلفية لا يكسر الشاشة). **والحملات التسويقية استثناء**: نصُّها هو المحتوى كما كتبه المشرف.

**عقد تسجيل الأجهزة — تلتزم به التطبيقات الثلاثة.** قاعدةُ استثناء الجهاز المفتوح أعلاه لا تعمل بغيره،
والخلفيةُ لا تستطيع فرضه: هي لا تعرف من الجهاز إلا ما يرسله.

| متى | ماذا يستدعي التطبيق |
|-----|---------------------|
| بعد الدخول، وكلما دار رمز FCM (`onTokenRefresh`) | `PUT /me/devices` بـ `{device_id, token, platform}` |
| عند الخروج، وقبل تبديل الحساب على نفس الجهاز | `DELETE /me/devices/{device_id}` |
| عند فتح المقبس | `WS /ws/rider?token=…&device_id=…` أو `/ws/driver?…` — **بنفس `device_id`** |

- **`device_id` يولّده التطبيق ويثبت مع تثبيته** (لا يتغير بتغير رمز FCM ولا بتسجيل خروجٍ ودخول). هو
  الوصلةُ الوحيدة بين «هذا المقبس مفتوح» و«هذا الرمز لا تُرسل إليه»؛ ومقبسٌ يفتح بـ `device_id` مختلف
  عن الذي سجّل الرمز يجعل الجهاز يستقبل الحدثَ مرتين: على الشاشة ومن نظام التشغيل.
- **مقبسٌ بلا `device_id` مقبولٌ ولا يمنع Push** — القرار مذكور أعلاه.
- **الخروج يحذف الصف ولا يكتفي بإطفائه**: رمزٌ لحسابٍ غادر الهاتف يوصل إشعاراته إلى من يحمله بعده.
  ولذلك أيضاً `token` فريدٌ على الجدول كله: تسجيلُه لحسابٍ آخر ينقله إليه (القسم 4).
- **الرمز لا يعود من الخلفية أبداً** في أي مخرج: `GET /me/devices` يعطي `device_id` والمنصة والحالة
  وآخر ظهور لا غير.

**أداة تطوير:** `tools/fcm-token/` صفحةٌ واحدة تُخرج رمز جهازٍ من المتصفح، فتُختبر السلسلة كاملةً
(عقد → OAuth → FCM → جهاز) قبل بناء التطبيقات — ومعها `POST /admin/campaigns/test-push` الذي يرسل
إلى رمزٍ يكتبه المشرف.
- **مسار REST احتياطي لبثّ الموقع:** `POST /drivers/me/location` بنفس الحمولة `{lat, lng, heading}`. القناة الأساسية تبقى الـ WebSocket؛ هذا المسار لتطبيقٍ فقد مقبسه ولم يُعِده بعد، فلا يسقط الكبتن من التوزيع ومن خريطة الركاب لأجل انقطاع لحظي. **محكوم بـ rate limiting لكل كبتن** بما يناسب دورة الثلاث ثوانٍ (نحو 30 طلباً في الدقيقة): مسار كتابة متكرر بلا كلفة على العميل، فبغير سقفٍ يصلح لإغراق Redis

---

## 11. تطبيق الراكب — الشاشات

1. **دخول**: رقم الهاتف + كلمة المرور — دائماً ولكل المستخدمين.
   **تسجيل**: إثباتُ الرقم مرةً (تدفّق Firebase في التطبيق، أو رمزٌ نرسله إن كان المُحقِّق SMS) ثم
   يضع المستخدم كلمة مروره في نفس الطلب. **نسيت كلمة المرور**: نفس الإثبات ثم كلمةٌ جديدة — شاشةٌ
   منفصلة، ولا تُفتح جلسةٌ بمجرد الإثبات بل بعد كتابة الكلمة. والواجهة تعرف أيَّ تدفّقٍ ترسم من
   `GET /auth/method` (`verification`)
2. الرئيسية: خريطة Mapbox ملء الشاشة تعرض **سيارات الكباتن القريبين تتحرك لحظياً** (أيقونة سيارة تدور مع الاتجاه heading) + bottom sheet "إلى أين؟"
3. اختيار الوجهة (دبوس + بحث Geocoding) → عرض السعر المقدر وفئة المركبة → تأكيد الطلب.
   **ومعه اختيارُ «أي كبتن / كبتنة فقط»** حيث الخدمة النسائية مفعّلة ولمن أعلنت جنسها أنثى
   (المرحلة 10-ج) — ولا يظهر لغيرها: عرضُ «اطلب كبتنة» على راكبٍ رجل يفتح البابَ الذي وُجدت
   الخدمة لإغلاقه. و**ثمنُ الاختيار مكتوبٌ قبل الضغط**: الكبتنات أقل عدداً فالانتظار أطول
   والبحث يتسع — قولُه هنا يجعل الانتظار خياراً، والسكوتُ عنه يجعله عطلاً ثم «لم نجد كبتناً»
   بلا سبب
4. البحث عن كبتن (مؤشر حي) → بطاقة الكبتن + تتبع لحظي + مشاركة الرحلة (رابط).
   وأثناء البحث عن كبتنة يُعاد قولُ سبب الانتظار، و«لم نجد كبتناً» على طلبٍ نسائي يحمل سببَه
   ومخرجَه («لا كبتنة قريبة الآن — جرّبي بعد قليل أو اطلبي أي كبتن») لا نصّاً عاماً يُقرأ رفضاً
   شخصياً. **وعند بدء الرحلة النسائية يُقترح مشاركتُها** مع من تثق: الزرُّ قائمٌ لكل رحلة، وما
   يضيفه الاقتراح هو **التوقيت** — لحظةَ تحرّك السيارة، ولمن طلبت الخدمة لا للجميع
4-ب. **ورقةُ التأكيد تحمل زرَّ «كوبون»** (المرحلة 12-ز، القسم 6.6): حقلُ رمزٍ وزرُّ «تطبيق»، ثم
   «خصم مُطبَّق» وأسعارُ الفئات مخصومة. والخصمُ يأتي من `POST /promo/validate` لا تحسبه الشاشة،
   وتُخفى الورقةُ كلُّها حيث `promo_codes_enabled` مطفأ
5. شاشة الدفع (الطرق المتاحة حسب الدولة) → التقييم، **ومعه أزرارُ بقشيشٍ عند أربع نجومٍ فأكثر**
   (المرحلة 12-و، القسم 6.5): زرّان بمبلغَي الدولة و«بدون». وتُخفى الأزرارُ كلَّها حيث
   `tips_enabled` مطفأ أو لا محفظةَ ولا رصيد — لا زرَّ معطَّلاً يرفع توقّعاً ثم يخيّبه
6. محفظتي: الرصيد، الشحن، التحويل (إن مفعّل)، السجل
7. رحلاتي (السجل والتفاصيل) + الملف الشخصي
8. **الإشعارات — عقدٌ يلتزم به التطبيق لا شاشة** (تفصيله في القسم 10):
   - بعد الدخول وكلما دار رمز FCM: `PUT /me/devices` بـ `{device_id, token, platform}`
   - عند الخروج: `DELETE /me/devices/{device_id}`
   - فتح المقبس بـ `?token=…&device_id=…` **بنفس `device_id`** — وإلا وصل الحدث مرتين: على الشاشة
     ومن نظام التشغيل
   - في الملف الشخصي مفتاحٌ واحد: **إشعارات العروض** (`marketing_push_enabled`). إشعارات الرحلة
     ليست خياراً — من يطفئ «وصل الكبتن» ينتظر كبتناً لا يعرف أنه وصل

## 12. تطبيق الكبتن — الشاشات

1. دخول برقم الهاتف + كلمة مرور (والتسجيل والاستعادة كما في القسم 11/1) → أول مرة: رفع المستندات →
   بانتظار الموافقة. **ولا تُعتمد وثائقُه ما لم يكن رقمه مُثبتاً** مهما كان مفتاح التحقق — رقمُه هو
   ما يستلم عليه حوالات كليك، فتطالبه الشاشة بالإثبات قبل أن ينتظر المراجعة.
   **ورفعُ المستندات مبنيٌّ في المرحلة 9-ب** (`PUT /drivers/me/documents/{doc_type}`): صورة أو PDF،
   والنوعُ والحجم يُفحصان من محتوى الملف لا من ترويسته، ورفعُ نوعٍ ثانيةً استبدالٌ يعيده «قيد
   المراجعة». وتعرض الشاشةُ حالة كل مستند وسببَ رفضه — **السبب جزءٌ من الإشعار** لا تفصيلٌ يُطلب
   بعده: كبتنٌ لا يعرف لماذا رُفضت رخصته يعيد رفع الصورة نفسها ويبقى منتظراً إلى الأبد
   **وبياناتُ المركبة تُحرَّر من التطبيق** (`PATCH /drivers/me/vehicles/{id}`) **بسياسة
   الاستبدال نفسِها**: رخصةُ المركبة أحدُ المستندات الثلاثة المطلوبة، فتغييرُ ما تشهد
   عليه — اللوحةُ أو الصنع أو الطراز أو السنة أو الفئة — **يعيد الكبتن المعتمد «قيد
   المراجعة»**، واللونُ لا يفعل. ومن دون ذلك يصير الحقلُ باباً لتشغيل مركبةٍ لم تُراجع
   أوراقُها تحت اعتمادٍ صدر لمركبةٍ أخرى. والقياسُ على ما **تغيّر فعلاً** لا على ما
   أُرسل — إرسالُ اللوحة نفسِها ليس تغييراً — والشاشةُ تقول الثمنَ قبل الحفظ لا بعده،
   والتعديلُ مرفوضٌ أثناء رحلةٍ جارية، **والفئةُ لا تُحرَّر من التطبيق**: هي التي تحدّد
   التعرفة، فتغييرُها قرارُ إدارة. **وحالةُ الكبتن تُقرأ تحت قفل صفِّه** في هذا المسار
   وفي استبدال المستند كليهما: بغيره يُلغي فعلُ الكبتن **إيقافاً إدارياً** وقع في نفس
   اللحظة — تُكتب `pending` فوق `suspended` فيعود الموقوف إلى طابور المراجعة
2. الرئيسية: مفتاح Online/Offline كبير + الخريطة (وضع ليلي افتراضي)
3. بطاقة طلب واردة: نقطة الانطلاق، المسافة إليها، السعر، عدّاد 20 ثانية، قبول/رفض.
   **وشارةُ «طلب نسائي»** على ما طُلب فيه جنسٌ (المرحلة 10-ج) — فوق السعر لأنها تُقرأ **قبل**
   قرار القبول: كبتنٌ يقبل ثم يكتشف أن الطلب ليس له يُلغي، وإلغاءٌ كان يمكن تجنّبه بشارة.
   وهي **وصفٌ للطلب لا هويةُ صاحبته**: جنسُ الراكبة لا يصل التطبيق أصلاً
   - وفي الإعدادات **تفضيلُه الدائم لجنس الركاب** (الجميع / النساء فقط / الرجال فقط)، معروضاً
     لكل كبتنٍ لا للكبتنات وحدهنّ — ولا يظهر حيث الخدمة مطفأة، فمفتاحٌ لا أثر له يوهم صاحبه
     أن طلباتِه تُصفّى وهي لا تُصفّى
4. الرحلة النشطة: ملاحة للانطلاق → "وصلت" → "بدء الرحلة" → "إنهاء" → شاشة تحصيل الدفع
5. اشتراكي: الحالة + التجديد بالخطط الثلاث
6. محفظتي: الرصيد + طلب سحب + السجل
7. أرباحي: اليوم/الأسبوع/الشهر + عدد الرحلات — `GET /drivers/me/earnings?period=`.
   **وسطرُ بقشيشٍ مستقل** (المرحلة 12-و): هو الدخلُ الوحيد بلا عمولةٍ عليه، فمبلغٌ يزيد «ما
   دخل المحفظة» بلا أن يزيد «ما خرج عمولةً» يجعل الرقمين لا يتّسقان لمن يجمعهما بيده.
   **وثلاثةُ أرقامٍ لا رقمان**: ما دخل المحفظة (`ride_earning`)، وما خرج منها عمولةً
   (`commission` — **ويُخصم على الرحلة النقدية أيضاً** حين يكون النطاق `all_rides`)، وما
   قُبض باليد كاشاً وكليكاً موسوماً **«مُحصَّل مباشرة»** كما يوجب القسم 9. **والصافي قد
   يكون سالباً ولا يُقصّ عند الصفر**: يومٌ كلُّه كاش يترك عمولةً بلا أرباحَ تقابلها،
   وإخفاءُ ذلك يجعل الكبتن يكتشف نقصان رصيده بلا سببٍ ظاهر. **والنافذةُ يومُ الدولة**
   بمنطقتها المخزَّنة كنافذة القسم 13/1، **والجمعُ في القاعدة** لا في التطبيق (القسم 14)
7-أ. **سجل الرحلات وتفاصيلها**: قائمةٌ من `GET /rides/me` (وقتٌ وأجرةٌ ونقطتان ومسافةٌ وحالة)،
   **ومعها حالُ دفع كلِّ رحلة**: يردّ المسار `{ride, has_open_dispute, payment_methods,
   paid_amount}` فتحمل الشارةُ البرتقالية «نزاع» ما دفعتُه متنازعٌ عليها. والملخّصُ
   **استعلامٌ ثانٍ على صفحة الرحلات كلِّها** لا نداءٌ لكل صفٍّ ولا ضمٌّ إلى الاستعلام
   الأول: الدفعُ المختلط صفّان (القسم 6)، فالضمُّ يضاعف الرحلة ويُسقط جزءاً منها من
   صفحةٍ مسقوفة. ثم
   تفاصيلُ رحلةٍ واحدة معها دفعاتُها وتقييمُه للراكب. **وزرُّ النزاع فيها مشروطٌ بدفعة كليك
   بانتظار التأكيد** — `dispute_by_driver` يرفض ما عداها (القسم 6.2): الكاش يقع يداً بيد، ومن لم
   يقبض لا يضغط «استلمت المبلغ» فتبقى الدفعة معلّقةً تراها الإدارة. ولا تفصيلَ تعرفةٍ سطراً سطراً
   في الشاشة: `GET /config` لا ينشر أسعار `pricing_settings`، والحسابُ في الخلفية حصراً (القسم 14)
8. **الإشعارات**: نفس عقد التسجيل في القسم 11/8 والقسم 10 حرفاً بحرف. وهو هنا **أشد إلزاماً**:
   بطاقة الطلب الواردة تصل بأولوية عالية ومهلتها عشرون ثانية، فجهازٌ لم يسجّل رمزه لا تصله طلبات
   والتطبيق مغلق — ويبدو للكبتن كأن لا رحلات في المنطقة

## 13. لوحة الإدارة — الأقسام

1. Dashboard: رحلات اليوم، الكباتن أونلاين (خريطة حية)، الإيرادات.
   و**«الخريطة الحيّة» لـ admin حصراً** (البند 8 يعطي الدعمَ قراءةً ومعالجةَ نزاعات، ومعرفةُ أين
   يقف كلُّ كبتنٍ الآن باسمه ورقمه ليست منهما): هي **المنفذ الوحيد في المنصة الذي يقرن هويةً
   بموقع**، وخريطةُ الراكب تبقى مجهَّلةً كما يفرض القسم 10 ولا يُعاد استعمال تمثيلها.
   **وفتحُها يدخل سجل التدقيق** (`read` على `live_map`) — قراءةٌ تُسجَّل، وهي نادرةٌ عمداً:
   صلاحيةٌ بهذا الاتساع تُسأل عنها. والقيدُ **مخنوقٌ بنافذة** فجلسةُ مراقبةٍ قيدٌ واحد، وإلا
   أغرقت شاشةٌ تُحدَّث كل خمس ثوانٍ السجلَّ بمئة صفٍّ فأخفت القرارات الحقيقية بينها.
   **ولا إسنادَ يدويّ فيها**: الرحلة تُعرض على كبتنٍ واحدٍ في كل لحظة (القسم 5)، وزرٌّ يتخطى
   العرضَ من اللوحة يُسند رحلةً لمن لم يقبلها — البند مؤجَّل في `FUTURE-FEATURES`
2. الكباتن: طلبات الانضمام (مراجعة مستندات: قبول/رفض)، القائمة، إيقاف/تفعيل.
   **الاعتماد يشترط `phone_verified_at`** ويردّ 403 بدونه — لا يعفيه إطفاء مفتاح التحقق.
   **ويشترط أيضاً (المرحلة 9-ب) قبولَ المستندات المطلوبة الثلاثة** ويردّ 409 بدونها تسمّي الناقص:
   بغير هذا الحارس تصير المراجعة عادةً لا شرطاً — زرُّ «اعتماد» يعمل والمستنداتُ فارغة، وهو ما كان
   يقع فعلاً. مراجعةُ المستند قرارٌ لـ **admin حصراً** (البند 8) ورفضُه يوجب سبباً مكتوباً يصل
   صاحبه، وكلُّ مراجعةٍ تدخل سجل التدقيق. والقراءة والمعاينة لـ staff
3. الركاب: قائمة، حظر، تجميد محفظة. **والحسابات غير محققة الرقم موسومةٌ وقابلة للفلترة**
   (`GET /admin/users?phone_verified=false`) — حالةٌ استثنائية لا تُعالَج إن لم تُرَ
4. الرحلات: بحث وفلترة + تفاصيل كاملة + النزاعات (`disputed`) للفصل
5. المالية: تأكيد شحنات كليك/كاش، طلبات السحب، تقارير الاشتراكات
6. الإعدادات per-country: التسعير، خطط الاشتراك، مفتاح العمولة ونسبتها، feature flags، حدود التحويل
   والسحب. ومنها **`otp_verification_enabled`**: لـ admin حصراً، بتحذيرٍ صريح أنه **للطوارئ فقط**،
   وبحقل **سبب إلزامي** عند الإطفاء يدخل سجل التدقيق. ولا يعطّل استعادة كلمة المرور أبداً
7. **العقود وتكاملات API:** بطاقة لكل مزود خارجي (SMS، Telr، Mapbox، CliQ acquirer، FCM، payout) بحقولها الخاصة (api_key، secret، merchant_id، sender_id...) + مفتاح تفعيل + زر "اختبار الاتصال". إدخال بيانات عقد جديد يفعّل ميزته آلياً بدون نشر كود. القيم تُعرض مقنّعة (\*\*\*\*) بعد الحفظ
8. صلاحيات: admin كامل / support قراءة + معالجة نزاعات فقط — صفحة العقود لـ admin حصراً.
   **ومعها التحقق الثنائي (المرحلة 12-د)**: قسمُ «الأمان» في حساب المستخدم نفسِه (تسجيلُ العامل
   وإطفاؤه ورموزُ الاسترداد)، ومفتاحُ **إلزام الـadmin** ومهلةُ الخمول في `security_settings` —
   وإشعالُ الإلزام مرفوضٌ حتى يُثبت المشرفُ الطالبُ أن استردادَه يعمل. التفصيل في القسم 14.1
9-ب. **جدولُ الكوبونات** في قسم «العروض والحملات» (المرحلة 12-ز): رمزٌ ونوعُ خصمٍ وقيمةٌ وسقفٌ
   وميزانيةٌ **ومصروفٌ قد يتجاوزها** (السقفُ يمنع تطبيقاً جديداً لا رحلةً جارية) وحدودُ استعمالٍ
   ومدةٌ ودولة، ومعها **جملةُ من يتحمّل الخصم** — «تتحمّلها الشركة» (شرطُ المالك). لـ**admin
   حصراً**: كوبونٌ سقفُه ألفُ دينار قرارٌ ماليّ لا إجراءُ دعم. وكلُّ كتابةٍ تدخل سجل التدقيق
9. **حملات الإشعارات** (المرحلة 8 بنت مساراتها، وواجهتها في **المرحلة 11**): إنشاء حملة وتعديلها
   وجدولتها وإلغاؤها، واختيار الجمهور (كل الركاب / كل الكباتن / مستخدمو دولة)، وسجلُّ من وصله الإشعار
   ومن تُخطّي ولماذا. **لـ admin حصراً**: حملةٌ تصل عشرات الآلاف ليست إجراء دعمٍ فني. وكل كتابةٍ تدخل
   سجل التدقيق. ومعها ضبط **ساعات الهدوء per-country** (البند 6): لا إرسال تسويقي داخلها، ويُؤجَّل
   للنافذة التالية لا يُلغى ولا يُرسل ناقصاً — والتقسيم بالدولة لا بالحملة، فحملةٌ للسوقين تُرسل في
   الأردن الآن وفي ليبيا بعد ساعة إن اختلفت نافذتاهما. **والإشعارات المعاملاتية خارج هذا كله**
   (القسم 10)

---

## 14. الأمان (إلزامي)

- كل الحسابات المالية والتسعير في الخلفية حصراً — الواجهة تعرض فقط
- التحقق من ملكية الرحلة/المحفظة في كل endpoint (لا IDOR)
- Rate limiting على محاولات الدخول والطلبات
- تحقق توقيع Webhooks من Telr
- Idempotency keys على عمليات الدفع والتحويل (منع التكرار)
- كل عمليات المحفظة داخل DB transactions ذرّية
- سجل تدقيق (audit log) لكل إجراء إداري مالي
- لا أسرار في الكود أو الواجهة: أسرار البنية التحتية فقط (DB, Redis, JWT secret, مفتاح تشفير provider_credentials) في `.env` — وكل مفاتيح المزودين الخارجيين (Mapbox، Telr، SMS، FCM...) في جدول provider_credentials مشفرةً، تُدار من صفحة العقود حصراً

### 14.1 التحقق الثنائي لدخول اللوحة (TOTP) — المرحلة 12-د

**TOTP لا SMS** (قرارُ المالك): مجانيٌّ، ولا يحتاج عقدَ مزوّدٍ يُنتظر، وأقوى من رمزٍ يمشي في شبكةٍ
يمكن تحويلُ رقمها. **وهو للوحة وحدها**: مساراتُ `POST /auth/me/totp/*` لـ`StaffUser` حصراً. عاملٌ
ثانٍ للراكب قرارُ منتجٍ بكلفةِ دعمٍ حقيقية (راكبٌ يفقد هاتفه يفقد حسابه) ولا يطلبه هذا القسم.

**والدخولُ خطوتان، ولا توكنَ قبل العاملين** — وهي قاعدةُ استعادة كلمة المرور نفسُها حرفياً
(«التوكنات تُصدر بعد كتابة الكلمة لا بعد التحقق»):

1. `POST /auth/login` كما هو بسقوفه. فإن كان لصاحب الحساب عاملٌ **مؤكَّد** ردَّ **بلا توكنات**:
   `{"totp_required": true, "challenge_token": …, "expires_in": 300}`.
2. `POST /auth/login/totp` بالـchallenge والرمز → زوجُ التوكنات المعتاد.

والـchallenge **ليس access token ولا نصفَ جلسة**: قيمةٌ عشوائيةٌ مفتاحُها في Redis مربوطٌ
بالمستخدم، مهلتُها خمسُ دقائق، تُستهلَك مرةً واحدة، **ولا تفتح أيَّ مسارٍ في المنصة**. رمزٌ مكانه
`sub` في توكنٍ حقيقيٍّ «ناقصِ صلاحية» يصير جلسةً كاملةً بخطأٍ واحدٍ في حارسٍ واحد.

الرمزُ ستةُ أرقامٍ بخطوةِ ٣٠ ثانية، ويُقبل انحرافُ خطوةٍ في كل اتجاه (±٣٠ث) — ساعةُ حاسوبٍ مكتبيٍّ
تنحرف. ولا يُقبل رمزُ خطوةٍ سبق قبولُها (`last_step`).

**السقوف**: خمسُ محاولاتٍ لكل challenge ثم يُهدَم فيعود صاحبُه إلى كلمة المرور، وسقفٌ ساعيٌّ على
الحساب فوقه. **ولا قفلَ حساب**: قفلُ حسابِ مشرفٍ بالمحاولات الخاطئة بابٌ لتعطيل اللوحة يدفعه من
يخمّن سيئاً — والحدُّ يُبطئ التخمين، والقفلُ يُسلّم اللوحةَ للمخمّن.

**والتسجيل نداءان لا واحد**: `POST /auth/me/totp/enroll` يُنشئ سرّاً غيرَ مؤكَّدٍ ويردّه مرةً واحدة
(`otpauth://totp/...` ترسمه اللوحةُ QR **محلياً** — خدمةُ QR خارجيةٌ تعني إرسالَ السرِّ إلى طرفٍ
ثالث)، و`POST /auth/me/totp/confirm` برمزٍ صحيحٍ يختم `confirmed_at` ويولّد **عشرةَ رموز استردادٍ
تُعرض مرةً واحدة**. ولا مسارَ يعيد قراءةَ السرِّ ولا الرموز بعدها: مسارٌ يعرض السرَّ ثانيةً يجعل
جلسةً مسروقةً كافيةً لاستخراج العامل الذي وُضع ضدها.

**والإطفاء يشترط عاملاً حاضراً**: `DELETE /auth/me/totp` يطلب رمزاً صحيحاً أو رمزَ استرداد، لا
جلسةً وحدها — وإلا أسقطت جلسةٌ مسروقةٌ العاملَ الذي وُضع لأجلها. ويُرفض أصلاً حين يكون الإلزام
مشتعلاً على دور صاحبه: مفتاحٌ يخرج منه كلُّ مشرفٍ بنداءٍ واحد ليس إلزاماً.

**وإشعالُ الإلزام مشروطٌ بإثباتِ استردادٍ حقيقيّ** (قرارُ المالك: «لا تُلزم أحداً قبل أن أُثبت أن
الاسترداد يعمل»): `PUT /admin/security` يرفض `admin_totp_required = true` إن لم يكن **للمشرف
الطالبِ نفسه** عاملٌ مؤكَّدٌ و`recovery_codes_verified_at` مختوم. والختمُ يقع بنداءٍ صريح
(`POST /auth/me/totp/recovery/verify`) **يستهلك رمزاً حقيقياً**: إثباتٌ يُجرَّب لا مربَّعٌ يُؤشَّر —
يكلّف واحداً من عشرة، ويخبرك أن ما نسختَه هو ما تحفظه القاعدة فعلاً.

**والمشرفُ المُلزَمُ بلا عامل** يدخل بكلمة مروره ويجد كلَّ مسارٍ إداريٍّ مردوداً 403
`totp_enrollment_required` إلا مساراتِ التسجيل. والحارسُ يقرأ الصفَّ في كل طلبٍ كما يقرأ
`is_blocked` — **فلا مطالبةٌ تعيش في توكن ولا حالةُ جلسةٍ جديدة**، والإلزامُ يسري على جلسةٍ قائمة
لحظةَ إشعاله. وردُّ الدخول لا يُعلن السياسة قبل كلمة المرور، و`GET /config` لا ينشرها: «هل تكفي
كلمةُ المرور هنا» سؤالٌ لا يفيد إلا من لا يملكها.

**ومفتاحُ الإلزام للـ`admin`**، أما **العاملُ المؤكَّد فيسري على صاحبه أيّاً كان دورُه**: من سجّل
عاملاً يُسأل عنه في كل دخول. و`support` تسجيلُه اختياريٌّ اليوم — وجعلُه إلزامياً حقلٌ ثانٍ في
`security_settings` لا بناءٌ جديد، ولا يُضاف قبل أن يُطلب.

**ومهلةُ الخمول ٣٠ دقيقة طبقتان، ولكلِّ طبقةٍ ما تملكه** — وواحدةٌ منهما وحدها وعدٌ لا يُوفى:

- **الخلفية**: مفتاحُ الـrefresh لحسابات `admin`/`support` يُكتب بـTTL = `admin_idle_timeout_minutes`
  ويُجدَّد عند كل تدوير. لا عمودَ «آخرِ نشاط» ولا مؤقّتَ ثانٍ: التدويرُ لمرةٍ واحدة قائمٌ أصلاً فهو
  نبضةُ النشاط نفسُها. وما تملكه هذه الطبقة أن **توكنَ تجديدٍ مسروقاً يبطل بعد نصف ساعة**.
- **اللوحة**: مؤقّتٌ على **تفاعل المستخدم** يُخرج عند سكونٍ بطول المهلة. وما يملكه أن **تبويباً
  متروكاً على مكتبٍ يُقفَل** — وهو ما لا تراه الخلفيةُ أبداً: الخريطةُ الحيّة تستفتي كل خمس ثوانٍ،
  فتبويبٌ لا أحدَ عنده يجدّد جلستَه إلى الأبد لو حُسبت المهلةُ من النداءات. والأحداثُ المقيسة
  نقرٌ وضغطُ مفتاحٍ وتمريرٌ ولمس — **و`mousemove` ليست منها**: فأرةٌ يهزّها اهتزازُ المكتب تُبقي
  الجلسةَ حيّةً بلا إنسان. والقياسُ **ختمُ وقتٍ يُفحص دورياً لا `setTimeout` يُعاد ضبطه**، لأن
  حاسوباً أُغلق غطاؤه ساعةً يُجمّد المؤقّتات فيستيقظ ومؤقّتُه لم ينتهِ — أما الختمُ فيُقرأ عند
  الاستيقاظ فيُخرج فوراً. والمدةُ تصل من `GET /auth/me/totp` لا مكتوبةً في كود اللوحة: رقمٌ في
  المتصفح يفترق عن `security_settings` أوّلَ مرةٍ يُعدَّل، و`support` يحتاجه ولا يقرأ
  `GET /admin/security`.
- **وشاشةُ الأمان في اللوحة بطاقتان بمدىً مختلف**: «التحقق الثنائي لحسابي» لكل من يدخل اللوحة
  (فالعاملُ المؤكَّد يسري على صاحبه أيّاً كان دورُه)، و«سياسةُ دخول اللوحة» لـ`admin` حصراً. وزرُّ
  الإلزام **يُعطَّل بسببٍ مكتوب** لا يُترك ليرتدّ 409 — قاعدةُ زرِّ «اعتماد الكبتن» نفسُها. والرمزُ
  يُرسم QR **في المتصفح** بلا خدمةٍ خارجية: حمولةُ `otpauth://` تحمل السرَّ نفسه.
- **و`totp_enrollment_required` قد يرتدّ من أيّ نداء** (الحارسُ يقرأ الصفَّ في كل طلب)، فترجمتُه
  في **عميل HTTP واحد** يرفع علماً تقود به طبقةُ الجلسة إلى شاشة الأمان — لا في كل شاشةٍ على حِدة،
  وإلا رأى المشرفُ خطأً أحمر في كل بطاقة بدل بابٍ يفتح. وشاشةُ الأمان وحدها خارج تلك البوابة، وإلا
  صارت الحلقةُ مغلقة.
- **والتوكنات تبقى في `localStorage` لا `sessionStorage`**: الثاني نسخةٌ لكل تبويب، فتبويبان على
  سوقين — وهي طريقةُ عملٍ مقصودةٌ في القسم 13 — يصيران دخولين. المهلةُ هي ما يحدّ المخاطرة لا
  مكانُ الحفظ.

**وكلُّ ما يمسّ العامل يدخل سجل التدقيق**: تسجيلٌ، تأكيدٌ، إطفاءٌ، استهلاكُ رمز استرداد، وإشعالُ
الإلزام وإطفاؤه. ومعه **صفُّ إشعارٍ لصاحب الحساب** عند استهلاك رمز استرداد أو إطفاء العامل: هذان
بالذات ما يفعله من استولى على حساب، وصاحبُه أوّلُ من يجب أن يعرف.

**وإطفاءُ العامل يُبطل كلَّ جلسات صاحبه** (`revoke_all_for_user`) — **وتأكيدُه لا يُبطلها**، والفرقُ
اتجاهُ التغيير: الإطفاءُ يُضعف الحماية فتبقى جلساتٌ فُتحت بسياسةٍ أقوى، والتأكيدُ يقوّيها وكلمةُ
المرور التي فتحت الجلسة لم تتبدّل. وإخراجُ صاحبها عند التأكيد لا يحمي شيئاً: توكنُ الوصول لا يُبطَل
قبل انتهائه أصلاً (أعلى هذا القسم)، فالإبطالُ يقتل تجديدَه وحده — خروجٌ صامتٌ بعد ربع ساعة، ومعه
**رمزُ الخطوة المحروقةِ في التأكيد** يجعل أوّلَ دخولٍ يبدو رفضاً بلا سبب.

**ومهلةُ الخمول حقلٌ يعدّله المشرف بسقفٍ في الكود: ٦٠ دقيقة، وحدٌّ أدنى ٥** (قرارُ المالك). اللوحة
تفتح مفاتيح المزوّدين والدفع، فمهلةٌ يوسّعها مشرفٌ إلى يومٍ تُبطل الحارسَ من داخله؛ والحدُّ الأدنى
ليس تجميلاً — مهلةُ دقيقةٍ تُخرج المشرفَ وهو يكتب فيصير الحارسُ عطلاً يُطفأ. والسقفُ محروسٌ في
ثلاث طبقات (Pydantic، الخدمة، وCHECK في القاعدة) لأنه العددُ الذي إن مرّ خطأً لم يظهر خطؤه.

---

## 15. ميزات Phase 2 — تُبنى الآن وتُفعَّل لاحقاً

كل ما يلي **يُبنى ضمن المشروع الحالي** لكنه معطل افتراضياً، على ثلاثة أنواع:

### أ) ميزات تعتمد على عقود مزودين — تتفعل من صفحة "العقود وتكاملات API"
تُبنى كاملة الآن ضد واجهة مزود موحدة (مع مزود وهمي mock للاختبار)، وتتفعل تلقائياً بمجرد إدخال بيانات العقد الحقيقي من اللوحة:
- **التحقق من الهاتف** — **لا الدخول**: الدخول كلمةُ مرور دائماً، والتحقق يقع في التسجيل وفي
  استعادة كلمة المرور. والمُحقِّق يُختار من حالة العقود، وترتيبُه بعد المرحلة 12-هـ:
  **عقد `whatsapp` مفعّلٌ ومفتاحُ `whatsapp_otp_enabled` مشتعلٌ في دولة الرقم → واتساب؛ وإلا `sms`
  مفعّل → رسالةٌ قصيرة؛ وإلا `firebase_auth` مفعّل → Firebase؛ وإلا لا مُحقِّق** (فيُرفض التسجيل
  بـ 503 ما لم يُطفأ مفتاح التحقق).
  - **وتبدّلَ الترتيب بقرار المالك**: كان Firebase أولاً. واتساب أوثقُ وصولاً في السوقين — لا
    شريحةَ تُبدَّل ولا رسالةٌ تضيع في بوابةٍ محلية — **وتبعتُه صريحة**: عقدُ Firebase مفعّلاً لا
    يُستعمل ما دام قبله عقدٌ مفعّل، فمن أراده يُطفئ ما قبله. والترتيبُ ثابتٌ في الكود لا يُقرأ من
    إعداد: مفتاحٌ يقول «أيُّها أولاً» حالةٌ ثانية تختلف يوماً عن حالة العقود نفسها.
  - **وواتساب وحده يشترط شرطين**: العقدَ **والمفتاح**. والعقدُ عامٌّ (رقمُ أعمالٍ واحد عند ميتا
    يخدم السوقين، والرقمُ يحمل دولته) والمفتاحُ per-country — فالعقدُ يقول «نستطيع» والمفتاحُ يقول
    «نفعل في هذا السوق»، ويُجرَّب في سوقٍ قبل الآخر أو يُطفأ حيث لم يُعتمد قالبُه بلغته. ولذلك لا
    `feature_key` في سجل المزودين لهذا العقد: المزامنةُ التلقائية تُشعل مفتاحَ **دولة العقد** وهذا
    عقدٌ بلا دولة، والمفتاحُ يجب أن يبقى بيد المشرف — **مطفأً افتراضاً ومكتوباً صريحاً في البذر**.
  - **وللقناة سلكان خلف عقدٍ واحد منذ 2026-08-16** (قرارُ المالك)، يختار بينهما حقلُ `transport`:
    `cloud` — WhatsApp Cloud API الرسمي من ميتا على Graph؛ و`baileys` — **بوابةٌ ذاتيةُ الاستضافة**
    على رقمٍ مخصّص، حاويةٌ مستقلةٌ في compose بلا منفذٍ منشور، لا تكلّمها إلا الخلفيةُ بمفتاحٍ مشترك.
    **والتبديلُ بينهما تعديلُ حقلٍ في صفحة العقود** — لا كودَ ولا نشر.
  - **وكان هذا القسمُ يمنع المكتباتِ غيرَ الرسمية منعاً، فنقضه المالكُ بقرارٍ صريح**، وحجّةُ المنع
    تبقى مكتوبةً لأنها لم تبطل: أتمتةُ حسابٍ بمكتبةٍ غير رسمية مخالفةٌ لشروط واتساب وعقوبتُها حظرُ
    الرقم، **بلا إنذار**. والذي تغيّر هو البديل: الرسميةُ تنتظر عقدَ أعمالٍ وقالباً معتمداً لم يصلا،
    و**حتى يصلا لا أحدَ يستطيع التسجيل أصلاً** — وهو أشدُّ حواجز الإطلاق. فقناةٌ قد تُحظر خيرٌ من
    قناةٍ لا وجودَ لها، **بشرط أن يكون للحظر مخرجٌ مقيس**.
  - **وحرّاسُ القناة الذاتية شرطُ جوازها لا تحسينٌ لها**: رقمٌ **مخصّصٌ** لا يُستعمل في واتساب
    العادي؛ **سقفٌ لكل رقمٍ وسقفٌ بالساعة** يُقاسان في الخلفية على Redis **قبل** أن يصل السلكَ شيء
    (سقفٌ يُفحص بعد الإرسال لا يحمي رقماً)؛ **تباعدٌ عشوائيٌّ** بين الرسائل و**نصٌّ واحدٌ ثابتٌ بلا
    روابط** داخل البوابة نفسِها — فالبوابةُ **لا تقبل نصّاً** من مستدعيها، تأخذ رقماً ورمزاً، ووعدُ
    «بلا روابط» يحرسه من يملك السلك لا من يستدعيه؛ **وحالةُ المصادقة على حجمٍ دائم** تدخل النسخَ
    الاحتياطي مشفّرةً كمفتاح Fernet (هي مفاتيحُ حسابٍ كاملة: من نسخها ربط نفسَه مكانَنا).
  - **وسقوطُ الجلسة حدثٌ يُعلَن لا يُكتشف**: مهمّةٌ دوريةٌ كلَّ دقيقة تقرأ حالَ الجلسة وتُنبّه كلَّ
    مشرفٍ **على تحوّلها** (لا في كل دورة، فتنبيهٌ يتكرر يُتجاهَل). **وخمسُ حالاتٍ لا اثنتان** —
    `linked` · `awaiting_qr` · `disconnected` · `unreachable` · `off` — لأن «غير متصل» لا تقول
    أينتظر إنساناً يمسح رمزاً أم شبكةً تعود، وتسجيلُ المستخدمين واقفٌ طوال ترددِه.
  - **والحكمُ على «أُرسلت» عقدةُ `<ack class="message">` من خادم واتساب، ولا شيءَ سواها**
    (مُصحَّحٌ 2026-08-18 بعد أن كان الحكمُ خاطئاً منذ 08-17). والقاعدةُ الحاكمة: **لا يُحكَم
    بحدثٍ لم يُتحقَّق من أن النظامَ يُصدره أصلاً.** كان المعيارُ `messages.update` بحالةٍ `>= 2`
    وسُمّي «إقرارَ الخادم»، و**Baileys لا تُصدِر هذا الحدثَ عن إقرار الخادم إطلاقاً**: حالتُه لا
    تأتي إلا من عقدة `<receipt>`، وعقدةُ `<ack>` الناجحةُ لا تُصدِر حدثاً. فكان النظام — بحكم
    المصدر لا بحكم الظنّ — **ينتظر إيصالَ تسليمٍ من هاتف المستقبِل**، وهو بعينه ما يمنعه نصُّ
    الملف نفسِه. **فمعادلةُ نجاح إرسال الرمز كانت: أن يكون هاتفُ المستقبِل مستيقظاً خلال عشر
    ثوانٍ** — وهاتفٌ نائمٌ يعني ٥٠٣، **و`otp.issue` تمحو البصمةَ عند فشل الإرسال**، فيصل الرمزُ
    إلى صاحبه وقد أُلغي عندنا قبل أن يقرأه. وهذا وحدَه ما أوقف التسجيل، لا حظرُ الرقم.
    - **وثلاثةُ فروقٍ تُقرأ اليوم منفصلةً بعد أن كانت سطراً واحداً**: `<ack>` بلا `error` نجاحٌ
      (قِيس عند ١٢٢ ثم ٤٨١ مللي ثانية)؛ و`<ack>` بـ`error` **رفضٌ صريحٌ يرتدّ في حينه** لا بعد
      عشر ثوانٍ من صمتٍ لم يقع (وهي الإشارةُ الوحيدةُ التي تعني «واتساب رفض»، وكانت مُرشَّحةً
      خارجاً بشرط `>= 2` لأن قيمتَها `0`)؛ ومهلةُ العشر ثوانٍ بلا أيِّ إقرارٍ تعني القناةَ لا تنقل.
    - **وإيصالُ التسليم يُسجَّل ولا يُحكَم به**: هو الشيءُ الوحيد الذي يثبت وصولاً فعلياً، فيبقى
      في السجل ليومٍ يُشكى فيه من رمزٍ لم يصل؛ لكنَّ انتظارَه يعلّق الرمزَ خلف هاتفٍ لا نملك
      إيقاظه، وذلك شأنُ صاحبه لا شأنُ قناتنا.
    - **والمُعرِّفُ يُولَّد قبل الإرسال** (`generateMessageIDV2`) ويُسجَّل المنتظِرُ قبل خروج
      بايتٍ واحد: الإقرارُ يقع في أعشار الثانية وقد يسبق عودةَ `sendMessage` نفسِها، فيُقرأ
      إقرارٌ وصل «إقراراً لم يصل» — وهو شكلُ العطب نفسِه في نطاقٍ أضيق.
  - **ولا سجلَّ مُخرَس على قناةٍ يُتَّهم فيها الخارج**: كان مُسجِّل Baileys `silent`، فابتلع
    السطرَ الوحيد الذي يقول إن واتساب رفض رسالةً بعينها (`'received error in ack'`) وابتلع معه
    ٤٠٨ استعلامات التهيئة. **وقناةٌ عمياءُ عن العطب المتهَمِ به تُنتج تشخيصاً واثقاً وخاطئاً**:
    قيل «الرقمُ محظور» بلا أن يكون في النظام مسارٌ واحدٌ يستطيع أن يقول ذلك. والمستوى `warn`
    افتراضاً، ويُرفع إلى `trace` بمتغيّرٍ في compose عند التشخيص.
  - **ولا استعلاماتِ تهيئة** (`fireInitQueries: false`): قراءةُ السلك الخام أظهرت أن `props`
    **لا تُجاب أبداً** (ثم ٤٠٨ بعد ٦٠ث) بينما `blocklist` و`privacy` والنبضُ كلُّها مُجابة —
    فالمقبسُ سليم، والمعلَّقُ استعلامٌ لا نقرأ جوابَه أصلاً على قناةٍ ترسل ولا تقرأ. **وهذا ينفي
    نسبةَ ابتلاع الرسائل إليه**: على المقبس نفسِه والـ`props` معلّقة، أُقِرَّت رسالةٌ في ١٢٢ مللي.
  - **وخطةُ الطوارئ مكتوبةٌ بمددها في `CLAUDE.md`**: ما يُفعل عند كل شكلٍ من أشكال السقوط، وكم
    يستغرق التحويلُ إلى القناة البديلة. وأثقلُها — حظرُ الرقم — مخرجُه تعديلُ حقلٍ في دقيقتين،
    ولا شيءَ في المنصّة يتعلق بذلك الرقم غيرُ إرسال الرموز.
  - **وقوالبُ المصادقة لا تقبل نصّاً حرّاً**: القالبُ معتمدٌ عند ميتا مسبقاً وما نرسله **مُعامِلٌ
    واحد هو الرمز**. ولذلك صار عقدُ الإرسال في `services/otp.py` هو `OtpSender.send_code` لا
    `send(body)`: القناةُ تعرف كيف يُصاغ الرمز فيها، وواجهةٌ تأخذ نصّاً تكذب على مستدعيها.
  - **وسقوفُ طلب الرمز سياسةُ حسابٍ لا خاصيةُ قناة** (قرارُ المالك 2026-08-16،
    `otp_settings` per-country و`services/otp_limits.py`): تُقاس على **الرقم
    المطلوب** فتسري على واتساب والرسائل معاً — ومن استنفد محاولاته لا يلتفّ
    عليها بتبديل القناة. **وFirebase خارجَها بحكم شكله**: الرمزُ يُرسل من جهاز
    المستخدم ولا يمرّ بنا، فليس لنا فيه ما نعدّه.
  - **وثلاثةُ سقوفٍ لا واحد**، وكلٌّ يمنع ما لا يمنعه غيرُه: **نافذةٌ قصيرة**
    (رشقٌ آليّ)، **ويوميّ** (من ينتظر الساعةَ ثم يعود)، **وعمرُ التسجيل** — وهو
    الذي **لا يُشترى بالصبر**: من طلب عشرةَ رموزٍ على رقمٍ ولم يُكمل تسجيلاً
    مرةً ليس مستخدماً متعثّراً، «فالتسجيلُ حدثٌ مرةً لا حدثٌ متكرر». ويُصفَّر
    الثالثُ **بإنشاء الحساب** لا بمرور الوقت، من `create_account` وحدَها.
  - **ومهلةُ الإعادة تتزايد** (٣٠ث ← دقيقة ← دقيقتان…) بسقفٍ مكتوب: مهلةٌ
    تتضاعف بلا حدٍّ تبلغ ساعاتٍ فتصير منعاً دائماً **لم يقرّره أحد**. وبعد
    الاستنفاد **انتظارٌ مُعلَنٌ برقمه** — وكلُّ رفضٍ يحمل `retry_after` يرسمه
    التطبيقان عدّاداً، فلا زرَّ يُضغط ولا يفعل شيئاً.
  - **والعدُّ على الرقم لا على IP**: عشراتُ المستخدمين خلف شبكةٍ واحدةٍ لا يخنق
    بعضُهم بعضاً، والمسيءُ يُلاحَق وحدَه — قاعدةُ سقف بثّ الموقع نفسُها.
    **وأصفارُها تعني «لا سقف» وتُكتب صراحة**: هذه حرّاسٌ لا ميزات، فانفتاحُها
    قرارٌ مكتوبٌ لا سكوت — وهو عكسُ «غيابُ الصف = ميزةٌ معطّلة» ولنفس سببها.
  - **ويرى المشرفُ من استنفد اليوم** في شاشة الإعدادات: تكرارٌ مشبوهٌ يُرى
    **قبل** أن يحرق رقمَ الإرسال لا بعده.
  - **وفشلُ القناة الأولى لا يُبدّلها في صمت**: الرمزُ ربما وصل فعلاً، وتبديلٌ صامتٌ يجعل صاحبَه
    يقرأ رمزاً من قناةٍ ويكتب رمزاً من أخرى فيُحرق الرمزان. فيرتدّ الطلب بـ502
    `verification_send_failed` حاملاً **`fallback_channel`** — القناةَ التالية المتاحة — والتطبيقُ
    يرسم زرَّها، وغيابُها (`null`) جوابٌ صادقٌ يعني ألّا مخرجَ آخر ومكانَ إصلاحه صفحةُ العقود.
    **ورفضٌ بلا مخرجٍ ليس رفضاً.**
  - **والتحققُ من الرمز بابٌ واحد لكل القنوات**: البصمةُ محفوظةٌ **بالرقم لا بالقناة**، فمن أُرسل
    إليه في واتساب ثم ارتدّ إلى الرسائل يُقبل رمزُه. ولو ارتبط التحققُ بالقناة لبطل الارتدادُ نفسُه.
- **إشعارات Push (FCM)** — قبلها: WebSocket أثناء فتح التطبيق يكفي
- **تكامل CliQ الآلي** (QR ديناميكي عبر merchant acquirer) — قبله: تدفق كليك اليدوي الموصوف في القسم 6
- **payout API آلي للسحوبات** — قبله: التحويل اليدوي الموصوف في القسم 9

**نُفِّذت الأربعة في المرحلة 8.** القرارات التي لم يحسمها النص أعلاه:

- **لكل عائلة مزودٍ نفس البنية الثلاثية** التي أرستها `card_gateway`: `base.py` عقدٌ مجرّد،
  و`mock.py` مزودٌ وهمي يُشغَّل بحقل `use_mock` في العقد و**يُتجاهل في الإنتاج**، وملفٌّ واحدٌ
  للمزود الحقيقي هو الوحيد الذي يعرف أسلاكه، و`__init__` نقطةَ قرارٍ تقرأ العقد من
  `provider_credentials` مشفَّراً. فتفعيلُ الميزة إدخالُ عقدٍ لا نشرُ كود.
- **FCM وحده وصل عقدُه الحقيقي، وجُرِّبت سلسلتُه كاملةً على جهاز**: ملفُّ حساب الخدمة يُقرأ من مساره
  في `.env.local` مرةً واحدة بـ `python -m scripts.seed` فيُكتب **مشفَّراً** في `provider_credentials`
  ولا يُقرأ من القرص بعدها (نفس ما يجري لتوكنات Mapbox). والحلقاتُ الأربع مُتحقَّقٌ منها فعلياً على
  مشروع `taxo-84a5f`: توقيعُ JWT بمفتاح حساب الخدمة، ومبادلةُ توكن OAuth، ونداءُ `messages:send`
  المُصادَق عليه، و**وصولُ الإشعار إلى جهازٍ حقيقي** — عاديَّ الأولوية وعاليَها (مسار بطاقة الطلب)
  كلاهما. أداةُ التجربة `tools/fcm-token/` تُخرج رمز جهازٍ من المتصفح، ومعها
  `POST /admin/campaigns/test-push` الذي يرسل إلى رمزٍ يكتبه المشرف — مِجَسٌّ يتخطى جدول الأجهزة
  وقاعدة الاستثناء عمداً، غرضُه إثباتُ السلسلة قبل أن يوجد تطبيقٌ يسجّل أجهزته.
  - ما جرى في التجربة يؤكد قاعدة الاستثناء نفسها: الصفحة كانت مفتوحة فوصل الإشعاران إلى
    `onMessage` **ولم يعرضهما المتصفح على الشاشة** — وهو بالضبط ما تفعله الخلفية حين ترى سوكتاً
    نشطاً لجهاز. وعرضُ الإشعار من عامل الخدمة والتطبيقُ مغلق لم يُرصد بعينه.
  - **المرحلة 9 بنت طرفَه الناقص ولم ترصده بعد**: `customer-app/public/firebase-messaging-sw.js`
    يعرض ما يصل والتطبيق مغلق، وتطبيق الراكب يسجّل جهازه بعد الدخول ويحذفه عند الخروج. تبقى
    التجربة على جهازٍ حقيقي بعقدٍ مُدخل — لا يُقال إنها جرت قبل أن تجري.
- **ولا عقد مُسلَّم للثلاثة الباقية في جلسة التنفيذ**، فأسلاكُ الحقيقيّين (SMS، CliQ acquirer، payout)
  مبنيّةٌ على الشكل الشائع لتلك الواجهات و**تحتاج مطابقةً بالتوثيق قبل أول تشغيل حقيقي** — مجموعةً في
  ثوابت مسمّاة أعلى كل ملف، كما فُعل مع `telr.py` في 6-ب. وخطؤها لا يفتح باباً: إرسالٌ فاشل يرتدّ
  ولا يُنشئ حساباً، والدفتر لا يتحرك إلا على جوابٍ صريح من المزود للخلفية. **المزودون الوهميون هم ما
  بُنيت عليه المرحلة واختُبرت.**
- **OTP تحقّقٌ لا دخول، والدخول كلمةُ مرور دائماً** (`services/verification.py`). كان القرار ثلاثياً
  يختار **طريقة الدخول** من حالة العقود، فصار الدخول ثابتاً والعقودُ تحكم **التحقق** وحده. والسبب أن
  طريقتَي دخولٍ تعنيان جوابين متناقضين لسؤال «كيف أدخل»: حسابٌ أُنشئ تحت OTP لا كلمةَ مرور له فلا
  يدخل حين يُطفأ العقد، وحسابٌ أُنشئ تحت كلمة المرور لا يُسأل عنها حين يُفعَّل.
  - **إثبات الرقم يقع مرتين لا أكثر**: عند التسجيل (ثم يضع صاحبه كلمة مروره)، وعند الاستعادة.
  - **`OtpAuthStrategy` حُذفت كطريقة دخول ولم يُحذف كودُها**: مزود SMS التقليدي تغيّر دورُه إلى
    **المُحقِّق الثاني**، فبقي `services/otp.py` بكامله — توليدُ الرمز وبصمتُه وعدّادُ المحاولات
    ومهلةُ الإرسال — يخدم نفس الحدثين حيث لا يعمل Firebase أو حيث عقدٌ محليٌّ أرخص.
  - **ترتيب المُحقِّقين ثابت** (Firebase ثم SMS): Firebase أقوى إثباتاً وأرخص تشغيلاً — لا رسائل ندفع
    ثمنها ولا recaptcha نبنيه. ولا يُقرأ الترتيب من إعداد: مفتاحٌ يقول «أيّهما أولاً» حالةٌ ثانية
    قابلة للاختلاف عن حالة العقود، ومن أراد الثاني يُطفئ عقد الأول.
- **الاستعادة مسارٌ منفصل، ولا توكن دخولٍ يُصدر بمجرد التحقق** بل بعد كتابة كلمة المرور فعلاً: لو
  انقطع الطلب بعد الإثبات لم يبق للمهاجم شيء. وتُبطَل **كل جلسات** صاحبها عند التغيير — كلمةٌ تُغيَّر
  لأن القديمة تسرّبت لا تُغيّر شيئاً إن بقيت جلسةُ من سرّبها مفتوحة. وسقفُ محاولاتها **يومي لا ساعي**:
  محاولةُ الاستيلاء على حسابٍ بعينه لا تُستعجل، ونافذةٌ ساعيةٌ تعطي المهاجم مئةً وعشرين محاولة في اليوم.
- **Firebase: قسمةُ عملٍ لا نقلُ ثقة على عمياء.** التطبيق يتحقق من الرقم عندهم ويرسل رمز الهوية،
  والخلفية تفحص فيه أربعة أشياء قبل أن تصدّقه (`services/firebase_auth/google_certs.py`):
  **التوقيع** بمفاتيح Google العامة (RS256، و`alg: none` مرفوضٌ قبل أي نظرٍ في المحتوى)، و**`aud`
  و`iss`** لمشروعنا وحده — فرمزٌ صحيح التوقيع من مشروعٍ آخر رمزٌ **صالح**، وقبولُه يعني أن أي مشروع
  Firebase في العالم يفتح حساباتنا — و**`exp`/`iat`**، و**`sign_in_provider = phone`** فدخولٌ بحساب
  Google أو بمجهول ليس إثباتاً لملكية رقم. ثم **يُطابَق هاتف الرمز بالهاتف المُرسَل**: بغيره يرسل من
  يملك رقماً رمزَه الصحيح مع هاتفِ غيره فيدخل حسابه — والرمز صحيحٌ فعلاً، وهذا ما يجعل الثغرة غير
  مرئية في السجل. والصيغتان E.164 كلتاهما فالمقارنة نصّية مباشرة.
- **عقد `firebase_auth` مستقل عن `fcm`** وإن كان المشروع واحداً: التحقق لا يحتاج مفتاح حساب الخدمة
  أصلاً (يكفيه `project_id` ومفاتيح Google العامة)، فلا يحمل عقدُ الدخول سرّاً لا يستعمله؛ وإطفاءُ
  الإشعارات قرارٌ لا يجوز أن يُطفئ الدخول معه. و`project_id` يُنشر في `GET /config` — التطبيق يحتاجه
  ليتحقق من الرقم أصلاً، وهو معرِّفٌ عام كتوكن Mapbox العام.
- **ولا `firebase-admin`**: الكود يفعل ما تفعله الحزمة في `verify_id_token` حرفاً بحرف، بـ PyJWT
  وcryptography الموجودتين — اتساقاً مع `push/fcm.py` الذي يوقّع ويبادل توكن OAuth بيده، وتفادياً
  لجرّ `google-cloud-*` و`grpcio`. وشهاداتُ Google تُخزَّن بالعمر الذي يقوله `Cache-Control` نفسه:
  جلبُها لكل دخول يجعل الدخول رهينةَ شبكةٍ ثالثة، وتثبيتُها أبداً يكسره يوم تُدوَّر المفاتيح.
- **مسار `/auth/challenge` وبصمةُ الرمز يبقيان** لمزود SMS التقليدي؛ ولا يمر بهما تدفّق Firebase
  (الرسالة تُرسل من جهاز المستخدم)، فيعود المسار بـ `sent=false` بدل خطأ — الواجهة تسأل دائماً ولا
  تحتاج أن تعرف الاستراتيجية قبل أن تسأل.
- **OTP: الحقل نفسه لا مسارٌ جديد.** `password` في `/auth/login` و`/auth/register` يصير معناه «رمز
  التحقق»، وتعرف الواجهةُ ذلك من `GET /auth/method`. ومسارُ `POST /auth/challenge` قائمٌ في الوضعين
  ويعيد `sent=false` قبل تفعيل العقد، فلا تحتاج الواجهة أن تعرف الاستراتيجية قبل أن تسأل. الرمز
  يُحفظ **بصمةً** (HMAC) لا نصاً، ويُستهلك عند النجاح، ويُحرق بعد خمس محاولات، وله مهلةُ إعادة إرسال
  وسقفان على الرقم وعلى الـ IP. وحسابُ OTP بلا `password_hash` — والعمود nullable لهذا بالضبط.
  وسياسةُ طول كلمة المرور انتقلت إلى `PasswordAuthStrategy`: سقفٌ في طبقة النقل يمنع رمزاً من ستة
  أرقام يعطّل الطريق الآخر بدل أن يحرس هذا.
- **CliQ الآلي نطاقه شحن المحفظة وحده** (القسم 7): حساب التاجر يشهد على ما يدخل حساب الشركة، ودفعُ
  الرحلة بكليك يذهب إلى alias الكبتن فلا شاهد له (القسم 6). وطلبُ الشحن يسكن `provider_orders` لا
  `wallet_topup_requests` لنفس سبب شحن البطاقة في 6-ب: ذاك الجدول بيتُ ما ينتظر **إنساناً**.
  والقناة اليدوية تبقى قائمة بجانبه — مزوّدٌ متوقف لا يقطع قناة شحنٍ كاملة.
- **payout يحل محل يد المحاسب لا حكمه**: الطلب يظل يمر بـ `approved`، و`paid` لا تُعلَّم إلا إن قال
  المزود «حوّلت» — وما دون ذلك يبقى `approved` بلا قيدٍ في الدفتر، ورصيدُ الكبتن ما زال محجوزاً
  بطلبه فلا يضيع ولا يُصرف مرتين. **وكليك وحدها**: لا عمود لحسابٍ بنكي في `drivers`، ووجهةٌ نخترعها
  حوالةٌ إلى لا مكان — فالحوالة البنكية تبقى يدوية.
- **زرّ «اختبار الاتصال» لا يترك أثراً**: لا طلبَ دفعٍ يُفتح ولا حوالةً تُرسل، ولا رسالةً مدفوعة إلا
  إن كتب المشرف رقماً صراحةً. ويعمل على عقدٍ **غير مفعّل** — يُختبر العقد قبل أن يُفتح لا بعد.
  وفشلُه جوابٌ يُقرأ (200 بـ `ok=false` ونصِّ السبب) لا شاشةُ خطأ. و`last_tested_at` يُكتب في
  الحالتين: السؤال «متى اختُبر» لا «متى نجح».

### ب) ميزات منتجية — خلف feature flags (معطلة افتراضياً، تفعيل من اللوحة)
- الرحلات المجدولة
- مشاركة الرحلة بين ركاب
- الكوبونات والعروض
- تسعير ديناميكي (surge)

### ج) خطوات تشغيلية لاحقة (ليست كوداً يُبنى الآن)
- تغليف Capacitor والنشر في المتاجر

---

## 16. خطة التنفيذ للوكيل (بالترتيب — مهمة واحدة في كل جلسة)

1. **الأساس:** docker-compose (postgres+postgis, redis) + هيكل FastAPI + Alembic + جداول users/drivers/vehicles + مصادقة (هاتف + كلمة مرور) / JWT — عبر auth service بواجهة موحدة تتحول لـ OTP عند تفعيل مزود SMS
2. **الإعدادات:** pricing_rules + feature_flags + commission_settings + subscription_plans + **جدول provider_credentials المشفر مع CRUD أساسي وseed لتوكنات Mapbox وTelr Sandbox** + CRUD إداري للبقية
3. **الرحلات:** rides + التسعير عبر Mapbox Directions + endpoints الطلب والحالات
4. **التوزيع والتتبع:** Redis GEO + خوارزمية الإسناد + WebSockets كاملة
5. **المحافظ:** ledger موحد + محفظة الراكب (شحن/تحويل) + محفظة الكبتن
6. **الدفع** — **مقسَّمة إلى جلستين على حدّ «المزوّد الخارجي»**: كل ما في 6-أ قابل للاختبار محلياً
   بالكامل، وتلر وحده يُدخل متغيرات خارجة عن السيطرة (بيانات Sandbox، توقيع webhooks، شبكة، تنسيق ردود
   المزود). خلطهما يعني أن تعثراً في إعدادات تلر يعطّل مراجعة منطقٍ مالي سليم. وبعد 6-أ يصير نظام الدفع
   عاملاً فعلاً وكافياً لتجريب ليبيا (كاش فقط)، فتصير تلر إضافةً على نظام مثبت لا شرطاً لتشغيله.

   - **6-أ (منجزة):** جداول `payments` (بحقول فصل النزاع و`idempotency_key` و`transaction_id`) و
     `ride_route_points` و`saved_cards` و`ratings` + عمود `rides.actual_distance_km` (ترحيلة `0007`)
     — كاش + كليك اليدوي + المحفظة + **الدفع المختلط** + **النزاعات وفصلها من اللوحة** + قيود
     `ride_earning`/`commission` + **تخزين نقاط مسار الرحلة** وإعادة حساب `final_fare` على المسافة
     الفعلية (القسم 5.7) + **`ratings`: تقييم متبادل بعد `completed` (endpoint لكل طرف) وتحديث
     `drivers.rating_avg`** — التقييم يلي الدفعَ مباشرة في تدفق القسم 5، فمكانه هنا لا ضمناً في مرحلة
     الواجهات
   - **6-ب (منجزة):** تكامل **Telr** — جدول `provider_orders` (ترحيلة `0008`) + إنشاء صفحة الدفع،
     الـ webhook والتحقق من توقيعه (القسم 14)، شحن المحفظة بالبطاقة شحناً فورياً آلياً (القسم 7)،
     ملء `saved_cards` بالـ tokenization والدفع بضغطة (القسم 6.4)، والاسترداد عبر المزود. القناة
     `card` كانت في ENUM وشاشة الدفع منذ 6-أ وترد بـ 501؛ صارت الآن تردّ 403 حين لا يكون عقد Telr
     مفعّلاً (قرارٌ إداري لا نقصُ تنفيذ) و503 حين يُرفع المفتاح بلا عقد.
     **بيانات Sandbox لم تكن متوفرة، فبُنيت المرحلة واختُبرت على المزود الوهمي** المُدار من عقد
     Telr نفسه (القسم 15/أ)؛ تفاصيل أسلاك Telr الثلاثة التي تحتاج مطابقةً بالتوثيق الرسمي مذكورة
     في القسم 6 وفي رأس `services/card_gateway/telr.py`
7. **الاشتراكات (منجزة):** الخطط + الشراء + فحص التوزيع + Celery للانتهاء — جدول
   `driver_subscriptions` وغرض `subscription` في `provider_orders` (ترحيلة `0009`)، الشراء بالقنوات
   الأربع (المحفظة والبطاقة من تطبيق الكبتن، والكاش وكليك تسجيلاً من اللوحة بعد القبض)، شرطُ
   الاشتراك الساري في `dispatch.eligible_driver_ids` وخريطة الراكب معاً، وخدمتا Celery
   (`worker`/`beat`) بمهمة كنسٍ كل خمس دقائق تعلّم المنتهي وتُخرج صاحبه من التوزيع وتنبّه من يقترب
   انتهاؤه. قرارات التنفيذ في القسم 8
8. **استكمال طبقة المزودين (منجزة):** صفحة العقود الكاملة (بطاقات + **اختبار اتصال** + تقنيع) +
   واجهات مزود موحدة مع mock providers (`services/sms`، `services/push`، `services/cliq`،
   `services/payout` — كلها على بنية `card_gateway` نفسها) + التكاملات الأربعة عاملةً وتنتظر بيانات
   العقود: **OTP** (تدفق التحدي والتحقق وحساباتٌ بلا كلمة مرور)، و**Push** (ترحيلة `0010`:
   `device_tokens` و`notification_campaigns` و`notification_deliveries` و`notification_settings`
   وعمود `users.marketing_push_enabled`؛ معاملاتيٌّ مع كل بثّ WebSocket، وحملاتٌ على دفعات بمهمة
   Celery)، و**CliQ الآلي** لشحن المحفظة (قيمة `cliq_acquirer` في `payment_provider` وعمود
   `provider_orders.qr_payload`)، و**payout** للسحوبات. قرارات التنفيذ في القسم 15/أ
   - **ما بقي للمرحلة 11:** واجهةُ صفحة الحملات في اللوحة (المسارات وسجلُّ التسليم قائمان)
9. **تطبيق الراكب (منجزة):** `customer-app/` — React 18 + Vite + TypeScript + Tailwind بـ RTL
   كامل، shadcn/Drawer (`vaul`) للأوراق السفلية، Framer Motion، Lucide، خط IBM Plex Sans Arabic،
   وMapbox GL JS بستايلٍ نهاري وليلي. الشاشات الثماني في القسم 11 كلها على المسارات القائمة، ومنها
   **صفحة دفع كليك داخل التطبيق** (الرمز والرابط وإدخال المرجع — القسم 6). وأضافت المرحلة إلى
   الخلفية ما كانت تلك الصفحة تنتظره: ترحيلة `0013` بحقول كليك الأربعة على `payments`، و
   `services/cliq_qr.py`، و`POST /payments/{id}/cliq-reference` بإشعارٍ فوري للكبتن. قرارات
   التنفيذ في القسم 6 وأدناه:
   - **الحقول العامة لتطبيق الويب لدى Firebase أُضيفت إلى عقدَي `firebase_auth` و`fcm`**
     (`api_key`, `auth_domain`, `app_id`, `sender_id`, `vapid_key`) وتُنشر في `GET /config`. حزمة
     Firebase على الجهاز لا تعمل بغيرها — لا تدفّقَ تحققٍ ولا رمزَ جهاز — وهي عامّةٌ بطبيعتها
     تُنشر في حزمة كل تطبيق ويب، لكنها **قيمُ مشروعٍ بعينه** فمكانها العقد لا ملفٌّ في الواجهة
     (القسم 14). وتكرارُها في العقدين ثمنُ استقلالهما الذي يقرره القسم 15/أ: مُحقِّقٌ عبر مزود SMS
     مع إشعاراتٍ عبر FCM حالةٌ يكون فيها عقد الدخول مطفأً تماماً والتطبيق ما زال يحتاج الإعداد.
   - **«مشاركة الرحلة (رابط)» تُنفَّذ بورقة مشاركة النظام لا برابط تتبعٍ عام**: الرابط العام يحتاج
     رمزاً على `rides` يفتحها لمن لا حساب له، وهو عمودٌ وعقدُ صلاحياتٍ لم يصفهما هذا المستند —
     وكل مسارٍ يلمس رحلةً يتحقق من ملكيتها (القسم 14). فالمشاركة تحمل ما يطمئن المنتظِر: اسم
     الكبتن ولوحته والوجهة على الخريطة.
   - **الخط بين نقطتي الرحلة مستقيمٌ متقطّع لا مسار طريق**: هندسة المسار تأتي من Mapbox Directions
     ونداؤها من الخلفية حصراً (القسم 2/14)، وما يصل الواجهة مسافةٌ ومدةٌ وسعر. وخطٌّ نرسمه من
     عندنا يوهم بمسارٍ لم يقله أحد.
   - **عامل الخدمة لا يلمس `/api/` إطلاقاً**: رصيدٌ أو حالةُ رحلةٍ من كاشٍ عمرُه دقيقة كذبةٌ على
     شاشةٍ مالية. الكاش لقشرة التطبيق وحدها.

   **9-ب (منجزة) — خلفيةٌ بلا واجهات، شُقّت عن المرحلة 10 لأنها شرطُها لا جزؤها.** كشفت مقارنةُ
   تصاميم الحزمة بالمشروع أن شاشتين في تطبيق الكبتن (والراكب) تصفان ميزتين غير موجودتين أصلاً:

   - **رفع المستندات ومراجعتها.** الجدول `driver_documents` قائمٌ منذ المرحلة 1 بكل حقوله ولا مسارَ
     واحد يكتبه أو يقرؤه، ولا تخزينَ ملفاتٍ في المشروع إطلاقاً — فالبند 12/1 والبند 13/2 كانا نصّاً
     بلا تنفيذ، والاعتماد يمر والمستنداتُ فارغة. أضافت المرحلة: `core/storage.py` (تحقّقٌ من النوع
     بالتوقيع الثنائي، وسقفُ حجمٍ يُفرض بالقراءة لا بترويسة العميل، واسمُ ملفٍ من عندنا)، و
     `services/documents.py`، ومساراتِ الرفع والقراءة للكبتن والمراجعةِ للوحة، وحارسَ الاعتماد
     الثاني.
   - **صندوق وارد الإشعارات** `user_notifications` — أيقونةُ الجرس في التطبيقين كانت تصف سجلاً لا
     وجود له.

   ولم يُبنَ `ride_offers` (سجلُّ العروض الذي يجعل «معدّل قبول الطلبات» و«زمن القبول» قابلَين
   للحساب): كتابةٌ على المسار الحرج للتوزيع ومهلتُه عشرون ثانية. الحلُّ حين يُطلب المؤشّران كتابةٌ
   غير متزامنة عبر Celery خارج مسار العرض.
10. **تطبيق الكبتن** — ومنه **بطاقة تأكيد حوالة كليك** («وصلني» / «لم يصلني») بمهلة تأكيد (القسم 6)
10-ج. **خدمة التوصيل النسائي** — خلفيةً وتطبيقين. أُنجزت **الخلفية**: حقول القسم 4 أعلاه،
    والمطابقة ثنائية الاتجاه داخل شرط الأهلية (القسم 5.3)، والمدى الأوسع للطلب المجنَّس،
    والإلغاء بلا رسوم بسبب `gender_mismatch` مع عدّاد البلاغات، و`PUT /admin/drivers/{id}/gender`
    بقيدِ تدقيق، ونشرُ المفتاح في `/config`. وأُنجزت **السِمة الوردية**: رمزٌ واحد `--brand`
    في `DESIGN.md` §1.1-ب وفي ملفات `tailwind.config.js` الثلاثة، ومفتاحٌ في إعدادات التطبيقين
    مفتوحٌ افتراضاً لمن جنسُها أنثى — ولا يظهر أصلاً حيث الخدمة مطفأة. وأُنجزت **شاشاتُ
    التطبيقين**: اختيارُ التفضيل في شاشة الطلب وفي الملف عند الراكبة مع قولِ ثمنِ الانتظار قبل
    الضغط لا بعده، وتفضيلُ الكبتن الدائم وشارةُ «طلب نسائي» على بطاقة العرض، واقتراحُ مشاركة
    الرحلة عند بدء الرحلة النسائية. ثم وصل **تصميمُ الشاشات النسائية** بعد البناء، فطوبقت عليه:
    رموزُ السِمة صارت أربعةً (`--brand-soft` و`--brand-brd` معهما) والوردي النهاري `#d81b73`،
    وأُضيف **إعلانُ الجنس** في التسجيل والملف — وبغيره كانت الخدمة كلُّها غيرَ قابلةٍ للوصول —
    و**ورقتا أسباب الإلغاء** في التطبيقين، وبغيرهما لا مسارَ لـ`gender_mismatch` أصلاً،
    و«أقبل أي كبتن» بعد `no_driver_found`، وشارةُ الرحلة النسائية عند الراكبة، وشريطُ التفضيل
    في رئيسية الكبتن، وبطاقةُ توثيق الجنس في حسابه. **وما بقي**: مراجعةُ أجناس الكباتن
    المعتمدين المتراكمين، وهي شرطُ إشعال المفتاح — وثلاثةُ بنودٍ في `FUTURE-FEATURES`
11. **لوحة الإدارة** — ✅ **مُنجزة**. `admin-panel/` على 5175 بنفس نظام التصميم وحارسَيه:
    الهيكلُ (رأسٌ من صفّين ومبدّلُ دولةٍ وحارسُ جلسة) والدخولُ **برقم الهاتف**، و**السائقون
    والوثائق** (القسم 13/2)، و**المالية**: طلبات السحب وشحنات كليك (13/5)، و**النزاعات** (13/4)،
    و**حملات الإشعارات** (13/9)، و**الإعدادات per-country** (13/6)، و**عقود مزوّدي API** (13/7).
    وأتمّت **الجلسة الأخيرة** الأقسامَ السبعة الباقية من القسم 13: **سجل الرحلات** (13/4)
    و**الركّاب** (13/3) و**الاشتراكات والباقات** (8 و13/5) و**التسعيرة** (13/6) و**التقارير
    والإحصاءات** (13/5) و**المستخدمون والصلاحيات** (13/8) و**سجل التدقيق** (القسم 14) — فلم يبق
    في القائمة عنصرٌ بلا شاشة. وآليةُ «قريباً» باقيةٌ في `Shell.tsx` لأقسام المرحلة 12.
    - و**«نظرة عامة»** بُنيت فوق `GET /admin/stats/overview` (بنافذةٍ من ثلاث ودولةٍ إلزامية):
      **التجميع في الخلفية حصراً** كما يحصر القسم 14 الحساب المالي فيها — لوحةٌ تجمع صفوفاً بيدها
      تعرض رقماً لا يطابق القاعدة حين تُقصّ الصفحة. و**اليومُ يومُ الدولة** بمِنطقتها المخزَّنة في
      `notification_settings.timezone`، فلا يقصّ خادمُ UTC ثلاث ساعاتٍ من أول اليوم في عمّان.
      و**المتصلون الآن من مفتاح الحضور في Redis لا من `drivers.is_online`**: العمود يقول «رفع
      المفتاح» لا «حاضرٌ الآن»
    - و**«الخريطة الحيّة»** فوق `GET /admin/live/map` — **لـ admin حصراً** وفتحُها يدخل سجل
      التدقيق (البند 1 أعلاه). الموقعُ من Redis والهويةُ من القاعدة، ولا فلترةَ أهليةٍ فيها
      خلافاً لخريطة الراكب: المشرف يريد **من في الشارع** بمن فيهم من في رحلةٍ أو انقضى اشتراكه.
      ومسارُ الراكب `GET /drivers/nearby` وإطارُ `nearby_drivers` في المقبس لم يُمسّا، ويحرس ذلك
      اختبارٌ يرى الكبتن نفسَه باسمه في اللوحة وبمعرّفٍ مستعارٍ عند الراكب
    - وأضافت جلسةُ السائقين إلى الخلفية ما كانت صفحتُهم تنتظره: `GET /admin/drivers` (قائمةٌ
      بحالها وعدِّ مستنداتها **في استعلامٍ واحد** — صفحةٌ من خمسين كبتناً لا تصير خمسين نداءً)،
      و`POST /admin/drivers/{id}/suspend` بسببٍ إلزامي، و`/activate` الذي **يمر بحارسَي الاعتماد
      نفسِهما**: لولا ذلك لصار الإيقافُ ثم التفعيل طريقاً للالتفاف على شرط المستندات
    - و**زرُّ توثيق الجنس صار له بابٌ في صفحة السائقين** (المرحلة 10-ج كانت قد بنت مساره ولم
      تبنِ له زرّاً — **قاعدةٌ بلا باب**، وهي أخطرُ من قاعدةٍ ناقصة لأنها تُقرأ منجزةً في
      الاختبارات): عمودُ الجنس في القائمة، ومفتاحُ فرزٍ **مستقلٌّ عن حبّات الحالة** يفتح
      `?gender_verified=false`، وبطاقةٌ في الدرج تكتب `PUT /admin/drivers/{id}/gender`. وهي
      أصغرُ قطعةٍ في هذه المرحلة وأكبرُها أثراً: بها **يُفرَّغ متراكمُ المعتمدين** الذي يبقى
      `women_service_enabled` مطفأً حتى يُفرَّغ. وغيرُ المختوم يُعرض «لم يُثبَّت» لا بجنسٍ
      مكتوب — المطابقةُ تقرأ المختوم وحده، وعرضُ قيمةٍ بلا ختمٍ يجعلها تبدو معتبَرة.
      و**المفتاحُ نفسه صار في شاشة الإعدادات** ولم يكن: `FeatureKey` في اللوحة كان ينقصه
      `women_service_enabled`، فلم يكن للمالك بابٌ يشعل به الخدمة بعد تفريغ المتراكم — نفس
      شكلِ العطب الذي كشفته المرحلة 10-ج، **قاعدةٌ بلا باب**
    - وأضافت الجلسةُ الأخيرة إلى الخلفية ما كانت الشاشاتُ السبع تنتظره:
      - **`GET /admin/rides` و`/admin/rides/{id}`** (`services/ride_log.py`) — قراءةٌ لا غير:
        لا مسارَ فيها يغيّر حالة رحلة، فكلُّ انتقالٍ يمر بـ`services/rides.py` وحده. وحالُ
        الدفع **مجموعةٌ في القاعدة** لا في اللوحة، و**في استعلامٍ ثانٍ لا في ضمٍّ**: رحلةٌ
        بدفعتين تصير صفّين في الضمّ فتُعدّ مرتين في صفحةٍ مسقوفة. والتفاصيلُ وحدها تحمل
        **نقاط المسار** — دليلَ النزاع (القسم 5.7) — مسقوفةً بألفٍ ومع `route_truncated`
        صريحة: مسارٌ قُصّ ذيلُه يُقرأ كاملاً دليلٌ يكذب
      - **`POST /admin/users/{id}/block|unblock`** بسببٍ إلزاميٍّ للحظر يدخل التدقيق (13/3).
        **`admin` لا `support`**، و**لا إبطالَ جلسات**: `core/deps` يقرأ العمود في كل طلب
        فيسري الحظرُ على التوكن القائم — وإبطالٌ هنا كان سيوهم أن الحماية منه. **وحسابُ
        الموظفين خارج هذا الباب**: القسم 13/3 عن الركّاب، ولا يُغلق به مشرفٌ على زميله
      - **`GET /admin/stats/reports`** — نِسَبٌ ومتوسطاتٌ **محسوبةٌ في الخدمة**: بسطُ كلٍّ منها
        ومقامُه مجموعان على الجدول كلِّه، وقسمةُ رقمين مسقوفَين في الواجهة تعطي متوسطَ الصفحة.
        وفيه تقاريرُ الاشتراكات (13/5) بما **بيع في الفترة** لا بما هو سارٍ الآن
      - وفلاترُ **سجل التدقيق** (`action`/`actor_id`/`entity_id`/`offset`) و**اسمُ الفاعل
        مضموماً** في نفس الاستعلام، وفلاترُ بحثٍ (`q`) على الحسابات والسائقين
    - **وثلاثةُ أشياء يرسمها التصميم ولم تُبنَ عمداً**، وكلُّها تُبنى يومَ يصفها هذا المستند:
      - **مصفوفةُ الصلاحيات مقروءةٌ لا تُنقر**، وأدوارُها **دوران لا أربعة**: `UserRole` فيه
        `admin` و`support` وحدهما ويفرضهما `core/deps` على كل مسار. وخليةٌ تُنقر كانت ستصير
        مصدرَ حقيقةٍ ثانياً يخالف الحارس — ومشرفٌ يظن أن ما أطفأه مُطفأ أخطرُ ممن يعرف أنه
        لا يستطيع
      - **لا «دعوة مستخدم»**: الحساب يُنشأ بإثبات ملكية رقمٍ من التطبيق (القسم 15/أ)، ولا مسارَ
        في المشروع يفتح حساباً بغيره
      - **ولا «أعلى المناطق طلباً» ولا «معدّل قبول الطلبات»** في التقارير: لا جدولَ مناطق في
        هذا المخطط ولا عمودَ منطقةٍ على الرحلة، ولا `ride_offers` يسجّل العروض (16/9-ب) —
        ورسمُ أيٍّ منهما من بياناتٍ أخرى قياسُ شيءٍ وتسميتُه باسم آخر
12-أ. **ترحيل تطبيق الراكب إلى نظام التصميم** — ✅ **مُنجزة**. مرحلةٌ مستقلة، وسببُ استقلالها أنها لا تخصّ
    ميزةً بعينها. `customer-app` بُني في المرحلة 9 **قبل** أن يوجد `design/DESIGN.md`، فلوحته
    ألوانٌ أخرى (`--brand` أصفر، ثلاثيّاتُ rgb) وسلالمُه سلالمُ Tailwind الافتراضية
    (`text-sm`، `rounded-xl`، `p-4`) — بينما تطبيق الكبتن واللوحة على النظام بمفاتيحه
    البكسلية وحارسَيه (`check:scale`، `check:enums`).
    - **وما كشفه**: تصميمُ الخدمة النسائية يرسم شاشات **الراكبة** بلوحة `DESIGN.md`
      (`--sur`/`--brd`/`--tx`)، فمطابقتُه بالبكسل تعني ترحيل التطبيق كلَّه — عملٌ أكبر من
      الميزة التي كشفته، ولا علاقة له بها.
    - **ولماذا مرحلةٌ لا بندُ مستقبليات**: البنود المؤجَّلة أشياءُ لم تُبنَ؛ وهذا شيءٌ **مبنيٌّ
      يعمل** بنظامٍ آخر. تركُه يعني نظامَي تصميمٍ في منتجٍ واحد إلى الأبد، وكلُّ شاشةٍ جديدة
      في تطبيق الراكب تُضاعف كلفةَ الترحيل.
    - **نطاقُها**: استبدال `tailwind.config.js` و`index.css` بنسخة §6/§1.1، ونقلُ كل صنفٍ
      من السلّم الافتراضي إلى المفاتيح البكسلية، وإضافةُ `check:scale` و`check:enums`
      و`cn` بسلّم المقاسات (المصيدة الموصوفة في `CLAUDE.md`)، ومراجعةٌ بصرية لكل شاشة.
      **ولا تغييرَ سلوكيّ فيها إطلاقاً** — من غيّر منطقاً أثناء ترحيلٍ بصريّ لا يعرف أيُّهما
      كسر الشاشة.
    - **وما نتج عنها**: الأصفرُ اختفى من تطبيق الراكب — §1.1 بلا لون علامةٍ تجارية، و`--brand`
      افتراضُه `--tx`. فالتطبيق فحميٌّ/أبيض كأخويه، ويتلوّن بالوردي وحده حين تُشعَل السِمة.
      وهذا **ما يعنيه الترحيل** لا انحرافٌ فيه. ومعه: الشفافياتُ على الألوان (`bg-brand/10`)
      لم تعد تعمل — اللوحةُ hex في متغيّر — فحلَّ محلَّها `--brand-soft` و`--sur2`.

12-ب. **تعدد الوجهات (multi-stop)** — مرحلةٌ كاملة خلف `multi_stop_enabled`. حتى **ثلاث وجهات**
    في الرحلة الواحدة: وسيطتان في `ride_stops` والأخيرة تبقى `rides.dropoff_point`. والتسعيرُ
    من مسارٍ متعدد المحطات عبر Mapbox Directions، ورسمُ انتظارٍ **ثلاثيُّ الحقول** يديره
    المشرف per-country (مقطوعٌ لكل محطة + مهلةٌ مجانية + سعرُ الدقيقة) ورابعٌ سقفاً، والأربعةُ
    **تُجمَّد على الرحلة** كالعمولة. وآلةُ الحالات (`in_progress ⇄ at_stop`، ومخرجٌ إلى
    `completed` عند السقف) داخل `services/rides.py` وحدها ومحروسةٌ بـ`ALLOWED_TRANSITIONS`.
    التفصيل في القسم 5.10، والجداول في القسم 4.
    - **الترتيب**: الخلفية أولاً (ترحيلة، تسعير، آلة حالات، **اختبار تزامنٍ يفشل بحذف القفل**)،
      ثم التطبيقان: إضافةُ المحطات وترتيبُها وحذفُها عند الطلب، وشريطُ تقدّمِ المحطات عند الكبتن
    - **والزمنُ يُختم في الخلفية** والمبلغُ يُحسب فيها: الواجهةُ تعرض عدّاداً وتتلقّى رقماً

12-ج. **حزمُ `FUTURE-FEATURES` بعد 12-ب** — ✅ **مُنجزة**: الأماكن المحفوظة والوجهاتُ الأخيرة
    و«أعد الطلب» (البنود 1–3)، وملخّصُ أرباح الكبتن (17)، وشارةُ النزاع في السجلّين (19)،
    وتعديلُ المركبة وaliasُ كليك (43/18). لا جديدَ في المواصفة عنها غيرَ ما كُتب في موضعه.

12-د. **التحقق الثنائي لدخول اللوحة (TOTP)** — القسم 14.1، وجداولُه الثلاثة في القسم 4. أوّلُ
    الحزمة المتفَق عليها (12-د ← البقشيش ← الكوبونات ← إحالةُ السائقات)، **وأوّلُها لأنه الوحيد
    الذي لا يلمس `pricing` ولا `payments` ولا الدفتر**: أبوابُ المالِ التي تفتحها الثلاثةُ بعده
    (سقفُ ميزانية حملة، مبلغُ حافزِ إحالة) كلُّها حقولٌ يكتبها من دخل اللوحة، فالقفلُ يُسبق ولا
    يُلحق.
    - **الترتيب**: الخلفيةُ أولاً — ترحيلةٌ (ولا نوعَ ENUM جديد فيها، فمصائدُ القسم 4 لا تنطبق؛
      وحدُها `security_settings` تحتاج قيدَ صفٍّ واحد)، ثم `services/totp.py` ومساراتُه وحارسُ
      `core/deps`، **واختبارُ تزامنٍ على استهلاك رمز الاسترداد** يفشل بحذف قفل الصف (رمزٌ «لمرةٍ
      واحدة» يُقبل مرتين) — ثم شاشتا اللوحة: شاشةُ الرمز بعد كلمة المرور، وقسمُ «الأمان».
    - **ومخرجُ الطوارئ يُبنى قبل الإلزام لا بعده**: أمرٌ في `scripts/` يُسقط عاملَ حسابٍ بعينه من
      خارج اللوحة (بيدِ من يملك القاعدة) ويكتب قيدَ تدقيق. بلا هذا المخرج يكون العطلُ الوحيدُ غيرُ
      القابل للإصلاح في المنصة كلها: لوحةٌ لا يدخلها أحدٌ ليصلح ما يمنع الدخول.
    - **ومفتاحُ الإلزام يبقى مطفأً** حتى يسجّل المالكُ عاملَه ويُثبت استردادَه — قرارُ إشعالٍ
      بيده، كما `women_service_enabled`.

12-هـ. **واتساب مُحقِّقاً ثالثاً للرقم** — WhatsApp Cloud API الرسمي من ميتا، بقالب مصادقة.
    التفصيل في القسم 15/أ. عقدٌ عامٌّ في `provider_credentials` (Phone Number ID، Access Token،
    اسم القالب ولغتُه، WABA ID) بمزودٍ وهميٍّ كبقية المزودين، ومفتاحٌ **per-country مطفأٌ
    افتراضاً** يُشعله المشرف من شاشة الإعدادات، ونقطةُ قرارٍ واحدة في
    `services/verification.py` صارت **متعددةَ القنوات** بترتيب `whatsapp ← sms ← firebase`.
    - **الترتيب**: الخلفيةُ أولاً (ترحيلةُ قيمةِ تعدادٍ واحدة، حزمةُ `services/whatsapp/`
      بالبنية الثلاثية، `OtpSender` في `services/otp.py`، والارتدادُ الحامل لمخرجه)، ثم مفتاحُ
      اللوحة، ثم القناةُ والمخرجُ في التطبيقين.
    - **وسلكان خلفها منذ 2026-08-16** (`cloud` و`baileys`) يختار بينهما حقلُ `transport` في العقد —
      القاعدةُ وحرّاسُها وخطةُ الطوارئ في القسم 15/أ.

12-و. **البقشيش** — القسم 6.5، وجدولُه في القسم 4. خلف `tips_enabled` per-country مطفأً
    افتراضاً. **قناةُ المحفظة وحدها** (والسببُ رقمٌ لا رأي: رسمُ بوابةِ البطاقات على نصف دينارٍ
    يتجاوز البقشيشَ نفسه)، ونوعان جديدان في الدفتر بلا عمولةٍ تقابلهما، وسطرٌ ثالثٌ في شاشة أرباح
    الكبتن.
    - **الترتيب**: ترحيلةٌ (جدولٌ + قيمتا تعدادٍ + إعادةُ بناء قيد CHECK على إشارة المبلغ + ثلاثةُ
      حقولٍ على `payment_settings`)، ثم `services/tips.py` بقفلَي المحفظتين، **واختبارُ تزامنٍ يفشل
      بحذف القفل**، ثم أزرارُ شاشة التقييم في تطبيق الراكب وسطرُ الأرباح عند الكبتن، ثم الحقولُ
      الثلاثة في شاشة التسعيرة باللوحة.
    - **وشرطُ «أربع نجوم» في الواجهة لا في الخلفية**، ولا مبلغَ حرّاً، والنافذةُ ٧٢ ساعة.

12-ز. **الكوبونات والخصومات** — القسم 6.6، وجدولُها وحقولُها المجمَّدة في القسم 4. خلف
    `promo_codes_enabled` per-country مطفأً افتراضاً.
    - **والقرارُ المحوريّ أن الخصمَ صفُّ دفعةٍ بقناة `promo`** لا نقصاً في `final_fare`: الأخيرُ
      ينقص أرباحَ الكبتن وعمولتَه لأنهما يُحسبان من `payment.amount`، والمالكُ قرر أن الشركة
      تتحمّله. وبالصفِّ الثاني تتولّى آليةُ «الدفع المختلط» الحسابَ كلَّه بلا قاعدةٍ مالية جديدة،
      وتُفتح «الرحلة مجاناً» بلا بابٍ ثانٍ يكتب المال عند الإنهاء.
    - **الترتيب**: ترحيلةٌ (جدولٌ + أربعةُ حقولٍ على الرحلة + قيمةُ `promo` في تعداد القنوات
      وإدخالُها في تصنيفَي القنوات)، ثم `services/promo.py` (تحقّقٌ، تجميد، حسابٌ عند الإنهاء)
      وتعديلُ `rides.complete` لينشئ الدفعةَ ويؤكّدها، **واختبارا تزامنٍ يفشلان بحذف قفل صفِّ
      الرمز** (ميزانيةٌ لا تُتجاوز بأكثر من الجاري، وحدُّ مستخدمٍ لا يُخترق بطلبين متزامنين)، ثم
      ورقةُ الكوبون في تطبيق الراكب وصفُّ الإيصال، ثم جدولُ الرموز في اللوحة.
    - **والسقفُ يمنع تطبيقاً جديداً لا رحلةً تحمل كوبوناً**، والتجاوزُ يُعرض كما هو.

12-ح. **حافزُ إحالة السائقات** — القسم 9.1، وجدولاه في القسم 4. رابعُ الحزمة وآخرُها. خلف
    `driver_referrals_enabled` per-country مطفأً افتراضاً، **وبمبلغٍ صفرٍ فوقه** (قرارُ المالك):
    فتشحن المرحلةُ خامدةً مرتين، والآليةُ والعدُّ والشرطُ تعمل كلُّها.
    - **وشرطُ الجنس على المُحالة وحدها**: `gender = female` **و**`gender_verified_at IS NOT NULL`
      — نفسُ قراءةِ التوفيق في 10-ج، فبلا وسمٍ «سائقة» كلمةٌ يكتبها أحدٌ عن نفسه. والمُحيلُ أيُّ
      كبتنٍ كان: كبتنٌ يسجّل أختَه يزيد عددَ السائقات كما تزيده سائقة، وقصرُ المُحيل على النساء
      يقصّ منافذَ الوصول بلا أن يحمي شيئاً. **(أقرّه المالك 2026-08-13.)**
    - **ولا يُربط بمفتاح `women_service_enabled`**: ذاك مطفأٌ حتى يُصفّى متراكمُ إثبات الجنس،
      والحافزُ هو ما يبني العرضَ الذي تنتظره الخدمة — فربطُهما يجعلها تنتظر ما لا سبيلَ لبنائه.
    - **الترتيب**: ترحيلةٌ (`driver_referrals` + `referral_settings` + `drivers.referral_code`
      بتوليدٍ للقائمين + قيمةُ `referral_bonus` في تعداد القيود وإشارتُها في قيد الفحص)، ثم
      `services/referrals.py` (توليدُ الرمز، الإسنادُ عند التسجيل، قياسُ الاستحقاق، الدفعُ تحت
      قفلِ الصف)، ثم `tasks/referrals.py` كلَّ عشر دقائق، **واختبارُ تزامنٍ يفشل بحذف قفل صفِّ
      الإحالة** (دورتان معاً لا تكتبان مكافأتين)، ثم قسمُ الكبتن وجدولُ اللوحة.
    - **والرمزُ يُستهلك في التسجيل وحده**: لا مسارَ إسنادٍ رجعيّ — وهو بابُ التلاعب الوحيد الذي لا
      يُغلق بعد فتحه.

12-ط. **الرحلات المجدولة** — القسم 5.11، وجدولُها في القسم 4. ✅ **مُنجزة** (أقرّها المالك
    2026-08-13: الحجزُ جدولٌ مستقل، والرحلةُ تُولد عند التنفيذ، وبلا تكرارٍ ولا محطاتٍ ولا إسنادٍ
    مسبقٍ في القطع الأول).
    خلف `scheduled_rides_enabled` per-country مطفأً افتراضاً.
    - **والقرارُ المحوريّ أن الحجزَ ليس رحلة**: `ride_bookings` صفٌّ مستقل، وصفُّ `rides` يُولد
      لحظةَ التنفيذ. ورحلةٌ بحالة `scheduled` تنتظر تكسر `uq_rides_active_rider` (فتمنع صاحبَها
      من طلب رحلةٍ اليوم) وتجمّد عمولةً قبل أسبوع وتُدخل حالةً جديدةً في كل قائمةٍ نشطة.
    - **والسعرُ يُحسب عند التنفيذ**؛ تقديرُ الحجز للعرض وحده.
    - **الترتيب**: ترحيلةٌ (جدولٌ واحد + قيمةُ مفتاح)، ثم `services/bookings.py` (إنشاءٌ وإلغاءٌ
      وتنفيذٌ يستدعي `rides.request_ride` نفسَه — لا بابَ ثانياً لإنشاء رحلة)، ثم
      `tasks/bookings.py` كلَّ دقيقة بهامشِ عشرِ دقائق، **واختبارُ تزامنٍ يفشل بحذف قفل صفِّ
      الحجز** (دورتان لا تُنشئان رحلتين لحجزٍ واحد)، ثم شاشةُ الراكب وقسمُ اللوحة.
    - **وما يُترك عن قصد**: لا تكرار، ولا محطاتٍ وسيطة، ولا تعيينَ سائقٍ مسبقاً (القسم 5.11).

12-ي. **مشاركة الرحلة بين ركاب** — القسم 5.12. **قراراتُ المالك مثبَّتةٌ هناك والبناءُ لم يبدأ**، وهي أكبرُ بنود
    Phase-2. خلف `ride_sharing_enabled` per-country مطفأً افتراضاً.
    - **والشكلُ المقترح «رحلتان بمجموعة» لا «رحلةٌ بركّاب»**: يبقى كلُّ صفِّ رحلةٍ لراكبه بملكيته
      ودفعته وتقييمه ونزاعه، ويجمعهما `share_group_id`. والشكلُ الآخر يجعل «من يملك هذه الرحلة»
      سؤالاً بجوابين — وهو السؤالُ الذي يحرس كلَّ منفذ (القسم 14).
    - **وثمنُه فهرسٌ واحد**: `uq_rides_active_driver` يمنع رحلتين على كبتنٍ واحدٍ **في القاعدة**،
      فيُستبدل بفريدٍ على `(driver_id, COALESCE(share_group_id, id))` — يبقى الحارسُ في القاعدة
      لا في الذاكرة. **وأولُ ما يُكتب اختبارُ تزامنه.**
    - **وقراراتُ المال الأربعةُ محسومةٌ كلُّها** (القسم 5.12، رابعاً): خصمٌ بنسبةٍ ثابتةٍ
      per-country، والكبتنُ يقبض من رحلتين أكثرَ من منفردة، والشركةُ تتحمّل الفرقَ حين لا يوجد
      شريك، **ومن ألغى يدفع الرسمَ العاديَّ وحدَه ومن بقي تصير رحلتُه منفردةً بسعرها الكامل مع
      إشعارٍ صريح** (القرار الخامس، 2026-08-13).
    - **وقرارُ سلامةٍ خامس، وهو أضيقُ مما اقترحته المواصفة**: طلبٌ بتفضيلٍ نسائيٍّ **غيرُ قابلٍ
      للمشاركة افتراضياً — حتى مع راكبةٍ أخرى** — ولا يصير قابلاً إلا بخيارٍ صريحٍ تختاره الراكبةُ
      نفسُها. فالقبولُ الصامتُ لا يكفي في مسألة أمان.
    - **وثلاثةُ فروعٍ حُسمت معها** (القسم 5.12، القرارات ٦–٨): من بقي وقد **بدأت** رحلتُه لا يُرفع
      عليه سعرٌ وتتحمّل الشركةُ الفرق (رفعُه بلا بديلٍ هو الخيارُ المرفوض)؛ **ولا إعفاء** من رسم
      الإلغاء لمن رفض السعرَ الجديد قبل الانطلاق (إعفاءٌ ثانٍ يفتح باباً، وقد عُرض عليه قبل أن
      يمضي)؛ **ولا بحثَ عن شريكٍ ثالث** في القطع الأول.
    - **وما بقي مفتوحاً شطرٌ من سؤال كلفةٍ** لا يمنع البناء: هل تتحمّل الشركةُ فرقَ من بقي **قبل**
      الانطلاق؟ مؤجَّلٌ حتى تُقاس الأرقام، وجوابُه حين يأتي **إعدادٌ لا بناء** (آليةُ 12-ز مبنيةٌ
      سلفاً).

12. **ميزات Phase 2 المنتجية** خلف feature flags: الرحلات المجدولة، مشاركة الرحلة، الكوبونات، surge
13. **اختبارات + تشغيل تجريبي كامل** لسيناريو: تسجيل كبتن → موافقة → اشتراك → طلب رحلة → تتبع → دفع → سحب

> اختبر كل مرحلة قبل الانتقال للتالية. اكتب tests للمنطق المالي (المحفظة، التسعير، العمولة) بالذات.

---

## 17. عقدُ الأخطاء (2026-08-18)

> **موضعُه في الذيل لا في المتن حفاظاً على الترقيم**: القسمُ ١٦ خطةُ المراحل، ويُحال إليه بالرقم
> في `CLAUDE.md` وفي إيداعاتٍ كثيرة — فإقحامُ قسمٍ قبله يُبطل كلَّ إحالةٍ مكتوبة. والعقدُ يسبق
> التنفيذَ زمناً لا ترتيباً في الصفحة.

**القاعدةُ الحاكمة: كلُّ خطأٍ يواجهه المستخدم يُعرض معه سببُه بالعربية. لا رسالةَ عامة، ولا شاشةَ
صامتة، ولا نصَّ مكتبةٍ خام.**

### ١٧.١ شكلُ الجسم — واحدٌ ولا استثناء

```json
{ "code": "vehicle_year_out_of_range", "message": "سنة الصنع يجب أن تكون بين ١٩٩٠ و٢١٠٠", "field": "year" }
```

- **`code`** — رمزٌ ثابتٌ لا يُترجم، وهو ما يبحث به المشرفُ في السجل.
- **`message`** — عربيةٌ **جاهزةٌ للعرض كما هي**. لا تصوغ الواجهةُ نصّاً لخطأٍ سمّته الخلفية.
- **`field`** — اسمُ الحقل حين يخصّ الخطأُ حقلاً بعينه، و**غائبٌ حين لا يخصّه**. وغيابُه ليس نقصاً:
  «رصيدُك لا يكفي» ليست عن حقل، ووسمُ حقلٍ بها يوجّه المستخدمَ إلى تعديل ما ليس خطأً.

**وكان اسمُ الحقل `detail` حتى 2026-08-18، وتغييرُه ليس تجميلاً**: `detail` هو اسمُ FastAPI نفسِه
في `RequestValidationError`، وقيمتُه هناك **مصفوفةُ أخطاءٍ لا نصّ**. فكان العميلُ يقرأ `body.detail`
فيستقبل كائناً ويعرض `[object Object]` — **اسمٌ واحدٌ لمعنيين، وهو العطبُ نفسُه لا تسميتُه**.
**ولا يُقرأ `detail` احتياطاً بعد اليوم**: احتياطٌ كهذا يُبقي الشكلَ القديم عاملاً فلا يُكتشف مسارٌ
نُسي، وقاعدةُ «لا شكلَ ثانٍ» تُحرَس بالكسر لا بالتسامح.

**وثلاثةُ معالجاتٍ تغطّي كلَّ مخارج الخلفية**، فلا يخرج جسمٌ بشكلٍ رابع: `AppError` (أخطاءُ
الأعمال)، و`RequestValidationError` (٤٢٢)، و`HTTPException` (ما ترفعه FastAPI نفسُها: ٤٠٤ مسارٍ
غيرِ موجود، ٤٠٥، وحرّاسُ الاعتماد).

### ١٧.٢ سجلٌّ مركزيٌّ واحد

النصوصُ العربيةُ كلُّها في `core/exceptions.py` (أصنافُ `AppError`) وفي سجلِّ أخطاء التحقق.
**وممنوعٌ نصٌّ عربيٌّ لخطأٍ داخل شاشةٍ أو داخل endpoint** — نصٌّ في شاشةٍ يختلف عن أخيه في شاشةٍ
ثانية للخطأ نفسِه، ونصٌّ في endpoint يفوت على المترجم والمراجع.

### ١٧.٣ التحقق في الطرفين، والنصُّ واحد

التطبيقُ يتحقق **فوراً قبل الإرسال**، والخلفيةُ هي **المرجعُ النهائي** — لا يُلغي أحدُهما الآخر:
الأولُ يمنع رحلةَ شبكةٍ ورفضاً بعد انتظار، والثاني يحرس الطلبَ الذي لم يمرّ بشاشتنا. **ونصُّ
الرسالة واحدٌ في الحالتين، مصدرُه السجل المركزي** — نصّان لشرطٍ واحد يجعلان المستخدمَ يظنّ أنهما
خطآن.

**وكيف يعرف التطبيقُ القواعدَ؟ تُنشر في `GET /config`** (قرارُ المالك 2026-08-18)، ولا نسخةَ منها
في التطبيق ولا حارسَ بناءٍ يقارن نسختين:

- **تُشتقّ من مخططات Pydantic برمجياً** (`core/validation_rules.py`): تُقرأ `model_fields` وقيودُها
  (`MinLen`/`MaxLen`/`Ge`/`Le`)، والإلزاميةُ من `is_required()`، والاختياراتُ من التعداد أو
  `Literal`. **ولا حدَّ يُكتب بيدٍ بجانب المخطط**: قيمةٌ في بيتين تفترقان عند أوّل تعديل — وهو ما
  وقع فعلاً حين رُفع سقفُ الرسائل من ٣ إلى ٢٠ وبقي ٣ في اختبارٍ ينسخه.
- **والنصُّ يُنشر مع القاعدة**، مولَّداً بنفس دالةِ الرسائل التي تُجيب بها ٤٢٢ — فالمنشورُ هو نصُّ
  الخلفية بعينه لا صياغةٌ ثانية.
- **ويخزّن كلُّ تطبيقٍ آخرَ نسخةٍ محلياً** ليتحقق قبل وصول الرد وعند انقطاع الشبكة. والخزنُ تحسينٌ
  لا شرط: فشلُه صامتٌ، والخلفيةُ تفحص على أي حال.
- **وحارسُه اختبارٌ يقارن المنشورَ بالمخطط حداً حداً** (`tests/test_validation_rules.py`)، ويفشل
  أيضاً إن **سقط حقلٌ** من النشر — فتطبيقٌ يتحقق من خمسةٍ من ستة يُفاجأ بالسادس من الخادم.
- **والاستثناءُ الأمنيُّ يمتدّ إلى المنشور**: مخططُ الدخول يقبل كلمةَ مرورٍ بأيِّ طول عمداً، فلا
  ينشر سياسةَ التسجيل (٨ محارف) — ونشرُها يمنح المخمّنَ حدَّ البحث مجاناً. ولذلك لا يُوحَّد
  المخططان «تنظيفاً»، والاختبارُ يحرس اختلافَهما.

### ١٧.٤ ما لا يظهر للمستخدم أبداً

نصُّ Pydantic الخام · اسمُ جدولٍ أو حقلٍ داخلي · مسارُ ملف · أثرُ استثناء · رسالةُ قاعدة بيانات.
**والسجلُّ يأخذها كاملةً** بمستوى `WARNING`: العطبُ يُشخَّص من السجل، لا من شاشة المستخدم.

### ١٧.٥ الاستثناءُ الأمني — رسالةٌ عامةٌ عمداً

**وهو استثناءٌ عن «قل السبب» لا نقضٌ له**: السببُ يُقال لمن يملكه، وهنا لا نعرف أن الواقفَ أمام
الشاشة هو صاحبُ الحساب.

- **فشلُ الدخول**: رسالةٌ واحدةٌ لا تفرّق بين رقمٍ غير مسجَّل وكلمةِ مرورٍ خاطئة — التفريقُ يمنح
  المخمّنَ نصفَ الجواب مجاناً. (`InvalidCredentials` تفعله اليوم.)
- **OTP والحظرُ ورفضُ الدفع من المزوّد**: رسالةٌ عامةٌ للمستخدم، **والسببُ التفصيليُّ في السجل
  ولوحة الإدارة وحدهما**. (`InvalidOtpCode` تجمع الحالاتِ الثلاثَ في نصٍّ واحدٍ اليوم.)
- **استعادةُ كلمة المرور**: **لا تكشف إن كان الرقمُ مسجَّلاً** — الجوابُ واحدٌ في الحالتين.

### ١٧.٦ أخطاءُ الشبكة — أربعٌ لا «خطأٌ مجهول»

انقطاعُ الاتصال · انتهاءُ المهلة · خادمٌ غير متاح · ردٌّ غير متوقَّع. **لكلٍّ نصُّه وزرُّ إعادةِ
محاولة**، ولا يُعرض أيٌّ منها بوصفه مجهولاً: «تعذّر الاتصال» و«الخادمُ لا يستجيب» يقودان صاحبَهما
إلى فعلين مختلفين — الأولُ يفحص شبكتَه والثاني ينتظر.

**والمهلةُ صنفٌ لا رقمٌ واحد** (قرارُ المالك 2026-08-18، بعد أن وُحِّدت أوّلاً على أبطأ مسار).
مهلةٌ واحدةٌ مأخوذةٌ من أبطأ مسارٍ تترك شاشةَ «اقبل الطلب» تدور دقيقةً على شبكةٍ ساقطة، والعرضُ
نفسُه ينتهي في عشرين ثانية. **وكلُّ رقمٍ مشتقٌّ من سقفِ الخادم لمساره**، والقاعدةُ: مهلةُ العميل
**تتجاوز** سقفَ الخادم — وإلا قُطع طلبٌ سيُجيب عنه الخادمُ فعلاً، فيعيد صاحبُه إرسالَ ما تمّ.

| الصنف | ما قِيس محلياً | سقفُ الخادم | مهلةُ العميل |
|---|---|---|---|
| تفاعلٌ لحظيّ (قاعدةُ بيانات وحدها) | ٦–٥١ مللي ثانية | لا نداءَ خارجيّ | **١٥ث** |
| نداءُ مزوّدٍ داخل الطلب (التقدير، إعادةُ التوجيه) | ١٩٩–٨٤٩ مللي | ١٠ث (`directions.REQUEST_TIMEOUT_SECONDS`) | **٢٠ث** |
| إرسالُ رمز (`/auth/challenge`) | ٠٫٧٤–٣٫٥ث | ٤٥ث (`15 + 30`؛ وطابورُ البوابة ٢٥ث + إقرارٌ ١٠ث) | **٥٠ث** |
| رفعُ مستند | يتبع حجمَ الملف والشبكة | لا سقف | **بلا مهلة** |

**والرفعُ بلا مهلةٍ يلزمه ثلاثة، لا أن يُترك بلا شيء**: **مؤشرُ تقدّمٍ بالنسبة**، و**زرُّ إلغاء**،
و**كاشفُ ركود**. والثالثُ ليس تكراراً للثاني: الإلغاءُ يعالج **من قرّر التوقّف**، والركودُ يعالج
**وصلةً ماتت صامتة** — ومقبسٌ يسقط أثناء الرفع لا يُطلق `onerror` بالضرورة، بل يعيد الـTCP الإرسالَ
دقائقَ في صمت، فيقف الشريطُ عند نسبةٍ بلا حدثٍ ولا رسالة. **ومدّتُه عشرون ثانيةً مقيسة**: أكبرُ
فجوةٍ بين حدثَي تقدّمٍ كانت ١٢٢ مللي على localhost، و٤٣٣ مللي على ٥٠ك.ب/ث، و**١٤٤٠ مللي على
١٢ك.ب/ث** — وصلةٌ بالكاد صالحة (٥٠٦ك.ب في ٤٣ث). فالعشرون نحوُ أربعةَ عشرَ ضعفَ أسوأِ فجوةٍ مقيسة:
لا تشتعل على رفعٍ بطيءٍ حيّ، وتسبق استسلامَ الـTCP بكثير. **ويُفرَّق القطعان**: قطعُ الركود خطأٌ
يُعرض ومعه زرُّ إعادةِ رفعٍ يحمل الملفَ نفسَه، وقطعُ المستخدم قرارُه هو فلا يُعرض له شيء.
و`ontimeout` معالَجةٌ صراحةً ولو لم تُضبط `xhr.timeout`: معالجٌ غائبٌ يعني أن ضبطَها يوماً يُسقط
الوعدَ بلا رفضٍ ولا حلّ.
ملفٌّ كبيرٌ على شبكةٍ بطيئة يتجاوز أيَّ رقمٍ معقول **وهو ناجح**، فقطعُه بعدّادٍ يعيد الرفعَ من أوّله
بلا سبب؛ **وشاشةٌ بلا مخرجٍ هي العطبُ الذي يوجد هذا العقدُ لإزالته**، فالمخرجُ بيد صاحبها لا بيد
عدّاد. والتقدّمُ يفرض `XMLHttpRequest`: `fetch` **لا يبلّغ عن تقدّم الرفع** إطلاقاً.

### ١٧.٧ ما تعرضه الشاشة

- **خطأٌ ذو `field`**: يُعلَّم الحقلُ بلونه، ورسالتُه أسفله، **والتركيزُ ينتقل إلى أوّل حقلٍ مرفوض**.
- **خطأٌ بلا `field`**: شريطٌ أو حوارٌ واضح **مع إجراءٍ ممكن** — إعادةُ محاولة، تعديلُ بيانات،
  أو مراسلةُ الدعم. **ورفضٌ بلا مخرجٍ ليس رفضاً** (قاعدةُ 12-هـ نفسُها).
- **كلمةُ المرور**: شروطُها **قائمةٌ تحت الحقل تُعلَّم لحظةَ الكتابة**، لا رسالةٌ بعد الضغط —
  الثانيةُ تجعل المستخدمَ يخمّن ما نقص.

### ١٧.٧-ب أوراقُ التأكيد في مسارات المال (قرارُ المالك 2026-08-19)

**خطوةٌ وسيطةٌ لا يضغطها إنسانٌ ليست مجهولةَ الحال، بل متوقَّعةُ العطب** — انظر الشكل التاسع في
`CLAUDE.md`. والقرارُ **مختلفٌ لكل مسار**، لا قاعدةٌ واحدة:

| المسار | القرار | لماذا |
|---|---|---|
| **التحويل بين الركاب** | **ورقةُ تأكيد** تعرض **اسمَ** المستقبِل لا رقمَه وحدَه، والمبلغ، والرصيدَ بعده | مالٌ إلى **شخصٍ آخر بلا رجعة**، ومستقبِلٌ يُختار **برقمٍ يُكتب** — ورقمٌ واحدٌ خاطئ يرسله إلى غريب |
| **سلفةُ الكبتن** | **ورقةُ تأكيد** تعرض المبلغ **وشرطَ السداد** (نسبةَ الاقتطاع والحدَّ الأدنى الباقي) | السلفةُ **تُنشئ ديناً** لا تُنفق رصيداً، فمن يوافق يرى كيف يُسترجَع **قبل** لا بعد |
| **البقشيش** | **لا ورقة** — يُختار المبلغُ ثم يُرسَل بزرٍّ واحد | **الورقةُ تقتل البقشيش، والإرسالُ الفوريُّ يقتل الثقة**: كان الضغطُ على مبلغٍ يُخرج المال فوراً في شاشةٍ يُضغط فيها بسرعة |
| الاشتراك · السحب · تأكيدُ الكاش · شحنُ المحفظة | أوراقُها قائمةٌ كما هي | مضغوطةٌ سلفاً |

**و«رصيدك بعده» يُحسب بأعدادٍ صحيحةٍ لا بعائم** (`subtractMoney`): `Number(a) - Number(b)` هو بعينه
ما شحن «٨ د.أ» بجانب «٥٫٠٠٠ د.أ» في شاشة الإحالة. والحسابُ على الأجزاء من الألف، **وهو عرضٌ لا قرار**
— الخلفيةُ تبقى من يرفض ما يتجاوز الرصيد.

---

### ١٧.٨ سياسةُ كلمة المرور — طولٌ وقائمةُ منع، لا قواعدَ تركيب

**ولا يُفرض رقمٌ ولا حرفٌ كبير** (قرارٌ مُتَّخذٌ 2026-08-18 وأقرّه المالك): `NIST SP 800-63B` يمنع
قواعدَ التركيب لأنها تُنتج أنماطاً متوقّعة (`Taxi2024`) فترفع الإزعاجَ لا العشوائية، ويوصي بدلَها
بطولٍ أدنى **وقائمةِ منع**. والحدُّ ٨–١٢٨ كما هو.

**وثلاثةُ أصنافِ منعٍ لا رابع** (`core/password_policy.py`، تُفحص في `validate_password` — البابُ
الوحيد الذي يمرّ به التسجيلُ والاستعادة معاً):

1. **الأشيعُ استعمالاً** — قائمةٌ ثابتة، كلُّ عضوٍ فيها ثمانُ خاناتٍ فأكثر (ما دونها يرفضه الطولُ
   أصلاً، فوضعُه سطرٌ لا يعمل).
2. **رقمُ هاتفه** — ويعني **الرقمَ وحدَه**: نصٌّ خاناتُه فقط، يطابق الرقمَ أو يكون ذيلاً له بسبعِ
   خاناتٍ فأكثر، فتُلتقط الصيغُ الثلاث. **والاحتواءُ المجرّد خطأٌ قِيس**: أوّلُ نسخةٍ رفضت
   `SuperSecret123` لأن خاناتِها `123` جزءٌ من `962791234567` — فسقط **٢٧ اختباراً**، وهو الفرقُ
   بين حارسٍ يمنع ما قُصد ومنعٍ يصيب من لم يُقصد.
3. **التكرارُ البحت** — نمطٌ طولُه ثلاثةٌ فأقلَّ يملأ الكلمةَ (`aaaaaaaa` · `abababab` · `123123123`).

**ولا تُنشر القائمةُ ولا عددُها**: `GET /config` ينشر الحدودَ لا هذه، وقائمةٌ منشورةٌ تُقرأ **دليلَ
تخمينٍ مرتَّباً**. **والرفضُ يقول سببَه ولا يقول القائمة** — رمزٌ واحدٌ (`weak_password`) ورسائلُ
ثلاث: المشرفُ يعدُّ صنفاً واحداً، وصاحبُ الكلمة يحتاج أن يعرف أيَّها.

**ولا تمسّ الحساباتِ القائمة**: الفحصُ على **تعيين** كلمةٍ لا على الدخول، فمن كلمتُه في القائمة
يدخل كما كان — وشاشةُ إجبارِ تغييرٍ قرارٌ لم يُطلب.

---

## 18. ما قبل الإطلاق — بنودٌ ليست من عمل اليوم

**كلُّ بندٍ هنا مؤجَّلٌ بقرارٍ لا بنسيان**، ومكتوبٌ كي لا يُكتشف يوم النشر.

### ١٨.١ توقيعُ النشر (قرارُ المالك 2026-08-19)

حزمتا الراكب والكبتن اليومَ موقّعتان بـ**مفتاح التنقيح** (`debug.keystore`) الذي يولّده أندرويد
ستوديو، وهو الصحيحُ لجولات الجهازين وحدَها. وقبل النشر يلزم:

1. **مفتاحُ إصدارٍ واحدٌ لكلِّ حزمة**، محفوظٌ خارج المستودع ومنسوخٌ احتياطياً — **وضياعُه لا
   يُعالَج**: حزمةٌ موقَّعةٌ بمفتاحٍ آخر لا تُحدِّث المثبَّتة، بل تحتاج **حذفَ التطبيق وإعادةَ
   تثبيته** من كل جهاز، أي فقدانَ جلسات كلِّ من ثبّت.
2. **`applicationId` نهائيٌّ لكلِّ تطبيق** — تغييرُه بعد النشر تطبيقٌ جديدٌ لا تحديث.
3. **`versionCode` يتزايد** في كل رفع؛ متجرُ اللعب يرفض ما لا يزيد.
4. **و`server.url` في `capacitor.config` ليس عنوانَ التطوير**: الحزمةُ لا تحمل حِزمةً بداخلها،
   فالهدفُ المعلَن هو التطبيقُ كلُّه (`check:target`/`check:dist` يحرسان الحِزمة، ولا يريان
   ملفَّ Capacitor).

**ليس بنداً برمجياً**، وهو ما يجعله عرضةً للنسيان: لا اختبارَ يفشل، ولا بناءَ يحمرّ.

### ١٨.١-ب قناةُ البطاقة تُقاس على Telr الحقيقي، ولا يُحكم عليها من المحاكي

**ما قِيس** (2026-08-19، على S21): بعد «متابعة» بقناة البطاقة، فتح التطبيقُ متصفحَ
النظام على `redirect_url`، فطلبه بـ`GET` وردّ الخادمُ **٤٠٥** — والمسارُ
`POST /payments/card/mock/{cart}` ويشترط توكنَ دخولٍ فوقَ ذلك. أي أنّ «صفحةَ الدفع»
الوهمية ليست صفحةً يفتحها متصفّح.

**والسببُ المحاكي لا الشيفرة**: `services/card_gateway/mock.py` هو ما تطلبه §15
للتطوير، وصفحةُ Telr الحقيقية تُستضاف عند المزوّد بعنوانٍ يفتحه المتصفح. فالعطبُ
الظاهرُ على الجهاز عطبُ بيئةٍ لا عطبُ قناة.

**ولذلك لا يُصلح الآن** (قرارُ المالك 2026-08-19): بيانات Telr Sandbox لم تصل بعد،
وتعديلُ المحاكي ليصير صفحةً يُرضي القياسَ ولا يقول شيئاً عن المزوّد الحقيقي.

**وما يجب أن يُقاس قبل الإطلاق**، على اعتماداتٍ حقيقية: فتحُ الصفحة من داخل غلاف
Capacitor، والعودةُ إلى `card_return_url` الصحيح لكلِّ دور (راكب/كبتن)، ثم تسويةُ
الطلب. وحتى ذلك اليوم **قناةُ البطاقة على الجهاز غيرُ مُثبَتة**، وهذا مكتوبٌ هنا
بدل أن يُقرأ نجاحاً من مجموعةٍ خضراء.

### ١٨.٢ «بابٌ بلا زرّ» — ما بُني وله واجهةٌ ناقصة

هذا الشكلُ تكرّر في المشروع مراراً (المفتاحُ السابع بلا زرّ، شارةُ الكبتن بلا زرّ منح). **فالمفتوحُ
منه اليومَ يُسجَّل هنا بدل أن يُكتشف حين يُحتاج**:

| الباب | ما ينقصه | الأثر اليوم |
|---|---|---|
| `subscription_offers.ends_at` | لا حقلَ له في شاشة العروض باللوحة | عرضٌ لا يمكن إنهاؤه بتاريخ؛ يُطفأ بالتفعيل وحدَه |
| منحُ شارةٍ للكبتن (البند ٥٣) | لا زرَّ في الدرج | الكتالوجُ والمسارُ قائمان بلا باب |
| `POST /admin/withdrawals/{id}/payout` | لا زرَّ صرفٍ آليٍّ في شاشة المالية | **مؤجَّلٌ بقرار** — انظر أدناه |
| `POST /admin/campaigns/test-push` | لا زرَّ «دفعةٌ تجريبية» في شاشة الحملات | تُرسل الحملةُ بلا تجربةٍ على جهازٍ واحد |
| ~~`PATCH /admin/campaigns/{id}`~~ | ✅ **وُصل 2026-08-19** | المؤلِّفُ صار محرِّراً، والزرُّ لما لم ينطلق وحدَه |
| ~~`PUT /admin/drivers/{id}/advance-cap`~~ | ✅ **وُصل 2026-08-19** | بطاقةٌ في درج الكبتن، بسببٍ إلزاميٍّ وتدقيق |
| ~~منحُ عرضٍ لجمهور `manual`~~ | ✅ **وُصل 2026-08-19** | مِنتقٍ بالبحث لا بقائمة، والزرُّ على `manual` وحدَه |
| `GET /subscriptions/plans` | لا نداءَ له في أيِّ تطبيق | **ليس نقصاً**: `/subscriptions/me` يحمل الخطط عمداً (الشكلُ الثامن)، وهذا بابٌ ثانٍ خاملٌ لا ناقص |
| `POST /payments/card/webhook` | لا نداءَ له في أيِّ تطبيق | **ليس نقصاً**: يناديه المزوّد، ولا يُفترض فيه زرّ |

**ولا يُبنى نصفُها**: زرٌّ يمنح بلا سجلِّ تدقيقٍ أسوأُ من غيابه.

**وثلاثةٌ وُصلت في 2026-08-19 بأمر المالك، وفي كلٍّ منها قاعدةٌ تستحق البقاء:**

1. **سقفُ سلفةِ كبتنٍ بعينه** — **يخفض ولا يرفع، وذلك بالبناء لا بفحصٍ عند
   الكتابة**: `advances.cap_for` تعيد `min(computed, override)`. وفحصٌ عند الكتابة
   كان سيكون خاطئاً لأن المحسوبَ **ينمو** بما سدّده الكبتن، فرقمٌ يتجاوزه اليومَ
   قد يقلّ عنه بعد شهر. **وثلاثُ حالاتٍ لا اثنتان**: `NULL` لا تخصيص، وصفرٌ منعٌ،
   ورقمٌ سقفٌ أضيق — ورقمٌ واحدٌ لا يحمل الأولَيَن (درسُ أصفار `wallet_settings`).
   **ونُشرت القيمةُ في `AdminDriverRow`**: زرٌّ يعدّل بلا عرضِ ما هو قائمٌ يكتب
   فوق ما لا يراه صاحبُه.
2. **منحُ عرضٍ يدويّ** — **مِنتقٍ بالبحث لا بقائمة** (أمرُ المالك): سوقٌ فيه ألفُ
   كبتنٍ قائمتُه غيرُ مقروءة. **والمنحُ لا يمنح خصماً بذاته**: هو ما يجعل العرضَ
   منطبقاً، والخصمُ يُحسب في `subscriptions._create` وحدَها — فالحدُّ لكلِّ كبتن
   وسقفُ الميزانية وقفلُ صفِّ العرض تعمل عند **الشراء** كما لأيِّ جمهورٍ آخر.
   **ولذلك لا يُرفض المنحُ لنفاد الميزانية**: قد تتحرر قبل أن يشتري.
3. **تعديلُ حملةٍ مجدولة** — وبلا زرٍّ كان التعديلُ **حذفاً وإعادةَ إنشاء**. والزرُّ
   لا يظهر إلا على `draft`/`scheduled`، لأن `campaigns.update` ترفض ما انطلق —
   وزرٌّ يعمل ثم يرتدّ بـ٤٠٩ يعلّم المشرفَ إعادةَ المحاولة.

**وأُغلق منها خمسةٌ في ٢٠٢٦-٠٨-١٩** (مسحٌ صنفيٌّ بكاشفَين آليَّين: ٢٢٧ مساراً
قوبلت بما تناديه الواجهات، و٥٠ مخططَ إدخالٍ إداريٍّ قوبلت بما ترسله اللوحة):
ردُّ الدفعة **بعد بناء صفِّ الدفعة وشاشته** — فالناقصُ كان الصفَّ لا الزرّ —
وقيدُ التصحيح، والشحنُ الإداريّ، ويومُ النسخ الأسبوعيّ، وسقفُ حجمها.

**والحارسُ صار في البناء**: `check:doors` (2026-08-19) يقابل كلَّ مسارٍ إداريٍّ
(١٢٠) بما تناديه اللوحةُ فعلاً، ويرفض ما لا زرَّ له إلا بعلّةٍ مكتوبةٍ في
`DELIBERATE`. **وشقّاه ما يجعله حارساً**: المسارُ مصرَّحٌ به في `endpoints.ts`،
**واسمُ دالّته يُستعمل خارجَه** — فالتصريحُ وحدَه ليس زرّاً، وهو ما أوقف خريطةَ
الراكب في البند ٧ (`/drivers/nearby` مصرَّحٌ به ولا ينادِيه أحد).

**وأولُ نسختين منه كذبتا، وكلتاهما صُحّحت بالقياس**: الأولى عدّت ذكرَ المسار
**في تعليقٍ** نداءً، فأخفت `POST /drivers/me/online`. والثانية عدّت **التصريحَ**
زرّاً، فأخفت ستةَ أبوابٍ منها ساعاتُ الهدوء كلُّها. وحُرِس بالحذف: إزالةُ نداءِ
الردّ تُسقطه.

**ويرفض الاستثناءَ الذي بُني له زرّ**: قائمةُ أعذارٍ لا تُنظَّف تصير كذباً.

### ١٨.٢-أ الصرفُ الآليُّ مؤجَّلٌ بشرطه (قرارُ المالك 2026-08-19)

`POST /admin/withdrawals/{id}/payout` مبنيٌّ ومختبَرٌ ولا زرَّ له، **ولا يُبنى الآن**:
مزوّدُ الصرف غيرُ مُثبَت، واعتماداتُ Telr Sandbox لم تصل (§18.1-ب)، **والصرفُ
اليدويُّ يعمل** — «وافِق» ثم «سجّل التحويل».

**وشرطُ بنائه مكتوب**: بعد ثبوت المزوّد على اعتماداتٍ حقيقية. وقبل ذلك زرٌّ يخرج
المالَ عبر واجهةٍ لم تُجرَّب قط هو أخطرُ من غيابه — وهذا هو الفرقُ بينه وبين
الثلاثة التي بُنيت: تلك تحرّك المالَ **داخل** المنصّة بقيدٍ يمكن عكسه، وهذا
يُخرجه إلى شبكةٍ لا نملك عكسَها.

---

## 19. قوالبُ رسائل رمز التحقق (2026-08-19)

### ١٩.١ الحارسُ انتقل ولم يُحذف — والوعدُ هو الوعدُ نفسُه

كان `POST /send` في البوابة **لا يقبل نصّاً**: تأخذ الرقمَ والرمزَ وتصوغ النصَّ
عندها، «فوعدُ نصٍّ واحدٍ ثابتٍ بلا روابط يحرسه من يملك السلك لا من يستدعيه».

ومنذ قوالبِ اللوحة يصل النصُّ مصاغاً من الخلفية. **وهذا ليس تنازلاً، ومن يقرؤه
لاحقاً فلا يقرأه كذلك**: الوعدُ المقصود لم يكن يوماً «البوابةُ تصوغ النصّ» بل
**«لا يخرج على السلك ما يخالف الشروط»** — وهو باقٍ عند من يملك السلك، محروساً
**بالفحص بدل الصياغة**.

`whatsapp-gateway/src/template.js::chooseText` تفحص كلَّ نصٍّ وارد قبل الإرسال،
فإن خالف **سقطت إلى نصِّ البوابة المدمج** — فلا تسقط قناةُ التسجيل بنصٍّ حرّره
مشرفٌ خطأً.

### ١٩.٢ الفحصُ في موضعين من مصدرٍ واحد

`whatsapp-gateway/otp-template-rules.json` **ملفٌ واحدٌ يقرؤه اثنان**: الخلفيةُ
ترفض به الحفظَ في اللوحة، والبوابةُ ترفض به الخروجَ على السلك. ونسختان تفترقان
أوّلَ تعديل، فيَقبل المشرفُ نصّاً ترفضه البوابةُ صامتةً.

**والفحصُ عند البوابة لا يُغني عن الفحص عند الحفظ**: من يعرف رفضَه في اللوحة
يصحّحه، ومن يكتشفه من رسالةٍ لم تصل لا يعرف أن شيئاً وقع أصلاً.

وثلاثةُ أبوابٍ يظهر منها الرفض:

| الباب | ماذا يقول |
|---|---|
| اللوحةُ عند الحفظ | ٤٢٢ `invalid_otp_template` — يسمّي القالبَ والشرطَ معاً |
| اللوحةُ على شاشة القالب | «هذا القالب مرفوضٌ عند الإرسال ويُستعمل النصّ الافتراضي» |
| سجلُّ البوابة | `warn` بأيِّ قالبٍ وأيِّ شرطٍ ومتى — **ولا سقوطَ صامت** |

### ١٩.٣ السقفُ مقيسٌ لا مقدَّر

**٣٩٨٩ بايت.** ومصدرُه: جسمُ `POST /send` مسقوفٌ بـ٤٠٩٦ بايت
(`MAX_BODY_BYTES`) — قِيس على الحاوية الحيّة فقُبل ٤٠٩٥ ورُفض ٤٠٩٧ — وغلافُ
JSON في أسوأ حالاته ١٠٦ بايت، فالمتبقّي للنصّ ٣٩٨٩، أي نحو ١٩٩٤ حرفاً عربياً
(الحرفُ العربيُّ بايتان في UTF-8). وهو **الحدُّ المُلزِم**: حدُّ واتساب لنصّ
الرسالة أعلى بكثير، فلا يبلغه أحد قبله.

### ١٩.٤ عالميّان لأن الرقمَ عالمي

عقدُ واتساب عالميٌّ ورقمٌ واحدٌ يخدم السوقين، **فالقالبُ صفةُ الرقم لا صفةُ
السوق**: هو ما يقوله هذا الرقمُ لمن يراسله. جدولٌ عالميٌّ كـ`security_settings`،
وشاشةٌ لا تتبدّل بمبدّل الدولة في الترويسة.

**وشرطُ ما يبطل هذا**: إن صار لكل سوقٍ رقمُه أو عقدُه، تبع القالبُ ذلك وأُعيدت
قراءةُ عالميّته. وحتى ذلك اليوم **لا عمودَ دولةٍ «احتياطاً»** — عمودٌ يُدار بيدٍ
بلا حاجةٍ قائمة، وهو ما رُفض في أولوية العروض.

### ١٩.٥ عزلُ القالبين — وأخطرُ ما فيه أنه لا يُحدث عطباً ظاهراً

خلطُ القالبين **يمرّ صامتاً**: كلاهما يحمل رمزاً صحيحاً، فلا استثناءَ يُرفع ولا
رسالةَ ترتدّ ولا شكوى تصل — ويقرأ صاحبُ الرقم «استعادةُ كلمة المرور» وهو ينشئ
حساباً. ولذلك:

1. **الغرضُ يُصرَّح عند البابِ لا يُستنتج**: `start_challenge` تعلن التسجيل،
   و`start_password_reset` تعلن الاستعادة، والطبقاتُ بينهما **تمرّر ولا تقرّر**.
2. **والحارسُ يقرأ الشجرة لا يعدّ التكرار.** وقد قِيس: أوّلُ صيغةٍ للاختبار
   عدّت «واحدٌ لكلٍّ» — **ومرّت والقالبان مقلوبان**، لأن القلبَ يُبقي العددَ
   صحيحاً. فالسؤالُ ليس «كم مرة؟» بل «أيُّهما في أيِّهما؟»
   (`tests/test_otp_template_isolation.py`).

### ١٩.٦ ما لا يحكمه القالب

**الناقلُ الرسميُّ (Cloud API) لا يقبل نصّاً حرّاً**: قوالبُ المصادقة عند ميتا
تُعتمد هناك ويُمرَّر إليها معاملٌ واحدٌ هو الرمز. فقالبُ اللوحة يحكم الناقلَ
الذاتيَّ (`baileys`) وحدَه، **وهذا مكتوبٌ في الشاشة نفسِها** كي لا يحرّر المشرفُ
حقلاً لا أثرَ له على ناقلٍ يظنّه يعمل. ومسارُ الرسائل القصيرة نصُّه في
`services/otp.py::MESSAGE_TEMPLATE` كما كان، ولم يُطلب تحريرُه.

### ١٩.٨ قاعدةُ المعاينة — القيمُ الحقيقيةُ إلا ما لا وجودَ له بعد

**كلُّ معاينةٍ في المنصّة تعرض القيمَ الحقيقيةَ لحظتَها.** والاستثناءُ الوحيدُ
المسموح: قيمةٌ **لا وجودَ لها بعد** — وتُعلَن عيّنةً بنصٍّ ظاهر، لا تُمرَّر
كأنها واقع.

**والسببُ ليس دقّةً**: معاينةٌ تعرض رقماً غير الواقع تجعل المشرفَ يحرّر على أساسٍ
كاذب. وهو صنفُ «شاشةٌ تقول ما لا يقع» بعينه — نفسُ ما أُصلح في ورقة تأكيد
الاشتراك، حيث عرضت السعرَ قبل الخصم بينما البطاقةُ فوقها تعرضه بعده.

**وقد وقع هنا مرتين**، والثانيةُ أخطر:

| القيمة | ما كان | ما صار |
|---|---|---|
| المهلة | `10` مكتوبةً في الراوتر، والحقيقيةُ `CODE_TTL_SECONDS // 60` = ٥ | تُقرأ من الثابت نفسِه |
| اسم التطبيق | `"تاكسو"` ثابتاً في **توقيع `render`** | `settings.app_name` |
| الرمز | عيّنة | عيّنة — **ويُنشر `preview_sample_code`** فتقولها الشاشة |

**والثانيةُ لم تكن عيبَ معاينةٍ بل عيبَ سلك**: الاسمُ المكتوبُ في `render` هو ما
كان سيخرج على السلك لو كتب المشرفُ `{app_name}` في قالبه — فالمعاينةُ كانت
صادقةً بالخطأ نفسِه. **وإصلاحُ المعاينة وحدَها كان سيُخفيه.**

**ولا يُولَّد رمزٌ حقيقيٌّ للمعاينة**: رمزٌ حيٌّ لم يطلبه أحد. فيبقى عيّنةً
معلَنة، ويقارن اختبارٌ ما يُعرض بما يخرج ويؤكّد أن **الفرقَ الرمزُ وحدَه**.

### ١٩.٧ النصُّ المنقول

كان **نصّاً واحداً يخدم المسارين** في `session.js::MESSAGE`، ونُقل بقيمته حرفياً
إلى القالبين معاً (بلا تحسين صياغة):

> `رمز تأكيد رقمك في تاكسو: {code}` ⏎ `صالح {minutes} دقيقة. لا تشاركه مع أحد.`

**ولا يُبذر صفٌّ في الجدول**: غيابُ الصفّ يقرأ «لم يُحرَّر بعد» ويُستعمل النصُّ
المدمج — وصفٌّ مبذورٌ يجعل «الافتراضيَّ» قيمةً يمكن أن تُحرَّر إلى فراغ، فيضيع
الافتراضيُّ الحقيقي.

---

## 20. صيغةُ عرض الأرقام — لاتينيةٌ في كل شاشةٍ وكل رقم (قرارُ المالك 2026-08-19)

**انقلبت صيغةُ العرض**، وهو عكسُ ما بُني عليه المشروع (`DESIGN.md` §4 كان يعرض
الأرقامَ عربيةً-هندية). القلبُ **في المحارف لا في الصيغة**: المالُ يبقى بثلاث
خانات (`1.500` لا `1.5`)، والحارسُ الكانس عليه باقٍ كما هو.

### ٢٠.١ مصفىً واحد، ولا تحويلَ مكتوبٌ في شاشة

`lib/utils.ts::digits` في كل تطبيق — **دالةٌ واحدةٌ يمرّ بها كل رقمٍ يُعرض**.
وهي `arabicDigits` نفسُها انقلب عملُها: كانت تبدّل اللاتينيةَ بالعربية، وصارت
تُطبِّع العربيةَ والفارسيةَ إلى `[0-9]`. **والتطبيعُ أقوى من الحذف**: نصٌّ آتٍ
من `toLocaleString` بخاناتٍ عربيةٍ يخرج منها لاتينياً، فالمصفى يضمن ولا يفترض.

**ولا تمرّ القيمةُ عبر `Number`** — القسم ١٤ كما هو.

### ٢٠.٢ لغةُ العرض مكتوبةٌ لا مفترَضة

`DISPLAY_LOCALE = "ar-u-nu-latn"` — أسماءُ الشهور عربيةٌ والخاناتُ لاتينية.
و`"ar"` وحدَه يعطي لاتينيةً في ICU **اليوم**، و`"ar-EG"` يعطي عربية؛ فالأولُ
افتراضٌ يتبدّل بترقيةِ مكتبةٍ بلا سطرٍ يتغيّر عندنا — وهو درسُ `check:target`
بعينه: ما يعتمد عليه العرضُ يُكتب.

### ٢٠.٣ المدخلاتُ لم تتغيّر

`toLatinDigits` باقيةٌ كما هي: من يكتب `٢٠٢٠` بلوحةِ مفاتيحَ عربيةٍ يُقبل. القلبُ
**في العرض وحدَه** — ولا مساسَ بتخزينٍ ولا حسابٍ ولا قيمةٍ تذهب إلى الخلفية.

### ٢٠.٤ الرمزُ على السلك

`otp.generate_code` يولّد `[0-9]` بالبناء، والقالبان يحملان `{code}` — فالرمزُ
لاتينيٌّ حتماً. **وأُضيفت قاعدةٌ تمنع الخاناتِ العربيةَ في القالب نفسِه**
(`otp-template-rules.json`, `kind: digits`): مشرفٌ يكتب «صالح ٥ دقائق» يُرفض عند
الحفظ وعند الإرسال، فلا يخرج نصٌّ مختلطٌ على السلك.

### ٢٠.٥ الحارسان

| الحارس | يقرأ | يمسك |
|---|---|---|
| `check:digits` (الثلاثة) | **شجرةَ** الشيفرة بمحلِّل TypeScript | خانةً عربيةً في سلسلةٍ أو نصِّ JSX، ولغةَ عرضٍ غيرَ مثبَّتة |
| `tests/digit_format.py` | **كلَّ جوابٍ تنتجه المجموعة** | خانةً عربيةً في نصٍّ تنشره الخلفية |

**ولا يغني أحدُهما عن الآخر**: الأولُ يقرأ ما كُتب، والثاني ما خرج. ورسالةُ خطأٍ
من الخلفية تحمل «٢٤» تظهر على الشاشة كما هي **ولا تمرّ بالمصفى** — لأنها نصٌّ
لا رقم.

**والاستثناءاتُ بأسمائها**: ملفّا `lib/utils.ts` و`lib/phone.ts` هما آلةُ
التحويل نفسُها فتحملان الخاناتِ العربيةَ بحقّ؛ وفي الخلفية يُستثنى ما **يكتبه
إنسان** (`reason`، `details`، وقالبُ الرسالة) — منعُه رفضٌ لما كتبه مشرفٌ بلوحة
مفاتيحه.

---

## 21. نموذجُ الأدوار — مجموعةٌ لا عمود (قرارُ المالك 2026-08-19)

### ٢١.١ لماذا تحوّل النموذج

`users.role` كان عموداً واحداً، فالحسابُ راكبٌ **أو** كبتنٌ ولا يكون الاثنين —
ولا مسارَ في المشروع كلِّه يكتب فيه بعد التسجيل. وذلك يجعل «تبديلاً بين
التطبيقين بنفس الحساب» **مستحيلاً بنيةً لا واجهةً**: `app_scope` يخدم كلُّ
تطبيقٍ دورَه، و`core/deps` يقرأ الدورَ من الصفِّ في كل طلب — فجلسةٌ منقولةٌ لا
تحمل دوراً لا يملكه صاحبُها.

فصار الدورُ مجموعةً (`user_roles`)، والسؤالُ في كل حارس: **«أيملك الدور؟»** بدل
«أدورُه هو؟».

**وما يبطل القرار**: إن صار لكل دورٍ حسابُه المستقلُّ برقمه، فالمجموعةُ بيتٌ ثانٍ
لحقيقةٍ يحملها الصفُّ نفسُه وتُعاد قراءتُها. وحتى ذلك اليوم `users.phone` فريد،
فالشخصُ الواحد حسابٌ واحد.

### ٢١.٢ ما لم يتغيّر

- **`app_scope` كما هو**: كلُّ تطبيقٍ يخدم دورَه، ومن لا يملك الدورَ يُرفض.
  **توسيعُ الأدوار لا يوسّع نطاقَ التطبيقات** — من يملك الدورين يدخل التطبيقين،
  ولا يتّسع شيءٌ للوحة.
- **`deps` يقرأ القاعدة لا التوكن**: مطالبةٌ تُوقَّع مرةً تبقى صادقةً بعد سحب
  الدور حتى تنتهي صلاحيتُها. والمطالبةُ في التوكن **إعلاميةٌ** وحدَها.
- **منحُ دورِ الكبتن بمساره القائم وحدَه** (تسجيلٌ ومركبةٌ وموافقةُ إدارة). ولا
  مسارَ جديدَ يمنح دوراً — ويحرسه اختبارٌ يقرأ الشيفرة: بابُ الإنشاء وحدَه
  يكتب صفَّ دور.
- **العمودُ `users.role` باقٍ**: حذفُه يجعل الرجوعَ فقداً.

### ٢١.٣ أسبقيةٌ معلَنة: ختمُ المشرف يغلب

**جنسُ الكبتن الذي يثبّته المشرفُ يغلب دائماً حين يوجد، أياً كانت أدوارُ الحساب
الأخرى.** لا خطأَ مسمّى ولا وقوف.

**والسببُ أن هذا مسارُ أمانٍ لا مسارُ مال**: الصياحُ هنا يوقف امرأةً عن استعمال
الخدمة النسائية، والغيابُ يفتح ثغرةً أخطر — كبتنٌ يعلن عن نفسه خلافَ ما ثبّته
المشرفُ ليصل إلى ما ليس له. **والقيمةُ الآمنةُ موجودةٌ وقاطعة، فلا تخمينَ أصلاً.**

وهذا **استثناءٌ معلَنٌ** من قاعدة «لا قيمةَ افتراضية» — أسبقيةٌ مكتوبةٌ لا
«أوّلُ متاح».

### ٢١.٤ ستةُ مواضعَ تصيح، وكلٌّ برمزه

مواضعُ تقرّر **معنىً** بالدور لا **إذناً** — وتخمينُ أحد الدورين فيها يكتب مالاً
في المكان الخطأ **بصمت**:

| الرمز | ما لم يُقرَّر |
|---|---|
| `wallet_owner_undecided` | أيُّ محفظة |
| `cancelling_side_undecided` | بأيِّ صفةٍ يُسجَّل الإلغاء (ومنه رسمُ الإلغاء) |
| `card_return_app_undecided` | إلى أيِّ تطبيقٍ يعود الدافع |
| `referral_programme_undecided` | أيُّ برنامجِ إحالة |
| `ride_side_undecided` | أيُّ جانبٍ من الرحلات يُعرض (الجارية والسجل) |

**ورمزٌ لكلٍّ لا رمزٌ عام**: من يقرأ السجلَّ يحتاج أن يعرف **أيُّ قرارٍ غاب**.
وهي مستحيلةُ الوقوع اليوم — الترحيلةُ تنقل كلَّ حسابٍ بدورٍ واحد — وتبيت يومَ
يُفتح بابُ المنح، وهو موضعُ القرار.

### ٢١.٥ برنامجُ الإحالة صار واقعةً تاريخية

كان يُشتقّ من `users.role` بحجّةٍ مكتوبة: «ثابتٌ مدى عمر الحساب، فتخزينُه بيتٌ
ثانٍ لحقيقةٍ قائمة». **وقد سقطت الحجّة.** فصار `referrals.referral_type` عموداً
يُختم **لحظةَ الإحالة**: أيُّ برنامجٍ انطبق يومَها، لا ما يُشتقّ من أدوارِ اليوم.

**ولا يُملأ بأثر رجعي**: الصفوفُ الأقدمُ من العمود تبقى فارغةً ويُشتقّ نوعُها من
الدور كما كان — وهو صحيحٌ لها بالضبط، لأنها كُتبت تحت الثبات. وملؤها اليومَ
كتابةُ **استنتاجٍ** في عمودِ وقائع، فيصير البيتُ الثاني ثالثاً.

---

## 22. القرارُ يتبع سياقَ الفعل لا دورَ الفاعل (قرارُ المالك 2026-08-19)

**القاعدةُ العامة، ومَن أضاف موضعاً رابعاً يجد الجوابَ هنا مكتوباً:**

> حيث يقرّر الدورُ **معنىً** لا **إذناً**، فالمرجعُ **سياقُ الفعل**: التطبيقُ
> الذي بدأه، أو الإعلانُ المرافق له، أو الواقعةُ المختومةُ لحظتَه — **لا الدورُ
> الذي يحمله فاعلُه اليوم**.

والسببُ أن الدورَ صار مجموعةً (§21)، فكفّ عن تعيين شيءٍ بمفرده. **والسياقُ
مفردٌ دائماً**: العمليةُ الواحدةُ تُبدأ من تطبيقٍ واحد، وتقع مرةً واحدة.

**وثلاثةُ لوازمَ تتبع القاعدة:**

1. **يُعلَن أو يُختم عند الإنشاء، ولا يُشتقّ عند الاستعمال.** الاشتقاقُ لاحقاً
   يقرأ أدوارَ اليوم عن فعلٍ وقع أمس.
2. **والإعلانُ لا يمنح شيئاً**: يُفحص أن صاحبَه يملك ما يدّعيه، وإلا رُفض —
   كـ`app_scope` سواءً بسواء.
3. **ومن لم يُعلن يرتدّ بخطأٍ مسمّى** لا بقيمةٍ افتراضية ولا بـ«أوّلِ متاح»،
   **إلا حيث توجد قيمةٌ آمنةٌ قاطعة** — وتُكتب أسبقيةً معلَنةً (§21.3).

### ٢٢.٠ قبل أن تضيف إعلاناً: **أيعرف الصفُّ الجواب؟**

**سؤالٌ واحدٌ يسبق كلَّ إعلان.** فإن كان الصفُّ نفسُه يحمل الجواب، فالإعلانُ
**بيتٌ ثانٍ للحقيقة** — يُسأل عنه عميلٌ ويمكن أن يخالف ما في الجدول.

**واثنان من خمسةٍ لم يحتاجاه** (قِيس 2026-08-19):

| الموضع | أيعرف الصفُّ؟ | ما استُعمل |
|---|---|---|
| صفةُ من ألغى | **نعم** — الرحلةُ تحمل راكبَها وكبتنَها بالاسم | يُشتقّ من الرحلة، بلا إعلان |
| تطبيقُ دفعِ رحلة | **نعم** — الدافعُ راكبُها بالبناء | ثابتٌ، بلا إعلان |
| تطبيقُ اشتراك | **نعم** — شأنُ كبتنٍ بتعريفه | ثابتٌ، بلا إعلان |
| تطبيقُ شحنِ محفظة | **يرث** المحفظةَ المعلَنة | لا إعلانَ ثانٍ لشيءٍ أُعلن |
| المحفظة · جانبُ الرحلات · برنامجُ الإحالة | **لا** | إعلانٌ صريح |

**فالإعلانُ لِما لا يحمله الصفُّ وحدَه، لا لكلِّ ما صار غامضاً بالأدوار.**

### ٢٢.١ المحفظة — محفظتان لا واحدة

**محفظةُ الكبتن** تحمل الأرباحَ وتخضع للسلَف والاقتطاع والاشتراك، **ومحفظةُ
الراكب** تحمل ما يشحنه لرحلاته. **ولا دمج**: دمجُهما يعني **اقتطاعَ السلفة من
مالٍ شحنه بنفسه**.

والعمليةُ تعلن محفظتَها (`wallet=` على المسارات المشتركة، ومحفوظةٌ على صفِّ طلب
الشحن) — ومن لم يُعلن وحمل الدورين يرتدّ بـ`wallet_owner_undecided`.

**ولا تحويلَ بين محفظتَي الشخص نفسِه** — بندٌ مستقلٌّ إن لزم، لا يُبنى ضمناً.

### ٢٢.٢ عودةُ صفحة البطاقة — التطبيقُ الذي بدأ الدفع

`provider_orders.opened_from_app` يُختم عند فتح الطلب، والعودةُ تقرؤه. ولا
يُشتقّ من الدور عند العودة: من بدأ الدفعَ من تطبيقٍ يعود إليه، ولو كسب دوراً
آخرَ بينهما.

### ٢٢.٣ برنامجُ الإحالة — برنامجُ التطبيق الذي صدرت منه

`referrals.referral_type` يُختم لحظةَ الإحالة من `app` المُعلَن في التسجيل.
**والقديمُ لا يُملأ بأثر رجعي** (§21.5).

### ٢٢.٤ لماذا مُلئ عمودان بأثر رجعيٍّ وثالثٌ لا

`wallet_topup_requests.owner_type` و`provider_orders.opened_from_app` **مُلئا**،
و`referrals.referral_type` **لم يُملأ** — والفرقُ ليس مزاجاً:

عند الترحيلة يملك كلُّ حسابٍ دوراً واحداً، فمحفظةُ كلِّ طلبٍ قديمٍ وتطبيقُ كلِّ
دفعةٍ قديمةٍ **معلومان يقيناً**؛ والملءُ نقلُ حقيقةٍ لا استنتاج. أما برنامجُ
الإحالة فيصير سؤالاً عن **أيِّ تطبيقٍ صدرت منه** — وذلك لم يُسجَّل يومَها،
فملؤه اليومَ كتابةُ **استنتاجٍ** في عمودِ وقائع.

**والقاعدةُ المستخلَصة**: يُملأ بأثر رجعيٍّ ما كانت قيمتُه **مسجَّلةً في مكانٍ
آخر**، ولا يُملأ ما يحتاج استنتاجاً.

### ٢٢.٥ بيتا الحقيقة جسرٌ مؤقت

`users.role` والجدولُ متطابقان، **وذلك جسرٌ لا حالةٌ نهائية**. والعمودُ **يُقرأ
للرجوع ولا يُكتب** — يحرسه اختبارٌ يفشل إن كتب أيُّ مسارٍ فيه.

**وشرطُ إسقاطه**: بعد استقرار مرحلة زرِّ التبديل، وثبوتِ أن لا رجوع — أي مضيُّ
دورةِ نشرٍ كاملةٍ بلا حاجةٍ إلى `downgrade`، وخلوُّ كلِّ مرشِّحٍ وقراءةٍ من ضمِّ
العمود (`has_role_clause`). وحتى ذلك اليوم، ضمُّه **حارسُ فقدٍ** لا مصدرٌ ثانٍ:
حساباتٌ تُنشأ خارج `create_account` (البذرة، ثوابتُ الاختبار) تحمل العمودَ بلا
صفّ، ومرشِّحٌ يقرأ الجدولَ وحدَه يُخفيها.

**والقياسُ في ٢٠٢٦-٠٨-٢٠ — الشرطُ يُطبَّق كما كُتب، والجوابُ: لا يُسقَط بعد.**

| الشرط كما كُتب | الحال المقيسة |
|---|---|
| **لا مسارَ يكتب العمود** | ✅ **مستوفى** — `test_no_path_writes_users_role` يُشغَّل ويمرّ |
| **مضيُّ دورةِ نشرٍ كاملةٍ بلا حاجةٍ إلى `downgrade`** | ❌ **لم تقع** — لا شيءَ منشورٌ للإنتاج بعد، فلا دورةَ تُقاس |
| **خلوُّ كلِّ مرشِّحٍ وقراءةٍ من ضمِّ العمود** | ❌ `has_role_clause` يُنادى في **ستة مواضع** بأربعة ملفات، ويضمّ `User.role.in_(...)` بحكم تعريفه؛ ومعه `User.roles` و`token_service` (ادّعاءُ `role` في الرمز) و`admin_referrals` و`referrals` |

**والشرطُ الثاني وحدَه كافٍ**: «ثبوتُ أن لا رجوع» يُقاس بدورةِ نشرٍ مضت، ولم
تمضِ واحدة. **فالإسقاطُ اليومَ يحذف طريقَ الرجوع قبل أن يُثبَت أنه لا يُحتاج.**

**وخطرٌ ثالثٌ قِيس ولم يكن مكتوباً**: §25.6/٣ تصف إنشاءَ **أول مشرفٍ على
الإنتاج** بـ«إدخالِ الصفِّ مباشرةً» — وهو بابُ إنشاءٍ لا يمرّ بـ`create_account`.
**فصفٌّ يُدخَل بالعمود وحدَه يصير، بعد إسقاطه، حساباً يخوّل ولا يظهر في أيِّ
قائمة** — أو لا يخوّل أصلاً. والبذرةُ تكتب صفَّ الدور معه (`role_grants=[…]`)،
**والإجراءُ اليدويُّ لم يكن يقول ذلك**. صار يقوله.

**فما يُفعل قبل الإسقاط، بهذا الترتيب**: تمضي دورةُ نشرٍ ⇒ تُنقل القراءاتُ
الستُّ إلى المجموعة وحدَها ⇒ يُنقل ادّعاءُ `role` في الرمز ⇒ **ثم** تُسقط
الترحيلة. ولا يُقدَّم شيءٌ منها على ما قبله.

---

## 23. التبديلُ بين التطبيقين بنفس الحساب (قرارُ المالك 2026-08-19)

### ٢٣.١ رمزُ تسليمٍ في Redis — لا نقلَ لرمز التجديد

`POST /auth/handoff` يعيد قيمةً عشوائيةً مفتاحُها في Redis، **مئةً وعشرين
ثانية، أحاديةَ الاستعمال، مربوطةً بالمستخدم وبالتطبيق الهدف**.

**والمئةُ والعشرون مقيسةٌ لا مختارة**: كانت ثلاثين على تقديرٍ بأن «فتحَ تطبيقٍ
فعلٌ من ثوانٍ»، فسقطت على جهازٍ حقيقي — الإقلاعُ **البارد** حتى بلوغ مسار
الاستقبال قِيس مرتين على S21 عند **٣٫٤ ثانية و١٥٫١ ثانية**، فالتهم أسوأُهما
نصفَ النافذة قبل أن يبدأ نداءُ المبادلة، وارتدّ تسليمٌ سليمٌ بـ`invalid_token`.

**ومن يقصّرها لاحقاً يعرف أنه يكسر استعمالاً ولا يضيف أماناً**: الأمانُ محمولٌ
على **أحاديّة الاستعمال** و**الارتباط بالتطبيق الهدف** و**إعادة الفحص كاملاً
عند المبادلة** — لا على قِصَر المهلة. و`POST /auth/handoff/
exchange` يستهلكها ويصدر جلسةً في التطبيق المستقبِل — **بلا كلمةِ مرورٍ ولا
رمزِ تحقق**، لأن صاحبَها أثبت هويّتَه في التطبيق المُصدِر قبل ثوانٍ.

**والنمطُ هو تحدّي العامل الثاني نفسُه** (§14.1) لا آليةٌ مخترَعة، وحجّتُه
مكتوبةٌ هناك: «رمزٌ مكانه `sub` في توكنٍ ناقصِ صلاحية يصير جلسةً كاملةً بخطأٍ
واحدٍ في حارسٍ واحد».

**ولا يُنقل رمزُ التجديد**: تدويرُه أحاديُّ الاستعمال، فحاملان يُبطل أحدُهما
الآخرَ ويخرج صاحبُهما عشوائياً — وهو فوق ذلك اعتمادٌ طويلُ العمر يسافر خارج
التطبيق.

**والفحصُ يُعاد كاملاً عند المبادلة**: الحسابُ من القاعدة، والحظرُ، و`app_scope`
ثانيةً. فسحبُ الدور بين الإصدار والمبادلة يُبطل الرمز — **وهو ليس إذناً مجمَّداً**.

### ٢٣.٢ الزرُّ ينقل ولا يمنح

**وظيفتُه فتحُ التطبيق الآخر، لا أكثر** (تصحيحُ المالك 2026-08-19): **ليس
مساراً للتسجيل بدورٍ آخر**، والتمييزُ الذي يعنيه هو **التطبيقُ لا الدور**.

| الحال | ما يقع |
|---|---|
| التطبيقُ الآخرُ مثبَّت | يُفتح — وتنتقل جلستُه إن ملك دورَه |
| غيرُ مثبَّت | **رابطُ تنزيله، في الاتجاهين** — صفحةٌ تشرح، لا خطأٌ ولا صمت |
| في رحلةٍ جاريةٍ على **أيِّ** جانب | يُرفض بـ`handoff_blocked_by_active_work`، **والزرُّ يقول لماذا** |

**وحارسُ الانشغال يسأل الجانبين معاً** (`has_any_active_ride`) ولا يمرّ
بـ`_side_of`: من يحمل الدورين هو بالضبط من يُسأل، فسؤالٌ يرتدّ عليه لا يجيب.

**وموضعُ الزرِّ «حسابي»** لا شريطاً سفلياً ولا شاشةً رئيسية: تبديلٌ لا يُضغط
يومياً.

**وما يقع لمن لا يملك دورَ الهدف قرارُ واجهةٍ مفتوح** (2026-08-19): مبادلةُ
الرمز تُرفض بـ`app_scope` بحكم التصميم، وما يراه المستخدمُ **قِيس** ولم يُفترض —
انظر التقرير. ولا يُبنى الجوابُ قبل أن يُحسم.

### ٢٣.٣ فتحُ التطبيق الآخر — `intent://` لا مخططاً عارياً

ثلاثةُ أسباب، وكلُّها عن **الحتمية**:

1. **`package=` يثبّت المستقبِل**، فلا يلتقط الرمزَ تطبيقٌ سجّل المخططَ نفسَه.
2. **`S.browser_fallback_url` يجعل «غير مثبَّت» جواباً من النظام** — بدل مهلةٍ
   نخمّن بعدها أن التطبيق لم يُفتح، والتخمينُ يخطئ على جهازٍ بطيء.
3. **الرمزُ في مخططٍ خاصٍّ لا يبلغ خادماً**: لا نداءَ شبكةٍ يحمله، وعمرُه ثلاثون
   ثانيةً واستعمالُه واحد.

**ومخططٌ خاصٌّ لا رابطُ تطبيق (App Link)**: الأخيرُ يحتاج `assetlinks.json` على
النطاق وتوقيعَ إصدارٍ مستقرّاً، وكلاهما بندُ ما قبل الإطلاق (§18.1). ويُعاد
النظرُ فيه يومَ يستقرّ التوقيع.

**والمستمعُ شرطٌ لا زينة**: الغلافُ يفتح عنوانَ المخطط، ولا يصير مسارَ ويبٍ من
نفسه — فبلا `appUrlOpen` تصل النيّةُ ولا يحدث شيء، وهو «بابٌ بلا زرّ» في ثوبِ
منصّة.

---

## 24. ظهورُ السوق في التطبيقات — مفتاحٌ لكل دولة (قرارُ المالك 2026-08-19)

سوقٌ تكتمل خلفيتُه قبل أن يُفتح يجب أن يُبنى **ولا يظهر**: لا في قائمة اختيار
الدولة، ولا في التسجيل، ولا في أي شاشةٍ تعرض الأسواق. وليبيا أولُ حالة استعمال
لا موضوعُ القاعدة — **ولا يُكتب اسمُ دولةٍ في أي شرط**.

### 24.1 المفتاح

`FeatureKey.COUNTRY_VISIBLE = "country_visible"`، صفٌّ في `feature_flags` لكل
دولة كسائر المفاتيح، يُبدَّل من شاشة الإعدادات في اللوحة ويُسجَّل كلُّ تبديلٍ في
سجلِّ التدقيق باسم فاعله.

**وسكوتُه ظهور** — وهو ثاني عضوٍ في `DEFAULT_ENABLED_FLAGS`، والاستثناءُ
معلَّل: القاعدةُ العامة أن الغياب يُطفئ لئلا تُشعَل ميزةُ مالٍ بالصمت، وهنا
الغيابُ يُطفئ **التطبيقَ كلَّه** — تثبيتٌ لم يُبذر يعطي قائمةً بلا دولةٍ واحدة
ولا شاشةَ دخولٍ تُرسم. فالإخفاءُ **صفٌّ صريحٌ يكتبه إنسان**.

**والسببُ المكتوب ليس شرطاً عليه.** كان شرطُ «اكتب سببَ الإطفاء» مقروءاً من
`DEFAULT_ENABLED_FLAGS` نفسِها، فلمّا دخلها هذا المفتاح كان سيرث رسالةً تقول
«مفتاح التحقق» عمّن يُخفي سوقاً. فُصلت المجموعتان: `DEFAULT_ENABLED_FLAGS` تقول
**كيف يُقرأ الغياب**، و`GUARDED_FLAGS` تقول **ما ثمنُ الإطفاء** — وفيها
`otp_verification_enabled` وحدَه. إخفاءُ سوقٍ **قرارُ إطلاقٍ لا إجراءُ طوارئ**.

### 24.2 التصفيةُ في الخلفية لا في التطبيقات

`GET /config` ينشر الدولَ الظاهرةَ وحدَها. وهذا هو الموضعُ لأن كلَّ ما تعرضه
التطبيقاتُ من أسواقٍ مشتقٌّ منه أصلاً — فقائمةٌ مكتوبةٌ في حزمةٍ لا تتغيّر إلا
بحزمةٍ جديدةٍ على كل جهاز، وهذا نقضُ الشرط.

**والافتراضيةُ لا تُعلَن وهي مخفيّة**: `default_country_code` يصير أولَ ظاهرةٍ
إن كانت المضبوطةُ مخفيّة — اسمُ دولةٍ ليست في `countries` حقلٌ بلا مرآة، يصل
ويُقرأ ولا يقابله شيء.

**واللوحةُ لا تقرأ من `/config`**: لها `GET /admin/countries` (`StaffUser`)
وترى الأسواقَ كلَّها ومعها `visible` واسمُ الدولة ووصفُها كاملاً — عملتُها
وساعاتُ هدوئها وقنواتُ تحققها. من يُجهّز سوقاً قبل فتحه — تسعيرتَه وحملاتِه
وعقودَه — يحتاج أن يراه وهو مطفأ، وإلا صار الإشعالُ زرّاً في شاشةٍ لا تُظهر ما
يشعله. وفي مبدّل الترويسة نقطةٌ على المخفيِّ تقول إنه لا يظهر في التطبيقات.

**والوصفُ يبنيه بانٍ واحدٌ للبابين** (`services/country_config.build`) — الشكلُ
الثامن: بابان ينشران الشيءَ نفسَه، وكلٌّ صادقٌ عن نفسه، ولا شيءَ يقارن.
و`test_the_two_doors_publish_the_same_description` هو ما يقارن.

### 24.3 ما مُسِح من الواجهات

| الموضع | ما كان | ما صار |
|---|---|---|
| `admin-panel/components/Shell.tsx` | `["JO","LY"]` + `COUNTRY_LABEL` مكتوبان | من `GET /admin/countries` — الاسمُ من الخلفية |
| `admin-panel/lib/config.tsx::useCountryConfig` | من `/config` وحده | من باب اللوحة أولاً، و`/config` مخرجاً قبل الدخول |
| `customer-app/lib/config.tsx::useAuthCountry` | `?? ["JO"]` و`?? "JO"` | `?? []` والافتراضيةُ من القائمة |
| `driver-app/lib/config.tsx::useAuthCountry` | مثلُه | مثلُه |
| `customer-app/screens/WalletTransfer.tsx` | `?? ["JO"]` لقائمة مفاتيح الهاتف | `?? []` — والتحويلُ إلى سوقٍ مخفيٍّ بابٌ يفتحه الإشعال |

**وما بقي مكتوباً عن قصد**: `CountryCode = "JO" \| "LY"` في `api/types.ts` في
التطبيقات الثلاثة — **مرآةُ تعدادٍ لا قائمةَ عرض**، يحرسها `check:enums`، ولا
يُشتقّ منها ما يُرسم. و`user?.country_code ?? "JO"` في مواضع الاحتياط: دولةُ
**صاحبِ الحساب** لا قائمةُ أسواق، ولا تُظهر سوقاً مخفيّاً لأحد.

### 24.4 ما قِيس، وكيف

**الشرطُ كان «يظهر فوراً بلا إعادة بناءٍ ولا نشر»، وقِيس على حزمةٍ مثبَّتةٍ
سلفاً** (`index---ktOf25.js` على Note 20، مبنيّةٌ قبل هذا العمل كلِّه):

| ما فُعل | ما قِيس |
|---|---|
| إطفاءُ ليبيا من باب اللوحة | `/config` يعيد `["JO"]` بعد **٢٠٦ ملّي ثانية** |
| قراءةُ شاشةِ التحويل على الهاتف | `select` فيه خيارٌ واحد: `JO=الأردن` |
| إشعالُ ليبيا من باب اللوحة | `country_visible = True` في ٤٢١ ملّي ثانية |
| إعادةُ تحميل **نفسِ الحزمة** | `["LY=ليبيا","JO=الأردن"]` — بلا بناءٍ ولا نشرٍ ولا تثبيت |
| إطفاؤها ثانيةً | عاد الخيارُ واحداً |
| `GET /admin/countries` في الحالين | الدولتان دائماً، و`visible:false` على ليبيا |

**والقياسُ من الشجرة لا من لقطة**: قيمُ `option` قُرئت نصّاً، لأن «ليبيا»
و«الأردن» في ذلك القياس متمايزتان بالنصِّ وحدَه.

### 24.5 إخفاءٌ لا إيقاف — وليبيا سوقٌ يعمل (تصحيحُ المالك 2026-08-19)

**ما أُخفي هو الأزرارُ في التطبيقين، للتطوير وحدَه.** ليبيا **لا تتوقف**:
الخلفيةُ تعمل عليها، وقناةُ تحققها تعمل (وهي القناةُ الوحيدةُ المثبَتةُ على
السلك في المنصّة كلِّها)، وبياناتُها وإعداداتُها وعقودُها حيّة. وإطلاقُها
**كبسةُ زرٍّ من اللوحة بعد الأردن**.

فاقرأ هذا البند بندَ **إخفاء** لا بندَ **إيقاف** — والفرقُ ليس لفظياً: بندُ
الإيقاف يُقرأ «سوقٌ معطَّلٌ ينتظر بناءً»، وهذا سوقٌ جاهزٌ ينتظر **ترتيب
الإطلاق**.

**ويُتحقَّق قبل إظهارها** من التسعيرة (`pricing_settings` و`commission_settings`
بأرقام المالك لا ببذرة التطوير)، وعقودِ الدفع المقابلة لكل مفتاحٍ مُشعَل، وحسابِ
مشرفٍ للسوق، وساعاتِ الهدوء ومنطقتِها الزمنية. **وقناةُ التحقق ليست في هذه
القائمة لأنها مُثبَتة** — انظر §24.6.

**والمفاتيحُ تُراجَع سوقاً سوقاً لا مرةً واحدة** (قرارُ المالك 2026-08-20).
`feature_flags` مفتاحُها `(country_code, feature_key)` — **فلكلِّ سوقٍ صفُّه**،
ولا وجودَ لمفتاحٍ «عامّ». وإشعالُ ميزةٍ في الأردن **لا يمسّ ليبيا بحرف**.

**وهذا مقيسٌ لا مبدأ** (٢٠٢٦-٠٨-٢٠، قاعدةُ التطوير):

| الشكل | أمثلةٌ مقيسة |
|---|---|
| مُشعَلٌ في الأردن ومطفأٌ في ليبيا | `multi_stop_enabled` · `promo_codes_enabled` · `ride_sharing_enabled` · `driver_advances_enabled` · `cliq_enabled` · `card_enabled` |
| **لا صفَّ لليبيا أصلاً** ⇒ مطفأٌ بالسكوت | `driver_levels_enabled` · `subscription_offers_enabled` · `rider_referrals_enabled` · `referred_reward_enabled` |
| مطفأٌ في الأردن ومُشعَلٌ في ليبيا | `wallet_transfer_enabled` |

**والشكلُ الثاني هو الفخّ**: مفتاحٌ بلا صفٍّ يُقرأ **مطفأً** بالقاعدة العامة
(«الغيابُ يُعطّل») — فمن يقرأ لوحةَ الأردن يرى ميزةً تعمل، ولا شيءَ في الشاشة
يقول إن السوقَ الآخرَ لا يعرفها. **ولا يُكتشف إلا بالسؤال عن ذلك السوق بعينه.**

**فيومَ تُشعَل ليبيا**: تُقرأ القائمةُ **صفّاً صفّاً لسوقها**، ويُسأل عن كلِّ
مفتاحٍ **مطفأٍ أو غائب**: أمطفأٌ بقرار، أم مطفأٌ لأن أحداً لم يسأل عنه؟ **ولكلِّ
مفتاحٍ مُشعَلٍ عقدُه وإعدادُه في ذلك السوق** — عمولةٌ بلا `commission_settings`
لليبيا، أو بطاقةٌ بلا عقدِ Telr ليبيّ، أو محطاتٌ بلا رسومِ انتظارٍ ليبية:
كلُّها ميزةٌ مُشعَلةٌ فوق فراغ.

**ولا تُنسخ حالُ الأردن إلى ليبيا نسخاً.** الأرقامُ سوقيةٌ بطبيعتها — أجرةٌ
وعمولةٌ ورسمُ إلغاءٍ وحدودُ محفظة — ونسخُها يعني تسعيرَ سوقٍ بأرقام آخر.

### 24.6 ⛔ أخطرُ بند إطلاق: قناةُ التحقق في **الأردن** (2026-08-19)

**الفحصُ قلب السؤال.** كان المكتوبُ أن ليبيا هي التي تنتظر قناةً، والمقيسُ
عكسُه: **ليبيا مُثبَتة، والأردنُ — سوقُ الإطلاق — قناتُه لا تعمل.**

| ما قِيس | الأردن `+962` | ليبيا `+218` |
|---|---|---|
| `whatsapp_otp_enabled` | **مطفأ** | مشتعل |
| ما يقرّره `method_for_phone` من الرقم | `firebase` وحدَه | `whatsapp_otp` ثم `firebase` |
| عقدُ الرسائل (`sms`) | **معطَّل**، وقيمتُه `use_mock` — للتطوير لا للإنتاج | معطَّل كذلك |
| إرسالٌ حقيقيٌّ أُقرَّ على السلك | **لا شيء** | إرسالان، `error: null`، ٥٣٨ و٨٠٧ مل |
| تسجيلٌ حقيقيٌّ اكتمل بالقناة | **لا شيء** | نعم — حسابٌ أُنشئ بعد دقيقةٍ من إقرار رمزه |

**وFirebase لا يفتح حساباً**: كشفت المرحلةُ ١٣ على الجهاز أنه يردّ
`auth/billing-not-enabled`، والمالكُ قرّر ألّا يُرقّى إلى Blaze. فالسلسلةُ في
الأردن اليوم هي: واتساب مطفأ ← رسائلُ معطَّلة ← **Firebase الذي لا يعمل** ←
لا شيء. و`otp_verification_enabled` مشتعلٌ هناك، فالتحققُ **مشترطٌ ولا يتمّ**.

**والحساباتُ الأردنيةُ الحاليةُ ليست دليلاً**: واحدٌ وثلاثون منها «مُتحقَّق»،
وكلُّها إمّا مبذورةٌ مباشرةً أو مرَّت **بمحاكي الرسائل** الذي كان مفعَّلاً
لتجاوز Firebase أثناء المرحلة ١٣ — وهو معطَّلٌ الآن ويرفضه الكودُ في الإنتاج
أصلاً. **صفرُ تسجيلٍ أردنيٍّ بقناةٍ حقيقية.**

**فما يُتحقَّق منه قبل الإطلاق في الأردن — وهذا أخطرُ ما في القائمة كلِّها:**

1. **قناةٌ حقيقيةٌ تصل `+962`** — وأقصرُ طريقٍ إليها موجود: القناةُ نفسُها
   المثبَتةُ على `+218` هي عقدٌ **عام** ومفتاحٌ **per-country**، فإشعالُ
   `whatsapp_otp_enabled` في الأردن لا يحتاج كوداً ولا عقداً جديداً.
2. **وقياسُ ذلك بإرسالٍ واحدٍ إلى رقمٍ أردنيٍّ يملكه المالك** — بإذنٍ صريح،
   وبنفس الأدلة الثلاثة: ما سلّمته الخلفية، وما خرج على السلك، وما وصل الهاتف.
3. **وقناةٌ ثانيةٌ خلفها، أو قرارٌ مكتوبٌ بألّا تكون.** اليوم `fallback_channel`
   فارغٌ في السوقين: يومَ يسقط الرقمُ أو تُقطع الجلسةُ يتوقف التسجيلُ كلُّه بلا
   مخرجٍ آلي. وهذا قرارُ صمودٍ لا قرارُ بناء — إعادةُ تفعيل عقد الرسائل حقلٌ في
   صفحة العقود بلا نشر.

**وترتيبُ الإطلاق يتبع هذا لا العكس**: إظهارُ ليبيا (§24.5) كبسةُ زرٍّ على سوقٍ
قناتُه تعمل، وفتحُ الأردن يشترط قناةً لم تُقَس بعد.

### 24.7 القياسُ الأردنيُّ الأول — ٢٠٢٦-٠٨-١٩

**إرسالةٌ واحدةٌ بإذنٍ صريح، عبر باب التسجيل الحقيقي** (`POST /auth/challenge`)
إلى رقمٍ أردنيٍّ يملكه المالك، بعد إشعال `whatsapp_otp_enabled` في الأردن.

**والاشتقاقُ سبق الإرسال**: `method_for_phone('+962787862343')` أعادت
`whatsapp_otp` والمتاح `['whatsapp_otp','firebase']` — فالسوقُ من البادئة لا من
الافتراض، وما أُعلن هو ما وقع.

| الدليل | ما قِيس |
|---|---|
| ما سلّمته الخلفية | `purpose=registration` · `deliver=True` · ٨٨ بايت · النصُّ **قالبُ اللوحة المحرَّر** لا المدمج |
| ما خرج على السلك | مطابقٌ حرفاً بحرف · **بلا سقوطٍ إلى الاحتياط** |
| إقرارُ خادم واتساب | **١٠٢٦ مل** · `error: null` · من `962787862343@s.whatsapp.net` |
| إيصالُ التسليم | **٢٣٥٠ مل** · نفسُ معرّف الرسالة ⇐ بلغت الجهاز |
| زمنُ النداء الكلّي | ٢٫٨٥ ثانية |

**وهو في الوقت نفسِه برهانُ §19**: النصُّ الخارجُ كان القالبَ المحرَّر من
اللوحة، وهي أولُ مرةٍ يبلغ فيها قالبٌ محرَّرٌ السلكَ منذ بُنيت الميزة.

### 24.8 ثلاثةُ شروطِ إطلاقٍ صارت مقيسة — لا ملاحظات

**١) الرقمُ المرسِل عارٍ بلا اسمِ عرضٍ ولا توثيق — شرطُ إطلاقٍ قائم.**

> **تحديثٌ بالقياس (2026-08-19، بعد تبديل المالك للرقم):** بُدِّل الرقمُ من
> `218930385734` إلى **`447366186776`** — بادئةُ **المملكة المتحدة**. فسقط
> **نصفُ** الشرط وبقي نصفُه، ولا يُحذف ما لم يسقط:
>
> | ما كان | ما صار | الحال |
> |---|---|---|
> | «رقمٌ **ليبيٌّ** يُرسل إلى أردنيين» | البادئةُ `+44` | **سقط** — ولم يُستبدل بأردنيّ: صار أجنبياً للسوقين معاً |
> | «بلا اسمِ عرض» (`me.name = null`) | **`null` كما هو** | **قائم** |
> | «بلا توثيق» (`registered = false`) | **`false` كما هو** | **قائم** |
> | «مربوطٌ برمز QR لا حسابَ أعمال» | `platform: android`، ومجلدُ اعتمادٍ كُتب من جديد | **قائم** |
>
> **والبادئةُ الأجنبيةُ لا تُحسّن الشرطَ بالضرورة**: مَن يسجّل في عمّان كان يصله
> رمزٌ من رقمٍ ليبيٍّ مجهول، وصار يصله من رقمٍ **بريطانيٍّ** مجهول — والمقارنةُ
> بين الاثنين ليست قياساً أجريناه، فلا تُدَّعى. **ما قِيس أنّ المرسِل ما زال بلا
> اسمٍ ولا شارة**، وهو جوهرُ الشرط.
>
> **ولم يتغيّر شيءٌ سوى الرقم واعتماده**: العقدُ كما هو (`updated_at`
> `2026-08-17`، وصفُّه عامٌّ بلا دولة)، وبنيةُ الجلسة واحدةٌ كما هي (§24.8/٣).
>
> **والجلسةُ عادت**: `linked` منذ `23:30:53Z`، و`whatsapp_otp` هي القناةُ
> المختارة في **السوقين معاً** — فالتسجيلُ يعمل فيهما.
>
> **وشيءٌ ثالثٌ يُسجَّل لأنه عاد**: قبل الارتباط بأربع دقائق ظهر
> `getaddrinfo EAI_AGAIN web.whatsapp.com` **أربعَ مرات** — تعثُّرُ حلِّ الأسماء
> الذي عولج بتثبيت المُحلِّلَين. لم يمنع الارتباط، **وهو الشكلُ الذي يبتلع رسالةً
> وهو نائم**: نظامُنا يقرأ «أُرسلت» والهاتفُ لا يرنّ.



> **✅ قرارُ المالك (2026-08-20) — والعلّةُ تُصحَّح معه.**
>
> **ما كان مكتوباً أعلاه يزن البادئة، وهذا نصفُ التشخيص.** «ليبيٌّ يُرسل إلى
> أردنيين» و«بريطانيٌّ يُرسل إلى الاثنين» ليسا درجتين على سُلَّمٍ واحد: **العلّةُ
> أن الرقمَ عارٍ وغريبٌ عن سوق المستقبِل**، لا أن بادئتَه كذا. والعُريُ
> (`me.name = null` · `registered = false`) هو ما يجعل الرسالةَ شكلاً احتيالياً،
> **والغربةُ عن السوق هي ما يجعل البلاغَ رد الفعل الأول** — فمن يسجّل في عمّان
> يقارن ما يصله بما يصله من بنكه ومن مشغّله، وكلاهما محلّيّ.
>
> **فالبريطانيُّ ليس تحسّناً**: هو **أغربُ للسوقين معاً** بعد أن كان الليبيُّ
> أهليّاً لأحدهما. وهذا حُسم لا يُقاس بمقارنةٍ بيننا، بل بالقاعدة نفسِها.
>
> **والقرار: شريحةٌ أردنيةٌ مخصَّصةٌ للإرسال قبل إطلاق الأردن — بندُ إطلاقٍ لا
> تفصيلُ تشغيل.** يدبّرها المالك، ولا يُبنى لها شيءٌ قبل وصولها.

**ووجهٌ ثانٍ للبند نفسِه: رقمُ الدعم ليبيٌّ أيضاً** (قرارُ المالك 2026-08-21).
`+218930385734` هو **رقمُ الدعم على الصفحة التعريفية ورقمُ الإرسال معاً**.

**فالإطلاقُ في الأردن يحتاج رقمَين أردنيَّين لا واحداً**: رقمَ إرسالٍ (أعلاه)
**ورقمَ دعم**. والعلّةُ واحدةٌ في الوجهين — **الغربةُ عن سوق المستقبِل**: من
يقرأ رمزَ تحققٍ من رقمٍ أجنبيٍّ يشكّ، ومن يتصل بدعمٍ خارج بلده **يدفع مكالمةً
دولية** ثم لا يتصل مرةً ثانية.

**وهما بندٌ واحدٌ لا بندان**: يُدبَّران معاً أو يبقى النقصُ قائماً — ورقمُ إرسالٍ
أردنيٌّ بدعمٍ ليبيٍّ يحلّ نصفَ المشكلة ويترك نصفَها في أول مكالمة.

**وموضعُ الرقم إعدادٌ لا نصّ**: `landing/config.js` — فتبديلُه سطرٌ واحد.

**وما يلزم لتبديل الرقم حين تصل الشريحة — مقيسٌ من الكود لا مقدَّر:**

**الرقمُ لا يُخزَّن في أيِّ موضع.** مسحُ الخلفية والبوابة يقول: لا حقلَ له في
`provider_credentials` (حقولُ العقد `base_url` و`api_key` و`transport` وأخواتُها،
وليس فيها رقم)، ولا عمودَ في القاعدة، ولا قيمةَ في `.env`. البوابةُ **تشتقّه
لحظةَ الارتباط** من `this._sock.user.id` وتحتفظ به في الذاكرة وحدَها
(`session.js:303`). **فتبديلُ الرقم لا يمسّ إعداداً واحداً.**

| الخطوة | ماذا تفعل | من يفعلها |
|---|---|---|
| ١ | «فصلُ الجلسة» من بطاقة الجلسة في صفحة العقود (`POST /admin/providers/whatsapp/session/logout`) | المشرف |
| ٢ | البوابةُ تُخرج الحسابَ، **وتمحو `/data/auth` كاملاً**، وتعود `awaiting_qr` وتعرض رمزاً جديداً (`session.js:506`) | آليّاً |
| ٣ | مسحُ الرمز الجديد **من هاتف الشريحة الأردنية** | المالك |
| ٤ | لا شيءَ بعدها — الرقمُ يُشتقّ من الجلسة الجديدة، والقالبان والسقوفُ والمفاتيحُ كما هي | — |

**وثلاثةُ تحفّظاتٍ تُقرأ قبل التبديل:**

1. **نافذةُ التوقف حقيقية**: بين الخطوة ١ والخطوة ٣ الحالُ `awaiting_qr`،
   **والتسجيلُ متوقفٌ في السوقين معاً** (الجلسةُ واحدة، §24.8/٣). فيُختار وقتٌ
   هادئ، ويكون الهاتفُ بيدِ من يمسح **قبل** الضغط لا بعده.
2. **`whatsapp:baileys:global` غيرُ مفتاحٍ بالرقم**، فسقفُ المئة في الساعة
   **ينتقل بعدّاده** إلى الرقم الجديد. لا يضرّ (سقفٌ علينا لا على أحد)، ويُذكر
   كي لا يُقرأ رفضٌ بعد التبديل على أنه عطبٌ في الشريحة.
3. **والأهم: هذا يحلّ الأردنَ ويُبقي ليبيا** — رقمٌ أردنيٌّ أهليٌّ لعمّان
   **وغريبٌ عن طرابلس** بالقاعدة نفسِها التي بُني عليها القرار. فيوم يُفتح
   السوقُ الليبيّ يعود السؤال، **وجوابُه حينئذٍ أحدُ اثنين مكتوبين سلفاً**:
   الطبقاتُ السبعُ في §24.8/٣ (رقمٌ لكل سوق)، أو القناةُ الرسمية (Cloud API)
   بحسابِ أعمالٍ موثَّق. **ومعهما يسقط شرطُ §19.4** («عالميّان لأن الرقمَ
   عالميّ») — فالقالبُ يصير خاصيّةَ سوقٍ لا خاصيّةَ رقمٍ واحد، وهو الشرطُ
   المكتوب هناك صراحةً لإعادة النظر.


`creds.json` يقول `me.name = null` و`registered = false`: حسابٌ مربوطٌ برمز QR،
بلا اسمِ عرضٍ وبلا شارةِ توثيق. فمن يسجّل في عمّان يصله رمزُ تحققٍ من
`+218 93 038 5734` — **رقمٍ دوليٍّ مجهولٍ بلا اسم**. وهذا لا يمسّ الثقةَ وحدَها:
**البلاغاتُ هي ما يحظر الأرقام غيرَ الرسمية**، ورسالةُ تحققٍ من رقمٍ مجهولٍ
أقربُ شكلاً إلى الاحتيال منها إلى المنصّة — فالبلاغُ نتيجةٌ متوقَّعة لا حادثة.

**١-ب) والحفرةُ وقعت مقيسةً — لم تعد افتراضاً** (٢٠٢٦-٠٨-١٩، بعد دقائقَ من
كتابة هذا البند). بعد **ثانيةٍ واحدة** من إيصال أولِ رسالةٍ أردنيةٍ إلى الجهاز
سقطت الجلسة:

```
19:17:02.017Z  CB:receipt              ← سُلِّمت
19:17:02.461Z  stream errored out
19:17:02.471Z  الجلسةُ انتهت نهائياً — Stream Errored (conflict)
19:23:23.588Z  تعذّر الإرسال — الجلسةُ انتهت      (الرسالةُ الثانيةُ لم تخرج)
```

**و`conflict` يعني أن الحسابَ فُتح في مكانٍ آخر فطردَ جلستَنا** — أي أن القناةَ
**قابلةٌ للطرد بفتحِ واتساب على أيِّ جهاز**، وليست عطباً نادراً. والأثرُ فوريّ:
`awaiting_qr` · `needs_human: true`، و**التسجيلُ متوقفٌ في السوقين معاً** لأن
الجلسةَ واحدة (§24.8/٣) — والعلاجُ **إنسانٌ بهاتفٍ يمسح رمزاً**، لا إعادةُ
محاولةٍ ولا خدمةٌ تعود وحدها.

**والكشفُ يعمل ولا يُغني**: التنبيهُ وقع بعد **٥٣ ثانية** (المهمّةُ الدورية كلَّ
دقيقة، و`awaiting_qr` في `URGENT` فتُعلن بلا إمهال) — صفٌّ في صندوق كلِّ مشرفٍ
«جلسةُ واتساب تنتظر ربطاً» ودفعةٌ إلى هاتفه، وبطاقةُ الجلسة في صفحة العقود
تعرض الحالَ ونصَّ الخطأ. فلا يُكتشف بأن أحداً لم يسجّل. **لكنَّ الإصلاح يبقى
بشرياً**، وهذا هو حجمُ الحفرة لا كشفُها.

**٢) مخرجُ الطوارئ الوحيد مخرجٌ لا حلّ.** لحظةَ سقوط الرقم يرى المسجّلُ `502`
`verification_send_failed` و**`fallback_channel: null`** — لأن
`CODE_CHANNELS = (whatsapp_otp, sms_otp)` وFirebase ليست قناةَ رمز. والمخرجُ
الوحيد إطفاءُ `otp_verification_enabled`: قِيس عند **٢٦١ مل**، يسري في النداء
التالي، `admin` وحدَه، بسببٍ مكتوبٍ إلزاميٍّ وصفِّ تدقيق. **وثمنُه أن كلَّ رقمٍ
يُقبل بلا إثبات** — في نظامٍ فيه محفظةٌ ومالٌ وتحويلٌ بين الأشخاص، أي بابٌ
مفتوحٌ لحساباتٍ وهمية. يُستعمل ساعاتٍ لا أياماً، وبقرارٍ مكتوب.

**٣) «جلسةٌ لكل سوق» لا وجودَ لها في الكود.** المقيس: عقدُ واتساب
`country_code` فيه **فارغ**، و`get_values(session, ProviderKey.WHATSAPP)` تُنادى
**بلا مُعامل دولة** في المواضع الثلاثة، والبوابةُ تحمل **مقبساً واحداً**
(`this._sock`) و**مجلدَ اعتمادٍ واحداً** (`/data/auth`)، ولا يرد في كودها ذكرٌ
لسوقٍ ولا لجلسةٍ مسمّاة.

**وما يتطلّبه رقمٌ لكل سوق — مكتوبٌ ولا يُبنى حتى يُقرَّر:**

| الطبقة | ما يلزم |
|---|---|
| العقد | `provider_credentials` تقبل عقدَ واتساب **per-country** — وهو نقضٌ صريحٌ لقرارٍ مكتوب («العقدُ عامٌّ والمفتاحُ per-country») فيُعاد النظرُ فيه أولاً |
| القراءة | المواضعُ الثلاثة تمرّر دولةً، و`build_provider` يبني مزوّداً لكل سوق |
| البوابة | جلسةٌ مسمّاة: مجلدُ اعتمادٍ ومقبسٌ وطابورٌ لكل رقم، و`/send` يقبل `session_id`، و`/status` و`/qr` و`/logout` تصير per-session |
| الربطُ البشري | مسحُ رمزٍ **لكل رقم**، وحجمٌ دائمٌ لكلٍّ منها |
| اللوحة | بطاقةُ جلسةٍ لكل سوق بدل واحدة، والتنبيهُ الدوريُّ يميّز أيَّها سقط |
| السقوف | `whatsapp:baileys:global` تصير per-session، وإلا خنق سوقٌ الآخر |
| التنبيه | `tasks/whatsapp.py` يتتبّع انتقالَ حالٍ **لكل جلسة** لا حالاً واحدة |

**والبديلُ الأرخص المسجَّل للمقارنة**: قناةُ واتساب **الرسمية** (Cloud API)
مبنيّةٌ في نفس الواجهة منذ 12-هـ (`transport: cloud`) — رقمُ أعمالٍ موثَّقٌ باسمٍ
معروضٍ وشارة، لا رمزَ QR ولا مقبسَ يسقط. تبديلُها حقلٌ واحدٌ في صفحة العقود،
وثمنُها قالبُ مصادقةٍ معتمدٌ من ميتا. **وهذه المقارنة هي القرار، لا بناءُ
الجلسات.**

### 19.9 القالبُ صار الرمزَ وحدَه — وما فُقد بذلك (قرارُ المالك 2026-08-19)

**القالبان — التسجيل والاستعادة — صارا `{code}` لا غير.** فما يخرج على السلك
ستةُ أرقامٍ عاريةٍ بلا جملةٍ واحدة.

**وقِيس على السلك لا عند الحفظ** (تجربةٌ جافةٌ عبر السلسلة الحقيقية: قالبٌ حُفظ
من باب اللوحة ← `body_for` + `render` ← `/send` بـ`deliver:false`):
`would_send = '663983'` · `isdigit() = True` · `fell_back = False` · بلا مخالفات،
**في القالبين معاً**. وهذا هو الفرقُ الذي علّمَنا إياه البند ١: «الحفظُ نجح» ليس
برهاناً.

**والحرّاسُ لم يُمسّوا**: `{code}` ما زال إلزامياً — «123456» مكتوبةً أرقاماً
تُرفض بـ`template_missing_variable` («ولا رسالةَ رمزٍ بلا رمز») — وحدُّ الطول
باقٍ على ٣٩٨٩ بايت المقيسة، يعضّ عند ٤٠٠٦ ويمرّ عند ٣٩٨٦. والمعاينةُ تُصاغ
**بنفس دالة الصياغة** التي تصوغ المُرسَل، فما يراه المشرفُ هو ما يستقبله صاحبُ
الهاتف.

**وما فُقد بهذا القرار — مأخوذٌ لا مغفول:**

1. **«لا تشاركه مع أحد» كان التحذيرَ الوحيد** في المنصّة كلِّها ضدّ احتيال
   «اتصالٌ يطلب منك الرمز». لا شاشةَ تقوله ولا رسالةَ أخرى، فالنصُّ كان محلَّه
   الوحيد. **الرمزُ العاري لا يحذّر أحداً.**
2. **ولا يقول أيَّ خدمةٍ تطلبه.** من يصله «663983» من رقمٍ دوليٍّ لا يعرف أنه
   من تاكسو — والرقمُ المرسِل نفسُه بلا اسمِ عرضٍ ولا توثيق (§24.8/١). فمن طلب
   رمزين في دقيقةٍ من خدمتين لا يميّزهما، ومن لم يطلب شيئاً لا يعرف ما هذا.
3. **وقراءةُ «صالح ٥ دقائق» ذهبت معها** — والمهلةُ لم تعد تُقال في أيِّ مكان
   يراه صاحبُ الهاتف.

**والنصُّ المدمج في البوابة يبقى بتحذيره ومهلته — قراراً مأخوذاً لا سهواً**
(المالك، 2026-08-19): **القالبُ أرقامٌ والاحتياطُ نصّ، والفرقُ مقصود.** وعلّتُه
أن الاحتياطَ لا يعمل إلا **لحظةَ فشل الطبيعيّ** — وحينها يكون المستقبِلُ في
أضعف حالاته معرفةً بما يجري، فالشرحُ أنفعُ من أرقامٍ عارية. فمن قرأ الملفين
ووجد نصّين مختلفين فلا يوحّدهما: التوحيدُ يُلغي الشرحَ حيث يُحتاج أكثرَ ما
يُحتاج.

---

## 25. خادمُ الإنتاج — التجهيز (2026-08-20)

**لا شيءَ منشورٌ للإنتاج بعد.** هذا سجلُّ التجهيز، والخادمُ لا يستقبل شيئاً
حقيقياً حتى يقول المالك.

### ٢٥.١ الأسرارُ جديدةٌ لا منقولة (قرارُ المالك 2026-08-20)

**سرُّ تطويرٍ عمرُه شهورٌ مرّ على جهازٍ وسجلاتٍ ولقطاتٍ لا يصلح للإنتاج**، وفصلُ
البيئتين يعني أن تسرُّبَ التطوير **لا يمسّ الإنتاج**. فما يمكن توليدُه وُلِّد من
جديدٍ **على الخادم نفسِه**: كلمةُ مرور Postgres، و`JWT_SECRET`،
و`CREDENTIALS_ENCRYPTION_KEY`، و`WA_GATEWAY_KEY`.

**والتوليدُ على الخادم لا على الجهاز**: سرٌّ يُولَّد هنا ثم يُنقل يمرّ بذاكرةِ
صَدَفةٍ وسجلِّ أوامرَ وربما لقطةِ شاشة — وهذا لا يخرج من ملفِّه قط.

**وموضعُه** `/home/taxo/secrets/taxo.env` — مالكُه `taxo`، صلاحياتُه `600`،
**خارج مجلد المشروع** فلا يدخل نسخةً ولا أرشيفاً ولا صورة.

### ٢٥.٢ مسحُ Fernet قبل تدوير مفتاحه

**تدويرُ مفتاح Fernet يجعل كلَّ ما شُفّر به غيرَ قابلٍ للفكّ**، فمُسح ما يحميه
قبل التوليد. النتيجة: **موضعان لا غير**، ولا ثالثَ لهما (مسحُ كلِّ نداءٍ
لـ`get_cipher()` — سبعةُ مواضعَ تقرأ أو تكتب هذين):

| العمود | محلياً | القرار |
|---|---|---|
| `provider_credentials.credentials` | ٧ صفوف | **تُعاد كتابتُها**: العقودُ الإنتاجيةُ تُدخَل في اللوحة بقيمها الجديدة على أيِّ حال |
| `user_totp.secret_encrypted` | **صفر** | لا شيءَ يُفقد |

**والرحلاتُ والمحافظُ والوثائقُ والمستخدمون لا شيءَ فيها مشفَّرٌ بـFernet** —
فلا خسارةَ في تدويره مع قاعدةٍ جديدةٍ فارغة.

**وشرطٌ يُقرأ قبل أيِّ نقلٍ لاحق**: إن نُقلت القاعدةُ المحليةُ كما هي إلى
الإنتاج، فإمّا أن ينتقل **المفتاحان معاً**، وإمّا أن تُفرَّغ `provider_credentials`
قبل النقل وتُعاد كتابتُها. مفتاحٌ جديدٌ على صفوفٍ قديمةٍ يجعلها بياناتٍ لا تُقرأ.

### ٢٥.٣ توكن Cloudflare — واسعُ الصلاحيات، وتضييقُه مؤجَّلٌ بقرار

**التوكنُ المستعمَل واسعُ الصلاحيات (+361)**، والنفقُ لا يحتاج منها إلا
**`Tunnel: Edit`**. **وتضييقُه بندٌ مؤجَّلٌ بقرار المالك (2026-08-20)** — مسجَّلٌ
هنا كي لا يُقرأ لاحقاً سهواً.

**وما يستطيعه حاملُه لو تسرّب**: يقرأ ويعدّل ويحذف **كلَّ أنفاق Cloudflare
وسجلّات DNS ونطاقات الحساب** — أي يوجّه `api.tajora.ly` و`app.tajora.ly` إلى
خادمه فيلتقط كلَّ ما يكتبه المستخدمون بما فيه كلماتُ المرور، **دون أن يمسّ
خادمَنا بشيء**.

**وموضعُه `.env` على الخادم** كغيره — لا في أمرٍ ولا في ملفِّ وحدةٍ ولا في
`compose`.

### ٢٥.٤ ما لا يُنقل

- **`data/auth` (جلسةُ واتساب)** — نسخُها يعني `conflict` يُسقط الجلستين معاً،
  وهو ما وقع مقيساً في §24.8/١-ب. الجلسةُ تُبنى على الخادم **برمزٍ جديد، مرةً
  واحدة، وبإذن المالك**.
- **`.env`** — يُنشأ على الخادم ولا يُنقل.
- **`dist`** — يُبنى هناك.

### ٢٥.٥ لا احتياطَ صامتٌ لسرّ

كان `${POSTGRES_PASSWORD:-taxo}` في **ثلاثة مواضع** في `docker-compose.yml`:
بيئةٌ ناقصةٌ في الإنتاج **تُقلع بكلمةِ مرورٍ معروفةٍ ولا يفشل شيء**. صار
`${POSTGRES_PASSWORD:?…}`، فيرفض compose الإقلاعَ ويسمّي الناقص.

**والبرهانُ مقيس**: بلا المتغيّر يخرج بـ`1` برسالةٍ تسمّيه، وبالملفِّ بـ`0`.
**وثمنُه المقبول**: كلُّ أمر compose محليٍّ صار يحتاج `--env-file .env.local`.
وما بقي له احتياطٌ (`POSTGRES_USER`/`POSTGRES_DB`/`CUSTOMER_APP_PORT`) **ليس
سرّاً ولا يفتح باباً**.

### ٢٥.٦ قاعدةٌ إنتاجيةٌ جديدة — بندا إطلاقٍ لا تفصيلان (قرارُ المالك 2026-08-20)

**١) اعتماداتُ المزوّدين السبعةُ تُدخَل من الصفر، ولا تُنقل مشفَّرة.**

`provider_credentials` تُملأ من **صفحة العقود في اللوحة** على الإنتاج، بقيمٍ
إنتاجيةٍ لا بنسخِ التطوير. والسببُ مزدوج: مفتاحُ Fernet جديدٌ فالمظاريفُ القديمةُ
لا تُفكّ (§25.2)، **واعتمادُ تطويرٍ مرّ على جهازٍ وسجلاتٍ لا يصلح للإنتاج**.

| العقد | حالُه اليوم | ما يلزم للإنتاج |
|---|---|---|
| **Mapbox** | حقلان: `public_token` للواجهات، و`secret_token` **للخلفية** | **توكنان عامّان (`pk`) لا نوعان** — انظر §25.8 |
| **Telr** | **ناقصٌ اليوم** — لا اعتمادات Sandbox ولا إنتاج (§18.1-ب) | حسابٌ حقيقيٌّ ومتجرٌ ومفتاحُ مصادقة. **وقناةُ البطاقة غيرُ مُثبَتةٍ على جهازٍ حتى تصل** |
| **SMS** | معطَّلٌ ومحاكٍ (`use_mock`) | عقدٌ حقيقيٌّ إن أُريد — وإلا يبقى معطَّلاً |
| **FCM** | حسابُ خدمةٍ من ملفٍّ محليّ | حسابُ خدمةٍ لمشروع Firebase الإنتاجيّ |
| **payout** | معطَّلٌ (LY) | مؤجَّلٌ بقرار (§18.2-أ) |
| **Firebase Auth** | فعّالٌ ولا يعمل (`billing-not-enabled`) | **لا يُنقل**: القرارُ ألّا يُرقّى (§24.6) |
| **WhatsApp** | فعّالٌ ويعمل | العقدُ يُعاد إدخالُه، و`WA_GATEWAY_KEY` **وُلِّد جديداً على الخادم**، و**الجلسةُ تُبنى برمزٍ جديد** لا تُنسخ (§25.4) |

**٢) تسجيلاتُ TOTP تُعاد من الصفر** — `user_totp` فارغةٌ على قاعدةٍ جديدة، فمن
يدخل اللوحةَ أولَ مرةٍ يسجّل جهازَه. ولا خسارةَ في ذلك: محلياً **صفرُ صفوف**.

**٣) وبابٌ يُقفل من الخارج إن لم يُفتح أولاً — أولُ مشرف.**

**مقيسٌ من الكود**: `scripts/seed.py::seed_admin` يرفض العملَ صراحةً حين
`settings.is_production`، والتسجيلُ الذاتيُّ مقصورٌ على `rider|driver` بحكم
النوع، و**لا مسارَ في المنصّة كلِّها يُنشئ حسابَ مشرف**. فعلى قاعدةٍ إنتاجيةٍ
جديدةٍ **لا أحدَ يستطيع فتح اللوحة**.

**وهذا مقصودٌ لا عطب** — التعليقُ يقوله: «هناك يُنشأ يدوياً بكلمة مرور لا تمرّ
من ملف بيئة». **لكنّ الإجراءَ اليدويَّ لم يكن مكتوباً في أيِّ مكان**، فيُكتشف
يومَ النشر. وهو مكتوبٌ الآن:

> **إدخالُ الصفِّ مباشرةً** — **ومعه صفُّ دوره في `user_roles`** (قِيس
> 2026-08-20): هذا بابُ إنشاءٍ لا يمرّ بـ`create_account`، وحسابٌ بالعمود وحدَه
> يعتمد على ضمِّ `has_role_clause` — **وذاك ضمٌّ يسقط يومَ يُسقَط العمود**
> (§22.5)، فيصير أولُ مشرفٍ حساباً لا يظهر في قائمة. والبذرةُ تكتبهما معاً
> (`role_grants=[UserRoleGrant(role=…)]`)، واليدويُّ مثلُها.
>
> وبتجزئةٍ من `app.services.auth.password.hash_password`،
> في جلسةٍ واحدةٍ على الخادم، بهاتفِ المالك وكلمةٍ يكتبها هو. **ولا يُرفع
> `ENVIRONMENT` مؤقتاً لتشغيل البذرة**: ذلك يُقلع التطبيقَ ببيئةٍ غيرِ إنتاجيةٍ
> ولو للحظة — فيُقبل المحاكي حيث يُرفض.

**والحسابُ الأول لا يحمل عاملاً ثانياً**، فأولُ عملٍ بعد دخوله تسجيلُ جهازه في
«الأمان» — وإلّا بقي البابُ على كلمةٍ واحدة.

### ٢٥.٧ نطاقاتٌ تجريبيةٌ للقياس (قرارُ المالك 2026-08-20)

**نفقٌ بلا سجلِّ DNS آمنٌ ولا يُقاس**: لا مسارَ حقيقيٌّ يُختبر، فلا زمنُ استجابةٍ
ولا مجموعةٌ تُشغَّل عبر ما سيعمل به الإنتاج. فأُنشئ **نفقٌ ثانٍ مستقلّ**
(`taxo-prod`) بمُعرِّفٍ واعتمادٍ خاصَّين، وعليه **ثلاثةُ نطاقاتٍ تجريبية**:
`stg-api` · `stg-app` · `stg-driver`.

**ونطاقاتُ الإنتاج (`api` · `app` · `driver`) تبقى على النفق المحليِّ بلا مساس،
ولا تُلمس إلا بأمرٍ صريحٍ من المالك.**

**ولا يُنسخ اعتمادُ النفق القائم**: نفقان بالمُعرِّف نفسِه **يتقاسمان الطلبات**
(محذورٌ مكتوبٌ في `cloudflared/config.docker.yml` منذ 2026-08-14)، فيكسر المحليَّ
الذي أُمر ببقائه يعمل.

### ٢٥.٨ توكنا Mapbox — **الفرقُ القيدُ لا النوع** (تصحيحٌ مقيس 2026-08-20)

**ما كُتب أولاً كان خاطئاً**: قيل إن الخلفيةَ تحتاج توكناً **سرّياً (`sk`)
بصلاحية `directions:read`**. **لا وجودَ لهذه الصلاحية** — قِيس من صفحة إنشاء
التوكن عند Mapbox نفسِها: الصلاحياتُ السرّيةُ **لا تتضمّن Directions إطلاقاً**،
وواجهةُ Directions تعمل بتوكنٍ عامّ. فالوصفُ الأول تخمينٌ لُبِّس لباسَ القياس،
ويُصحَّح هنا كي لا يُقرأ لاحقاً قياساً.

**والصحيح: توكنان كلاهما عامّ (`pk`)، والفرقُ بينهما القيدُ لا النوع.**

| | توكنُ الواجهات | توكنُ الخادم |
|---|---|---|
| الحقل | `public_token` | `secret_token` |
| يُنشر لمن؟ | **يصل المتصفحَ** عبر `GET /config` (`expose_to_clients`) | **لا يغادر الخلفيةَ أبداً** |
| القيد | **مقيَّدٌ بنطاقاتنا** — يُستدعى من متصفحاتٍ لها `Origin` | **بلا قيدِ نطاق** — نداءُ خادمٍ بلا `Origin` يُرفض لو قُيّد |
| من يستعمله | `mapbox-gl` في التطبيقين وخريطةِ اللوحة | `services/directions.py` و`providers/health.py` |

**واسمُ الحقل `secret_token` يعني «لا يُنشر»** (`secret=True` في
`providers/registry.py` ⇒ لا `expose_to_clients`)، **لا «توكن من نوع `sk`»**.

**ولا يفحص الكودُ بادئةَ التوكن في أيِّ موضع** — مقيسٌ بمسحِ الخلفية كلِّها:
`directions.py` يقرأ القيمةَ نصّاً ويضعها في `access_token`. فتوكنٌ عامٌّ في هذا
الحقل **يعمل بلا تعديلِ سطر**.

**وخطرُ التوكن غير المقيَّد يُسجَّل صراحةً**: **لو تسرّب استُعمل من أيِّ مكانٍ في
العالم وحوسبنا على استعماله** — لا نطاقَ يردّه ولا مرجعَ يحدّه. **وحمايتُه
الوحيدةُ أنه لا يوجد إلا في `provider_credentials` مشفَّراً بـFernet على الخادم**:
لا في `.env`، ولا في حزمةٍ مبنيّة، ولا في شجرة المشروع. ولذلك **لا يُدخَل إلا من
صفحة العقود على الإنتاج**، ولا تُنقل قيمتُه من التطوير.

### ٢٥.٩ لوحةُ الإدارة تُدخَل باسمِ مستخدم (قرارُ المالك 2026-08-20)

**جدولٌ للمشرفين وحدَهم لا عمودٌ على `users`** (`admin_credentials`). والعلّةُ
تطبيقُ §22: اسمُ المستخدم **صفةُ حسابٍ إداريّ لا صفةُ إنسان**. وعمودٌ على
`users` يجعل «اسمُ مستخدمٍ لراكب» ممكناً **بالبناء** ثم يحتاج حارساً يمنع ما
أتاحه — وهو الشكلُ الذي أنشأ «دورٌ صار مجموعةً».

**والمسحُ سبق البناء**: `users.phone` كان `NOT NULL` وفريداً، و٣٩ موضعاً يقرؤه —
**واحدٌ منها فقط يخصّ مشرفاً**. فالكسرُ محصورٌ في ثلاثة: الدخول، والاستعادة،
و`admin_backups.py` (صار `phone or name`). **والفريدُ باقٍ**: Postgres يعدّ كلَّ
`NULL` مختلفاً، فمشرفون بلا أرقامٍ لا يتزاحمون ورقمٌ حقيقيٌّ لا يتكرر — واختباران
يقيسان النصفين.

**وحسابان لا واحد.** مشرفٌ واحدٌ بلا رقمٍ ولا بريدٍ ولا استعادةٍ ذاتية يعني أن
ضياعَ جهازٍ يقفل اللوحة. والثاني **حسابُ طوارئ** يبقى مغلقاً، **ودخولُه يُكتب في
التدقيق بصفته** (`admin_break_glass_login`) فيُعرف حين يُستعمل.

**واستعادةُ كلمة المرور لا تكون إلا من الخادم** — `scripts/admin_password_reset.py`
على غرار `totp_reset`: بلا فاعلٍ في السجل (الفاعلُ إنسانٌ على الخادم)، وبإبطال
الجلسات. **ومن يفقد الوصولَ إلى الخادم يفقد اللوحة — خطرٌ مقبولٌ مكتوبٌ لا سهو**،
ومخرجُه حسابُ الطوارئ الثاني.

**ومشرفٌ يعدّل مشرفاً آخرَ غيرُ مبنيٍّ عمداً**: يخلق ما تحتاج الاستعادةُ منعَه
(استيلاءٌ من الداخل)، والحسابان اثنان لا فريق، والاستعادةُ من الخادم تغني عنه.
**وشرطُه لو بُني لاحقاً**: يُمنع من المساس بحساب الطوارئ — وإلا صار البابُ الذي
يُفتح للطوارئ هو نفسُه الذي يُغلقها.

### ٢٥.١٠ العاملُ الثاني اختياريٌّ — قرارٌ مأخوذٌ لا سهو (المالك 2026-08-20)

**لا يُشترط بعد أول دخول.** يفعّله المشرفُ من «الأمان» متى شاء، ومفتاحُ
`admin_totp_required` يشترطه على الجميع متى شُغّل — **وهو مطفأٌ اليوم** (افتراضُه
`false` في القاعدة نفسِها: صفٌّ يُدخَل من `psql` ليلاً يجب أن يخرج آمناً لا
مُلزِماً).

**والآليةُ لم تُحذف**: التسجيلُ والتحدي ورموزُ الاسترداد و`totp_reset` كلُّها
مبنيّةٌ ومختبَرة.

**وحسابُ الطوارئ لا يُلزَم مهما كان المفتاح** — وإلا صار العاملُ الثاني **هو ما
يقفل بابَ الطوارئ**: الحسابُ الثاني موجودٌ ليُفتح حين يضيع جهازُ الأول، فاشتراطُ
جهازٍ عليه يجعله يحتاج ما وُجد ليعوّضه. **وثمنُه معلوم**: يبقى على كلمةٍ واحدة،
ولذلك كلُّ دخولٍ به يُكتب في التدقيق — **مكشوفٌ لا محميّ**.

**وما يُقبل بهذا القرار صراحةً**: **كلمةُ المرور وحدَها تفصل بين من يعرفها ولوحةٍ
فيها محافظُ ومال** — سجلُّ الرحلات، وأرصدةُ الكباتن، وطلباتُ السحب، وقيدُ
التصحيح، والشحنُ الإداريّ. **واستعادتُها لا تكون إلا من الخادم** (§25.9). قرارٌ
مأخوذٌ بعد عرض الثمن، لا سهو.

### ٢٥.١١ تشغيلُ العمولة لا يمسّ اشتراكاً قائماً (قرارُ المالك 2026-08-20)

**الوعدُ المعلَن**: «صفر عمولة **ما دام اشتراكك سارياً**» — لا «صفر عمولة»
مطلقة.

**والقاعدة**: إشعالُ العمولة يسري على **الاشتراكات الجديدة وحدَها**؛ ومن اشترك
قبل الإشعال يُكمل مدّتَه بما اشترى عليه، ثم يخضع للجديدة عند التجديد.

**وعلّتُها أعمقُ من صدق الوعد** (تعليلُ المالك): بلا هذا الربط **زرُّ تشغيل
العمولة غيرُ قابلٍ للاستعمال أصلاً** — أولُ ضغطةٍ عليه تمسّ كلَّ من اشترك أمسِ،
فلا يُضغط أبداً ويبقى مبنيّاً لا يُستعمل. **الربطُ هو ما يجعل العمولةَ ممكنةً
يوماً**، لا ما يجعل الوعدَ صادقاً فقط.

**وصارت مبنيّةً لا مقرَّرةً** (2026-08-20، ترحيلة `0051`):

| الطبقة | ما بُني |
|---|---|
| `driver_subscriptions.commission_percent_at_purchase` | **مجمَّدٌ** كـ`list_price`، يكتبه `subscriptions._create` وحدَه — وهو البابُ الوحيد للقنوات الأربع |
| `drivers.commission_percent_from_subscription` | **عمودٌ محضَّر** كـ`advance_blocked` و`level`، يكتبه الشراءُ وتمحوه دورةُ الانتهاء |
| `rides.accept_ride` | يقرأ العمودَ المحضَّرَ ويكتبه على الرحلة — **بلا استعلامٍ إضافي** (§٥-ج) |
| التعبئة | صفرٌ للاشتراكات القائمة — **واقعٌ لا افتراض**: العمولةُ لم تُشغَّل قط، فكلُّهم اشتروا على صفر |

**والحسمُ لحظةَ القبول لا لحظةَ الإنشاء — وهو انحرافٌ عن §4 يُسجَّل هنا.**
§4 تقول «تُجمَّد عند إنشاء الرحلة»، **ولا كبتنَ يُعرف حينها**: التوزيعُ يعرض على
واحدٍ بعد واحد، فنسبةُ **اشتراكه** لا وجودَ لها بعد. فما يُكتب عند الإنشاء نسبةُ
السوق مبدئياً، ويستبدلها `accept_ride` بنسبة صاحبِها الفعليّ **مرةً واحدةً وقبل
أن يتحرك أيُّ مال** (`payments` لا تعمل قبل `completed`). **فالتجميدُ يبقى
واحداً ولا يُعاد حسابُه بعد ذلك أبداً** — وهو ما تعنيه §4، لا لحظتُها بعينها.

**و`NULL` ليست صفراً**: الأولى «لا اشتراكَ ساري ⇒ اقرأ نسبةَ الدولة»، والثانية
«اشتراكٌ اشتُري على صفر». ورقمٌ واحدٌ لا يحملهما — درسُ أصفار `wallet_settings`.

**وحالُ النموذج قبل هذا القرار — مقيسٌ**: `commission_percent_for(session,
country_code)` مُعامِلُها **دولةٌ فقط**، تُنادى مرةً في `rides.request_ride`
لتُجمَّد على الرحلة. و`driver_subscriptions` **لا عمودَ عمولةٍ فيه**. فالتجميدُ
على الرحلة يحمي **رحلةً جارية** ولا يحمي **اشتراكاً قائماً** — وهما شيئان.

---

## 26. الملاحةُ في تطبيق الكبتن — قرارٌ واحدٌ يُسجَّل هنا (2026-08-20)

**خطةُ عمل الخريطة كلُّها في `design/FUTURE-FEATURES.md` §17** — البنودُ السبعةُ بترتيب
المالك، وأربعةٌ منها مبنيّة. **ولا تُنسخ هنا**: خطةٌ ببيتَين تتباعد عند أول تعديل.

**وما يُسجَّل في المواصفة قرارٌ واحد، لأنه يحكم ما لم يُبنَ بعد** — البندُ «التعليمةُ
التالية» (تخزينُ `steps`):

> **التعليمةُ تختفي عند الانحراف ولا تُجمَّد.** الشريطُ يظهر ما دام الكبتن على الخط
> المجمَّد، ويختفي **صامتاً** حين ينحرف — بلا رسالةٍ وبلا «يُعاد الحساب…». ولا يفتح هذا
> باباً ثانياً إلى `Directions`: إعادةُ التوجيه تبقى **المحدودةَ القائمة** (٣ لكل رحلة،
> `rides.reroute_count`)، وإن انطلقت جدّدت **الخطَّ والخطواتِ معاً** فعاد الشريطُ من
> نفسه. **وعتبةُ الانحراف تُقاس من مسارٍ حقيقيٍّ لا تُقدَّر.**

**والعلّةُ قاعدةٌ عامةٌ لا تفصيلُ ملاحة**: **شاشةٌ تقول ما لا يقع أسوأ من شاشةٍ صامتة.**
تعليمةٌ مجمَّدةٌ تُقرأ على أنها الآن، فتصير كذباً لا نقصاً — وهي علّةُ ورقة التأكيد التي
كانت تعرض السعرَ الكامل (§17.7-ب) نفسُها.

**والتفصيلُ الكامل بأسبابه في `design/FUTURE-FEATURES.md` §17، تحت قرار 2026-08-20.**

---

## 27. الصفحةُ التعريفيةُ العامة `taxo.tajora.ly` (2026-08-21)

**صفحةٌ ساكنةٌ يخدمها `nginx` من `landing/` على نفق الخادم** — **قراءةٌ محضة**:
لا نموذجَ تسجيلٍ ولا جمعَ بياناتٍ ولا مسارَ يكتب حرفاً في القاعدة (شرطُ المالك).
**ونطاقاتُ الإنتاج الأربعةُ لا تُمسّ** — نفقان بمُعرِّفين.

### ٢٧.١ ما يُقرأ ولا يُكتب


**ومصدرُ التحميل ثلاثةُ قيمٍ لا اثنتان**: `"none"` · `"apk"` · `"play"`.

**و`"apk"` هي حالُ اليوم بقرارٍ باطّلاع** (المالك 2026-08-21). عُرض عليه أن الحزمتين
موقَّعتان بمفتاح التطوير وتشيران إلى نفقِ جهاز التطوير، **فأبقى الزرّين** وأمر بأن
يُقال للمحمِّل صراحةً في جملةٍ واحدة: من يحمّل الآن سيحتاج **إزالةً وتثبيتاً جديداً**
عند النسخة الرسمية. **فالمستخدم يقرّر وهو يعلم** — ولا تُطفأ عنه ميزةٌ نيابةً عنه.

**والقاعدةُ التي خرجت من هذا أعمُّ من الصفحة**، وموضعُها `CLAUDE.md`: **التنبيهُ
معلومةٌ لا أمر**، والحكمُ عليها للمالك — فما ترتّب على معلومةٍ من تصرُّفٍ **يعطّل
ميزةً أو يُخفي سطحاً أو يضيّق على المستخدم** يُسأل عنه قبل أن يُفعل.

و`"none"` تُخفي الزرّين **والعنوانَ (`dl-heading`) وشرحَ التثبيت وتنبيهَ التوقيع
معهما** بلا مكانٍ محجوز — لأن **عنواناً فوق فراغٍ أسوأُ من زرٍّ معطَّل**؛ والملفّان
يبقيان مخدومَين على `./downloads/` فلا يُفقد `check-apk` عمودَه الرابع. **ولا
يُطلب `manifest.json` أصلاً، ولا يُخرَج من الدالة** — تحتَ كتلةِ التحميل سطرُ
العرضِ القائم، والخروجُ المبكّر كان يقتله.


**وقِيس في الاتجاهين على الصفحة الحيّة**: بـ`"none"` غابت السبعةُ كلُّها ولم
يُطلب البيان؛ وبتبديل الحرفِ وحدَه عاد العنوانُ والزرّان بمسارَيهما وسطرِ البصمة.

| ما يُعرض | من أين |
|---|---|
| العرضُ («الشهر الأول مجاناً») | `GET /public/landing` — **بابٌ عامٌّ للقراءة** |
| بيانُ الحزمة (بصمة · تاريخ · حجم) | `downloads/manifest.json` المولَّد من الملفّ |
| الدعمُ ومصدرُ التحميل | `landing/config.js` |

**والعرضُ يُقرأ ولا يُكتب نصّاً**: عرضٌ أُطفئ أو نفد سقفُه **يختفي سطرُه من
نفسه** — قاعدةُ ورقة الترحيب. **ويُنشر `all`/`new_driver` وحدَهما**: عرضُ
`lapsed`/`manual` يخصّ شخصاً بعينه، ونشرُه على صفحةٍ عامةٍ يَعِد من لا يستحقّه.
**ولا يُنشر سقفٌ ولا ميزانية**: «بقي ٣ مقاعد» ضغطٌ لا نعرف صدقَه لحظةَ القراءة.

### ٢٧.٢ الحزمةُ تُعرَّف ببصمتها لا برقمها

**قِيس (2026-08-21)**: الحزمتان تقولان `versionName=1.0` و`versionCode=1`،
ونسختان بفارقِ **خمسةِ أيامٍ وستةِ ميجابايت** تحملان الرقمَ نفسَه. **فالرقمُ
زينةٌ والبصمةُ هويّة** — وهي معروضةٌ على الصفحة: من يحمّل يعرف ما حمّل، ومن
يشتكي نسأله عن بصمته لا عن نسخته.

**وحارسان**:
- `tools/apk-manifest.mjs` يرفض **بصمةً تغيّرت و`versionCode` لم يتغيّر** —
  أندرويدُ يقرأها النسخةَ نفسَها، فلا تحديثَ يُعرض ومن يحمّل يظنّ أنه حدّث.
  **والبيانُ السابقُ هو الذاكرة**، لا عمودَ ولا ملفَّ حالةٍ ثانٍ.
- `tools/check-apk.mjs` يقارن **ما يُحمَّل من الصفحة** بما بُني — العمودُ الرابع
  في خطوة الصفر.

### ٢٧.٣ الحافةُ عمودٌ خامسٌ ظهر بالقياس

**نُشرت الصفحةُ برقمِ دعمٍ جديدٍ وظلّت Cloudflare تخدم القديم**: `cf-cache-status:
HIT` و`Age: 1086` تحت `max-age=14400`. **الأصلُ صحيحٌ تماماً والناسُ يقرأون نسخةً
عمرُها أربعُ ساعات.**

**وهو الشكلُ العاشر بوجهٍ جديد، وأخطرُ من سابقيه**: يقع **بعد** أن يصير كلُّ
شيءٍ صحيحاً عندنا.

**والعلاجُ عند الأصل لا بالتنظيف اليدويّ** (`landing/nginx.conf`): `no-cache`
لكلِّ `html`/`js`/`json` **وللحزمة**. **والحزمةُ هي الأخطر**: اسمُها ثابتٌ
ومحتواها يتغيّر مع كل بناء، فحافةٌ تخزّنها تعطي إنساناً بناءً قديماً **يبقى في
هاتفه شهوراً**. ومسحُ الذاكرة بعد كل نشرةٍ خطوةٌ تُنسى، **والرأسُ قاعدةٌ تُطبَّق**.

### ٢٧.٤ مصدرُ التحميل إعدادٌ لا رابط

`config.js::source` — `"apk"` اليومَ و`"play"` بعد التوقيع. **تُبدَّل قيمةٌ ولا
تُعاد صفحة.** ويومَ يتبدّل **يختفي معه** شرحُ «السماح من هذا المصدر» وتنبيهُ ما
قبل التوقيع، لأنهما يخصّان نسخةَ ما قبل المتجر.

**وتنبيهُ ما قبل التوقيع يُقال قبل التحميل لا بعده**: الحزمتان موقَّعتان بمفتاح
التطوير (`CN=Android Debug` — قِيس بـ`apksigner`)، ويومَ تُوقَّعان للمتجر
**يرفض أندرويد التحديثَ فوق توقيعٍ آخر** فتلزم إزالةٌ ثم تثبيت. ومن لا يعرف
ذلك يقرأ الرفضَ عطباً في التطبيق. **والصفحةُ تقول معه إن الحسابَ والمحفظةَ
عندنا لا في الهاتف** — فالخوفُ من الإزالة هو ما يمنعها.

### ٢٧.٥ الشهرُ المجاني — **قِيَمُه مقرَّرةٌ ولم يُشعَل** (قرارُ المالك 2026-08-21)

| الحقل | القيمة | لماذا |
|---|---|---|
| `discount_type` | `money_percent` | الخصمُ **مالٌ لا وقت** (الفرعُ الأول من البند ٥٤) |
| `discount_value` | `100` | الشهرُ الأول مجاناً |
| `plan_id` | الخطةُ **الشهرية** | «شهر» — ولا يُترك فارغاً وإلا انطبق على اليوميّ فصار خصمَ دينارٍ ونصف |
| `audience` | `new_driver` | دعوةٌ لمن يسجّل، لا هديةٌ لمن يعمل معنا منذ شهور |
| `max_uses_per_driver` | `1` | مرةٌ لكل كبتن |
| `total_budget` | `300.000` | **≈ عشرةُ كباتن** على خطةٍ بثلاثين ديناراً |

**والسقفُ هو الأمان**: يُعرض العرضُ على من ينطبق عليه، **وينفد بانفاد الميزانية**
— فيختفي سطرُه من الصفحة ومن ورقة الترحيب **من نفسه**، بلا سطرٍ يُحذف وبلا
حاجةٍ إلى أن يتذكّر أحد.

**ولم يُشعَل** — الإشعالُ بيد المالك يومَ يقرّر فتحَ التسجيل. **والخطواتُ
بالضبط:**

1. **مفتاحُ العروض**: «الإعدادات ← مفاتيح الميزات» ← أشعِل
   `subscription_offers_enabled` **للأردن**. (بلاه لا يُقرأ عرضٌ أصلاً — لا في
   التطبيق ولا على الصفحة.)
2. **العرضُ نفسُه**: «الاشتراكات ← العروض ← عرضٌ جديد»، بالقيم الستِّ أعلاه.
3. **ولا خطوةَ ثالثة**: السطرُ يظهر على `taxo.tajora.ly` وفي ورقة ترحيب
   الكبتن **في أول قراءةٍ بعدها** — لا بناءَ ولا نشرَ ولا مسحَ ذاكرة، لأن
   `no-cache` مضبوطٌ عند الأصل ونداءُ العرض لا يُخزَّن.

**وللتراجع**: إطفاءُ المفتاح أو تعطيلُ صفِّ العرض — والسطرُ يختفي في القراءة
التالية. **ولا يمسّ ذلك من اشترى**: `offer_id` و`discount_amount` مجمَّدان على
صفِّ الاشتراك (البند ٥٤).


**ومقيسٌ 2026-08-21 على الإنتاج**: الأردنُ ينشر `verification: firebase` بينما
`otp_verification_enabled` مشتعل — أي **تحققٌ مطلوبٌ ومستحيل**، وهي عينُ نتيجةِ
تدقيق 2026-08-19 التي أُصلحت على جهاز التطوير ولم تُنقل. **فمراجعةُ مفاتيح السوق
تُعاد لكلِّ بيئةٍ على حِدة**، ولا تُقرأ نتيجةُ بيئةٍ حكماً على أخرى: العقدُ عالميٌّ
والمفتاحُ لكلِّ دولة، فما يعبر البيئاتِ هو العقدُ وحدَه.

**وبقاؤه مطفأً قرارٌ لا سهو** (قرارُ المالك 2026-08-21). `whatsapp_otp_enabled/JO`
**يبقى مطفأً على الإنتاج حتى تصل الشريحةُ الأردنية**، والسببُ عينُ ما في §24.8:
رقمُ الإرسال هناك **بريطانيٌّ عارٍ**، ورسالةُ تحققٍ من رقمٍ أجنبيٍّ مجهولٍ **مشكَّلةٌ
كالاحتيال** — والبلاغاتُ هي ما يُحظر به الرقم. فالتسجيلُ على الإنتاج **مغلقٌ بعلم**،
ولا يُعطِّل شيئاً اليوم (حسابان، ولا كبتنَ واحد). **ومن وجده مطفأً لاحقاً يعرف
لماذا**، فلا يُشعله ظانّاً أنه نسيان.

**وإشعالُه يومَ تصل الشريحة خطوتان بهذا الترتيب**: يُحذف سطرُ `JO/verification` من
`DELIBERATE` في `tools/check-markets.mjs` **أولاً** — فيصير الفحصُ نفسُه هو ما
يذكّر بالثانية — ثم يُشعل الصفُّ من اللوحة.

**والفحصُ الذي يكشف هذا مرةً أخرى**: `scripts/check-markets.sh <أساس>`. ثلاثةُ
قراراتٍ فيه تستحق القراءة قبل تعديله:

1. **مرجعُه ما تشحنه `scripts/seed.py::FEATURE_DEFAULTS`، لا جهازُ التطوير.** أولُ
   صياغةٍ قارنت البيئتين مباشرةً فأخرجت **١٣ سطراً، ١٢ منها ضجيج**: قاعدةُ التطوير
   **تُبدَّل عمداً** في كلِّ جولةٍ بصرية، والقاعدةُ مكتوبةٌ منذ 10-ج. **وحارسٌ يصيح
   على سليمٍ يُطفأ**، فيسقط معه ما يمسكه حقاً.
2. **ويُقرأ المرجعُ من الوحدة التي تشحنه** لا بتحليل نصِّها — قاعدةُ «يُقرأ من
   الأداة التي تستهلكه» في لباسٍ ثالث.
3. **وفيه قاعدةٌ لا تحتاج بيئةً ثانيةً أصلاً**: **سوقٌ يشترط التحقق ولا ينشر قناةً
   تعمل عطبٌ في نفسه** — وهذه وحدَها كانت تكفي لكشفِ ما كُشف بالصدفة بعد شهر.

**وعذرٌ باقٍ بعد زوال سببه كذبةٌ تُقرأ حارساً**، فالفحصُ يسقط أيضاً على عذرٍ بائد:
يومَ تصير قناةُ الأردن حيّةً يقول «احذفه».

