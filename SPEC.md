# SPEC.md — TAXO | تطبيق توصيل تاكسي

> هذا الملف هو المرجع الوحيد للمشروع. أي غموض → اسأل قبل التنفيذ. لا تضف ميزات غير مذكورة هنا.

---

## 1. نظرة عامة

**اسم التطبيق: TAXO** — يُستخدم في كل الواجهات، عناوين PWA manifest، ورسائل النظام.

منصة توصيل تاكسي مكونة من ثلاثة تطبيقات + خلفية واحدة:

| التطبيق | المستخدم | النوع |
|---------|----------|-------|
| Customer App | الراكب | PWA (موبايل أولاً) |
| Driver App | الكبتن | PWA (موبايل أولاً) |
| Admin Panel | الإدارة | Web (ديسكتوب أولاً) |

**الأسواق:**
- 🇱🇾 ليبيا — مرحلة التجريب (كاش فقط، عملة LYD)
- 🇯🇴 الأردن — الإطلاق الفعلي (كل طرق الدفع، عملة JOD)

كل الإعدادات الحساسة (طرق الدفع، الميزات، الأسعار) تُدار **per-country** من لوحة الإدارة عبر نظام Feature Flags — بدون تعديل كود.

**نموذج الربح:** اشتراك مسبق الدفع للكبتن (يومي/أسبوعي/شهري) + صفر عمولة على الرحلات مبدئياً، مع مفتاح لتفعيل عمولة نسبية من الإدارة لاحقاً.

---

## 2. التقنيات (ملزمة — لا تستبدلها)

### الخلفية
- **Python 3.12+ (يُفضَّل أحدث إصدار مستقر، 3.14 مقبول) + FastAPI** — REST API + WebSockets. قبل التثبيت تحقق أن كل الاعتماديات (SQLAlchemy, Celery, asyncpg) تدعم الإصدار المختار، وإن ظهر تعارض انزل لـ 3.12
- **PostgreSQL 16 + PostGIS** — البيانات + الاستعلامات الجغرافية
- **Redis** — مواقع السائقين اللحظية (GEO commands) + قنوات pub/sub + كاش
- **SQLAlchemy 2 (async) + Alembic** — ORM + migrations
- **Celery + Redis broker** — مهام خلفية (إشعارات، انتهاء اشتراكات، تقارير)
- **JWT** (access + refresh) — المصادقة. **الدخول اليومي بكلمة المرور دائماً ولكل المستخدمين.**
  و**OTP تحقّقٌ لا دخول**: إثباتُ ملكية الرقم يقع مرتين في عمر الحساب — عند التسجيل وعند استعادة
  كلمة المرور — عبر **Firebase Phone Auth** (التطبيق يتحقق عندهم ويرسل رمز الهوية، والخلفية تتحقق
  منه) أو عبر مزود SMS تقليدي حين لا عقد Firebase. ولا يدخل النظامَ أحدٌ بتوكن Firebase: ذاك إثباتُ
  ملكيةِ رقمٍ لا جلسةَ عمل، والجلساتُ تُصدرها الخلفية وتُبطلها (القسم 15/أ)
- إدارة الأسرار: أسرار البنية التحتية فقط (DB, Redis, JWT, مفتاح التشفير) في `.env` — **أما كل مفاتيح المزودين الخارجيين فمن صفحة العقود حصراً** (القسم 15)

### الواجهات
- **React 18 + Vite + TypeScript**
- **Tailwind CSS** — مع دعم RTL كامل من اليوم الأول (`dir="rtl"`, خصائص `start/end`)
- **shadcn/ui** — المكونات (خصوصاً Drawer للـ bottom sheets)
- **Framer Motion** — الحركات والانتقالات
- **Lucide Icons**
- خط عربي: **IBM Plex Sans Arabic**
- وضع ليلي كامل (`dark:`) — إلزامي لتطبيق الكبتن
- PWA: manifest + service worker (لاحقاً تُغلّف بـ Capacitor)

### الخرائط — Mapbox
- **Mapbox GL JS** — العرض (ستايل `streets-v12` نهاري + `dark-v11` ليلي)
- **Directions API** — المسار + المسافة + المدة (تُستدعى من الخلفية فقط)
- **Geocoding API** — العناوين (مع اعتماد أساسي على تحديد الموقع بالدبوس)
- التوكنات تُدار من صفحة العقود (لا `.env`): التوكن السري (sk) للخلفية حصراً، والتوكن العام (pk، مقيد بالنطاق) تسلّمه الخلفية للواجهات عبر endpoint إعدادات عام `GET /config`

### الدفع
- **Telr** — بطاقات + Apple Pay + Google Pay (Hosted Payment Page + Webhooks، وضع Sandbox أثناء التطوير)
- **CliQ** — يدوي: عرض alias + تأكيد بشري (لا يوجد API)
- **كاش** — تأكيد الكبتن
- **المحفظة الداخلية** — خصم فوري

---

## 3. هيكل المشروع

```
taxo/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/          # config, security, deps
│   │   ├── models/        # SQLAlchemy models
│   │   ├── schemas/       # Pydantic
│   │   ├── routers/       # auth, riders, drivers, rides,
│   │   │                  # payments, wallets, subscriptions, admin
│   │   ├── services/      # dispatch, pricing, wallet, telr
│   │   ├── ws/            # WebSocket: tracking, ride events
│   │   └── tasks/         # Celery
│   ├── alembic/
│   └── tests/
├── customer-app/          # React PWA
├── driver-app/            # React PWA
├── admin-panel/           # React
└── docker-compose.yml     # postgres+postgis, redis, backend
```

---

## 4. قاعدة البيانات (الجداول الأساسية)

> كل الجداول تحتوي `id (uuid)`, `created_at`, `updated_at`. الأعمدة المالية `NUMERIC(12,3)` — ممنوع float.

### users
- `phone` (unique — هو مُعرّف الدخول), `name`, `role` (rider/driver/admin/support)
  - **وبادئةُ كل دولة وطولُ رقمها الوطني يُنشران في `GET /config`** (`dial_code`,
    `national_number_length`) من `core/phone.py` نفسه، ومعهما `default_country_code` —
    الدولة التي تفترضها شاشاتُ ما قبل الدخول. فالتطبيقان يرسمان البادئة ثابتةً أمام الحقل
    ويحذفان الصفر البادئ، بلا كتابة «962» في كودهما: قيمةٌ مكتوبةٌ في تطبيقين تفترق عن ذلك
    الجدول يوماً وتفترق عن نفسها في التطبيقين قبله
- `country_code` (LY/JO), `is_blocked`, `password_hash` — **الدخول بهاتف + كلمة مرور دائماً**
  (المرحلة 8-ب). العمود nullable لحساباتٍ أُنشئت قبلها بـ OTP وحده، ولا يُفتح حسابٌ بلا كلمة مرور
  بواحدةٍ يخترعها أحد — مخرجُه استعادةُ كلمة المرور
- `phone_verified_at` (nullable) — **لحظةُ إثبات ملكية الرقم** (المرحلة 8-ب). فارغةٌ تعني رقماً غير
  محقق، ولا تقع إلا حين يُطفئ المشرف `otp_verification_enabled` للطوارئ. الحساب حينها **موسومٌ في
  اللوحة وقابلٌ للفلترة** ويُطالَب بالتحقق عند أول فرصة (`POST /auth/me/verify-phone`)، **ولا يُعتمد
  كبتنٌ قبل ملئها مهما كان المفتاح**: رقمُ الكبتن هو ما يستلم عليه حوالات كليك (القسم 6/9)
- `marketing_push_enabled` (افتراضي true) — إشعارات الحملات وحدها (القسم 10)
- `wallet_frozen` — تجميد المحفظة وحدها دون حظر الحساب (القسم 7/13.3): تُوقَف حركة محفظةٍ مشبوهة بينما يبقى صاحبها راكباً يدفع نقداً
- **`gender`** (male/female، nullable) و**`gender_verified_at`** (nullable) — خدمة التوصيل النسائي
  (المرحلة 10-ج). قيمتان لا ثلاث و**الغيابُ `NULL` لا عضوٌ ثالث**: «غير معروف» ليس جنساً، وجعلُه
  قيمةً يجعل الاستعلام يقارنه بجنسٍ فيطابق. ومصدرُ الحقل يختلف باختلاف من يعلنه، وهو فرقٌ في
  الأمان لا في التنفيذ:
  - **الراكبة تعلنه عن نفسها** عند التسجيل أو من ملفها (`PATCH /auth/me`) — إعلانُها يقيّد رحلتَها
    هي، فلا يُختم ولا يُشترط ختمُه.
  - **وجنسُ الكبتن يضبطه المشرف** من هويته المرفوعة (`PUT /admin/drivers/{id}/gender`، admin
    حصراً، بقيدِ تدقيق) ويُختم بـ`gender_verified_at`. **ومسارُ تسجيل الكبتن يرفض الحقل** ولا
    يُهمله صامتاً. وسببُ التشديد أن إعلانه يقيّد أمانَ غيره: بلا ختمٍ يصير بلوغُ صفة «سائقة
    للنساء» كتابةَ كلمةٍ في حقل.
  - **والمطابقة تقرأ المختوم وحده** في جانب الكبتن. وضبطُ الجنس **لا يعيد دورة اعتماد**: الهوية
    مرفوعةٌ ومراجَعةٌ أصلاً، وإرجاعُ كبتنٍ معتمدٍ إلى الطابور لأجل حقلٍ واحد يجعل تفريغ متراكم
    المعتمدين مستحيلاً عملياً — وهو المتراكم الذي يبقى `women_service_enabled` مطفأً حتى يُفرَّغ
    (`GET /admin/drivers?gender_verified=false` هو ما يجعله مرئياً)
- **`ride_gender_preference`** (male/female/any، افتراضي `any`) — تفضيلُ الراكبة الافتراضي، يُنسخ
  إلى الرحلة عند الطلب ولا يُقرأ بعده
- **`gender_mismatch_reports`** (افتراضي 0) — عددُ بلاغات «الطرف ليس بالجنس المعلَن» على الحساب.
  **عدّادٌ لا راية**: الوسمُ سؤالٌ عن العدد مقابل حدٍّ في الخدمة (ثلاثة)، فتغييرُ الحدّ يعيد تقييم
  الجميع ولا يترك حساباتٍ موسومةً بحدٍّ قديم. وثلاثةٌ لا واحد لأن بلاغاً واحداً قد يكون سوءَ فهم،
  ووسمٌ من أول بلاغ سلاحٌ بيد من يريد الإضرار

### drivers (يمتد من users)
- `user_id`, `status` (pending/approved/rejected/suspended)
- `cliq_alias` (لاستلام التحويلات), `rating_avg`
- `is_online`, `current_ride_id`
- **`gender_preference`** (male/female/any، افتراضي `any`) — تفضيلُ جنس الركاب. **دائمٌ لا لكل
  رحلة**: الراكبة تختار لرحلةٍ بعينها والكبتن يقرر لعمله كلِّه، فمكانُه هنا لا على الرحلة

### driver_documents (مستندات الكبتن — المرحلة 9-ب)

الجدول قائمٌ منذ المرحلة 1 ولم يكن له مسارٌ يكتبه: كان الاعتماد يمر والمستنداتُ فارغة. المرحلة 9-ب
بنت الرفع والمراجعة وأضافت عمودَي وصف الملف.

- `driver_id`, `doc_type` (driving_license/national_id/vehicle_registration/vehicle_photo)
- `file_path` — **مسارٌ نسبي** إلى جذر التخزين، و`content_type` و`size_bytes` مستنتجان من **بايتات
  الملف** لا من ترويسة العميل
- `review_status` (pending/approved/rejected), `review_note`, `reviewed_by`, `reviewed_at`
- **فريد `(driver_id, doc_type)`**: النوع الواحد صفٌّ واحد، ورفعُه ثانيةً **استبدال** يعيد الحالة
  `pending` ويمحو أثر المراجعة السابقة — مراجعةٌ لملفٍ لم يعد موجوداً حكمٌ على غائب. وثلاثُ رخصٍ
  متراكمة تجعل سؤال «هل رخصتُه مقبولة؟» بلا جواب واحد
- **قرارُ سياسة: استبدالُ مستندٍ مطلوبٍ يُسقط اعتماد الكبتن نفسه** إلى `pending` تلقائياً حتى
  تُراجَع الوثيقة الجديدة. الاعتماد شهادةٌ على مستنداتٍ بعينها رآها مشرف؛ فمن استبدلها صار
  معتمَداً على ما لم يره أحد — وذاك بابُ تبديلِ رخصةٍ سارية برخصةٍ منتهية **بعد** الاعتماد،
  والاعتمادُ يفتح باب استقبال الطلبات وقبض المال. القيدُ يدخل سجل التدقيق بـ
  `{"status": "pending", "reason": "document_replaced"}` **بلا فاعلٍ مشرف** — فعلُ الكبتن نفسه هو
  ما أسقطه، وسؤال «لماذا سقط اعتمادُه؟» يُسأل بعد شهر. والمستندُ الاختياري (`vehicle_photo`) لا
  يمسّ الاعتماد لأنه لا يُشترط له
- **ويُرفض الاستبدال وحده أثناء رحلةٍ جارية** (409): إسقاطُ الاعتماد وسطها يُخرج الكبتن من
  `presence_context` فيتجمّد موقعُه على خريطة راكبه ثم يُطلق تنبيهَ «انقطع اتصال الكبتن» (القسم 5)
  على من لم ينقطع — نفس الاعتبار الذي يجعل كنسَ الاشتراكات يترك صاحب الرحلة الجارية أونلاين.
  والانتظارُ إلى نهاية الرحلة نافذةٌ واحدة قصيرة. والمستندُ الاختياري يمر: لا يمسّ الاعتماد فلا
  يمسّ الرحلة
- **وجوابُ الرفع يقول ذلك صراحةً** (`approval_reverted` و`driver_status`): بغيره يكتشف الكبتن أنه
  خرج من التوزيع حين لا تصله طلبات، لا حين فعلَ ما أخرجه
- **المطلوب للاعتماد ثلاثة**: الرخصة والهوية ورخصة المركبة والتأمين. `vehicle_photo` خارجها —
  تُطمئن الراكب ولا تُثبت حقاً
- التخزين **على القرص لا في خدمةٍ سحابية**: مفاتيح أي مزوّد خارجي مكانها `provider_credentials`
  وصفحةُ العقود، فإدخال S3 اليوم يعني مفتاحاً في `.env` وهو ما يمنعه القسم 14. والمجلد حجمٌ مسمّى
  في compose، خارج Git
- **ولا يُخدَم بخادم ملفاتٍ ساكن**: القراءة تمر بمسارٍ يتحقق من الملكية أولاً، واسمُ الملف على
  القرص `uuid4` بامتدادٍ من محتواه — لا اسمَ العميل ولا امتدادَه (اجتياز المسار)

### vehicles
- `driver_id`, `make`, `model`, `year`, `color`, `plate_number`, `category` (economy/comfort)

### rides
- `rider_id`, `driver_id` (nullable قبل القبول)
- `pickup_point` (geography POINT), `pickup_address`
- `dropoff_point`, `dropoff_address`
- `status`: `requested → searching → accepted → arrived → in_progress → completed | cancelled_by_rider | cancelled_by_driver | no_driver_found`
  - **لا انتقال مباشر `requested → accepted`.** مخرجا `requested` هما `searching` (يدخلها التوزيع) أو `cancelled_by_rider` لا غير، فالقبول لا يكون إلا من `searching` وبعد عرضٍ قائم (القسم 5.4)
  - و**`at_stop`** (المرحلة 12-ب) بين `in_progress` ونفسها: `in_progress → at_stop → in_progress`
    لكل محطةٍ وسيطة، ومخرجٌ ثالثٌ منها `at_stop → completed` حين يُنهي الكبتن الرحلة عند المحطة
    بعد تجاوز سقف الانتظار (القسم 5.10). **ولا إلغاء من `at_stop`** كما لا إلغاء من `in_progress`:
    الراكب في السيارة، والانتظارُ جزءٌ من الرحلة لا فاصلٌ يعيد فتح ما أُغلق
- **`stops_count`** (المرحلة 12-ب، افتراضي 0) — عددُ المحطات الوسيطة. عمودٌ على الرحلة لا عدٌّ
  للجدول: يُقرأ في كل بطاقةِ عرضٍ وفي كل صفٍّ في اللوحة، وعدُّ جدولٍ لكل صفٍّ هو بالضبط ما
  تتجنبه بقيةُ القوائم
- **`current_leg`** (المرحلة 12-ب، افتراضي 0) — أيُّ ساقٍ يسير فيها الكبتن الآن، ومنها تُنسب
  نقاطُ المسار. **كاتبُها واحدٌ لا غير** (`rides.resume_stop` تحت قفل صف الرحلة)، فلا تفترق عن
  عدد المحطات المستأنَفة
- `distance_km`, `duration_min` (من Mapbox Directions)
- `actual_distance_km` (nullable) — المسافة المقطوعة فعلاً محسوبةً من `ride_route_points` عند الإنهاء
  (القسم 5.7). **فارغة لا صفر** حين لا تكفي النقاط: صفرٌ يعني «سارت صفر كيلومتر» فيهبط السعر إلى
  الحد الأدنى ظلماً بسبب تطبيقٍ صمت
- `estimated_fare`, `final_fare`, `currency`
- **`commission_percent_at_ride`** — تُجمّد لحظة إنشاء الرحلة (صفر مبدئياً). ممنوع إعادة حسابها بأثر رجعي
- **وأربعةُ حقولِ انتظارٍ مجمَّدةٌ معها** (المرحلة 12-ب): `stop_fee_at_ride` و
  `stop_free_minutes_at_ride` و`stop_price_per_min_at_ride` و`stop_max_wait_minutes_at_ride`.
  تُنسخ من `pricing_rules` لحظة الإنشاء ولا تُقرأ من الإعدادات بعدها أبداً — **نفس منطق
  العمولة حرفاً بحرف**: مشرفٌ يرفع سعر الدقيقة ورحلةٌ واقفةٌ عند محطةٍ الآن لا يجوز أن يتغيّر
  عدّادُها تحت عين راكبها. وأربعةٌ لا واحد لأن كلَّ واحدٍ منها يدخل الحساب وحدَه
- **`gender_preference`** (male/female/any، افتراضي `any`) — ما طُلب في **هذه** الرحلة. يُنسخ من
  ملف الراكبة عند الطلب و**يُجمَّد** كالعمولة: تغييرُ التفضيل في الملف يحكم الطلب القادم لا طلباً
  يبحث له عن كبتنٍ الآن
- `cancelled_reason` (نصٌّ حر) و**`cancel_reason_code`** (`gender_mismatch` / `other`، nullable)
  — سببٌ **مصنَّف** بجانب النص، وهو `String(32)` محروسٌ في طبقة Pydantic لا ENUM في القاعدة (نفس
  استثناء `feature_flags.feature_key`). و`gender_mismatch` ليست تصنيفاً للتقارير: هي التي **تُسقط
  رسوم الإلغاء** وتُدخل بلاغاً على حساب الطرف الآخر، فلا تُترك لنصٍّ حر
- timestamps لكل انتقال حالة

### ride_stops (المحطات الوسيطة — تعدد الوجهات، المرحلة 12-ب)

الراكب يضيف **حتى ثلاث وجهات** في الرحلة الواحدة. والوجهةُ الأخيرة تبقى
`rides.dropoff_point` كما كانت، وهذا الجدول يحمل **الوسيطة وحدها** (صفراً إلى اثنتين):

- **ولماذا لا تُنقل الوجهاتُ الثلاث كلُّها إلى الجدول؟** لأن `dropoff_point` يقرؤه اليوم التسعيرُ
  والتوزيعُ وبطاقةُ العرض وسجلُّ اللوحة وإطاراتُ المقبس. ونقلُه يعني إمّا تعديلَ كلِّ ذلك، وإمّا
  إبقاءَ العمود **مرآةً** لآخر صفٍّ في الجدول — ومرآةٌ تُكتب في مكانين تفترق يوماً. فالوجهةُ
  الأخيرة عمودٌ واحدٌ كما كانت، والوسيطةُ صفوفٌ: لا قيمةَ لها موضعان.
- **والترتيبُ والحذفُ يقعان قبل الطلب لا بعده**: الراكب يرتّب ويحذف في شاشة الطلب، فتصل الخلفيةَ
  قائمةٌ مرتَّبة. فلا مسارَ إعادةِ ترتيبٍ على رحلةٍ قائمة — تغييرُ الوجهة بعد أن سار الكبتن
  يغيّر السعرَ المتفق عليه، وهو طلبٌ جديد لا تعديل.

الأعمدة:

- `ride_id`, `sequence` (1..2) — **فريد `(ride_id, sequence)`**
- `point` (geography POINT), `address` (nullable)
- `arrived_at`, `resumed_at` (كلاهما nullable) — **نافذةُ الانتظار، وهي مصدرُها الوحيد**:
  زمنُ الانتظار **يُحسب من ختم الخلفية لا من مؤقتٍ في الواجهة** (القسم 14)، ويُقفل عند
  الاستئناف. **ولا عمودَ `waited_minutes`**: عمودان لزمنٍ واحد عمودان يفترقان — نفس سبب غياب
  `recorded_at` عن `ride_route_points`
- `notified_at` (nullable) — لحظةُ تنبيه الطرفين ببلوغ سقف الانتظار، **أثرٌ يمنع تكرار التنبيه**
  في كل دورة كنس (نفس دور مفتاح Redis في تنبيه الاشتراك، وعمودٌ هنا لأن الصفَّ قائمٌ أصلاً)
- `ON DELETE RESTRICT` كبقية ما يتعلق بالرحلة: المحطةُ جزءٌ من فاتورةٍ، لا تُمحى بمحو غيرها

### ride_route_points (نقاط المسار الفعلي — القسم 5.7)
- `ride_id`, `point` (geography POINT), `heading` (nullable)
- **`leg`** (المرحلة 12-ب، افتراضي 0) — أيُّ ساقٍ من الرحلة: صفرٌ من الانطلاق إلى المحطة الأولى،
  ثم واحدٌ منها إلى التالية، وهكذا حتى الوجهة الأخيرة. **تُنسخ من `rides.current_leg`** لحظة
  الالتقاط، فلا تُحسب من الصفوف ولا يعرفها الملتقِط من عنده. وبها تُقرأ المسافةُ الفعلية
  **لكل ساقٍ على حدة** في تفاصيل الرحلة وفي دليل النزاع
- **لا عمود `recorded_at`:** حمولة البثّ `{lat, lng, heading}` بلا زمن، فزمن التسجيل هو `created_at`
  نفسه — وعمودان لزمنٍ واحد عمودان يفترقان. الترتيب الزمني على فهرس `(ride_id, created_at)`
- الاستثناء الوحيد لقاعدة «الموقع اللحظي في Redis وحده» (القسم 10): للنقاط غرضان لا يؤديهما مفتاحٌ
  عمره 60 ثانية — المسافة الفعلية ودليلُ النزاع
- الصف يُكتب ولا يُعدَّل، لكن **بلا مُشغّل قاعدة** بخلاف `wallet_transactions`: هذه أدلةٌ لا قيود
  مالية، وكلفةُ مُشغّلٍ على جدولٍ يستقبل نقطةً كل 20 ثانية لكل رحلة جارية أعلى من فائدته

### payments
- `ride_id`, `method` (cash/cliq/card/wallet), `provider` (telr/null)
- `provider_payment_id`, `amount`, `currency`
- `status`: `pending → confirmed | failed | disputed | refunded`
- `confirmed_by` (driver/system/admin), `confirmed_at`
- `idempotency_key` (nullable، فريد) — يفرضه القسم 14 على **الدفع** كما على التحويل: webhook يُعاد إرساله أو ضغطةٌ مكررة لا يجوز أن تُحصّل مرتين
- `transaction_id` (nullable) — القيد الناتج في دفتر المحفظة حين تُدفع الرحلة من الرصيد؛ الوصلة بين
  الدفعة وأثرها المالي، كما في `wallet_topup_requests` و`withdrawal_requests`
- **حقول قناة كليك** (المرحلة 9، ترحيلة `0013`): `cliq_alias` (المعروض لحظتَها — **مُجمَّد**، فلا
  يغيّر الكبتنُ وجهةَ حوالةٍ وقعت)، `cliq_reference` (المرجع الداخلي الذي يولّده TAXO، **فريد**)،
  `cliq_transfer_reference` + `cliq_reference_at` (ما أدخله الراكب ومتى — **يُكتبان مرةً واحدة**).
  بهذه الأربعة يكتمل ما يطلبه القسم 6.5: المبلغ وalias والمرجعان ومن أكّد ومتى. وقيدان في القاعدة:
  حقولُ كليك لا تُملأ على دفعةٍ بقناةٍ أخرى، و`(cliq_transfer_reference IS NULL) = (cliq_reference_at IS NULL)`
- **حقول فصل النزاع:** `dispute_reason`, `disputed_at`, `resolution` (paid/unpaid), `resolution_note`,
  `resolved_by`, `resolved_at`
  - `resolution` تصف **الواقعة** لا الحالة الناتجة: `paid` = وصل المالُ الكبتنَ فعلاً فتصير الدفعة
    `confirmed` وتُحسب عمولتها، و`unpaid` = لم يصل فتصير `failed` ويعود مبلغها ديناً على الرحلة يفتح
    له الراكب قناةً أخرى. اشتقاق الحالة من الحكم في مكان واحد يمنع صفّاً محكوماً بحالةٍ تناقض حكمه
  - قيد في القاعدة: `(resolution IS NULL) = (resolved_at IS NULL)` — حكمٌ بلا زمنٍ أو زمنٌ بلا حكم
    نصفُ فصلٍ، والنصف لا يُقرأ
- دعم الدفع المختلط: صفان مرتبطان بنفس الرحلة (wallet + cash). لذلك **لا قيد فريد على `ride_id`**،
  وحارسُ «لا تُدفع الرحلة مرتين» هو مجموع الدفعات في `pending`/`confirmed`/`disputed` مقابل
  `final_fare` تحت قفل صف الرحلة
- **قناة البطاقة لا تُقيَّد على محفظة الراكب.** ثلاث فئات لا فئتان: ما لا يمر بالمنصة (كاش وكليك)،
  وما يمر بها من المحفظة، وما يمر بها من **غير** المحفظة — البطاقة. مالُ البطاقة ينتقل من بطاقة
  الراكب إلى المزود مباشرة، فقيدُ `ride_payment` على رصيده يخصم منه مرتين. والكبتن يُقيَّد له
  `ride_earning` في الفئتين الأخيرتين معاً (القسم 6.3). الفئات مكتوبة قوائمَ في
  `models/payment.py` (`DIRECTLY_COLLECTED_METHODS` و`WALLET_FUNDED_METHODS`) لا شروطاً مبثوثة

### provider_orders (طلبات الدفع لدى مزود البطاقات — القسم 6.4، المرحلة 6-ب)

جدولٌ **أضافته المرحلة 6-ب** ولم يكن في هذا القسم: بين لحظة إنشاء صفحة الدفع ولحظة جواب المزود
فراغٌ زمنيٌّ يعيش فيه العميل على شبكةٍ لا نملكها — يغادر الراكب الصفحة، أو يصل الـ webhook مرتين،
أو يصل قبل أن يعود المتصفح. لا صفَّ في المخطط السابق يعرف «طلبٌ فُتح ولم يُحسم بعد»، ولا مكانَ
لمُعرِّفٍ نرسله للمزود ويعود به إلينا. بديلاه كانا أسوأ: تحميلُ `payments.provider_payment_id`
معنيين، أو صفُّ `wallet_topup_requests` لشحن بطاقةٍ لا ينتظر إنساناً — وهو نقضٌ لتعريف ذلك الجدول.

- `provider` (telr), `purpose` (ride_payment / wallet_topup / **subscription** — أضافته المرحلة 7)،
  `status` (created/paid/failed/cancelled)
- `cart_id` (**فريد**) — ما نرسله للمزود ويعود به؛ به وحده يجد الإشعار صاحبَه ولا يجد صاحبين.
  قصيرٌ عمداً (25 حرفاً) لسقف `ivp_cart` لدى Telr
- `provider_order_ref`, `redirect_url`, `failure_reason`, `refund_ref`
- `user_id` (الدافع), `country_code` (**دولة العقد** — العقود per-country، فبغير تثبيتها يُستعلم
  طلبُ الأمس بعقد اليوم إن انتقل صاحبه دولة), `amount`, `currency`
- `ride_id` + `payment_id` لغرض `ride_payment`، و`transaction_id` لغرض `wallet_topup`، و`plan_id`
  لغرض `subscription` (المرحلة 7) — **الخطةُ لا الاشتراك**: صفُّ الاشتراك لا يُنشأ إلا بعد أن يقول
  المزود «دُفع»، فالطلب يحمل ما يكفي لإنشائه لا وصلةً إلى صفٍّ قد لا يوجد أبداً
- `save_card` — حفظُ البطاقة قرار صاحبها لا افتراضنا
- ثلاثة قيود في القاعدة: `(purpose='ride_payment') = (ride_id IS NOT NULL)` والمثلُه على
  `payment_id`، و`(purpose='subscription') = (plan_id IS NOT NULL)`
- **حالةُ الطلب هي حارس عدم التكرار الأول:** `created` وحدها تُسوّى، فإشعارٌ مُعاد لا حدث

### saved_cards (البطاقات المحفوظة — Tokenization، القسم 6.4)
- `user_id`, `provider` (telr), `provider_token`
- `brand`, `last4`, `expiry_month`, `expiry_year`, `is_default`
- **لا رقم بطاقة ولا CVV ولا أي بيانٍ يمكن أن يُدفع به.** المخزَّن رمزٌ يصدره المزود ولا يعمل إلا لدى
  نفس المتجر، وما يكفي لعرض البطاقة لصاحبها. هذا هو كل غرض الـ tokenization: أن يبقى رقم البطاقة عند
  المزود لا عندنا
- فريد `(user_id, provider, provider_token)`؛ و`ON DELETE CASCADE` بخلاف بقية الجداول المالية —
  الرمز ليس سجلاً محاسبياً يُحتفظ به بل بيانُ راحةٍ لا معنى له بعد ذهاب صاحبه
- الجدول من **المرحلة 6-أ** ويملؤه الكود في **6-ب**: توليد `payment_provider` وقيود الجدول مع بقية
  مخطط الدفع في ترحيلة واحدة أرخص من تعديل ENUM على جدولٍ حيّ لاحقاً
- **`provider_token` لا يخرج من الخلفية أبداً** (6-ب): هو ما يُدفع به، فعرضُه يبطل غرض
  الـ tokenization كله. مخطط الخروج يحمل العلامة وآخر أربعة والانتهاء و`is_default` لا غير
- أولى بطاقات المستخدم هي الافتراضية بلا اختيار، وحذفُ الافتراضية يرقّي غيرها: محفظةُ بطاقاتٍ بلا
  افتراضية تجعل «الدفع بضغطة» بلا ضغطة تُعرض

### device_tokens (رموز أجهزة الإشعارات — القسم 10، المرحلة 8)

جدولٌ **أضافته المرحلة 8** ولم يكن في هذا القسم: إشعار Push لا يُرسل إلى مستخدم بل إلى **جهاز**،
ورمزُ الجهاز يصدره FCM على الجهاز نفسه فلا سبيل لمعرفته إلا أن يرسله التطبيق ويُحفظ. وعمودٌ على
`users` كان يفترض جهازاً واحداً لحسابٍ قد يكون على هاتفٍ ولوحٍ ومتصفح معاً.

- `user_id`, `device_id` (مُعرّف ثابت يولّده التطبيق), `token` (رمز FCM), `platform` (ios/android/web)
- `last_seen_at`, `is_active`
- **`token` فريد على الجدول كله**: رمز FCM يعرّف تثبيتَ تطبيقٍ واحد، فإن سجّل عليه حسابٌ آخر انتقل
  الصفُّ إليه — وإلا وصل إشعارُ الحساب السابق إلى من يحمل الهاتف الآن
- **`(user_id, device_id)` فريد**: الرمز يدور من تلقاء FCM، فالجهاز الواحد صفٌّ يُحدَّث لا صفوفٌ
  تتراكم لرموزٍ ماتت
- `ON DELETE CASCADE` بخلاف الجداول المالية — بيانُ توصيلٍ لا سجلٌّ محاسبي (كـ `saved_cards`)
- **الرمز الميت يُعطَّل ولا يُحذف**: ردُّ FCM بـ unregistered/invalid يُنزل `is_active`، فيبقى الصفُّ
  يتعرّف على الجهاز عند إعادة التسجيل ولا يُعاد الإرسال إليه في كل حملة
- **`token` لا يخرج من الخلفية**: به يُرسل إشعارٌ باسم المنصة إلى جهاز صاحبه

### notification_campaigns (حملات إدارية/تسويقية — القسم 13، المرحلة 8)
- `title`, `body`, `audience` (all_riders/all_drivers/by_country/segment), `country_code` (مُضيِّق لأي
  جمهور، ومطلوبٌ لجمهور «مستخدمو دولة»)
- `status` (draft/scheduled/sent/cancelled), `scheduled_at`, `sent_at`, `sent_count`, `created_by`
- **لا حالة «قيد الإرسال»**: الحملة تُرسل في دورةِ مهمةٍ واحدة على دفعات وصفُّها مقفول طوالها،
  فمهمتان متزامنتان لا ترسلان حملةً مرتين
- `segment` محجوزة لشرائح المرحلة 12 وتُرفض اليوم برسالة صريحة
- قيدان في القاعدة: `(audience='by_country') ⇒ country_code IS NOT NULL`، و
  `(status='scheduled') ⇒ scheduled_at IS NOT NULL`

### notification_deliveries (سجل التسليم)
- `campaign_id`, `user_id`, `status` (sent/failed/skipped), `sent_at`
- **فريد `(campaign_id, user_id)`** — وهو حارس عدم التكرار: مهمةٌ أُعيد تشغيلها بعد سقوطٍ في منتصف
  دفعة لا ترسل لمن وصله
- `skipped` ليست فشلاً: أطفأ التسويق أو لا جهاز مسجّل له. الفرق بينها وبين `failed` هو الفرق بين
  «لم نرسل عمداً» و«أرسلنا فلم يصل» — وبها يصير احترامُ الإطفاء **مُثبَتاً** لا مزعوماً

### notification_settings (ساعات الهدوء per-country — المرحلة 8)
- `country_code`, `quiet_hours_start`, `quiet_hours_end`, `timezone`
- **المِنطقة الزمنية عمودٌ صريح**: «لا إرسال بعد العاشرة» جملةٌ بلا معنى ما لم يُقل عاشرةُ مَن
- النافذة الافتراضية 22:00 → 08:00 وتعبر منتصف الليل، وتساوي الطرفين يعني «لا هدوء» لا «هدوءٌ دائم»
- تخص **التسويقي وحده**: كبتنٌ يعمل ليلاً يجب أن تصله بطاقة الطلب

> وفي `users` عمود `marketing_push_enabled` (افتراضي true) — إطفاءُ الإعلانات بيد صاحبها، ولا أثر له
> على المعاملاتي إطلاقاً.

### user_notifications (صندوق وارد المستخدم — المرحلة 9-ب)

قبل هذا الجدول كان الإشعار حدثاً بلا سجل: تُبثّ على القناتين ثم يُنسى. فمن أُغلق تطبيقُه ساعةً لا
يعرف أن اشتراكه انتهى ولا أن نزاعه فُصل ولا أن وثيقته رُفضت — وأيقونةُ الجرس في التطبيقين بلا مصدر.

- `user_id`, `kind`, `title`, `body`, `data` (jsonb), `read_at`
- **يُكتب من بابَي الإرسال وحدهما** (`services/notifications.py` و`services/campaigns.py`)، فقناةٌ
  ثالثة تُضاف يوماً تكتب فيه لأن البابين هما ما يُضاف إليهما لا لأن أحداً تذكّر
- **يُكتب ولو لم يُرسل Push**: لا عقد FCM، أو الجهاز مفتوحٌ فلا يُرسل إليه — كلاهما لا يعني أن
  الحدث لم يقع. الصفُّ أثرُ الحدث لا أثرُ المزوّد
- **`kind` هي `data["type"]` نفسها** التي تحملها حمولة Push، فلا يفترق ما يفتحه الضغط على الإشعار
  عمّا يفتحه الضغط على صفّه في الصندوق
- **بطاقة الطلب وحدها لا تُحفظ** (`EPHEMERAL_KINDS`): عمرُها عشرون ثانية، وبعدها إمّا قُبلت — فحدثُ
  `driver_assigned` هو الأثر الصحيح — أو انتقلت لغيره. وصفٌّ باقٍ لطلبٍ مضى يفتح عند الضغط لا شيء
- **والتسويقي يُكتب لمن أُرسل إليه فعلاً وحده** (`sent` لا `skipped`): من أطفأ إشعارات العروض
  أطفأها، وإدخالُها صندوقَه من بابٍ آخر التفافٌ على إطفاءٍ صريح
- `ON DELETE CASCADE` — سجلُّ عرضٍ لا سجلٌّ مالي، يذهب مع صاحبه كـ `device_tokens`
- **لا تقليم بعد**: الجدول ينمو، والقراءةُ مسقوفة والفهرس على `(user_id, created_at)`. مهمةُ الكنس
  الدورية في المرحلة 12 مع بقية مهام الصيانة

### wallet_transactions (Ledger — سجل قيود غير قابل للتعديل)
- `owner_type` (driver/rider), `owner_id` — **`owner_id` يشير إلى `users.id` في الحالتين** (مفتاح أجنبي حقيقي بـ RESTRICT)، و`owner_type` هو ما يميّز محفظة الراكب عن محفظة الكبتن. فرصيدُ كبتنٍ يُستعلم بـ `driver.user_id` لا `driver.id`
- `type`: `topup | ride_payment | ride_earning | commission | transfer_in | transfer_out | withdrawal | refund | subscription_payment | adjustment`
- `amount` (+/-), `balance_after`, `ride_id` (nullable), `reference`, `created_by`
- `idempotency_key` (nullable، فريد لكل مالك) — منع تكرار عملية أُعيد إرسالها (القسم 14)
- **قاعدة صارمة:** الرصيد يُحسب من القيود، لا عمود رصيد يُحدَّث. كل عملية = قيد جديد. التحويلات والخصومات داخل DB transaction ذرّية
- **«غير قابل للتعديل» مفروضٌ في القاعدة** بمُشغّل يرفض UPDATE وDELETE على الجدول. تصحيح الخطأ قيدُ `adjustment` مضاد، لا تعديلُ صفٍّ ولا محوُه
- إشارة `amount` يمليها النوع (قيد فحصٍ في القاعدة)، و`balance_after >= 0` دائماً: المحفظة مسبقة الدفع لا تُسحب على المكشوف

### wallet_topup_requests (طلب شحن ينتظر تأكيداً بشرياً)
- `owner_id`, `method` (cliq/cash/card), `amount`, `reference` (مرجع الحوالة), `note`
- `status` (pending/confirmed/rejected), `processed_by`, `processed_at`, `transaction_id` (القيد الناتج)
- كليك بلا API فيؤكدها إنسان (القسم 7)؛ الكاش يُنشئه الموظف مؤكداً؛ شحن Telr فوريٌّ آلي فلا يمر بطلب
- **لا رصيد يتغيّر قبل `confirmed`** — الطلب المعلّق أثرٌ نصّي لا مالي

### withdrawal_requests (للكباتن فقط)
- `driver_id`, `amount`, `method` (cliq/bank), `status` (pending/approved/paid/rejected)
- `reference`, `note`, `processed_by`, `processed_at`, `transaction_id`
- قيد `withdrawal` يُكتب عند `paid` وحدها. الطلبات في `pending`/`approved` تحجز مبلغها من الرصيد المتاح، فلا يُطلب نفس المال مرتين

### wallet_settings (حدود المحفظة per-country — تُدار من اللوحة)
- `country_code`, `transfer_daily_limit`, `transfer_monthly_limit`, `min_withdrawal_amount`
- صفرٌ في حدّي التحويل يعني **«لم يُضبط بعد»** فيُرفض التحويل — لا يخترع الكود حداً سخياً لقيمةٍ مالية غائبة

### subscription_plans
- `name`, `duration_type` (daily/weekly/monthly), `price`, `currency`, `country_code`, `is_active`

### driver_subscriptions
- `driver_id`, `plan_id`, `starts_at`, `expires_at`
- `amount_paid`, `payment_method` (cash/cliq/wallet/card), `status` (active/expired)
- التجديد = سجل جديد (تاريخ كامل محفوظ)
- أضافت **المرحلة 7** ثلاثة أعمدة (ترحيلة `0009`):
  - `transaction_id` (nullable) — قيد `subscription_payment` الناتج، **للمحفظة وحدها**: القسم 9
    يكتب الرصيد «أرباح − عمولات − الاشتراكات المدفوعة **منها** − سحوبات»، وكاشٌ وكليكٌ وبطاقةٌ لا
    يمر مالها بالمحفظة فقيدُ خصمٍ عليها يخصم من الكبتن مرتين
  - `reference` (nullable) — حوالة كليك أو إيصال الكاش أو مرجع الطلب لدى المزود
  - `idempotency_key` (nullable، **فريد**) — ضغطتان على «تجديد» لا تشتريان اشتراكين (القسم 14)
- **حالتان لا ثالثة**: لا `pending` لأن الصف لا يُنشأ إلا وقد وصل ماله (القنوات اليدوية تُسجَّل بعد
  القبض، والفوريتان بعد جواب الدفتر أو المزود)، ولا `cancelled` لأن مدفوعاً مقدماً لا يُلغى

### commission_settings
- `country_code`, `commission_enabled` (default **false**), `commission_percent`, `applies_to`
- `applies_to`: `all_rides` (كل الرحلات) أو `cashless_rides` (البطاقة والمحفظة فقط — ما تمر أمواله عبر المنصة)
- **هذا الجدول هو المصدر الوحيد لتفعيل العمولة.** لا مفتاح `commission_enabled` في `feature_flags`: التفعيل والنسبة والنطاق تسكن معاً في صف واحد، فلا تنشأ حالتان قابلتان للاختلاف في أمر مالي

### feature_flags
- `country_code`, `feature_key`, `enabled`
- المفاتيح المبدئية: `cliq_enabled`, `card_enabled`, `wallet_enabled`, `wallet_transfer_enabled`،
  و`otp_verification_enabled` (المرحلة 8-ب)، و`women_service_enabled` (المرحلة 10-ج)،
  و`multi_stop_enabled` (المرحلة 12-ب)، و`whatsapp_otp_enabled` (المرحلة 12-هـ)
- غياب الصف = الميزة **معطّلة** — لا يُفترض التفعيل أبداً
- **الاستثناء الوحيد: المفاتيح الحارسة** (`otp_verification_enabled`). القاعدة أعلاه وُضعت كي لا
  تُفتح ميزةٌ بالسكوت؛ وهذا ليس ميزةً تُفتح بل حارساً يُطفأ، فغيابُ صفّه يعني **مفعّلاً**. القائمة
  في `settings_service.DEFAULT_ENABLED_FLAGS` ولا يُضاف إليها مفتاح ميزة
- `otp_verification_enabled`: **لـ admin حصراً**، و**إطفاؤه يشترط سبباً مكتوباً** يدخل سجل التدقيق
  (من أطفأه ومتى ولماذا). أثرُه محصورٌ في **التسجيل**: يُنشأ الحساب بـ `phone_verified_at = NULL`.
  **ولا يمسّ استعادة كلمة المرور إطلاقاً** — عفوُها يجعل إطفاءَ المفتاح طريقاً للاستيلاء على أي حساب
  بمجرد معرفة رقمه. وهو إجراء طوارئ تعرضه اللوحة بتحذير صريح
- `whatsapp_otp_enabled`: **ميزةٌ لا حارس** (المرحلة 12-هـ)، وغيابُ صفّها معطَّلة. وهي المفتاحُ
  الوحيد الذي **يُقرأ مع عقدٍ عامّ**: عقدُ واتساب لا دولةَ له (رقمُ أعمالٍ واحد يخدم السوقين)
  فالمفتاحُ هو ما يقول «نفعل في هذا السوق» — ويُشعَل بعد اعتماد قالب المصادقة **بلغة ذلك السوق**،
  فقالبٌ غير معتمد يجعل كلَّ تسجيلٍ يرتدّ من ميتا. ولذلك لا مزامنةَ تلقائية بينه وبين العقد كما في
  عقود البطاقة وكليك: المزامنةُ تُشعل مفتاحَ دولةِ العقد، وهذا عقدٌ بلا دولة
- `women_service_enabled`: **ميزةٌ لا حارس**، فغيابُ صفّها يعني معطّلة كالبقية. وتبقى مطفأةً
  حتى تكتمل **مراجعةُ أجناس الكباتن المعتمدين المتراكمين**: تشغيلُها قبلها يعني خدمةً نسائيةً
  بلا سائقاتٍ يمكن ترشيحُهنّ، فتُقرأ ميزةً معطوبة لا ميزةً جديدة. **وما دامت مطفأةً لا يظهر
  مفتاحُ التفضيل للراكبة إطلاقاً** — لا خياراً معطّلاً ولا رسالةَ اعتذار: خيارٌ معطّلٌ يعرض
  عليها ما لا تناله، ورسالةٌ تعتذر تعِد بما لم يُوعَد به. والتطبيق يقرأ المفتاح من `GET /config`
  كبقية المفاتيح، فتشغيلُها لا يحتاج نشراً
- **وإطفاؤها بعد تشغيلها لا يحبس أحداً**: تفضيلٌ نسائيٌّ بقي على حساب راكبةٍ
  من سوقٍ كانت الخدمةُ فيه مشتعلة كان يجعل **كلَّ طلباتها ترتدّ** — الخلفيةُ
  ترفض بحق (`women_service_unavailable`: طلبٌ يُسنَد لأيّ كبتنٍ بعد أن طُلب
  فيه غيرُه أسوأ من رفض)، **والتطبيقُ يخفي المفتاح فلا تجد ما تغيّره**. فصار
  التطبيق **لا يرسل تفضيلاً لخدمةٍ لا تُعرض عليه** (`lib/women.ts`)،
  و**المخزَّن لا يُمحى** — هو اختيارُها ويعود بعودة الخدمة، والذي يتوقف
  إرسالُه لا حفظُه. ومعه رسالةٌ صريحة وزرُّ «اقبل أي كبتن» إن ارتدّ طلبٌ
  بالسبب نفسه من مسارٍ آخر: **رفضٌ بلا مخرجٍ ليس رفضاً**
- `multi_stop_enabled`: **ميزةٌ لا حارس**، فغيابُ صفّها يعني معطّلة — **ولا تُضاف إلى
  `DEFAULT_ENABLED_FLAGS`** مهما بدت صغيرة: تلك قائمةُ الحرّاس وحدهم (القسم 4/feature_flags).
  ويُكتب صفُّها **صراحةً مطفأةً في `scripts/seed.py`** للدولتين، فلا يُقرأ غيابُها اجتهاداً.
  وثلاث قواعد لإطفائها:
  - **يختفي زرُّ «إضافة محطة» من شاشة الطلب تماماً** — لا خياراً معطّلاً ولا رسالةَ اعتذار،
    حرفياً كما يختفي مفتاحُ التفضيل النسائي (القسم 4/`women_service_enabled`)
  - **والرحلاتُ الجارية متعددةُ المحطات تكمل طبيعياً**: الإطفاء يمنع **الطلبات الجديدة** وحدها.
    مفتاحٌ يقطع رحلةً في منتصفها يترك راكباً بين محطتين، والمحطاتُ مجمَّدةٌ على الرحلة أصلاً
  - والفحصُ عند **إنشاء الطلب** لا عند العرض: واجهةٌ تخفي الزر لا تمنع طلباً مصنوعاً بيد
- إعداد ليبيا الافتراضي: الكل **false** (كاش فقط) / الأردن: `cliq_enabled` و`card_enabled` و`wallet_enabled` مفعّلة، و`wallet_transfer_enabled` معطّل

### provider_credentials (بيانات عقود المزودين — تُدار من صفحة العقود)
- **القاعدة: أي مزود خارجي يحتاج مفتاح/عقد API يُدار من هنا حصراً — بما فيهم Mapbox وTelr** — لا مفاتيح مزودين في `.env` أو الكود
- `provider_key` (mapbox / telr / sms / cliq_acquirer / fcm / payout), `country_code` (nullable للعام)
- `credentials` (JSONB **مشفر at rest** — حقول مختلفة لكل مزود، مع تمييز الحقول العامة publishable عن السرية secret)
- `is_active`, `last_tested_at`
- تفعيل المزود = تفعيل ميزته تلقائياً؛ كل تعديل يدخل audit log
- الحقول السرية للخلفية فقط (لا تصل للواجهات)؛ الحقول العامة بطبيعتها (مثل Mapbox pk) تُسلَّم للواجهات عبر `GET /config`
- **ملاحظة تشغيلية:** Mapbox وTelr Sandbox أساسيان من اليوم الأول — أول خطوة بعد تشغيل النظام هي إدخال توكناتهما من صفحة العقود (أو seed script للتطوير المحلي)

### pricing_rules
- `country_code`, `vehicle_category`, `base_fare`, `price_per_km`, `price_per_min`, `minimum_fare`, `cancellation_fee`
- **وأربعةُ حقولِ محطاتٍ تديرها الإدارة per-country** (المرحلة 12-ب)، وكلُّها **صفرٌ افتراضاً**
  فلا تُفتح كلفةٌ على راكبٍ بالسكوت (نفس قاعدة العمولة):
  - `stop_fee` — رسمٌ **مقطوع** يُضاف عن كل محطةٍ وسيطة، يدخل التقدير المعروض قبل الطلب
  - `stop_free_minutes` — دقائقُ انتظارٍ مجانية لكل محطة
  - `stop_price_per_min` — سعرُ الدقيقة بعد المجانية
  - `stop_max_wait_minutes` — **سقفُ الانتظار للمحطة الواحدة**؛ ببلوغه يُخطَر الطرفان ويُفتح
    للكبتن خيارُ إنهاء الرحلة عند تلك المحطة (القسم 5.10). **وصفرٌ يعني «لا سقف»** لا «سقفٌ
    مقداره صفر» — كما يُقرأ صفرُ حدِّ التحويل «لم يُضبط» (القسم 7)

### ratings
- `ride_id`, `rater_type` (rider/driver), `rater_id`, `stars` (1-5), `comment`
- فريد `(ride_id, rater_type)` — كل طرف يقيّم مرة واحدة، **مفروضاً في القاعدة** فسباقُ ضغطتين لا
  ينتج تقييمين
- `rater_id` إلى جانب `rater_type`: الأول يقول أيّ حساب والثاني يقول أيّ الطرفين، فيبقى السجل مقروءاً
  بعد أي تغيّر في إسناد الرحلة
- **`drivers.rating_avg` مشتق لا مُراكَم:** يُعاد حسابه من الجدول كله بعد كل تقييمٍ يكتبه راكب. المتوسط
  المتدحرج يعتمد على عدد سابقٍ مخزَّن، وأيُّ قيدٍ ضاع أو أُعيد يجعله ينحرف انحرافاً لا يُكتشف — نفس سبب
  حساب رصيد المحفظة من دفترها
- التقييم اختياري ويقع بعد `completed` وحدها، ومن الطرفين وحدهما: الدور لا يكفي، فكبتنٌ آخر دورُه
  `driver` ليس طرفاً في هذه الرحلة، والإدارة تقرأ الرحلات ولا تقيّمها

### user_totp (العامل الثاني لدخول اللوحة — TOTP، المرحلة 12-د)
- `user_id` (FK users، **unique**) — صفٌّ واحدٌ للمستخدم، وغيابُه يعني «لا عامل ثانٍ»
- `secret_encrypted` (JSONB) — مظروفُ Fernet `{"v":1,"ciphertext":…}` بنفس `core/crypto.py`
  الذي يحمي `provider_credentials`. سرُّ TOTP مفتاحٌ دائم: من قرأه من نسخةٍ احتياطية يولّد
  رموزَ صاحبه إلى الأبد، فهو مثلُ مفتاح مزوّدٍ لا مثلُ كلمة مرور — **يُشفَّر ولا يُهشَّم**،
  لأن التحقق يحتاج قراءتَه
- **ولماذا جدولٌ مستقلٌّ لا عمودان على `users`**: `users` يُقرأ في **كل** طلبٍ لكل مستخدم
  (`core/deps.get_current_user`)، فوضعُ السرِّ على الصف يحمّل سرَّ عاملٍ ثانٍ في كل نداءٍ في
  المنصة كلها — ولمن لا يملك عاملاً أصلاً. والجدولُ المستقل لا يُقرأ إلا في مسارَي الدخول
  والتسجيل
- `confirmed_at` (nullable) — **تسجيلٌ غير مؤكَّد ليس عاملاً**: السرُّ يُنشأ قبل أن يُثبت صاحبُه
  أنه وصل تطبيقَه، فاعتبارُه عاملاً يقفل صاحبَه خارج اللوحة برمزٍ لا يملك مصدره
- `recovery_codes_verified_at` (nullable) — لحظةُ إثبات أن الاسترداد يعمل فعلاً. شرطُ إشعال
  مفتاح الإلزام (القسم 14.1)
- `last_step` (BIGINT، nullable) — آخرُ خطوةِ زمنٍ قُبلت. **منعُ إعادة الاستخدام**: الرمزُ صالحٌ
  ثلاثين ثانية، فمن قرأه من فوق كتف صاحبه يستعمله في نفس النافذة — والقبولُ مرةً واحدةً لكل خطوة
- ولا عدّادَ محاولاتٍ هنا: المحاولاتُ في Redis عبر `core/rate_limit` كبقية سقوف القسم 14 —
  عدّادٌ في القاعدة يعني كتابةً على كل محاولةٍ فاشلة

### user_recovery_codes (رموز استرداد العامل الثاني — المرحلة 12-د)
- `user_id` (FK users)، `code_hash`، `used_at` (nullable)
- **عشرةُ رموزٍ تُولَّد مرةً وتُعرض مرةً ولا تُقرأ بعدها أبداً**، وتُخزَّن **مُهشَّمة** لا مشفَّرةً
  كالسرّ: التحقق منها مقارنةٌ لا استرجاع، وجدولٌ يحفظها مقروءةً هو نسخةٌ ثانيةٌ من العامل الثاني
  بلا عاملٍ يحرسها
- **والتهشيم HMAC-SHA256 بمفتاح الخدمة لا bcrypt** — خلافاً لكلمة المرور، ولسببين يعملان معاً:
  الرموزُ عاليةُ العشوائية (من مولّدٍ تشفيري) فدالةٌ بطيئةٌ لا تشتري مقاومةً لتخمينٍ غيرِ ممكنٍ
  أصلاً؛ والبصمةُ الحتمية تتيح **استعلاماً مفهرساً عن الصفِّ الواحد**، وهو ما يجعل القفل على صفٍّ
  واحد بدل قفلِ عشرةِ صفوفٍ وتجربةِ bcrypt على كلٍّ منها تحته. وهو نفسُ `services/otp.py::_digest`
- **وأبجديةُ الرمز بلا `I O U L 1 0`**: يُنسخ بيدٍ ويُقرأ من ورقة، والحرفُ الذي يُقرأ رقماً يجعل
  رمزاً صحيحاً يبدو خاطئاً في اللحظة التي لا يملك صاحبُه فيها بديلاً
- **والاستهلاك صفٌّ واحدٌ تحت قفل صفّه** (`with_for_update` قبل قراءة `used_at`): بلا القفل
  يمرّ نداءان متزامنان بنفس الرمز فيُستعمل رمزٌ «لمرةٍ واحدة» مرتين — وهي القاعدةُ نفسُها التي
  تحرس كل انتقالِ حالةٍ في هذا المشروع
- واستهلاكُ رمزٍ يكتب قيدَ تدقيقٍ **وصفَّ إشعارٍ لصاحب الحساب** (`user_notifications`): لا بريدَ
  في المنصة، والحدثُ من أوضح ما يفعله من استولى على حساب

### security_settings (سياسةُ دخول اللوحة — **صفٌّ واحدٌ عالمي**، المرحلة 12-د)
- `admin_totp_required` (boolean، افتراضي **false**)، `admin_idle_timeout_minutes` (int، افتراضي 30)
- **عالميٌّ لا per-country** خلافاً لكل جداول الإعدادات، ولا شيءَ في القسم 14 per-country: أمنُ
  لوحةٍ مكتبية ليس سياسةَ سوق. **والأخطرُ أن per-country يفتح ثقباً**: حسابُ المشرف في دولةٍ
  واحدة واللوحةُ تخدم السوقين، فإلزامٌ يُقرأ من دولة الحساب يُتجاوَز بحسابٍ دولتُه الأخرى
- صفٌّ واحدٌ يحرسه عمودٌ ثابتٌ عليه `unique` + `CHECK`، والغيابُ يُقرأ بالافتراضات أعلاه
- **والافتراضُ `false` عكسَ `DEFAULT_ENABLED_FLAGS`، وليس نقضاً لها**: قاعدةُ «غيابُ صفِّ حارسٍ
  يعني مفعّلاً» وُضعت حيث السكوتُ **يُطفئ** حماية. وهنا السكوتُ لا يطفئ شيئاً بل **يُشعل قفلاً على
  بابٍ لا مفتاح له**: صفٌّ مفقودٌ يُقرأ «إلزام» يقفل كلَّ مشرفٍ خارج اللوحة، ولا أحدَ يستطيع
  إطفاءه من داخلها. والإلزامُ فعلُ مالكٍ صريحٌ لا حالةٌ تقع بالسكوت

### admin_audit_logs (سجل التدقيق الذي يفرضه القسم 14)
- `actor_id` (يبقى بعد حذف الحساب), `action` (create/update/activate/…), `entity_type`, `entity_id`, `details`
- `details` يحمل **أسماء الحقول المتغيّرة فقط، لا قيمها** — سجلُّ تدقيقٍ يخزّن الأسرار يصير نسخةً ثانيةً منها
- يُكتب في **نفس معاملة** التغيير: لا تغييرَ بلا قيد، ولا قيدَ لتغييرٍ فشل

---

## 5. تدفق الرحلة (Ride Lifecycle)

1. **الطلب:** الراكب يحدد نقطتي الانطلاق والوصول (دبوس على الخريطة + Geocoding اختياري)
2. **التسعير:** الخلفية تستدعي Mapbox Directions → `estimated_fare = base_fare + (km × price_per_km) + (min × price_per_min)` مع حد أدنى. **السعر يُحسب في الخلفية فقط — أبداً في الواجهة**
3. **التوزيع (Dispatch):**
   - Redis GEO: أقرب الكباتن ضمن نصف قطر 3كم (يتوسع حتى 7كم — **وحتى 10كم للطلب المجنَّس**،
     المرحلة 10-ج: دائرةُ المرشَّحين فيه أضيق ابتداءً، فسبعةٌ تعني «لم نجد كبتناً» لطلبٍ كانت
     تسعةٌ كافيةً له. والانتظارُ الأطول خيارُ من طلبت الخدمة، أما الرفضُ فليس خياراً لأحد)
   - الشروط: `is_online=true` + `approved` + `اشتراك ساري (expires_at > now)` + بدون رحلة حالية
   - **ومطابقةُ الجنس في اتجاهين** (المرحلة 10-ج)، ومكانُها داخل شرط الأهلية نفسه لا فوقه —
     فالدالةُ التي تجيب «من يصلح لهذا الطلب» واحدة، وأيُّ تصفيةٍ فوقها تُنسى في أحد المسارين
     (التوزيع وخريطة الراكب يستدعيانها معاً). والاتجاهان **مستقلان**:
     - *ما تطلبه الراكبة*: تفضيلٌ غير `any` يقصر النتيجة على كبتنٍ جنسُه **مختوم** بذلك الجنس
     - *ما يقبله الكبتن*: كبتنٌ تفضيلُه غير `any` لا يُعرض عليه إلا راكبٌ من ذلك الجنس، ومن لم
       يعلن جنسه لا يطابق أحداً منهم
     فراكبٌ اختار «لا يهمّني» **لا يُعرض طلبُه** على كبتنةٍ قصرت عملها على النساء — وسكوتُ
     الاتجاه الأول لا يُلغي الثاني
   - **والمفتاح `women_service_enabled` مطفأً يعني لا مطابقةَ إطلاقاً** لا «مطابقةً بتفضيلٍ
     فارغ»: تفضيلٌ باقٍ على حساب كبتنةٍ في سوقٍ لا خدمةَ نسائيةً فيه كان سيحجب عنها الطلبات
     صامتاً بلا أن يفهم أحدٌ لماذا جفّت
   - العرض للأقرب أولاً، مهلة قبول **20 ثانية**، ثم التالي. بعد 5 محاولات أو دقيقتين → `no_driver_found`
4. **القبول:** يقبل الرحلةَ **الكبتنُ المعروضة عليه في تلك اللحظة وحده وضمن مهلته**. لا يقبلها كبتن لم تُعرض عليه ولو رآها، ولا من انقضت مهلته أو انتقل العرض لغيره — وإلا التفَّ على الدور الذي بُني عليه التوزيع كله. القبول ممكن من `searching` فقط (القسم 4)، فرحلةٌ لم يبدأ توزيعها ليست قابلة للقبول أصلاً
   → بعد القبول يشاهد الراكب بيانات الكبتن (اسم، صورة، مركبة، لوحة، تقييم) وموقعه لحظياً
5. **التتبع:** WebSocket — الكبتن يبث موقعه كل 3 ثوانٍ → Redis → يُبث للراكب
6. **الرحلة:** `arrived` (إشعار للراكب) → `in_progress` → `completed`
7. **مسار الرحلة:** أثناء `in_progress` تُخزَّن نقاط المسار الفعلي **في القاعدة** — نقطة كل 15-30 ثانية مأخوذة من بثّ الكبتن. غرضان لا يؤديهما البثّ اللحظي لأنه متطايرٌ بعمر محدود في Redis:
   - **المسافة الفعلية** عند الإنهاء، وعليها يُعاد حساب `final_fare` عند الانحراف الكبير عن المقدّر
   - **دليلٌ عند النزاعات** في لوحة الإدارة (القسم 13.4): أين سار فعلاً ومتى

   نُفِّذ في **المرحلة 6-أ**. قرارات التنفيذ التي لم يحسمها النص أعلاه:
   - **العيّنة كل 20 ثانية** (وسط نافذة 15-30). البثّ كل 3 ثوانٍ، فتخزينه كله عشرون صفاً لكل دقيقة
     رحلة بلا زيادةٍ تُذكر في دقة المسافة. النافذة نفسها هي عمر مفتاح Redis، فلا عدّاد ثانٍ يفترق عنها
     ولا يكتب عاملا uvicorn نقطتين للحظة واحدة
   - **«الانحراف الكبير» = أكثر من 20%** بين المقدّرة والفعلية، **في الاتجاهين**: أطولُ يُنصف الكبتن
     وأقصرُ يُنصف الراكب. الرقم يتجاوز خطأ تقدير Mapbox المعتاد وتحويلةً قصيرة، ولا يبلغه إلا مسارٌ
     اختلف فعلاً
   - **إعادة الحساب على المسافة وحدها**، والمدة تبقى المقدّرة: هذا البند يبني على المسافة، وإقحامُ
     زمنٍ فعليٍّ لم يطلبه يحمّل الراكبَ ازدحامَ الطريق مرتين
   - **أقل من نقطتين = لا مسافة فعلية** فيبقى المقدَّر هو الحكم — لا تخمين لمسافةٍ صمت عنها التطبيق
8. **الدفع:** `final_fare` (يُعاد حسابه على المسافة الفعلية من نقاط المسار إن انحرف كثيراً، وإلا = المقدّر) → شاشة الدفع
9. **التقييم:** متبادل، اختياري

### 5.10 تعدد الوجهات — المحطات الوسيطة (المرحلة 12-ب)

الراكب يضيف **حتى ثلاث وجهات**: وسيطتان على الأكثر ثم الوجهة الأخيرة. ويرتّبها ويحذفها **قبل
التأكيد** لا بعده — تغييرُ الوجهة بعد أن سار الكبتن يغيّر السعرَ المتفق عليه، وذاك طلبٌ جديد.

**آلةُ الحالات لكل محطة، داخل `services/rides.py` وحدها ومحروسةً بـ`ALLOWED_TRANSITIONS`:**

```
in_progress ──(وصل المحطة)──> at_stop ──(استئناف)──> in_progress
                                 └────(إنهاء عند المحطة بعد السقف)────> completed
```

- **`arrive_stop`** يختم `ride_stops.arrived_at` ويضع الرحلة في `at_stop`.
- **`resume_stop`** يختم `resumed_at`، ويعيدها `in_progress`، و**يزيد `rides.current_leg` واحداً**
  — وهو الكاتبُ الوحيد لذلك العمود، تحت قفل صف الرحلة.
- والانتقالان يمران بنفس الباب الذي تمر به كل انتقالات الرحلة: **لا راوتر يكتب حالة**.

**والزمنُ يُختم في الخلفية لا في الواجهة** (القسم 14): العدّادُ الذي يراه الراكب يمشي عنده من
`arrived_at`، **وأما المبلغُ فيأتي محسوباً من الخلفية** في حمولة الرحلة ويُحدَّث معها. عرضُ
الوقت حسابُ وقت، وعرضُ المال حسابُ مال — والثاني في الخلفية حصراً. **ولا مفاجأةَ في شاشة
الدفع**: ما يراه أثناء الانتظار هو ما يُحصَّل.

**والحساب:**

```
التقدير  = الأساس + (كم × سعر) + (دقيقة × سعر) + (عدد المحطات الوسيطة × stop_fee)
النهائي  = التقدير (أو المُعاد حسابه على المسافة الفعلية، 5.7)
           + Σ لكل محطة: max(0، دقائق الانتظار − stop_free_minutes) × stop_price_per_min
```

- **رسمُ الانتظار خارج التقدير عمداً**: لا يُعرف قبل أن يقع. والشاشةُ تقول سعرَه قبل الطلب،
  فيكون معلوماً ولو لم يكن مقدَّراً.
- **والأربعةُ مجمَّدةٌ على الرحلة** (`*_at_ride`) كالعمولة: تعديلُ الإعداد يحكم ما بعده.

**وسقفُ الانتظار** (`stop_max_wait_minutes`، وصفرٌ يعني لا سقف): مهمةٌ دورية تمر كل دقيقة،
فما تجاوز سقفَه **يُخطر الطرفين** — الراكبَ أنه تجاوز، والكبتنَ أن له مخرجاً — ويُختم
`notified_at` فلا يتكرر التنبيه في كل دورة. **ولا إنهاءَ آلي**: الإنهاءُ فعلُ الكبتن وحده،
لأن راكباً تأخّر دقيقتين فوق السقف ليس راكباً تُترك أمتعتُه على الرصيف. وما بعد السقف **يُحتسب
كاملاً**: السقفُ حدُّ صبرٍ لا حدُّ فاتورة.

**ونقاطُ المسار تُنسب لكل ساقٍ على حدة** (`ride_route_points.leg`)، فتُقرأ المسافةُ الفعلية
مفصَّلةً في دليل النزاع: أين سار بين المحطتين، لا أين سار في الرحلة كلها.

---

**الإلغاء:** مجاني قبل القبول. بعد القبول تُطبق `cancellation_fee` (قابلة للتصفير من الإدارة). انقطاع اتصال الكبتن > 60 ثانية أثناء `in_progress` → تنبيه للطرفين + عدم إنهاء الرحلة آلياً.

**واستثناءٌ واحد يُسقط الرسم بعد القبول: `cancel_reason_code = gender_mismatch`** (المرحلة 10-ج).
امرأةٌ طلبت سائقةً فوجدت غيرها لا تُغرَّم لأنها رفضت الركوب — وإلا صار الأرخصَ لها أن تركب، وهو
ثمنٌ لا يجوز وضعُه على أحد. ويسري على الطرفين: الكبتن لا يدفع رسوماً أصلاً، لكن **البلاغ يُسجَّل
على حساب الطرف الآخر** (`gender_mismatch_reports`) وعند بلوغ الحدّ يُوسم للمراجعة.
و**السبب مرفوضٌ حيث لا محلَّ له**: لا تُقبل حجّةُ الجنس من راكبٍ لم يطلب جنساً ولا من كبتنٍ لم
يقصر عمله على جنس — بغير هذا الشرط يصير السببُ باباً للإفلات من الرسوم ووسماً لبريء.

---

## 6. الدفع — التدفقات التفصيلية

### كاش
نهاية الرحلة → الراكب يسلّم → الكبتن يضغط "استلمت المبلغ" → `confirmed`

### كليك (الأردن فقط) — صفحة دفع كليك داخل التطبيق

يستبدل هذا التدفقُ التدفقَ اليدوي السابق (عرض alias + زر نسخ + تأكيد):

1. شاشة الدفع تعرض **المحفظة خياراً أول وكليك ثانياً**
2. عند اختيار كليك: TAXO يولّد **QR ديناميكياً بصيغة CliQ** يحمل **alias الكبتن** والمبلغ
   و**مرجعاً داخلياً يولّده TAXO**، ومعه **deep link** يفتح تطبيق البنك
3. الراكب يحوّل من بنكه، ثم يعود ويُدخل **رقم مرجع الحوالة** (ورفع لقطة شاشة اختياري)
4. **إشعار فوري للكبتن** → يؤكد «وصلني» أو يرفض
5. كل الخطوات تُسجَّل بطوابع زمنية في `payments`: المبلغ، وalias المعروض، والمرجع الداخلي، ومرجع
   الحوالة، ومن أكّد ومتى — **سجلٌّ غير قابل للتعديل** كبقية الدفتر
6. عند الرفض أو انقضاء مهلة التأكيد → `disputed` بحقول الفصل القائمة (القسم 4/13.4)
   - **المهلة مبنيةٌ (المرحلة 10):** رقمُها `payment_settings.cliq_confirmation_hours` لكل دولة
     (افتراضُه ٢٤ ساعة، يديره المشرف من `PATCH /admin/settings/payments/{country}`)، **ويُجمَّد
     على الدفعة** في `payments.cliq_confirmation_expires_at` لحظة إدخال المرجع — فتعديلُ الإعداد
     يحكم ما يأتي بعده ولا يحرّك مهلةً رآها الكبتن. ومهمةُ
     `app/tasks/payments.py::sweep_cliq_confirmations` تمر كل خمس دقائق فتنقل ما انقضى إلى
     `disputed` تحت قفل الصف، **وتُخطر الطرفين**: الكبتنَ أن دفعته صارت نزاعاً، والراكبَ أن
     تحويله لم يُؤكَّد — وصمتُ النظام هنا يصنع تذكرتَي دعمٍ لا واحدة
   - **ولا يُؤكِّد الكبتن دفعةً صارت نزاعاً**: `disputed → confirmed` بابُ فصلِ الإدارة (القسم
     13.4)، فتأكيدٌ من التطبيق يُقيّد المال ويترك النزاع بلا `resolution` ولا مَن فصل. من انقضت
     مهلته ثم وصله المال يفصل له المشرف `paid`

**المال لا يمر بحساب الشركة إطلاقاً في هذه القناة**، فلا تأكيد آلي ممكن لدفع الرحلة بكليك: ما يشهد
عليه حساب التاجر هو ما يدخله، والحوالة هنا تذهب من الراكب إلى **alias الكبتن** مباشرةً. الحمايةُ من
النزاع تأتي من **السجل والمرجع** لا من تحقّقٍ آلي. وتفعيلُ عقد الـ acquirer (القسم 15/أ) **لا يغيّر
هذا**: نطاقه شحن المحفظة وحده (القسم 7)، ولا يُخرج `cliq` من `DIRECTLY_COLLECTED_METHODS`.

**التنفيذ:** الخلفية والسجل في المرحلة 6-أ (قائمان)، وشاشتا القناة — توليد الـ QR والـ deep link
وإدخال المرجع عند الراكب، وبطاقة التأكيد عند الكبتن — في **المرحلة 9** (تطبيق الراكب) و**المرحلة 10**
(تطبيق الكبتن).

**نُفِّذت شاشة الراكب في المرحلة 9.** القرارات التي لم يحسمها النص أعلاه:

- **الرمز يُبنى في الخلفية والواجهة ترسمه.** الحمولة تحمل مبلغاً ومرجعاً يُحاسَب عليهما، وبناؤها
  في الواجهة يجعلها طرفاً في تحديد ما يُدفع (القسم 14). فـ `services/cliq_qr.py` هو **الملف
  الوحيد الذي يعرف صيغة الرمز**، والواجهة تأخذ `qr_payload` نصّاً وترسمه مربّعاً.
- **الحقول الأربعة أضافتها الترحيلة `0013` إلى `payments`**: `cliq_alias` و`cliq_reference`
  (فريد) و`cliq_transfer_reference` و`cliq_reference_at`. البند 5 أعلاه يطلب تسجيل الخطوات كلها،
  والمبلغُ ومن أكّد ومتى قائمةٌ منذ 6-أ — والثلاثة الباقية كانت مفقودة.
- **alias مُجمَّد على الدفعة لا مقروءٌ من `drivers` عند العرض:** الكبتن قد يغيّر aliasه غداً،
  فيقرأ فاصلُ النزاع بعد أسبوع وجهةً غير التي حُوِّل عليها فعلاً — وهي بالضبط المعلومة التي وُجد
  السجل لأجلها.
- **مرجع الحوالة يُكتب مرةً واحدة ولا يُعدَّل** (`409` على الثانية)، وفاءً لـ«سجلٌّ غير قابل
  للتعديل» في البند 5: مرجعٌ يتبدّل بعد أن رآه الكبتن سجلٌّ لا يُحتج به. ومن أخطأ في الرقم يقول
  الكبتنُ «لم يصلني» وتفصل الإدارة (13.4). والكتابة تحت **قفل صف الدفعة**، وإلا كتبت ضغطتان
  متزامنتان مرجعين وأرسلتا إشعارين لحوالةٍ واحدة.
- **صيغة الرمز معيارية وثلاثةُ تفاصيلها تحتاج مطابقة**، كما في `telr.py` و`cliq/acquirer.py`:
  الهيكل EMVCo Merchant-Presented Mode بـ CRC-16 في الحقل 63 (معيارٌ لا اجتهاد فيه)، وما يخص
  كليكاً بعينه — معرّف النظام `CLIQ_GUID`، ورمز فئة التاجر، ومخطط الـ deep link — مجموعٌ في
  ثوابت مسمّاة أعلى الملف. وخطؤها **لا يحرّك مالاً**: لا شيء في ذلك الملف يكتب في الدفتر، ورمزٌ
  لا يقرؤه تطبيق البنك يعني تحويلاً يدوياً على الـ alias المعروض بجانبه ثم إدخالَ المرجع كما هو.
- **لا زرّ «تم الدفع» عند الراكب**: المال يذهب إلى alias الكبتن ولا يمر بالمنصة، فلا شاهد عندنا —
  والتأكيد قولُ الكبتن وحده. و**مهلةُ التأكيد** (البند 6) مع بطاقة الكبتن في **المرحلة 10**.
- **رفعُ لقطة الشاشة الاختياري مؤجَّل**: لا بنيةَ رفعِ ملفاتٍ في المشروع بعد (مستندات الكبتن في
  المرحلة 10 تحتاجها كذلك)، وبناءُ مسارِ تخزينٍ لحقلٍ وصفَه النص **اختيارياً** بناءٌ سابقٌ لأوانه.
  المرجعُ النصّي هو الدليل الملزِم في السجل، واللقطة تدعمه ولا تقوم مقامه.

### بطاقة عبر Telr (الأردن)
1. الخلفية تنشئ طلب دفع لدى Telr → توجيه الراكب لصفحة الدفع (بطاقة/Apple Pay/Google Pay)
2. Telr Webhook → الخلفية تتحقق وتحدّث `confirmed`
3. قيد `ride_earning` بنصيب الكبتن في محفظته (المبلغ − العمولة إن كانت مفعلة)
4. Tokenization: حفظ البطاقة للدفع بضغطة لاحقاً
5. **مرجع التوثيق:** يُسلّم رابط توثيق Telr الرسمي في جلسة التنفيذ. البناء على Sandbox

**نُفِّذ في المرحلة 6-ب.** القرارات التي لم يحسمها النص أعلاه:

- **الحقيقةُ جوابُ المزود للخلفية، لا حمولةُ الإشعار.** التوقيع شرطٌ لا سند: يُتحقق منه، ثم لا
  يُقرأ من الحمولة إلا **أيُّ طلبٍ ننظر فيه**؛ والحالة والمبلغ يأتيان من نداء `check` من الخلفية
  إلى المزود، وعليه وحده يتحرك الدفتر. فتوقيعٌ صحيحٌ لحمولةٍ عُدّل مبلغها لا يقيّد ديناراً زائداً،
  ومبلغٌ آتٍ من الشبكة لا يصل الدفتر بحال. `WebhookNotice` بلا مبلغ عمداً كي لا يكون ذلك ممكناً.
- **الإشعار واستعلامُ العميل شريكان لا أصلٌ واحتياط.** الواجهة تعود من صفحة الدفع فتسأل
  `GET /payments/card/orders/{cart_id}`، وأيُّهما وصل أولاً سوّى؛ الآخر يجد الطلب محسوماً فلا
  يقيّد شيئاً ولا يرتدّ بخطأ. مسارٌ واحدٌ فقط يحرّك المال (`card_payments.apply_state`).
- **لا تخمين على غير محسوم، في الاتجاهين.** رمزُ حالةٍ لا نعرفه أو `pending` يبقيان الطلب معلّقاً:
  عدُّه مدفوعاً يقيّد مالاً لم يصل، وعدُّه ساقطاً يفتح الرحلة لقناةٍ ثانية والراكب على صفحة الأولى.
  وسقوطُ العملية يُسقط دفعتها فيعود مبلغها ديناً على الرحلة — نفس منطق `resolution=unpaid` في كليك.
- **مبلغُ المزود يخالف مبلغَنا ⇒ نزاع لا قيد.** الطلب `failed` والدفعة `disputed` (لا `failed`):
  مالُ الراكب قد خرج فعلاً، فلا يُقيَّد بلا يقين ولا تُفتح الرحلة للدفع مرتين — تفصل الإدارة (13.4).
- **الاسترداد يعود من حيث جاء المال:** دفعةُ البطاقة تُردّ **عند المزود** إلى نفس البطاقة، فلا قيد
  `refund` للراكب — رصيدُه لم يُخصم أصلاً فردُّه إليه رصيداً يمنحه مرتين. وقيدُ `adjustment` المضاد
  على الكبتن يبقى في الحالتين. ومرجعُ الردّ يُحفظ في `provider_orders.refund_ref`، وحالةُ الطلب
  تبقى `paid`: الطلب دُفع فعلاً، والردُّ حركةٌ تالية له لا نقضٌ لتاريخه.
- **مزوّدٌ وهمي يُدار من صفحة العقود** (حقل `use_mock` في عقد Telr) — وهو ما تصفه فقرة (أ) من
  القسم 15. عليه بُنيت المرحلة واختُبرت كلها، لأن بيانات Sandbox لم تكن متوفرة. **ممنوع في
  الإنتاج**: مزودٌ يقول «دُفع» بلا مال بابٌ لا يُترك مفتوحاً حيث مالٌ حقيقي.
- **أسلاك Telr في ملف واحد** (`services/card_gateway/telr.py`) وكلُّ ما عداه يتكلم واجهةً موحدة
  (`base.CardGateway`). ولمّا لم يُسلَّم رابط التوثيق الرسمي، ثلاثة تفاصيل فيه **مبنيّة على أفضل
  قراءةٍ للواجهة العامة وتحتاج مطابقةً قبل أول تشغيل حقيقي**: ترتيب حقول توقيع الإشعار
  (`TRAN_CHECK_FIELDS`)، وحقول الخصم على بطاقة محفوظة (`SAVED_CARD_*`)، وموضع رمز البطاقة في جواب
  `check`. وخطؤها لا يحرّك مالاً خطأً: توقيعٌ لا يطابق يرفض الإشعار بـ 400 ظاهرٍ في السجل، ويبقى
  مسار الاستعلام يسوّي؛ وخصمٌ فاشل يرتدّ 502 قبل أي قيد.
- **لا انتهاء آلي للطلب المعلّق في هذه المرحلة.** المزود يُنهي طلباته غير المدفوعة بنفسه، وأول
  استعلامٍ بعدها يُسقط الدفعة. مهمة Celery دورية تستعلم عن الطلبات المعلّقة تأتي في **المرحلة 7**.

### المحفظة
- رصيد كافٍ → خصم فوري + `ride_earning` للكبتن
- رصيد جزئي → **دفع مختلط**: خصم المتاح + الباقي كاش (صفا payments)
- إلغاء بعد دفع محفظة → `refund` فوري آلي

### قيود التنفيذ على قيود الدفتر (المرحلة 6-أ)

- **`ride_earning` بكامل المبلغ ثم `commission` خصماً منفصلاً**، لا قيدَ واحداً صافياً. الصافي واحد في
  الحالتين، لكن القسم 9 يكتب رصيد الكبتن «أرباح − عمولات» فيقرأ حرفياً بهذا الشكل، وتبقى العمولة سطراً
  ظاهراً في الكشف. بقيدٍ واحد صافٍ لا تظهر عمولةُ الرحلات النقدية إلا وحدَها (لأن لا `ride_earning` لها
  تُطرح منه) فيصير الكشفان مختلفين لنفس النسبة.
- **النسبة مجمّدة والنطاق يُقرأ لحظة الدفع.** `commission_percent_at_ride` لا تُقرأ من الإعدادات أبداً
  (القسم 4)، أما `applies_to` فلا يمكن تجميده أصلاً: يتعلق بقناة الدفع وهي لا تُعرف قبل شاشة الدفع.
- **عمولة الرحلة النقدية تُخصم من محفظة الكبتن** حين يكون النطاق `all_rides`: القسم 9 ينفي أثر الكاش
  على الرصيد بوصفه **أرباحاً**، لا بوصفه إعفاءً من عمولةٍ لا قناة تحصيل أخرى لها. ومحفظةٌ فارغة تعني
  **رفضَ تأكيد التحصيل** برسالة صريحة لا خصماً على المكشوف (القسم 4). قيدٌ تشغيلي على من يرفع النطاق
  إلى `all_rides`: على الكباتن رصيدٌ يغطي عمولاتهم.
- **«إلغاء بعد دفع محفظة → refund آلي» لا مسار له في 6-أ**، وليس نقصاً في التنفيذ: الدفع يقع بعد
  `completed`، ولا إلغاء بعد `in_progress` (القسم 5)، فلا تتقاطع الحالتان. الاسترداد موجود **إجراءً
  إدارياً** على دفعةٍ مرّ مالها بالمنصة، بقيدين متقابلين (`refund` للراكب و`adjustment` مضاد على الكبتن
  بصافي ما قُيّد له) لا بمحو قيد. يصير الاسترداد الآلي مسارَ عملٍ حقيقياً في 6-ب مع حجز البطاقة المسبق.
- **الكاش وكليك لا يُستردّان من المنصة**: مالهما لم يدخلها فلا تملك ردّه (القسم 9).

---

## 7. محفظة الراكب

- **شحن فقط — لا سحب نهائياً** (لا يُبنى أي مسار سحب للراكب)
- قنوات الشحن:
  - Telr (فوري آلي)
  - كليك: تحويل على alias الشركة + إدخال المرجع → تأكيد الإدارة من اللوحة
  - كاش: موظف/نقطة معتمدة تشحن من اللوحة
- **شحن Telr لا يمر بجدول طلبات الشحن** (المرحلة 6-ب): `wallet_topup_requests` بيتُ ما ينتظر
  تأكيداً بشرياً، والبطاقة يشهد عليها المزود. فالمسار `POST /wallet/me/topups/card` يفتح صفَّ
  `provider_orders` ويعيد رابط صفحة الدفع، وأثرُه الوحيد قيدُ `topup` عند تأكيد المزود. ولذلك
  مسارٌ خاص لا `POST /wallet/me/topups`: ذاك يعيد صفَّ طلبٍ معلّق وهذا يعيد رابطاً، وردّان
  مختلفان لعمليتين مختلفتين لا يُدمجان في مسارٍ بحقولٍ فارغة في كل حالة
- **تجميدُ المحفظة يمنع فتح الشحن لا إكمالَه:** طلبٌ بدأ قبل التجميد ووصل ماله فعلاً يُقيَّد،
  والمنعُ يبقى على الإنفاق. مالٌ دخل لا يُحرق لأن حالةَ الحساب تغيّرت بين خطوتين
- **تحويل P2P بين محافظ الركاب** — خلف `wallet_transfer_enabled`:
  - إدخال رقم هاتف المستلم → عرض الاسم للتأكيد → تنفيذ ذرّي (قيدا `transfer_out`/`transfer_in`)
  - حدود يومية وشهرية تحددها الإدارة
  - راكب→راكب فقط في هذه المرحلة
  - سجل كامل في اللوحة + إمكانية تجميد محفظة

---

## 8. اشتراكات الكباتن

- **لا اشتراك ساري = لا رحلات.** الفحص في الخلفية عند التوزيع (وليس في الواجهة فقط)
- الدفع **مقدماً** بأي قناة: كاش (تأكيد إدارة)، كليك (تأكيد إدارة)، رصيد المحفظة (فوري)، بطاقة Telr (فوري)
- تطبيق الكبتن: عدّاد الأيام المتبقية + إشعار قبل الانتهاء بـ 24 ساعة + تجديد بضغطة
- Celery task دورية: تعليم الاشتراكات المنتهية + إخراج الكبتن من التوزيع
- العمولة: صفر افتراضياً. لو فُعّلت من اللوحة تُطبق على الرحلات **الجديدة فقط** (عبر `commission_percent_at_ride`)

نُفِّذ في **المرحلة 7**. قرارات التنفيذ التي لم يحسمها النص أعلاه:

- **مدد الخطط: 1 / 7 / 30 يوماً.** الشهر ثلاثون يوماً لا شهرٌ تقويمي: التقويمي يعطي مشتري الأول من
  فبراير ثمانيةً وعشرين يوماً بنفس الثمن، وليس في النظام مِنطقةٌ زمنية لكل دولة يُبنى عليها حدُّ يومٍ
  أصلاً. الأرقام ثوابت كود لا إعدادات — «الخطة الشهرية» في القسم 4 هي مدتُها
- **التجديد المبكر يُكدَّس لا يُحرَق:** الاشتراك الجديد يبدأ من `expires_at` السابق لا من الآن، فمن
  جدّد قبل يومٍ من الانتهاء لا يخسر يومه. ولذلك «متى ينتهي اشتراكي» = **أقصى** `expires_at` بين
  صفوفه لا أحدثُها، وعدّادُ الأيام وتنبيهُ الأربع والعشرين ساعة يُبنيان عليه: تنبيهُ من اشترى شهرين
  بأن اشتراكه ينتهي غداً يدفعه لدفعٍ لا يحتاجه
- **السريان يُقرأ بالساعة لا بعمود الحالة**: شرط التوزيع `status='active' AND starts_at <= now() AND
  expires_at > now()`. المهمة الدورية تمرّ كل خمس دقائق، وبين دورتين يبقى صفٌّ انقضى وقتُه محمولاً
  على `active` — ولو قرأ التوزيعُ العمودَ وحده لأخذ كبتنٌ رحلةً بلا اشتراك. غرضُ التعليم أن يقول
  الجدولُ ما تقوله الساعة فتُقرأ التقارير بلا حساب، لا أن يحرس الأهلية
- **القناتان اليدويتان تُسجَّلان من اللوحة بعد قبض المال، ولا جدولَ طلباتٍ لهما**: لا حالة «بانتظار
  الدفع» في `driver_subscriptions` كما لا يتغيّر رصيدٌ قبل تأكيد شحنته (القسم 7). والكبتن الذي يريد
  كليكاً بلا انتظارِ موظفٍ يشحن محفظته بكليك (القسم 7) ثم يشتري منها فوراً — فلا مسار مفقود
- **لا يُباع الاشتراك لحسابٍ غير معتمد**: عدُّ أيامه يبدأ فوراً، فبيعُه لمن ينتظر الموافقة يحرق أيامه
  في الانتظار. وهو ترتيب القسم 16.13 نفسه: تسجيل ← موافقة ← اشتراك
- **الكنس لا يُخرج كبتناً في رحلة**: إسقاط حضوره وسط `in_progress` يقطع خريطة راكبه ويوقظ تنبيه
  «انقطع الكبتن» (القسم 5) على من لم ينقطع. ولا رحلة جديدة تصله على أي حال — الأهلية تُقرأ بالساعة
- **تنبيه الأربع والعشرين ساعة أثرُه مفتاحُ Redis لا عمودٌ في الجدول**: حدثٌ عابر لا سجلٌّ يُحاسَب
  عليه، وبغير أثرٍ يتكرر التنبيه في كل دورة كنس حتى ينتهي الاشتراك. وقناته `ws:user:{id}` حتى تصل
  إشعاراتُ Push في المرحلة 8 (القسم 15/أ)

---

## 9. محفظة الكبتن والسحب

- الرصيد = أرباح البطاقات/المحفظة − العمولات (إن وجدت) − الاشتراكات المدفوعة منها − السحوبات
- طلب سحب: حد أدنى (إعداد إدارة) → `pending` → المحاسب يحوّل (كليك على `cliq_alias` أو بنكي) → يسجل المرجع → `paid` → قيد `withdrawal`
  - **ومن التطبيق كليك وحدها** (المرحلة 10): لا حقلَ لبيانات حسابٍ بنكي على `drivers` ولا على
    `withdrawal_requests`، فطلبٌ بنكيٌّ من الكبتن طلبٌ بلا وجهةٍ يعرفها أحد. والقناة البنكية تبقى
    للإدارة حيث تُعرف الوجهة خارج الجدول، وأعمدتُها في `FUTURE-FEATURES.md` بند 41
- الرحلات النقدية وكليك **لا تؤثر على رصيد المحفظة** (الكبتن قبضها مباشرة)، لكنها **تُسجل في العمليات والكشوفات**: تظهر في شاشة "أرباحي" وكشف حساب الكبتن (من جدولي rides/payments) بوسم "مُحصَّل مباشرة"، وتدخل في تقارير الإدارة المالية — فيكون الكشف شاملاً لكل الدخل بغض النظر عن طريقة التحصيل

---

## 10. التتبع اللحظي (WebSockets)

- قناة الكبتن: يبث `{lat, lng, heading}` كل 3 ثوانٍ → تحديث Redis GEO
- قناة الراكب:
  - **قبل الطلب (الشاشة الرئيسية):** يستقبل مواقع الكباتن المتاحين القريبين (ضمن ~3كم من Redis GEO) لعرضهم كأيقونات سيارات تتحرك على الخريطة — تحديث كل 5 ثوانٍ، **بيانات مجهّلة تماماً**: إحداثيات + اتجاه + فئة المركبة فقط، بدون هوية الكبتن أو رقم اللوحة، والمتاحون فقط (online + اشتراك ساري + بدون رحلة). حركة الأيقونات تُنعَّم بالـ interpolation في الواجهة
  - **أثناء الرحلة:** يستقبل موقع كبتنه المُسنَد فقط (كل 3 ثوانٍ، ببياناته الكاملة)
- أحداث الرحلة تُبث للطرفين: `driver_assigned`, `driver_arrived`, `ride_started`, `ride_completed`, `ride_cancelled`
- Reconnect تلقائي مع استرجاع آخر حالة عبر REST

**إشعارات Push بجانب البثّ (المرحلة 8).** الـ WebSocket يصل التطبيقَ وهو مفتوح، وPush يصله وهو مغلق —
فكلُّ ما يُبث في هذه القائمة يُرسل Push معه: أحداث الرحلة الخمسة، وعرضُ الطلب على الكبتن، وتنبيها
الاشتراك (قبل 24 ساعة وعند الانتهاء). وثلاث قواعد تحكمه:

- **لا Push لجهازٍ سوكته نشط.** المقبس يحمل `?device_id=` ويترك أثراً في Redis ما دام مفتوحاً، فيُستثنى
  ذلك الجهاز وحده: الحدثُ وصله بالفعل، وإشعارٌ ثانٍ على شاشةٍ مفتوحة يقول للمستخدم إن التطبيق لا يعرف
  أنه يستعمله. وهذا نفسه ما يمنع أن يصل الفاعلَ إشعارٌ بفعله. ومقبسٌ بلا `device_id` لا يمنع Push:
  حرمانُ كل أجهزة الحساب لأن أحدها مفتوح يعني هاتفاً في الجيب لا يرنّ لأن لوحاً على الطاولة مفتوح
- **طلبات الرحلة بأولوية عالية** (`android.priority=high` و`apns-priority=10` و`Urgency: high`): مهلة
  القبول عشرون ثانية، وDoze mode ووضعُ توفير البطارية يؤجّلان الإشعار العادي دقائق — فتصل البطاقة بعد
  أن انتقل الطلب لكبتنٍ آخر. وما عداها بأولوية عادية
- **هذه الفئة غير قابلة للإيقاف من المستخدم**: جزءٌ من الخدمة لا إعلان. ولا تحبسها ساعات هدوء
  (القسم 13). أما الحملات التسويقية فأولويةٌ عادية، ومفتاحُ إطفاءٍ بيد صاحبها، ونافذةُ هدوء

**الفشل يُبتلع ويُسجَّل**: إشعارٌ لم يصل خسارةٌ أهون من رحلةٍ لم تبدأ لأن خدمةً بعيدة تأخرت.

**حمولةُ الإشعار: `data` قيمٌ خام، و`title`/`body` لدرج النظام وحده.** كلُّ إشعارٍ معاملاتي يضع في
`data` ما تحتاجه الواجهة لتصوغ جملتها: `type` و`ride_id` و`amount` و`currency` وما يخصّ الحدث
(`payment_id`، `transfer_reference`، `expires_at`، `doc_type`، `review_note`…) — **لا جملةً مصوغة
ولا أرقاماً منسّقة**. والتنسيقُ قرارُ عرضٍ لا قرارُ خدمة: نصٌّ تبنيه الخلفية بـ`f"{amount}
{currency}"` يخرج «4.100 JOD» بخاناتٍ لاتينية ورمزٍ إنجليزي في تطبيقٍ كلُّ أرقامه عربيةٌ-هندية.
**ولا تُعرَّب الخانات في الخلفية**: ذلك يضع قرارَ لغةٍ في طبقةٍ لا تعرف من يقرأ، ويمرّ بالمال حيث
لا يجوز أن يُمسّ إلا حسابياً.

**وساعاتُ الهدوء تُنشر في `GET /config`** لكل دولة ومعها مِنطقتُها الزمنية (`quiet_hours_start`،
`quiet_hours_end`، `quiet_hours_timezone`): عامّةٌ بطبيعتها — تخبر المستخدم متى **لا** تصله
الحملات — وبغير نشرها تكتب الواجهةُ رقماً من عندها فيخالف الجدول يوم يغيّره المشرف. والغائبةُ
`null` لا صفر: «لم تُضبط» ليست «منتصف الليل».

وتبقى `title`/`body` مكتوبتين في الخلفية لسببٍ واحد: **درجُ نظام التشغيل يرسمهما والتطبيق مغلق**،
فلا واجهةَ تصوغ شيئاً حينها. أما ما ترسمه الواجهة بنفسها — صندوقُ الوارد والأوراق السفلية — فيصوغ
نصَّه من `data`، ويقع على `title`/`body` المخزَّنين احتياطاً لنوعٍ لا يعرف صياغته (نوعٌ جديد في
الخلفية لا يكسر الشاشة). **والحملات التسويقية استثناء**: نصُّها هو المحتوى كما كتبه المشرف.

**عقد تسجيل الأجهزة — تلتزم به التطبيقات الثلاثة.** قاعدةُ استثناء الجهاز المفتوح أعلاه لا تعمل بغيره،
والخلفيةُ لا تستطيع فرضه: هي لا تعرف من الجهاز إلا ما يرسله.

| متى | ماذا يستدعي التطبيق |
|-----|---------------------|
| بعد الدخول، وكلما دار رمز FCM (`onTokenRefresh`) | `PUT /me/devices` بـ `{device_id, token, platform}` |
| عند الخروج، وقبل تبديل الحساب على نفس الجهاز | `DELETE /me/devices/{device_id}` |
| عند فتح المقبس | `WS /ws/rider?token=…&device_id=…` أو `/ws/driver?…` — **بنفس `device_id`** |

- **`device_id` يولّده التطبيق ويثبت مع تثبيته** (لا يتغير بتغير رمز FCM ولا بتسجيل خروجٍ ودخول). هو
  الوصلةُ الوحيدة بين «هذا المقبس مفتوح» و«هذا الرمز لا تُرسل إليه»؛ ومقبسٌ يفتح بـ `device_id` مختلف
  عن الذي سجّل الرمز يجعل الجهاز يستقبل الحدثَ مرتين: على الشاشة ومن نظام التشغيل.
- **مقبسٌ بلا `device_id` مقبولٌ ولا يمنع Push** — القرار مذكور أعلاه.
- **الخروج يحذف الصف ولا يكتفي بإطفائه**: رمزٌ لحسابٍ غادر الهاتف يوصل إشعاراته إلى من يحمله بعده.
  ولذلك أيضاً `token` فريدٌ على الجدول كله: تسجيلُه لحسابٍ آخر ينقله إليه (القسم 4).
- **الرمز لا يعود من الخلفية أبداً** في أي مخرج: `GET /me/devices` يعطي `device_id` والمنصة والحالة
  وآخر ظهور لا غير.

**أداة تطوير:** `tools/fcm-token/` صفحةٌ واحدة تُخرج رمز جهازٍ من المتصفح، فتُختبر السلسلة كاملةً
(عقد → OAuth → FCM → جهاز) قبل بناء التطبيقات — ومعها `POST /admin/campaigns/test-push` الذي يرسل
إلى رمزٍ يكتبه المشرف.
- **مسار REST احتياطي لبثّ الموقع:** `POST /drivers/me/location` بنفس الحمولة `{lat, lng, heading}`. القناة الأساسية تبقى الـ WebSocket؛ هذا المسار لتطبيقٍ فقد مقبسه ولم يُعِده بعد، فلا يسقط الكبتن من التوزيع ومن خريطة الركاب لأجل انقطاع لحظي. **محكوم بـ rate limiting لكل كبتن** بما يناسب دورة الثلاث ثوانٍ (نحو 30 طلباً في الدقيقة): مسار كتابة متكرر بلا كلفة على العميل، فبغير سقفٍ يصلح لإغراق Redis

---

## 11. تطبيق الراكب — الشاشات

1. **دخول**: رقم الهاتف + كلمة المرور — دائماً ولكل المستخدمين.
   **تسجيل**: إثباتُ الرقم مرةً (تدفّق Firebase في التطبيق، أو رمزٌ نرسله إن كان المُحقِّق SMS) ثم
   يضع المستخدم كلمة مروره في نفس الطلب. **نسيت كلمة المرور**: نفس الإثبات ثم كلمةٌ جديدة — شاشةٌ
   منفصلة، ولا تُفتح جلسةٌ بمجرد الإثبات بل بعد كتابة الكلمة. والواجهة تعرف أيَّ تدفّقٍ ترسم من
   `GET /auth/method` (`verification`)
2. الرئيسية: خريطة Mapbox ملء الشاشة تعرض **سيارات الكباتن القريبين تتحرك لحظياً** (أيقونة سيارة تدور مع الاتجاه heading) + bottom sheet "إلى أين؟"
3. اختيار الوجهة (دبوس + بحث Geocoding) → عرض السعر المقدر وفئة المركبة → تأكيد الطلب.
   **ومعه اختيارُ «أي كبتن / كبتنة فقط»** حيث الخدمة النسائية مفعّلة ولمن أعلنت جنسها أنثى
   (المرحلة 10-ج) — ولا يظهر لغيرها: عرضُ «اطلب كبتنة» على راكبٍ رجل يفتح البابَ الذي وُجدت
   الخدمة لإغلاقه. و**ثمنُ الاختيار مكتوبٌ قبل الضغط**: الكبتنات أقل عدداً فالانتظار أطول
   والبحث يتسع — قولُه هنا يجعل الانتظار خياراً، والسكوتُ عنه يجعله عطلاً ثم «لم نجد كبتناً»
   بلا سبب
4. البحث عن كبتن (مؤشر حي) → بطاقة الكبتن + تتبع لحظي + مشاركة الرحلة (رابط).
   وأثناء البحث عن كبتنة يُعاد قولُ سبب الانتظار، و«لم نجد كبتناً» على طلبٍ نسائي يحمل سببَه
   ومخرجَه («لا كبتنة قريبة الآن — جرّبي بعد قليل أو اطلبي أي كبتن») لا نصّاً عاماً يُقرأ رفضاً
   شخصياً. **وعند بدء الرحلة النسائية يُقترح مشاركتُها** مع من تثق: الزرُّ قائمٌ لكل رحلة، وما
   يضيفه الاقتراح هو **التوقيت** — لحظةَ تحرّك السيارة، ولمن طلبت الخدمة لا للجميع
5. شاشة الدفع (الطرق المتاحة حسب الدولة) → التقييم
6. محفظتي: الرصيد، الشحن، التحويل (إن مفعّل)، السجل
7. رحلاتي (السجل والتفاصيل) + الملف الشخصي
8. **الإشعارات — عقدٌ يلتزم به التطبيق لا شاشة** (تفصيله في القسم 10):
   - بعد الدخول وكلما دار رمز FCM: `PUT /me/devices` بـ `{device_id, token, platform}`
   - عند الخروج: `DELETE /me/devices/{device_id}`
   - فتح المقبس بـ `?token=…&device_id=…` **بنفس `device_id`** — وإلا وصل الحدث مرتين: على الشاشة
     ومن نظام التشغيل
   - في الملف الشخصي مفتاحٌ واحد: **إشعارات العروض** (`marketing_push_enabled`). إشعارات الرحلة
     ليست خياراً — من يطفئ «وصل الكبتن» ينتظر كبتناً لا يعرف أنه وصل

## 12. تطبيق الكبتن — الشاشات

1. دخول برقم الهاتف + كلمة مرور (والتسجيل والاستعادة كما في القسم 11/1) → أول مرة: رفع المستندات →
   بانتظار الموافقة. **ولا تُعتمد وثائقُه ما لم يكن رقمه مُثبتاً** مهما كان مفتاح التحقق — رقمُه هو
   ما يستلم عليه حوالات كليك، فتطالبه الشاشة بالإثبات قبل أن ينتظر المراجعة.
   **ورفعُ المستندات مبنيٌّ في المرحلة 9-ب** (`PUT /drivers/me/documents/{doc_type}`): صورة أو PDF،
   والنوعُ والحجم يُفحصان من محتوى الملف لا من ترويسته، ورفعُ نوعٍ ثانيةً استبدالٌ يعيده «قيد
   المراجعة». وتعرض الشاشةُ حالة كل مستند وسببَ رفضه — **السبب جزءٌ من الإشعار** لا تفصيلٌ يُطلب
   بعده: كبتنٌ لا يعرف لماذا رُفضت رخصته يعيد رفع الصورة نفسها ويبقى منتظراً إلى الأبد
   **وبياناتُ المركبة تُحرَّر من التطبيق** (`PATCH /drivers/me/vehicles/{id}`) **بسياسة
   الاستبدال نفسِها**: رخصةُ المركبة أحدُ المستندات الثلاثة المطلوبة، فتغييرُ ما تشهد
   عليه — اللوحةُ أو الصنع أو الطراز أو السنة أو الفئة — **يعيد الكبتن المعتمد «قيد
   المراجعة»**، واللونُ لا يفعل. ومن دون ذلك يصير الحقلُ باباً لتشغيل مركبةٍ لم تُراجع
   أوراقُها تحت اعتمادٍ صدر لمركبةٍ أخرى. والقياسُ على ما **تغيّر فعلاً** لا على ما
   أُرسل — إرسالُ اللوحة نفسِها ليس تغييراً — والشاشةُ تقول الثمنَ قبل الحفظ لا بعده،
   والتعديلُ مرفوضٌ أثناء رحلةٍ جارية، **والفئةُ لا تُحرَّر من التطبيق**: هي التي تحدّد
   التعرفة، فتغييرُها قرارُ إدارة. **وحالةُ الكبتن تُقرأ تحت قفل صفِّه** في هذا المسار
   وفي استبدال المستند كليهما: بغيره يُلغي فعلُ الكبتن **إيقافاً إدارياً** وقع في نفس
   اللحظة — تُكتب `pending` فوق `suspended` فيعود الموقوف إلى طابور المراجعة
2. الرئيسية: مفتاح Online/Offline كبير + الخريطة (وضع ليلي افتراضي)
3. بطاقة طلب واردة: نقطة الانطلاق، المسافة إليها، السعر، عدّاد 20 ثانية، قبول/رفض.
   **وشارةُ «طلب نسائي»** على ما طُلب فيه جنسٌ (المرحلة 10-ج) — فوق السعر لأنها تُقرأ **قبل**
   قرار القبول: كبتنٌ يقبل ثم يكتشف أن الطلب ليس له يُلغي، وإلغاءٌ كان يمكن تجنّبه بشارة.
   وهي **وصفٌ للطلب لا هويةُ صاحبته**: جنسُ الراكبة لا يصل التطبيق أصلاً
   - وفي الإعدادات **تفضيلُه الدائم لجنس الركاب** (الجميع / النساء فقط / الرجال فقط)، معروضاً
     لكل كبتنٍ لا للكبتنات وحدهنّ — ولا يظهر حيث الخدمة مطفأة، فمفتاحٌ لا أثر له يوهم صاحبه
     أن طلباتِه تُصفّى وهي لا تُصفّى
4. الرحلة النشطة: ملاحة للانطلاق → "وصلت" → "بدء الرحلة" → "إنهاء" → شاشة تحصيل الدفع
5. اشتراكي: الحالة + التجديد بالخطط الثلاث
6. محفظتي: الرصيد + طلب سحب + السجل
7. أرباحي: اليوم/الأسبوع/الشهر + عدد الرحلات — `GET /drivers/me/earnings?period=`.
   **وثلاثةُ أرقامٍ لا رقمان**: ما دخل المحفظة (`ride_earning`)، وما خرج منها عمولةً
   (`commission` — **ويُخصم على الرحلة النقدية أيضاً** حين يكون النطاق `all_rides`)، وما
   قُبض باليد كاشاً وكليكاً موسوماً **«مُحصَّل مباشرة»** كما يوجب القسم 9. **والصافي قد
   يكون سالباً ولا يُقصّ عند الصفر**: يومٌ كلُّه كاش يترك عمولةً بلا أرباحَ تقابلها،
   وإخفاءُ ذلك يجعل الكبتن يكتشف نقصان رصيده بلا سببٍ ظاهر. **والنافذةُ يومُ الدولة**
   بمنطقتها المخزَّنة كنافذة القسم 13/1، **والجمعُ في القاعدة** لا في التطبيق (القسم 14)
7-أ. **سجل الرحلات وتفاصيلها**: قائمةٌ من `GET /rides/me` (وقتٌ وأجرةٌ ونقطتان ومسافةٌ وحالة)،
   **ومعها حالُ دفع كلِّ رحلة**: يردّ المسار `{ride, has_open_dispute, payment_methods,
   paid_amount}` فتحمل الشارةُ البرتقالية «نزاع» ما دفعتُه متنازعٌ عليها. والملخّصُ
   **استعلامٌ ثانٍ على صفحة الرحلات كلِّها** لا نداءٌ لكل صفٍّ ولا ضمٌّ إلى الاستعلام
   الأول: الدفعُ المختلط صفّان (القسم 6)، فالضمُّ يضاعف الرحلة ويُسقط جزءاً منها من
   صفحةٍ مسقوفة. ثم
   تفاصيلُ رحلةٍ واحدة معها دفعاتُها وتقييمُه للراكب. **وزرُّ النزاع فيها مشروطٌ بدفعة كليك
   بانتظار التأكيد** — `dispute_by_driver` يرفض ما عداها (القسم 6.2): الكاش يقع يداً بيد، ومن لم
   يقبض لا يضغط «استلمت المبلغ» فتبقى الدفعة معلّقةً تراها الإدارة. ولا تفصيلَ تعرفةٍ سطراً سطراً
   في الشاشة: `GET /config` لا ينشر أسعار `pricing_settings`، والحسابُ في الخلفية حصراً (القسم 14)
8. **الإشعارات**: نفس عقد التسجيل في القسم 11/8 والقسم 10 حرفاً بحرف. وهو هنا **أشد إلزاماً**:
   بطاقة الطلب الواردة تصل بأولوية عالية ومهلتها عشرون ثانية، فجهازٌ لم يسجّل رمزه لا تصله طلبات
   والتطبيق مغلق — ويبدو للكبتن كأن لا رحلات في المنطقة

## 13. لوحة الإدارة — الأقسام

1. Dashboard: رحلات اليوم، الكباتن أونلاين (خريطة حية)، الإيرادات.
   و**«الخريطة الحيّة» لـ admin حصراً** (البند 8 يعطي الدعمَ قراءةً ومعالجةَ نزاعات، ومعرفةُ أين
   يقف كلُّ كبتنٍ الآن باسمه ورقمه ليست منهما): هي **المنفذ الوحيد في المنصة الذي يقرن هويةً
   بموقع**، وخريطةُ الراكب تبقى مجهَّلةً كما يفرض القسم 10 ولا يُعاد استعمال تمثيلها.
   **وفتحُها يدخل سجل التدقيق** (`read` على `live_map`) — قراءةٌ تُسجَّل، وهي نادرةٌ عمداً:
   صلاحيةٌ بهذا الاتساع تُسأل عنها. والقيدُ **مخنوقٌ بنافذة** فجلسةُ مراقبةٍ قيدٌ واحد، وإلا
   أغرقت شاشةٌ تُحدَّث كل خمس ثوانٍ السجلَّ بمئة صفٍّ فأخفت القرارات الحقيقية بينها.
   **ولا إسنادَ يدويّ فيها**: الرحلة تُعرض على كبتنٍ واحدٍ في كل لحظة (القسم 5)، وزرٌّ يتخطى
   العرضَ من اللوحة يُسند رحلةً لمن لم يقبلها — البند مؤجَّل في `FUTURE-FEATURES`
2. الكباتن: طلبات الانضمام (مراجعة مستندات: قبول/رفض)، القائمة، إيقاف/تفعيل.
   **الاعتماد يشترط `phone_verified_at`** ويردّ 403 بدونه — لا يعفيه إطفاء مفتاح التحقق.
   **ويشترط أيضاً (المرحلة 9-ب) قبولَ المستندات المطلوبة الثلاثة** ويردّ 409 بدونها تسمّي الناقص:
   بغير هذا الحارس تصير المراجعة عادةً لا شرطاً — زرُّ «اعتماد» يعمل والمستنداتُ فارغة، وهو ما كان
   يقع فعلاً. مراجعةُ المستند قرارٌ لـ **admin حصراً** (البند 8) ورفضُه يوجب سبباً مكتوباً يصل
   صاحبه، وكلُّ مراجعةٍ تدخل سجل التدقيق. والقراءة والمعاينة لـ staff
3. الركاب: قائمة، حظر، تجميد محفظة. **والحسابات غير محققة الرقم موسومةٌ وقابلة للفلترة**
   (`GET /admin/users?phone_verified=false`) — حالةٌ استثنائية لا تُعالَج إن لم تُرَ
4. الرحلات: بحث وفلترة + تفاصيل كاملة + النزاعات (`disputed`) للفصل
5. المالية: تأكيد شحنات كليك/كاش، طلبات السحب، تقارير الاشتراكات
6. الإعدادات per-country: التسعير، خطط الاشتراك، مفتاح العمولة ونسبتها، feature flags، حدود التحويل
   والسحب. ومنها **`otp_verification_enabled`**: لـ admin حصراً، بتحذيرٍ صريح أنه **للطوارئ فقط**،
   وبحقل **سبب إلزامي** عند الإطفاء يدخل سجل التدقيق. ولا يعطّل استعادة كلمة المرور أبداً
7. **العقود وتكاملات API:** بطاقة لكل مزود خارجي (SMS، Telr، Mapbox، CliQ acquirer، FCM، payout) بحقولها الخاصة (api_key، secret، merchant_id، sender_id...) + مفتاح تفعيل + زر "اختبار الاتصال". إدخال بيانات عقد جديد يفعّل ميزته آلياً بدون نشر كود. القيم تُعرض مقنّعة (\*\*\*\*) بعد الحفظ
8. صلاحيات: admin كامل / support قراءة + معالجة نزاعات فقط — صفحة العقود لـ admin حصراً.
   **ومعها التحقق الثنائي (المرحلة 12-د)**: قسمُ «الأمان» في حساب المستخدم نفسِه (تسجيلُ العامل
   وإطفاؤه ورموزُ الاسترداد)، ومفتاحُ **إلزام الـadmin** ومهلةُ الخمول في `security_settings` —
   وإشعالُ الإلزام مرفوضٌ حتى يُثبت المشرفُ الطالبُ أن استردادَه يعمل. التفصيل في القسم 14.1
9. **حملات الإشعارات** (المرحلة 8 بنت مساراتها، وواجهتها في **المرحلة 11**): إنشاء حملة وتعديلها
   وجدولتها وإلغاؤها، واختيار الجمهور (كل الركاب / كل الكباتن / مستخدمو دولة)، وسجلُّ من وصله الإشعار
   ومن تُخطّي ولماذا. **لـ admin حصراً**: حملةٌ تصل عشرات الآلاف ليست إجراء دعمٍ فني. وكل كتابةٍ تدخل
   سجل التدقيق. ومعها ضبط **ساعات الهدوء per-country** (البند 6): لا إرسال تسويقي داخلها، ويُؤجَّل
   للنافذة التالية لا يُلغى ولا يُرسل ناقصاً — والتقسيم بالدولة لا بالحملة، فحملةٌ للسوقين تُرسل في
   الأردن الآن وفي ليبيا بعد ساعة إن اختلفت نافذتاهما. **والإشعارات المعاملاتية خارج هذا كله**
   (القسم 10)

---

## 14. الأمان (إلزامي)

- كل الحسابات المالية والتسعير في الخلفية حصراً — الواجهة تعرض فقط
- التحقق من ملكية الرحلة/المحفظة في كل endpoint (لا IDOR)
- Rate limiting على محاولات الدخول والطلبات
- تحقق توقيع Webhooks من Telr
- Idempotency keys على عمليات الدفع والتحويل (منع التكرار)
- كل عمليات المحفظة داخل DB transactions ذرّية
- سجل تدقيق (audit log) لكل إجراء إداري مالي
- لا أسرار في الكود أو الواجهة: أسرار البنية التحتية فقط (DB, Redis, JWT secret, مفتاح تشفير provider_credentials) في `.env` — وكل مفاتيح المزودين الخارجيين (Mapbox، Telr، SMS، FCM...) في جدول provider_credentials مشفرةً، تُدار من صفحة العقود حصراً

### 14.1 التحقق الثنائي لدخول اللوحة (TOTP) — المرحلة 12-د

**TOTP لا SMS** (قرارُ المالك): مجانيٌّ، ولا يحتاج عقدَ مزوّدٍ يُنتظر، وأقوى من رمزٍ يمشي في شبكةٍ
يمكن تحويلُ رقمها. **وهو للوحة وحدها**: مساراتُ `POST /auth/me/totp/*` لـ`StaffUser` حصراً. عاملٌ
ثانٍ للراكب قرارُ منتجٍ بكلفةِ دعمٍ حقيقية (راكبٌ يفقد هاتفه يفقد حسابه) ولا يطلبه هذا القسم.

**والدخولُ خطوتان، ولا توكنَ قبل العاملين** — وهي قاعدةُ استعادة كلمة المرور نفسُها حرفياً
(«التوكنات تُصدر بعد كتابة الكلمة لا بعد التحقق»):

1. `POST /auth/login` كما هو بسقوفه. فإن كان لصاحب الحساب عاملٌ **مؤكَّد** ردَّ **بلا توكنات**:
   `{"totp_required": true, "challenge_token": …, "expires_in": 300}`.
2. `POST /auth/login/totp` بالـchallenge والرمز → زوجُ التوكنات المعتاد.

والـchallenge **ليس access token ولا نصفَ جلسة**: قيمةٌ عشوائيةٌ مفتاحُها في Redis مربوطٌ
بالمستخدم، مهلتُها خمسُ دقائق، تُستهلَك مرةً واحدة، **ولا تفتح أيَّ مسارٍ في المنصة**. رمزٌ مكانه
`sub` في توكنٍ حقيقيٍّ «ناقصِ صلاحية» يصير جلسةً كاملةً بخطأٍ واحدٍ في حارسٍ واحد.

الرمزُ ستةُ أرقامٍ بخطوةِ ٣٠ ثانية، ويُقبل انحرافُ خطوةٍ في كل اتجاه (±٣٠ث) — ساعةُ حاسوبٍ مكتبيٍّ
تنحرف. ولا يُقبل رمزُ خطوةٍ سبق قبولُها (`last_step`).

**السقوف**: خمسُ محاولاتٍ لكل challenge ثم يُهدَم فيعود صاحبُه إلى كلمة المرور، وسقفٌ ساعيٌّ على
الحساب فوقه. **ولا قفلَ حساب**: قفلُ حسابِ مشرفٍ بالمحاولات الخاطئة بابٌ لتعطيل اللوحة يدفعه من
يخمّن سيئاً — والحدُّ يُبطئ التخمين، والقفلُ يُسلّم اللوحةَ للمخمّن.

**والتسجيل نداءان لا واحد**: `POST /auth/me/totp/enroll` يُنشئ سرّاً غيرَ مؤكَّدٍ ويردّه مرةً واحدة
(`otpauth://totp/...` ترسمه اللوحةُ QR **محلياً** — خدمةُ QR خارجيةٌ تعني إرسالَ السرِّ إلى طرفٍ
ثالث)، و`POST /auth/me/totp/confirm` برمزٍ صحيحٍ يختم `confirmed_at` ويولّد **عشرةَ رموز استردادٍ
تُعرض مرةً واحدة**. ولا مسارَ يعيد قراءةَ السرِّ ولا الرموز بعدها: مسارٌ يعرض السرَّ ثانيةً يجعل
جلسةً مسروقةً كافيةً لاستخراج العامل الذي وُضع ضدها.

**والإطفاء يشترط عاملاً حاضراً**: `DELETE /auth/me/totp` يطلب رمزاً صحيحاً أو رمزَ استرداد، لا
جلسةً وحدها — وإلا أسقطت جلسةٌ مسروقةٌ العاملَ الذي وُضع لأجلها. ويُرفض أصلاً حين يكون الإلزام
مشتعلاً على دور صاحبه: مفتاحٌ يخرج منه كلُّ مشرفٍ بنداءٍ واحد ليس إلزاماً.

**وإشعالُ الإلزام مشروطٌ بإثباتِ استردادٍ حقيقيّ** (قرارُ المالك: «لا تُلزم أحداً قبل أن أُثبت أن
الاسترداد يعمل»): `PUT /admin/security` يرفض `admin_totp_required = true` إن لم يكن **للمشرف
الطالبِ نفسه** عاملٌ مؤكَّدٌ و`recovery_codes_verified_at` مختوم. والختمُ يقع بنداءٍ صريح
(`POST /auth/me/totp/recovery/verify`) **يستهلك رمزاً حقيقياً**: إثباتٌ يُجرَّب لا مربَّعٌ يُؤشَّر —
يكلّف واحداً من عشرة، ويخبرك أن ما نسختَه هو ما تحفظه القاعدة فعلاً.

**والمشرفُ المُلزَمُ بلا عامل** يدخل بكلمة مروره ويجد كلَّ مسارٍ إداريٍّ مردوداً 403
`totp_enrollment_required` إلا مساراتِ التسجيل. والحارسُ يقرأ الصفَّ في كل طلبٍ كما يقرأ
`is_blocked` — **فلا مطالبةٌ تعيش في توكن ولا حالةُ جلسةٍ جديدة**، والإلزامُ يسري على جلسةٍ قائمة
لحظةَ إشعاله. وردُّ الدخول لا يُعلن السياسة قبل كلمة المرور، و`GET /config` لا ينشرها: «هل تكفي
كلمةُ المرور هنا» سؤالٌ لا يفيد إلا من لا يملكها.

**ومفتاحُ الإلزام للـ`admin`**، أما **العاملُ المؤكَّد فيسري على صاحبه أيّاً كان دورُه**: من سجّل
عاملاً يُسأل عنه في كل دخول. و`support` تسجيلُه اختياريٌّ اليوم — وجعلُه إلزامياً حقلٌ ثانٍ في
`security_settings` لا بناءٌ جديد، ولا يُضاف قبل أن يُطلب.

**ومهلةُ الخمول ٣٠ دقيقة طبقتان، ولكلِّ طبقةٍ ما تملكه** — وواحدةٌ منهما وحدها وعدٌ لا يُوفى:

- **الخلفية**: مفتاحُ الـrefresh لحسابات `admin`/`support` يُكتب بـTTL = `admin_idle_timeout_minutes`
  ويُجدَّد عند كل تدوير. لا عمودَ «آخرِ نشاط» ولا مؤقّتَ ثانٍ: التدويرُ لمرةٍ واحدة قائمٌ أصلاً فهو
  نبضةُ النشاط نفسُها. وما تملكه هذه الطبقة أن **توكنَ تجديدٍ مسروقاً يبطل بعد نصف ساعة**.
- **اللوحة**: مؤقّتٌ على **تفاعل المستخدم** يُخرج عند سكونٍ بطول المهلة. وما يملكه أن **تبويباً
  متروكاً على مكتبٍ يُقفَل** — وهو ما لا تراه الخلفيةُ أبداً: الخريطةُ الحيّة تستفتي كل خمس ثوانٍ،
  فتبويبٌ لا أحدَ عنده يجدّد جلستَه إلى الأبد لو حُسبت المهلةُ من النداءات. والأحداثُ المقيسة
  نقرٌ وضغطُ مفتاحٍ وتمريرٌ ولمس — **و`mousemove` ليست منها**: فأرةٌ يهزّها اهتزازُ المكتب تُبقي
  الجلسةَ حيّةً بلا إنسان. والقياسُ **ختمُ وقتٍ يُفحص دورياً لا `setTimeout` يُعاد ضبطه**، لأن
  حاسوباً أُغلق غطاؤه ساعةً يُجمّد المؤقّتات فيستيقظ ومؤقّتُه لم ينتهِ — أما الختمُ فيُقرأ عند
  الاستيقاظ فيُخرج فوراً. والمدةُ تصل من `GET /auth/me/totp` لا مكتوبةً في كود اللوحة: رقمٌ في
  المتصفح يفترق عن `security_settings` أوّلَ مرةٍ يُعدَّل، و`support` يحتاجه ولا يقرأ
  `GET /admin/security`.
- **وشاشةُ الأمان في اللوحة بطاقتان بمدىً مختلف**: «التحقق الثنائي لحسابي» لكل من يدخل اللوحة
  (فالعاملُ المؤكَّد يسري على صاحبه أيّاً كان دورُه)، و«سياسةُ دخول اللوحة» لـ`admin` حصراً. وزرُّ
  الإلزام **يُعطَّل بسببٍ مكتوب** لا يُترك ليرتدّ 409 — قاعدةُ زرِّ «اعتماد الكبتن» نفسُها. والرمزُ
  يُرسم QR **في المتصفح** بلا خدمةٍ خارجية: حمولةُ `otpauth://` تحمل السرَّ نفسه.
- **و`totp_enrollment_required` قد يرتدّ من أيّ نداء** (الحارسُ يقرأ الصفَّ في كل طلب)، فترجمتُه
  في **عميل HTTP واحد** يرفع علماً تقود به طبقةُ الجلسة إلى شاشة الأمان — لا في كل شاشةٍ على حِدة،
  وإلا رأى المشرفُ خطأً أحمر في كل بطاقة بدل بابٍ يفتح. وشاشةُ الأمان وحدها خارج تلك البوابة، وإلا
  صارت الحلقةُ مغلقة.
- **والتوكنات تبقى في `localStorage` لا `sessionStorage`**: الثاني نسخةٌ لكل تبويب، فتبويبان على
  سوقين — وهي طريقةُ عملٍ مقصودةٌ في القسم 13 — يصيران دخولين. المهلةُ هي ما يحدّ المخاطرة لا
  مكانُ الحفظ.

**وكلُّ ما يمسّ العامل يدخل سجل التدقيق**: تسجيلٌ، تأكيدٌ، إطفاءٌ، استهلاكُ رمز استرداد، وإشعالُ
الإلزام وإطفاؤه. ومعه **صفُّ إشعارٍ لصاحب الحساب** عند استهلاك رمز استرداد أو إطفاء العامل: هذان
بالذات ما يفعله من استولى على حساب، وصاحبُه أوّلُ من يجب أن يعرف.

**وإطفاءُ العامل يُبطل كلَّ جلسات صاحبه** (`revoke_all_for_user`) — **وتأكيدُه لا يُبطلها**، والفرقُ
اتجاهُ التغيير: الإطفاءُ يُضعف الحماية فتبقى جلساتٌ فُتحت بسياسةٍ أقوى، والتأكيدُ يقوّيها وكلمةُ
المرور التي فتحت الجلسة لم تتبدّل. وإخراجُ صاحبها عند التأكيد لا يحمي شيئاً: توكنُ الوصول لا يُبطَل
قبل انتهائه أصلاً (أعلى هذا القسم)، فالإبطالُ يقتل تجديدَه وحده — خروجٌ صامتٌ بعد ربع ساعة، ومعه
**رمزُ الخطوة المحروقةِ في التأكيد** يجعل أوّلَ دخولٍ يبدو رفضاً بلا سبب.

**ومهلةُ الخمول حقلٌ يعدّله المشرف بسقفٍ في الكود: ٦٠ دقيقة، وحدٌّ أدنى ٥** (قرارُ المالك). اللوحة
تفتح مفاتيح المزوّدين والدفع، فمهلةٌ يوسّعها مشرفٌ إلى يومٍ تُبطل الحارسَ من داخله؛ والحدُّ الأدنى
ليس تجميلاً — مهلةُ دقيقةٍ تُخرج المشرفَ وهو يكتب فيصير الحارسُ عطلاً يُطفأ. والسقفُ محروسٌ في
ثلاث طبقات (Pydantic، الخدمة، وCHECK في القاعدة) لأنه العددُ الذي إن مرّ خطأً لم يظهر خطؤه.

---

## 15. ميزات Phase 2 — تُبنى الآن وتُفعَّل لاحقاً

كل ما يلي **يُبنى ضمن المشروع الحالي** لكنه معطل افتراضياً، على ثلاثة أنواع:

### أ) ميزات تعتمد على عقود مزودين — تتفعل من صفحة "العقود وتكاملات API"
تُبنى كاملة الآن ضد واجهة مزود موحدة (مع مزود وهمي mock للاختبار)، وتتفعل تلقائياً بمجرد إدخال بيانات العقد الحقيقي من اللوحة:
- **التحقق من الهاتف** — **لا الدخول**: الدخول كلمةُ مرور دائماً، والتحقق يقع في التسجيل وفي
  استعادة كلمة المرور. والمُحقِّق يُختار من حالة العقود، وترتيبُه بعد المرحلة 12-هـ:
  **عقد `whatsapp` مفعّلٌ ومفتاحُ `whatsapp_otp_enabled` مشتعلٌ في دولة الرقم → واتساب؛ وإلا `sms`
  مفعّل → رسالةٌ قصيرة؛ وإلا `firebase_auth` مفعّل → Firebase؛ وإلا لا مُحقِّق** (فيُرفض التسجيل
  بـ 503 ما لم يُطفأ مفتاح التحقق).
  - **وتبدّلَ الترتيب بقرار المالك**: كان Firebase أولاً. واتساب أوثقُ وصولاً في السوقين — لا
    شريحةَ تُبدَّل ولا رسالةٌ تضيع في بوابةٍ محلية — **وتبعتُه صريحة**: عقدُ Firebase مفعّلاً لا
    يُستعمل ما دام قبله عقدٌ مفعّل، فمن أراده يُطفئ ما قبله. والترتيبُ ثابتٌ في الكود لا يُقرأ من
    إعداد: مفتاحٌ يقول «أيُّها أولاً» حالةٌ ثانية تختلف يوماً عن حالة العقود نفسها.
  - **وواتساب وحده يشترط شرطين**: العقدَ **والمفتاح**. والعقدُ عامٌّ (رقمُ أعمالٍ واحد عند ميتا
    يخدم السوقين، والرقمُ يحمل دولته) والمفتاحُ per-country — فالعقدُ يقول «نستطيع» والمفتاحُ يقول
    «نفعل في هذا السوق»، ويُجرَّب في سوقٍ قبل الآخر أو يُطفأ حيث لم يُعتمد قالبُه بلغته. ولذلك لا
    `feature_key` في سجل المزودين لهذا العقد: المزامنةُ التلقائية تُشعل مفتاحَ **دولة العقد** وهذا
    عقدٌ بلا دولة، والمفتاحُ يجب أن يبقى بيد المشرف — **مطفأً افتراضاً ومكتوباً صريحاً في البذر**.
  - **والواجهة رسميةٌ من ميتا حصراً** (WhatsApp Cloud API على Graph API). **وممنوعةٌ أيُّ مكتبة
    واتساب غير رسمية** — `whatsapp-web.js` و`Baileys` وما يشبههما: تلك تُؤتمت حساباً شخصياً عبر
    واجهة الويب، وهو مخالفٌ لشروط واتساب وعقوبتُه حظرُ الرقم — أي توقّفُ تسجيل المستخدمين الجدد
    كلهم، بلا إنذارٍ ومن رقمٍ لا نملك استرجاعه. والكلفةُ ليست الحظرَ بل أنه يقع **في أسوأ لحظة وبلا
    مخرجٍ تقني**.
  - **وقوالبُ المصادقة لا تقبل نصّاً حرّاً**: القالبُ معتمدٌ عند ميتا مسبقاً وما نرسله **مُعامِلٌ
    واحد هو الرمز**. ولذلك صار عقدُ الإرسال في `services/otp.py` هو `OtpSender.send_code` لا
    `send(body)`: القناةُ تعرف كيف يُصاغ الرمز فيها، وواجهةٌ تأخذ نصّاً تكذب على مستدعيها.
  - **وفشلُ القناة الأولى لا يُبدّلها في صمت**: الرمزُ ربما وصل فعلاً، وتبديلٌ صامتٌ يجعل صاحبَه
    يقرأ رمزاً من قناةٍ ويكتب رمزاً من أخرى فيُحرق الرمزان. فيرتدّ الطلب بـ502
    `verification_send_failed` حاملاً **`fallback_channel`** — القناةَ التالية المتاحة — والتطبيقُ
    يرسم زرَّها، وغيابُها (`null`) جوابٌ صادقٌ يعني ألّا مخرجَ آخر ومكانَ إصلاحه صفحةُ العقود.
    **ورفضٌ بلا مخرجٍ ليس رفضاً.**
  - **والتحققُ من الرمز بابٌ واحد لكل القنوات**: البصمةُ محفوظةٌ **بالرقم لا بالقناة**، فمن أُرسل
    إليه في واتساب ثم ارتدّ إلى الرسائل يُقبل رمزُه. ولو ارتبط التحققُ بالقناة لبطل الارتدادُ نفسُه.
- **إشعارات Push (FCM)** — قبلها: WebSocket أثناء فتح التطبيق يكفي
- **تكامل CliQ الآلي** (QR ديناميكي عبر merchant acquirer) — قبله: تدفق كليك اليدوي الموصوف في القسم 6
- **payout API آلي للسحوبات** — قبله: التحويل اليدوي الموصوف في القسم 9

**نُفِّذت الأربعة في المرحلة 8.** القرارات التي لم يحسمها النص أعلاه:

- **لكل عائلة مزودٍ نفس البنية الثلاثية** التي أرستها `card_gateway`: `base.py` عقدٌ مجرّد،
  و`mock.py` مزودٌ وهمي يُشغَّل بحقل `use_mock` في العقد و**يُتجاهل في الإنتاج**، وملفٌّ واحدٌ
  للمزود الحقيقي هو الوحيد الذي يعرف أسلاكه، و`__init__` نقطةَ قرارٍ تقرأ العقد من
  `provider_credentials` مشفَّراً. فتفعيلُ الميزة إدخالُ عقدٍ لا نشرُ كود.
- **FCM وحده وصل عقدُه الحقيقي، وجُرِّبت سلسلتُه كاملةً على جهاز**: ملفُّ حساب الخدمة يُقرأ من مساره
  في `.env.local` مرةً واحدة بـ `python -m scripts.seed` فيُكتب **مشفَّراً** في `provider_credentials`
  ولا يُقرأ من القرص بعدها (نفس ما يجري لتوكنات Mapbox). والحلقاتُ الأربع مُتحقَّقٌ منها فعلياً على
  مشروع `taxo-84a5f`: توقيعُ JWT بمفتاح حساب الخدمة، ومبادلةُ توكن OAuth، ونداءُ `messages:send`
  المُصادَق عليه، و**وصولُ الإشعار إلى جهازٍ حقيقي** — عاديَّ الأولوية وعاليَها (مسار بطاقة الطلب)
  كلاهما. أداةُ التجربة `tools/fcm-token/` تُخرج رمز جهازٍ من المتصفح، ومعها
  `POST /admin/campaigns/test-push` الذي يرسل إلى رمزٍ يكتبه المشرف — مِجَسٌّ يتخطى جدول الأجهزة
  وقاعدة الاستثناء عمداً، غرضُه إثباتُ السلسلة قبل أن يوجد تطبيقٌ يسجّل أجهزته.
  - ما جرى في التجربة يؤكد قاعدة الاستثناء نفسها: الصفحة كانت مفتوحة فوصل الإشعاران إلى
    `onMessage` **ولم يعرضهما المتصفح على الشاشة** — وهو بالضبط ما تفعله الخلفية حين ترى سوكتاً
    نشطاً لجهاز. وعرضُ الإشعار من عامل الخدمة والتطبيقُ مغلق لم يُرصد بعينه.
  - **المرحلة 9 بنت طرفَه الناقص ولم ترصده بعد**: `customer-app/public/firebase-messaging-sw.js`
    يعرض ما يصل والتطبيق مغلق، وتطبيق الراكب يسجّل جهازه بعد الدخول ويحذفه عند الخروج. تبقى
    التجربة على جهازٍ حقيقي بعقدٍ مُدخل — لا يُقال إنها جرت قبل أن تجري.
- **ولا عقد مُسلَّم للثلاثة الباقية في جلسة التنفيذ**، فأسلاكُ الحقيقيّين (SMS، CliQ acquirer، payout)
  مبنيّةٌ على الشكل الشائع لتلك الواجهات و**تحتاج مطابقةً بالتوثيق قبل أول تشغيل حقيقي** — مجموعةً في
  ثوابت مسمّاة أعلى كل ملف، كما فُعل مع `telr.py` في 6-ب. وخطؤها لا يفتح باباً: إرسالٌ فاشل يرتدّ
  ولا يُنشئ حساباً، والدفتر لا يتحرك إلا على جوابٍ صريح من المزود للخلفية. **المزودون الوهميون هم ما
  بُنيت عليه المرحلة واختُبرت.**
- **OTP تحقّقٌ لا دخول، والدخول كلمةُ مرور دائماً** (`services/verification.py`). كان القرار ثلاثياً
  يختار **طريقة الدخول** من حالة العقود، فصار الدخول ثابتاً والعقودُ تحكم **التحقق** وحده. والسبب أن
  طريقتَي دخولٍ تعنيان جوابين متناقضين لسؤال «كيف أدخل»: حسابٌ أُنشئ تحت OTP لا كلمةَ مرور له فلا
  يدخل حين يُطفأ العقد، وحسابٌ أُنشئ تحت كلمة المرور لا يُسأل عنها حين يُفعَّل.
  - **إثبات الرقم يقع مرتين لا أكثر**: عند التسجيل (ثم يضع صاحبه كلمة مروره)، وعند الاستعادة.
  - **`OtpAuthStrategy` حُذفت كطريقة دخول ولم يُحذف كودُها**: مزود SMS التقليدي تغيّر دورُه إلى
    **المُحقِّق الثاني**، فبقي `services/otp.py` بكامله — توليدُ الرمز وبصمتُه وعدّادُ المحاولات
    ومهلةُ الإرسال — يخدم نفس الحدثين حيث لا يعمل Firebase أو حيث عقدٌ محليٌّ أرخص.
  - **ترتيب المُحقِّقين ثابت** (Firebase ثم SMS): Firebase أقوى إثباتاً وأرخص تشغيلاً — لا رسائل ندفع
    ثمنها ولا recaptcha نبنيه. ولا يُقرأ الترتيب من إعداد: مفتاحٌ يقول «أيّهما أولاً» حالةٌ ثانية
    قابلة للاختلاف عن حالة العقود، ومن أراد الثاني يُطفئ عقد الأول.
- **الاستعادة مسارٌ منفصل، ولا توكن دخولٍ يُصدر بمجرد التحقق** بل بعد كتابة كلمة المرور فعلاً: لو
  انقطع الطلب بعد الإثبات لم يبق للمهاجم شيء. وتُبطَل **كل جلسات** صاحبها عند التغيير — كلمةٌ تُغيَّر
  لأن القديمة تسرّبت لا تُغيّر شيئاً إن بقيت جلسةُ من سرّبها مفتوحة. وسقفُ محاولاتها **يومي لا ساعي**:
  محاولةُ الاستيلاء على حسابٍ بعينه لا تُستعجل، ونافذةٌ ساعيةٌ تعطي المهاجم مئةً وعشرين محاولة في اليوم.
- **Firebase: قسمةُ عملٍ لا نقلُ ثقة على عمياء.** التطبيق يتحقق من الرقم عندهم ويرسل رمز الهوية،
  والخلفية تفحص فيه أربعة أشياء قبل أن تصدّقه (`services/firebase_auth/google_certs.py`):
  **التوقيع** بمفاتيح Google العامة (RS256، و`alg: none` مرفوضٌ قبل أي نظرٍ في المحتوى)، و**`aud`
  و`iss`** لمشروعنا وحده — فرمزٌ صحيح التوقيع من مشروعٍ آخر رمزٌ **صالح**، وقبولُه يعني أن أي مشروع
  Firebase في العالم يفتح حساباتنا — و**`exp`/`iat`**، و**`sign_in_provider = phone`** فدخولٌ بحساب
  Google أو بمجهول ليس إثباتاً لملكية رقم. ثم **يُطابَق هاتف الرمز بالهاتف المُرسَل**: بغيره يرسل من
  يملك رقماً رمزَه الصحيح مع هاتفِ غيره فيدخل حسابه — والرمز صحيحٌ فعلاً، وهذا ما يجعل الثغرة غير
  مرئية في السجل. والصيغتان E.164 كلتاهما فالمقارنة نصّية مباشرة.
- **عقد `firebase_auth` مستقل عن `fcm`** وإن كان المشروع واحداً: التحقق لا يحتاج مفتاح حساب الخدمة
  أصلاً (يكفيه `project_id` ومفاتيح Google العامة)، فلا يحمل عقدُ الدخول سرّاً لا يستعمله؛ وإطفاءُ
  الإشعارات قرارٌ لا يجوز أن يُطفئ الدخول معه. و`project_id` يُنشر في `GET /config` — التطبيق يحتاجه
  ليتحقق من الرقم أصلاً، وهو معرِّفٌ عام كتوكن Mapbox العام.
- **ولا `firebase-admin`**: الكود يفعل ما تفعله الحزمة في `verify_id_token` حرفاً بحرف، بـ PyJWT
  وcryptography الموجودتين — اتساقاً مع `push/fcm.py` الذي يوقّع ويبادل توكن OAuth بيده، وتفادياً
  لجرّ `google-cloud-*` و`grpcio`. وشهاداتُ Google تُخزَّن بالعمر الذي يقوله `Cache-Control` نفسه:
  جلبُها لكل دخول يجعل الدخول رهينةَ شبكةٍ ثالثة، وتثبيتُها أبداً يكسره يوم تُدوَّر المفاتيح.
- **مسار `/auth/challenge` وبصمةُ الرمز يبقيان** لمزود SMS التقليدي؛ ولا يمر بهما تدفّق Firebase
  (الرسالة تُرسل من جهاز المستخدم)، فيعود المسار بـ `sent=false` بدل خطأ — الواجهة تسأل دائماً ولا
  تحتاج أن تعرف الاستراتيجية قبل أن تسأل.
- **OTP: الحقل نفسه لا مسارٌ جديد.** `password` في `/auth/login` و`/auth/register` يصير معناه «رمز
  التحقق»، وتعرف الواجهةُ ذلك من `GET /auth/method`. ومسارُ `POST /auth/challenge` قائمٌ في الوضعين
  ويعيد `sent=false` قبل تفعيل العقد، فلا تحتاج الواجهة أن تعرف الاستراتيجية قبل أن تسأل. الرمز
  يُحفظ **بصمةً** (HMAC) لا نصاً، ويُستهلك عند النجاح، ويُحرق بعد خمس محاولات، وله مهلةُ إعادة إرسال
  وسقفان على الرقم وعلى الـ IP. وحسابُ OTP بلا `password_hash` — والعمود nullable لهذا بالضبط.
  وسياسةُ طول كلمة المرور انتقلت إلى `PasswordAuthStrategy`: سقفٌ في طبقة النقل يمنع رمزاً من ستة
  أرقام يعطّل الطريق الآخر بدل أن يحرس هذا.
- **CliQ الآلي نطاقه شحن المحفظة وحده** (القسم 7): حساب التاجر يشهد على ما يدخل حساب الشركة، ودفعُ
  الرحلة بكليك يذهب إلى alias الكبتن فلا شاهد له (القسم 6). وطلبُ الشحن يسكن `provider_orders` لا
  `wallet_topup_requests` لنفس سبب شحن البطاقة في 6-ب: ذاك الجدول بيتُ ما ينتظر **إنساناً**.
  والقناة اليدوية تبقى قائمة بجانبه — مزوّدٌ متوقف لا يقطع قناة شحنٍ كاملة.
- **payout يحل محل يد المحاسب لا حكمه**: الطلب يظل يمر بـ `approved`، و`paid` لا تُعلَّم إلا إن قال
  المزود «حوّلت» — وما دون ذلك يبقى `approved` بلا قيدٍ في الدفتر، ورصيدُ الكبتن ما زال محجوزاً
  بطلبه فلا يضيع ولا يُصرف مرتين. **وكليك وحدها**: لا عمود لحسابٍ بنكي في `drivers`، ووجهةٌ نخترعها
  حوالةٌ إلى لا مكان — فالحوالة البنكية تبقى يدوية.
- **زرّ «اختبار الاتصال» لا يترك أثراً**: لا طلبَ دفعٍ يُفتح ولا حوالةً تُرسل، ولا رسالةً مدفوعة إلا
  إن كتب المشرف رقماً صراحةً. ويعمل على عقدٍ **غير مفعّل** — يُختبر العقد قبل أن يُفتح لا بعد.
  وفشلُه جوابٌ يُقرأ (200 بـ `ok=false` ونصِّ السبب) لا شاشةُ خطأ. و`last_tested_at` يُكتب في
  الحالتين: السؤال «متى اختُبر» لا «متى نجح».

### ب) ميزات منتجية — خلف feature flags (معطلة افتراضياً، تفعيل من اللوحة)
- الرحلات المجدولة
- مشاركة الرحلة بين ركاب
- الكوبونات والعروض
- تسعير ديناميكي (surge)

### ج) خطوات تشغيلية لاحقة (ليست كوداً يُبنى الآن)
- تغليف Capacitor والنشر في المتاجر

---

## 16. خطة التنفيذ للوكيل (بالترتيب — مهمة واحدة في كل جلسة)

1. **الأساس:** docker-compose (postgres+postgis, redis) + هيكل FastAPI + Alembic + جداول users/drivers/vehicles + مصادقة (هاتف + كلمة مرور) / JWT — عبر auth service بواجهة موحدة تتحول لـ OTP عند تفعيل مزود SMS
2. **الإعدادات:** pricing_rules + feature_flags + commission_settings + subscription_plans + **جدول provider_credentials المشفر مع CRUD أساسي وseed لتوكنات Mapbox وTelr Sandbox** + CRUD إداري للبقية
3. **الرحلات:** rides + التسعير عبر Mapbox Directions + endpoints الطلب والحالات
4. **التوزيع والتتبع:** Redis GEO + خوارزمية الإسناد + WebSockets كاملة
5. **المحافظ:** ledger موحد + محفظة الراكب (شحن/تحويل) + محفظة الكبتن
6. **الدفع** — **مقسَّمة إلى جلستين على حدّ «المزوّد الخارجي»**: كل ما في 6-أ قابل للاختبار محلياً
   بالكامل، وتلر وحده يُدخل متغيرات خارجة عن السيطرة (بيانات Sandbox، توقيع webhooks، شبكة، تنسيق ردود
   المزود). خلطهما يعني أن تعثراً في إعدادات تلر يعطّل مراجعة منطقٍ مالي سليم. وبعد 6-أ يصير نظام الدفع
   عاملاً فعلاً وكافياً لتجريب ليبيا (كاش فقط)، فتصير تلر إضافةً على نظام مثبت لا شرطاً لتشغيله.

   - **6-أ (منجزة):** جداول `payments` (بحقول فصل النزاع و`idempotency_key` و`transaction_id`) و
     `ride_route_points` و`saved_cards` و`ratings` + عمود `rides.actual_distance_km` (ترحيلة `0007`)
     — كاش + كليك اليدوي + المحفظة + **الدفع المختلط** + **النزاعات وفصلها من اللوحة** + قيود
     `ride_earning`/`commission` + **تخزين نقاط مسار الرحلة** وإعادة حساب `final_fare` على المسافة
     الفعلية (القسم 5.7) + **`ratings`: تقييم متبادل بعد `completed` (endpoint لكل طرف) وتحديث
     `drivers.rating_avg`** — التقييم يلي الدفعَ مباشرة في تدفق القسم 5، فمكانه هنا لا ضمناً في مرحلة
     الواجهات
   - **6-ب (منجزة):** تكامل **Telr** — جدول `provider_orders` (ترحيلة `0008`) + إنشاء صفحة الدفع،
     الـ webhook والتحقق من توقيعه (القسم 14)، شحن المحفظة بالبطاقة شحناً فورياً آلياً (القسم 7)،
     ملء `saved_cards` بالـ tokenization والدفع بضغطة (القسم 6.4)، والاسترداد عبر المزود. القناة
     `card` كانت في ENUM وشاشة الدفع منذ 6-أ وترد بـ 501؛ صارت الآن تردّ 403 حين لا يكون عقد Telr
     مفعّلاً (قرارٌ إداري لا نقصُ تنفيذ) و503 حين يُرفع المفتاح بلا عقد.
     **بيانات Sandbox لم تكن متوفرة، فبُنيت المرحلة واختُبرت على المزود الوهمي** المُدار من عقد
     Telr نفسه (القسم 15/أ)؛ تفاصيل أسلاك Telr الثلاثة التي تحتاج مطابقةً بالتوثيق الرسمي مذكورة
     في القسم 6 وفي رأس `services/card_gateway/telr.py`
7. **الاشتراكات (منجزة):** الخطط + الشراء + فحص التوزيع + Celery للانتهاء — جدول
   `driver_subscriptions` وغرض `subscription` في `provider_orders` (ترحيلة `0009`)، الشراء بالقنوات
   الأربع (المحفظة والبطاقة من تطبيق الكبتن، والكاش وكليك تسجيلاً من اللوحة بعد القبض)، شرطُ
   الاشتراك الساري في `dispatch.eligible_driver_ids` وخريطة الراكب معاً، وخدمتا Celery
   (`worker`/`beat`) بمهمة كنسٍ كل خمس دقائق تعلّم المنتهي وتُخرج صاحبه من التوزيع وتنبّه من يقترب
   انتهاؤه. قرارات التنفيذ في القسم 8
8. **استكمال طبقة المزودين (منجزة):** صفحة العقود الكاملة (بطاقات + **اختبار اتصال** + تقنيع) +
   واجهات مزود موحدة مع mock providers (`services/sms`، `services/push`، `services/cliq`،
   `services/payout` — كلها على بنية `card_gateway` نفسها) + التكاملات الأربعة عاملةً وتنتظر بيانات
   العقود: **OTP** (تدفق التحدي والتحقق وحساباتٌ بلا كلمة مرور)، و**Push** (ترحيلة `0010`:
   `device_tokens` و`notification_campaigns` و`notification_deliveries` و`notification_settings`
   وعمود `users.marketing_push_enabled`؛ معاملاتيٌّ مع كل بثّ WebSocket، وحملاتٌ على دفعات بمهمة
   Celery)، و**CliQ الآلي** لشحن المحفظة (قيمة `cliq_acquirer` في `payment_provider` وعمود
   `provider_orders.qr_payload`)، و**payout** للسحوبات. قرارات التنفيذ في القسم 15/أ
   - **ما بقي للمرحلة 11:** واجهةُ صفحة الحملات في اللوحة (المسارات وسجلُّ التسليم قائمان)
9. **تطبيق الراكب (منجزة):** `customer-app/` — React 18 + Vite + TypeScript + Tailwind بـ RTL
   كامل، shadcn/Drawer (`vaul`) للأوراق السفلية، Framer Motion، Lucide، خط IBM Plex Sans Arabic،
   وMapbox GL JS بستايلٍ نهاري وليلي. الشاشات الثماني في القسم 11 كلها على المسارات القائمة، ومنها
   **صفحة دفع كليك داخل التطبيق** (الرمز والرابط وإدخال المرجع — القسم 6). وأضافت المرحلة إلى
   الخلفية ما كانت تلك الصفحة تنتظره: ترحيلة `0013` بحقول كليك الأربعة على `payments`، و
   `services/cliq_qr.py`، و`POST /payments/{id}/cliq-reference` بإشعارٍ فوري للكبتن. قرارات
   التنفيذ في القسم 6 وأدناه:
   - **الحقول العامة لتطبيق الويب لدى Firebase أُضيفت إلى عقدَي `firebase_auth` و`fcm`**
     (`api_key`, `auth_domain`, `app_id`, `sender_id`, `vapid_key`) وتُنشر في `GET /config`. حزمة
     Firebase على الجهاز لا تعمل بغيرها — لا تدفّقَ تحققٍ ولا رمزَ جهاز — وهي عامّةٌ بطبيعتها
     تُنشر في حزمة كل تطبيق ويب، لكنها **قيمُ مشروعٍ بعينه** فمكانها العقد لا ملفٌّ في الواجهة
     (القسم 14). وتكرارُها في العقدين ثمنُ استقلالهما الذي يقرره القسم 15/أ: مُحقِّقٌ عبر مزود SMS
     مع إشعاراتٍ عبر FCM حالةٌ يكون فيها عقد الدخول مطفأً تماماً والتطبيق ما زال يحتاج الإعداد.
   - **«مشاركة الرحلة (رابط)» تُنفَّذ بورقة مشاركة النظام لا برابط تتبعٍ عام**: الرابط العام يحتاج
     رمزاً على `rides` يفتحها لمن لا حساب له، وهو عمودٌ وعقدُ صلاحياتٍ لم يصفهما هذا المستند —
     وكل مسارٍ يلمس رحلةً يتحقق من ملكيتها (القسم 14). فالمشاركة تحمل ما يطمئن المنتظِر: اسم
     الكبتن ولوحته والوجهة على الخريطة.
   - **الخط بين نقطتي الرحلة مستقيمٌ متقطّع لا مسار طريق**: هندسة المسار تأتي من Mapbox Directions
     ونداؤها من الخلفية حصراً (القسم 2/14)، وما يصل الواجهة مسافةٌ ومدةٌ وسعر. وخطٌّ نرسمه من
     عندنا يوهم بمسارٍ لم يقله أحد.
   - **عامل الخدمة لا يلمس `/api/` إطلاقاً**: رصيدٌ أو حالةُ رحلةٍ من كاشٍ عمرُه دقيقة كذبةٌ على
     شاشةٍ مالية. الكاش لقشرة التطبيق وحدها.

   **9-ب (منجزة) — خلفيةٌ بلا واجهات، شُقّت عن المرحلة 10 لأنها شرطُها لا جزؤها.** كشفت مقارنةُ
   تصاميم الحزمة بالمشروع أن شاشتين في تطبيق الكبتن (والراكب) تصفان ميزتين غير موجودتين أصلاً:

   - **رفع المستندات ومراجعتها.** الجدول `driver_documents` قائمٌ منذ المرحلة 1 بكل حقوله ولا مسارَ
     واحد يكتبه أو يقرؤه، ولا تخزينَ ملفاتٍ في المشروع إطلاقاً — فالبند 12/1 والبند 13/2 كانا نصّاً
     بلا تنفيذ، والاعتماد يمر والمستنداتُ فارغة. أضافت المرحلة: `core/storage.py` (تحقّقٌ من النوع
     بالتوقيع الثنائي، وسقفُ حجمٍ يُفرض بالقراءة لا بترويسة العميل، واسمُ ملفٍ من عندنا)، و
     `services/documents.py`، ومساراتِ الرفع والقراءة للكبتن والمراجعةِ للوحة، وحارسَ الاعتماد
     الثاني.
   - **صندوق وارد الإشعارات** `user_notifications` — أيقونةُ الجرس في التطبيقين كانت تصف سجلاً لا
     وجود له.

   ولم يُبنَ `ride_offers` (سجلُّ العروض الذي يجعل «معدّل قبول الطلبات» و«زمن القبول» قابلَين
   للحساب): كتابةٌ على المسار الحرج للتوزيع ومهلتُه عشرون ثانية. الحلُّ حين يُطلب المؤشّران كتابةٌ
   غير متزامنة عبر Celery خارج مسار العرض.
10. **تطبيق الكبتن** — ومنه **بطاقة تأكيد حوالة كليك** («وصلني» / «لم يصلني») بمهلة تأكيد (القسم 6)
10-ج. **خدمة التوصيل النسائي** — خلفيةً وتطبيقين. أُنجزت **الخلفية**: حقول القسم 4 أعلاه،
    والمطابقة ثنائية الاتجاه داخل شرط الأهلية (القسم 5.3)، والمدى الأوسع للطلب المجنَّس،
    والإلغاء بلا رسوم بسبب `gender_mismatch` مع عدّاد البلاغات، و`PUT /admin/drivers/{id}/gender`
    بقيدِ تدقيق، ونشرُ المفتاح في `/config`. وأُنجزت **السِمة الوردية**: رمزٌ واحد `--brand`
    في `DESIGN.md` §1.1-ب وفي ملفات `tailwind.config.js` الثلاثة، ومفتاحٌ في إعدادات التطبيقين
    مفتوحٌ افتراضاً لمن جنسُها أنثى — ولا يظهر أصلاً حيث الخدمة مطفأة. وأُنجزت **شاشاتُ
    التطبيقين**: اختيارُ التفضيل في شاشة الطلب وفي الملف عند الراكبة مع قولِ ثمنِ الانتظار قبل
    الضغط لا بعده، وتفضيلُ الكبتن الدائم وشارةُ «طلب نسائي» على بطاقة العرض، واقتراحُ مشاركة
    الرحلة عند بدء الرحلة النسائية. ثم وصل **تصميمُ الشاشات النسائية** بعد البناء، فطوبقت عليه:
    رموزُ السِمة صارت أربعةً (`--brand-soft` و`--brand-brd` معهما) والوردي النهاري `#d81b73`،
    وأُضيف **إعلانُ الجنس** في التسجيل والملف — وبغيره كانت الخدمة كلُّها غيرَ قابلةٍ للوصول —
    و**ورقتا أسباب الإلغاء** في التطبيقين، وبغيرهما لا مسارَ لـ`gender_mismatch` أصلاً،
    و«أقبل أي كبتن» بعد `no_driver_found`، وشارةُ الرحلة النسائية عند الراكبة، وشريطُ التفضيل
    في رئيسية الكبتن، وبطاقةُ توثيق الجنس في حسابه. **وما بقي**: مراجعةُ أجناس الكباتن
    المعتمدين المتراكمين، وهي شرطُ إشعال المفتاح — وثلاثةُ بنودٍ في `FUTURE-FEATURES`
11. **لوحة الإدارة** — ✅ **مُنجزة**. `admin-panel/` على 5175 بنفس نظام التصميم وحارسَيه:
    الهيكلُ (رأسٌ من صفّين ومبدّلُ دولةٍ وحارسُ جلسة) والدخولُ **برقم الهاتف**، و**السائقون
    والوثائق** (القسم 13/2)، و**المالية**: طلبات السحب وشحنات كليك (13/5)، و**النزاعات** (13/4)،
    و**حملات الإشعارات** (13/9)، و**الإعدادات per-country** (13/6)، و**عقود مزوّدي API** (13/7).
    وأتمّت **الجلسة الأخيرة** الأقسامَ السبعة الباقية من القسم 13: **سجل الرحلات** (13/4)
    و**الركّاب** (13/3) و**الاشتراكات والباقات** (8 و13/5) و**التسعيرة** (13/6) و**التقارير
    والإحصاءات** (13/5) و**المستخدمون والصلاحيات** (13/8) و**سجل التدقيق** (القسم 14) — فلم يبق
    في القائمة عنصرٌ بلا شاشة. وآليةُ «قريباً» باقيةٌ في `Shell.tsx` لأقسام المرحلة 12.
    - و**«نظرة عامة»** بُنيت فوق `GET /admin/stats/overview` (بنافذةٍ من ثلاث ودولةٍ إلزامية):
      **التجميع في الخلفية حصراً** كما يحصر القسم 14 الحساب المالي فيها — لوحةٌ تجمع صفوفاً بيدها
      تعرض رقماً لا يطابق القاعدة حين تُقصّ الصفحة. و**اليومُ يومُ الدولة** بمِنطقتها المخزَّنة في
      `notification_settings.timezone`، فلا يقصّ خادمُ UTC ثلاث ساعاتٍ من أول اليوم في عمّان.
      و**المتصلون الآن من مفتاح الحضور في Redis لا من `drivers.is_online`**: العمود يقول «رفع
      المفتاح» لا «حاضرٌ الآن»
    - و**«الخريطة الحيّة»** فوق `GET /admin/live/map` — **لـ admin حصراً** وفتحُها يدخل سجل
      التدقيق (البند 1 أعلاه). الموقعُ من Redis والهويةُ من القاعدة، ولا فلترةَ أهليةٍ فيها
      خلافاً لخريطة الراكب: المشرف يريد **من في الشارع** بمن فيهم من في رحلةٍ أو انقضى اشتراكه.
      ومسارُ الراكب `GET /drivers/nearby` وإطارُ `nearby_drivers` في المقبس لم يُمسّا، ويحرس ذلك
      اختبارٌ يرى الكبتن نفسَه باسمه في اللوحة وبمعرّفٍ مستعارٍ عند الراكب
    - وأضافت جلسةُ السائقين إلى الخلفية ما كانت صفحتُهم تنتظره: `GET /admin/drivers` (قائمةٌ
      بحالها وعدِّ مستنداتها **في استعلامٍ واحد** — صفحةٌ من خمسين كبتناً لا تصير خمسين نداءً)،
      و`POST /admin/drivers/{id}/suspend` بسببٍ إلزامي، و`/activate` الذي **يمر بحارسَي الاعتماد
      نفسِهما**: لولا ذلك لصار الإيقافُ ثم التفعيل طريقاً للالتفاف على شرط المستندات
    - و**زرُّ توثيق الجنس صار له بابٌ في صفحة السائقين** (المرحلة 10-ج كانت قد بنت مساره ولم
      تبنِ له زرّاً — **قاعدةٌ بلا باب**، وهي أخطرُ من قاعدةٍ ناقصة لأنها تُقرأ منجزةً في
      الاختبارات): عمودُ الجنس في القائمة، ومفتاحُ فرزٍ **مستقلٌّ عن حبّات الحالة** يفتح
      `?gender_verified=false`، وبطاقةٌ في الدرج تكتب `PUT /admin/drivers/{id}/gender`. وهي
      أصغرُ قطعةٍ في هذه المرحلة وأكبرُها أثراً: بها **يُفرَّغ متراكمُ المعتمدين** الذي يبقى
      `women_service_enabled` مطفأً حتى يُفرَّغ. وغيرُ المختوم يُعرض «لم يُثبَّت» لا بجنسٍ
      مكتوب — المطابقةُ تقرأ المختوم وحده، وعرضُ قيمةٍ بلا ختمٍ يجعلها تبدو معتبَرة.
      و**المفتاحُ نفسه صار في شاشة الإعدادات** ولم يكن: `FeatureKey` في اللوحة كان ينقصه
      `women_service_enabled`، فلم يكن للمالك بابٌ يشعل به الخدمة بعد تفريغ المتراكم — نفس
      شكلِ العطب الذي كشفته المرحلة 10-ج، **قاعدةٌ بلا باب**
    - وأضافت الجلسةُ الأخيرة إلى الخلفية ما كانت الشاشاتُ السبع تنتظره:
      - **`GET /admin/rides` و`/admin/rides/{id}`** (`services/ride_log.py`) — قراءةٌ لا غير:
        لا مسارَ فيها يغيّر حالة رحلة، فكلُّ انتقالٍ يمر بـ`services/rides.py` وحده. وحالُ
        الدفع **مجموعةٌ في القاعدة** لا في اللوحة، و**في استعلامٍ ثانٍ لا في ضمٍّ**: رحلةٌ
        بدفعتين تصير صفّين في الضمّ فتُعدّ مرتين في صفحةٍ مسقوفة. والتفاصيلُ وحدها تحمل
        **نقاط المسار** — دليلَ النزاع (القسم 5.7) — مسقوفةً بألفٍ ومع `route_truncated`
        صريحة: مسارٌ قُصّ ذيلُه يُقرأ كاملاً دليلٌ يكذب
      - **`POST /admin/users/{id}/block|unblock`** بسببٍ إلزاميٍّ للحظر يدخل التدقيق (13/3).
        **`admin` لا `support`**، و**لا إبطالَ جلسات**: `core/deps` يقرأ العمود في كل طلب
        فيسري الحظرُ على التوكن القائم — وإبطالٌ هنا كان سيوهم أن الحماية منه. **وحسابُ
        الموظفين خارج هذا الباب**: القسم 13/3 عن الركّاب، ولا يُغلق به مشرفٌ على زميله
      - **`GET /admin/stats/reports`** — نِسَبٌ ومتوسطاتٌ **محسوبةٌ في الخدمة**: بسطُ كلٍّ منها
        ومقامُه مجموعان على الجدول كلِّه، وقسمةُ رقمين مسقوفَين في الواجهة تعطي متوسطَ الصفحة.
        وفيه تقاريرُ الاشتراكات (13/5) بما **بيع في الفترة** لا بما هو سارٍ الآن
      - وفلاترُ **سجل التدقيق** (`action`/`actor_id`/`entity_id`/`offset`) و**اسمُ الفاعل
        مضموماً** في نفس الاستعلام، وفلاترُ بحثٍ (`q`) على الحسابات والسائقين
    - **وثلاثةُ أشياء يرسمها التصميم ولم تُبنَ عمداً**، وكلُّها تُبنى يومَ يصفها هذا المستند:
      - **مصفوفةُ الصلاحيات مقروءةٌ لا تُنقر**، وأدوارُها **دوران لا أربعة**: `UserRole` فيه
        `admin` و`support` وحدهما ويفرضهما `core/deps` على كل مسار. وخليةٌ تُنقر كانت ستصير
        مصدرَ حقيقةٍ ثانياً يخالف الحارس — ومشرفٌ يظن أن ما أطفأه مُطفأ أخطرُ ممن يعرف أنه
        لا يستطيع
      - **لا «دعوة مستخدم»**: الحساب يُنشأ بإثبات ملكية رقمٍ من التطبيق (القسم 15/أ)، ولا مسارَ
        في المشروع يفتح حساباً بغيره
      - **ولا «أعلى المناطق طلباً» ولا «معدّل قبول الطلبات»** في التقارير: لا جدولَ مناطق في
        هذا المخطط ولا عمودَ منطقةٍ على الرحلة، ولا `ride_offers` يسجّل العروض (16/9-ب) —
        ورسمُ أيٍّ منهما من بياناتٍ أخرى قياسُ شيءٍ وتسميتُه باسم آخر
12-أ. **ترحيل تطبيق الراكب إلى نظام التصميم** — ✅ **مُنجزة**. مرحلةٌ مستقلة، وسببُ استقلالها أنها لا تخصّ
    ميزةً بعينها. `customer-app` بُني في المرحلة 9 **قبل** أن يوجد `design/DESIGN.md`، فلوحته
    ألوانٌ أخرى (`--brand` أصفر، ثلاثيّاتُ rgb) وسلالمُه سلالمُ Tailwind الافتراضية
    (`text-sm`، `rounded-xl`، `p-4`) — بينما تطبيق الكبتن واللوحة على النظام بمفاتيحه
    البكسلية وحارسَيه (`check:scale`، `check:enums`).
    - **وما كشفه**: تصميمُ الخدمة النسائية يرسم شاشات **الراكبة** بلوحة `DESIGN.md`
      (`--sur`/`--brd`/`--tx`)، فمطابقتُه بالبكسل تعني ترحيل التطبيق كلَّه — عملٌ أكبر من
      الميزة التي كشفته، ولا علاقة له بها.
    - **ولماذا مرحلةٌ لا بندُ مستقبليات**: البنود المؤجَّلة أشياءُ لم تُبنَ؛ وهذا شيءٌ **مبنيٌّ
      يعمل** بنظامٍ آخر. تركُه يعني نظامَي تصميمٍ في منتجٍ واحد إلى الأبد، وكلُّ شاشةٍ جديدة
      في تطبيق الراكب تُضاعف كلفةَ الترحيل.
    - **نطاقُها**: استبدال `tailwind.config.js` و`index.css` بنسخة §6/§1.1، ونقلُ كل صنفٍ
      من السلّم الافتراضي إلى المفاتيح البكسلية، وإضافةُ `check:scale` و`check:enums`
      و`cn` بسلّم المقاسات (المصيدة الموصوفة في `CLAUDE.md`)، ومراجعةٌ بصرية لكل شاشة.
      **ولا تغييرَ سلوكيّ فيها إطلاقاً** — من غيّر منطقاً أثناء ترحيلٍ بصريّ لا يعرف أيُّهما
      كسر الشاشة.
    - **وما نتج عنها**: الأصفرُ اختفى من تطبيق الراكب — §1.1 بلا لون علامةٍ تجارية، و`--brand`
      افتراضُه `--tx`. فالتطبيق فحميٌّ/أبيض كأخويه، ويتلوّن بالوردي وحده حين تُشعَل السِمة.
      وهذا **ما يعنيه الترحيل** لا انحرافٌ فيه. ومعه: الشفافياتُ على الألوان (`bg-brand/10`)
      لم تعد تعمل — اللوحةُ hex في متغيّر — فحلَّ محلَّها `--brand-soft` و`--sur2`.

12-ب. **تعدد الوجهات (multi-stop)** — مرحلةٌ كاملة خلف `multi_stop_enabled`. حتى **ثلاث وجهات**
    في الرحلة الواحدة: وسيطتان في `ride_stops` والأخيرة تبقى `rides.dropoff_point`. والتسعيرُ
    من مسارٍ متعدد المحطات عبر Mapbox Directions، ورسمُ انتظارٍ **ثلاثيُّ الحقول** يديره
    المشرف per-country (مقطوعٌ لكل محطة + مهلةٌ مجانية + سعرُ الدقيقة) ورابعٌ سقفاً، والأربعةُ
    **تُجمَّد على الرحلة** كالعمولة. وآلةُ الحالات (`in_progress ⇄ at_stop`، ومخرجٌ إلى
    `completed` عند السقف) داخل `services/rides.py` وحدها ومحروسةٌ بـ`ALLOWED_TRANSITIONS`.
    التفصيل في القسم 5.10، والجداول في القسم 4.
    - **الترتيب**: الخلفية أولاً (ترحيلة، تسعير، آلة حالات، **اختبار تزامنٍ يفشل بحذف القفل**)،
      ثم التطبيقان: إضافةُ المحطات وترتيبُها وحذفُها عند الطلب، وشريطُ تقدّمِ المحطات عند الكبتن
    - **والزمنُ يُختم في الخلفية** والمبلغُ يُحسب فيها: الواجهةُ تعرض عدّاداً وتتلقّى رقماً

12-ج. **حزمُ `FUTURE-FEATURES` بعد 12-ب** — ✅ **مُنجزة**: الأماكن المحفوظة والوجهاتُ الأخيرة
    و«أعد الطلب» (البنود 1–3)، وملخّصُ أرباح الكبتن (17)، وشارةُ النزاع في السجلّين (19)،
    وتعديلُ المركبة وaliasُ كليك (43/18). لا جديدَ في المواصفة عنها غيرَ ما كُتب في موضعه.

12-د. **التحقق الثنائي لدخول اللوحة (TOTP)** — القسم 14.1، وجداولُه الثلاثة في القسم 4. أوّلُ
    الحزمة المتفَق عليها (12-د ← البقشيش ← الكوبونات ← إحالةُ السائقات)، **وأوّلُها لأنه الوحيد
    الذي لا يلمس `pricing` ولا `payments` ولا الدفتر**: أبوابُ المالِ التي تفتحها الثلاثةُ بعده
    (سقفُ ميزانية حملة، مبلغُ حافزِ إحالة) كلُّها حقولٌ يكتبها من دخل اللوحة، فالقفلُ يُسبق ولا
    يُلحق.
    - **الترتيب**: الخلفيةُ أولاً — ترحيلةٌ (ولا نوعَ ENUM جديد فيها، فمصائدُ القسم 4 لا تنطبق؛
      وحدُها `security_settings` تحتاج قيدَ صفٍّ واحد)، ثم `services/totp.py` ومساراتُه وحارسُ
      `core/deps`، **واختبارُ تزامنٍ على استهلاك رمز الاسترداد** يفشل بحذف قفل الصف (رمزٌ «لمرةٍ
      واحدة» يُقبل مرتين) — ثم شاشتا اللوحة: شاشةُ الرمز بعد كلمة المرور، وقسمُ «الأمان».
    - **ومخرجُ الطوارئ يُبنى قبل الإلزام لا بعده**: أمرٌ في `scripts/` يُسقط عاملَ حسابٍ بعينه من
      خارج اللوحة (بيدِ من يملك القاعدة) ويكتب قيدَ تدقيق. بلا هذا المخرج يكون العطلُ الوحيدُ غيرُ
      القابل للإصلاح في المنصة كلها: لوحةٌ لا يدخلها أحدٌ ليصلح ما يمنع الدخول.
    - **ومفتاحُ الإلزام يبقى مطفأً** حتى يسجّل المالكُ عاملَه ويُثبت استردادَه — قرارُ إشعالٍ
      بيده، كما `women_service_enabled`.

12-هـ. **واتساب مُحقِّقاً ثالثاً للرقم** — WhatsApp Cloud API الرسمي من ميتا، بقالب مصادقة.
    التفصيل في القسم 15/أ. عقدٌ عامٌّ في `provider_credentials` (Phone Number ID، Access Token،
    اسم القالب ولغتُه، WABA ID) بمزودٍ وهميٍّ كبقية المزودين، ومفتاحٌ **per-country مطفأٌ
    افتراضاً** يُشعله المشرف من شاشة الإعدادات، ونقطةُ قرارٍ واحدة في
    `services/verification.py` صارت **متعددةَ القنوات** بترتيب `whatsapp ← sms ← firebase`.
    - **الترتيب**: الخلفيةُ أولاً (ترحيلةُ قيمةِ تعدادٍ واحدة، حزمةُ `services/whatsapp/`
      بالبنية الثلاثية، `OtpSender` في `services/otp.py`، والارتدادُ الحامل لمخرجه)، ثم مفتاحُ
      اللوحة، ثم القناةُ والمخرجُ في التطبيقين.
    - **ولا مكتبةَ واتساب غير رسمية** — القاعدة في القسم 15/أ، وعقوبةُ خرقها حظرُ الرقم.

12. **ميزات Phase 2 المنتجية** خلف feature flags: الرحلات المجدولة، مشاركة الرحلة، الكوبونات، surge
13. **اختبارات + تشغيل تجريبي كامل** لسيناريو: تسجيل كبتن → موافقة → اشتراك → طلب رحلة → تتبع → دفع → سحب

> اختبر كل مرحلة قبل الانتقال للتالية. اكتب tests للمنطق المالي (المحفظة، التسعير، العمولة) بالذات.
