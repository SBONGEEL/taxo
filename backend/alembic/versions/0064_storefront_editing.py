"""تحريرُ المتجر من اللوحة — **ما عُرض يُخفى، والمسوّدةُ تُحذف** (قرارُ المالك 2026-08-31).

## عمودان لا أكثر، ولكلٍّ سؤالٌ لا يجيبه غيرُه

**`first_shown_at`** — **لحظةُ أوّلِ ظهورٍ لأحد**. وهو ما يفرّق **المسوّدةَ**
التي لم يرَها إنسانٌ قطُّ من **الصفِّ الذي عُرض مرّة**.

**والحذفُ للمسوّدة وحدَها** (قرارُ المالك): **ما عُرض مرّةً لا يُحذف بل يُخفى**
— فحذفُه **يمحو شاهداً على ما رآه الناس**، ومن يقرأ بعد شهرٍ «لمَ ارتفعت
الضغطاتُ ذلك الأسبوع» يجد فراغاً. **ولافتةٌ أُخفيت خيرٌ من صفٍّ ذهب.**

> **ولمَ عمودٌ لا استنتاج**: «أعُرضت؟» لا يُشتقّ من الحال الراهنة — بلاطةٌ
> فعّالةٌ اليومَ أُخفيت غداً تعود `hidden`، **فتبدو مسوّدةً ولم تكن**. والحالُ
> تقول أين هي الآن، **والعمودُ يقول ما مضى** — وهما سؤالان لا يجيبهما حقلٌ
> واحد.
>
> **ولا يُملأ عند الإنشاء**: يُختم حين تصير البلاطةُ `active` أوّلَ مرّة، أو
> حين تُشعَل اللافتةُ داخل نافذتها. **وفارغٌ يعني «لم يرَها أحدٌ بعد»** —
> وذلك هو الإذنُ بالحذف.

## و`image_path` تعود — **بالرافع والخادم معاً هذه المرّة**

نُزعت في 2026-08-30 **وكان النزعُ صواباً في حينه**: عمودٌ لا يكتبه أحدٌ ولا
يخدمه باب، **والحمولةُ تنشر له عنواناً لمسارٍ لا وجودَ له** — فيرسم التطبيقُ
صورةً مكسورة، **وهي أسوأُ من لا صورة**.

**وتعود الآن لأن الأربعةَ تُبنى معاً** (قرارُ المالك): العمودُ · ورفعٌ على
`core/storage.py::save` كسابقة `upload_cliq_qr` · وبابٌ يخدمها · وعرضٌ في
البطاقة. **وواحدٌ منها ناقصاً يعيد العطبَ نفسَه.**
"""

from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "0064"
down_revision = "0063"
branch_labels = None
depends_on = None


def upgrade() -> None:
    # ── أوّلُ ظهورٍ لأحد — الفرقُ بين المسوّدة وما عُرض
    for table in ("service_tiles", "promo_banners"):
        op.add_column(
            table,
            sa.Column(
                "first_shown_at",
                sa.DateTime(timezone=True),
                nullable=True,
            ),
        )

    # ── صورةُ اللافتة — **مسارُ قرصٍ يخدمه باب**، لا عنوانٌ يُنشر بلا خادم
    op.add_column(
        "promo_banners",
        sa.Column("image_path", sa.String(length=512), nullable=True),
    )


def downgrade() -> None:
    op.drop_column("promo_banners", "image_path")
    for table in ("promo_banners", "service_tiles"):
        op.drop_column(table, "first_shown_at")
