"""سطرُ البطل — **وعدٌ مطلقٌ في صفٍّ قائم** (البند ٥٥، ٢٠٢٦-٠٩-٠٦).

**تصحيحُ `DEFAULTS` لا يبلغ صفّاً موجوداً**، والتعليقُ فوقها يقولها بحرفه:
«تُكتب مرّةً عند أوّل قراءة، **ولا تُعاد كتابتُها بعدها**». **فالمصدرُ صار
صحيحاً والصفحةُ بقيت تقول «بلا عمولة على الكبتن المشترِك»** — وهو الوعدُ الذي
أمر المالكُ بنزعه، **ويكذب لحظةَ إشعال العمولة**.

**وهو الشكلُ العاشر بعينه**: خُضرةُ المصدر لا تقول شيئاً عمّا يُعرض.
**وحارسُ `check:commission-text` يمسح المصدرَ لا صفوفَ القاعدة** — ولا يقدر،
فالبابُ المكتوبُ لسدِّ فجوته هو هذه الترحيلة.

**ولا يُكتب إلا فوق النصِّ القديم بحرفه**: أيُّ تحريرٍ من اللوحة **يبقى**
— فالمالكُ يملك هذا النصَّ، **وترحيلةٌ تكتب فوق تحريره تسرق قراره**.

**والرجوعُ يعيد الوعدَ المطلق** — بقصدٍ مكتوب: `downgrade` يعيد الحالَ لا
الصوابَ، **وإلا فالنزولُ ثمّ الصعودُ لا يعودان كما بدآ** ويسقط
`test_migrations.py`.

Revision ID: 0074
Revises: 0073
"""

from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "0074"
down_revision = "0073"
branch_labels = None
depends_on = None

OLD = (
    "اطلب رحلتك أو اقبل الطلبات من التطبيق نفسه — بلا عمولة على الكبتن المشترِك."
)
NEW = (
    "اطلب رحلتك أو اقبل الطلبات من التطبيق نفسه — وعمولةٌ منخفضةٌ تُجمَّد لحظةَ اشتراكك."
)


def _swap(old: str, new: str) -> None:
    op.execute(
        sa.text(
            "UPDATE site_settings SET hero_subtitle = :new WHERE hero_subtitle = :old"
        ).bindparams(new=new, old=old)
    )


def upgrade() -> None:
    _swap(OLD, NEW)


def downgrade() -> None:
    _swap(NEW, OLD)
