[pytest]
testpaths = tests
asyncio_mode = auto
; حلقة أحداث واحدة للجلسة كلها: محرك SQLAlchemy وRedis يُنشآن مرة واحدة
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
filterwarnings =
    ignore::DeprecationWarning
