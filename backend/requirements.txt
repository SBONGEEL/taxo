# TAXO backend
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy[asyncio]>=2.0.30,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
# أعمدة geography(POINT) للرحلات + تعريف نوع postgis لانعكاس alembic
GeoAlchemy2>=0.15,<0.20
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
PyJWT>=2.8,<3.0
bcrypt>=4.1,<5.0
# تشفير provider_credentials at rest (Fernet)
cryptography>=42.0,<47.0
redis>=5.0,<7.0
python-json-logger>=2.0,<4.0
# استدعاء Mapbox Directions من الخلفية (وعميل الاختبارات كذلك)
httpx>=0.27,<1.0

# اختبارات
pytest>=8.2,<9.0
pytest-asyncio>=0.23,<2.0
# عميل WebSocket للاختبارات — يشغّل التطبيق في نفس حلقة الأحداث، بخلاف
# TestClient الذي يفتح حلقة في خيط آخر فتنكسر عليه اتصالات asyncpg
httpx-ws>=0.6,<1.0
