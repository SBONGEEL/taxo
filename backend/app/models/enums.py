from __future__ import annotations

from enum import StrEnum


class CountryCode(StrEnum):
    LY = "LY"
    JO = "JO"


class Currency(StrEnum):
    LYD = "LYD"
    JOD = "JOD"


class AccountKind(StrEnum):
    """نوعُ الحساب — **نصفُ مفتاحه** مع الرقم (`SPEC-DELIVERY.md` §D1.4، 1-أ).

    الراكبُ والكبتنُ حسابٌ واحدٌ عبر تطبيقين (`taxo`)، والزبونُ حسابٌ منفصلٌ
    تماماً (`market`)، والتاجرُ ثالث (`merchant`، Q40). **والفصلُ بالنوع لا
    بالتطبيق**: مفتاحٌ بالتطبيق كان سيشقّ الراكبَ عن كبتنه.
    """

    TAXO = "taxo"
    MARKET = "market"
    MERCHANT = "merchant"


class UserRole(StrEnum):
    RIDER = "rider"
    DRIVER = "driver"
    ADMIN = "admin"
    SUPPORT = "support"


class ClientApp(StrEnum):
    """التطبيقُ الطالبُ للجلسة — ثلاثةٌ لا أكثر، وهي التي نبنيها.

    **وبيتُه هنا منذ البند ٨** (2026-09-03، §43): كان في `core/app_scope.py`
    وحدَه — **وهو صحيحٌ ما دام يُقرأ في الحراسة وحدَها**. ولمّا صار **عموداً
    في `app_releases`** لزم أن يسكن حيث تسكن التعدادات: **نموذجٌ يستورد من
    `core/` يقلب الطبقات** (`core` يستورد من `models` أصلاً)، **ونسخةٌ ثانيةٌ
    بالأعضاء نفسِها هي «شكلان لشيءٍ واحد»** بحرفها.

    **و`core.app_scope` يعيد تصديرَه**، فكلُّ `from app.core.app_scope import
    ClientApp` قائمٌ كما كان — **نقلُ بيتٍ لا تغييرُ اسم**.
    """

    RIDER = "rider"
    DRIVER = "driver"
    PANEL = "panel"


class DriverStatus(StrEnum):
    PENDING = "pending"
    APPROVED = "approved"
    REJECTED = "rejected"
    SUSPENDED = "suspended"
    # **ألغى تفعيلَ حسابه بطلبه** (البند ١٣) — لا عقوبةً كـ`suspended` بل
    # خروجاً باختياره. وهي حالةٌ مستقلة لأن السجلَّ يُقرأ بعد شهر: «لماذا لا
    # تصله طلبات» جوابُها «أوقفناه» غير جوابها «طلب الإغلاق».
    # **والعودةُ من باب `approve` وحدَه** بشروطه — لا بضغطةٍ تعكس الحالة
    DEACTIVATED = "deactivated"


class VehicleCategory(StrEnum):
    ECONOMY = "economy"
    COMFORT = "comfort"


class DocumentType(StrEnum):
    """مستنداتُ الكبتن — **أنواعٌ مسمّاةٌ لا صورٌ مرقَّمة** (البند ١١).

    قرارُ المالك (2026-08-14): «الصورة الجانبية مرفوضة» جملةٌ مفهومة، و«الصورة
    الثالثة مرفوضة» ليست كذلك. فكلُّ صورةٍ نوعٌ باسمه، ويبقى القيدُ الفريد
    `(driver_id, doc_type)` على حاله — صفٌّ واحدٌ لكل نوع، ورفعُه ثانيةً استبدال.
    """

    DRIVING_LICENSE = "driving_license"
    NATIONAL_ID = "national_id"
    VEHICLE_REGISTRATION = "vehicle_registration"
    # **مهجورٌ ولا يُعرض** (البند ١١): كان صورةَ المركبة الواحدة قبل أن تصير ستّاً
    # مسمّاة. ويبقى في التعداد لأن Postgres لا يحذف قيمةً منه، ولأن صفوفاً قديمةً
    # تحمله — وحذفُه من الكود يجعل صفّاً في القاعدة بلا اسمٍ يقرؤه أحد
    VEHICLE_PHOTO = "vehicle_photo"
    # صورُ المركبة الستّ — ثلاثٌ منها شرطُ اعتماد (`REQUIRED_DOCUMENT_TYPES`)
    VEHICLE_FRONT = "vehicle_front"
    VEHICLE_BACK = "vehicle_back"
    VEHICLE_SIDE_RIGHT = "vehicle_side_right"
    VEHICLE_SIDE_LEFT = "vehicle_side_left"
    VEHICLE_INTERIOR = "vehicle_interior"
    VEHICLE_PLATE = "vehicle_plate"
    # **الصورةُ الشخصية** (البند ٥٢، SPEC `driver_documents`) — وهي وحدَها من
    # هذه الأنواع **مستندٌ يُراجَع وصورةٌ تُنشر معاً**: المراجعةُ تمنع صورةَ
    # شخصٍ آخر أو صورةً مسيئة، والنشرُ يجعل الراكبَ يعرف من سيركب معه.
    # وشرطُها للاعتماد **يُقرأ لكل كبتنٍ على حدة** لا من جدولٍ ثابت — انظر
    # `models/driver.py::required_document_types`
    PROFILE_PHOTO = "profile_photo"


class DeactivationStatus(StrEnum):
    """حالُ طلب إلغاء التفعيل (البند ١٣) — أربعٌ لا أكثر.

    `cancelled` للكبتن يعدل عن طلبه، و`rejected` للمشرف يرفضه بسببٍ مكتوب.
    وخلطُهما يجعل «من أنهى الطلب» سؤالاً بلا جواب في سجلٍّ يُقرأ بعد شهر.
    """

    PENDING = "pending"
    APPROVED = "approved"
    REJECTED = "rejected"
    CANCELLED = "cancelled"


class DocumentReviewStatus(StrEnum):
    PENDING = "pending"
    APPROVED = "approved"
    REJECTED = "rejected"


class RideStatus(StrEnum):
    """دورة حياة الرحلة (SPEC القسم 4/5).

    `searching` تدخلها خوارزمية التوزيع في المرحلة 4، و`no_driver_found`
    مخرجها حين تنفد المحاولات.
    """

    REQUESTED = "requested"
    SEARCHING = "searching"
    ACCEPTED = "accepted"
    ARRIVED = "arrived"
    IN_PROGRESS = "in_progress"
    # وقوفٌ عند محطةٍ وسيطة (المرحلة 12-ب). حالةٌ **داخل** الرحلة لا فاصلٌ
    # بينها وبين غيرها: الراكب في السيارة، فلا إلغاءَ منها كما لا إلغاء من
    # `in_progress`. ومخرجاها `in_progress` باستئنافٍ، أو `completed` حين
    # يُنهي الكبتن عند المحطة بعد تجاوز سقف الانتظار (SPEC القسم 5.10)
    AT_STOP = "at_stop"
    COMPLETED = "completed"
    CANCELLED_BY_RIDER = "cancelled_by_rider"
    CANCELLED_BY_DRIVER = "cancelled_by_driver"
    NO_DRIVER_FOUND = "no_driver_found"


class PaymentMethod(StrEnum):
    """قنوات دفع الرحلة (SPEC القسم 4/6).

    `card` هنا منذ الآن وإن كان مساره معطّلاً حتى المرحلة 6-ب: إضافة قيمة إلى
    ENUM في postgres لاحقاً ترحيلةٌ وتعديلُ قيود، وشاشة الدفع تعرض القناة
    مطفأةً لا محذوفة.
    """

    CASH = "cash"
    CLIQ = "cliq"
    CARD = "card"
    WALLET = "wallet"
    # خصمُ الكوبون (المرحلة 12-ز): **قناةٌ لا يدفعها الراكب** — تُنشئها المنصةُ
    # وتؤكّدها بنفسها لحظةَ الإنهاء بقيمة الخصم. وهي **كالبطاقة: ليست في أيٍّ من
    # تصنيفَي القنوات** — لا في `DIRECTLY_COLLECTED_METHODS` (كي يُكتب للكبتن
    # `ride_earning`) ولا في `WALLET_FUNDED_METHODS` (كي لا يُخصم من محفظة
    # الراكب شيء). ولو وُضعت في الثاني لدفع الراكبُ الخصمَ من رصيده — أي لانقلب
    # الكوبونُ ضريبةً عليه، وهو نقيضُ الغرض كلِّه
    PROMO = "promo"
    # خصمُ المشاركة (المرحلة 12-ي): **قناةٌ لا يدفعها الراكب** كـ`promo` تماماً،
    # وتُنشئها المنصةُ وتؤكّدها لحظةَ الإنهاء — والشركةُ تتحمّلها (قرارُ المالك
    # الثالث) فلا يَنقص ما يقبضه الكبتن بخصمٍ لم يقرّره.
    #
    # **وقيمةٌ مستقلةٌ لا إعادةُ استعمالٍ لـ`promo`**، والسببُ ليس التسمية:
    # `promo.spent()` يجمع دفعاتِ `promo` على رحلات الرمز ليقيس المصروفَ ضد
    # الميزانية، فخصمُ مشاركةٍ على رحلةٍ تحمل كوبوناً كان سيُحسب **إنفاقاً من
    # ميزانية الكوبون** — أي سقفٌ ماليٌّ يُستهلك بما لا يخصّه. والتسميةُ تتبع:
    # إيصالُ الراكب يقول لماذا خُصم، و«خصم كوبون» على رحلةٍ بلا كوبونٍ كذبة
    SHARE = "share"


class PaymentProvider(StrEnum):
    """المزود الذي مرّت عليه العملية — فارغ لما لا مزود له (كاش، كليك، محفظة).

    `cliq_acquirer` أضافته **المرحلة 8**: حساب التاجر الذي يشهد على شحن
    المحفظة بكليك آلياً (SPEC القسم 7/15-أ). لا يظهر على `payments` أبداً —
    دفعُ الرحلة بكليك يذهب إلى alias الكبتن لا إلى حساب الشركة، فلا مزود له
    ولا تأكيد آلي (القسم 6).
    """

    TELR = "telr"
    CLIQ_ACQUIRER = "cliq_acquirer"


class PromoDiscountType(StrEnum):
    """نوعُ خصم الكوبون (المرحلة 12-ز).

    النسبةُ للحملات، والثابتُ لأول رحلةٍ مجاناً وللتعويضات (قرارُ المالك).
    والنسبةُ تحتاج سقفاً (`max_discount`) وإلا ابتلع كوبونُ ٥٠٪ رحلةً طويلة.
    """

    PERCENT = "percent"
    FIXED = "fixed"


class DispatchMode(StrEnum):
    """كيف يُعرض الطلبُ على الكباتن (SPEC §5.3، قرارُ المالك 2026-08-30).

    **تسلسليٌّ**: واحدٌ في كلِّ مرّةٍ والدورُ محفوظ — أعدلُ للكبتن.
    **وبثٌّ**: دفعةٌ معاً وأولُ من يقبل يأخذ — أسرعُ للراكب.

    **ولا ثالثَ لهما**، ومن أراد «الأقرب أوّلاً» فذاك ترتيبُ المرشَّحين لا
    نمطُ العرض — وهو قائمٌ في النمطين.
    """

    SEQUENTIAL = "sequential"
    BROADCAST = "broadcast"


class DriverDebtSource(StrEnum):
    """من أين نشأ المستحقُّ على الكبتن.

    **عضوٌ واحدٌ اليوم وعمودٌ مصرَّح**: «كلُّ ما يستحقّ من الرحلة» (قرارُ
    المالك 2026-08-30) — واليومَ عمولةُ ما يُقبض مباشرةً وحدَها هي ما يستحقّ،
    لأن ما تمرّ به المنصّةُ يُخصم من الدفتر لحظتَه ولا يصير دَيناً.
    """

    RIDE_COMMISSION = "ride_commission"


class DriverDebtStatus(StrEnum):
    """حالُ المستحقّ — **والجزئيُّ ليس حالاً** بل عمودُ `collected`."""

    OUTSTANDING = "outstanding"
    SETTLED = "settled"
    WRITTEN_OFF = "written_off"


class ProviderOrderPurpose(StrEnum):
    """لماذا فُتح طلبٌ لدى مزود الدفع (SPEC القسم 6.4/7/8).

    ثلاثة أغراض، وكلٌّ منها يستقر في مكانٍ مختلف عند النجاح: أجرةُ رحلة تستقر
    في صفّ `payments`، وشحنُ محفظة يستقر في قيد `topup` مباشرةً بلا طلبِ شحنٍ
    ينتظر إنساناً — شحن البطاقة فوريٌّ آلي (القسم 7) — واشتراكُ كبتنٍ يستقر في
    صفّ `driver_subscriptions` بلا قيدٍ في الدفتر أصلاً: مالُه خرج من بطاقته لا
    من محفظته (المرحلة 7، القسم 8).
    """

    RIDE_PAYMENT = "ride_payment"
    WALLET_TOPUP = "wallet_topup"
    SUBSCRIPTION = "subscription"
    #: **سدادُ دَينِ كبتنٍ بكليك** — يستقر في `driver_debts.collected` ولا
    #: يكتب قيداً في الدفتر: مالُه خرج من يده لا من محفظته، كالاشتراك.
    DEBT = "debt"


class ProviderOrderSource(StrEnum):
    """**من حصّل هذا المال** — يدويّاً أم من القابض (قرارُ المالك 2026-08-29).

    **والتحصيلُ خطوةٌ واحدةٌ يقرأها الاشتراك**: «تأكيد الدفع» يملؤها المشرفُ
    اليوم بيده، ويملؤها القابضُ غداً بإشعاره — **وما بعدها لا يعرف مَن ملأها**.

    **والمصدرُ يُختم على الصفّ** ليُقرأ في التدقيق من أين جاء التأكيد، **ولا
    يُخمَّن بعد شهر**. و`provider` يبقى `cliq_acquirer` في الحالين: القضيبُ
    واحد، **والمختلفُ من يحصّل**.
    """

    MANUAL = "manual"
    ACQUIRER = "acquirer"


class ProviderOrderStatus(StrEnum):
    """حالة الطلب لدى المزود.

    `created` تعني «فُتح ولم يُحسم»: الراكب على صفحة الدفع أو غادرها. الحسم
    يأتي من المزود وحده — بـ webhook أو باستعلامٍ من الخلفية — لا من العميل.
    """

    CREATED = "created"
    PAID = "paid"
    FAILED = "failed"
    CANCELLED = "cancelled"


class PaymentStatus(StrEnum):
    """`pending → confirmed | failed | disputed | refunded` (SPEC القسم 4)."""

    PENDING = "pending"
    CONFIRMED = "confirmed"
    FAILED = "failed"
    DISPUTED = "disputed"
    REFUNDED = "refunded"


class PaymentConfirmedBy(StrEnum):
    """من أكّد التحصيل: الكبتن يدوياً، أو المزود آلياً، أو الإدارة فصلاً لنزاع."""

    DRIVER = "driver"
    SYSTEM = "system"
    ADMIN = "admin"


class DisputeResolution(StrEnum):
    """حكم الإدارة في نزاع دفعة (SPEC القسم 6/13.4).

    `paid` = المبلغ وصل الكبتنَ فعلاً فتصير الدفعة `confirmed`، و`unpaid` =
    لم يصل فتصير `failed`. القيمتان تصفان الواقعة لا الحالة الناتجة، فحالةُ
    الدفعة تُشتق منهما في مكان واحد.
    """

    PAID = "paid"
    UNPAID = "unpaid"


class RatingRaterType(StrEnum):
    """من يقيّم من — التقييم متبادل بعد `completed` (SPEC القسم 5.9)."""

    RIDER = "rider"
    DRIVER = "driver"


class SubscriptionDurationType(StrEnum):
    DAILY = "daily"
    WEEKLY = "weekly"
    MONTHLY = "monthly"


class SubscriptionStatus(StrEnum):
    """حال اشتراك الكبتن (SPEC القسم 4/8).

    الاشتراك يُنشأ مدفوعاً — لا صفّ لاشتراكٍ لم يصل ماله — ويصير `expired` حين
    تتجاوزه الساعة. **ولا `pending`**: القنوات اليدوية (كاش/كليك) تُسجَّل بعد
    قبض المال لا قبله، تماماً كما لا يتغيّر رصيدٌ قبل تأكيد شحنته (القسم 7).

    **و`cancelled` أُضيفت 2026-09-02 (البند ٢، §38) — وهي نقضٌ صريحٌ لسطرٍ كان
    هنا**: «حالتان لا ثالثة … اشتراكٌ مدفوعٌ مقدماً لا يُلغى». **وعلّةُ النقض
    بنصِّ المالك**: «ألغيتُه» يجب أن تعني «أوقفتُه» وإلا كذب الزرّ، **والإلغاء
    بيد الإدارة لا بيد الكبتن** — فما لم يُستعمل يُردّ.

    **ولا تُدمج في `expired`**: تلك تقول «انقضى وقتُه»، وهذه «أوقفناه ورددنا
    مالَه». **والفرقُ يُقرأ بعد شهر** — وعليه تقوم أهليةُ السلفة: دورةُ عملٍ
    اكتملت ليست دورةً أُلغيت ورُدَّ مالُها.
    """

    ACTIVE = "active"
    EXPIRED = "expired"
    CANCELLED = "cancelled"


class CommissionAppliesTo(StrEnum):
    """نطاق تطبيق العمولة.

    `cashless_rides` = البطاقة والمحفظة فقط، أي ما تمر أمواله عبر المنصة —
    الرحلات النقدية وكليك يقبضها الكبتن مباشرة (القسم 9 من SPEC).
    """

    ALL_RIDES = "all_rides"
    CASHLESS_RIDES = "cashless_rides"


class ProviderKey(StrEnum):
    """مزودو الخدمات الخارجية المُدارون من صفحة العقود."""

    MAPBOX = "mapbox"
    TELR = "telr"
    SMS = "sms"
    CLIQ_ACQUIRER = "cliq_acquirer"
    FCM = "fcm"
    PAYOUT = "payout"
    # التحقق من الهاتف لدى Firebase (المرحلة 8-ب): عقدٌ مستقل عن `fcm` وإن
    # كان المشروع واحداً — إطفاءُ الإشعارات لا يجوز أن يُطفئ الدخول
    FIREBASE_AUTH = "firebase_auth"
    # واتساب مُحقِّقاً ثالثاً عبر WhatsApp Cloud API الرسمي من ميتا (12-هـ).
    # **عقدٌ عامٌّ لا per-country** كعقد SMS: رقمُ الهاتف يحمل دولته، ورقمُ
    # الأعمال الواحد يخدم السوقين. والذي يختلف بالدولة هو **المفتاح**
    # `whatsapp_otp_enabled` — فالعقد يقول «نستطيع» والمفتاح يقول «نفعل هنا»
    WHATSAPP = "whatsapp"
    # مُرسِلُ البريد (قرارُ المالك 2026-08-31). **عقدٌ عامٌّ لا per-country**
    # كعقدَي SMS وواتساب: نطاقٌ واحدٌ يرسل للسوقين، **والذي يختلف بالدولة هو
    # المفتاح** `email_otp_enabled`. فالعقدُ يقول «نستطيع» والمفتاحُ «نفعل هنا».
    #
    # **ولا `feature_key` له في السجلّ** للعلّة نفسِها التي في واتساب:
    # المزامنةُ التلقائيةُ تُشعل مفتاحَ **دولةِ العقد**، **وهذا عقدٌ بلا دولة**.
    EMAIL = "email"


class Gender(StrEnum):
    """جنسُ صاحب الحساب (المرحلة 10-ج).

    **قيمتان لا ثلاث، والغيابُ `NULL` لا عضوٌ ثالث**: «غير معروف» ليس جنساً،
    وجعلُه قيمةً يجعل الاستعلام يقارنها بجنسٍ فيطابق.

    ومصدرُها يختلف باختلاف من يعلنها، وهذا فرقٌ في الأمان لا في التنفيذ:
    **الراكبة تعلن جنسها بنفسها** — إعلانُها يقيّد رحلتَها هي؛ أما **جنسُ
    الكبتن فيضبطه المشرف من الهوية المرفوعة** ويُختم بـ`gender_verified_at`،
    لأن إعلانَه يقيّد أمانَ غيره. ولذلك تقرأ المطابقةُ **الكبتنَ المختوم
    وحده**: رجلٌ يكتب في حقلٍ أنه امرأة لا يصير سائقةً للنساء.
    """

    MALE = "male"
    FEMALE = "female"


class GenderPreference(StrEnum):
    """تفضيلُ جنس الطرف الآخر (المرحلة 10-ج).

    ثلاثيٌّ لأن «لا يهمّني» موقفٌ صريح لا غيابُ موقف: `any` هو الافتراضي،
    وهو ما يجعل تشغيل الميزة لا يغيّر شيئاً لمن لم يطلبها.
    """

    MALE = "male"
    FEMALE = "female"
    ANY = "any"


class CancelReasonCode(StrEnum):
    """سببُ إلغاءٍ **مصنَّف** بجانب النصّ الحر (المرحلة 10-ج).

    عمودُه `String(32)` محروسٌ بهذا التعداد في طبقة Pydantic لا `ENUM` في
    القاعدة — نفس استثناء `feature_flags.feature_key`: أسبابٌ جديدة تُضاف
    كوداً بلا ترحيلة.

    و`gender_mismatch` ليست تصنيفاً للتقارير فقط: هي التي تجعل الإلغاء **بلا
    رسوم** وتُدخل بلاغاً على حساب الطرف الآخر، فلا يجوز أن تكون نصاً حراً
    يكتبه من يشاء كما يشاء.
    """

    GENDER_MISMATCH = "gender_mismatch"
    OTHER = "other"


class FeatureKey(StrEnum):
    """مفاتيح feature_flags المعروفة.

    العمود في القاعدة نص لا ENUM، فإضافة مفتاح لاحقاً (مثل ميزات المرحلة 12)
    تغييرُ كود فقط بلا ترحيلة. هذا النوع يحرس الكتابة عبر طبقة Pydantic.
    """

    # لا مفتاح للعمولة هنا: مصدرها الوحيد `commission_settings` حيث تسكن نسبتها
    # ونطاقها — مفتاح ثانٍ يعني حالتين قابلتين للاختلاف لأمرٍ مالي.
    CLIQ_ENABLED = "cliq_enabled"
    CARD_ENABLED = "card_enabled"
    WALLET_ENABLED = "wallet_enabled"
    WALLET_TRANSFER_ENABLED = "wallet_transfer_enabled"
    # **الاستثناء الوحيد لقاعدة «غياب الصف = معطّل»** (المرحلة 8-ب): هذا
    # مفتاح **حارس** لا ميزة، وإطفاؤه يفتح باباً لا يغلقه. فغيابُ صفّه يعني
    # **مفعّلاً**، لأن القاعدة الأصلية وُضعت كي لا تُفتح ميزةٌ بالسكوت — وهنا
    # السكوتُ يُطفئ حارساً. انظر `settings_service.DEFAULT_ENABLED_FLAGS`
    OTP_VERIFICATION_ENABLED = "otp_verification_enabled"
    # **حارسان لا ميزتان** (قرارُ المالك 2026-08-23) — وغيابُ صفِّهما **يعمل**،
    # وإطفاؤهما يحتاج **سبباً مكتوباً** يدخل التدقيق. وهما أشدُّ ثماني شاشاتٍ
    # تكتب مالاً: الأولى تُعيد تسعيرَ السوق فوراً، والثانية تُخرج المالَ إلى
    # طرفٍ ثالثٍ فلا يعود.
    #
    # **وتجميدُ التسعير يوقف الكتابةَ لا التسعير**: الرحلاتُ تُسعَّر بالصفوف
    # القائمة والطلبُ لا يُرفض. ومفتاحٌ يوقف السوقَ لا يُضغط أبداً، ومفتاحٌ
    # كلفتُه صفرٌ على الراكب يُضغط في ثانية — وهذا هو ما يجعله حارساً مستعمَلاً
    # لا زينةً في شاشة. والجاري لا يتأثّر بحكمِ التجميد على الرحلة نفسِها.
    PRICING_WRITES_ENABLED = "pricing_writes_enabled"
    # **وإيقافُ الصرف يوقف مغادرةَ المال لا الطلبات**: الكبتنُ يطلب والمشرفُ
    # يعتمد، والموقوفُ خطوةُ الصرف وحدَها. ومنعُ الطلب يُقرأ في تطبيقه
    # «محفظتي معطوبة» فيصنع موجةَ شكاوى عن عطبٍ لا وجودَ له.
    #
    # **ويُقرأ عند باب المسار لا في وسطه**: `pay_via_provider` هو الاستثناءُ
    # الوحيدُ المصرَّحُ به من «لا تُمسك قفلاً عبرَ نداءِ مزوّد» لأن النداءَ هو
    # حركةُ المال — فنداءٌ غادر **يُكمَل ولا يُقطع**، إذ قطعُه يترك مالاً تحرّك
    # عند طرفٍ ثالثٍ وصفّاً يقول إنه لم يتحرّك.
    WITHDRAWAL_PAYOUT_ENABLED = "withdrawal_payout_enabled"
    # خدمة التوصيل النسائي (المرحلة 10-ج). **ميزةٌ لا حارس**، فغيابُ صفّها
    # يعني معطّلة كبقية الميزات — وتبقى كذلك حتى تُراجَع أجناسُ الكباتن
    # المعتمدين المتراكمين. وحين تكون مطفأةً **لا مطابقةَ جنسٍ أصلاً**: لا
    # يُرشَّح تفضيلٌ ولا يُصفّى سائق، وإلا لأبقى تفضيلٌ قديمٌ على حساب كبتنٍ
    # يحجب عنه الطلبات في سوقٍ لا خدمةَ نسائيةً فيه
    WOMEN_SERVICE_ENABLED = "women_service_enabled"
    # تعدد الوجهات (المرحلة 12-ب). **ميزةٌ لا حارس**، فغيابُ صفّها معطَّلة —
    # ولا تُضاف إلى `DEFAULT_ENABLED_FLAGS` مهما بدت صغيرة. وإطفاؤها يمنع
    # **الطلبات الجديدة** وحدها: رحلةٌ جارية بمحطاتها تكمل، ومحطاتُها مجمَّدة
    MULTI_STOP_ENABLED = "multi_stop_enabled"
    # واتساب مُحقِّقاً للرقم (12-هـ). **ميزةٌ لا حارس** فغيابُ صفّها معطَّلة،
    # ولا تُضاف إلى `DEFAULT_ENABLED_FLAGS`: إشعالُها يعني إرسالَ رمزٍ في قناةٍ
    # مدفوعةٍ لها قوالبُ معتمدة، وسكوتٌ يفتحها يُرسل في قناةٍ لم يُعتمد قالبُها.
    # وهي **per-country كبقية المفاتيح** بينما العقد عام: العقدُ يقول «نستطيع»
    # والمفتاحُ يقول «نفعل في هذا السوق» — فيُجرَّب في سوقٍ قبل الآخر
    WHATSAPP_OTP_ENABLED = "whatsapp_otp_enabled"
    # البريدُ قناةً بديلة (قرارُ المالك 2026-08-31). **ميزةٌ لا حارس** فغيابُ
    # صفّها معطَّلة، **ولا تُضاف إلى `DEFAULT_ENABLED_FLAGS`**.
    #
    # **ولها شرطٌ يعلو على السكوت**: لا تُشعَل **بلا عقدِ مُرسِلٍ فعّال** —
    # يرفضه `upsert_feature_flag` بنصّه. **وإشعالُها بلا مُرسِلٍ يرسم في
    # التطبيق بابَ «سجّل ببريدك» ثم يسقط عند أول ضغطة**، وهو «زرٌّ بلا باب»
    # مقلوباً: البابُ مرسومٌ ولا شيءَ خلفه.
    EMAIL_OTP_ENABLED = "email_otp_enabled"
    # البقشيش (المرحلة 12-و). **ميزةٌ لا حارس** فغيابُ صفّها معطَّلة، ولا
    # تُضاف إلى `DEFAULT_ENABLED_FLAGS`. وتحكم **ما يُرسله التطبيق كما تحكم ما
    # يرسمه**: مطفأةً لا تظهر الأزرار ولا يُقبل النداء
    TIPS_ENABLED = "tips_enabled"
    # الكوبونات (المرحلة 12-ز). **ميزةٌ لا حارس** فغيابُ صفّها معطَّلة، وتحكم
    # **ما يُرسله التطبيق كما تحكم ما يرسمه**: مطفأةً لا تظهر ورقةُ الكوبون ولا
    # يُقبل رمزٌ في طلب الرحلة
    PROMO_CODES_ENABLED = "promo_codes_enabled"
    # حافزُ إحالة السائقات (المرحلة 12-ح). **ميزةٌ لا حارس** فغيابُ صفّها
    # معطَّلة. **ومفتاحُها ليس `women_service_enabled`** وإن كان الغرضُ واحداً:
    # ذاك مطفأٌ حتى يُصفّى متراكمُ إثبات الجنس، والحافزُ هو ما يبني العرضَ الذي
    # تنتظره الخدمةُ لتُشعَل — فربطُهما يجعلها تنتظر ما لا سبيلَ لبنائه.
    # وإطفاؤها يوقف **الدفعَ والعرض**، ولا يمحو إحالةً سُجّلت (SPEC القسم 9.1)
    DRIVER_REFERRALS_ENABLED = "driver_referrals_enabled"
    # إحالةُ راكبٍ راكباً (تعميمُ 12-ح). **مفتاحٌ ثانٍ لا توسيعٌ للأول**: من
    # أشعل حافزَ السائقين لأنه يبني عرضاً لم يشعل بالضرورة حافزاً يبني طلباً،
    # والمبلغان مختلفان لأن قيمةَ الحسابين مختلفة. **والبرنامجُ يُختار من دور
    # المُحال**، فمفتاحُ الراكب هو ما يُقرأ حين يسجّل راكبٌ برمزٍ أياً كان دورُ
    # صاحبِ الرمز — وهو ما يجعل رمزاً واحداً يعمل في البرنامجين بلا التباس
    RIDER_REFERRALS_ENABLED = "rider_referrals_enabled"
    # كوبونُ ترحيبٍ للمُحال (القرارُ الأول في التعميم). **مفتاحٌ مستقلٌّ عن
    # الحافزَين** لأنه المالُ الوحيد فيهما الذي **تتحمّله الشركةُ عن الطرف
    # الثاني**: يُشعَل ويُطفأ بلا مساس بما وُعد به المُحيلون
    REFERRED_REWARD_ENABLED = "referred_reward_enabled"
    # الرحلاتُ المجدولة (المرحلة 12-ط). **ميزةٌ لا حارس** فغيابُ صفّها معطَّلة،
    # وتحكم **ما يُقبل كما تحكم ما يُرسم**: مطفأةً لا تُنشأ حجوزٌ جديدة —
    # **وما حُجز قبل الإطفاء يُنفَّذ**، كقاعدةِ المحطات: الإطفاءُ يمنع الجديد
    # ولا يخلف وعداً قائماً لراكبٍ رتّب موعدَه عليه
    SCHEDULED_RIDES_ENABLED = "scheduled_rides_enabled"
    # سياراتُ الكباتن على خريطةِ الكبتن نفسِه (2026-08-22). **ميزةٌ لا حارس**
    # فغيابُ صفّها معطَّلة، **وتُشحن مطفأةً بقرارِ المالك**: أن يرى الكبتنُ
    # زملاءَه سؤالُ سوقٍ لا سؤالُ عرض — يُقرأ عوناً على اختيار موضعٍ، ويُقرأ
    # مطاردةً بين الكباتن على الزحام. **فالبناءُ جاهزٌ والقرارُ صفٌّ واحد.**
    #
    # **والمعروضُ مجهَّلٌ كما يراه الراكبُ سواءً بسواء** — نفسُ `anonymous_ref`
    # ونفسُ الحقول: إحداثياتٌ واتجاهٌ وفئة. فالكبتنُ **ليس طرفاً أولى بالهوية
    # من الراكب**، وإشعالُ المفتاح لا يفتح باباً على أحد.
    DRIVER_MAP_NEARBY_ENABLED = "driver_map_nearby_enabled"
    # كراجُ المركبات ومتجرُها (2026-08-22). **ميزةٌ لا حارس** فغيابُ صفّها
    # معطَّلة. **وإطفاؤها يخفي المتجرَ والكراجَ ولا ينزع مركبةً مملوكة**: من
    # اشترى بماله لا تُسحب منه بمفتاح — قاعدةُ الحجوزات والمحطات نفسُها.
    # **والمركبةُ النشطةُ تبقى مرسومةً** لأن نزعَها يجعل الإطفاءَ عقوبةً على
    # من دفع
    VEHICLE_SKINS_ENABLED = "vehicle_skins_enabled"
    # مشاركةُ الرحلة بين ركاب (المرحلة 12-ي). **ميزةٌ لا حارس** فغيابُ صفّها
    # معطَّلة. وتحكم **ما يُقبل كما تحكم ما يُرسم**: مطفأةً لا يُقبل طلبٌ
    # بمشاركة ولا تُعرض. **وإطفاؤها لا يفكّ مجموعةً قائمة** (قاعدةُ المحطات
    # والحجوزات): راكبان في سيارةٍ واحدةٍ الآن يكملان رحلتَهما بخصمهما.
    #
    # **ولا تكفي وحدَها**: `ride_sharing_settings.discount_percent` صفرٌ يعني
    # «لم تُقرَّر النسبةُ بعد» فتبقى الميزةُ مخفيةً ولو أُشعل المفتاح — كالبقشيش
    # بمبالغه الصفرية، فوعدُ خصمٍ مقدارُه صفرٌ أسوأ من غياب الوعد
    RIDE_SHARING_ENABLED = "ride_sharing_enabled"
    # عروضُ اشتراكات الكباتن (البند ٥٤). **ميزةٌ لا حارس** فغيابُ صفّها معطَّلة.
    #
    # **ومطفأةً: لا خصمَ يُحسب ولا عرضٌ يُعرض — وعروضٌ قائمةٌ لا تُلغى**: من
    # اشترى بخصمٍ احتفظ به، كقاعدة المحطات والحجوزات. والإطفاءُ يمنع الجديدَ
    # ولا يسترجع ما وقع.
    # **ظهورُ الدولة في التطبيقات** (قرارُ المالك 2026-08-19). سوقٌ يُبنى قبل
    # أن يُفتح: الأردنُ أولاً وليبيا بعده، ولا مستخدمين فيها اليوم — فهذا
    # **إخفاءُ واجهةٍ لا إيقافُ خدمة**: الخلفيةُ والبياناتُ والإعداداتُ كما هي.
    #
    # **وسكوتُه يعني الظهور، وهو استثناءٌ معلَّل**: `DEFAULT_ENABLED_FLAGS`
    # للحُرّاس وحدَها، وهذا ليس ميزةً ولا حارساً بل **صفةُ سوق**. ولو قُرئ
    # الغيابُ إخفاءً لاختفت **كلُّ** دولةٍ على تثبيتٍ لم يُبذر — أي تطبيقٌ بلا
    # دولةٍ واحدة. فالدولةُ الموجودةُ في `CountryCode` ولها إعداداتٌ تُخدَم
    # حتى يقول أحدٌ غيرَ ذلك، والإخفاءُ صفٌّ صريحٌ يكتبه إنسان.
    COUNTRY_VISIBLE = "country_visible"
    SUBSCRIPTION_OFFERS_ENABLED = "subscription_offers_enabled"
    # **شريطُ «التعليمة التالية»** (البند ٧) — مطفأٌ افتراضاً كغيره. وإشعالُه
    # يطلب `steps` من Mapbox في نداء القبول، فيصير للمفتاح **كلفةُ حمولةٍ**
    # لا كلفةُ نداءٍ إضافيّ: النداءُ واقعٌ على أيِّ حال
    NEXT_INSTRUCTION_ENABLED = "next_instruction_enabled"
    # سلفُ الكباتن (البند ١٥). **ميزةٌ لا حارس** فغيابُ صفّها معطَّلة.
    # **وهي per-country لأن القرارَ نفسَه سوقٌ واحدٌ أولاً** (قرارُ المالك
    # 2026-08-14: الأردن): المفتاحُ هو ما يجعلها تجربةً لا التزاماً في السوقين.
    # **ولا تكفي وحدَها**: `advance_settings` بلا خطةٍ يوميةٍ في الدولة تعني
    # سقفاً لا مصدرَ له — والسقفُ الأولُ **قيمةُ الاشتراك اليومي** بنصِّ القرار
    DRIVER_ADVANCES_ENABLED = "driver_advances_enabled"
    # المهامُّ والمستويات (البند ٥٣). **ميزةٌ لا حارس** فغيابُ صفّها معطَّلة.
    # ومطفأً: **لا ترجيحَ في التوزيع، ولا شاشةَ في التطبيق، ولا مهمّةَ تُحسب** —
    # فالمفتاحُ يحكم الحسابَ كما يحكم العرض، وميزةٌ تُحسب في الظل تُشحن يوماً
    # بمستوياتٍ بُنيت على تعريفٍ لم يره أحد.
    #
    # **ولا يكفي وحدَه**: `level_settings.discount_meters` صفرٌ يعني «بلا أثر»،
    # فيبقى الترتيبُ حرفياً كما هو اليوم ولو أُشعل المفتاح — كنسبة المشاركة
    # ومبالغ البقشيش بالضبط
    DRIVER_LEVELS_ENABLED = "driver_levels_enabled"


class BookingStatus(StrEnum):
    """حالُ حجزٍ مجدول (SPEC القسم 5.11).

    **ثلاثُ حالاتٍ ولا رابعة**: `pending` ينتظر موعدَه، و`dispatched` سُلّم إلى
    التوزيع (و`ride_id` مكتوبٌ حينها)، و`missed` حلَّ موعدُه وصاحبُه في رحلةٍ
    جارية فلم تُنشأ ثانية، و`cancelled` ألغاه صاحبُه.

    **ولا `fulfilled` ولا `no_driver`**: ما جرى بعد التسليم تقوله الرحلةُ نفسُها،
    وقيمةٌ هنا تكرّرها بيتٌ ثانٍ يفترق عن الأول أولَ مرةٍ تُلغى فيها رحلةٌ وُجد
    لها كبتن — وهي قاعدةُ «لا عمودَ مشتقّاً» نفسُها (رصيدُ المحفظة، حالُ
    الاشتراك، استحقاقُ الإحالة).
    """

    PENDING = "pending"
    DISPATCHED = "dispatched"
    MISSED = "missed"
    CANCELLED = "cancelled"


class WalletOwnerType(StrEnum):
    """صاحب المحفظة — الراكب أو الكبتن (SPEC القسم 4).

    `owner_id` يشير إلى `users.id` في الحالتين؛ هذا العمود يميّز أي المحفظتين
    هي، فحسابُ كبتنٍ ما هو إلا امتداد لحساب مستخدم.
    """

    RIDER = "rider"
    DRIVER = "driver"


class WalletTransactionType(StrEnum):
    """أنواع قيود دفتر المحفظة (SPEC القسم 4).

    ما لا يُنشئه كودُ هذه المرحلة موجود هنا لأن النوع جزء من المواصفة:
    `ride_payment`/`ride_earning`/`commission`/`refund` تأتي مع الدفع
    (المرحلة 6)، و`subscription_payment` مع الاشتراكات (المرحلة 7).
    """

    TOPUP = "topup"
    RIDE_PAYMENT = "ride_payment"
    RIDE_EARNING = "ride_earning"
    COMMISSION = "commission"
    TRANSFER_IN = "transfer_in"
    TRANSFER_OUT = "transfer_out"
    WITHDRAWAL = "withdrawal"
    REFUND = "refund"
    SUBSCRIPTION_PAYMENT = "subscription_payment"
    ADJUSTMENT = "adjustment"
    # البقشيش (المرحلة 12-و) — زوجٌ يقابل زوجَ الأجرة حرفياً: خصمٌ على الراكب
    # وإضافةٌ للكبتن. **ولا `commission` يقابلهما**: الكبتن يقبضه كاملاً
    # (قرارُ المالك)، فهو دخلُه الوحيد بلا عمولةٍ عليه
    TIP = "tip"
    TIP_PAYMENT = "tip_payment"
    # شراءُ مركبةِ الكراج (2026-08-22) — **مدينٌ من أرباح الكبتن**، ولا عمولةَ
    # عليه ولا يقابله دائنٌ في محفظةٍ أخرى: هو **إيرادُ منصّةٍ** كالعمولة،
    # ووعاءُ الشركة ليس محفظةً في هذا النظام. **ولا يُخلط بـ`subscription_payment`**
    # لأن السؤالَ «كم أنفق الكباتنُ على الزينة» يجب أن يبقى له جوابٌ واحد
    SKIN_PURCHASE = "skin_purchase"
    # حافزُ الإحالة (المرحلة 12-ح) — **دائنٌ بلا مدينٍ مقابل**: الشركةُ تتحمّله
    # كما تتحمّل خصمَ الكوبون، ووعاءُ الشركة ليس محفظةً في هذا النظام. ولا
    # عمولةَ عليه ولا يدخل أرباحَ الرحلات (SPEC القسم 9.1)
    REFERRAL_BONUS = "referral_bonus"
    # السلفة (البند ١٥) — **زوجٌ يفترق عن كل زوجٍ قبله**: `advance` دائنٌ حين
    # تُصرف، و`advance_repayment` مدينٌ عند كل اقتطاعٍ من أرباحٍ داخلة. والدَّينُ
    # نفسُه **ليس رصيداً سالباً**: `balance_after >= 0` يبقى غيرَ ممسوس، والدَّينُ
    # رقمٌ في جدوله يُقرأ بطرح المدين من الدائن (قرارُ المالك 2026-08-14)
    ADVANCE = "advance"
    ADVANCE_REPAYMENT = "advance_repayment"
    # رسمُ الإلغاء (`design/CANCELLATION-FEE.md`) — **زوجٌ بين مستخدمَين لا بين
    # المنصّة وأحدهما**: `cancellation_fee` مدينٌ على من ألغى (أو على الكبتن
    # الحاملِ الذي قبض المبلغَ بيده نقداً)، و`cancellation_compensation` دائنٌ
    # للكبتن المتضرر. **ولا يدخلان أرباحَ الرحلات ولا عمولةَ عليهما**: تعويضٌ
    # عن وقتٍ أُنفق لا أجرةُ رحلةٍ وقعت — وإدخالُهما يجعل الكشفَ يكذب على
    # الطرفين معاً، وهي قاعدةُ البقشيش نفسُها من الجهة الأخرى
    CANCELLATION_FEE = "cancellation_fee"
    CANCELLATION_COMPENSATION = "cancellation_compensation"


class CancellationChargeStatus(StrEnum):
    """حالُ رسمِ إلغاءٍ استُحقّ (`design/CANCELLATION-FEE.md` §9).

    أربعٌ كلُّها تُكتب: `pending` لم يُحصَّل، و`settled` وصل المتضرر،
    و`waived` أعفته الإدارة، و`written_off` مضت مدّتُه فتحمّلته الشركة بقرارٍ
    مسجَّل. **ولا حالةَ «جزئيّ»**: لا سدادَ جزئيّ بقرار المالك.
    """

    PENDING = "pending"
    SETTLED = "settled"
    WAIVED = "waived"
    WRITTEN_OFF = "written_off"


class UnpaidCancellationOutcome(StrEnum):
    """مآلُ دَينِ إلغاءٍ لم يعد صاحبُه (`design/CANCELLATION-FEE.md` §10).

    **الإدارةُ تختار، والكودُ لا يحسم** (قرارُ المالك): يبقى معلّقاً، أو
    تشطبه الإدارة بقرارٍ مسجَّل، أو تتحمّله الشركةُ فيصل المتضررَ ماله.
    """

    KEEP_PENDING = "keep_pending"
    ADMIN_DECIDES = "admin_decides"
    COMPANY_BEARS = "company_bears"


class AdvanceStatus(StrEnum):
    """حالُ سلفةٍ صُرفت (البند ١٥).

    **ثلاثُ حالاتٍ كلُّها تُكتب**: `outstanding` قائمةٌ لم تُسدَّد، و`repaid`
    اكتمل سدادُها، و`written_off` شُطبت بقرارٍ إداريٍّ بعد ٩٠ يوماً (قرارُ
    المالك 2026-08-14). فليست هذه حالةً لا سبيلَ لكتابتها كـ`status` الذي
    حُذف من `tips`.

    **وليست هي المبلغَ المتبقّي**: المتبقّي يُطرح من الدفتر (دائنُ الصرف ناقص
    مجموعِ المدين)، ولا عمودَ رصيدٍ له — قاعدةُ المحفظة نفسُها. وما تحمله
    الحالةُ هو ما لا يقوله المجموع: **أنَّ الصفَّ أُغلق ولماذا**، وهو أيضاً ما
    يقرؤه الفهرسُ الجزئيُّ «سلفةٌ قائمةٌ واحدةٌ لكل كبتن» — ومجموعٌ لا يُقرأ في
    مُسنَد فهرس.
    """

    OUTSTANDING = "outstanding"
    REPAID = "repaid"
    WRITTEN_OFF = "written_off"


class TopupMethod(StrEnum):
    """قنوات شحن محفظة الراكب (SPEC القسم 7).

    `card` هو Telr — شحن فوري آلي يأتي في المرحلة 6، فلا يمر بطلب ينتظر
    تأكيداً بشرياً كما تمر `cliq` و`cash`.
    """

    CLIQ = "cliq"
    CASH = "cash"
    CARD = "card"


class TopupRequestStatus(StrEnum):
    PENDING = "pending"
    CONFIRMED = "confirmed"
    REJECTED = "rejected"


class WithdrawalMethod(StrEnum):
    CLIQ = "cliq"
    BANK = "bank"


class WithdrawalStatus(StrEnum):
    PENDING = "pending"
    APPROVED = "approved"
    PAID = "paid"
    REJECTED = "rejected"


class DevicePlatform(StrEnum):
    """منصة الجهاز المسجَّل لإشعارات Push (SPEC القسم 4 — المرحلة 8).

    تُقرأ عند الإرسال: أولوية FCM العالية تُكتب بحقلٍ مختلف لكل منصة
    (`android.priority` و`apns-priority` و`Urgency`).
    """

    ANDROID = "android"
    IOS = "ios"
    WEB = "web"


class ServiceTileStatus(StrEnum):
    """حالُ بلاطة الخدمة (الترحيلة `0063`، قرارُ المالك 2026-08-30).

    **وثلاثٌ لا أربع**: «جديد» ليست حالاً بل **شارةٌ مؤقّتةٌ على `active`**
    (`new_until`) — **وحالٌ رابعةٌ كانت ستحتاج شرطاً في كلِّ موضعٍ يسأل
    «أتُفتح؟»، والشارةُ لا تغيّر الجواب.**

    و`soon` **تُقرأ ولا تُنقر**: تظهر بعلامتها ولا تفتح باباً **ولا تصيح
    بخطأٍ حين تُلمس** — وخطأٌ على لمسةٍ متوقَّعةٍ يُقرأ عطباً.
    """

    SOON = "soon"
    ACTIVE = "active"
    HIDDEN = "hidden"


class BannerLinkKind(StrEnum):
    """ما تفتحه اللافتة — **والنوعُ معلَنٌ لأن المقاصدَ مختلفةُ الطبيعة**.

    `internal` مسارٌ في التطبيق يُتحقَّق من بنائه · `external` رابطٌ يخرج به
    من التطبيق · `none` لافتةُ خبرٍ لا تُنقر.
    """

    INTERNAL = "internal"
    EXTERNAL = "external"
    NONE = "none"


class CampaignAudience(StrEnum):
    """جمهور الحملة الإدارية/التسويقية (SPEC القسم 13 — المرحلة 8).

    `by_country` يعني «كل مستخدمي الدولة» ركاباً وكباتن؛ وعمود `country_code`
    يبقى مستقلاً عن هذا الحقل لأنه **مُضيِّق** لأي جمهور: «كل الكباتن في
    الأردن» جمهورٌ وتضييق، لا قيمةٌ ثالثة في الأنواع.

    `segment` محجوزة لشرائح المرحلة 12 (الكوبونات والعروض) — تُرفض اليوم
    برسالة صريحة بدل أن يُخترع لها معنى.
    """

    ALL_RIDERS = "all_riders"
    ALL_DRIVERS = "all_drivers"
    BY_COUNTRY = "by_country"
    SEGMENT = "segment"


class CampaignStatus(StrEnum):
    """`draft → scheduled → sent`، و`cancelled` مخرجٌ من الأوليين.

    لا حالة «قيد الإرسال»: الحملة تُرسل في دورةِ مهمةٍ واحدة على دفعات، وصفُّها
    مقفولٌ طوالها — فمهمتان متزامنتان لا ترسلان حملةً مرتين. وسجلُّ
    `notification_deliveries` هو ما يقول من وصله الإشعار فعلاً.
    """

    DRAFT = "draft"
    SCHEDULED = "scheduled"
    SENT = "sent"
    CANCELLED = "cancelled"


class DeliveryStatus(StrEnum):
    """نتيجة إرسال حملةٍ إلى مستخدم بعينه.

    `skipped` ليست فشلاً: أطفأ التسويق، أو لا جهاز مسجّل له — والفرق بينها
    وبين `failed` هو الفرق بين «لم نرسل عمداً» و«أرسلنا فلم يصل».
    """

    SENT = "sent"
    FAILED = "failed"
    SKIPPED = "skipped"


class AuditAction(StrEnum):
    CREATE = "create"
    UPDATE = "update"
    DELETE = "delete"
    ACTIVATE = "activate"
    DEACTIVATE = "deactivate"
    # قراءةٌ تُسجَّل — نادرةٌ عمداً: تُكتب حيث تكون القراءةُ نفسها فعلاً يُسأل
    # عنه، كفتح خريطة مواقع الناس بهوياتهم (القسم 13/1)
    READ = "read"


class VerificationCampaignStatus(StrEnum):
    """حالُ حملةِ تأكيد الأرقام (قرارُ المالك 2026-08-31).

    **خمسٌ لا ثلاث، والفرقُ بين `paused` و`cancelled` هو المهمّ**: الأولى
    **آليّةٌ تعود** حين تعود قناةُ السوق، والثانيةُ **قرارُ إنسانٍ لا يعود**.
    وجعلُهما واحدةً يجعل انقطاعَ قناةٍ ساعتين يُنهي حملةً أُطلقت.

    **و`draft` تُنشأ ولا تُطلق** — والإطلاقُ ضغطةُ المالك وحدَه.
    """

    DRAFT = "draft"
    RUNNING = "running"
    #: **تجمّدٌ آليٌّ بلا مشرف** — حين تسقط قناةُ التحقّق في السوق
    PAUSED = "paused"
    DONE = "done"
    CANCELLED = "cancelled"


class VerificationMethod(StrEnum):
    """أيُّ مُحقِّقٍ فعّالٌ في هذا السوق — تنشره `/config` وترسم به الواجهةُ تدفّقَها.

    **وليس عموداً في القاعدة**: لا جدولَ يخزّن هذه القيمة، **وتُشتقّ من
    المفاتيح** في `services/verification.available_methods`.

    **فلمَ تعدادٌ إذاً؟** لأنّ الأربعةَ **عقدٌ منشورٌ يقرؤه تطبيقان**،
    و`check:enums` **لا يقرأ إلا هذا الملفّ**. فبقاؤها ثوابتَ وحدات جعل
    الحارسَ يقرأ **مرآةً صادقةً** على أنها قيمٌ مخترعة (قِيس 2026-08-30)،
    **وحارسٌ يصيح على سليمٍ يُطفأ فيسقط معه ما يمسكه حقاً**.

    **والقيمُ هي هي حرفاً**: `verification.py` يشتقّ ثوابتَه من هنا، فلا
    مقارنةَ تتبدّل ولا سلكَ يتغيّر — **والتعدادُ يجعلها مرئيّةً لا يبدّلها**.
    """

    FIREBASE = "firebase"
    SMS_OTP = "sms_otp"
    WHATSAPP_OTP = "whatsapp_otp"
    # **البريدُ يُثبت البريدَ لا الهاتف** (قرارُ المالك 2026-08-31) — وهو
    # **ليس عضواً في سلسلة إثبات الرقم** ولا يُعاد إليه `available_methods`.
    #
    # **ولمَ عضوٌ في التعداد إذاً**: هو قناةُ رمزٍ حقيقيةٌ نولّد فيها ونرسل،
    # ويقرؤها التطبيقان في `/config` ليرسما بابَ «سجّل ببريدك». **وحصرُه في
    # ثوابتِ وحدةٍ يجعل `check:enums` يقرأ مرآةً صادقةً قيماً مخترعة** — وهو
    # العطبُ الذي قِيس في 2026-08-30.
    EMAIL_OTP = "email_otp"
    NONE = "none"


class PolicyDocType(StrEnum):
    """أيُّ وثيقةٍ قانونية — **وثيقتان مستقلّتان لا واحدة** (§34، الترحيلة `0068`).

    **العلّةُ بنصِّ المالك**: «المستخدم يوافق على زرّين لا زرّ واحد». ولكلٍّ
    نسخُها وموافقتُها ورقمُها المتصاعد.
    """

    PRIVACY_POLICY = "privacy_policy"
    TERMS_OF_USE = "terms_of_use"


class PolicyApp(StrEnum):
    """أيُّ تطبيقٍ تخصّه الوثيقة — **لكلِّ تطبيقٍ نصُّه** (§34، `0068`).

    **فما يخصّ الوثائقَ والمركبةَ لا يُعرض على راكب**: نصُّ الكبتن يذكر رفعَ
    رخصةٍ وبياناتِ مركبةٍ وحسابَ صرف، **ونصٌّ واحدٌ للاثنين إمّا يَعِد الراكبَ
    بما لا يقع أو يسكت عمّا يلزم الكبتن**.

    **ولا عضوَ للمشرف** (رأيٌ عرضتُه وأقرّه المالك 2026-09-02): اللوحةُ علاقةُ
    عملٍ لا استهلاك، **ولا شاشةَ قبولٍ فيها** — وقيمةٌ في التعداد بلا سطحٍ
    ينشرها «بابٌ بلا زرّ» في القاعدة. **وإضافتُها يومَ تلزم سطرٌ واحد**:
    `ALTER TYPE … ADD VALUE`، وهو نمطٌ مكتوبٌ في هذا المستودع.
    """

    RIDER = "rider"
    DRIVER = "driver"


class AdminPermission(StrEnum):
    """صلاحياتُ المشرفين — **مصفوفةٌ لا مستوياتٌ مبهمة** (البند ٥، §39٫٥).

    **والاثنتا عشرةَ مشتقّةٌ من ملفات الموجّهات نفسِها** لا مخترعةً: كلُّ واحدةٍ
    تقابل مجموعةَ أبوابٍ قائمة، **فلا صلاحيةَ بلا بابٍ ولا بابَ بلا صلاحية**.

    **و«مستوى ٢» لا يقول ماذا يملك**: من يقرأ «مشرفٌ من الدرجة الثانية» بعد
    شهرٍ لا يعرف أيفتح صفحةَ العقود أم لا — **والمصفوفةُ تقول بالاسم**.
    """

    #: التسعيرةُ والخطط والمفاتيح وحدودُ المحفظة — كلُّ ما في «الإعدادات»
    SETTINGS_WRITE = "settings.write"
    #: الحسابات والكباتن: الاعتمادُ والإيقافُ وتوثيقُ الجنس والوثائق
    USERS_MANAGE = "users.manage"
    #: المالُ الذي يتحرّك: الشحنُ والصرفُ والتسوياتُ والاشتراكاتُ ورسومُ الإلغاء
    FINANCE_MANAGE = "finance.manage"
    #: النموُّ: الحملاتُ والعروضُ والكوبوناتُ والإحالات
    GROWTH_MANAGE = "growth.manage"
    #: الأسطول: المهامُّ والمستوياتُ والشاراتُ ومركباتُ المتجر وبلاغاتُ الصور
    FLEET_MANAGE = "fleet.manage"
    #: العقودُ وقوالبُ الرسائل — **صفحةٌ فيها أسرار**
    PROVIDERS_MANAGE = "providers.manage"
    #: النسخُ الاحتياطية وجدولتُها
    BACKUPS_MANAGE = "backups.manage"
    #: فصلُ النزاعات — **وهو ما يملكه `support` اليومَ حرفاً**
    PAYMENTS_RESOLVE = "payments.resolve"
    #: قراءةُ القوائم والتقارير — **ولا كتابةَ معها**
    READ_ONLY = "read.only"
    #: سياسةُ الدخول والعاملُ الثاني
    SECURITY_MANAGE = "security.manage"
    #: **منحُ الصلاحيات ونزعُها** — ومن يملكها يملك أن يمنح نفسَه ما شاء،
    #: **ولذلك تُحرس بثلاثة شروطٍ في الخلفية** (§39٫٥)
    PERMISSIONS_MANAGE = "permissions.manage"
    #: **شاشةُ الأعطال** — قراءةُ التقارير وحسمُها (2026-09-20).
    #:
    #: **وتحكم القراءةَ والحسمَ معاً** رغم اسمها: «حُسم» و«كُتم» **إقراران لا
    #: فعلان على مالٍ ولا على حساب**، وصلاحيةٌ ثانيةٌ لزرّين تصير صلاحيةً
    #: بزرّ — وهو تفصيلٌ أدقُّ ممّا تحتمله مصفوفةٌ تُقرأ بعد شهر.
    ERRORS_READ = "errors.read"


# ----------------------------------------- تقاريرُ الأعطال (2026-09-20، تقاريرُ الأعطال)
#
# **ثلاثةُ تعداداتٍ لا واحد**: «من أين جاء» و«ما حالُه عند المشرف» و«على أيِّ
# منصّة» أسئلةٌ ثلاثةٌ تُفلتَر بها الشاشةُ استقلالاً — وعمودٌ واحدٌ يجمعها
# يجعل «مفتوحٌ من حدِّ خطأٍ على أندرويد» قيمةً واحدةً لا تُقسَم.


class ErrorKind(StrEnum):
    """البابُ الذي دخل منه التقرير — **وأربعةٌ تفشل أربعَ فشلاتٍ مختلفة**.

    `ERROR` استثناءٌ عامٌّ التقطه `window.onerror`؛ و`REJECTION` وعدٌ مرفوضٌ
    بلا ماسك (`unhandledrejection`) — **وهو الذي لا يُرى في شاشةٍ أصلاً**؛
    و`BOUNDARY` ما أسقط شجرةَ React فاستُبدلت به شاشةُ الحدّ؛ و`USER_REPORT`
    ما ضغط صاحبُه زرَّه بنفسه.

    **والتمييزُ ليس تصنيفاً للأرشيف**: تقريرٌ من `USER_REPORT` يحمل جملةَ
    إنسانٍ ويستحقّ قراءةً، و`REJECTION` قد لا يكون رآه أحدٌ على الشاشة.
    """

    ERROR = "error"
    REJECTION = "rejection"
    BOUNDARY = "boundary"
    USER_REPORT = "user_report"


class ErrorStatus(StrEnum):
    """حالُ المجموعة عند المشرف — **ولا تُحذف مجموعةٌ بضغطة**.

    `IGNORED` ليست `RESOLVED`: الأولى «أعرفه ولا أريد أن أراه»، والثانية
    «أُصلح». **وخلطُهما يجعل عطباً مكتوماً يُقرأ مُصلَحاً بعد شهر.**
    """

    OPEN = "open"
    RESOLVED = "resolved"
    IGNORED = "ignored"


class ErrorPlatform(StrEnum):
    """منصّةُ الجهاز الذي وقع عليه العطب.

    **وليست `DevicePlatform`** رغم تشابه العضوين: تلك تُقرأ عند إرسال إشعار
    فتحكم حقلَ الأولوية في FCM، **ولا `web` فيها** لأن المتصفّح لا يُرسَل
    إليه بها. وإضافةُ عضوٍ إليها لأجل شاشةِ أعطالٍ **تغيّر معنى عمودٍ يقرؤه
    الإرسال** — وهي «شكلان لشيءٍ واحد» مقلوبةً: شيئان في شكلٍ واحد.
    """

    ANDROID = "android"
    IOS = "ios"
    WEB = "web"


class ErrorSort(StrEnum):
    """ترتيبُ قائمة الأعطال — **والافتراضُ «المتأثّرون» لا «المرّات»**.

    **وهذا هو الفرقُ الذي يجعل الشاشةَ تُقرأ**: حلقةُ رسمٍ في هاتفٍ واحدٍ
    تُنتج آلافَ الأحداث، فالترتيبُ بالمرّات يضعها فوق عطبٍ يمسّ نصفَ
    الأسطول. **والسؤالُ الذي يُبنى عليه القرار هو «كم إنساناً»**.
    """

    USERS = "users"
    LAST_SEEN = "last_seen"
    EVENTS = "events"
