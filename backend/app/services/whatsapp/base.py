"""واجهة مُحقِّق واتساب — العقد الذي يتكلمه كل ما عدا المزود نفسه.

نفس نهج `services/sms/` و`services/card_gateway/`: `services/otp.py` وطبقةُ
التحقق لا تعرفان أيَّ قناةٍ أوصلت الرمز، وتبديلُ القناة **إدخالُ عقدٍ من صفحة
العقود وإشعالُ مفتاحٍ لا تعديلُ كود** (SPEC القسم 15/أ).

**والفرقُ الجوهري عن `SmsProvider`: `send_code` لا `send`.** ليس تجميلاً في
الاسم — قوالبُ المصادقة في واتساب لا تقبل نصّاً حرّاً أصلاً: القالبُ معتمدٌ
مسبقاً عند ميتا، وما نرسله **مُعامِلٌ واحد هو الرمز**. فواجهةٌ تأخذ `body`
نصّياً تكذب على مستدعيها: ستقبل جملةً وتُلقيها.

ولذلك صار مسارُ الرمز في `services/otp.py` يطلب `OtpSender` — من يعرف **كيف
يُصاغ** الرمزُ في قناته — بدل أن يصوغ هو النصَّ ويسلّمه لمن لا يقبله.
"""

from __future__ import annotations

from typing import Protocol

from app.core.exceptions import AppError

# مهلة نداء ميتا. أقصر من صبر المستخدم على شاشة «أرسل الرمز»، وأطول من زمن
# ردٍّ طبيعيٍّ من Graph API بمرّتين
REQUEST_TIMEOUT_SECONDS = 15.0


class WhatsAppUnavailable(AppError):
    """لا عقد واتساب مُدخل أو مفعّل — أو عقدٌ ناقص الحقول.

    503 لا 501: الميزة مبنية والعقد غائب، نفس منطق `SmsUnavailable`.
    """

    status_code = 503
    code = "whatsapp_unavailable"
    message = "تعذّر إرسال الرمز عبر واتساب الآن. جرّب طريقة أخرى."


class WhatsAppError(AppError):
    """ميتا ردّت بخطأ أو تعذّر الوصول إليها.

    **وهذا الخطأ بعينه هو ما يوجب الارتداد**: القناةُ لا تعمل الآن، وصاحبُ
    الرقم لا ذنب له. فمن يستدعي `otp.issue` عبر واتساب يلتقطه ويعرض مخرجاً
    بالقناة التالية (`services/verification.py`) — رفضٌ بلا مخرجٍ ليس رفضاً.
    """

    status_code = 502
    code = "whatsapp_send_failed"
    message = "تعذّر إرسال رمز واتساب"


class WhatsAppOtpProvider(Protocol):
    """ما يحتاجه مسارُ التحقق من قناة واتساب."""

    provider_name: str

    async def send_code(
        self,
        to: str,
        code: str,
        *,
        ttl_minutes: int,
        purpose: str,
        body: str,
    ) -> str:
        """يرسل رمزاً إلى رقم E.164 ويعيد مرجعَه لدى المزود.

        `purpose` و`body` مضافان في قوالب الرمز (2026-08-19). و`body` **مصاغٌ
        سلفاً** في `otp.issue`: القالبُ يُقرأ من قاعدة البيانات، وقارئُه هو من
        يملك الجلسة — والمزودُ لا يملكها. و`purpose` يسافر معه لا لأنه يُستعمل
        في الصياغة، بل ليُسجَّل عند السقوط: «أيُّ قالبٍ خالف» سؤالٌ بلا جواب
        إن وصل النصُّ مجرَّداً من هويّته.
        """
        ...

    async def test_connection(self, test_phone: str | None = None) -> str:
        """يختبر العقد ويعيد وصفاً لما جرى — أو يرفع `WhatsAppError`.

        `test_phone` اختياري كما في مزود الرسائل: بغيابه لا تُرسل رسالةٌ
        مدفوعةٌ إلى رقمٍ لم يطلبها أحد، ويُكتفى بما **لا يترك أثراً** — وهو هنا
        أقوى من مجرّد «الخدمة تجيب»: يُتحقق من رقم الأعمال ومن **وجود القالب
        واعتماده**، وهو أكثرُ ما يُخطئ فيه الإعداد.
        """
        ...
