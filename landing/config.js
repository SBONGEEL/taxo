/** **إعدادُ الصفحة — يُبدَّل ولا يُعاد بناؤها** (شرطُ المالك 2026-08-21).
 *
 * **ولماذا ملفٌّ مستقلٌّ لا قيمٌ في `index.html`**: يومَ يصل توقيعُ Google Play
 * **تُبدَّل قيمة** ولا تُلمس صفحة. وقيمةٌ مكتوبةٌ في وسط صفحةٍ تُنسى، ومن
 * ينساها يترك زرَّاً يشير إلى ملفٍّ لم يعد موجوداً.
 */
window.TAXO_LANDING = {
  /** مصدرُ التحميل: `"none"` · `"apk"` · `"play"`.
   *
   * **و`"apk"` اليومَ بقرار المالك 2026-08-21، وهو قرارٌ باطّلاع**: الحزمتان
   * موقَّعتان بمفتاح التطوير، وتشيران إلى `app.tajora.ly`/`driver.tajora.ly`.
   * عُرض ذلك عليه فأبقى الزرّين وأمر بأن يُقال للمحمِّل صراحةً — **فالمستخدم
   * يقرّر وهو يعلم**، لا تُطفأ الميزةُ عنه.
   *
   * و`"none"` تُخفي الزرّين **والعنوانَ وشرحَ التثبيت وتنبيهَ التوقيع معهما**،
   * بلا مكانٍ محجوز؛ والملفّان يبقيان مخدومَين على `./downloads/`. وهي حالُ
   * يومٍ يُقرَّر فيه ذلك — **لا حالٌ تُتَّخذ من تلقاء أحد**. */
  source: "apk",
  /** روابطُ المتجر — تُملأ يومَ النشر، ويتحوّل `source` إليها حينها. */
  play: {
    rider: "",
    driver: "",
  },
  /** الدعم — رقمٌ واحدٌ يُقرأ ويُتصل به وتُفتح به محادثةُ واتساب.
   *
   * **وهو ليبيٌّ اليوم** (`+218`)، والإطلاقُ في الأردن يحتاج رقمَ دعمٍ أردنياً
   * كما يحتاج رقمَ إرسالٍ أردنياً — **بندٌ واحدٌ بوجهين** (SPEC §24.8). ومن
   * يتصل بدعمٍ خارج بلده يدفع مكالمةً دولية، ومن يقرأ رقماً غريباً يشكّ. */
  support: {
    phone: "+218930385734",
    whatsapp: "218930385734",
  },
  /** من أين يُقرأ العرضُ القائم — والصفحةُ لا تكتب شيئاً. */
  api: "https://stg-api.tajora.ly/api/v1",
  country: "JO",
};
