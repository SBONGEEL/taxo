/** «هل المشاركةُ معروضةٌ على صاحب هذه الشاشة؟» — سؤالٌ بجواب واحد.
 *
 * بابٌ واحدٌ كـ`lib/promo.ts` و`lib/multistop.ts` و`lib/women.ts`: ورقةُ تأكيد
 * الطلب هي القارئُ الوحيد اليوم، وشارةُ الرحلة تقرأ **الرحلة** لا المفتاح —
 * فما وقع يبقى معروضاً ولو أُطفئ المفتاحُ بعده (قاعدةُ «الإطفاء يمنع الجديد
 * ولا يفكّ القائم»).
 *
 * **ومطفأً لا يظهر شيءٌ إطلاقاً**: لا مفتاحٌ معطّل ولا سطرُ «غير متاح». وخيارٌ
 * يَعِد بخصمٍ ثم يرتدّ طلبُه بـ`ride_sharing_unavailable` هو بعينه العطبُ الذي
 * أصلحته 10-ج مرة: رفضٌ بلا مخرج.
 *
 * **والسعرُ لا يُشتقّ من هنا**: المفتاحُ يقول «تُعرض»، والرقمُ يأتي من
 * `share_fare` في التقدير — فنسبةٌ في الواجهة تضربها في أجرةٍ حسابُ مال
 * (القسم 14)، وصفرُها في القاعدة يُخفي الميزةَ ولو كان المفتاح مشتعلاً.
 */

import { useFeature } from "@/lib/config";
import { useSession } from "@/lib/session";

export function useRideSharing(): boolean {
  const { user } = useSession();
  return useFeature(user?.country_code, "ride_sharing_enabled");
}
