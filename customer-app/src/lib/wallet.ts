/** مبالغُ الشحن السريعة — بيتٌ واحدٌ للورقة والشاشة معاً.
 *
 * **ثلاثةٌ لا أربعة** (`DESIGN-DECISIONS.md` 20 والتصميم `topupAmounts`): الشكلُ
 * المقرَّر ثلاثةُ أزرارٍ متساوية + حقلُ مبلغٍ حرّ. وكان في الشاشة أربعةٌ
 * (٥/١٠/٢٠/٥٠) مكتوبةً في ملفها وحده.
 *
 * **وقيمُها ثابتةٌ اليوم وقرارُ 20 يريدها مشتقّةً من حدود `wallet_settings`** —
 * وذلك **غيرُ ممكنٍ من الواجهة الآن**: `GET /config` ينشر العملةَ والمفاتيحَ
 * وساعاتِ الهدوء ولا ينشر حدودَ المحفظة. ونشرُها قرارٌ خلفيٌّ مستقل (كسعرِ كل
 * فئة)، لا أثرٌ جانبيٌّ لورقة. فالثوابتُ هنا هي أقربُ ما يُسلَّم بصدق، ومكتوبٌ
 * فوقها ما ينقص كي لا يُقرأ النقصُ سهواً.
 */
export const QUICK_TOPUP_AMOUNTS = ["5", "10", "20"] as const;
