/** رموزُ الحركة كما يقرؤها Framer — مرآةُ `DESIGN.md` §8.
 *
 * **ولماذا نسخةٌ ثانية؟** لأن المستهلكَين طبقتان لا تتشاركان مصدراً: CSS يقرأ
 * `duration-med` و`ease-standard` من `tailwind.config.js`، وFramer يأخذ أرقاماً
 * بالثواني ومصفوفةَ منحنى في JS. ولا سبيلَ لأن يقرأ أحدُهما الآخر بلا حساب في
 * زمن التشغيل.
 *
 * **فالمصدرُ هو §8 وكلاهما مرآةٌ له** — والقيمُ مكتوبةٌ هنا مرةً واحدةً لكلِّ
 * التطبيق، لا في كلِّ مكوّنٍ يتحرك. وهذا هو ما طُلب: «مددٌ ومنحنياتٌ رموزٌ لا
 * أرقامٌ متفرقة».
 */

/** بالثواني — `DESIGN.md` §8 (120ms · 200ms · 320ms). */
export const DURATION = {
  fast: 0.12,
  med: 0.2,
  slow: 0.32,
} as const;

/** منحنيا §8 كمصفوفةِ bezier — `cubic-bezier(0.2, 0, 0, 1)` و`(0, 0, 0, 1)`. */
export const EASE = {
  standard: [0.2, 0, 0, 1],
  outSoft: [0, 0, 0, 1],
} as const;

/** إزاحةُ انتقال الشاشات — صغيرةٌ عمداً: الشاشةُ تُزاح ولا تطير. */
export const SLIDE_PX = 24;

/** المهلةُ بين عنصرٍ وتاليه في القوائم (§8). */
export const STAGGER_STEP = 0.04;
