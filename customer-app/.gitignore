node_modules/
dist/
.vite/
*.local
# ذاكرةُ `tsc -b` بين البناءات — أثرُ تشغيلٍ على جهازٍ بعينه لا مصدر
*.tsbuildinfo

# مخرجاتُ بناء أندرويد (المرحلة 13) — تُبنى ولا تُخزَّن
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/app/src/main/assets/public/
android/app/google-services.json

# حزمُ التصحيح تُنسخ إلى `public/` لتنزيلها على الهاتف عبر النفق — أثرُ
# تشغيلٍ لا مصدر، وتُحذف بعد التثبيت (README: النفق يُغلق بعد التجربة)
public/*.apk
