<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!-- **نافذةُ الإقلاع لونٌ صامتٌ لا صورة** (2026-08-14، بقرار المالك).
         كانت تعرض الشعارَ ثم تعرضه الشاشةُ الترحيبيةُ المتحركة مرةً ثانية —
         شاشتان متتاليتان لشيءٍ واحد، وبينهما قفزةُ لون. فما يبقى هنا هو
         **لونُ الخلفية وحدَه**: يملأ ما بين ضغطةِ الأيقونة وأوّلِ رسمةٍ من
         `index.html`، فلا وميضَ أبيضَ ولا أسود، ثم تبدأ حركةُ TAXO فوقه بلا
         انتقالٍ يُرى. واللونُ يتبع وضعَ النظام عبر `values-night`. -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="android:background">@color/launch_background</item>
        <item name="windowSplashScreenBackground">@color/launch_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_silent</item>
        <item name="windowSplashScreenAnimationDuration">0</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>